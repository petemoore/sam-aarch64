<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/items.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Item registry — open

Work items with status `OPEN` or `IN_PROGRESS`. Items with status `DONE` or
`WONTFIX` live in `item-registry-closed.md`.

## Id conventions

`i<N>` · `i<N><letter>` · `i<N><letter>-b<M>` · `i<N><letter>-b<M><letter>`
(base → sub-item → brick → brick-part). Ids are immutable once used in a PR,
branch, or commit; never reused. Sort order is true-numeric (i5 < i41 < i237).

## Controlled status vocabulary (open file)

| token | meaning |
|---|---|
| `OPEN` | not yet started; or started but no agent has it in-hand |
| `IN_PROGRESS` | actively being worked right now |

"Blocked" is not a status token — it is a *derived* property of the dependency
graph. An item with an unsatisfied `depends_on` edge is still `OPEN`; its gating
item/question ids are listed in the `deps` column (the edges of the dependency DAG).

## Atomic-item rule

One row = one deliverable. If a row bundles independently completable parts, split
it into letter sub-ids (`i81a`/`i81b`…) so no finished part hides inside an
unfinished row.

## Dependency model

`depends_on` edges declare what must finish before this item can proceed, and are
shown in the `deps` column. A target may be an item (must reach `DONE`) or a
question (must be answered and deleted). Edges form a DAG; the validator rejects
cycles and dangling targets. The DAG is built from the `depends_on` fields in
`registry/items.yaml`; `grep depends_on` there for the machine-readable edge list.
The `dependents` column shows the reverse edges (items that depend on this one).

## Column layout

The generated table has 7 columns: `id | item | status | PR | deps | dependents |
refs/links`. `PR`, `deps`, `dependents`, and `refs/links` are each independent
first-class columns. The generated `.md` files are views of `registry/items.yaml`
— do not edit them by hand (hand edits fail CI).

<!-- The table below is generated — do not edit it by hand. -->


| **id** | item | status | owner | gate | PR | deps | dependents | refs/links |
|---|---|---|---|---|---|---|---|---|
| <a id="i3"></a>**i3** | **Editor groundwork (Phase 2) — full vision** — Phase-2 editor full vision umbrella. The editor groundwork encompasses all Phase-2 editor sub-features (see i43–i47 and sub-items). Full scope in ROADMAP. | OPEN | agent | ready |  |  | [i101](#i101), [i168](#i168), [i77](#i77), [i90](#i90) | docs/ROADMAP.md |
| <a id="i31"></a>**i31** | **On-SAM preprocessor (.if build-constraints + macros)** — On-SAM preprocessor (.if build-constraints + macros + .include), reducing reliance on the host-side preprocess step. Direction approved (q38 GO, 2026-06-21, on the i209 assessment); unblocked by the b8 umbrella close (2026-07-03). Design spec: docs/specs/on-sam-preprocessor-design.md (i31a); implementation (i31b) is spec-gated on q70. | OPEN | agent | ready |  | i48c-b8a, i48c-b8b, i48c-b8c, i48c-b8d, i48c-b8e, i48c-b8f |  |  |
| <a id="i31b"></a>**i31b** | **On-SAM preprocessor — implementation (Z80 port of preprocess.go per the approved spec)** — Implement the on-SAM preprocessor per docs/specs/on-sam-preprocessor-design.md (spec APPROVED by Pete 2026-07-04, q70 closed): Go-authority-first bricks (1: .set table + .if/.else/.endif stack; 2: .macro/.endm + \param substitution + invocation with recursion guard; 3: .include + # line emission via a pluggable reader vector, real reader = UIFA+HGTHD+trampoline-HLOAD; 4: prep_run wired in front of b8d_chain_paged + corpus gate). Oracle gates: byte-equal vs host sam-aarch64 -E on ported preprocess_test.go fixtures + new preprocessor-bearing corpus fixtures (spectrum4 slice), then end-to-end text->.tbn vs CompactTBNBytes. Buffers from the i2 pool (new PP_PREP tag); own paged image (b8d two-image pattern). The expanded-text LEX_SRC ceiling lift is explicitly NOT this item (spec par.3). | OPEN | agent | ready |  |  |  |  |
| <a id="i31b-b4"></a>**i31b-b4** | **Brick 4: prep_run wiring in front of b8d chain + corpus gate** — Wire prep_run in front of b8d_chain_paged (own paged image, b8d two-image pattern; PP_PREP buffer tag in the i2 pool). New preprocessor-bearing corpus fixtures incl. a spectrum4 slice (tests/test_print_w0.s), gated: expanded text byte-equal to host -E, then end-to-end text->.tbn byte-equal to host CompactTBNBytes. SimCoupe pre-merge gate incl. real-HLOAD include path. PREREQUISITE from b3a scoping (i344-encoded): cross-file macro def-FILE provenance — a macroDef's expansion directive uses defPos.FILE, which b3a stores as the invocation-site CURRENT_FILE (correct only when a macro is defined and invoked in the SAME file). The corpus defines macros in shared includes and invokes them elsewhere, so b4 must add a def-file field to the MACRO_TAB record (interned in PATH_ARENA at define time) and emit it in tryExpandMacroInvocation's pre-directive. Also right-size the flat-harness pools (shrunk in b3a to fit the 32 KB org-&8000 window) appropriately for the paged image. Depends on i31b-b3. | OPEN | agent | ready |  | i31b-b3, i370 |  |  |
| <a id="i31b-b4c"></a>**i31b-b4c** | **Brick 4c: PP_PREP i2-pool tag + right-size paged prep buffers + spectrum4 corpus slice + corpus gate** — Scale the b4b wiring to the real corpus. Add PP_PREP owner tag (=9) to src/pagepool.asm + the matching Go Owner const. Right-size the paged prep buffers (PREP_OUT/PREP_SRC/PREP_FILES, currently oracle-fixture-sized 8K/4K/4K tiling page 8) via pool-allocated pages so real spectrum4 inputs fit; also right-size the flat-harness pools shrunk in b3a to fit the org-&8000 32KB window. Author tests/test_print_w0.s — a spectrum4 slice (the smallest real macro consumer, spec §7). Extend the b4b end-to-end harness gate to the preprocessor-bearing/spectrum4 fixtures: expanded text byte-equal to host -E, then text→.tbn byte-equal to host CompactTBNBytes. Depends on b4b. | OPEN | agent | ready |  | i31b-b4b | [i31b-b4d](#i31b-b4d) |  |
| <a id="i31b-b4d"></a>**i31b-b4d** | **Brick 4d: SimCoupé pre-merge gate incl. real-HLOAD include path** — The spec §7 SimCoupé pre-merge gate: run the wired prep→b8d chain under SimCoupé with real B-DOS, exercising the real-HLOAD .include path (src/prep_sam_reader.asm — UIFA+HGTHD+trampoline-HLOAD, the prep_sam_reader vector) rather than the memory reader. Prove the wired chain produces the correct .tbn for a preprocessor-bearing source whose .include is satisfied by a real on-disk file. Add the SimCoupé gate script + build-disk payload/loader wiring as needed. Depends on b4c (the pooled buffers the real reader needs) and b3b (prep_sam_reader, DONE). | OPEN | agent | ⏳ i31b-b4c |  | [i31b-b4c](#i31b-b4c) |  |  |
| <a id="i32"></a>**i32** | **Multiple source files + staged/partial loading** — Beyond-M7 / future idea. Support for assembling from multiple source files with staged or partial loading, enabling larger projects than currently fit in a single-file workflow. | OPEN | agent | 🔒 q46 |  | i2a, q46 |  |  |
| <a id="i41"></a>**i41** | **Editor edit-buffer data structure — insertion performance for large source** — Architectural decision: the editor holds source in a separate insertion-friendly in-memory document model (paged block-list: block = page, intra-block gap buffer, record-id side-tables; worst-case edit ~30–80 ms at 400 KB) and serializes to .tbn on save/assemble. Unifies the editor document model with i48's IR (edit-model records = in-memory symbolic IR). Design agreed (opus, 2026-06-08), 5 key decisions in docs/specs/editor-edit-model-design.md §7. Decomposed into i41a–i41e. | OPEN | agent | ready |  |  | [i43](#i43), [i44](#i44), [i45](#i45), [i46](#i46), [i47](#i47), [i65](#i65) | docs/specs/editor-edit-model-design.md §7, docs/notes/m9-status.md, i41a, i41b, i41c, i41d, [i41e](#i41e) |
| <a id="i41e"></a>**i41e** | **Editor edit-model — i48-IR record payload + document symbol table + hybrid record model** — Replace i41a's raw-text line payload with the i48 in-memory symbolic IR (parsed format.Record): a document-global symbol table + name-id/record-id side-tables (§3), a hybrid committed-parsed / active-raw / invalid-error record type (§7.3), per-line text→IR entry. q24 resolved (2026-06-20): the editor validates ONE LINE AT A TIME — no whole-document import to serialize invalidly; the design point is how an in-progress/invalid LINE is held + round-tripped (whole-file import = i165). Open design (symbol-table lifecycle, hybrid sum type, per-line parse) — needs a design pass (§8/§5.2/§6/§7.3). | OPEN | agent | 🔒 q51 |  | i2a, q51 |  | docs/specs/editor-edit-model-design.md §8/§5.2/§6/§7.3, docs/specs/i48-syntactic-encoder-design.md, i41a, i41c, i48c |
| <a id="i43"></a>**i43** | **Editor: register simulator with user-chosen seeds + replay-on-edit** — Phase-2 editor sub-feature. Register simulator with user-chosen seeds: step instructions, inject/randomise source regs, show dest regs/flags/memory, and replay-on-edit. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | docs/ROADMAP.md |
| <a id="i44"></a>**i44** | **Editor: inline instruction-explanation panel (prose semantics, flags, regs, bit-field layout)** — Phase-2 editor sub-feature. Inline instruction-explanation panel showing prose semantics, flags set, regs read/written, and bit-field layout. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | docs/ROADMAP.md |
| <a id="i45"></a>**i45** | **Editor: 'did you mean a simpler instruction?' result-equivalent rewrite hint** — Phase-2 editor sub-feature. Suggests result-equivalent but simpler instruction rewrites when a more concise encoding exists. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | docs/ROADMAP.md |
| <a id="i46"></a>**i46** | **Editor: system-register documentation panel (ARM MRA subset + editorial overlay)** — Phase-2 editor sub-feature. System-register documentation panel surfaced from the cursor line, embedding an ARM MRA subset with editorial overlay. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | docs/ROADMAP.md |
| <a id="i47"></a>**i47** | **Editor: inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations)** — Phase-2 editor sub-features. 'Why is this instruction here?' inverse-flow navigation plus retro UI affordances including chiptune audio, period fonts, and animations. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | docs/ROADMAP.md |
| <a id="i50"></a>**i50** | **Host Go-tool CLI shape — deferred design question (one binary vs three vs pandoc-style)** — DECIDED (Pete, 2026-06-25): adopt the pandoc-style --from/--to CLI for the host Go tools (e.g. 'sam-aarch64 --from=asm --to=tbn foo.s'; --from=tbn --to=asm; future --from=gnu --to=tbn). Supersedes the earlier deferral (one front door vs three vs pandoc). Implementation is a thin presentation front-end: the three concerns are ALREADY cleanly separated as libraries (aarch64enc / aarch64dec / sam-aarch64-format+frontend), so this is CLI wiring over existing libs, not new functionality.<br><br>Scope notes (from a partial 2026-06-25 survey): the current integrated 'sam-aarch64' (tools/sam-aarch64/main.go) modes map onto from/to — assemble src->bin (--from=asm --to=bin/img), assemble .tbn->bin (--from=tbn --to=bin), --render .tbn->text (--from=tbn --to=asm), -E preprocess. Settle the format taxonomy (is 'gnu' distinct from 'asm'? our source IS gnu-syntax; --from=gnu may just alias, OR imply the future bulk GNU->.tbn import which does NOT exist yet — do NOT build that here unless scoped). Keep the existing CI/Makefile/run-*.sh invocations working (they use the current flags — see tools/run-disasm-roundtrip.sh, run-release-gate.sh, Makefile release targets). LOW priority: per memory project_go_tools_are_z80_scaffolding, the Go tools are Z80-port scaffolding + oracle — do not gold-plate; this is a presentation reshape, not a value-add. | OPEN | pete | 👤 pete |  |  |  |  |
| <a id="i65"></a>**i65** | **SAM-side save-without-comments option (editor phase)** — On-SAM counterpart to the host -strip-comments/-thin-comments flags. Saves a .tbn with the editor region stripped/thinned so a 256 KB SAM can assemble sources authored on a 512 KB machine. q9 resolved (Pete 2026-06-10): the tool targets both 256 and 512 KB SAMs; the full unstripped release .tbn is allowed to exceed 256 KB, but a 256 KB user must still be able to assemble via this stripped disk handoff. Host equivalent already exists. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | i59 |
| <a id="i70"></a>**i70** | **Phase-3 firmware self-provisioning: SAM fetches Pi firmware blobs via HTTP and persists to Trinity storage** — SAM IP stack fetches Pi firmware (several MB) from cdn.githubraw.com over HTTP (q11 resolved: on-SAM HTTP via uIP, host-tool-free; q12 resolved: Model A only). Full host-verifiable HTTP-fetch path complete + host-verified across Bricks 1–6 (#245/#247/#254/#256/#257/#259 netboot_http.asm). Remaining: hardware-gated (EEPROM read + section-C paging + HSAVE RST-8 dispatch on real Trinity; emulation-verified != hardware-verified, CLAUDE.md §5). i82 TFTP client bridges provisioning meanwhile; HTTPS stretch is i88. | OPEN | agent | ready |  | i133, i93b |  | docs/specs/phase3-delivery-design.md, docs/notes/trinity-capabilities.md, docs/notes/bdos-version-landscape.md, src/netboot/netboot_http.asm, docs/notes/netboot-trinity-testing.md, i61, i66, i84, [i88](#i88) |
| <a id="i70c"></a>**i70c** | **Directory-safe multi-blob persist + full 6-file cdn manifest on hardware (start.elf-scale spanning)** — The full i70 payoff, blocked on a persist design reconciliation: at FW_RECORD_CAP=6144 (http_main.asm:333) start.elf alone becomes 486 HSAVE files in ONE B-DOS record directory (~80-slot limit) — the fw_span record-spanning design (src/netboot/fw_span.asm, phase3-delivery-design.md) must be reconciled with directory capacity (span across multiple first-FREE records or raise the per-file window) before any multi-MB persist. Also assess the i347 IM2 exposure for the 10-18+ min full fetch (Z80 SHA-256 in the loop dominates; measured bands in trinity-capabilities.md), and decide the cdn reachability story (LAN mirror of the pinned a43df3a firmware blobs vs resolved cdn IP; blobs verified live 2026-07-03, SHA-256-pinned so an untrusted proxy is fine). | OPEN | agent | ready |  | i70b, i360 |  |  |
| <a id="i70c-b2"></a>**i70c-b2** | **Directory-safe spanning impl + full 6-file cdn manifest hardware shot** — Implement Option B (docs/specs/firmware-persist-spanning.md §4): Go authority placement layer (index -> record = base + i/80, host-verified like span_test.go); Z80 storage_sink_leaf record-select base + FW_REC_IDX/FILES_PER_RECORD with format-per-record; store_end claim EVERY spanned record (extends i360b single-record claim); serve-side per-index record mapping (manifest already supports it, manifest.go:294-315); FW_RECORD_CAP stays 6144. Then the 10-18+ min multi-MB hardware shot with the pinned 6-file manifest. Gated on q74 (design sign-off + cdn reachability). | OPEN | agent | 🔒 q74 |  | q74 |  |  |
| <a id="i77"></a>**i77** | **spectrum4 source succinctification — comments + pathological constant expressions** — Agent-driven, section-by-section, human-reviewed (Pete 2026-06-12): (a) comments rewritten aggressively succinct but still readable; (b) pathological constant expressions replaced with named equates (e.g. the longest instruction in the corpus: `movk w0, (65536 \| (((BORDER_COLOUR >> 16) & 65535) & 65535)), lsl #16`). Coordinate with upstream spectrum4 (content originates there; release.s re-flattens). Ties to editor-tui spec D1 (all comments <256B → u16 record length can revert to u8). Parked (editor era). | OPEN | agent | ⏳ i3 |  | [i3](#i3) |  | docs/specs/editor-tui-prototype-design.md, i76 |
| <a id="i81"></a>**i81** | **Upstream SimCoupé issue triage — review open issues and assist where we can** — Umbrella for SimCoupé upstream community contributions. Triage complete (2026-06-13): produced contributions i81a (PNG-endian fix PR — BLOCKED:Pete), i81b (issue #28 comment — DONE PR #370), i81c (SDL headless fallback — BLOCKED:Pete). i81a and i81c remain open; i81b is done. | OPEN | agent | ready |  |  |  | tools/font-proof/simcoupe-local-capture.patch, i80 |
| <a id="i81a"></a>**i81a** | **PNG-endian one-line upstream SimCoupé PR: the __aarch64__ little-endian-guard fix in Base/SimCoupe.h** — hunk 2 of tools/font-proof/simcoupe-local-capture.patch; confirmed NOT in upstream HEAD; ready to offer standalone; community submission is Pete's call. | OPEN | pete | 👤 pete |  |  |  | tools/font-proof/simcoupe-local-capture.patch, [i81](#i81), i80 |
| <a id="i81c"></a>**i81c** | **Optionally offer the SDL dummy-driver headless renderer fallback upstream** — Lower priority than i81a; community submission is Pete's call. | OPEN | pete | 👤 pete |  |  |  | [i81](#i81), tools/font-proof/simcoupe-local-capture.patch |
| <a id="i88"></a>**i88** | **On-SAM HTTPS/TLS-1.3 client to fetch Pi firmware directly from GitHub** — Phase-3 stretch, LOWEST priority (Pete: only when nothing else is left). TLS-crypto-only: github.com-only, pinned public key, no CA store (content is SHA-256-pinned so cert verification is skipped). Decomposed into i88a (6a host-verifiable state machine), i88b (6b record-reassembler), and i88c (6b hardware-gated integration). | OPEN | agent | ready |  |  |  | docs/specs/phase3-delivery-design.md, src/netboot/tls_client.asm, tools/netboot-oracle/tls/, docs/specs/tls13-handshake.md, [i70](#i70), i84, [i100](#i100) |
| <a id="i88c"></a>**i88c** | **i88 6b — hardware-gated integration: wire tls_reasm into tcp_conn, paging layout, bootable disk, real RST 8 on Trinity** — Wire tls_reasm_feed into tcp_conn CONN_SINK_FILTER; finalise paging layout; build a bootable disk; exercise over real RST 8 on Trinity hardware. Prerequisite: TC_RX must grow from 1056 to 16645 B (TLS max) before a real Certificate record fits. Hardware-gated (CLAUDE.md §5). | OPEN | agent | ready |  | i133, i93b |  | src/netboot/tls_reasm.asm, src/netboot/tls_client.asm, src/netboot/tcp_conn.asm, [i88](#i88), i88b |
| <a id="i88c-b2"></a>**i88c-b2** | **i88c-b2 — grow TC_RX 1056->16645 + finalise paged buffer layout for full-size TLS records** — Grow TC_RX to REASM_MAX (16645) so a real Certificate record fits, and finalise the paged layout: measured, REASM_BUF(16645)+TC_RX(16645)+crypto code cannot coexist in a flat build (tls_client composite already 30629 B at &8000), so a multi-page arrangement is required. No Go authority (a Z80 paging constraint) -> design decision q72. | OPEN | agent | 🔒 q72 |  | i88c-b1, q72 | [i88c-b3](#i88c-b3) | src/netboot/tls_client.asm, src/netboot/tls_reasm.asm |
| <a id="i88c-b3"></a>**i88c-b3** | **i88c-b3 — bootable TLS disk: tls_main.asm as NETBOOT_STREAM boot binary + make target + build-disk wiring** — Build the bootable TLS artifact: tls_main.asm assembled with NETBOOT_STREAM, org &8000 (jp entry), a make netboot-tls-boot/-disk target and build-disk wiring, mirroring netboot-http-boot/-disk. depends_on the finalised paged layout (b2). | OPEN | agent | 🔒 q72 |  | [i88c-b2](#i88c-b2) | [i88c-b4](#i88c-b4) | src/netboot/tls_main.asm, Makefile |
| <a id="i88c-b4"></a>**i88c-b4** | **i88c-b4 — real RST-8 TLS shot on Trinity hardware (hardware-gated)** — Exercise the composed TLS build over real RST 8 on the Trinity SAM. Gated on the b3 bootable disk + q72 (hardware-leg scope: live-internet TLS vs captured replay; what it fetches/stores). If the q72 answer makes the shot store via HSAVE, additionally gate on i357 (the free-record store-leg wedge, itself q71-gated) — add that edge at decision time. Emulation-first: no shot until the software legs are emulation-green and Pete signs off q72. | OPEN | agent | 🔒 q72 |  | [i88c-b3](#i88c-b3), q72 |  | src/netboot/tls_main.asm, docs/notes/netboot-trinity-testing.md |
| <a id="i89"></a>**i89** | **Capture a Pi 3 netboot exchange (Phase-3) — extend i83/i86 oracle to the older Pi family** — Confirm Pi 3 boot ROM's DHCP behaviour (may differ from Pi 4 PXE option-43 dance) and its TFTP file set (bootcode.bin/start.elf/fixup.dat). The oracle extension proceeds from the already-known Pi 3 file set (from the tftproot), so a fresh capture is not a blocker; an optional real-hardware confirmation (hub + Wireshark / trinload technique) can be run when convenient. | OPEN | agent | ready |  |  |  | docs/notes/pi-netboot-capture-analysis.md §4, i83, i86, i84 |
| <a id="i89b"></a>**i89b** | **Capture a real Pi 3 netboot exchange + generate Pi 3 golden wire vectors** — Hardware-gated confirmation tail of i89. On Pete's Pi 3 + SAM with the hub+Wireshark rig, capture the DHCP DISCOVER/OFFER/ACK + first TFTP probe sequence and generate masked Pi 3 golden vectors (as the Pi 4 vectors were). Decisive open detail: whether the Pi 3 boot ROM is byte-strict about the option-43 'Raspberry Pi Boot' menu string (trailing spaces). Findings to confirm are in pi-netboot-capture-analysis.md section 4. | OPEN | pete | ⏳ i345 |  | [i345](#i345) |  |  |
| <a id="i90"></a>**i90** | **%nobits/BSS section bodies dropped by -flatten — source-structure loss** — When source is flattened (-flatten, the release path), the entire body of every %nobits/BSS section (bss_kernel/bss_roms/…) is discarded: contributes zero bytes, so blank lines/comments inside BSS don't round-trip (release.s: 132 of 1472 blank lines, all in NOBITS sections). Pre-existing flatten property, not an i78 bug — i78 blank/comment preservation is exact for PROGBITS. Only matters once the SAM editor edits a flattened source with BSS content. Captured only — decide whether to preserve NOBITS editor content if/when the flattened-edit path needs it. | OPEN | agent | ⏳ i3 |  | [i3](#i3) |  | docs/specs/source-structure-preservation-design.md, tools/sam-aarch64/frontend/flatten.go, i78 |
| <a id="i95"></a>**i95** | **Phase-3 netboot integrated server — single dispatch loop (ARP+DHCP+TFTP)** — Headline Phase-3 program: SAM+Trinity netboots a Pi by answering ARP+DHCP+TFTP in one loop, serving boot files by name from the flat store. Go authority + Z80 netboot_serve_once LANDED + host-verified: full DISCOVER→OFFER→REQUEST→ACK→ARP→RRQ→OACK→DATA session vs Go byte-for-byte. Bootable disk shipped. Hardware first-light (2026-06-18): ARP+TFTP sub-responders real-Trinity-verified (i96). Remaining: DHCP server + B-DOS store provisioning (i93) + Pi-boot gated on real Trinity. | OPEN | agent | ready |  |  |  | tools/netboot-oracle/server/server.go, src/netboot/netboot_server.asm, docs/plans/phase3-netboot-implementation-plan.md, docs/notes/pi-netboot-capture-analysis.md, docs/notes/netboot-trinity-testing.md, i80, i83, i86, i93, i94 |
| <a id="i95b"></a>**i95b** | **End-to-end Pi boot on real Trinity — hardware-gated: DHCP + B-DOS provisioning + real Pi boot** | OPEN | agent | ready |  | i93b |  |  |
| <a id="i95b-b2"></a>**i95b-b2** | **Real Pi netboots from the SAM end-to-end (rig-gated): DHCP + TFTP boot of the physical target Pi** — The physical-client tail of the end-to-end Pi boot: with the i345 hub-isolated rig in place and the i95b-b1 simulated-client shot proven, power the netboot-enabled target Pi and confirm it DHCP-leases from the SAM, TFTP-fetches its boot file set from the Trinity store, and boots. Multi-MB firmware provisioning/serving (spanning) rides the i70 residuals. OPEN RISK (Pete, 2026-07-02): at the measured bit-bang throughput (~5-9 KB/s) a full Pi 400 fetch (start4.elf ~2.9 MB + kernel) plausibly takes 10-20 min; TFTP's per-block lock-step keeps per-block gaps ~60-110 ms (fine for retransmit timeouts), but whether the Pi 4/400 EEPROM bootloader has a GLOBAL watchdog that aborts a multi-minute TFTP phase is unknown - this shot is what answers it. Use the i95b-b1 measured serve throughput to set expectations before the rig session. | OPEN | agent | ⏳ i345 |  | [i345](#i345), i95b-b1, i346 |  |  |
| <a id="i100"></a>**i100** | **Firmware-download provisioning UX — user-selectable revision and file subset** — On-SAM multi-file firmware downloader with user-selectable revision pin and file subset. Host-verified slice complete as i100a; bootable migration + real HSAVE store leaf complete as i100b (closed). Remaining: i100c picker UX (Pete-gated interface design). | OPEN | agent | ready |  |  |  | src/netboot/fw_source.asm, src/netboot/http_main.asm, docs/specs/phase3-delivery-design.md, i100a, i100b, [i100c](#i100c) |
| <a id="i100c"></a>**i100c** | **Picker UX — user-facing revision/file-selection interface for the firmware downloader** — User-facing revision/file-selection interface for the firmware downloader. Interface design is Pete's call (editor-era UX, no autonomous decision here). No specific qN gates it — bare Pete decision. | OPEN | pete | 👤 pete |  |  |  | docs/specs/phase3-delivery-design.md, [i100](#i100) |
| <a id="i101"></a>**i101** | **End-to-end deliverable: fetch RPi firmware → assemble spectrum4 release on SAM → TFTP-serve a full Pi 400 netboot** — The integrated SAM IDE on ONE bootable disk — editor + assembler/disassembler + TFTP server — an interactive USER-DRIVEN program (q23 resolved 2026-06-20: NOT a boot→fetch→assemble→serve auto-sequence). User loads it, starts/edits a project, assembles release.bin/kernel8.img from release.tbn (M6 byte-match), and TFTP-serves so a Pi 400 netboots — features exercised on demand. The three org-&8000 layers coexist via paging (implementation detail), not sequential overlays. NOT SimCoupé-testable (no Trinity ENC/SD); real E2E is Pete's hardware gate. Arch: docs/specs/phase3-delivery-design.md §13. | OPEN | agent | ⏳ i3 |  | [i3](#i3) | [i325](#i325) | docs/specs/phase3-delivery-design.md §13, [i100](#i100), [i70](#i70), i83, i86, i99, i80 |
| <a id="i106"></a>**i106** | **SAM-as-terminal: SSH (or telnet) client to Pete's Mac over the physical LAN** — KEX-stub SSH the sweet spot: send constant X25519 basepoint as client ephemeral pubkey; use server's received pubkey as shared secret — zero EC math, instant connect, interoperates with stock OpenSSH sshd. Skip host-key verification (drops Ed25519+SHA-512). Per-byte: ChaCha20-Poly1305 ~few hundred B/s, usable for SAM MODE 3 64x24. Secure-mode toggle (real X25519, ~10-min) nearly free via existing i88 X25519. Telnet fallback: minimal SAM code but needs Homebrew inetutils on Mac. Reuses i88 crypto + i70 tcp_conn. | OPEN | agent | 🔒 q57 |  | q57 |  | [i88](#i88), [i70](#i70), docs/specs/tls13-handshake.md §Prior art |
| <a id="i117"></a>**i117** | **Extract sam-aarch64 learnings into a reusable open-source autonomous Claude project framework** — Starter framework/template so a new project inherits: autonomous-loop monitor (i97), split iN/qN registries + controlled-status discipline (i115 validator), §3 pre-merge review gate, per-session retro (i104), prime-directive verification ethos, brick + Go-authority-oracle loop, delegate-to-subagents context discipline, and doc-lifecycle + single-source-of-truth rules. Must be self-contained (portable out of Pete's ~/.claude/CLAUDE.md). Delivery options: template repo / init scaffold or Claude Code plugin. Scope + sequencing + packaging are Pete's call; do not auto-pick-up. | OPEN | pete | 👤 pete |  |  |  | i97, i104, i115 |
| <a id="i117a"></a>**i117a** | **Prior-art survey: existing Claude Code autonomous-orchestration plugins/frameworks vs i117** — Preliminary 2026-06-18 scan: metaswarm, ccswarm, ruflo, claude-plan-orchestrator, autonomous-dev, closedloop-ai, barkain, Composio AO + Claude Code Agent Teams; plus awesome-agent-orchestrators, awesome-cli-coding-agents. Most emphasise PARALLELISM. i117's model differs: single serial orchestrator + deterministic non-LLM monitor (i97) + prime-directive culture + blameless retro + iN/qN registry backbone. Deliverable: feature-by-feature comparison to confirm differentiation and whether gap justifies building i117. Research-first; cite each tool. Pete's call; do not auto-pick-up. | OPEN | pete | 👤 pete |  |  |  | [i117](#i117) |
| <a id="i118"></a>**i118** | **i82 TFTP client — OACK handling / standard-TFTP-server interop** — The i82 client requests RRQ options (blksize/tsize/timeout/windowsize) but has no OACK (opcode 6) handling. A standards-compliant server (e.g. macOS tftpd) answers an optioned RRQ with OACK before DATA; the client ignores it, never ACKs block 0, and hangs. Fix: on OACK -> learn server endpoint + adopt negotiated blksize (512 default if absent) + send ACK block 0; on direct DATA -> 512 default (today's hard-coded 1428 truncates); drop windowsize from request. Mirrored in Go authority + host tests. PR #422 landed the Go/Z80/host side; hardware test pending. | OPEN | agent | ready |  |  |  | tools/netboot-oracle/tftp/tftp.go, tools/netboot-oracle/tftp/clientloop.go, tools/netboot-oracle/client/client.go, src/netboot/netboot_client.asm, src/netboot/tftp_client_front.asm, i82, [i119](#i119), i120 |
| <a id="i118b"></a>**i118b** | **hardware verification — OACK path on real Trinity hardware** — Confirm the OACK path works on real Trinity hardware: netboot client connects to a standards-compliant TFTP server (e.g. macOS tftpd), receives OACK, ACKs block 0, and receives the file. Gated on Trinity hardware access + a standards-compliant server in the test environment. | OPEN | agent | ⏳ i310 |  | i272, [i310](#i310) |  | [i119](#i119), i82 |
| <a id="i119"></a>**i119** | **i82 TFTP client — persistence write-out on real hardware** — Wire the i82 client's B-DOS persistence write-out; deferred by Pete (first test = receive only; persistence later). Gaps: (1) record-0-selection bug — client_main selects record 0 = floppy; (2) HRECORD-stamp question — whether a build-disk .mgt record is HRECORD-selectable; (3) large-file staging via the i99 fw_span port; (4) filename >10-char UIFA truncation. Free-record detection reads the on-card list directly per docs/specs/trinity-record-detection-design.md (Trinity SD is SHARED — show-name + confirm-before-overwrite mandatory). HSAVE write-out + raw-SD list read stay hardware-gated. | OPEN | agent | ready |  |  |  | src/netboot/netboot_client.asm, src/netboot/bdos_seam.asm, docs/specs/trinity-record-detection-design.md, docs/specs/samdos-file-io.md, i82, i93, i99, i114, i134 |
| <a id="i123"></a>**i123** | **Investigate migrating the build from make to tup** — tup tracks the full dependency graph and rebuilds exactly what is stale (and removes orphaned outputs), structurally preventing the dirty-build/stale-artifact class — same class i116 patches test-by-test; tup would make stale-.bin reads impossible by construction. Pete's ~/git/spectrum4 uses tup ('amazing'). Scope: evaluate tup against current Makefile (pyz80 .asm→.bin, Go builds, disk-image assembly, CI jobs), then if it wins, plan the migration. | OPEN | agent | ready |  |  |  | Makefile, i116, i104 |
| <a id="i123b"></a>**i123b** | **Perform the make->tup build migration (Pete-greenlit)** — Contingent on Pete's go-ahead per the i123a evaluation. Port the 167-target Makefile to per-dir Tupfiles + Tuprules.tup, reusing spectrum4's template. Order: (1) symbol-import cluster (enc_fix_payload->assembler->test_mem/test_cluster) first — it is the footgun; (2) netboot standalone binaries (parallel); (3) disk-pack + table-codegen edges. Keep SimCoupe/release-gate in ci.yml; byte-compare tup outputs vs make until parity; add the FUSE-privileged tup-under-docker CI launcher. owner:pete = the go/no-go decision is his; implementation is agent work once greenlit. | OPEN | pete | 👤 pete |  |  |  |  |
| <a id="i126"></a>**i126** | **One faithful SAM emulation layer used by every test — fix fragmented/inadequate emulation** — Root cause of repeated hardware-debug detours (i82 client_main; the 2026-06-23 yellow-screen): partial emulators, each modelling half the SAM. The flat Go netboot harness has Trinity seams (ENC/EEPROM/SD) + paging but NO screen/timing/sound/line-interrupts; SimCoupe is a faithful full SAM (real ROM, boots from address 0, screen, timing, sound, line-interrupt render) but has NO Trinity. So a routine that is a verbatim ROM copy can PASS the screenless Go harness and fail flat-yellow on hardware (the rainbow stripes are rendered by the live line-interrupt handlers, which the Go harness does not model). NORTH STAR (Pete 2026-06-23, feedback_comprehensive_emulation): ONE comprehensive emulation = fork SimCoupe (the most complete SAM) and PORT THE TRINITY MODELS IN (ENC28J60 + EEPROM + SD/B-DOS), so the single layer captures Trinity, network, storage, screen, timing, sound AND their interactions, boots from address 0 (i232), and is used by EVERY test. Then cross-subsystem surprises fail in emulation, not first on silicon, and the NETBOOT_HOSTTEST carve-outs (i231) are deleted. Stop the 'SimCoupe port is too much work' / 'merging the two emulators is overkill' shortcuts. Subsumes i124/i231/i232. | OPEN | agent | 🔒 q58 |  | q58 | [i232](#i232), [i254](#i254) | tools/z80-test-harness-go/harness.go, tools/netboot-oracle/z80/, src/netboot/, i124, i125, i116 |
| <a id="i143"></a>**i143** | **bdos_picker: hardware MODE-2 blit for picker_render (follow-up from i119d)** — picker_render in src/netboot/bdos_picker.asm is a stub ret (all decision/show-name/confirm logic is host-verified in i119d). This item wires the actual SAM MODE-2 screen blit so the picker display is visible on real hardware. Depends on i119d being merged. | OPEN | pete | 👤 pete |  | i119d |  |  |
| <a id="i168"></a>**i168** | **Future: import a full source file into the editor (fail-with-line-number on bad line)** — Future feature (q24, Pete 2026-06-20). The SAM editor accepts ONE LINE AT A TIME (parsed/validated on entry) — no whole-file import today. If/when import is added: on a bad line, fail and report the line number + the error; the user fixes the source and retries (Pete's preferred behaviour). Gated on the editor groundwork (depends i3) — a normal backlog item, not part of current editor specs. | OPEN | agent | ⏳ i3 |  | [i3](#i3) |  |  |
| <a id="i177"></a>**i177** | **Publish trinboot: open-source EEPROM boot-config patcher (Python-over-trinload + bootable .mgt) for Trinity users** — Release the SAMBOOT EEPROM boot-config + stripes-restore patch as a standalone open-source tool for any Trinity owner: a Python program sent over trinload that patches the EEPROM, or a bootable .mgt. Gives Trinity users the configurable default-boot-record + opening-stripes feature for free. NOT a requirement for SAMBOOT -- depends on it (publication only). | OPEN | agent | 🔒 q56 |  | i135, i176, q56 |  | docs/specs/samboot.md |
| <a id="i182"></a>**i182** | **Bootable fetch-and-boot disk + real-Trinity run of client_fetch_boot** — i122c delivered the host-verified PXE-style fetch-and-boot orchestration entry (client_fetch_boot: EEPROM identity -> TFTP-fetch a .mgt straight into a scratch record via the raw_record_sink -> validate -> ALHK-boot). To exercise it on real hardware, build a bootable SAM disk whose autorun invokes client_fetch_boot (a netboot-fetch-boot-disk make target mirroring netboot-client-disk), decide the autorun entry vs client_main, and run it on real SAM+Trinity: serve a stamped 819200-byte .mgt, confirm it streams into the scratch record (1600 HWSAD sectors), validates, and ALHK-boots it. Hardware-gated (the whole point is the real SD write + real boot, unverifiable in emulation); entangled with the auto-boot/control-return autonomy (i132/i135). The full-size valid stream is also not driven through the bit-banged ENC in the host harness, so the real wire run is the first full-scale fetch-and-boot. | OPEN | agent | ready |  | i122c |  |  |
| <a id="i182b"></a>**i182b** | **Real-Trinity run of client_fetch_boot via the fetch-and-boot disk** — Hardware-gated completion of i182 (unverifiable in emulation). Boot the i182a fetch-and-boot disk on real SAM+Trinity, serve a stamped 819200-byte .mgt, confirm it streams into the scratch record (1600 HWSAD sectors), validates, and ALHK-boots it. Entangled with auto-boot/control-return autonomy (i132/i135). The full-size valid stream is the first full-scale fetch-and-boot (not driven through the bit-banged ENC in the host harness). Needs i182a's disk. | OPEN | agent | 🔒 q68 |  | i182a, i272, i295, q68 |  |  |
| <a id="i215"></a>**i215** | **IDE page-persistence (spill): lazy-spill cold DOC blocks to Trinity or floppy/tape when the pool is full** — Downstream of i2 (Pete, 2026-06-22, q36): the i2 baseline refuses-with-message when the page pool is exhausted. This OPTIONAL feature makes alloc_page lazy-SPILL a cold DOC block to a persistence backend instead of refusing (design §4.5/§5/§7): serialize the block to disk, free its page, satisfy the alloc; reload on demand. Backend is PLUGGABLE — Trinity storage if the machine has it, else a physical floppy/tape — so a space-limited user on ANY machine gains document headroom beyond the ~144KB/256KB ceiling. HARD CONSTRAINT: the assembler must run fully on a floppy/tape-only machine with NO Trinity, so the spill backend is optional and must degrade to refuse-with-message when absent. Depends on i2 (the pool allocator + the page_owner[] discipline) but NEVER blocks it. | OPEN | agent | ready |  | i2a |  |  |
| <a id="i215c"></a>**i215c** | **Spill manager: concrete pluggable backends (Trinity record / floppy-tape) + boot wiring** — Concrete SpillBackend implementations behind the i215a interface: Trinity SD-record store when present, else physical floppy/tape; selected at boot by capability detection. The assembler must run fully on a floppy/tape-only machine with NO Trinity (HARD constraint), so the backend is optional and degrades to refuse-with-message when absent. SimCoupe/boot integration. Depends on i215b. | OPEN | agent | 🔒 q60 |  | i215b, q60 |  |  |
| <a id="i232"></a>**i232** | **All Go-harness tests boot the SAM from address 0 (real ROM cold-init) for faithful machine state, not Call-with-seeded-state** — Pete (2026-06-23), foundational fidelity principle (part of i126): booting from address 0 with the real ROM IS what a SAM does — running a routine in isolation with hand-seeded registers/sysvars (e.g. a hand-seeded PALTAB) is not emulating the machine, it is testing a fragment in a made-up state. The Go library boots from reset in milliseconds (samboot_real_boot_test.go already does it: load rom.bin, run from PC=0, the ROM cold-init MNINIT->NEW2->streams populates PALTAB/STREAMS/FLAGS/etc.), so every harness test should boot from 0 first and then exercise the code in the REAL post-boot state. Deliverable: a reusable 'boot the SAM from reset, then run X' harness helper, adopted as the default across the koron-go tests (the netboot leaf tests currently Load+Call without booting). Big refactor; FUTURE — must not block the current bootblock RAM-test work. Captured per Pete's instruction to record + move on. | OPEN | agent | 🔒 q58 |  | [i126](#i126) |  |  |
| <a id="i254"></a>**i254** | **Run ALL tests in CI: private binaries as repo secrets (SKIP_PRIVATE_TESTS default-true, our CI overrides) + add currently-excluded packages** — Pete (2026-06-25), follow-on to i253. GOAL: CI runs EVERY test; nothing silently excluded. (1) SKIP_PRIVATE_TESTS=true is the CI DEFAULT (forks get no secrets -> they skip the gated tests explicitly). (2) OUR repo's CI materialises the private binaries from encrypted GitHub repo secrets (rom.bin, eeprom.bin, B-DOS 1.5t -> the capture paths) and overrides SKIP_PRIVATE_TESTS=false so our CI runs them; GitHub withholds secrets from fork PRs. Pete is fine storing them as secrets. (3) ADD the currently-CI-excluded packages (z80-test-harness-go, zx0-greedy, editor-prototype) to CI, guarded by SKIP_PRIVATE_TESTS where they need private resources. TENSION RESOLVED (Pete 2026-06-25): the 'go harness is not a CI gate' rule predates Trinity emulation — SimCoupe was then strictly more complete so it gated CI; now the go harness emulates Trinity (ENC/SD/EEPROM) which SimCoupe does NOT, so gating Trinity-requiring tests on the go harness in CI is CORRECT (netboot-oracle/z80 already does). CONSOLIDATION: i126 (port Trinity INTO SimCoupe) -> once it lands CI can consolidate on SimCoupe. CAVEAT on 'private': the proprietary/non-redistributable status of Colin's binaries is a PRECAUTIONARY assumption by Pete, NOT confirmed fact — they may be freely redistributable. If confirmed redistributable, they could simply be committed/fetched and skip the secrets entirely (simpler). PETE-GATED: Pete creates the secrets. depends_on i253 + i126. Detail: docs/plans/i253-no-silent-skips.md. | OPEN | agent | 🔒 q58 |  | i253, [i126](#i126) | [i311b](#i311b) |  |
| <a id="i264"></a>**i264** | **SAMBOOT selector firmware: hold-key-at-boot launches a record picker (choose boot record by number/name) instead of hardcoded record 3** — Today's forked bootloader (trinity-autoboot, flashed via i135c) auto-boots a HARDCODED record (3 = trinload). Make it configurable: at power-on, if a chosen key is held (e.g. F9 or ESC — pick one, note the alt), the bootloader launches a small RECORD-PICKER program that lists the available Trinity SD records and lets the user select one by NUMBER or NAME; the choice is persisted to the 'SAMBOOT Config' EEPROM chunk (the bootloader's read_config path already exists and is dormant behind the hardcode) so subsequent power-ons auto-boot the selected record. If the key is NOT held, boot the configured record (or fall to the opening screen + wait-key when none). Build in the PRIVATE sibling repo trinity-autoboot (q55: local, not in CI); reuse the by-name EEPROM chunk reader + record-list reader (trinity_storage_shared_resource: read the on-card record list directly, show name + confirm). Develop + verify in the netboot-oracle emulation (key-held vs not, picker lists records, selection persists to config chunk, boot dispatches the chosen record). HARDWARE flash (re-flash via eeprom_flash_chunk1) + on-SAM test GATED ON PETE PRESENT (TAPO smart-plug incoming will let the agent power-cycle for autonomous test). This + i194 (write disks to storage) are the two gaps between today's fixed-record auto-boot and full autonomous SAM control. | OPEN | agent | ready |  |  |  |  |
| <a id="i264b"></a>**i264b** | **Record picker UI (Increment 2): list available records, select by number/name, persist the SAMBOOT Config chunk** — Record-picker UI (Increment 2 of i264). HOME DECISION (Pete 2026-07-01, q59): the picker lives in the WRITABLE EEPROM as its OWN chunk(s), extending the bootblock code -- NOT the ~250-byte config chunk (A), not B-DOS scratch (B), not a separate SD record (C). The ~250B limit was the CONFIG chunk only. VERIFIED against ~/sam-archive/samboot-capture/eeprom.bin: 128KB total, 88.3% blank flash (0xFF) = ~113KB FREE, ~15KB used (~12KB is B-DOS 1.5t in the low ~12KB; the chunk directory is up at ~122-131KB; the middle ~110KB is empty). Ample. On a key-held boot the bootblock loads+runs the picker chunk (as it loads the B-DOS chunks), reads the on-card record list via the raw SD driver, renders a BIOS-style menu by number+name, takes a selection, persists to the SAMBOOT Config chunk (write_chunk). Develop+emulate in netboot-oracle; EEPROM flash hardware-gated (i266/Pete); build in the private trinity-autoboot repo (q55). BIOS-menu UI design = a future sprint. | OPEN | agent | 🔒 q76 |  | q76 |  |  |
| <a id="i291"></a>**i291** | **Measure the real ENC28J60/shared-PIC settle time on hardware (unattended ethernet-reporting bisection) to replace the conservative 1200-T-state emulator constant** — Follow-up to i289 (#755). The enc28j60.go sdInitSettling window is currently a conservative 1200-T-state constant (4x the documented ~50us) — a pure emulator-fidelity knob that never runs on real silicon, safely bounded so it catches the i242/i287 SD-before-ENC ordering bug without latching. This item MEASURES the real settle to make the model faithful rather than conservative, which also independently validates the shared-PIC settle model underpinning the §8ag serve-crash interpretation. Approach (Pete's, 2026-06-30): a trinload-pushed Z80 probe that does the &38 SD-init, waits N T-states, issues an identity probe, and reports stale-vs-fresh over ETHERNET (UDP markers, not screen — so it runs unattended; bisect N between a too-small lower bound and a too-large upper bound to converge on the real settle). Then set the emulator constant to the measured value (+ margin). owner agent: now self-serviceable (i266 TAPO power-cycle + i272 passwordless tcpdump). NOT required for correctness — purely a fidelity refinement. ALSO fold in (PR 820 §3 review residual, 2026-07-02): a window still OPEN at a run boundary carries its old-timeline ABSOLUTE deadline unclamped into the new run, so a run stopping within 1200T of its last SD byte could pin the entire next run's probes stale (the i327 artifact in a narrower shape); clamp on survival to t + (settleUntilT - tNow_old) — the remaining budget — which preserves both committed boundary pins (arithmetic checked in the PR 820 review). | OPEN | agent | ready |  |  |  |  |
| <a id="i291b"></a>**i291b** | **Measure real ENC28J60/shared-PIC settle time on hardware (unattended UDP bisection); set the emulator constant** — Replace the conservative 1200-T-state sdInitSettleTStates constant in enc28j60.go (4x the documented ~50us — a pure emulator-fidelity knob that never runs on silicon) with a MEASURED value. Approach (Pete, 2026-06-30): a trinload-pushed Z80 probe does the &38 SD-init, waits N T-states, issues an ENC identity probe (chk_trinity-style &08/&09 IDENT select + IN &DD), and reports stale-vs-fresh over ETHERNET UDP markers (not screen, so unattended); bisect N between a too-small lower bound and a too-large upper bound to converge on the real settle. Then set the constant to measured + margin. NOT required for correctness — fidelity refinement; also validates the shared-PIC settle model behind the serve-crash interpretation. Self-serviceable: i266 TAPO power-cycle + i272 passwordless tcpdump.<br><br>SCOPE (2026-07-04 investigation, sibling i291a clamp already DONE #855): LARGE / multi-session — do it in a fresh dedicated session, emulation-first. Pieces: (1) the probe needs the FULL ENC bring-up (drv_init + MAC/IP config + the ENC TX path via encdrv.asm/enc_link.asm/build_udp_frame.asm) just to EMIT the report — model on src/netboot/csd_probe.asm (org &8000, DUMPER=1, includes netboot_serve.asm, reports via test_report.asm) as the closest skeleton, or build a lighter standalone UDP emitter; (2) a T-state-precise PARAMETERIZABLE delay loop between the SD-init and the identity read (the measured quantity); (3) host-test the probe LOGIC against enc28j60.go's sdInitSettleTStates model + the sdInitSettling/settleUntilT IDENT-stale window BEFORE hardware (validates it reports stale<settle, fresh>=settle); (4) a host launcher that patches N into the binary + pushes + captures the UDP reply (the trinpush-serve.py / boot-record.py config-block-patch pattern; capture via tcpdump/listen-markers.py); (5) the multi-shot hardware bisection. WATCH the i242 SD-before-ENC ordering: the probe does SD-init then needs the ENC working to report — sequence carefully. NOTE: frame agent/subagent prompts as benign retro SAM-Coupe ethernet-emulation work — the 'probe/identity/settle' framing tripped a subagent-safeguard FALSE POSITIVE on 2026-07-04 (Opus cyber filter); investigate inline or frame plainly. | OPEN | agent | ready |  |  |  |  |
| <a id="i291b-b2"></a>**i291b-b2** | **Run the hardware settle-time bisection: sweep N via the probe, capture UDP replies, converge on the real ENC/PIC settle, set sdInitSettleTStates=measured+margin** — Using the i291b-b1 probe: a host launcher patches settle_delay_count (N) into the binary, trinload-pushes it, and captures the SATR UDP reply, bisecting N (stale lower / fresh upper) to converge on the real ENC/PIC settle, then set enc28j60.go sdInitSettleTStates = measured + margin. LAUNCHER BUILT (2026-07-04): tools/hardware-shot/settle-bisect.py on branch i291b-settle-bisect (WIP, pushed, NOT merged) — --selftest proves the SATR capture+classify path; --single N is a diagnostic shot. BLOCKED by a hardware finding: the settle_probe HANGS on real hardware before reporting (a live push/execute succeeded once, but no SATR was captured and trinload never resumed → hang before test_report/tr_terminate). Prime suspect drv_wait_link (fresh drv_init resets the PHY; silicon link-renegotiation >> the emulator's instant link-up); secondary: test_report's SATR TX never hardware-proven, and the i228 tr_terminate hw-vs-emu branch. Also: trinload push-readiness lags ping (wait longer after boot before the first shot). NEXT (emulation-first): triage the hang in koron-go (drop drv_wait_link? model a slow link-up?), A/B port_probe (shares drv_init+drv_wait_link+test_report) to prove the TX path on hardware, THEN resume the bisection. Self-serve via i266 TAPO + i272 tcpdump. See the settle-bisect.py HARDWARE FINDINGS header. | OPEN | agent | 🔒 q78 |  | i291b-b1, i291b-b3, [i291b-b5](#i291b-b5) | [i291b-b4](#i291b-b4) |  |
| <a id="i291b-b4"></a>**i291b-b4** | **Bound the remaining unbounded ENC MII busy-polls (rphy_wait, wr_phy_wait) for the MII-wedge / drv_wait_link hang class** — Follow-up to i291b-b3 (which bounded the TX-path &DC polls). Bound the remaining unbounded ENC busy-polls that poll the MII MISTAT.BUSY register (NOT the &DC bit-3 flag): rphy_wait (enc_link.asm, the drv_wait_link PHY-link-status read) and wr_phy_wait (encdrv.asm, the PHY-write completion poll), with the i280b/i291b ENC_BUSY_LIMIT bounded-poll pattern. These are the settle_probe hang's COMPETING hypothesis -- the i291b-b2 finding's PRIME suspect was drv_wait_link/rphy_wait (a fresh drv_init resets the PHY; silicon link-renegotiation >> the emulator's instant link-up, so rphy_wait's unbounded MII poll could spin). The StuckBusy model wedges &DC bit 3, NOT MISTAT.BUSY, so it does not exercise these; testing needs a StuckMII (or equivalent) model flag + leaf gates mirroring b3's. Gated on b2's A/B evidence: if the shared TX-path bounds (b3) already resume trinload on hardware, this is lower priority; if the hang is the MII/link path, this is the fix. | OPEN | agent | 🔒 q78 |  | [i291b-b2](#i291b-b2) |  |  |
| <a id="i291b-b5"></a>**i291b-b5** | **settle_probe SATR never egresses on hardware (test_report TX fails while &38 SD-init holds shared &DC); make the probe report before b2's bisection can run** — HARDWARE FINDING (2026-07-04, first shot with the b3-fixed probe rebuilt from main): b3's bounded &DC polls DID change behaviour — the probe no longer hangs trinload permanently (trinload RECOVERED and re-accepted pushes between shots 1->2->3). BUT no SATR report ever egressed (test_report's TX times out per the b3 bound instead of sending), and after ~3 consecutive shots trinload wedges (no discovery reply). So b2's bisection cannot run: the probe cannot report FRESH/STALE. Two candidate causes to disentangle: (a) test_report's SATR TX contends the &38-held shared &DC bus (b3 root cause) so the frame never leaves the wire; (b) test_report's TX path was NEVER hardware-proven at all (no host listener existed before settle-bisect.py). DECISIVE EXPERIMENT (set up, interrupted before firing): push port_probe_standalone (test_id 2) which shares drv_init+drv_wait_link+test_report but does NO &38 SD-init OUT — if its SATR egresses on hardware, the fault is &38 contention (probe must wait for &DC BUSY-clear before test_report); if it also fails, the shared TX path is broken on silicon. EMULATION-FIRST GAP (rule 7): the koron-go model currently egresses the SATR fine (its &38 busy self-clears fast), so it does NOT reproduce this no-egress; model the &38 busy-hold so a TX during the hold fails to egress, reproduce, then fix. Launcher: settle-bisect.py on branch i291b-settle-bisect (WIP). Self-serve via i266 TAPO. | OPEN | agent | 🔒 q78 |  | q78 | [i291b-b2](#i291b-b2) |  |
| <a id="i304"></a>**i304** | **Reconstruct a labelled B-DOS 1.5t source from 1.5a + reassemble to byte-match** — Root-cause fix for the recurring 1.5a-vs-1.5t authority lure (Pete flagged it 2 sessions running; feedback_bdos_15t_not_15a). Start from reference/bdos/bdos15a.src.txt (the clean labelled 1.5a COMET source), identify the routines 1.5t changed (per bdos15t-beta6.annotated.dis: the SD init ladder, the 32-bit linear seek replacing hd.seek/set.chs/seek.base, the CSD/records math, record-base poke), splice the 1.5t versions in, and reassemble (pyz80) iterating until it produces the EXACT 1.5t binary (extracted/bdos15t-beta6.bin) -- the byte-match is the proof of correctness. Retain all 1.5a comments; finally annotate the new routines. Differences are minimal. Deliverable: a committed reference/bdos/bdos15t.src.txt (+ a make target asserting byte-identity) that becomes THE cited Trinity-HW B-DOS authority, and update the in-repo references/plan/code citations that currently point at 1.5a. NOTE: 1.5t is Colin's proprietary binary (kept out-of-repo, SKIP_PRIVATE_TESTS-gated) -- the reconstructed SOURCE is ours but the byte-match gate must guard against committing Colin's bytes; follow the existing private-artifact pattern (csd_decode_colin_test.go resolves the bin from ~/sam-archive). | OPEN | agent | ready |  |  |  |  |
| <a id="i304b"></a>**i304b** | **Splice 1.5t device-layer diffs into the 1.5a pyz80 source to byte-match bdos15t-beta6.bin** — Second brick for i304 (q79 resolved: option b -- the byte-exact 1.5t SOURCE stays PRIVATE, per the publication policy 'trinity.mgt contents stay out of the repo'; it reproduces Colin's proprietary binary so it is not committed to the public repo). Base = reference/bdos/bdos15a.asm (i304a byte-exact, l_XXXX address-labels == the ANALYSIS region-diff a&XXXX). In a PRIVATE working copy under ~/sam-archive, apply the documented 1.5t (Trinity fork) changes region-by-region (~/sam-archive/bdos/analysis: region-diffs.txt + bdos15t-beta6.annotated.dis): Atom device layer -> SD/SPI driver, CHS->32-bit linear seek, SD init ladder, CSD/records math, record-base poke, hot-swap RESTORE, banner/DIR/message text, DEVICE command removed. Iterate assemble->diff until byte-identical to extracted/bdos15t-beta6.bin. Graft Edwin's routine names from bdos15a.src.txt onto touched routines (human-verified). IN-REPO DELIVERABLE (public): a SKIP_PRIVATE_TESTS-gated byte-match test that resolves both the source and the bin from ~/sam-archive (like csd_decode_colin_test.go) and asserts pyz80(source)==bin -- proves correctness without committing Colin's bytes. The private annotated source lives in ~/sam-archive (agents on Pete's host read it as the authority). | OPEN | agent | 🔒 q80 |  | i304a, q80 | [i304c](#i304c) |  |
| <a id="i304c"></a>**i304c** | **Annotate 1.5t routines; make bdos15t src THE cited Trinity-HW B-DOS authority + update citations** — Final brick for i304 (q79 option b). Annotate the private 1.5t source from i304b (graft Edwin's retained 1.5a comments + names, per-routine human-verified). Make it THE cited Trinity-HW B-DOS authority WITHOUT publishing Colin's bytes: the in-repo public authority pointer is docs/notes/bdos-trinity-fork-analysis.md (the existing publication-safe derived doc) + the private-gated byte-match test (i304b); the byte-exact annotated source stays in ~/sam-archive (accessible to agents on Pete's host). Update the in-repo references/plan/code citations that currently point at 1.5a to point at the 1.5t authority (the doc + private source) -- feedback_bdos_15t_not_15a root-cause fix. If a Trinity-HW Go model needs the authority marker, add tools/trinity-authority-ledger.txt entry per i273. | OPEN | agent | 🔒 q80 |  | [i304b](#i304b) |  |  |
| <a id="i306"></a>**i306** | **L14 (record 14) first ~388 sectors corrupted by the pre-fix buggy push -- restore if needed** — The pre-fix sd_push used base=2438 (not B-DOS 2050), so cj 1600-sector write at LBA 21638-23237 spilled 388 sectors into B-DOS record 14 (catalogue L12/L14 region, body 22850-24449): LBA 22850-23237 overwritten. Pete data -- restore L14 from backup if it mattered. The base=2050 fix + the out-of-band write-guard (both in #773) prevent recurrence; future pushes only touch a genuinely free record. | OPEN | pete | 👤 pete |  |  |  |  |
| <a id="i310"></a>**i310** | **Provide a standards-compliant TFTP server in the test environment (for the i118b OACK-interop hardware test)** — i118b needs the netboot CLIENT to fetch from a STANDARDS-COMPLIANT third-party TFTP server (e.g. tftpd-hpa on the Pi, or macOS tftpd) that emits OACK for optioned RRQ. No such server is installed/reachable in the test env (no tftpd on this host; a standards-compliant server binds privileged port 69 -> needs apt install + config or a setcap/sudoers grant, which is Pete's host action, not covered by i272's tcpdump/arping caps). Once provided + reachable from the SAM (192.168.2.75), i118b unblocks and the agent runs the OACK-receive shot self-serve (power-cycle + push client + tcpdump). owner:pete = the one-time host setup. | OPEN | pete | 👤 pete |  |  | [i118b](#i118b) |  |
| <a id="i311"></a>**i311** | **Retire the AttachBDOS Go mock behind a shrink-to-zero ledger, now the faithful B-DOS rig has landed** — i274 P2 (retire the mock-substitutes-for-real anti-pattern). The AttachBDOS Go mock (bdos_store.go BDOSStore / Machine.AttachBDOS) and the armed-dispatch double stand in for real B-DOS and HID the serve/WRQ write-path failure across the SD-write saga (i274 axis A, 'mock-substitutes-for-real'). The faithful rig now exists (sd_push_faithful_test.go: real Colin ROM + B-DOS 1.5t via LoadROMImage), so B-DOS behaviour can be exercised authentically. TASK: migrate every AttachBDOS-based test to the faithful rig, and put any remaining AttachBDOS usages behind a shrink-to-zero ledger (name->reason, modelled on tools/hosttest-carveout-allowlist.txt) that only ratchets DOWN, so no NEW mock-based B-DOS test can be added and the mock is provably retired when the ledger hits zero. The over-aggressive-settle negative-control gate (the other half of P2) is already i289 (DONE). Authority: CLAUDE.md rule 8; memory feedback_trinity_sd_write_settled_truths ('never use the AttachBDOS mock to test B-DOS behaviour — always real Z80 B-DOS in emulation'). May be partially gated on the faithful rig covering each AttachBDOS use case (assess coverage when worked). | OPEN | agent | ready |  |  |  |  |
| <a id="i311b"></a>**i311b** | **Migrate AttachBDOS mock tests to the faithful B-DOS rig; ratchet the ledger to zero** — Second half of i311, gated on i254. Migrate every AttachBDOS-based test (per the i311a ledger) to the faithful rig (LoadROMImage + real Colin ROM + B-DOS 1.5t, as sd_push_faithful_test.go), ratcheting the i311a ledger DOWN toward zero; delete the AttachBDOS mock (bdos_store.go BDOSStore / Machine.AttachBDOS + the armed-dispatch double) when the ledger hits zero. GATED ON i254: the faithful rig is SKIP_PRIVATE_TESTS-gated and does NOT run in CI until i254 makes the repo's CI materialise the private captures (rom.bin/eeprom.bin/B-DOS 1.5t) and run SKIP_PRIVATE_TESTS=false -- migrating a mock test (runs in CI) to the faithful rig (skips in CI) before i254 would DELETE CI coverage. Assess per-use-case faithful coverage as ledger entries are eliminated. Authority: i274 P2, CLAUDE.md rule 8. | OPEN | agent | 🔒 q58 |  | [i254](#i254) |  |  |
| <a id="i321"></a>**i321** | **Verify +232 'BDOS' stamp-patch parity across SD-write paths (sd_push patches; serve-WRQ + client_finalize size-only)** — VERIFIED (this session): INTENDED / not-a-standalone-gap — subsumed by q68. The +232 'BDOS' stamp difference is real (serve-WRQ raw_record_sink->bdos_write_record + client_finalize validate SIZE-ONLY, don't stamp; build-disk does NOT pre-stamp), BUT both stampless paths use the ABANDONED B-DOS-HWSAD write mechanism (bdos_write_record RST-8), which hangs on silicon (i270) and would wedge at the HRECORD-select (i356: real B-DOS 1.5t longjmps 'Invalid record' on a free/unstamped record) BEFORE writing a sector. The LIVE persist paths already stamp +232: sd_push (sd_push.asm:547-587 — name@+210, 'BDOS'@+232, name-tail@+250 into body sector-0, own-CMD24) and http store_format_record (i357). A standalone stamp-port would be WRONG: it patches a mechanism slated for replacement and would not even work (the HRECORD wedge fires first). q68 owns the serve-WRQ/client write-path rework (converge onto the proven own-CMD24 engine); that rework MUST carry the +232 body stamp (mirror sd_push) — inherent to making the feature work on hardware. So i321 rides q68 (gated on it), keeping the +232 stamp as an explicit checklist item for whoever executes q68. | OPEN | agent | 🔒 q68 |  | q68 |  |  |
| <a id="i325"></a>**i325** | **Optimise the assembler emit hot path (T-state tuning) -- measure candidates by total T-states to assemble release.s** — Lowest-priority future perf item (Pete 2026-07-01). After i24 lands option A (uniform per-byte LMPR-bracketed emit into a pool run), the emit hot path pays ~1/4 of its per-byte cost on the page-swap bracket and ~3/4 on register push/pop (ABI), zone/full checks, and OUT_PC/OUT_LEN bookkeeping. Candidates to evaluate: (a) a small staging buffer (8-128 bytes) in an always-paged region, flushed via ONE bracket + ldir per N bytes (amortises the page-swap); (b) restructure so the per-byte hot path is just store-to-buffer + pointer-bump, moving zone/page-run advance + OUT_LEN + the bracket into the per-flush path (potentially ~3x); (c) alternative-register / reduced-stack-op tuning of emit_byte. METHOD: measure EACH candidate by TOTAL T-states to assemble release.s (a representative full source); best T-states that STILL fits the memory budget wins. DO WHEN CODE IS FEATURE-COMPLETE (so the free-memory / staging-buffer budget is known). One-shot cost -> lowest priority. | OPEN | agent | ⏳ i101 |  | [i101](#i101) |  |  |
| <a id="i345"></a>**i345** | **Netboot-target Pi rig: hub-isolated network segment with a netboot-enabled Pi wired to the SAM (Pete hardware setup)** — Physical prerequisite for any REAL-Pi netboot exchange against the SAM (i95b real-Pi boot; i89b Pi-3 capture): a netboot-enabled target Pi (Pi 400 per the existing captures, or Pi 3 for i89b) wired to the SAM on an ISOLATED segment (the hub+Wireshark rig from the trinload captures, pi-netboot-capture-analysis.md). Isolation is load-bearing: the SAM's DHCP server answering DISCOVERs on the shared house LAN would be a rogue DHCP server handing broken leases to other devices - the reference dnsmasq capture setup used a separate USB-NIC subnet (192.168.50.x) for exactly this reason. Pete-owned: physically wiring/enabling the rig; once it exists the exchanges themselves can run agent-side. | OPEN | pete | 👤 pete |  |  | [i89b](#i89b), [i95b-b2](#i95b-b2) | docs/notes/pi-netboot-capture-analysis.md |
| <a id="i347"></a>**i347** | **Mitigate ENC-EI frame-hook contention on the HSAVE-mid-loop serve/fetch loops (naive IM2-at-&FD00 port proven UNSAFE)** — ASSESSED 2026-07-05 (this was 'assess and IF CONFIRMED port'): the exposure is CONFIRMED real, but the netboot_server IM2-takeover pattern does NOT port safely to http_main / netboot_serve. Their loops do a mid-loop B-DOS HSAVE (http: storage_sink_leaf; serve: WRQ-record write) which REPAGES section D and SHADOWS the &FD00 IM2 vector table; a frame interrupt firing mid-hook reads a corrupt vector and WILD-JUMPS. VERIFIED: with arm_im2_takeover in http_main, TestHTTPMainFaithfulStoreLeafValidRecord + FreeRecord wild-jump to &04FD; with the arm removed (isolation-tested), they pass. netboot_server (the reference) is safe ONLY because its serve loop reads (RRQ arena / raw-CMD17 large-file), never invoking a section-D-repaging B-DOS hook mid-loop. So the pattern is safe for read-serve loops, unsafe for write/HSAVE-mid loops. The IM2 code + a shared im2_takeover.asm were written + reverted this session (clean; nothing merged). SCOPE REDEFINED (was: port the pattern): implement a REPAGING-SAFE mitigation once q81 picks the approach (repaging-safe table location via B-DOS 1.5t HSAVE-paging analysis / DI-management / accept-as-theoretical). The i70c full fetch remains the acute case whose IM2 pre-flight this assessed. Gated on q81. | OPEN | agent | 🔒 q81 |  | q81 |  |  |
| <a id="i352"></a>**i352** | **Local SimCoupé disk-boot broken on the Pi host (baseline 107 File-not-found at boot)** — Found 2026-07-03 during i39c: EVERY .mgt boot under local SimCoupé fails — the disk auto-boots, AUTO runs, and the boot loader's first HLOAD dies with SAM-screen '107 File not found, 30:1' — on a FRESH origin/main worktree build (baseline, pre-i39c) and with a factory-default SimCoupe.cfg, while the same commit's CI SimCoupé matrix is green. So local disk-boot confirmation (the 'SimCoupé locally before pushing' inner-loop step) is unavailable on this host; the Go harness remains the local loop and CI the gate. Evidence gathered: all 10 files present in the .mgt (samfile ls); ROM identical between /usr/local/share/simcoupe and the i62-local Resource dir (md5 ad08ed47...); default cfg (deleted + regenerated) reproduces; DISPLAY=:0 (labwc/Xwayland) + SDL_VIDEODRIVER=x11 + SDL_AUDIODRIVER=dummy is the working launch env (dummy video fails, offscreen emulates but same error; screenshots via grim). PRIME SUSPECT: local /usr/local/bin/simcoupe is a newer main-branch build (strings show 1.3.1, built Jun 13) vs CI's pinned v1.2.16 (0e8a69f, docs/notes/headless-simcoupe.md) — a boot/autoload behaviour change upstream would explain baseline-red/CI-green. NEXT ACTION: build+install the pinned v1.2.16 locally (the headless doc has the recipe) and re-run tools/run-simcoupe.sh on a make-disk test.mgt; if green, pin the local install and note the env (DISPLAY=:0, SDL vars) in the headless doc's local-host section. Note: ~/.simcoupe/SimCoupe.cfg was consumed/rewritten during diagnosis (pre-existing backup: SimCoupe.cfg.claude-backup-i24; today's: .claude-backup-i39c); Pete's outpath default may need restoring per feedback_agent_simcoupe_config_pollution. | OPEN | agent | ready |  |  |  |  |
| <a id="i353"></a>**i353** | **b8d corpus fixture pinning the packed-header length-escape EMIT branch (15+-byte patch expr)** — PR 837 §3-review residual (non-blocking note recorded on the PR): the Z80 packed patch-header DECODE escape branch (insn_patch_header, len nibble 15 -> real u8) is CI-proven via the release gate (the release .tbn carries 4 escaped patches and the SAM leg byte-matches), but the Z80 EMIT escape branch (compact_emit.asm cemit_ai_hdr_esc) has no pinned fixture — no b8d corpus source produces a 15+-byte patch expression, so that branch's only proof is code review. Add a corpus fixture (e.g. a symbol-diff expression long enough to exceed 14 bytes of expr bytecode) under tests/*/sources/ so TestChainPagedB8dCorpus byte-compares the emit escape path against host CompactTBNBytes. | OPEN | agent | ready |  |  |  |  |
| <a id="i354"></a>**i354** | **Delete the gated synthetic list-hook machinery (BD_HOOK_LISTREAD/LISTWRITE 161/162 + oracle cases)** — i70e follow-up hygiene: after PR 838, NOTHING uses the synthetic RST-8 list hooks — every image builds with NETBOOT_REAL_LISTREAD (raw CMD17/CMD24) and no test sets AllowSyntheticListHooks. Delete the dead machinery end-to-end: the NETBOOT_SYNTHETIC_LIST_HOOKS branches + BD_HOOK_LISTREAD/LISTWRITE equs in src/netboot/bdos_seam.asm (the scan-cluster guard then collapses to "if defined(NETBOOT_REAL_LISTREAD)"); the oracle bdHookListRead/bdHookListWrite cases, AllowSyntheticListHooks, requireSyntheticListHooks, the hook-read usedRecords overlay (the SPI mirror via MarkRecordListUsed supersedes it — check whether usedRecords itself is then write-only and deletable), applyListWrite/ListWrite/ListWrites() (sole remaining consumer: netboot_serve_terminate_test len==0 assert — re-point it at the SD model captured writes), and CardModel.ListSector if its last consumer goes. Keep CardModel itself (HRSAD/HSAVE still use it). ALSO sweep the two stale comments the PR-838 review flagged (recorded on the PR): sd_csd.asm:323-327 ("production serve + http_main use the RST 8 hook" — false; that block collapses with the fork) and the "With BD_RECORDS=10 ... usedRecords overlay" comment inside TestProvStoreDemarcation (~line 176). Pure deletion + test re-pointing + comment sweep; no behaviour change. | OPEN | agent | ready |  |  |  |  |
| <a id="i358"></a>**i358** | **Clean up working-note narrative in test_erratum843419.asm Test 2 comment block** — The Test 2 comment block in src/test_erratum843419.asm (from the original i27c commit 872e52e4, NOT the fix) carries working-note narrative with wrong intermediate values ('0x1fff8040', 'something is wrong', 'let me recompute properly'). The final conclusion + the test assertions are correct (0x10,0x40,0x80,0xFF = 0x10FF8040), but the narrative violates the present-tense/no-history comment rule. Trivial: rewrite the comment to state the correct ADR re-encode derivation in present tense. Flagged by the #846 §3 re-review as a non-blocking YELLOW. LOW priority (comment-only, no behaviour change). | OPEN | agent | ready |  |  |  |  |
| <a id="i359"></a>**i359** | **Disk-image catalogue + store all built .mgt images as Trinity records (with in-repo record->disk mapping)** — Pete (2026-07-04), TOP PRIORITY. Three parts: (1) A single high-level catalogue README (docs/disk-catalogue.md) of EVERY .mgt disk image the build produces (~27). Per disk: what it IS, its PURPOSE, which source .asm dirs/files it is composed of, the Makefile target that builds it, HOW it is run/booted (AUTO BASIC / CODE-auto / record-boot / SimCoupe), HOW to test/verify it (expected result + any CI/harness test), and its Trinity record number/name. Pete has lost the overview of where the asm code is and what the disks are - this is the map. (2) Store ALL built disk images as records on the real SAM via the hardware-proven sd_push CMD24 path (NOT q71-blocked; q71 only gates the HTTP-fetch-to-free-record leg). (3) Maintain the record->disk mapping in-repo (in the catalogue). So Pete can test/inspect all ~27. | OPEN | agent | ready |  |  |  |  |
| <a id="i359b"></a>**i359b** | **Store (sd_push) + boot-verify every built .mgt disk to a Trinity record; fill the record->disk mapping** — Store the built .mgt disks as Trinity records + fill the record->disk mapping. SCOPE REFINED by the i359a catalogue (docs/disk-catalogue.md): of the 27 disks in build/, MOST are NOT record-worthy — the 12 per-fixture corpus disks (inst_*/dir_*/expr_*/empty/set_neg_highword) are transient test residue regenerated each test-run, and okstub/dbgstub/keyin-proof are deletable i12b one-offs (no Makefile target). PRODUCT disks warranting a permanent bootable record: test_record.mgt (rec 186, DONE), netboot_server_record.mgt (187, DONE), netboot_serve_record.mgt (145, DONE), release.mgt (no -code-auto record vessel built yet). Disassembler+editor are NOT standalone disks (disasm ships as the d15 payload inside the assembler disk; editor = font-proof prototype) so there is nothing to push for them. NET new push is small. AWAITING PETE STEER (coverage): only the product disks, or a BROADER set (incl. corpus) pushed for on-hardware test coverage (the i319b per-image-boot goal)? SAFE sub-step now: run list-records (read-only) against the live SAM (reachable 2026-07-04) to CONFIRM the record->disk mapping (documented->confirmed) in the catalogue. Bulk unattended pushing to the shared card deliberately NOT done unattended (no-retransmit i348 fragility + shared-resource discipline). | OPEN | agent | 🔒 q73 |  | q73 |  |  |
| <a id="i361"></a>**i361** | **Local incremental build: TestBootRecordAssemblerRecordVessel fails on stale build/ (enctab.enc vs vessel out of sync); CI-green** — On the Pi host, TestBootRecordAssemblerRecordVessel (the i319b-b2 gate) fails locally: 'enctab.enc page 4 bytes differ ... got &40 want &45 — the boot's HLOAD from the record did not deliver it' (the boot otherwise reaches main_assemble, finalPC=&9D21 reachedStop). FAILS on clean origin/main (proven by stash tests in the i357 AND i360 sessions), UNRELATED to any source change. CI is GREEN — netboot-z80 ran 2m26s->success on #851's run 28694694321 and #850's merge run — so this is LOCAL-ENVIRONMENT-ONLY, i352-class (but a different mechanism: i352 = SimCoupe emulator; this = the Go koron harness + the pyz80 disk-record build). UPDATE (i360 session, 2026-07-04): 'make -B disk-record' (a FORCED full rebuild of the entire vessel chain) STILL fails identically — so this is NOT the incremental-staleness / missing-Makefile-prereq hypothesis from the i357 partial diagnosis (a forced rebuild would clear that). A fresh local build genuinely produces a malformed vessel; a prior-session test_record.mgt (built when the env worked) passed. NEW PRIME SUSPECT: this host's pyz80 differs from CI's pinned pyz80 (CI: pip install git+github.com/simonowen/pyz80), so disk-record assembles a subtly-different/wrong vessel locally (pyz80_packaging asserts byte-identical output only for a matched version). NEXT: diff the locally-built build/test_record.mgt page-4 payload vs build/enctab.enc byte-by-byte to see WHAT is delivered (&40...); compare local pyz80 version/commit vs CI's; if they differ, pin the local pyz80 and re-test. Impairs local dev/verification of the disk-record/vessel path; not a CI gate. Low priority; saves the next local session the re-diagnosis. | OPEN | agent | ready |  |  |  |  |
| <a id="i366"></a>**i366** | **registry split: repoint parent's dependents onto the umbrella, not a child leaf** — When 'build/registry split' promotes a leaf to an umbrella, it rewrites the parent's dependents onto a CHILD leaf. That recreates the i87b done-leaf trap the CLI warns about — a dependent left pointing at a child that becomes DONE reads as satisfied while an OPEN sibling is its real prerequisite, so it surfaces at the ready tip prematurely. Hit 3x in the 2026-07-04 i365c session (i365e->i365d-b1, i365d-b2->i365c-b1, i365d->i365c-b1), each needing a manual dep rm/dep add to repoint onto the umbrella. Fix: split should repoint each of the parent's existing dependents onto the UMBRELLA (which stays OPEN until all children DONE) by default. Low priority; from ~/claude-retro/2026-07-04-i365c-largefile-serve-and-split.md. | OPEN | agent | ready |  |  |  |  |
| <a id="i367"></a>**i367** | **TestBootRecordAssemblerRecordVessel fails on private rig: page-4 enctab byte-compare &40≠&45** — Pre-existing failure surfaced during i365d-b2b (2026-07-05). TestBootRecordAssemblerRecordVessel (tools/netboot-oracle/z80/assembler_record_vessel_test.go) FAILS on the private-capture rig: it boots the BUILD_TESTS AUTOasm record vessel green and reaches main_assemble (all boot self-tests pass), but its post-boot assertion that runtime physical page 4 byte-0 equals build/enctab.enc byte-0 fails — got &40, want &45 ('E', the ENC1 magic). i.e. load_enctab does not leave the raw enctab.enc header resident at page-4 offset 0 (it HLOADs then processes the DIFA header), yet the test compares page-4[0:len] to the raw file. Since the assembler boots green + reaches main_assemble, enctab is functionally loaded fine; only the raw-byte assertion is wrong (or the ENC1 header format post-dates the assertion). SKIP_PRIVATE_TESTS-gated so it SKIPS in public CI (main shows green without ever running it) — a latent failure. Confirmed pre-existing: assembler_record_vessel_test.go, src/loader.asm, src/assembler.asm byte-identical to origin/main; a full 'make netboot-z80-artifacts' fresh rebuild still fails. Orthogonal to i365d-b2b (which passes). Fix: correct the test's expected page-4 layout to match what load_enctab leaves (skip/adjust for the ENC1/DIFA header), or confirm+fix a real HLOAD-placement bug. Likely applies to the zx013 page-13 compare in the same test too. | OPEN | agent | ready |  |  |  |  |
| <a id="i368"></a>**i368** | **i365d-b2c demo: on-screen RST&10 progress messages ('Generating source…' / 'Generating image…')** — The one sub-piece of i365d-b2c deferred from the emulation-green capstone: print RST&10 progress banners between the assemble/render/serve phases (mgt_screen_demo_standalone idiom). Deferred because RST&10 hangs at come-up under an ALHK record boot (b2a durable-truth #5) and there is no screen to observe in the faithful harness — so it belongs with the on-hardware run (i365e) where a real screen exists. The capstone demo itself (assemble→render→serve, both files byte-exact over TFTP) is DONE + faithful-gated without messages. Add the banners during/after i365e, verifying on real hardware that a post-come-up RST&10 between phases does not wedge. | OPEN | agent | ready |  | i365e |  |  |
| <a id="i369"></a>**i369** | **netboot-oracle z80 tests: relocate remaining hardcoded high (>=0x8000) staging buffers to low RAM (i313 audit)** — i313's investigation audited the z80 test scratch buffers and found ~8 more hardcoded >=0x8000 staging constants beyond the fixed rrsScratch: claimNameScratch=&C000 + claimNameScratchReal=&D000 (bdos_claim_name_test.go), nbSrcOrg=&E000 (netboot_server_test.go), demoSrcOrgA=&C000 + demoSrcOrgB=&D000 (netboot_serve_test.go), spScratch=&F000 (spill_test.go), shaInputStage=&A000 (sha256_test.go), srcOrg=&C000 (tftp_server_loop_test.go). Each is a latent collision of the i313/i231b-b4g class: it only bites when the test's org-&8000 binary GROWS UP into that window. The full suite currently passes (none collide at present sizes), so this is PREVENTIVE hardening, not a live bug. NUANCE: some mirror an ASM equ, not a free test choice (e.g. csdProbeStage=&C000 mirrors 'STAGE equ &C000' in the asm — cannot be moved test-side without the asm). So relocate CASE-BY-CASE: the test-chosen ones to low RAM (like rrsScratch), leaving equ-mirrors as-is (or move the equ too if warranted). Low priority — do opportunistically or when a binary approaches its buffer. | OPEN | agent | ready |  |  |  |  |
| <a id="i371"></a>**i371** | **End-to-end real-HLOAD .include exercise under SimCoupé — bind READER_VEC=prep_sam_reader in the paged prep build + disk include-file wiring** — The 'exercise the real-HLOAD include path under SimCoupe with real DOS (a disk image carrying include files)' half of the original i31b-b3b, split out under the id depth limit. Bind READER_VEC=prep_sam_reader (i31b-b3b) in the paged prep image (i370) so both the memory provider (b3a) and the real reader get emulator coverage — no HOSTTEST carve-outs (discipline rule 7 / i231). Add an include file to the test disk via build-disk (tools/build-disk/main.go AddCodeFile, pattern of the 'IN' file at main.go:508) behind a new flag + the Makefile disk target; register it as a NamedFile in the SAM-fidelity harness. Exercise a source with .include resolving to the on-disk file, asserting the expansion end-to-end under SimCoupe (the CI gate). Depends on i31b-b3b (reader routine) + i370 (paged prep harness). | OPEN | agent | ready |  | i31b-b3b, i370 |  |  |

## Tree view — umbrella hierarchy

- [i3](#i3) — `OPEN` — Editor groundwork (Phase 2) — full vision
  - [i43](#i43) — `OPEN` — Editor: register simulator with user-chosen seeds + replay-on-edit
  - [i44](#i44) — `OPEN` — Editor: inline instruction-explanation panel (prose semantics, flags, regs, bit-field layout)
  - [i45](#i45) — `OPEN` — Editor: 'did you mean a simpler instruction?' result-equivalent rewrite hint
  - [i46](#i46) — `OPEN` — Editor: system-register documentation panel (ARM MRA subset + editorial overlay)
  - [i47](#i47) — `OPEN` — Editor: inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations)
- [i31](#i31) — `OPEN` — On-SAM preprocessor (.if build-constraints + macros)
  - [i31b](#i31b) — `OPEN` — On-SAM preprocessor — implementation (Z80 port of preprocess.go per the approved spec)
    - [i31b-b4](#i31b-b4) — `OPEN` — Brick 4: prep_run wiring in front of b8d chain + corpus gate
      - [i31b-b4c](#i31b-b4c) — `OPEN` — Brick 4c: PP_PREP i2-pool tag + right-size paged prep buffers + spectrum4 corpus slice + corpus gate
      - [i31b-b4d](#i31b-b4d) — `OPEN` — Brick 4d: SimCoupé pre-merge gate incl. real-HLOAD include path
- [i41](#i41) — `OPEN` — Editor edit-buffer data structure — insertion performance for large source
  - [i41e](#i41e) — `OPEN` — Editor edit-model — i48-IR record payload + document symbol table + hybrid record model
- [i70](#i70) — `OPEN` — Phase-3 firmware self-provisioning: SAM fetches Pi firmware blobs via HTTP and persists to Trinity storage
  - [i70c](#i70c) — `OPEN` — Directory-safe multi-blob persist + full 6-file cdn manifest on hardware (start.elf-scale spanning)
    - [i70c-b2](#i70c-b2) — `OPEN` — Directory-safe spanning impl + full 6-file cdn manifest hardware shot
- [i81](#i81) — `OPEN` — Upstream SimCoupé issue triage — review open issues and assist where we can
  - [i81a](#i81a) — `OPEN` — PNG-endian one-line upstream SimCoupé PR: the __aarch64__ little-endian-guard fix in Base/SimCoupe.h
  - [i81c](#i81c) — `OPEN` — Optionally offer the SDL dummy-driver headless renderer fallback upstream
- [i88](#i88) — `OPEN` — On-SAM HTTPS/TLS-1.3 client to fetch Pi firmware directly from GitHub
  - [i88c](#i88c) — `OPEN` — i88 6b — hardware-gated integration: wire tls_reasm into tcp_conn, paging layout, bootable disk, real RST 8 on Trinity
    - [i88c-b2](#i88c-b2) — `OPEN` — i88c-b2 — grow TC_RX 1056->16645 + finalise paged buffer layout for full-size TLS records
    - [i88c-b3](#i88c-b3) — `OPEN` — i88c-b3 — bootable TLS disk: tls_main.asm as NETBOOT_STREAM boot binary + make target + build-disk wiring
    - [i88c-b4](#i88c-b4) — `OPEN` — i88c-b4 — real RST-8 TLS shot on Trinity hardware (hardware-gated)
- [i89](#i89) — `OPEN` — Capture a Pi 3 netboot exchange (Phase-3) — extend i83/i86 oracle to the older Pi family
  - [i89b](#i89b) — `OPEN` — Capture a real Pi 3 netboot exchange + generate Pi 3 golden wire vectors
- [i95](#i95) — `OPEN` — Phase-3 netboot integrated server — single dispatch loop (ARP+DHCP+TFTP)
  - [i95b](#i95b) — `OPEN` — End-to-end Pi boot on real Trinity — hardware-gated: DHCP + B-DOS provisioning + real Pi boot
    - [i95b-b2](#i95b-b2) — `OPEN` — Real Pi netboots from the SAM end-to-end (rig-gated): DHCP + TFTP boot of the physical target Pi
- [i100](#i100) — `OPEN` — Firmware-download provisioning UX — user-selectable revision and file subset
  - [i100c](#i100c) — `OPEN` — Picker UX — user-facing revision/file-selection interface for the firmware downloader
- [i117](#i117) — `OPEN` — Extract sam-aarch64 learnings into a reusable open-source autonomous Claude project framework
  - [i117a](#i117a) — `OPEN` — Prior-art survey: existing Claude Code autonomous-orchestration plugins/frameworks vs i117
- [i118](#i118) — `OPEN` — i82 TFTP client — OACK handling / standard-TFTP-server interop
  - [i118b](#i118b) — `OPEN` — hardware verification — OACK path on real Trinity hardware
- [i119](#i119) — `OPEN` — i82 TFTP client — persistence write-out on real hardware
  - [i143](#i143) — `OPEN` — bdos_picker: hardware MODE-2 blit for picker_render (follow-up from i119d)
- [i123](#i123) — `OPEN` — Investigate migrating the build from make to tup
  - [i123b](#i123b) — `OPEN` — Perform the make->tup build migration (Pete-greenlit)
- [i182](#i182) — `OPEN` — Bootable fetch-and-boot disk + real-Trinity run of client_fetch_boot
  - [i182b](#i182b) — `OPEN` — Real-Trinity run of client_fetch_boot via the fetch-and-boot disk
- [i215](#i215) — `OPEN` — IDE page-persistence (spill): lazy-spill cold DOC blocks to Trinity or floppy/tape when the pool is full
  - [i215c](#i215c) — `OPEN` — Spill manager: concrete pluggable backends (Trinity record / floppy-tape) + boot wiring
- [i264](#i264) — `OPEN` — SAMBOOT selector firmware: hold-key-at-boot launches a record picker (choose boot record by number/name) instead of hardcoded record 3
  - [i264b](#i264b) — `OPEN` — Record picker UI (Increment 2): list available records, select by number/name, persist the SAMBOOT Config chunk
- [i291](#i291) — `OPEN` — Measure the real ENC28J60/shared-PIC settle time on hardware (unattended ethernet-reporting bisection) to replace the conservative 1200-T-state emulator constant
  - [i291b](#i291b) — `OPEN` — Measure real ENC28J60/shared-PIC settle time on hardware (unattended UDP bisection); set the emulator constant
    - [i291b-b2](#i291b-b2) — `OPEN` — Run the hardware settle-time bisection: sweep N via the probe, capture UDP replies, converge on the real ENC/PIC settle, set sdInitSettleTStates=measured+margin
    - [i291b-b4](#i291b-b4) — `OPEN` — Bound the remaining unbounded ENC MII busy-polls (rphy_wait, wr_phy_wait) for the MII-wedge / drv_wait_link hang class
    - [i291b-b5](#i291b-b5) — `OPEN` — settle_probe SATR never egresses on hardware (test_report TX fails while &38 SD-init holds shared &DC); make the probe report before b2's bisection can run
- [i304](#i304) — `OPEN` — Reconstruct a labelled B-DOS 1.5t source from 1.5a + reassemble to byte-match
  - [i304b](#i304b) — `OPEN` — Splice 1.5t device-layer diffs into the 1.5a pyz80 source to byte-match bdos15t-beta6.bin
  - [i304c](#i304c) — `OPEN` — Annotate 1.5t routines; make bdos15t src THE cited Trinity-HW B-DOS authority + update citations
- [i311](#i311) — `OPEN` — Retire the AttachBDOS Go mock behind a shrink-to-zero ledger, now the faithful B-DOS rig has landed
  - [i311b](#i311b) — `OPEN` — Migrate AttachBDOS mock tests to the faithful B-DOS rig; ratchet the ledger to zero
- [i359](#i359) — `OPEN` — Disk-image catalogue + store all built .mgt images as Trinity records (with in-repo record->disk mapping)
  - [i359b](#i359b) — `OPEN` — Store (sd_push) + boot-verify every built .mgt disk to a Trinity record; fill the record->disk mapping
