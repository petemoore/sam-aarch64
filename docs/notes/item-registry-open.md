# Item index — open items (`iN` registry, open half)

This is the **open half** of the canonical, milestone-neutral `iN` registry. A
fresh agent reads **only this file** to see all outstanding work — every row here
is unresolved. When an item is resolved, its row **moves** to
`item-registry-closed.md` **in the same PR that resolves it** (mirroring "delete
the plan in the completing PR"). Ids are **globally unique** across both halves
and are **never reused**.

The companion registry for open questions is `docs/notes/question-registry-open.md`.

## Id conventions

Every tracked item has a stable **`iN`** id (`i` = item). Rules: (1) once an id
appears in a PR title, branch, or commit it is **locked** — never renumber it;
(2) sub-items take letter suffixes (`i12a`/`i12b`/`i12c`); (3) a new item gets
the next free integer; (4) reference items by id in conversation, PRs, and these
docs. (`i13` is locked to "gitignore" to match shipped PR #107, so the "replace
`cls`" item — tentatively i13 in conversation — is registered as `i16`.)

## Controlled status vocabulary

Every status cell starts with exactly one token:
- `OPEN` — outstanding, no blocker
- `BLOCKED:<what>` — outstanding but gated on the named prerequisite or person

## Atomic items

One row = one deliverable = one status. Rows that bundle multiple independently-
completable deliverables are split into letter sub-ids (the existing `i12a`/`i48a`
convention) so no outstanding sub-part hides in a done-looking row.

## Two registries, one discipline (read this for *both* items and questions)

There are **two sibling registries**, and they follow the **same governance** — so
learning one teaches the other:

- **Items** → `item-registry-open.md` (this file) + `item-registry-closed.md`, ids **`iN`** — tracked *work*.
- **Questions for Pete** → `docs/notes/question-registry-open.md` + `question-registry-closed.md`, ids **`qN`** —
  *unresolved decisions* (things an agent can't settle itself, or defers rather than
  guessing). Every open question goes there the moment it arises — not just in chat
  (chat questions get lost in simultaneous-edit races); the registry is the single
  sure-fire list of what's still open.

Shared rules for **both** `iN` and `qN`: (1) **stable ids, locked** once they appear
in a PR/branch/commit — never renumbered; (2) **milestone-neutral** — when a milestone
status doc is superseded, its items *and* questions live on in their registry, never
stranded in an archived `m{N}-status` doc; (3) **marked done/resolved by moving to
the closed file**, so the id is never reused; (4) referenced by id everywhere; (5)
**landed on `main`** via PR (branch-protection forbids direct pushes) so Pete sees
them where he reads.

| id | item | status | pointer |
|----|------|--------|---------|
| **i2** | On-SAM IDE memory model (edit buffer + IN/OUT paging; "claim all free RAM, grow on demand") | OPEN — reframed; deferred to editor work | scope row "IN/OUT paged-buffer ceiling" |
| **i3** | Editor groundwork (Phase 2) — full vision | OPEN — 🧭 | ROADMAP "Editor vision" |
| **i4** | Basic read-only listing/scroll viewer (centre-locked cursor; up/down only) | OPEN — 🧭 carried forward by **i76 P1** (the host prototype viewer is the functional authority the SAM i4 viewer ports) | i76; `docs/specs/editor-tui-prototype-design.md` §5 |
| **i5** | UI visual prototyping via image generation (MODE 3 64×24 vs MODE 4 32×24 mockups) | OPEN — 🧭 carried forward by the **i76 mockup backend** (PNG/SCREEN$ at exact mode geometry — q1 resolved to a programmatic renderer) | i76; `docs/specs/editor-tui-prototype-design.md` §2.4; q1 |
| **i6** | SAM screen-mode decision (MODE 3 vs 4, or user preference) | OPEN — 🧭 decided empirically via **i76** (geometry flag + font sheets + P3 memo) | i76; `docs/specs/editor-tui-prototype-design.md` §3, §5; ROADMAP "Editor vision" |
| **i7** | Codegen sysreg/mnemonic/form tables from Go authority | OPEN — 🔨 **in progress** (spec approved PR #184) — **phases A + B + C1 + C2 landed**: A deleted ENCTAB_LEN (DIFA-read refactor); B renamed `enctab-gen`→`tables-gen`, added the `-sysreg-inc` emitter + the `make tables` / `tables-sync-check` freshness gate (in CI job `sysreg-sync`), and `src/sysreg_tables.inc` is now generated from `sysregs.go` (name-sorted, byte-neutral); **C1** added the `-constants-inc` emitter and `src/tbn_constants.inc` (the `OP_KIND_*`/`REC_KIND_*`/`DIR_*` equates, `REC_KIND_*` a 6-of-9 subset) is generated from `operands.go`/`kinds.go`/`directives.go`; **C2** added the `-mnemonic-ids-inc` emitter (`tools/tables-gen/emit/mnemonicids.go`) and `src/mnemonic_ids.inc` (the `MNEM_<NAME>` mnemonic-ID equates) is generated from `mnemonics.go` — every bare-numeric `cp <id>` dispatch site in `src/intercepts.asm` (the top dispatcher + `is_mem_mnemonic_id` + `is_shifted_reg_mnemonic` + the barrier/lslsr/bitfield/tbz encoders) now names its mnemonic; `assembler{,-prod}.bin` byte-identical throughout; `TestTbnConstantsZ80Sync` + `TestMnemonicIDsZ80Sync` + the broadened `tables-sync-check` guard all three `.inc` files. **All of phase C is done**; the only remaining phase is D = i74 (gated on the `aarch64dec/sys.go` code→data refactor). | `docs/specs/codegen-tables-design.md`; `docs/notes/m9-status.md`; i74 |
| **i12b** | Editor-testing input injection (`-keyin` vs `FLAGS`/`LASTK` memory injection) | OPEN — 🧭 new 2026-06-08 | `tools/run-simcoupe.sh`; for automated editor tests |
| **i15** | `adds` 3-register form (Z80) | OPEN — 🧭 | deferred-backlog row |
| **i16** | Replace `cls` test instruction with a real spectrum4 one | OPEN — 🧭 (was tentatively "i13" in conversation) | `docs/ROADMAP.md` deferred checklist |
| **i19** | Subagent worktree-isolation leak — harden | OPEN — 🧭 | scope row |
| **i20** | Linker-layout coupling (`spectrum4.ld` hardcoded in flatten) | OPEN — 🧭 | scope row |
| **i21** | Go-harness fidelity follow-ups (watchpoint, `make harness-sweep`, USAGE.md) | OPEN — ⏳ | scope row |
| **i22** | Per-fail-site diagnostic strings | OPEN — 🧭 | deferred-backlog row |
| **i23** | Paged-IN >16.5 KB HLOAD-ceiling lift | OPEN — 🧭 | deferred-backlog row |
| **i24** | 64 KB output / 16-bit `OUT_LEN` limit | OPEN — 🧭 | deferred-backlog row |
| **i25** | `(hksp)` HSAVE/HLOAD error handler | OPEN — 🧭 | deferred-backlog row |
| **i26** | text2bin operand-kind validation (Task 21) | OPEN — 🧭 | deferred-backlog row |
| **i27** | Cortex-A53 errata workarounds (`--fix-cortex-a53-*`) | OPEN — 🧭 | deferred-backlog row |
| **i28** | Absolute `.set` high-word edge vs Go | OPEN — 🧭 | deferred-backlog row |
| **i29** | Harden slot self-test `PASS_PC` dependency | OPEN — 🧭 | deferred-backlog row |
| **i30** | LDIR-fan-out for cross-page-shared blocks (smaller binary + faster boot) | OPEN — 🧭 deferred/possible | `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-07-strand-b-pr4-z80-disassembler-port.md` "Future enhancements" |
| **i31** | On-SAM preprocessor (`.if` build-constraints + macros) | OPEN — 🧭 beyond-M7 | "Beyond-M7 / future ideas" |
| **i32** | Multiple source files + staged/partial loading | OPEN — 🧭 beyond-M7 | "Beyond-M7 / future ideas" |
| **i33** | Trinity SD/flash storage → bigger-kernel architecture | OPEN — 🧭 beyond-M7 | `memory/trinity_hardware.md` |
| **i39c** | Phase 3 — bitfield-packing polish on the overlay slot bytes. Migrated to M9 at M8 close (2026-06-12); folds into the i48c overlay-encoder work. | OPEN — 🧭 designed (low priority) — M9 strand | design §3.1; `docs/notes/m9-status.md`; i48c |
| **i41** | Editor edit-buffer data structure — insertion performance for large source. A naive contiguous edit buffer (mutating the `.tbn` in place) requires shifting all bytes after an insert; at ~400 KB on the SAM Z80 that is ~1.5–2+ s (before paging overhead), exceeding the ~1 s/edit bound. **Resolution is architectural:** the `.tbn` is the *serialized* storage/assembly form (offset/PC-based, contiguous); the editor holds the source in a separate insertion-friendly in-memory document model (options: gap buffer / piece table / record linked-list) and serializes to `.tbn` on save/assemble — edits never shift the `.tbn`, and offsets/PCs are recomputed in one O(n) pass on serialize, not per keystroke. Comments/symbols are anchored by record-reference (or a stable record-id), NOT by byte-offset, so an insert touches only local nodes (no global reindex). Capture now; full edit-model design at editor time (Phase 2). Does NOT change i39 (i39 is the storage/assembly format). Cross-links: i39, i40, i3/i4 (editor). **Design AGREED (opus, 2026-06-08):** **paged block-list** (block = page, intra-block gap buffer, record-id side-tables) — worst-case edit ~30–80 ms at 400 KB, every op 0–2 page swaps. **5 decisions (§7):** ½-page blocks · bounded ring-journal undo · **edit-model records = the i48 in-memory symbolic IR** (pre-lexed/parsed → validate-on-entry, canonical layout, compact via interned names; active line raw-until-commit, raw/error record for invalid lines) · build the block-list directly (clean serialize seam, no throwaway MVP) · u24 no-reuse record-ids. Unifies the editor document model with i48's IR. | OPEN — 📋 **M9 strand** — design agreed, 5 decisions recorded (Pete 2026-06-08); implementation begins in M9 (the editor era) | `docs/specs/editor-edit-model-design.md` (design §7); `docs/notes/m9-status.md`; ROADMAP "Editor vision"; cross-links i39, i40, i3, i4, i48 |
| **i42** | CRC / signature in the `.tbn` file header (integrity check) | OPEN — 🧭 (deferred since M1; the v1 header has magic/version/flags but no integrity field) | `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/m1-status.md` "Known gaps" (CRC / signature in the file header) |
| **i43** | Editor: register simulator with user-chosen seeds (step instructions; inject/randomise source regs; show dest regs/flags/memory) + replay-on-edit | OPEN — 🧭 Phase-2 editor sub-feature | `docs/ROADMAP.md` "Editor vision"; sub-feature of i3 |
| **i44** | Editor: inline instruction-explanation panel (prose semantics, flags set, regs read/written, bit-field layout) | OPEN — 🧭 Phase-2 editor sub-feature | `docs/ROADMAP.md` "Editor vision"; sub-feature of i3 |
| **i45** | Editor: "did you mean a simpler instruction?" result-equivalent rewrite hint | OPEN — 🧭 Phase-2 editor sub-feature | `docs/ROADMAP.md` "Editor vision"; sub-feature of i3 |
| **i46** | Editor: system-register documentation panel surfaced from the cursor line (embed ARM MRA subset + editorial overlay) | OPEN — 🧭 Phase-2 editor sub-feature | `docs/ROADMAP.md` "Editor vision"; sub-feature of i3 |
| **i47** | Editor: "why is this instruction here?" inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations) | OPEN — 🧭 Phase-2 editor sub-features | `docs/ROADMAP.md` "Editor vision"; sub-features of i3 |
| **i48c** | Z80 **text→overlay encoder** (editor input path) — the SAM-side mirror of the host front-end; Go i48b is the authority. Absorbs i39c (overlay slot bitfield-packing polish). | OPEN — 🧭 **M9 strand (editor era)** | i48 spec §2, §7; `docs/notes/m9-status.md`; i39c |
| **i50** | Host Go-tool CLI shape — *deferred design question*. Whether the host tools stay one format-aware front door (`sam-aarch64`, auto-detects the `SA64` magic), revert to three binaries (encode / decode / translate), or adopt a pandoc-style `--from/--to`. The three *concerns* are already cleanly separated as **libraries** (`aarch64enc` / `aarch64dec` / `sam-aarch64-format`+`frontend`); CLI count is a thin presentation choice, **orthogonal** to the i48 in-memory-only-symbolic-form win (three front doors would NOT reintroduce that confusion). | OPEN — 🧭 **parked** (Pete 2026-06-09) — low-stakes: the Go tools are Z80-port scaffolding + oracle (memory `project_go_tools_are_z80_scaffolding`), so aim the "easy/clean" effort at the SAM-side product + non-ported host features (bulk GNU→`.tbn` import, listings), not Go-CLI polish. Revisit when those host features land. | this session's brainstorm; supersedes the earlier "roll the `aarch64dec` CLI into `sam-aarch64`" framing |
| **i55** | Multi-section / linker-script honouring in `refenc` — explicitly punted in favour of the `text2bin -flatten` approach. Revisit only if a non-spectrum4-shaped project ever needs it. | OPEN — 🧭 deferred | ROADMAP deferred checklist (retired i52 PR 3) |
| **i56** | `SpectrumFourLayout` extraction (`-layout` flag or linker-script parser) — not worth doing unless a second project surfaces. | OPEN — 🧭 deferred — Pete's call | ROADMAP deferred checklist (retired i52 PR 3) |
| **i65** | SAM-side save-without-comments option (editor-phase): saving a `.tbn` with the editor region stripped/thinned, so a 256 KB SAM can assemble sources authored on a 512 KB machine. Host equivalent exists (`-strip-comments`/`-thin-comments`); this is the on-SAM counterpart. Per q9. | OPEN — 🧭 editor phase | q9, i59 |
| **i66** | Trinity page-persistence as a general "persist pages to mass storage" technique (via B-DOS records per `bdos-version-landscape.md`). Deferred by decision — revisit only if a concrete need appears (q9: "no value implementing right now"). | OPEN — 🧭 deferred (Pete 2026-06-10) | q9; `docs/notes/bdos-version-landscape.md` |
| **i69** | Test-memory architecture: relieve the section-C test-variant squeeze (86 B headroom post-i68) using the principle that **tests need not coexist in memory** (Pete, 2026-06-11). Levers in order: (1) evict the remaining in-section suites off-axis (test_symbols, test_expr_eval, test_local_labels, test_emit_paged, test_reader_paged, test_sysreg_paged, test_trampoline, test_paged_call — the PR #52 pattern); (2) a serial "test bay": one page through which suites stream (load → run → load next over it, same boot — the page-13 time-multiplex generalized); (3) chunked payloads across more pages/disk; (4) multiple boots with different disks (CI wall-clock cost). Disk and total RAM are not scarce — only the section-C resident window is. | OPEN — ⏳ **Lever 1 partially applied (PR #180):** test_symbols (~247 B) + test_local_labels (~522 B) evicted from the main binary to the off-axis page-12 cluster; test variant code_end `&BFAA` → `&BCA3` (86 B → 861 B headroom). Remaining levers (test_emit_paged, test_reader_paged, test_sysreg_paged, test_trampoline, test_paged_call, and the serial test-bay / multi-page options) stand ready for the next pressure event. | i68 watch-item; `src/README.md` off-axis pattern |
| **i70** | Phase-3 firmware self-provisioning (Pete, 2026-06-11): PXE-booting a Pi needs the SAM to serve Raspberry Pi firmware blobs (several MB — fits Trinity SD), not just the kernel. Rather than requiring newcomers to pre-populate Trinity storage — non-trivial since B-DOS media is raw records, not FAT, so a PC can't simply drop files on the SD — the SAM's IP stack could speak HTTP or FTP, fetch firmware from an online location once, write it to Trinity storage (B-DOS hooks), then stream it over TFTP at boot time. Natural base: the uIP TCP/IP stack found on the corpus Trinity utility disk (ARP/IPv4/TCP; trinload's stack is UDP-only). Alternative worth weighing at design time: a host-side tool that writes B-DOS records to SD from a PC. | OPEN — 🔨 **in progress** — Phase-3, **route decided** (Pete 2026-06-14, q11/q12): **on-SAM HTTP fetch via uIP** — the SAM pulls Pi firmware from the internet itself (one-time/rare; firmware is stable per chosen revision), staying host-tool-free; the host-side-tool alternative is **rejected**. Needs uIP (ARP/IPv4/TCP, corpus Trinity disk, i61) + a minimal HTTP client. **Brick 1 landed:** the **TCP transport** (the rest of the stack is UDP-only) — Go authority `tcp/tcp.go::BuildSegment`/`ParseSegment` + the Z80 port `src/netboot/build_tcp_segment.asm` (`build_tcp_segment`), host-verified byte-for-byte over the koron-go/z80 harness incl. the mandatory pseudo-header checksum (the one arithmetic UDP did not need; cross-checked vs Python). **Brick 2 landed:** the **TCP connection state machine** (client active open) — Go authority `tcp/conn.go::Conn` (`Connect`/`OnSegment`: SYN→SYN-ACK→ACK handshake, 32-bit seq/ack tracking, data ACK cadence, server-initiated FIN teardown) + the Z80 port `src/netboot/tcp_conn.asm` (`tcp_connect`/`tcp_conn_recv`, with `tcp_inc32`/`tcp_add16_to_rcvnxt`/`tcp_cmp32` — the new 32-bit arithmetic vs the 16-bit UDP loops), host-verified over the i80 emulation byte-for-byte vs the Go authority (`tcp_conn_test.go`: SYN + handshake + the full SYN→data×2→FIN lifecycle with accumulated body + wrong-port/out-of-order/empty-wire controls; `make netboot-tcp-conn`, the `netboot-z80` CI job). Connection management only — it ACKs inbound data but originates none. **Brick 3 landed:** the **minimal HTTP/1.0 GET client** (rides the connection) — Go authority `http/http.go` (`BuildRequest` the `GET … HTTP/1.0\r\nHost: …\r\n\r\n` bytes + `ParseResponse` status-code/body-offset + a thin `Client` over `tcp.Conn`; `tcp/conn.go` gained `Conn.Send` to push the request as a PSH|ACK data segment) + the Z80 port `src/netboot/http_get.asm` (`http_get_start` builds+sends the request; `http_parse_response` parses the accumulated `CONN_DATA`), host-verified over the i80 emulation byte-for-byte vs the Go authority (`http_get_test.go`: handshake → GET segment on the wire == `Client.Start` + a streamed two-segment response with each ACK byte-exact + the parse vs `ParseResponse`; plus direct parser vectors 200/404/empty/incomplete/no-status-line; `make netboot-http-get`, the `netboot-z80` CI job). HTTP/1.0 so the server closes after the response (the FIN is the end-of-body signal — no chunked/keep-alive). **Brick 4 (Go authority) landed:** the **integrated HTTP fetch phase machine** `http/fetcher.go::Fetcher` — the HTTP analogue of the TFTP `client.Client`, composing the host-verified pieces (`frame` ARP + `tcp.Conn` + `http` GET/parse) into the full self-provision flow: `First()` broadcasts the ARP, `OnFrame(rx)` drives PhaseARP (ARP reply → learn server MAC → SYN) → PhaseHandshake (SYN-ACK → one ACK+GET segment, the handshake-completing ACK carrying the GET payload per RFC 793) → PhaseRecv (response DATA → ACK + accumulate; server FIN → FIN-ACK + done), then `Bytes()`/`Response()`. Go-tested (`fetcher_test.go`: full fetch + stray-ARP / bad-SYN-ACK / FIN-with-data controls). **Brick 5 (Z80 capstone) landed:** the **Z80 port** `src/netboot/netboot_http.asm` (`http_fetch_first`/`http_fetch_onframe`) — the integrated fetch phase machine, a mechanical port of `Fetcher`: it `include`s `http_get.asm` whole (→ `tcp_conn` + `build_tcp_segment` + `encdrv`) plus `build_arp_request`, each phase doing its own `drv_read` so PH_RECV hands the step straight to the proven `tcp_conn_recv`; the PH_HANDSHAKE step validates the SYN-ACK inline (mirroring `conn_syn_sent`) then `http_get_start` sends the handshake-completing ACK+GET as one segment. Host-verified over the i80 emulation byte-for-byte vs `http/fetcher.go::Fetcher` (`netboot_http_test.go`: the full ARP→SYN→ACK+GET→response→FIN fetch with the server-MAC learned + the accumulated body + the parse, plus stray-ARP control; `make netboot-http`, the `netboot-z80` CI job). **Brick 6 (the capstone) landed:** the **bootable HTTP-fetch disk + the B-DOS write-out**, in the same `src/netboot/netboot_http.asm` — `http_main` (behind `NETBOOT_HOSTTEST==0`, mirroring `client_main` line-for-line for the hardware paths): reads the SAM's MAC/IP from the Trinity EEPROM (`eeprom.asm`), fills the connection config (client port `&C000`, a fixed firmware-server IP `ht_server_ip`, server port 80, a fixed ISS), inits the ENC, runs `http_fetch_first` then loops `http_fetch_onframe` until `FETCH_PHASE == PH_DONE`, calls `http_parse_response`, copies the body (`CONN_DATA + HTTP_BODY_OFF`, length `CONN_DATA_LEN - HTTP_BODY_OFF`) into a section-C `STAGING` buffer, and HSAVEs it under `ht_out_name` (`firmware.bin`) via the i93 `bdos_seam` (`bdos_select_record` / `bdos_fill_save_uifa` / `bdos_save_hook`) using the **same `STAGING >> 14` / `STAGING & &3FFF | &8000` source-paging math** as `client_main`. Three Makefile rules (mirroring `netboot-client`): the host-test binary now builds with `-D NETBOOT_HOSTTEST=1` (excluding `http_main`/`eeprom`/HSAVE so the phase-machine test is unaffected), `netboot-http-boot` (bootable, no flag), `netboot-http-disk` (`build/netboot_http.mgt`, in the CI "Bootable netboot disks build" step). The **fetch loop + the body-staging + the disk build are host-verified/host-checked**; the **EEPROM read + the section-C paging + the HSAVE RST-8 dispatch are hardware-gated** (validated only on real Trinity — emulation-verified ≠ hardware-verified, CLAUDE.md §5). On-hardware run guide: `docs/notes/netboot-trinity-testing.md` "HTTP fetch". Not on the daily-loop critical path — the i82 TFTP client bridges provisioning from a standard TFTP source meanwhile. The canonical GitHub source is HTTPS-only → tracked separately as **i88** (stretch). | `docs/specs/phase3-delivery-design.md` §7; `docs/notes/trinity-capabilities.md`; `docs/notes/bdos-version-landscape.md`; `src/netboot/netboot_http.asm`; `docs/notes/netboot-trinity-testing.md`; i61, i66, i84, i88 |
| **i73** | i17 review residuals — LOW-severity hardening + dead-code finding (split out of i17, see PR #185): **L1** `emit_byte` rejects an exactly-32768-byte output (off-by-one at the buffer ceiling); **L3** `symbol_abs_bit_ptr` lacks an `id < 512` bound; **L4** exactly-full-buffer conservative rejections (litpool expr buf; reader 1024-byte staging); **L5** zero-length `LIT_INSTS` BC underflow (corrupt input only); **L6** `.align 16..31` silently no-ops (should fail); **L7** pass-1 backward-`.org` check missing (pass 2 catches it late); **L9** dead production code — `try_mnemonic_intercept` dispatch in `src/intercepts.asm` has no callers in the v2 build (only `test_litpool.asm` exercises it); a deletion candidate worth section-C bytes, own PR; **L10** u8 segment-counter wrap at 256 flushes (contrived); **L11** `load_in_file` editor-offset decode wraps on absurd offsets (hostile input); **L12** Z80 fails on `.set` redefinition where Go/GNU allow last-wins (matters when the editor makes the SAM a producer); plus **loader.asm** has the same EI-then-`call TRAMPOLINE_DST` window as load_in_file's L8 (boot has no armed interrupt source in practice — left unfixed at L8). | OPEN — 🧭 deferred — new 2026-06-12 | PR #185; i17 row |
| **i74** | i7 phase D — at/ic/barrier operand-table codegen from the Go authority (spec §6 Q3). Deferred by sequencing only (Pete 2026-06-12: tracked so it automatically gets done); queue after i7 phases A–C land. | OPEN — 📋 queued | `docs/specs/codegen-tables-design.md` §6; i7 |
| **i77** | spectrum4 source succinctification — agent-driven, section-by-section, human-reviewed (Pete 2026-06-12): (a) comments rewritten aggressively succinct but still readable; (b) pathological constant expressions replaced with named equates (e.g. the 69-col `movk w0, (65536 | (((BORDER_COLOUR >> 16) & 65535) & 65535)), lsl #16` monster — the longest instruction in the corpus). Coordinate with upstream spectrum4 (content originates there; release.s re-flattens). Ties to editor-tui spec D1 (all comments < 256 B → the u16 record length can revert to u8) and to the same-line-comment fit-rate. | OPEN — 🧭 parked (editor era) | `docs/specs/editor-tui-prototype-design.md` §8 (D1); i76 lab findings |
| **i80** | Trinity network-interface emulation — emulate the Quazar Trinity's ENC28J60 Ethernet (SPI via port `&DE`, per `trinity-capabilities.md`) so the SAM network stack (uIP / TFTP / firmware-fetch) can be iterated host-side at the project's usual fast inner-loop speed, with real Trinity hardware as the gate. **Scope grew (2026-06-14):** must carry enough of a virtual network to exercise the full netboot handshake (DHCP + TFTP) and the HTTP firmware-fetch, not just raw frames. Built the **Go-first** way (Pete 2026-06-13, prioritised): the emulation is host-side Go (the rig/authority); the real Z80 driver runs against it — turning the ENC28J60 wire I/O from hardware-gated into host-verifiable. A SimCoupé-side emulation (to iterate the *whole* SAM program, not just the driver routines) is the later/optional delivery; its upstreaming scope against Simon Owen's repo stays open. | OPEN — 🔨 **first brick LANDED** (this PR) — `tools/netboot-oracle/z80/enc28j60.go` emulates the Trinity microcontroller (`&DC` select/busy + `&DD` identity probe) + the ENC28J60 SPI chip (`&DE`: RCR/WCR/RBM/WBM/BFS/BFC, the 4 banks, the 8 KB RX/TX buffer, the MAC/MII read-dummy) per the ENC28J60 datasheet (DS39662E) + the driver's port usage; the vendored real driver `src/netboot/encdrv.asm` (simonowen/trinload, verbatim) runs against it under the koron-go/z80 harness and asserts: `drv_init` succeeds (+ a missing-board negative control), `drv_write` → frame byte-exact on the virtual wire (incl. a captured RRQ), an injected RX → `drv_read` byte-exact, and a write→loopback→read of a real DHCP frame. Wired into CI via `make ci-netboot-z80` (the `netboot-z80` job). Found + corrected (at the harness boundary) a Z80 spec-conformance bug in koron-go/z80 v0.10.2 — its INI/IND-family ops pass the B register as the port instead of C. **Emulation-verified ≠ hardware-verified**: the DHCP/TFTP transfer-loop state machines + an end-to-end Pi boot remain to do; real Trinity stays the final gate. Next: build the wire-I/O state machines (DHCP responder loop) on top of `encdrv.asm`+the emulation. | `docs/notes/trinity-capabilities.md`; `docs/specs/phase3-delivery-design.md` §9; `src/netboot/encdrv.asm`; `tools/netboot-oracle/z80/enc28j60.go`; i58, i82, i83, i86, i92 |
| **i81a** | PNG-endian one-line upstream SimCoupé PR: the `__aarch64__` little-endian-guard fix in `Base/SimCoupe.h` | BLOCKED:Pete — PNG-endian one-line upstream PR: the `__aarch64__` little-endian-guard fix in `Base/SimCoupe.h` (hunk 2 of `tools/font-proof/simcoupe-local-capture.patch`); confirmed NOT in upstream HEAD; ready to offer standalone; community submission is Pete's call. | the aarch64 endian patch (`tools/font-proof/simcoupe-local-capture.patch`); i81; i80 |
| **i81b** | Comment on upstream SimCoupé issue #28 offering our DI;HALT + BC-return + -exitonhalt idiom for pass/fail detection | BLOCKED:Pete — comment on upstream SimCoupé issue #28 (unit-test pass/fail detection) offering our DI;HALT + BC-return + `-exitonhalt` idiom. | i81; upstream SimCoupé #28 |
| **i81c** | Optionally offer the SDL dummy-driver headless renderer fallback upstream | BLOCKED:Pete — optionally offer the SDL dummy-driver headless renderer fallback upstream (lower priority). | i81; `tools/font-proof/simcoupe-local-capture.patch` |
| **i83** | SAM TFTP server (Phase-3 PXE shipper) — serve the spectrum4 kernel + Raspberry Pi firmware over TFTP to the Pi 400 for PXE boot. The Phase-3 headline goal; pairs with i70 (firmware self-provisioning: fetch firmware online, persist to Trinity storage, serve from here). | OPEN — 🧭 Phase-3, **design confirmed** (Pete 2026-06-14) — **Model A**. The SAM is a **DHCP + TFTP server** (DHCP = new item **i86**, mandatory for Pi-3 netboot + multi-client LAN) that serves Pi firmware + `spectrum4.img` **by name from one flat Trinity store** — **model-agnostic**, so all TFTP-bootable aarch64 Pis are covered (Pi 3 + Pi 400 first), with no per-machine SAM config. Supports **both** topologies (direct SAM↔Pi cable and shared LAN with several Pis). **Server spec settled from a real Pi 400 capture** → `docs/notes/pi-netboot-capture-analysis.md`: must do **OACK** (`tsize`=size + `blksize` 1024/1468), serve **by name**, and return **TFTP ERROR(1) on every miss and keep going** (the boot ROM probes many optional files); the served-file *set* is **not fixed** — it varies by project / firmware revision / features (e.g. HDMI-audio → extra dtbo/overlays), so the **mechanism, not any file list, is the invariant**. **Transfer loop LANDED + host-verified end-to-end** (this PR) — `src/netboot/tftp_server_loop.asm` is the reply-driven server state machine: `tftp_handle_rrq` (`drv_read` → UDP dst 69? `parse_request` → `resolve`; on a hit `build_oack` with the negotiated blksize + the stored `tsize`, arm the streamed transfer; on a miss/serial-subdir `build_error(1)` and keep serving), `tftp_first_data` (DATA block 1 after the client ACKs block 0), `tftp_handle_ack` (advance + send the next DATA, short-final-block ends it). It composes the host-verified pieces (`build_udp_frame`/`tftp_build`/`tftp_parse`, i92) + the real `encdrv.asm`, runs over the **i80 emulated Trinity**, and `tftp_server_loop_test.go` asserts the OACK + the full DATA/ACK transfer (incl. the exact-multiple zero-length terminator) + ERROR(1)-on-miss + serial-subdir-404 on the virtual wire match the new Go authority `tftp/serverloop.go::ServerLoop` **byte-for-byte**. New Z80 `negotiate_blksize` (AcceptedBlksize clamp) + `write_dec_u16`/`parse_dec_u16` (the OACK option decimals). Wired into `make ci-netboot-z80`. The flat-store name→record seam (the UIFA/DIFA arithmetic gluing this to the B-DOS HGTHD/HLOAD hooks) landed as **i93** (`bdos_seam.asm`, host-verified field arithmetic; the RST 8 hook dispatch stays hardware-gated). The current test fills an in-memory store via `bdos.Store.Wire()`. **Emulation-verified ≠ hardware-verified.** | `docs/plans/phase3-netboot-implementation-plan.md`; `docs/specs/phase3-delivery-design.md` §6; `docs/notes/pi-netboot-capture-analysis.md`; `src/netboot/tftp_server_loop.asm`; `tools/netboot-oracle/tftp/serverloop.go`; i70, i80, i82, i84, i86, i89, i93; q11, q12 |
| **i86** | Minimal SAM DHCP/bootp responder (Phase-3, new — Pete 2026-06-14, q11) — the netboot server's other half: answer the Pi boot ROM's DHCP `DISCOVER`/`REQUEST` with an `OFFER`/`ACK` carrying an address from a small pool, the subnet, the next-server (`siaddr` = the SAM), plus the Raspberry-Pi netboot DHCP conventions **now confirmed from a real capture** (`docs/notes/pi-netboot-capture-analysis.md`): echo `PXEClient` (opt 60 — present in every working capture; echo it, strict necessity unverified), send the fixed 32-byte option-43 PXE blob containing the literal `Raspberry Pi Boot`, echo the client UUID (opt 97), no opt-67 bootfile. **Mandatory**: Pi-3-class netboot has no static-IP path, and a shared LAN needs address handout for several Pis. Incremental on trinload's UDP stack (UDP 67/68 broadcasts); model-general, not per-model. Mirrors Pete's `dnsmasq` DHCP behaviour. | OPEN — 🔨 **responder loop LANDED + host-verified end-to-end** (this PR) — `src/netboot/dhcp_loop.asm` is the SAM-side `dhcp_serve_once` state machine: `drv_read` a frame → dispatch (UDP dst 67? BOOTREQUEST? option-53 DISCOVER→OFFER / REQUEST→ACK) → per-MAC lease from a fixed pool → `build_dhcp_reply` + `build_udp_frame` (UDP 67→68 L2-broadcast) → `drv_write`. It composes the host-verified packet primitives (`build_udp_frame`/`dhcp_reply`) and the real driver (`encdrv.asm`) into one binary and runs it over the **i80 emulated Trinity**; `dhcp_loop_test.go` asserts the OFFER + the full DORA (DISCOVER→OFFER, REQUEST→ACK, stable per-MAC lease) on the virtual wire match the new Go authority `dhcp/responder.go::Responder.OnRequest` **byte-for-byte**, plus non-DHCP-ignored + empty-wire controls. Fixed a multi-packet-RX gap in the emulation (the chip's RX write position must follow the next-pointer chain, not ERXRDPT). Wired into `make ci-netboot-z80`. **Emulation-verified ≠ hardware-verified**: an end-to-end Pi boot stays the real-Trinity gate (CLAUDE.md §5). | `docs/plans/phase3-netboot-implementation-plan.md`; `docs/specs/phase3-delivery-design.md` §6.3; `src/netboot/dhcp_loop.asm`; `tools/netboot-oracle/dhcp/responder.go`; i80, i83, i84, q11 |
| **i87** | Dump + reverse-engineer Pete's patched SAM ROM (Phase-3, new — Pete 2026-06-14, q11) — Pete's SAM runs a modified system ROM giving Trinity-interface access at power-on (no B-DOS floppy needed). When his SAM is next online he dumps the ROM to disk; we then diff it against the stock SAM ROM to document exactly how Trinity-at-boot is patched in, so the boot mechanism is reproducible/understood. **Not a blocker** — trinload can net-load the client/server in the interim, and `trinity.mgt` + `reference/bdos` cover the interface. | BLOCKED:hardware — Phase-3 (artifact pending — Pete to dump ROM) | `docs/specs/phase3-delivery-design.md` §3; i84, q11 |
| **i88** | On-SAM **HTTPS/TLS client** to fetch Pi firmware from GitHub (Phase-3 **stretch**, Pete 2026-06-14). **Rationale (Pete):** the canonical RPi firmware source is GitHub (HTTPS-only); an on-SAM fetch lowers the **barrier to entry for other SAM+Trinity contributors** (no SD-card dance), making the project far more approachable — the value is for *others*, not Pete. The single hardest Z80 component: TLS 1.3 with **certificate/public-key pinning** (we control both ends → no CA store) and the ARX-friendly **X25519 + ChaCha20-Poly1305 + SHA-256** suite (no AES S-boxes / GHASH); slow handshake is fine (firmware fetch is rare). **Not on any critical path** — interim provisioning is plain HTTP/TFTP from a local source (i70); the next agent should also check for a plain-HTTP RPi-firmware mirror, which would make this optional. **DOWNGRADED 2026-06-15 (mirror-check done, design §7): on-SAM TLS is now OPTIONAL, not "needed for the purest form."** Verified end-to-end that the **Raspberry Pi apt archive** `http://archive.raspberrypi.org/debian/` serves the *complete* Pi 3 + Pi 4/400 netboot firmware set over **plain HTTP** (`raspberrypi-bootloader_*.deb` = `bootcode.bin`/`start*.elf`/`fixup*.dat`; `raspberrypi-kernel_*.deb` = kernels/DTBs; apt's default source line is `http://`). The already-built i70 HTTP client (no TLS) suffices; only GitHub itself is HTTPS-locked. The remaining work shifts from crypto to **`.deb` format parsing** (ar + xz-inflate + untar) — far smaller than TLS, and avoidable via a pre-extracted HTTP mirror → the extraction-strategy choice is **q15**. i88 stays a genuine stretch (only needed if direct-from-GitHub is ever wanted). **RE-EXAMINED 2026-06-15 (Pete: the self-service / other-devs goal needs commit-pinned firmware fetchable with no maintainer-hosted store — "if we can't find an HTTP mirror that allows specifying git commit we build HTTPS for GitHub").** Research **found one**: `http://cdn.githubraw.com/<owner>/<repo>/<sha>/<path>` — plain HTTP, **commit-SHA-addressable**, verified byte-identical for the pinned firmware (q15). So i88 is **still avoidable** for the fetch — **but** that proxy is a single fragile free service (its lineage has died before), so for long-term durability i88 (TLS→canonical GitHub) remains the robust path. **Pete's call (q15 a/b/c):** (a) the proxy + a SAM-side SHA-256 verify [agent's lean] vs (b) build i88 vs (c) both. A SAM-side **SHA-256** primitive is a building block for i88's cipher suite either way. **RESOLVED 2026-06-15 — Pete picked q15 option (c): build i88, but at the LOWEST priority** (agents work it ONLY when nothing else is left to do; the active path is `cdn.githubraw.com` + a SHA-256 verify). **Scope reduction (Pete):** since the firmware content is SHA-256-pinned by the active path, i88 can be **github.com-only** (pin its public key — no general CA store) and may even **skip TLS certificate verification** entirely (the content hash already covers integrity), shrinking i88 from "full TLS+PKI" to ~**the TLS-1.3 crypto only** (X25519 + ChaCha20-Poly1305 + SHA-256 handshake + cipher — no X.509/cert-chain/CA parsing). This makes i88 materially smaller than its original framing — still the hardest component, but the crypto handshake, not the PKI. **Building blocks landed (lowest-priority, started once the host-verifiable netboot work drained):** **SHA-256** (`src/netboot/sha256.asm`, #269 — also the i100 verify primitive) and **HMAC-SHA256** (`src/netboot/hmac_sha256.asm`, 2026-06-15 — RFC 2104, a thin orchestration over `sha256.asm`: K0 prep + the ipad/opad passes; host-verified vs Go `crypto/hmac` over the RFC 4231 vectors + the K0-branch boundaries). HMAC-SHA256 is the basis for TLS 1.3's **HKDF** key schedule (RFC 5869). **HKDF landed (2026-06-15):** `src/netboot/hkdf.asm` — `hkdf_extract` (PRK = HMAC(salt, IKM)) + `hkdf_expand` (the `T(i)` chain truncated to L), orchestration over the landed HMAC, host-verified vs Go `crypto/hkdf` over the RFC 5869 Appendix-A vectors + multi-block-OKM + zero-salt/empty-info + the L boundaries (1/32/33/64). So the **hash / MAC / KDF cluster** (SHA-256 → HMAC → HKDF) is complete. **HKDF-Expand-Label landed (2026-06-15):** `src/netboot/hkdf_expand_label.asm` — TLS 1.3's `HKDF-Expand-Label` (RFC 8446 §7.1), the function the **whole TLS key schedule** is built from (every handshake/traffic secret + record key/IV). `expand_label` assembles the HkdfLabel (`uint16 length` + `"tls13 "+label` + `context`, length-prefixed) and calls `hkdf_expand` — no new arithmetic. Host-verified vs a Go RFC 8446 §7.1 reference **anchored to the independent RFC 8448 known-answer** (TLS 1.3 Early-Secret + its "derived" secret), across the key-schedule labels (`derived`/`key`/`iv`/`finished`/traffic) with empty + hash contexts. So the **key-schedule layer is complete** (SHA-256 → HMAC → HKDF → Expand-Label); `Derive-Secret` is Expand-Label with context = the transcript hash (a thin caller, with the handshake). **ChaCha20 block function landed (2026-06-15):** `src/netboot/chacha20.asm` — the first i88 **cipher** and the only suite member that is **new arithmetic** (a from-scratch ARX port), not orchestration. `chacha20_block(key, counter, nonce)` builds the 16-word state, runs 20 rounds of the quarter-round on a working copy (the four rotates ROTL16/12/8/7 built from byte permutes + single-bit rotates, since the Z80 has no 32-bit ops), adds the initial state word-wise, serialises the 64-byte keystream. Host-verified vs the RFC 8439 known-answer vectors (§2.3.2 + Appendix A.1 #1/#2 — distinct keys/counters/nonces; all byte-exact first try). **ChaCha20 stream cipher landed (2026-06-15):** `chacha20_encrypt` in `chacha20.asm` — RFC 8439 §2.4: XOR the keystream into the message one 64-byte block at a time, incrementing the block counter per block (encrypts and decrypts). Host-verified vs the RFC 8439 §2.4.2 worked example (a 114-byte 2-block plaintext, counter from 1) + an encrypt∘decrypt round-trip across the block boundaries (1/63/64/65/128/200). So **ChaCha20 is complete** (block + stream). **Poly1305 landed (2026-06-15):** `src/netboot/poly1305.asm` — the ChaCha20-Poly1305 **MAC** and the **first of the two big from-scratch multi-precision ports**. `poly1305_mac` clamps `r`, runs `a = (a+n)·r mod 2^130-5` over each 16-byte block (`n` = block || 0x01, little-endian), adds `s`, emits the low 16 bytes. The Z80 has no multiply, so it is all byte-radix big-integer: `mul8` (8×8→16 shift-add) → `poly_mul_a_r` (the 17×16 schoolbook product, `a < 2^131` × `r < 2^124` → < 2^256) → reduction mod 2^130-5 via `2^130 ≡ 5 (mod p)` (split `P = low + 2^130·high`, replace with `low + 5·high`, fold until < 2^130) → `poly_freeze` (the final conditional subtract of `p`, so the tag is canonical before `+s`). Host-verified vs the RFC 8439 §2.5.2 KAT **and** an independent `math/big` reference (`poly1305_test.go`: the KAT, then the reference across message lengths 0/1/partial/block-boundary/multi-block (0…256) and edge-case keys incl. zero/max `r`/`s`, all byte-exact). **X25519 field arithmetic landed (2026-06-15):** `src/netboot/x25519.asm` — the GF(2²⁵⁵−19) layer, the foundation of the **last/biggest** multi-precision port. Field elements are 32-byte LE, almost-reduced (`< 2²⁵⁵`); `fe_freeze` canonicalises to `< p`. `fe_mul` = `mul8` (8×8→16) → `fe_schoolbook` (32×32→64-byte product) → reduction via `2²⁵⁶ ≡ 38` (fold high half ×38) then `2²⁵⁵ ≡ 19` (`fe_reduce33` folds residual bit-255 overflow ×19 until `< 2²⁵⁵`); plus `fe_add`, `fe_sub` (via `+2p`), `fe_mul121665` (the a24 constant). Host-verified vs a `math/big` reference mod p (`x25519_field_test.go`: every op over edge operands 0/1/p/p−1/2²⁵⁵−1/19/38/121665/random — byte-exact for `fe_freeze`, value-equal mod p for the almost-reduced ops, `< 2²⁵⁵` invariant, `fe_mul` covers max×max). **`fe_invert` landed (2026-06-15):** the modular inverse a⁻¹ = a^(p−2) (Fermat) in `x25519.asm`, a faithful port of TweetNaCl's `inv25519` addition chain (square every step; multiply by the input except at chain positions 2 and 4); host-verified by a·a⁻¹ ≡ 1 (mod p) + equality with `math/big` ModInverse (0→0). It runs ~37M byte-ops, so the harness gained an optional per-call `Entry.StepCap` (raising the default runaway guard for the heavy multi-precision routines). **X25519 Montgomery ladder + `x25519(scalar, u)` landed (2026-06-15) — X25519 COMPLETE:** the `x25519` entry in `x25519.asm` (RFC 7748 §5): clamp the scalar, decode/mask u, run the 255-step Montgomery ladder (`ladderstep` differential add-and-double + a constant-bit `cond_swap_state`, the field ops marshaled through `FE_A`/`FE_B`/`FE_OUT` via `to_A`/`to_B`/`from_OUT`), then `x2 · z2⁻¹` (`fe_invert`) frozen to 32 LE bytes. Host-verified vs the **RFC 7748 §5.2 KATs** (vectors 1 & 2 + the basepoint-9 iterative value) **and** Go `crypto/ecdh` over the base point (`x25519_test.go`, ~6s/scalar via the raised `StepCap`). So the **i88 cipher-suite primitives are all complete**: SHA-256, HMAC, HKDF, HKDF-Expand-Label (key schedule), ChaCha20 (block+stream), Poly1305 (MAC), X25519 (field + inversion + ladder). **AEAD_CHACHA20_POLY1305 landed (2026-06-15) — the TLS record protection:** `src/netboot/aead.asm` (RFC 8439 §2.8), composing the verified ChaCha20 + Poly1305 (`include`d; built with `-D NETBOOT_AEAD` so the primitives' `NETBOOT_STANDALONE` org guard stays inert and aead sets the org once — their own builds unchanged; the composition idiom for the handshake too). `aead_encrypt` (otk = chacha20 block-0 keystream; ct = chacha20_encrypt counter-1; tag = poly1305 over AAD‖pad‖CT‖pad‖le64 lens) + `aead_decrypt` (recompute tag, difference-accumulating compare, decrypt only if authentic → `AEAD_OK`). Host-verified vs the RFC 8439 KATs (`aead_test.go`: §2.6.2 key-gen, §2.8.2 encrypt ct+tag byte-exact, a decrypt round-trip across lengths 0…200 ±AAD, and tamper rejection of a flipped tag/ct/AAD byte). Remaining i88 = the **TLS-1.3 handshake state machine**. **Spec'd 2026-06-15:** `docs/specs/tls13-handshake.md` decomposes it into host-verifiable bricks — (1) the key schedule (RFC 8446 §7.1 over the done Expand-Label, verified vs RFC 8448), (2) record protection (nonce + header-AAD AEAD), (3) the ClientHello builder, (4) the server-flight parser (ServerHello + the encrypted EncryptedExtensions/Certificate/CertificateVerify/Finished), (5) the transcript hash, (6) the state-machine capstone over `tcp_conn`. Bricks 1–5 are standalone leaves that proceed autonomously; the capstone (6) was gated on **q17** (the `&10000` budget / paging question) — **q17 RESOLVED Pete 2026-06-15: use paging, the agent owns the layout**, so the capstone's remaining gate is the bootable/hardware integration, not the memory architecture; the primitives are currently leaves *not* in the bootable image. **Brick 1 landed (2026-06-15):** `src/netboot/tls_keyschedule.asm` — `tls_key_schedule` derives the whole RFC 8446 §7.1 secret tree (Early → derived → Handshake (+ c/s hs traffic) → derived2 → Master (+ c/s ap traffic) + each traffic secret's key/iv/finished) from the ECDHE secret + the two transcript hashes, pure orchestration over `hkdf_extract` + `expand_label`; host-verified vs a Go RFC 8446 §7.1 reference across two input sets, Early/derived anchored to RFC 8448 (`tls_keyschedule_test.go`). **Brick 2 landed (2026-06-15):** `src/netboot/tls_record.asm` — TLS 1.3 record protection (RFC 8446 §5.2-5.3): `tls_record_seal`/`tls_record_open` build the per-record nonce (iv XOR seq), the 5-byte header AAD, and the content‖type inner-plaintext over the verified AEAD; host-verified by a seal→open round-trip + an independent Go framing cross-check (byte-exact record via the verified `aead_encrypt`) + tamper/wrong-seq rejection (`tls_record_test.go`). **Brick 5 landed (2026-06-15):** `src/netboot/tls_transcript.asm` — the handshake transcript hash (RFC 8446 §4.4.1): `tls_transcript_init`/`update` are the running SHA-256 over the handshake messages, `tls_transcript_snapshot` writes SHA-256(messages-so-far) without ending the stream (save the 105-byte SHA-256 state → `sha256_final` → restore), the Derive-Secret context. Host-verified vs Go `crypto/sha256` — empty + interleaved snapshots across the block boundary (`tls_transcript_test.go`). **Brick 3 landed (2026-06-15):** `src/netboot/tls_client_hello.asm` — the ClientHello builder (RFC 8446 §4.1.2): `tls_build_client_hello` emits the handshake message for our fixed offer (one cipher suite TLS_CHACHA20_POLY1305_SHA256, a supplied X25519 key_share + 32-byte random/session_id, the five extensions server_name/supported_versions/supported_groups/signature_algorithms/key_share), back-patching the variable container lengths from the emitted positions; pure byte assembly, no crypto. Host-verified three ways (`tls_client_hello_test.go`): byte-identical to an independent Go reconstruction across host-name lengths, parsed by Go `crypto/tls` into a `tls.ClientHelloInfo` (the stdlib TLS implementation as an independent structural oracle, which also rejects malformed input), and a TLV walk recovering the x25519 key_share = the supplied public key. **Brick 4 landed (2026-06-15):** `src/netboot/tls_server_flight.asm` — the server-flight parser (RFC 8446 §4.1.3/§4.3): `tls_parse_server_hello` extracts the X25519 key_share from a ServerHello and validates the negotiated suite/version (rejecting a wrong cipher, missing key_share, non-1.3 version, and the HelloRetryRequest sentinel random); `tls_walk_server_flight` walks the decrypted EncryptedExtensions/Certificate/CertificateVerify/Finished flight, folding each into the transcript, snapshotting Hash(CH..CertificateVerify) just before the Finished, and capturing the server Finished verify_data (SF_OK iff all four seen). Host-verified vs Go-generated flights (`tls_server_flight_test.go`): key_share recovery + the five rejection cases, the captured Finished + four flags, SF_HASH_BEFORE_FIN = Go SHA-256(EE‖Cert‖CertVerify), a post-walk snapshot = Go SHA-256(the whole flight), incl. a CH..SH-prefixed transcript variant. So bricks 1–5 are all landed; only the capstone brick 6 (state machine) remains (q17 resolved 2026-06-15: use paging, agent owns layout). | OPEN — 🧭 Phase-3 — **build at LOWEST priority** (Pete: c; only when nothing else to do); scope reduced to TLS-crypto-only (github.com-only, content already hash-verified). **Landed: SHA-256 (#269) + HMAC-SHA256 + HKDF + HKDF-Expand-Label (key-schedule layer) + ChaCha20 (block + stream) + Poly1305 (the MAC) + X25519 (field + `fe_invert` + ladder, COMPLETE) + AEAD_CHACHA20_POLY1305 (`aead.asm`) + handshake spec (`docs/specs/tls13-handshake.md`) + brick 1 = key schedule (`tls_keyschedule.asm`) + brick 2 = record protection (`tls_record.asm`) + brick 5 = transcript hash (`tls_transcript.asm`); brick 3 = ClientHello builder (`tls_client_hello.asm`) + brick 4 = server-flight parser (`tls_server_flight.asm`); bricks 1–5 all landed, only the capstone brick 6 (state machine) remains (q17 resolved 2026-06-15: use paging, agent owns layout). **Brick-6 Go authority LANDED (2026-06-16):** `tools/netboot-oracle/tls/` — a host-side `Client` that completes a **real 1-RTT TLS 1.3 handshake against Go `crypto/tls.Server`** over `net.Pipe` and cross-checks all four traffic secrets against the server's `KeyLogWriter` (byte-for-byte: our independent key schedule + ECDHE = the stdlib's). It composes the lifted brick-1..5 references (`ExpandLabel`/`KeySchedule`, record `Seal`/`Open`, `BuildClientHello`, `ParseServerHello`/`WalkServerFlight`, the running-SHA-256 transcript) plus a from-scratch RFC 8439 ChaCha20-Poly1305 (`aead.go`, KAT-pinned — keeps the module zero-external-dep and the AEAD independent of crypto/tls's internal copy). This is the reference the Z80 brick-6 state machine mirrors (CLAUDE.md §6). The Go authority was NOT q17-gated; **the Z80 capstone (brick 6) remains** — lowest-priority, its gate the bootable/hardware integration (the agent owns the paging layout, q17 resolved). The Go-authority execution plan was consumed + deleted in PR #325. **Z80 brick-6 port plan ready (2026-06-16):** `docs/plans/z80-tls-client-port-plan.md` decomposes the Z80 port into **6a** (the record-level state machine `tls_client.asm` = a faithful, FULLY host-verifiable port of the Go `Client.OnRecord`, using the Go authority as a deterministic capture-then-replay oracle with checkable intermediate secrets — NOT all-or-nothing) and **6b** (TLS-over-TCP + the paging layout for the bootable, partly hardware-gated like `http_main` brick 7). **Composition prerequisites mapped by actually building the composite (2026-06-16) — TWO cross-brick collisions:** (1) `sha256` is double-INCLUDED (key-schedule chain + transcript chain); (2) the i102 **quarter-square multiply** (`mul8`/`mul8_diff_pos`/`qsq_init`/`qsq_init_loop` + the `QSQ` table) is independently DEFINED in both `poly1305` and `x25519` (poly1305's was ported from x25519's; same algorithm + table, differ only in comments). Resolution (plan Part 0): **extract a shared `src/netboot/qsq.asm` leaf** (its own PR, gated on `poly1305_test`/`x25519_test`/`aead_test` green — it refactors i102-optimized crypto, so wants fresh care) + flag-suppress the redundant `sha256` AND `qsq` includes under `-D NETBOOT_TLS_CLIENT` in the 6a composite. **Generic include-once guards are pyz80-incompatible** (pyz80 runs 2 passes and never resets its symbol table, so an `if defined(SENTINEL)/equ` guard skips the body on pass 2 → phase errors; verified from `pyz80.py` + a reproducer). Also **q19** (the real-hardware entropy source for the ephemeral scalar — low stakes, content is hash-pinned). The first 6a increment is the **qsq extraction**, then the composition + ClientHello sub-brick, then `OnRecord`. **qsq extraction LANDED (2026-06-16, PR #330):** `src/netboot/qsq.asm` now holds the shared quarter-square multiply (`qsq_table` equate + `mul8`/`mul8_diff_pos`/`qsq_init`/`qsq_init_loop`), `include`d by both `poly1305.asm` and `x25519.asm` (which dropped their byte-identical in-file copies); all four affected binaries (`poly1305`/`x25519`/`aead`/`tls_record`) assemble byte-identical pre/post and their host tests stay green. The `-D NETBOOT_TLS_CLIENT` flag-suppression of the duplicate `qsq` + `sha256` includes folds into the next increment (the 6a composite). **6a composition + ClientHello sub-brick LANDED (2026-06-16, PR #331):** `src/netboot/tls_client.asm` composes all five bricks + `x25519` into one binary (`-D NETBOOT_TLS_CLIENT=1`), applying the Part-0 suppressions (sha256 via the key-schedule chain; qsq via `tls_record`→`aead`→`poly1305`). Carries `tls_client_init` (ECDHE pubkey: `CH_PUBKEY = X25519(scalar, basepoint)` + transcript init + run-state zero) and `tls_client_first` (build ClientHello, fold the transcript, emit the `0x16 0x03 0x01 len16 ‖ CH` record into `TC_TX`). Host-verified vs the deterministic Go authority (`tls_client_test.go`: `CH_PUBKEY` == `crypto/ecdh` pubkey, `TC_TX` == `Client.First()` byte-for-byte). **Layout decision (agent-owned, q17 spirit):** the composite is ~29 KB, and `SF_FLIGHT` straddles the standalone bricks' `qsq_table` scratch at `&C000`, so under `NETBOOT_TLS_CLIENT` the table relocates to `&FB00` (above the image; the host test asserts `tls_client_end < &FB00`). **Remaining 6a = `tls_client_on_record`** (the state machine — next increment, wants fresh context per the plan). Two notes for that increment: (a) the brick-1 **two-phase key-schedule split** (`tls_key_schedule_handshake`/`_application`) the plan describes — **LANDED (2026-06-16, PR #332):** `tls_keyschedule.asm` now exposes both phase entries (ports of `KeySchedule.DeriveHandshake`/`DeriveApplication`), and the one-shot `tls_key_schedule` is just `call _handshake` + `jp _application` (byte-identical output, the full entry stays RFC-8448-pinned); host-verified two-phase (`tls_keyschedule_test.go::TestTLSKeyScheduleTwoPhase` — the handshake phase proven to derive every handshake secret/key without reading `KS_HASH_AP`, then the application phase completes from `KS_DERIVED2` + `KS_HASH_AP`); (b) the composite shares ONE `sha256` instance, so the running transcript hash (live in `SHA_*`) must be saved/restored around any key-schedule `hmac` call that would clobber it — a Z80-specific concern the single-hash.Hash Go authority doesn't have (still open for the state-machine increment). **Part-2 capture oracle LANDED (2026-06-16, PR #333):** `tools/netboot-oracle/tls/capture.go` — `CaptureHandshake(host, priv, random, sid)` drives a deterministic client through a full 1-RTT handshake against an in-process `crypto/tls.Server` over `net.Pipe` and records the wire records (`CHRecord`/`Inbound`/`FinRecord`) + the four traffic secrets into a `Capture`. Verified by `capture_test.go::TestCaptureHandshakeReplay` (replaying `Inbound` through a fresh deterministic client reproduces every captured output + secret) — the exact diff the Z80 state-machine test will perform with the Z80 in the client's place. So the state-machine increment now needs only the Z80 `tls_client_on_record`/`on_server_hello`/`on_encrypted` + the shared-SHA save/restore + a `tls_client_test.go` replay harness over this oracle. **q19 RESOLVED (PR #334): build-time-injected per-build seed** for the ephemeral X25519 scalar — adequate because security rests on the SHA-256 content pin, not the transport (see closed q19; runtime-entropy mix is the documented upgrade path). **Prior-art survey (2026-06-16):** a real-X25519 classic-Z80 TLS 1.3 client appears **novel** — Konamiman's TLSforZ80 stubs the curve (`priv=1`), the only real native 8-bit TLS 1.3 is on 6502 (`c64-https`, the same ChaCha20-Poly1305 suite, ~2–3 h/handshake), and all other retro "HTTPS" offloads TLS to a coprocessor; recorded in `docs/specs/tls13-handshake.md` §Prior art. **6a COMPLETE — the record-level state machine LANDED (2026-06-16, PR #335):** `tls_client.asm` gains `tls_client_on_record` + `tls_client_on_server_hello` + `tls_client_on_encrypted` (faithful ports of the Go `Client.OnRecord`/`onServerHello`/`onEncrypted`), plus the Z80-specific **shared-SHA-256 save/restore** (`TC_TRANSCRIPT_SAVE` brackets every off-transcript hmac/key-schedule use of the single shared `SHA_*` state — carry-forward (b), resolved). **Host-verified end-to-end** (`tls_client_test.go::TestTLSClientHandshakeReplay`): the capture-replay oracle drives a complete 1-RTT handshake and the Z80 reproduces the captured client Finished record **+ all four traffic secrets (CHS/SHS/CAP/SAP) byte-for-byte**, with an AEAD-tamper negative control; composite image `tls_client_end=&F410` (< `qsq_table` &FB00). **So brick 6a — the FULLY host-verifiable slice of i88 — is DONE.** The port plan was consumed + deleted in this PR (its 6b design = blob `72db9b8`). **Remaining: brick 6b** — TLS-over-TCP (record reassembly over `tcp_conn`) + the paging layout + the bootable; partly **hardware-gated** like `http_main` brick 7 (the real `RST 8` handshake on Trinity = Pete's test), the wire side host-verifiable over the i80 ENC28J60 emulation. | `docs/specs/phase3-delivery-design.md` §7; `src/netboot/tls_client.asm`; `tools/netboot-oracle/tls/`; `docs/specs/tls13-handshake.md`; `http://cdn.githubraw.com`; `src/netboot/sha256.asm`; `src/netboot/hmac_sha256.asm`; `src/netboot/hkdf.asm`; `src/netboot/hkdf_expand_label.asm`; `src/netboot/chacha20.asm`; `src/netboot/poly1305.asm`; `src/netboot/x25519.asm`; `src/netboot/aead.asm`; `src/netboot/tls_keyschedule.asm`; `src/netboot/tls_record.asm`; `src/netboot/tls_transcript.asm`; `src/netboot/tls_client_hello.asm`; `src/netboot/tls_server_flight.asm`; i70, i84, q15, q17, i100 |
| **i89** | Capture a **Pi 3 netboot** exchange (Phase-3, Pete 2026-06-14) — extend the i83/i86 oracle to the older Pi family: confirm the Pi 3 boot ROM's DHCP behaviour (it may not present the full PXE option-43 dance the Pi 4 does) and its TFTP file set (`bootcode.bin`/`start.elf`/`fixup.dat`), empirically validating the model-agnostic serve-by-name claim across families. Pete runs it on real hardware (hub + Wireshark, the trinload `9ff9099` technique) when convenient — **not a blocker** (the Pi 3 file set is already known from his `tftproot`); the agent provides capture-setup steps at the time. | BLOCKED:hardware — Phase-3 (future capture — Pete to run) | `docs/notes/pi-netboot-capture-analysis.md` §4; i83, i86, i84 |
| **i90** | `%nobits`/BSS section bodies are dropped by `-flatten` — source-structure loss (found during i78(a), 2026-06-14). When a source is flattened (`-flatten`, the release path), the entire body of every `%nobits`/BSS section (`bss_kernel`/`bss_roms`/…) is discarded: it contributes zero bytes to the flat image, so its records (blank lines, comments, `.space`) have no PC region to anchor editor content to. Result: blank lines / comments authored inside BSS sections do not round-trip (release.s: 132 of 1472 blank lines, all in NOBITS sections). This is a **pre-existing flatten property**, not an i78 bug — i78's blank/comment preservation is exact for PROGBITS (`.text`/`.data`). Only matters once the SAM editor edits a *flattened* source with BSS content (the normal editor path edits the un-flattened source, where the structure is intact). Capture only — decide whether to preserve NOBITS editor content (e.g. zero-PC-region anchoring) if/when the flattened-edit path needs it. | OPEN — 🧭 captured (not a blocker; PROGBITS preservation is exact) | `docs/specs/source-structure-preservation-design.md`; `tools/sam-aarch64/frontend/flatten.go`; i78 |
| **i92** | Phase-3 netboot **Z80 port** of the protocol logic (`src/netboot/*.asm`, new — 2026-06-14, netboot impl plan §8.2). The SAM-side Z80 implementation of the DHCP/TFTP packet builders/parsers, ported faithfully from the i91 Go authority (`tools/netboot-oracle/`) and **host-verified byte-for-byte** against the same masked golden vectors via a flat-memory koron-go/z80 harness (`tools/netboot-oracle/z80/`, a nested module): each routine is assembled with pyz80, run, and its output compared to the golden frame (`make ci-netboot-z80`, the `netboot-z80` CI job). **Verification scope:** the protocol logic (pure arithmetic + memory writes) IS host-verifiable and merges green; the ENC28J60 wire I/O + end-to-end Pi boot are NOT (gated on i80/real-Trinity) and stay unmerged/unverified per CLAUDE.md §5. | OPEN — 🔨 **in progress** — bricks landed (each host-verified byte-equal to the Go authority): **`build_udp_frame`** (#221, the fresh-frame primitive trinload lacks, plan §5.1; port of `frame/frame.go::BuildUDPFrame` + the trinload `chksum_blk` IP-checksum idiom); **`build_dhcp_reply`** (#222, the i86 OFFER/ACK body — option template + the fixed 32-byte option-43 "Raspberry Pi Boot" blob + the echoed UUID; port of `dhcp/dhcp.go::BuildReply`); **`build_oack`/`build_data`/`build_error`** (the i83 TFTP server reply builders, each also byte-exact to the captured OACK/DATA/ERROR; port of `tftp/tftp.go::BuildOACK`/`BuildDATA`/`BuildError`); **`parse_request`/`resolve`** (the i83 TFTP server request side — RRQ parse + serve-by-name resolve: 404 serial-subdir, OACK a hit with the stored size, ERROR(1) every miss; verified against the captured RRQs `TFTPRrqSerial`/`TFTPRrqRoot1024`/`TFTPRrqRoot1468`; port of `tftp/tftp.go::ParseRequest` + `tftp/server.go::Resolve`); **`build_rrq`/`parse_oack`/`find_option`** (the i82 TFTP client request side — RRQ builder byte-exact to the captured RRQs + the client option set, and OACK parse learning the negotiated blksize / file tsize; port of `tftp/tftp.go::BuildRRQ` + `::ParseOACK`); **`build_arp_request`** (the fresh Ethernet ARP request — RFC 826 — the client broadcasts to learn the server MAC before its RRQ, plan §5.1; byte-exact to the new Go authority `frame/frame.go::BuildARPRequest`). All packet build/parse logic is now ported; what remains for the netboot port are the **wire-I/O state machines** (ENC28J60 send/receive, the DHCP/TFTP/transfer loops), which are NOT host-verifiable and stay on a gated branch (i80/real-Trinity). | `src/netboot/`; `tools/netboot-oracle/z80/`; `docs/plans/phase3-netboot-implementation-plan.md` §5.1/§8.2; `docs/notes/pi-netboot-capture-analysis.md`; i82, i83, i86, i91 |
| **i93** | Phase-3 netboot **B-DOS storage seam** (`src/netboot/bdos_seam.asm` + `tools/netboot-oracle/bdos/`, new — 2026-06-14). The name→record glue shared by the i83 server (serve a file by name from B-DOS records) and the i82 client (write a received file by name): the UIFA/DIFA field arithmetic between the netboot loops and the B-DOS / SAMDOS-2 file-I/O hooks (HGTHD/HLOAD/HSAVE over the HRECORD-selected flat store). Three routines, ported from the Go authority `bdos/bdos.go` and **host-verified byte-for-byte** (`bdos_seam_test.go`, `make ci-netboot-z80`): `bdos_name_to_uifa` (NUL-terminated name → the 48-byte UIFA name block: type 19 + 10-char space-padded name + 4-char ext + 0xFF filler, the `fill_uifa` convention), `bdos_difa_to_size` (DIFA+34 pages × 16384 + DIFA+35..36 length-mod-16K with the bit-7 marker masked → the 32-bit file size the OACK `tsize` reports), `bdos_fill_save_uifa` (the inverse for the client write-out: name + page + offset + size → the HSAVE UIFA, `save_out_file`'s length encoding). The field conventions are ported verbatim from the assembler's own real hook call sites (`src/io.asm` fill_uifa, `src/loader.asm` load_payload_generic, `src/main_loop.asm` save_out_file) + `docs/specs/samdos-file-io.md` — a port of established in-tree idioms, not new design. Also `bdos/bdos.go::Store` — the flat name→size directory model the loops resolve against (its `Wire()` is the `name\0`+LE-size format the existing Z80 `resolve` already walks). **Verification scope (the honesty line, CLAUDE.md §5):** the field arithmetic IS host-verifiable (pure memory build/decode) and merges green; the actual **RST 8 hook dispatch** (HGTHD/HLOAD/HSAVE/HRECORD) is NOT — the flat-memory harness has no ROM/SAMDOS bank/RST 8, so the hook bodies (behind `NETBOOT_HOSTTEST`, excluded from the host build) stay UNVERIFIED until exercised on real Trinity. The i62 dual-run proved the hooks round-trip on real SAMDOS-2 + B-DOS-AL backends; the netboot fetch end-to-end is the final integration gate. Emulation-verified ≠ hardware-verified. | OPEN — 🔨 **seam arithmetic LANDED + host-verified** (this PR); the RST 8 hook path stays branch-resident + unverified until real Trinity. Remaining: wire the seam into the i83 server resolve (serve-by-name from records) + the i82 client write-out, exercised on hardware. | `src/netboot/bdos_seam.asm`; `tools/netboot-oracle/bdos/`; `docs/specs/samdos-file-io.md`; `docs/notes/bdos-version-landscape.md`; i82, i83, i62, i71 |
| **i95** | Phase-3 netboot **integrated server** — the headline Phase-3 program: a SAM + Trinity that netboots a Pi by answering its ARP + DHCP + TFTP in one main loop, serving boot files by name from the flat store. **Go authority LANDED** (this PR): `tools/netboot-oracle/server/server.go::Server.OnFrame` — the combined frame-in/reply-out dispatcher that routes a received frame to the right responder (ARP request for our IP → `smoke.Responder`; UDP:67 DISCOVER/REQUEST → `dhcp.Responder`; UDP:69 RRQ → `tftp.ServerLoop` OACK/ERROR + arm the transfer; UDP:our-TID ACK → FirstData/next DATA), composing the already-host-verified sub-responders. Host-verified (`TestIntegratedServerDispatch`/`TestIntegratedServerMiss`, `make ci-netboot-oracle`): a full DISCOVER→OFFER→REQUEST→ACK→ARP→RRQ→OACK→ACK→DATA-transfer session through `OnFrame` matches each standalone sub-responder byte-for-byte, incl. the short-final-block termination + ERROR(1)-on-miss-keep-serving + non-matching-frame-ignored. This is the **byte-for-byte porting spec for the Z80 integrated loop** (`src/netboot/netboot_server.asm`, to come — a new state machine that reads one frame and dispatches, composing the host-verified builders/parsers + the real `encdrv.asm` directly; it cannot `include` the standalone loop files, which each define their own RXBUF/CONFIG/`build_udp_frame`/`encdrv` — symbol collisions). **Z80 integrated loop LANDED (this PR):** `src/netboot/netboot_server.asm`'s `netboot_serve_once` — the new single-dispatch state machine (one `drv_read` → route ARP / DHCP / TFTP-RRQ / TFTP-ACK → `drv_write`), composing the host-verified builders/parsers + the real `encdrv.asm` directly (it does NOT `include` the standalone loops — `RXBUF`/`CONFIG_*`/`build_udp_frame`/`encdrv` symbol collisions avoided). Host-verified over the i80 emulation: a full DISCOVER→OFFER→REQUEST→ACK→ARP→RRQ→OACK→ACK→DATA-to-short-final-block session matches the Go authority byte-for-byte (`netboot_server_test.go::TestServerFullSession`, + lease-stable / miss-keeps-serving / serial-subdir-404 / ignores-unrelated; `make ci-netboot-z80`). Bootable disk shipped (`make netboot-server-disk` → B-DOS boot + AUTO LOADs+CALLs `netboot_main` at &8000, which reads the EEPROM MAC/IP + sets a fixed DHCP pool on the SAM's subnet) + the on-hardware guide (`netboot-trinity-testing.md` §"Increment 2"). **Remaining:** the B-DOS store walk (i93) supplies the real `src(name)` Source on real hardware (the host build stubs it with an in-RAM source + a flat STORE the test fills). **Verification line (CLAUDE.md §5):** the dispatch logic IS host-verifiable over the i80 emulation; an end-to-end Pi boot, the real ENC28J60 silicon, and the RST-8 B-DOS hook dispatch stay gated on real Trinity. Emulation-verified ≠ hardware-verified. | OPEN — 🔨 **Go authority + Z80 integrated loop + bootable disk LANDED + host-verified**; the only piece left is the on-hardware B-DOS store provisioning. | `tools/netboot-oracle/server/server.go`; `src/netboot/netboot_server.asm`; `docs/plans/phase3-netboot-implementation-plan.md` §3; `docs/notes/pi-netboot-capture-analysis.md`; `docs/notes/netboot-trinity-testing.md`; i80, i83, i86, i93, i94 |
| **i100** | **Firmware-download provisioning UX — user-selectable revision + file subset** (Pete, 2026-06-15). The on-SAM firmware downloader (i70's acquisition step) must let the user **pin an exact firmware revision** and **select which files to download** (e.g. list available + pick the subset), not hard-code "latest / all". **Reference:** Pete's `~/git/spectrum4/src/spectrum4/firmware/Tupfile` pins git SHA `a43df3a002f60c4c2243a416d045eb5937585e8b` (deliberately the last-working version before the next revision broke — RPi firmware #1979) + downloads 6 specific files (`LICENCE.broadcom`, `bootcode.bin`, `start.elf` ≈ 2.9 MB, `start4.elf` ≈ 2.2 MB, `fixup.dat`, `fixup4.dat`) from `github.com/raspberrypi/firmware/blob/<sha>/boot/<file>?raw=true`. Exact-revision pinning is **load-bearing** (the newest firmware may be broken); the `start*.elf` are the multi-MB blobs driving i99 streaming + the q16 record-spanning. **Open (the crux) = q15 (sharpened):** the **source model** — the reference is GitHub-raw-by-SHA over HTTPS, but the on-SAM download wants plain HTTP, and the apt archive is versioned by apt-pkgver (not git SHA) → can't serve an arbitrary pinned commit. Options: (b1) a pre-extracted HTTP mirror keyed by revision (agent's lean — plain HTTP, exact-SHA-addressable, no `.deb`/TLS), (b2) the apt archive (coarser + `.deb` extraction), or (c2) i88 TLS-to-GitHub. **SOURCE RESOLVED (Pete 2026-06-15: q15 option c):** fetch from `http://cdn.githubraw.com/<owner>/<repo>/<sha>/<path>` (plain HTTP, commit-SHA-addressable) + a SAM-side SHA-256 verify; i88 (TLS) the lowest-priority fallback. The downloader is now unblocked: revision pin (the `<sha>` in the URL) + file-subset selection (a manifest + picker) + stream-to-storage (i99) + record-spanning (q16) + SHA-256-verify. The revision/file-selection logic is autonomous + host-verifiable. **SHA-256 primitive landed:** `src/netboot/sha256.asm` (streaming `sha256_init`/`sha256_update`/`sha256_final`, FIPS 180-4), host-verified byte-for-byte vs Go `crypto/sha256` over the FIPS/NIST vectors (empty, "abc", the 56-byte two-block vector, the 1,000,000-'a' long vector, the 55/56/64/65/119/120-byte padding-boundary cases), fed both whole AND in awkward chunks (1/13/64/100/999) to prove the streaming partial-block carry; `make netboot-sha256`, the `netboot-z80` CI job (`tools/netboot-oracle/z80/sha256_test.go`). **Stream+verify increment landed (2026-06-15):** the SHA-256 primitive is now wired into the i99 streaming sink so a fetched body is **hashed AS it streams (never buffered whole) and verified against a pinned hash** — the q15 option (c) verify step. In `tcp_conn.asm` (all behind `NETBOOT_HOSTTEST`, alongside the streaming sink): `sha256.asm` is `include`d only in the host-test build; `conn_verify_init` resets the running hash before the body; `storage_sink_flush` feeds each flushed window through `sha256_update` (every body byte hashed exactly once, in order); `conn_verify_final` writes the digest into a 32-byte `CONN_HASH` and sets `CONN_HASH_MATCH` (1/0) by byte-comparing it against a caller-filled 32-byte `CONN_PINNED_HASH`. Go authority `tcp.HashingSink` (in `tcp/sink.go`) wraps any `tcp.Sink`, runs `crypto/sha256` over each Write, exposes `Sum()`/`Verify()`. Host-verified: `tcp_conn_verify_test.go` streams a multi-window body (≥3 flush windows, non-window-multiple length, segments straddling windows) with the hash on and asserts `CONN_HASH` == `crypto/sha256.Sum256(body)` byte-for-byte + the flag is 1 for a correct pin / 0 for wrong pins; `tcp/sink_test.go` covers `HashingSink` vs `crypto/sha256`. The default path + every existing oracle test stay green (`CONN_SINK_ENABLED` defaults 0 → no hash path taken; wire frames unchanged); the bootable `netboot_http_boot.bin` is **unchanged** (ends at `&FA52`, 1454 B under `&10000`) — the SHA-256 code + state are `NETBOOT_HOSTTEST`-only. `make netboot-tcp-conn` now also depends on `sha256.asm`. **cdn.githubraw.com path builder landed (2026-06-15, the downloader's first brick):** `src/netboot/fw_source.asm` — `fw_build_path` concatenates the request path `/<owner>/<repo>/<sha>/<path>` (e.g. `/raspberrypi/firmware/<sha>/boot/start4.elf`) into `FW_PATH` from the configured `FW_OWNER`/`FW_REPO`/`FW_SHA`/`FW_REPOPATH` (the reference pin). Standalone leaf (NOT yet wired into the fetch → the bootable image stays byte-identical), host-verified byte-for-byte vs the Go authority `http.GithubRawPath` (`http/source.go`) by `fw_source_test.go` — the FW_* config read back from the binary, the same one-source-of-truth pattern as HTTP_PATH/HTTP_HOST; `make netboot-fw-source`, in the `netboot-z80` CI job. The FW_* constants are where the user-selectable revision + file plug in later. **Manifest landed (2026-06-15, the downloader's second brick):** `fw_source.asm` now carries `FW_MANIFEST` — the six Tupfile files at `FW_SHA`, each a `{ output-name ptr, in-repo-path ptr, pinned SHA-256 }` record, walked by `fw_manifest_entry` (BC=index → BC=record ptr). The Go authority is `http.RPiFirmware` (`http/manifest.go`); the **pinned SHA-256s + sizes are verified to be the real content** of the firmware files at the commit (`manifest_test.go` re-hashes the local files when present — a local dev guard, skipped in CI), and the Z80 table is byte-compared to the Go manifest by `fw_source_test.go::TestFWManifest`. The hashes are baked (32 B each); the multi-MB blobs stay out of the repo. **Download plan / file selection (Go authority) landed (2026-06-15):** `http.Manifest.Plan(selection)` turns a manifest + a file selection (indices, in order; nil = all) into the ordered `FetchSpec{Name, Path, Host, SHA256}` list the per-file fetch loop executes; `Manifest.SelectByName` resolves user-chosen output names → indices (errors on unknown). This is the picker's backend logic + the authority the Z80 multi-file fetch loop will mirror (`http/manifest.go`, `manifest_test.go`). **bodySink (HTTP-header-skip) Z80 port landed (2026-06-15):** `src/netboot/body_sink.asm` — `body_sink_write` is the adapter that drops the HTTP/1.0 response header (everything up to and including the first `\r\n\r\n`) on the first flushed window, then forwards body bytes whole, so the bytes reaching storage + the SHA-256 verify begin at the first body byte (the digest is of the body alone, matching the pinned hash). The header skip lives here, one layer above `tcp_conn.asm` (a pure byte transport with no HTTP knowledge), relying on the header fitting in the first flush window (a chunk with no terminator is dropped — a missing/short body, never a header leak, byte-for-byte the Go bodySink). Standalone leaf, NOT yet wired into the fetch → the bootable `netboot_http_boot.bin` is byte-identical (still ends `&FA52`). Go authority `http.NewBodySink` (exported from `http/fetcher.go`; `StreamTo` routes through it); host-verified by `z80/body_sink_test.go` — the Z80 port and the Go `bodySink` (wrapping `tcp.ChunkSink`) fed the identical chunk sequence produce the same forwarded body bytes + per-Write boundaries, across the header-in-one-chunk, header-then-body split, multi-window, and degenerate no-terminator / split-header cases. `make netboot-body-sink`, in the `netboot-z80` CI job. **Per-file download-plan path builder landed (2026-06-15):** `fw_source.asm` `fw_plan_path` (BC = file index → builds that file's cdn path into `FW_PATH`) is the Z80 analogue of `http.Manifest.Plan`'s per-spec `Path` (= `PathFor(Files[i])`) — the per-file fetch loop's "build each selected file's URL" step. It composes `fw_manifest_entry` with the new parameterized `fw_build_path_for` (takes any in-repo path pointer; `fw_build_path` is now its fixed-`FW_REPOPATH` wrapper, behaviour preserved). Added the `FW_HOST` constant (`cdn.githubraw.com`, mirrors `http.GithubRawHost`) so the FetchSpec's Host is on the Z80 side too; the spec's Name/SHA come straight off the manifest record (+0/+4). Host-verified by `fw_source_test.go`: `TestFWPlanPath` asserts `fw_plan_path(i)` == `http.RPiFirmware.Plan(nil)[i].Path` for all six files, `TestFWHost` asserts `FW_HOST` == `http.GithubRawHost`; `TestFWBuildPath`/`TestFWManifest` stay green. Standalone leaf, bootable image byte-identical. With this, the host-verifiable Z80 wiring pieces are all done (`fw_build_path`/`fw_build_path_for`/`fw_plan_path` + `FW_HOST` ✅ + `FW_MANIFEST`/`fw_manifest_entry` ✅ + the verifying streaming sink ✅ + bodySink ✅); the remaining work is the **hardware-gated orchestration brick** (the `http_main` per-file loop + the real storage sink, validated on real Trinity) + the **picker UX** (interface design — for Pete). | OPEN — 🔨 unblocked (source resolved = `cdn.githubraw.com` + SHA-256 verify); **SHA-256 verify primitive landed + wired into the i99 streaming sink** (hash-as-you-stream + pinned-hash verify, host-verified); **cdn.githubraw.com path builder + pinned manifest + download-plan/selection authority landed** (`fw_source.asm`: `fw_build_path` vs `http.GithubRawPath`; `FW_MANIFEST`/`fw_manifest_entry` vs `http.RPiFirmware`; `Manifest.Plan`/`SelectByName`); **bodySink (HTTP-header-skip) Z80 port landed** (`body_sink.asm` vs `http.NewBodySink`, host-verified, bootable image byte-identical); **per-file download-plan path builder + `FW_HOST` landed** (`fw_plan_path` vs `http.Manifest.Plan`'s per-spec Path, `FW_HOST` vs `http.GithubRawHost`; `fw_build_path` refactored to `fw_build_path_for`) — all host-verifiable wiring pieces now done; **Go-authority orchestrator `http.Provisioner` LANDED + host-verified** (`tools/netboot-oracle/http/provision.go` — the rx-driven multi-file loop: walk the plan → per-file ARP/handshake/GET → stream the body through the header-skip + a SHA-256 verify into a per-file `Store` → record the pinned-hash verdict → advance to the next file; `provision_test.go` drives a 3-file plan end-to-end + the corrupted-body-rejected negative + the single-file degenerate case; the byte-for-byte porting spec for the Z80 `http_main`); next = the **Z80 `http_main` port** of the Provisioner (host-verified against this authority, wired to a stub storage sink) + the **hardware-gated real storage sink** (the B-DOS HSAVE write, &10000-ceiling-sensitive, validated only on real Trinity — Pete's test) + the **picker UX** (interface design, for Pete). **UPDATE 2026-06-16 — the Z80 `http_main` port HOST SLICE is COMPLETE** (Bricks 1–6 of `docs/plans/z80-http-main-port-plan.md`, PRs #308–#314: compose → dynamic path `HTTP_PATH_PTR` → bodySink header-skip seam → `prov_start` per-file re-init → the per-file store double (`store_begin`/`store_end`) → the `prov_first`/`prov_onframe`/`prov_next` download loop), every frame + verdict host-verified byte-for-byte vs the Go `Provisioner` (`http_main_*_test.go`). **Brick 7** (migrate the bootable onto the streaming loop + the real B-DOS HSAVE-per-record store leaf) is **DEFERRED — hardware/Pete (q18)**: the record cap is a real-Trinity RAM decision (`bdos/span.go` leaves it to "when the real persist is built") and the HSAVE leaf is non-host-verifiable (the q16 gate), so it cannot be built+verified autonomously. The **picker UX** also remains Pete's interface design. | `~/git/spectrum4/src/spectrum4/firmware/Tupfile`; `http://cdn.githubraw.com`; `src/netboot/sha256.asm`; `src/netboot/fw_source.asm` (`fw_build_path`/`fw_build_path_for`/`fw_plan_path`, `FW_HOST`, `FW_MANIFEST`/`fw_manifest_entry`); `src/netboot/body_sink.asm` (`body_sink_write`); `src/netboot/tcp_conn.asm` (`conn_verify_init`/`conn_verify_final`); `tools/netboot-oracle/http/source.go` (`GithubRawPath`/`GithubRawHost`), `http/manifest.go` (`RPiFirmware`/`Manifest.Plan`), `http/fetcher.go` (`NewBodySink`/`bodySink`); `tools/netboot-oracle/tcp/sink.go` (`HashingSink`); `docs/specs/phase3-delivery-design.md` §7–§8; i70, i99; q15 |
| **i101** | **End-to-end deliverable — one bootable SAM disk: fetch RPi firmware → assemble the spectrum4 release kernel on the SAM → TFTP-serve a full Pi 400 netboot** (Pete, 2026-06-15; "a beautiful deliverable"). A single bootable SAM disk that, on power-on, drives the whole stack end-to-end: **(1) fetch firmware** — the i100 downloader fetches the exact RPi firmware revision + files spectrum4 pins (`a43df3a002f60c4c2243a416d045eb5937585e8b`, the six `boot/` files) from `cdn.githubraw.com` over plain HTTP, **SHA-256-verified**, written to **Trinity storage** (i99 streaming + q16 record-spanning); **(2) assemble the kernel** — the **SAM-side aarch64 assembler** builds the spectrum4 release image (the byte-matched M6 `release.bin`/`kernel8.img`) from `release.tbn` **on the SAM itself**; **(3) serve it** — the integrated **DHCP/TFTP netboot server** (i70/i83/i86) serves the firmware + the freshly-assembled kernel so a real **Raspberry Pi 400 netboots from the SAM over the network**. This is the capstone integration of much of the whole project at once: the assembler (M6 release byte-match) + the firmware downloader (i100) + Trinity storage + the netboot server. **Goal:** the whole flow fits on **one bootable SAM disk image**. Verifiable in layers — the fetch, the assemble, and the serve are each already host-verified; the chained flow is testable in **SimCoupé emulation**; the **real deliverable is booting Pete's Pi 400 from it on real hardware** (the final integration gate, Pete's test — CLAUDE.md §5). Largely a *composition* of completed / in-progress parts; the new work is the orchestration (boot → fetch → assemble → serve, on one disk) + fitting it together. | BLOCKED:i100-prereq — captured (Pete 2026-06-15) — the capstone end-to-end deliverable; **gated on i100** (firmware download) landing first; the chained emulation E2E is autonomous-eligible, the real Pi-400 boot is Pete's hardware test | i100 (firmware download); M6 release byte-match ([[m6_release_bytematch_on_sam]]); i70/i83/i86 (netboot/TFTP server); i99 + q16 (Trinity storage); `~/git/spectrum4`; `docs/specs/phase3-delivery-design.md` |
| **i102** | **Aggressive T-state optimization of the netboot crypto / TLS-1.3 cipher-suite Z80** (Pete, 2026-06-15 — "a fun exercise"). Drive down both the execution time **and** the code size of the host-verified crypto leaves (`sha256`, `hmac_sha256`, `hkdf`, `hkdf_expand_label`, `chacha20`, `poly1305`, `x25519`, `aead`) by exploiting the full Z80 opcode set **including undocumented instructions**, while preserving **bit-exact reproducibility** (the per-primitive byte-for-byte host tests are the guardrail — an optimization that changes output fails CI). Methodology mirrors the ZX0 greedy-recompressor optimization precedent: a **T-state-counting harness** (extend the koron-go/z80 harness with cycle accounting, or a cycle-exact counter) measures an initial baseline against a fixed payload, then iterate to drive the total T-states down as far as it goes. **Dual benefit:** a faster handshake **and** a smaller resident footprint, which directly eases the q17 `&10000` budget (less code/state to page). Craft/fun exercise, no deadline; sits alongside i88 below the HTTP firmware deliverable in priority. | OPEN — 📋 captured (Pete 2026-06-15) — methodology = the ZX0-recompressor T-state benchmark; guarded by the existing byte-for-byte host tests; benefit = speed + size (eases q17). **Step 1 landed:** the cycle-exact T-state counter (`tools/netboot-oracle/z80/tstates.go`, anchor-verified + error-on-unknown completeness) is wired into the harness (`CallResult.TStates`) and the crypto baselines are recorded (`tstates_baseline_test.go`). **Opt-1 landed (`sha256` rotations):** rewrote the SHA-256 sigma/Sigma rotates to reach each fixed distance the SHORT way — whole-byte rotates (`sha_rotr8`/new `sha_rotl8`) plus a minimal bit residual that rotates left when nearer (e.g. ROTR7 = ROTR8+ROTL1, ROTR22 = ROTL8+ROTL2, ROTR25 = ROTL8+ROTR1), and SHR≥8 via a new whole-byte `sha_shr8` (SHR10 = SHR8+2·SHR1); the generic bit-at-a-time `sha_rotr_word_by` is deleted. Documented opcodes only (harness-faithful), byte-for-byte NIST vectors green. **−27.4%** on every SHA-256-bound leaf: sha256(256B) 6.45M→4.68M T, hmac 6.45M→4.68M, hkdf 15.50M→11.26M, expand_label 5.17M→3.76M; standalone binary +84 B. **Opt-2 landed (`x25519` quarter-square multiply):** replaced the hot `mul8` 8x8 shift-add loop (~2.6M calls per scalar mult, the dominant cost of the whole field layer) with the quarter-square identity `a*b = QSQ[a+b] − QSQ[|a−b|]`, `QSQ[n]=⌊n²/4⌋` — two lookups + a `sbc hl,de`. The 1022-byte table is built once at the `x25519` entry by `qsq_init` via the exact recurrence `QSQ[n]=QSQ[n−2]+(n−1)` (pure 16-bit adds, no multiply); it lives in RAM above the image (`equ &C000`), costing no file/ROM bytes. Documented opcodes only. **−23.0%**: x25519 2.084G→1.604G T (~479M cycles saved). Verified by a new exhaustive `TestMul8Exhaustive` (all 65536 byte pairs == true product) plus the RFC 7748 + Go-curve25519 end-to-end byte-exact tests; harness gained reusable `Entry.A`/`CallResult.HL` for register-level probing. Code +76 B, +1 KB RAM table. **Opt-3 landed (`poly1305` quarter-square multiply):** ported the proven x25519 quarter-square `mul8` + `qsq_init` to poly1305's identical shift-add `mul8` (self-built once at the `poly1305_mac` entry, so no test-loader change). Verified by a copy-independent exhaustive `TestPoly1305Mul8Exhaustive` (all 65536 pairs) + the poly1305 RFC/ref + aead RFC/round-trip/tamper byte-exact tests. **−9.5%** poly1305_mac (575k→521k T; the small-input qsq_init build cost is amortized) and **−11.5%** aead_encrypt (3.04M→2.69M T — the cascade, since aead includes poly1305). No double-definition risk (poly1305 included only by aead; x25519 standalone-only; no build pulls in both). **Opt-4 landed (`chacha20` redundant push/pop):** `cc_add`/`cc_xor` preserved HL/DE via push/pop, but every `cc_qr` caller reloads them — dropped the push/pop; since `cc_add` walks LSB→MSB it now leaves HL/DE advanced by 4, which let `cc_addstate`'s manual word-advance be deleted too (faster AND smaller). Byte-exact RFC 8439 + aead tests green. **−7.5%** chacha20_block (374k→345k T) / chacha20_encrypt (756k→700k T); aead a further −3.1% (−14.3% cumulative vs original baseline with Opt-3). First i102 increment that **shrinks** code. **Opt-5 landed (`sha256` add4 push/pop):** dropped the HL/DE-preserving push/pop from `sha_add4` (~648 calls/compress) — all 11 callers already reload HL/DE before reuse, and the documented contract already said "Clobbers DE,HL", so this just aligns code to contract. **−2.7%** on every SHA-256-bound leaf (sha256 256B 4.68M→4.55M, hmac 4.68M→4.56M, hkdf 11.26M→10.95M, expand_label 3.76M→3.66M); binary −4 B; full 1M-'a' NIST vector + dependents byte-exact. SHA-256 is now ~29.4% below its original baseline (6.45M→4.55M). **All four crypto leaves optimized**; the rotation/multiply/overhead surfaces are done — remaining i102 work is deeper structural passes (round-loop pointer plumbing, the quarter-round register allocation) with progressively smaller/riskier returns. | i88; q17; the netboot crypto leaves under `src/netboot/`; the koron-go/z80 harness `tools/netboot-oracle/z80` (cycle counting); the ZX0 greedy-recompressor optimization precedent |
| **i103** | **Autonomous-loop monitor — activity-gated quiescence** (stop drained-backlog nudges without going deaf; Pete, 2026-06-15). **Problem:** when the non-Pete backlog is genuinely drained, the monitor's ~30-min hang-detection keeps waking an idle session; each wake costs a model turn ("nothing to do") for zero progress. **Fix (Pete's design):** on confirming a genuine drain at a clean turn boundary, the agent writes a **quiescence semaphore** (e.g. `$SEMA_DIR/idle-confirmed`) telling the monitor to stop nudging — but the quiescence is **only valid while the session stays inactive**, so it can't strand the loop. **Mechanism:** when the monitor first observes the quiescence file it records the session transcript's current position (`~/.claude/projects/<slug>/<session>.jsonl` size/mtime); each poll, while the file exists *and* the transcript has not grown past that mark, the monitor stays **silent** (zero nudges, zero tokens); the instant the transcript grows (Pete writes back, or any new turn), the monitor **deletes the quiescence file itself** and resumes normal monitoring. So "leave me in peace" auto-expires on the first sign of life — no manual restart, no risk of a permanently-deaf loop. **Subtlety:** the agent writes the file as its last action, so the transcript may flush just after — record the transcript mark *when the monitor first sees the file* (not the file's own mtime), or add a small grace window, to avoid a spurious immediate re-wake. Agent side dovetails with the `feedback_autonomous_loop` drained-terminal note (confirm drain → write file → end turn). Builds on **i97**. | OPEN — 📋 captured (Pete 2026-06-15) — design agreed; activity signal = session `.jsonl` growth; auto-re-arms on new activity so it can't strand the loop; **non-blocking** enhancement | i97; q14; `tools/autonomous-loop/monitor.sh`; `feedback_autonomous_loop` memory |
| **i104** | **Per-session retro reports** (Pete, 2026-06-15). At wind-down each session writes a short, blameless retrospective to `~/claude-retro/` (outside the repo) on genuine process/workflow friction, **written independently** — not reading other sessions' reports first — so that converging signal across sessions is real, not echoed. The one rule: *say what genuinely happened — no manufactured problems, no suppressed ones*; **"nothing notable" is a valid, good report**; each friction grounded in a specific incident (the project's cite-your-evidence discipline). Reviewed by hand every few sessions / at milestone close → recurring frictions become actionable changes (CLAUDE.md / memory / a `qN`), and each round checks whether the *previous* round's fixes worked. No aggregation tooling until reports accumulate (don't build the analysis machine before the data). Distinct from the diary (`~/claude-diary/`, freeform personal continuity). | OPEN — 🔨 in progress — practice STARTED 2026-06-15 (`~/claude-retro/README.md` + report #1 written); the wind-down step is wired into the ROADMAP hygiene ritual (this PR); manual review in a few days | `~/claude-retro/README.md`; `docs/ROADMAP.md` §Session hygiene; `feedback_autonomous_loop` memory |
| **i106** | **SAM-as-terminal: an SSH (or telnet) client to reach a host — Pete's Mac — over the physical LAN** (Pete, 2026-06-16; a fun daily-driver direction, **usability is the priority**). The i88 TLS work built the reusable crypto: a SAM SSH client reuses **ChaCha20 + Poly1305 + SHA-256 directly** (modern SSH defaults = `curve25519-sha256` KEX + `chacha20-poly1305@openssh.com` cipher + an SHA-256-based KDF), riding the i70 **`tcp_conn`** transport. **Usability levers (Pete's framing — trust the physical cable, cheapen computation):** (1) **KEX stub à la Konamiman** — send the constant X25519 basepoint (`u=9`) as the client ephemeral pubkey and use the server's *received* pubkey directly as the shared secret (exploits `X25519(server_priv, basepoint) = server_pub`, so **zero EC math on the SAM** — the ~10-min real-X25519 handshake collapses to ~instant, and it still interoperates with stock OpenSSH `sshd`). Cost: no confidentiality (a handshake-capturing LAN sniffer derives the keys) — acceptable on a trusted direct link. Caveat: confirm OpenSSH accepts the basepoint as a client ephemeral key (Konamiman's secp256r1 analogue works against real servers). (2) **Skip host-key verification** — drops Ed25519 + SHA-512 entirely (the one genuinely-new crypto SSH would otherwise force), fine on a trusted cable. (3) **Rekey** — `RekeyLimit none` on the Mac (and with the KEX stub a rekey is free anyway) → no mid-session ~10-min stalls. After those, the only cost is per-byte ChaCha20-Poly1305 (~a few hundred B/s — usable for SAM **MODE 3 = 64×24**: a full redraw ≈ 5 s, typing instant). A **secure-mode toggle** (real X25519, ~10-min connect) is nearly free to offer since the i88 X25519 code exists. **Telnet alternative:** minimal SAM code (TCP + the telnet option dance, no crypto), but modern macOS ships no `telnetd` (install via Homebrew `inetutils`, bind+firewall to the link interface); plaintext. **Either path:** restrict to the physical link (bind + macOS firewall) so it is never internet-reachable. New SAM work = the SSH packet framing / state machine **or** a telnet client; the crypto primitives (SSH path) are already done. | OPEN — 💡 future daily-driver idea (Pete 2026-06-16); not scheduled; reuses i88 crypto + i70 `tcp_conn`; usability-first — **KEX-stub SSH the sweet spot** (instant, uses the Mac's built-in sshd, looks like real SSH), telnet the minimal-code fallback | i88 (reusable crypto: `src/netboot/chacha20.asm`/`poly1305.asm`/`sha256.asm`/`x25519.asm`); i70 (`src/netboot/tcp_conn.asm`); `docs/specs/tls13-handshake.md` §Prior art (the Konamiman trick); `future_roadmap` + `project_personal_pi_dev_machine` memory |
