<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/items.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Item registry — open

Work items with status `OPEN` or `IN_PROGRESS`. Items with status `DONE` or
`WONTFIX` live in `item-registry-closed.md`.

## Id conventions

`i<N>` · `i<N><letter>` · `i<N><letter>-b<M>` · `i<N><letter>-b<M><letter>`
(base → sub-item → brick → brick-part). Ids are immutable once used in a PR,
branch, or commit; never reused. Sort order is true-numeric (i5 < i41 < i237).

## Controlled status vocabulary (open file)

| token | meaning |
|---|---|
| `OPEN` | not yet started; or started but no agent has it in-hand |
| `IN_PROGRESS` | actively being worked right now |

"Blocked" is not a status token — it is a *derived* property of the dependency
graph. An item with an unsatisfied `depends_on` edge is still `OPEN`; its gating
item/question ids are listed in the `deps` column (the edges of the dependency DAG).

## Atomic-item rule

One row = one deliverable. If a row bundles independently completable parts, split
it into letter sub-ids (`i81a`/`i81b`…) so no finished part hides inside an
unfinished row.

## Dependency model

`depends_on` edges declare what must finish before this item can proceed, and are
shown in the `deps` column. A target may be an item (must reach `DONE`) or a
question (must be answered and deleted). Edges form a DAG; the validator rejects
cycles and dangling targets. The DAG is built from the `depends_on` fields in
`registry/items.yaml`; `grep depends_on` there for the machine-readable edge list.
The `dependents` column shows the reverse edges (items that depend on this one).

## Column layout

The generated table has 7 columns: `id | item | status | PR | deps | dependents |
refs/links`. `PR`, `deps`, `dependents`, and `refs/links` are each independent
first-class columns. The generated `.md` files are views of `registry/items.yaml`
— do not edit them by hand (hand edits fail CI).

<!-- The table below is generated — do not edit it by hand. -->


| **id** | item | status | owner | PR | deps | dependents | refs/links |
|---|---|---|---|---|---|---|---|
| **i2** | **On-SAM IDE memory model (edit buffer + IN/OUT paging; claim all free RAM, grow on demand)** — On-SAM IDE memory model — DECIDED (q36, Pete 2026-06-22). A SINGLE dynamic page pool (a resident page_owner[] allocator in section D, boot-sized from PRAMTP) shared by the editor AND the assembler: any subsystem alloc_page/free_page; a requester that can't get a page raises its own out-of-memory error. Supersedes the assembler's hardcoded off-axis pages 4-15 and FOLDS the i23 96KB-IN / i24 32KB-OUT ceilings into the pool. 256KB is FIRST-CLASS (~144KB doc ceiling; ~9 free pages); 512KB ~25 free pages. Pool exhaustion = refuse-with-message (the honest ceiling) — page persistence/SPILL is a DOWNSTREAM feature (depends on i2, never blocks it). Lazy edit/assemble spill: DOC blocks serialize to disk only when FREE can't satisfy an alloc, and only as many as needed (§4.5) — a 512KB machine touches disk zero times. IDE owns allocation for its session (rebuild BASIC ALLOCT only on exit). Spec: docs/specs/ide-memory-model-design.md. Implements the pool i41 §5.2 already assumes; lifts i23/i24 for free. | OPEN | agent |  |  |  | docs/specs/ide-memory-model-design.md, i41, i23, i24 |
| **i3** | **Editor groundwork (Phase 2) — full vision** — Phase-2 editor full vision umbrella. The editor groundwork encompasses all Phase-2 editor sub-features (see i43–i47 and sub-items). Full scope in ROADMAP. | OPEN | agent |  |  | i168 | docs/ROADMAP.md |
| **i4** | **Basic read-only listing/scroll viewer (centre-locked cursor; up/down only)** — SAM-side read-only viewer ported from the host prototype. The i76 P1 host viewer is the functional authority this SAM implementation will port. Carried forward by i76 Phase-1. | OPEN | agent |  | i2a |  | i76, docs/specs/editor-tui-prototype-design.md §5 |
| **i12** | **SimCoupé + test-harness tooling improvements** — Umbrella for SimCoupé emulator + editor-test-harness tooling improvements (version bumps, paste support, automated-editor input injection). Created 2026-06-20 (i153) so the i12a/b/c sub-items have their required umbrella parent — the children predate the parent-integrity rule. | OPEN | agent |  |  |  | tools/Dockerfile.dev |
| **i20** | **Linker-layout coupling (spectrum4.ld hardcoded in flatten)** — The flatten step has spectrum4.ld hardcoded; decouple the linker layout so other projects can use the toolchain without baking spectrum4-specific layout assumptions. Scope row. | OPEN | agent |  | i56 |  |  |
| **i24** | **16-bit OUT_LEN / 32 KB-output limit (OUT_LEN: defw 0 src/main_loop.asm; 32768-byte OUT ceiling src/encoder.asm)** — The output length field is 16-bit and the output buffer ceiling is 32768 bytes (src/encoder.asm). Lift the OUT_LEN field and ceiling to support assemblies larger than 32 KB. Deferred-backlog item. | OPEN | agent |  | i2a, i2b, q45, i218 |  |  |
| **i25** | **File-I/O error handler via DOSER (&5BC0)** — Surface SAMDOS file-I/O failures (HGTHD/HLOAD/HSAVE file-not-found, disk-full, wrong-type, ...) to the user instead of crashing the assembler. The app-facing error vector is DOSER (&5BC0), NOT (hksp) (see i185: (hksp) is zeroed on every hook entry — samdos/src/b.s:450-451 — and is SAMDOS-internal/NMI-only; an app handler there is never reached). Mechanism: port COMET's idiom (comet.asm prepare/dier/sproom, 1265-1382) — set (&5BC0) to a handler before file ops; ROM PTDOS does JP (DOSER) after every hook (rom DOSC :12977-12980,13003) with A = error number (0 on success). Scope: alt D (minimal, chosen) — the handler does AND A; RET Z on success, else converts the error into the existing diagnosed FAIL (jp fail with a load/save tag, reusing src/assembler.asm fail/fail_with_tag + src/print.asm); alt A (future enhancement) — per-error-number messages + stack recovery so the program could continue rather than halt. Also CORRECT the i183 harness, which models the wrong vector ((hksp)): model the DOSER post-hook dispatch (success + error) and treat an app-set (hksp) as zeroed, so the handler is verified against the real mechanism (emulation-first). Refs: docs/specs/samdos-file-io.md, comet.asm:1265-1382, src/loader.asm + src/main_loop.asm (hook call sites), tools/z80-test-harness-go/harness.go. | OPEN | agent |  | i183, i185 |  |  |
| **i25b** | **SAM-side DOSER (&5BC0) file-I/O error handler (port COMET)** — Implement the assembler-side DOSER (&5BC0) error handler: set (&5BC0) to a handler before file ops; the handler does AND A; RET Z on success, else converts the SAMDOS file-I/O error into the existing diagnosed FAIL (jp fail with a load/save tag, reusing src/assembler.asm fail/fail_with_tag + src/print.asm). Port COMET's prepare/dier/sproom idiom (comet.asm:1265-1382). Verify in the i25a harness DOSER model + SimCoupe. Scope: alt D (minimal, chosen); alt A (per-error-number messages + stack recovery) future. Refs: src/loader.asm + src/main_loop.asm hook call sites. | OPEN | agent |  | i25a |  |  |
| **i27** | **Cortex-A53 errata workarounds (--fix-cortex-a53-*)** — Cortex-A53 errata workarounds (--fix-cortex-a53-835769 / -843419). Pete (2026-06-22, q37): INCORPORATE — these are hard-to-debug latent bugs (835769: mul-accumulate then a 64-bit ldr/str spanning a page boundary; 843419: adrp at certain page offsets then a load); we've been lucky not to trip them. Ship as an EXPLICIT enable/disable feature in the SAM program (off by default for later silicon like Pi 4/400; on for Cortex-A53 / Pi 3). Greenfield (no Go authority): design + Go-first authority + Z80 port + binutils cross-check; scope = BOTH errata (assembler-side 835769 + linker-side 843419, placed at the right pipeline stage since we produce a linked release.bin). CAVEAT from Pete: if it turns out to be a massive amount of code OR a significant assemble-time performance hit, flag it and we reconsider — otherwise keep it as a toggle option. Fresh-session-sized. | IN_PROGRESS | agent |  |  |  |  |
| **i31** | **On-SAM preprocessor (.if build-constraints + macros)** — Beyond-M7 / future idea. On-SAM preprocessor supporting .if build-constraints and macro expansion, reducing reliance on the host-side preprocess step. | OPEN | agent |  | q38 |  |  |
| **i32** | **Multiple source files + staged/partial loading** — Beyond-M7 / future idea. Support for assembling from multiple source files with staged or partial loading, enabling larger projects than currently fit in a single-file workflow. | OPEN | agent |  | i2a |  |  |
| **i33** | **Trinity mass-storage: leverage B-DOS record seam for staged/overlaid assembly (i23/i24 ceiling lift)** — Use the proven Trinity B-DOS storage seam (src/netboot/bdos_seam.asm; spanning records in src/netboot/fw_span.asm) for staged/overlaid assembly, lifting the i23 96 KB IN / i24 32 KB OUT ceilings. Storage-access half proven by netboot work. Assembler still uses plain HSAVE/HLOAD for single-file IN→OUT; this item extends that to mass-storage-backed staging. Rescoped 2026-06-17: netboot proved records are accessible + writable; remaining deliverable is the assembler-scale integration. | OPEN | agent |  | i2a |  | memory/trinity_hardware.md, src/netboot/bdos_seam.asm, i23, i24 |
| **i39c** | **Phase 3 — bitfield-packing polish on the overlay slot bytes** — Optional second-order optimisation: pack the INSN_RUN overlay operand fields (reg=5b, kind-tag, shift-kind=2b, cond=4b, width=1b, mem-shape=3b) into sub-byte bitfields instead of one byte per field, per docs/specs/compact-tbn-nextgen-design.md §3.1 (which rates it "REFINE / accept narrowly" — real ~-4 KB but the least leverage per unit of Z80 complexity, a polish layered on the structural wins, not a headline). A SEPARATE concern from the comment/blank-run parsing brick (i48c-b7); the earlier "absorbed into i48c-b7" framing conflated two unrelated subsystems and pointed at a stale design ref (i48-syntactic-encoder-design.md has no §3.1). De-coupled 2026-06-21: gated on i48c-b8 (do the format-packing polish once the overlay encoder is functionally complete, so it isn't re-churned). Migrated to M9 at M8 close (2026-06-12). | OPEN | agent |  | i48c-b8a, i48c-b8b, i48c-b8c, i48c-b8d, i48c-b8e |  | docs/specs/compact-tbn-nextgen-design.md §3.1, docs/notes/m9-status.md, i48c |
| **i41** | **Editor edit-buffer data structure — insertion performance for large source** — Architectural decision: the editor holds source in a separate insertion-friendly in-memory document model (paged block-list: block = page, intra-block gap buffer, record-id side-tables; worst-case edit ~30–80 ms at 400 KB) and serializes to .tbn on save/assemble. Unifies the editor document model with i48's IR (edit-model records = in-memory symbolic IR). Design agreed (opus, 2026-06-08), 5 key decisions in docs/specs/editor-edit-model-design.md §7. Decomposed into i41a–i41e. | OPEN | agent |  |  |  | docs/specs/editor-edit-model-design.md §7, docs/notes/m9-status.md, i41a, i41b, i41c, i41d, i41e |
| **i41d** | **Editor edit-model — Z80 port of the paged block-list (on-SAM)** — Faithful Z80 port of the i41a Go authority (tools/sam-aarch64/editmodel), per CLAUDE.md §6. Scoped in design §8.1. Bricks 1+3+4a COMPLETE: insert/split/loc/goto (PR #387), delete/merge/free-list (PR #389), bounded ring-journal undo/redo (PR #391), EMDL serialize/load (PR #392), src/editmodel.asm, host-verified vs oracle. REMAINING: Brick 2 (paging onto i2 pool via OUT (251) — SimCoupé-gated), Brick 4b (real v2 .tbn path — couples to assembler build, deferred). | OPEN | agent |  | i2a |  | src/editmodel.asm, tools/netboot-oracle/z80/editmodel_test.go, docs/specs/editor-edit-model-design.md §8.1/§4.2/§5, i41a |
| **i41e** | **Editor edit-model — i48-IR record payload + document symbol table + hybrid record model** — Replace i41a's raw-text line payload with the i48 in-memory symbolic IR (parsed format.Record): a document-global symbol table + name-id/record-id side-tables (§3), a hybrid committed-parsed / active-raw / invalid-error record type (§7.3), per-line text→IR entry. q24 resolved (2026-06-20): the editor validates ONE LINE AT A TIME — no whole-document import to serialize invalidly; the design point is how an in-progress/invalid LINE is held + round-tripped (whole-file import = i165). Open design (symbol-table lifecycle, hybrid sum type, per-line parse) — needs a design pass (§8/§5.2/§6/§7.3). | OPEN | agent |  | i2a |  | docs/specs/editor-edit-model-design.md §8/§5.2/§6/§7.3, docs/specs/i48-syntactic-encoder-design.md, i41a, i41c, i48c |
| **i42** | **CRC / signature in the .tbn file header (integrity check)** — Deferred since M1. The v1 header has magic/version/flags but no integrity field. Add a CRC or signature field to the .tbn header for integrity checking. Referenced in the M1 status 'Known gaps' list. | OPEN | agent |  |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/m1-status.md |
| **i43** | **Editor: register simulator with user-chosen seeds + replay-on-edit** — Phase-2 editor sub-feature. Register simulator with user-chosen seeds: step instructions, inject/randomise source regs, show dest regs/flags/memory, and replay-on-edit. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent |  |  |  | docs/ROADMAP.md |
| **i44** | **Editor: inline instruction-explanation panel (prose semantics, flags, regs, bit-field layout)** — Phase-2 editor sub-feature. Inline instruction-explanation panel showing prose semantics, flags set, regs read/written, and bit-field layout. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent |  |  |  | docs/ROADMAP.md |
| **i45** | **Editor: 'did you mean a simpler instruction?' result-equivalent rewrite hint** — Phase-2 editor sub-feature. Suggests result-equivalent but simpler instruction rewrites when a more concise encoding exists. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent |  |  |  | docs/ROADMAP.md |
| **i46** | **Editor: system-register documentation panel (ARM MRA subset + editorial overlay)** — Phase-2 editor sub-feature. System-register documentation panel surfaced from the cursor line, embedding an ARM MRA subset with editorial overlay. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent |  |  |  | docs/ROADMAP.md |
| **i47** | **Editor: inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations)** — Phase-2 editor sub-features. 'Why is this instruction here?' inverse-flow navigation plus retro UI affordances including chiptune audio, period fonts, and animations. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent |  |  |  | docs/ROADMAP.md |
| **i48** | **Single serialized format + pass-free syntactic encoder (the design)** — Design for single serialized format + pass-free syntactic encoder. Decision A: the compact overlay is the only serialized .tbn; the symbolic kinds (KindInst/LabelDef/LocalDef/LitInsts) become an in-memory IR, never written. Decision B: text->overlay is a pure syntactic transform; value-dependent bits computed in the fold at assemble time; forego GNU silent rewrites; narrow assemble-time mov->movz/orr/movn fallback. Host side COMPLETE (i48a #144, i48b, i48d). i48c (Z80 text->overlay encoder) is the M9 carry-forward. | OPEN | agent |  |  |  | docs/specs/i48-syntactic-encoder-design.md, https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md, i48c |
| **i48c** | **Z80 text→overlay encoder (editor input path)** — SAM-side mirror of the host front-end (tools/sam-aarch64/frontend: lexer→parser→records). Go i48b is the authority. Absorbs i39c (overlay slot bitfield-packing polish). Large multi-PR port decomposed into bricks B1–B8 (scoped 2026-06-17). B1–B7 are flat-memory-autonomous (koron-go/z80 harness, netboot-z80 CI). B8 is assembler-coupled, lands last. Host Preprocess (.include/.macro/.if) is out of scope (host-only bulk-import concern). | OPEN | agent |  |  |  | src/asmlex.asm, src/asmparse.asm, tools/netboot-oracle/z80/asmlex_test.go, tools/netboot-oracle/z80/asmparse_test.go, tools/sam-aarch64/frontend/lexer.go, tools/sam-aarch64/frontend/parser.go, docs/specs/i48-syntactic-encoder-design.md §2, docs/specs/i48-syntactic-encoder-design.md §7, docs/notes/m9-status.md, i39c, i109 |
| **i48c-b8** | **Integration/fixture round-trip (assembler-coupled, lands last)** — Wire the completed text->overlay encoder (B1-B7) into the full assembler path so the SAM produces host-identical compact v2 .tbn from typed text, proving text->overlay byte-match over the fixture corpus. DISCOVERED SCOPE (2026-06-22): the SAM assembler reads label/local DEFINITIONS from the .tbn HEADER TABLES (reader.asm:151), not from the encoder's inline LABEL_DEF/LOCAL_DEF IR records, so a Z80 port of the host compact-serializer pipeline sits between encoder and assembler: assemble.Pass1 (PC assignment + label/local position capture + symbol resolution over the IR) -> assemble.Compact (IR -> INSN_RUN/LIT_DATA overlay records + COMMENT/BLANK_RUN->sidecar + .global->globals) -> headerRows + format.WriteFile (header tables w/ uvarint deltas + front-coded name table + editor region). Go authority: tools/sam-aarch64/assemble/{pass1,compact}.go + tools/sam-aarch64-format/writer.go. Umbrella; decomposed into b1-b4. | OPEN | agent |  | i48c-b5b, i48c-b5c, i48c-b5d, i48c-b5e, i48c-b5f, i48c-b6, i48c-b5 |  | src/assembler.asm, src/asmparse.asm |
| **i48c-b8c** | **Z80 serialize: headerRows + WriteFile -> compact v2 .tbn bytes** — Port headerRows (compact.go:196) + format.WriteFile (tools/sam-aarch64-format/writer.go) to emit the full compact v2 .tbn: 12-byte header (magic SA64 + version 2 + flags + editor_region_offset u32), header label table ([count u16][name_id u16][delta uvarint] rows) and local table ([count u16][digit u8][delta uvarint] rows) built from b8a positions, the front-coded name table, the b8b compacted records, then the editor region (sidecar comment/blank rows + globals). Deltas are uvarint-encoded running offsets. Harness-test the serialized bytes vs host CompactTBNBytes/WriteFile over fixtures. | OPEN | agent |  | i48c-b8e | i39c, i48c-b8d |  |
| **i48c-b8d** | **Corpus round-trip integration: text -> compact .tbn on SAM byte-matches host (+optionally assemble to OUT)** — The lands-last wiring: drive the full SAM pipeline text -> lex(asmlex) -> parse(asmparse) -> Pass1-over-IR(b8a) -> Compact(b8b) -> serialize(b8c) over the fixture corpus and assert the produced compact v2 .tbn bytes byte-match the host CompactTBNBytes output (the m9 i48c done-criterion: text->overlay byte-match host for the corpus). Optionally feed the produced .tbn through the existing assembler reader+pass1/pass2 to OUT bytes and compare to the host-assembled output, closing text->bytes end-to-end. Wire a corpus-driven harness test (and, if it fits the boot budget, a SimCoupe self-test). ARCHITECTURE CONSTRAINT (found in b8a, PR #557): the parser tables (asmparse SYM_NAMES/PARSE_RECS) and the assembler pass-1 tables (assembler.asm OPVAL_ARRAY/PASS_PC@&C159/LITPOOL@&D200/LOCAL_LABEL_TABLE@&E280) BOTH claim the &C100-&EFFF scratch region, so the full text->.tbn chain cannot run in one flat binary with today's layout. b8a side-stepped this by producing the IR host-side; b8d (and the real SAM editor) must resolve the memory layout (relocate/page one stage's tables) so parse_run + pass1 + compact + serialize coexist. This is the integrated-build memory-model question (relates to q36). | OPEN | agent |  | i48c-b8c | i39c |  |
| **i48c-b8e** | **Compact encoder adapter: literal-probe + overlay-patch emission (paged, needs ENCTAB)** — Port the encoder-dependent half of assemble.Compact. SCOPE FINDING (2026-06-22 investigation): this is the LARGEST B8 brick and the architecturally most significant — the SAM gains its FIRST standalone instruction-encode capability. The Z80 has NEVER encoded a symbolic INST record from scratch; today it only FOLDS pre-computed base words (host produces base words via Go encodeInst; the Z80 insn_run.asm pass2 folds {slot,expr} patches). For text->.tbn on the SAM, the SAM must encode. So b8e ports ~800 LOC of NEW Z80: (1) encodeInst dispatch (pass2.go:214-373 + operandsToValues: operand-kinds -> form -> slot values -> base word, incl. the special-form intercepts lsl/lsr/bitfield/bic-imm/csetm/barriers/ror/mrs-msr/dc-tlbi + compound mem/shifted/extended forms); (2) overlayClassify (overlay.go:82-163: the encodeInst mirror that returns {relocated-slot, expr} instead of emitting); (3) literalWord (compact.go:354-370: encode at pcProbeA=0 and pcProbeB=0x40000; equal => bare 4-byte literal element). REUSES (already proven in pass2): the 13 slot folders (insn_run.asm fold_*), the per-slot encoders (src/slots/*.asm), form_lookup (form_lookup.asm), eval_expr_const, the symbol/local tables. HARNESS: flat is IMPOSSIBLE (needs the paged ENCTAB + form_lookup + slot encoders = the whole assembler). Use a BUILD_TESTS boot self-test inside the full paged assembler build (precedent: assembler.asm:357-468 self-tests; test_mem_offaxis.asm off-axis pattern), where ENCTAB is already mapped by main_assemble's enctab_map_in bracket; the Go harness synthesises a fixture, the self-test runs the encoder, the Go side compares vs host encodeInst/overlayClassify. SimCoupe-gated (it's in the assembler build). Likely deserves a short spec + possible further decomposition (encodeInst dispatch / overlayClassify / literalWord+wiring) at session start. Go authority: pass2.go encodeInst+operandsToValues, overlay.go overlayClassify, compact.go literalWord. | OPEN | agent |  | i48c-b8b, i199, i204, i203, i201 | i39c, i48c-b8c |  |
| **i50** | **Host Go-tool CLI shape — deferred design question (one binary vs three vs pandoc-style)** — Deferred design question (Pete 2026-06-09): whether host tools stay one format-aware front door (sam-aarch64, auto-detects SA64 magic), revert to three binaries (encode/decode/translate), or adopt pandoc-style --from/--to. Three concerns are already cleanly separated as libraries (aarch64enc / aarch64dec / sam-aarch64-format+frontend); CLI count is a thin presentation choice, orthogonal to the i48 in-memory-only-symbolic-form win. Revisit when bulk GNU→.tbn import and listings host features land. Go tools are Z80-port scaffolding + oracle (memory/project_go_tools_are_z80_scaffolding.md). | OPEN | pete |  |  |  |  |
| **i55** | **Multi-section / linker-script honouring in refenc** — Explicitly punted in favour of the text2bin -flatten approach. Multi-section / linker-script handling in refenc would only be needed if a non-spectrum4-shaped project surfaces. Deferred; from ROADMAP deferred checklist (retired in i52 PR 3). | OPEN | agent |  |  |  |  |
| **i56** | **SpectrumFourLayout extraction (-layout flag or linker-script parser)** — Not worth doing unless a second project surfaces. SpectrumFourLayout extraction via -layout flag or linker-script parser is a Pete-gated call; deferred. From ROADMAP deferred checklist (retired in i52 PR 3). | OPEN | agent |  | q42 | i20 |  |
| **i65** | **SAM-side save-without-comments option (editor phase)** — On-SAM counterpart to the host -strip-comments/-thin-comments flags. Saves a .tbn with the editor region stripped/thinned so a 256 KB SAM can assemble sources authored on a 512 KB machine. q9 resolved (Pete 2026-06-10): the tool targets both 256 and 512 KB SAMs; the full unstripped release .tbn is allowed to exceed 256 KB, but a 256 KB user must still be able to assemble via this stripped disk handoff. Host equivalent already exists. | OPEN | agent |  |  |  | i59 |
| **i66** | **Trinity page-persistence as a general persist-to-mass-storage technique** — Persist SAM pages to Trinity B-DOS records as a general storage technique. Deferred by Pete's decision (2026-06-10, q9 resolved): 'no value implementing right now'; revisit only if a concrete need appears. Conditional on i66's reactivation being Pete's call. | OPEN | agent |  |  |  | docs/notes/bdos-version-landscape.md |
| **i70** | **Phase-3 firmware self-provisioning: SAM fetches Pi firmware blobs via HTTP and persists to Trinity storage** — SAM IP stack fetches Pi firmware (several MB) from cdn.githubraw.com over HTTP (q11 resolved: on-SAM HTTP via uIP, host-tool-free; q12 resolved: Model A only). Full host-verifiable HTTP-fetch path complete + host-verified across Bricks 1–6 (#245/#247/#254/#256/#257/#259 netboot_http.asm). Remaining: hardware-gated (EEPROM read + section-C paging + HSAVE RST-8 dispatch on real Trinity; emulation-verified != hardware-verified, CLAUDE.md §5). i82 TFTP client bridges provisioning meanwhile; HTTPS stretch is i88. | OPEN | agent |  | i133 |  | docs/specs/phase3-delivery-design.md, docs/notes/trinity-capabilities.md, docs/notes/bdos-version-landscape.md, src/netboot/netboot_http.asm, docs/notes/netboot-trinity-testing.md, i61, i66, i84, i88 |
| **i73** | **i17 review residuals — LOW-severity hardening and dead-code findings** — Low-severity hardening items split from i17 (PR #185). Landed: L1 emit_byte off-by-one (PR #338), L6 .align>=16 silent no-op (PR #339), L7 pass-1 backward-.org check missing (PR #340), L9 retired symbolic-encoder path removed (PR #371, q20 sweep). Open: L3 symbol_abs_bit_ptr missing id<512 bound; L4 exactly-full-buffer conservative rejections; L5 zero-length LIT_INSTS BC underflow; L10 u8 segment-counter wrap; L11 load_in_file editor-offset wrap; L12 .set redefinition divergence. Follow-up i109 (vestigial soft-reject hook). | OPEN | agent |  |  |  | PR #185, i17 |
| **i74** | **i7 phase D — at/ic/barrier operand-table codegen from the Go authority** — Codegen the at/ic/barrier operand tables from the Go authority (spec §6 Q3). Deferred by sequencing only (Pete 2026-06-12: tracked so it automatically gets done); queue after i7 phases A–C land. Gated on the aarch64dec/sys.go code→data refactor (i7 phase D prerequisite). | OPEN | agent |  |  |  | docs/specs/codegen-tables-design.md, i7 |
| **i77** | **spectrum4 source succinctification — comments + pathological constant expressions** — Agent-driven, section-by-section, human-reviewed (Pete 2026-06-12): (a) comments rewritten aggressively succinct but still readable; (b) pathological constant expressions replaced with named equates (e.g. the longest instruction in the corpus: `movk w0, (65536 \| (((BORDER_COLOUR >> 16) & 65535) & 65535)), lsl #16`). Coordinate with upstream spectrum4 (content originates there; release.s re-flattens). Ties to editor-tui spec D1 (all comments <256B → u16 record length can revert to u8). Parked (editor era). | OPEN | agent |  |  |  | docs/specs/editor-tui-prototype-design.md, i76 |
| **i81** | **Upstream SimCoupé issue triage — review open issues and assist where we can** — Umbrella for SimCoupé upstream community contributions. Triage complete (2026-06-13): produced contributions i81a (PNG-endian fix PR — BLOCKED:Pete), i81b (issue #28 comment — DONE PR #370), i81c (SDL headless fallback — BLOCKED:Pete). i81a and i81c remain open; i81b is done. | OPEN | agent |  |  |  | tools/font-proof/simcoupe-local-capture.patch, i80 |
| **i81a** | **PNG-endian one-line upstream SimCoupé PR: the __aarch64__ little-endian-guard fix in Base/SimCoupe.h** — hunk 2 of tools/font-proof/simcoupe-local-capture.patch; confirmed NOT in upstream HEAD; ready to offer standalone; community submission is Pete's call. | OPEN | pete |  |  |  | tools/font-proof/simcoupe-local-capture.patch, i81, i80 |
| **i81c** | **Optionally offer the SDL dummy-driver headless renderer fallback upstream** — Lower priority than i81a; community submission is Pete's call. | OPEN | pete |  |  |  | i81, tools/font-proof/simcoupe-local-capture.patch |
| **i87** | **Dump + reverse-engineer Pete's patched SAM ROM (Phase-3)** — Pete's SAM runs a modified system ROM giving Trinity-interface access at power-on (no B-DOS floppy needed). When his SAM is next online he dumps the ROM to disk; we then diff it against the stock SAM ROM to document exactly how Trinity-at-boot is patched in, so the boot mechanism is reproducible/understood. Not a blocker — trinload can net-load the client/server in the interim, and trinity.mgt + reference/bdos cover the interface. Hardware-gated: artifact production needs Pete's hardware. | OPEN | agent |  |  |  | docs/specs/phase3-delivery-design.md §3, i84 |
| **i88** | **On-SAM HTTPS/TLS-1.3 client to fetch Pi firmware directly from GitHub** — Phase-3 stretch, LOWEST priority (Pete: only when nothing else is left). TLS-crypto-only: github.com-only, pinned public key, no CA store (content is SHA-256-pinned so cert verification is skipped). Decomposed into i88a (6a host-verifiable state machine), i88b (6b record-reassembler), and i88c (6b hardware-gated integration). | OPEN | agent |  |  |  | docs/specs/phase3-delivery-design.md, src/netboot/tls_client.asm, tools/netboot-oracle/tls/, docs/specs/tls13-handshake.md, i70, i84, i100 |
| **i88c** | **i88 6b — hardware-gated integration: wire tls_reasm into tcp_conn, paging layout, bootable disk, real RST 8 on Trinity** — Wire tls_reasm_feed into tcp_conn CONN_SINK_FILTER; finalise paging layout; build a bootable disk; exercise over real RST 8 on Trinity hardware. Prerequisite: TC_RX must grow from 1056 to 16645 B (TLS max) before a real Certificate record fits. Hardware-gated (CLAUDE.md §5). | OPEN | agent |  | i133 |  | src/netboot/tls_reasm.asm, src/netboot/tls_client.asm, src/netboot/tcp_conn.asm, i88, i88b |
| **i89** | **Capture a Pi 3 netboot exchange (Phase-3) — extend i83/i86 oracle to the older Pi family** — Confirm Pi 3 boot ROM's DHCP behaviour (may differ from Pi 4 PXE option-43 dance) and its TFTP file set (bootcode.bin/start.elf/fixup.dat). The oracle extension proceeds from the already-known Pi 3 file set (from the tftproot), so a fresh capture is not a blocker; an optional real-hardware confirmation (hub + Wireshark / trinload technique) can be run when convenient. | OPEN | agent |  |  |  | docs/notes/pi-netboot-capture-analysis.md §4, i83, i86, i84 |
| **i90** | **%nobits/BSS section bodies dropped by -flatten — source-structure loss** — When source is flattened (-flatten, the release path), the entire body of every %nobits/BSS section (bss_kernel/bss_roms/…) is discarded: contributes zero bytes, so blank lines/comments inside BSS don't round-trip (release.s: 132 of 1472 blank lines, all in NOBITS sections). Pre-existing flatten property, not an i78 bug — i78 blank/comment preservation is exact for PROGBITS. Only matters once the SAM editor edits a flattened source with BSS content. Captured only — decide whether to preserve NOBITS editor content if/when the flattened-edit path needs it. | OPEN | agent |  |  |  | docs/specs/source-structure-preservation-design.md, tools/sam-aarch64/frontend/flatten.go, i78 |
| **i93** | **Phase-3 netboot B-DOS storage seam (bdos_seam.asm) — name-to-record glue** — Name→record glue shared by i83 server and i82 client. Three routines from Go authority bdos/bdos.go, host-verified: bdos_name_to_uifa (name→48-byte UIFA), bdos_difa_to_size (DIFA→32-bit size for OACK tsize), bdos_fill_save_uifa (name+page+offset+size→HSAVE UIFA). Field conventions from src/io.asm/loader.asm/main_loop.asm + samdos-file-io.md. Seam arithmetic LANDED + host-verified. Remaining: RST 8 hook dispatch (HGTHD/HLOAD/HSAVE/HRECORD) stays unverified until real Trinity (hardware-gated, CLAUDE.md §5). | OPEN | agent |  |  |  | src/netboot/bdos_seam.asm, tools/netboot-oracle/bdos/, docs/specs/samdos-file-io.md, docs/notes/bdos-version-landscape.md, i82, i83, i62, i71 |
| **i93b** | **B-DOS RST 8 hook dispatch on real Trinity — hardware-gated (HGTHD/HLOAD/HSAVE/HRECORD)** | OPEN | agent |  | i133 | i95b |  |
| **i95** | **Phase-3 netboot integrated server — single dispatch loop (ARP+DHCP+TFTP)** — Headline Phase-3 program: SAM+Trinity netboots a Pi by answering ARP+DHCP+TFTP in one loop, serving boot files by name from the flat store. Go authority + Z80 netboot_serve_once LANDED + host-verified: full DISCOVER→OFFER→REQUEST→ACK→ARP→RRQ→OACK→DATA session vs Go byte-for-byte. Bootable disk shipped. Hardware first-light (2026-06-18): ARP+TFTP sub-responders real-Trinity-verified (i96). Remaining: DHCP server + B-DOS store provisioning (i93) + Pi-boot gated on real Trinity. | OPEN | agent |  |  |  | tools/netboot-oracle/server/server.go, src/netboot/netboot_server.asm, docs/plans/phase3-netboot-implementation-plan.md, docs/notes/pi-netboot-capture-analysis.md, docs/notes/netboot-trinity-testing.md, i80, i83, i86, i93, i94 |
| **i95b** | **End-to-end Pi boot on real Trinity — hardware-gated: DHCP + B-DOS provisioning + real Pi boot** | OPEN | agent |  | i93b |  |  |
| **i100** | **Firmware-download provisioning UX — user-selectable revision and file subset** — On-SAM multi-file firmware downloader with user-selectable revision pin and file subset. Host-verified slice complete as i100a; bootable migration + real HSAVE store leaf complete as i100b (closed). Remaining: i100c picker UX (Pete-gated interface design). | OPEN | agent |  |  |  | src/netboot/fw_source.asm, src/netboot/http_main.asm, docs/specs/phase3-delivery-design.md, i100a, i100b, i100c |
| **i100c** | **Picker UX — user-facing revision/file-selection interface for the firmware downloader** — User-facing revision/file-selection interface for the firmware downloader. Interface design is Pete's call (editor-era UX, no autonomous decision here). No specific qN gates it — bare Pete decision. | OPEN | pete |  |  |  | docs/specs/phase3-delivery-design.md, i100 |
| **i101** | **End-to-end deliverable: fetch RPi firmware → assemble spectrum4 release on SAM → TFTP-serve a full Pi 400 netboot** — The integrated SAM IDE on ONE bootable disk — editor + assembler/disassembler + TFTP server — an interactive USER-DRIVEN program (q23 resolved 2026-06-20: NOT a boot→fetch→assemble→serve auto-sequence). User loads it, starts/edits a project, assembles release.bin/kernel8.img from release.tbn (M6 byte-match), and TFTP-serves so a Pi 400 netboots — features exercised on demand. The three org-&8000 layers coexist via paging (implementation detail), not sequential overlays. NOT SimCoupé-testable (no Trinity ENC/SD); real E2E is Pete's hardware gate. Arch: docs/specs/phase3-delivery-design.md §13. | OPEN | agent |  |  |  | docs/specs/phase3-delivery-design.md §13, i100, i70, i83, i86, i99, i80 |
| **i102** | **Aggressive T-state optimization of the netboot crypto / TLS-1.3 cipher-suite Z80** — Drive down T-states and code size of host-verified crypto leaves (sha256, hmac_sha256, hkdf, chacha20, poly1305, x25519, aead) using full Z80 opcode set including undocumented instructions; bit-exact reproducibility preserved (byte-for-byte host tests are the guardrail). Craft/fun exercise, no deadline. Landed as sub-items i102a–i102l + i102n–i102r (all DONE, closed registry): x25519 cumulative -50.4% (1.604G→0.796G), SHA-256 -56.8% (729K→315K T/block, fastest verified Z80 SHA-256). Remaining: i102m (sha256 throughput umbrella — sub-items mostly done, one small lever left). | OPEN | agent |  |  |  | i88, src/netboot/, tools/netboot-oracle/z80/ |
| **i102m** | **i102 sha256 throughput rewrite for the firmware-verify path** — Increments i102n-i102r landed (closed registry): sha256 729,795->315,355 T/block (-56.8%) — fastest verified Z80 SHA-256. Decomposed into i102n–i102r (all DONE) + i102s (message-schedule extend loop lever, OPEN). Low priority (world-best already, no deadline). | OPEN | agent |  |  |  | src/netboot/sha256.asm, tools/sha256-unroll-gen/, src/netboot/tcp_conn.asm, i100, i101, i102 |
| **i102s** | **i102 sha256 message-schedule extend loop — BCDE accumulate in add4_body (~2% gain)** — The message-schedule extend loop still does 3 term adds memory-to-memory (add4_body). Applying the BCDE register-accumulate pattern used in the round path to add4_body eliminates the write-back round-trips, netting an estimated ~2% further reduction. One clean lever remaining after i102r. Low priority (world-best already, no deadline). | OPEN | agent |  |  |  | src/netboot/sha256.asm, i102m, i102r |
| **i103** | **Autonomous-loop monitor — activity-gated quiescence** — When non-Pete backlog is drained, the monitor keeps waking an idle session. Fix: agent writes a quiescence semaphore on confirming a drain; monitor stops nudging while the file exists AND session transcript has not grown past the recorded mark. The instant the transcript grows (Pete writes back or any new turn) the monitor deletes the file and resumes. Auto-expires on first sign of life — no manual restart, no permanently-deaf loop. Record the transcript mark when monitor first sees the file (not file mtime) to avoid spurious re-wake. Builds on i97. | OPEN | agent |  |  |  | i97, tools/autonomous-loop/monitor.sh |
| **i104** | **Per-session retro reports** — At wind-down each session writes a short blameless retrospective to ~/claude-retro/ on genuine process/workflow friction, written independently (not reading other sessions' reports first) so converging signal across sessions is real, not echoed. Rule: say what genuinely happened; 'nothing notable' is a valid report; each friction grounded in a specific incident. Reviewed by hand every few sessions / at milestone close -> recurring frictions become actionable changes (CLAUDE.md / memory / qN). Distinct from the diary (~/claude-diary/). Practice started 2026-06-15. | OPEN | agent |  |  |  | ~/claude-retro/README.md, docs/ROADMAP.md |
| **i106** | **SAM-as-terminal: SSH (or telnet) client to Pete's Mac over the physical LAN** — KEX-stub SSH the sweet spot: send constant X25519 basepoint as client ephemeral pubkey; use server's received pubkey as shared secret — zero EC math, instant connect, interoperates with stock OpenSSH sshd. Skip host-key verification (drops Ed25519+SHA-512). Per-byte: ChaCha20-Poly1305 ~few hundred B/s, usable for SAM MODE 3 64x24. Secure-mode toggle (real X25519, ~10-min) nearly free via existing i88 X25519. Telnet fallback: minimal SAM code but needs Homebrew inetutils on Mac. Reuses i88 crypto + i70 tcp_conn. | OPEN | agent |  |  |  | i88, i70, docs/specs/tls13-handshake.md §Prior art |
| **i109** | **Collapse the now-unarmed encode_logical_imm soft-reject hook** — PR #371 (q20/i73-L9 sweep) deleted encode_logical_imm_try, the only caller that ever armed encode_logical_imm_soft; with no armer, encode_logical_imm_reject always jp fails. The soft branch + encode_logical_imm_soft/encode_logical_imm_rejected flags are vestigial. Collapsing back to a plain jp fail + dropping the two equates is behaviourally identical (flag never set) -> release.bin byte-identical. LOW priority (touches v2-production code). The hook may be re-armed by the future i48c editor encoder — decide when i48c's encoder is designed. | OPEN | agent |  |  |  | src/slots/logical_imm.asm, i73, i48c |
| **i113** | **Scrape, QA, and integrate the recovered Trinity documentation** — Pete recovered the full Trinity docs as 51 photographs (closes i61 gap: Quazar manual NOT found). Off-repo workspace ~/sam-archive/trinity-docs/ (third-party copyrighted; preservation-only). Three immutable layers: photos/ -> text/ (raw OCR via ocr_docs.py, Tesseract + rotation auto-detect) -> docs/notes/trinity-capabilities.md (verified facts only). Pipeline: OCR -> QA agent (prose errors + data-point inventory; wrong digits must be photo-verified) -> discovery agent (what's new vs current knowledge) -> photo-verify flagged data -> fold only photo-confirmed facts into trinity-capabilities.md. | OPEN | agent |  |  |  | ~/sam-archive/trinity-docs/README.md, docs/notes/trinity-capabilities.md, i61, i80, i87 |
| **i113b** | **photo-verify flagged data + fold verified facts into trinity-capabilities.md** — QA report flags specific data points (port addresses, EEPROM offsets, hex values, diagrams) that require photo confirmation before committing to the repo. Deliverable: run vision-verify on flagged items, then fold only confirmed facts into docs/notes/trinity-capabilities.md via the established off-repo pipeline. | OPEN | agent |  |  |  | ~/sam-archive/trinity-docs/VERIFY_CHECKLIST.md, docs/notes/trinity-capabilities.md |
| **i114** | **Netboot serve manifest + record-allocation strategy** — A manifest on the booted record maps TFTP names/paths -> B-DOS records, decoupling the served namespace from B-DOS 10-char flat filename limits. Entries carry {locator(s), span count, size, optional SHA-256}. Storage-allocation strategy in manifest header (first-free / highest-free / fixed-list; never silently steal a record). Multi-project use with shared-files-by-reference. Sub-choices settled: encoding=text, locator key=number+name, span naming=hash-derived, default=highest-free. Storage-class: trinity-sam-disks/ prefix = validated raw-record disk. Design at §6.5. | OPEN | agent |  |  |  | docs/specs/netboot-storage-manifest-design.md §6.5, i93, i95, i99, i70, i100, i101, i119, i122 |
| **i114a** | **Serve manifest (Go authority): text format + parser + manifest-backed name→record resolve** — design §1-3: a human-editable line-based text manifest, read once from the boot record, mapping full Pi-facing TFTP names/paths -> internal B-DOS record locator(s) + span count + total size + optional SHA-256, with local vs remote entry kinds. Go authority: define the format, write the parser, back the tftp.Store/Resolve path so exact-match resolve returns record-list+size into the existing bdos.SpanPlan path. Host-verifiable, no hardware gate; Z80 port is a follow-on. | OPEN | agent |  |  |  | docs/specs/netboot-storage-manifest-design.md, tools/netboot-oracle/tftp/server.go |
| **i114b** | **Netboot storage-allocation strategy: header policy (first-free / fixed-list / highest-free)** — design §4: storage-allocation policy in the manifest header (front section), user-chosen, default highest-free. s1 first-free (pack already-claimed records then lowest free), s2 fixed-list (overflow WARNS, never steals an unlisted record), s3 highest-free (keep low slots for the user's own disks). Invariants: overflow is an explicit warning, never a silent record-steal; the header records whether the manifest's own disk is in the pool; resume reuses already-claimed space. Go authority over a modelled store; host-verifiable. | OPEN | agent |  |  |  | docs/specs/netboot-storage-manifest-design.md §4 |
| **i116** | **Harness measurement / byte-match tests must assert the build succeeded — no stale-.bin reads** — A failed pyz80/make build leaves the previous .bin in place (or a timestamp-keyed target no-ops after a source restore), so a T-state or byte-identical check reads the stale artifact and reports a false pass. Bit the project 4 sessions: sha256-fastest-z80, i88-tls-go-authority-arc, i48c-expr-parser-bricks, and a timestamp-keyed variant. Fix is structural (the i21 USAGE.md doc note did not take): make harness measurement/oracle tests depend on the .bin build target (or assert build exit-0 / a freshness check) so they cannot run against a stale artifact. | OPEN | agent |  |  |  | tools/z80-test-harness-go/, i104, i21 |
| **i117** | **Extract sam-aarch64 learnings into a reusable open-source autonomous Claude project framework** — Starter framework/template so a new project inherits: autonomous-loop monitor (i97), split iN/qN registries + controlled-status discipline (i115 validator), §3 pre-merge review gate, per-session retro (i104), prime-directive verification ethos, brick + Go-authority-oracle loop, delegate-to-subagents context discipline, and doc-lifecycle + single-source-of-truth rules. Must be self-contained (portable out of Pete's ~/.claude/CLAUDE.md). Delivery options: template repo / init scaffold or Claude Code plugin. Scope + sequencing + packaging are Pete's call; do not auto-pick-up. | OPEN | pete |  |  |  | i97, i104, i115 |
| **i117a** | **Prior-art survey: existing Claude Code autonomous-orchestration plugins/frameworks vs i117** — Preliminary 2026-06-18 scan: metaswarm, ccswarm, ruflo, claude-plan-orchestrator, autonomous-dev, closedloop-ai, barkain, Composio AO + Claude Code Agent Teams; plus awesome-agent-orchestrators, awesome-cli-coding-agents. Most emphasise PARALLELISM. i117's model differs: single serial orchestrator + deterministic non-LLM monitor (i97) + prime-directive culture + blameless retro + iN/qN registry backbone. Deliverable: feature-by-feature comparison to confirm differentiation and whether gap justifies building i117. Research-first; cite each tool. Pete's call; do not auto-pick-up. | OPEN | agent |  |  |  | i117 |
| **i118** | **i82 TFTP client — OACK handling / standard-TFTP-server interop** — The i82 client requests RRQ options (blksize/tsize/timeout/windowsize) but has no OACK (opcode 6) handling. A standards-compliant server (e.g. macOS tftpd) answers an optioned RRQ with OACK before DATA; the client ignores it, never ACKs block 0, and hangs. Fix: on OACK -> learn server endpoint + adopt negotiated blksize (512 default if absent) + send ACK block 0; on direct DATA -> 512 default (today's hard-coded 1428 truncates); drop windowsize from request. Mirrored in Go authority + host tests. PR #422 landed the Go/Z80/host side; hardware test pending. | OPEN | agent |  |  |  | tools/netboot-oracle/tftp/tftp.go, tools/netboot-oracle/tftp/clientloop.go, tools/netboot-oracle/client/client.go, src/netboot/netboot_client.asm, src/netboot/tftp_client_front.asm, i82, i119, i120 |
| **i118b** | **hardware verification — OACK path on real Trinity hardware** — Confirm the OACK path works on real Trinity hardware: netboot client connects to a standards-compliant TFTP server (e.g. macOS tftpd), receives OACK, ACKs block 0, and receives the file. Gated on Trinity hardware access + a standards-compliant server in the test environment. | OPEN | agent |  | i133 |  | i119, i82 |
| **i119** | **i82 TFTP client — persistence write-out on real hardware** — Wire the i82 client's B-DOS persistence write-out; deferred by Pete (first test = receive only; persistence later). Gaps: (1) record-0-selection bug — client_main selects record 0 = floppy; (2) HRECORD-stamp question — whether a build-disk .mgt record is HRECORD-selectable; (3) large-file staging via the i99 fw_span port; (4) filename >10-char UIFA truncation. Free-record detection reads the on-card list directly per docs/specs/trinity-record-detection-design.md (Trinity SD is SHARED — show-name + confirm-before-overwrite mandatory). HSAVE write-out + raw-SD list read stay hardware-gated. | OPEN | agent |  |  |  | src/netboot/netboot_client.asm, src/netboot/bdos_seam.asm, docs/specs/trinity-record-detection-design.md, docs/specs/samdos-file-io.md, i82, i93, i99, i114, i134 |
| **i120** | **Windowed TFTP (RFC 7440) for the netboot client/server — throughput optimization** — i118 drops windowsize from the client RRQ to stay a correct lock-step (one ACK per block) client. That costs round-trips on large transfers (an 800 KB .mgt at blksize 1428 ~574 lock-step round-trips over the SAM's slow ~20-80 KB/s SPI stack). Windowed delivery (window 4 -> ~4x fewer round-trips) is the throughput win. Requires the receiver to ACK only the last block of each window (+ handle lost-block/out-of-window rewind) on both client (i82) and server (i83/i95); re-add windowsize to ClientOptionSet only once windowed logic exists on both sides + is host-verified. | OPEN | agent |  |  |  | tools/netboot-oracle/tftp/, src/netboot/netboot_client.asm, docs/notes/tftp-protocol-research.md, i118, i82, i83, i95 |
| **i121** | **SAM TFTP server — WRQ (write / accept-in) support: push disk images to the SAM, auto-slotted** — PRIMARY netboot use case: boot SAM, 'tftp put' a disk image from the Mac, SAM writes it to a free Trinity record, boot+test. Combines with the i83/i95 serve-out server into one program (RRQ+WRQ). q30 resolved (2026-06-20): auto-pick is a DISK CONFIG — at launch the program reads it; if absent it PROMPTS (auto-pick a free record? y/n); test/shipped disks ship that config allowing any free record, so unattended push works with a pre-configured disk + never overwrites a named record. Reuses i82 receive loop + i119 write-out; adds WRQ parse + OACK/ACK-0 + free-slot alloc (i114). | OPEN | agent |  |  |  | src/netboot/, tools/netboot-oracle/tftp/, i82, i83, i95, i114, i119, i70 |
| **i121c** | **WRQ auto-slot + write-out (write-to-free-only)** — Final block -> bdos_find_free_record (i119); none free -> reply TFTP ERROR(3,'no free record'); else select + bdos_fill_save_uifa (name from WRQ filename) + bdos_save_hook. q30 RESOLVED (write-to-free-only via disk-config auto-pick; prompt if config absent) so ungated. E2E mirrors TestClientE2EConfirm: assert HSAVE in the free record + right bytes; all-full -> ERROR(3) + zero HSAVEs. rst-8 HSAVE persist stays hardware-gated. | OPEN | agent |  | i121b |  |  |
| **i121d** | **Combined RRQ+WRQ bootable program + disk/Makefile/CI wiring** — Packaging vessel 1 (trinload): the self-contained WRQ-server code block + a host PUSH LAUNCHER (a trinpush wrapper) that reads a config / sets the record-placement strategy in the i121h config block (default highest-free), then pushes the block via trinpush-push.py for trinload to load+run. Combined RRQ+WRQ program; the .mgt vessel is the sibling i121j. Make target produces the pushable block; the launcher exposes --strategy (highest\|lowest\|explicit:N). Host-verifiable; the real wire push stays hardware-gated. | OPEN | agent |  |  | i194 |  |
| **i121i** | **.mgt bootable server-disk vessel + BASIC loader that appends the same config block** — The OTHER shipping vessel (sibling of i121d's trinload vessel): a bootable .mgt that a BASIC loader boots, loading the WRQ server code + appending the i121h config block at the same memory location, so the runtime image matches the trinload vessel exactly. The disk carries the config file the loader reads (absence -> the future i121k SAM prompt). Same program source as the trinload vessel; only the packaging differs. Sibling deliverable to i121d. | OPEN | agent |  |  |  |  |
| **i121j** | **SAM-side interactive strategy prompt when config is undefined (deferred)** — DEFERRED (Pete 2026-06-21: more complex to model, not needed now). Complementary to the host-launcher config: if the in-memory config block's strategy is undefined (the host launcher / disk config left it unset, e.g. user chose 'ask on the SAM'), the program prompts the user on the SAM keyboard for the placement strategy at launch. For now the host launcher always sets a concrete strategy (default highest-free), so this path is unused. Requires modelling a SAM keyboard prompt in emulation. | OPEN | agent |  |  |  |  |
| **i122** | **SAM fetch-and-boot (PXE-style): TFTP-client-fetch a disk image and boot it directly** — SAM acts like a netbooting Pi: on boot, TFTP-fetches a named disk image, writes to a scratch/boot record, and boots into it (no control return). Design notes: (a) use reusable scratch record (no cleanup post-boot); (b) sha256-dedup before fetch — if content hash already in storage, skip fetch and boot existing copy (idempotent). Reuses i114/q26 content-addressed record naming. Builds on i82 receive loop + i119 write-out + a boot-a-record primitive (Trinity RECORD-then-BOOT model). | OPEN | agent |  | i114c, i114d | i133 | src/netboot/netboot_client.asm, docs/notes/netboot-trinity-testing.md, i82, i114, i119 |
| **i123** | **Investigate migrating the build from make to tup** — tup tracks the full dependency graph and rebuilds exactly what is stale (and removes orphaned outputs), structurally preventing the dirty-build/stale-artifact class — same class i116 patches test-by-test; tup would make stale-.bin reads impossible by construction. Pete's ~/git/spectrum4 uses tup ('amazing'). Scope: evaluate tup against current Makefile (pyz80 .asm→.bin, Go builds, disk-image assembly, CI jobs), then if it wins, plan the migration. | OPEN | agent |  |  |  | Makefile, i116, i104 |
| **i124** | **Emulate full Trinity boot path so netboot boot wrappers run end-to-end in the harness** — Close the emulate-before-hardware gap. PR #423 landed: EEPROM modeled + ROM1-read-only-at-&C000; smoke_main + client_main run end-to-end. i82 hang localized: client_main logic sound (reaches client_first, emits ARP); fault = proactive ARP TX before PHY link-up + no ARP retransmit (→ i127). Remaining: model PHY link-up (LSTAT + drop-TX-while-down) to reproduce the drop in emulation; SD/RST-8 stub for full client fetch; fold server/serve/http wrappers + delete NETBOOT_HOSTTEST carve-outs (→ i126). | OPEN | agent |  |  |  | tools/netboot-oracle/z80/eeprom.go, tools/netboot-oracle/z80/harness.go, tools/netboot-oracle/z80/enc28j60.go, tools/netboot-oracle/z80/netboot_boot_test.go, src/netboot/eeprom.asm, src/netboot/bdos_seam.asm, i119, i125, i126, i127, i93 |
| **i126** | **One faithful SAM emulation layer used by every test — fix fragmented/inadequate emulation** — Root cause of i82 hardware-debug detour: three partial emulators — flat 64 KB netboot harness (no paging/ROM/EEPROM), full assembler harness (LMPR/HMPR but no ROM1-at-&C000-at-boot), SimCoupé (faithful SAM, no Trinity). Deliverable: consolidate to one layer that (a) models ROM1 read-only at &C000 at boot, (b) carries Trinity seams (ENC28J60 + EEPROM + SD/B-DOS), (c) used by every test, (d) runs netboot boot wrappers end-to-end so NETBOOT_HOSTTEST carve-outs are deleted. Reuses harness.go paging. Subsumes i124 netboot slice. | OPEN | agent |  |  |  | tools/z80-test-harness-go/harness.go, tools/netboot-oracle/z80/, src/netboot/, i124, i125, i116 |
| **i133** | **Self-service autonomous hardware testing via trinload** — With trinload on SAM (recs 3/128) + control returning between programs (i132), run unattended experiments: push a program via trinload3.py, it runs+RETs, push the next; feedback via tcpdump + a net result channel. PRIORITIZED to top (Pete 2026-06-20): the crux is whether we can BOOTSTRAP our own code from EEPROM / Trinity storage WITHOUT a human typing BOOT or picking a record — if a power-cycled SAM auto-starts a service that boots a pushed image, hardware testing goes fully autonomous (no Pete). The sooner we build it, the sooner we know. Auto-boot = i135; i172 audits dependents. | OPEN | agent |  | i135, i121a, i122 | i118b, i143, i152, i70, i88c, i93b | ~/git/trinload/test/trinload.py, src/netboot/, i132, i127 |
| **i135** | **Auto-boot a Trinity record on power-up — unsupervised end-to-end hardware automation** — Pete has a Tapo smart plug for programmatic power-cycling. If a record auto-boots on power-on (no manual BOOT), agent runs fully unsupervised: power-cycle → SAM boots chosen record → runs → results over net (i133) → repeat. Investigate: EEPROM/config default-boot-record setting, Trinity AutoBOOT ROM (Colin's patch loads B-DOS from 128K EEPROM at reset), or patch forked B-DOS for early boot-a-record hook. Research: ~/sam-archive/bdos/analysis, Trinity AutoBOOT ROM, EEPROM layout, ~/sam-archive/trinity-docs. Pairs with i133 for full unsupervised loop. | OPEN | agent |  |  | i133, i177 | ~/sam-archive/bdos/analysis, i87, i133 |
| **i135c** | **SAMBOOT: flash the patched EEPROM with backup/restore and confirm unattended auto-boot (answers the crux)** — Flash the prototyped patched EEPROM to Pete's Trinity via write_chunk, keeping the captured EEPROM as the restore backup; power-cycle and confirm unattended auto-boot of the configured record. Hardware (Pete). | OPEN | pete |  | i135d, i87a-b1, i87b, i197c |  | docs/specs/samboot.md |
| **i137** | **build-disk netboot ceiling check doesn't distinguish section class (i125 follow-up)** — tools/build-disk/main.go rejects netboot image only if it loads past &10000 (≤32768 bytes). A section-C bootable (smoke/serve/server/client) must fit &8000-&BFFF (≤16384); above that it loads into ROM1 at boot and crashes. Mostly masked: the per-program make fit-check (i125, 16384 for section-C) fires first, so an over-section-C binary never reaches build-disk normally. Gap only reachable by calling build-disk -netboot directly on an over-section-C image. Fix needs a -netboot-section-c flag threaded through disk targets. Low priority (masked by i125 gate). | OPEN | agent |  |  |  | tools/build-disk/main.go, tools/netboot-boot-fit-check.sh, i125, i119 |
| **i142** | **make registry should rebuild build/registry before mutations — stale-binary footgun** — The build/registry binary is a built artifact; running 'build/registry set-status/add' with a STALE binary (not rebuilt after a tool change) misbehaves silently — it caused the i138 DONE-but-in-priority inconsistency (a pre-#462 binary skipped the reconcilePriority step, stranding i138 in the queue). Fix: make 'registry-gen' a prerequisite of 'make registry' and the mutation paths (or have the CLI self-rebuild / a wrapper), so an agent cannot operate the registry with a stale binary. | OPEN | agent |  |  |  |  |
| **i143** | **bdos_picker: hardware MODE-2 blit for picker_render (follow-up from i119d)** — picker_render in src/netboot/bdos_picker.asm is a stub ret (all decision/show-name/confirm logic is host-verified in i119d). This item wires the actual SAM MODE-2 screen blit so the picker display is visible on real hardware. Depends on i119d being merged. | OPEN | agent |  | i119d, i133 |  |  |
| **i144** | **Harness HSAVE write-back model: persist captured bytes into CardModel sectors** — BDOSStore.handle captures the HSAVE UIFA (record+name+page+addr+size) but does NOT copy the Size bytes from the source page into CardModel.Sectors[record]. Add a write-back so a follow-up HRSAD can read the saved file back — enabling the i119e E2E to assert record CONTENTS (not just that the right record was selected + CONFIRMED). Not needed for the safety gate; useful for round-trip verification. | OPEN | agent |  |  |  |  |
| **i145** | **SD CSD capacity read -> BD_RECORDS (hardware-gated)** — client_main's auto-pick needs BD_RECORDS (card record count = tot.sct/1600). tot.sct comes from the SD CSD capacity read over SPI (ports &DC-&DF), which is hardware-gated (no emulator). Until this lands, BD_RECORDS=0 on real hardware => picker finds no free record => safe decline (no write). The emulation E2E injects BD_RECORDS directly. Implement the CSD read so the bootable client can actually claim a record on hardware. | OPEN | agent |  |  |  |  |
| **i145b** | **Production SD CSD read -> BD_RECORDS, wired into serve_main + client_main startup** — Fold the proven SD-init/CMD9/CSD-read/decode (from i145a) into a routine called at serve_main + client_main startup that sets BD_RECORDS from the card, REPLACING the injected value (the shortcut that hid the i145 gap). Prefer a NON-DISRUPTIVE bare CMD9 (the card is already B-DOS-initialised at boot) over a full re-init. Faithfully PORTS Colin's 16-bit BD_RECORDS behaviour by default (matches hardware; the 16-bit cap holds 65535 records x 800KB ~= 51GB, ample for the disk-image-storage use case); widening to 32-bit for >51GB cards is the optional enhancement tracked in the BD_RECORDS-width question. Emulation-verified against the i145c model (BD_RECORDS computed, not injected). Depends i145a (proven SD-read code) + i145c (model). | OPEN | agent |  | i145a, i145c |  |  |
| **i145g** | **CSD probe HARDWARE run: push the emulation-proven probe to the real SAM, capture the real CSD** — Hardware half of the CSD probe (split from i145a per emulation-first). Push the EMULATION-PROVEN probe (i145a) to the real SAM (trinload @ 192.168.2.75), run it, and tftp get csd.bin to capture the REAL CSD bytes + confirm SPI timing/token-retry behaviour. Runs ONLY AFTER i145a passes in emulation against the i145f-validated model — the safety gate Pete set 2026-06-22 (a trinload crash on hardware cannot be recovered remotely). The captured real CSD then re-parameterises the i145c model faithfully. owner:agent (hardware reachable, trinload left running) but irreversible-risk, so gated behind emulation. | OPEN | agent |  | i145a, i145f |  |  |
| **i147** | **Turn-end safety valve: make stop-after-merge structural, not discretionary** — The autonomous-loop context protection (touch task-done -> end turn -> monitor /clears for a fresh context) relies on the agent CHOOSING to stop after each merged PR — the easiest step to skip mid-flow. 2026-06-20: a session never stopped, accumulating ~10 merged PRs into one polluted context (judgment-picking + missing-edge blindness followed). Make end-turn-after-merge STRUCTURAL: e.g. the monitor detects 'new merge to main + no task-done while a turn is still running' and intervenes, and/or a gh-pr-merge wrapper auto-touches task-done. Design + own PR. | OPEN | agent |  |  |  |  |
| **i152** | **Verify the A=drive-register contract for HRSAD/HWSAD raw-sector hooks on real Trinity** — bdos_read_sector (HRSAD, i119) and bdos_write_sector (HWSAD, i114c) both leave A = the sector value at the rst 8, but the B-DOS hook code reads A as the drive number (bdos15a.src.txt rwsad/xsad: LD A,(hk.a)). After HRECORD-select a Trinity record presents as drive 2. The koron-go harness models the selected record via HRECORD and ignores A, so it cannot catch a wrong-drive bug. Confirm on real Trinity whether these hooks need A explicitly set (e.g. A=2 or A=record) or whether HRECORD makes the record the active context regardless of A; fix both routines symmetrically if needed. | OPEN | agent |  | i133 |  | src/netboot/bdos_seam.asm, reference/bdos/bdos15a.src.txt, i119, i114c |
| **i158** | **Padlock/gate marker in item-registry-open.md (mirror backlog.md gate column)** — Add the gate marker (ready / in-progress / 🔒 qN blocked-on-Pete / ⏳ iN blocked-on-work) to item-registry-open.md, mirroring the backlog.md gate column (computeGates), so the open-items view also shows at a glance what's actionable vs blocked-on-Pete. Pete request 2026-06-20. | OPEN | agent |  |  |  |  |
| **i159** | **Hyperlink item refs to in-file anchors in the registry .md tables** — In item-registry-open/closed.md + backlog.md, render id references (deps, dependents, parent, id-shaped refs) as in-file anchor links (e.g. [i122](#i122)) so a reader can jump to the referenced row within the same doc. Needs per-row anchors (GFM heading/HTML anchors in a table cell) + ref→anchor rewriting in gen.go. Pete request 2026-06-20. | OPEN | agent |  |  |  |  |
| **i160** | **Tree-view preview at the bottom of the registry .md docs** — Append a visual tree view (umbrellas → children → bricks) to item-registry-open.md / item-registry-closed.md / backlog.md, below the full table, so the hierarchy is visible at a glance. Derive from the parent edges in gen.go. Pete request 2026-06-20 (the corrupted '#3'). | OPEN | agent |  |  |  |  |
| **i161** | **Research CIIP (Z80 TCP/IP stack): locate source, assess license + portability** — From q25 (Pete: yes, investigate). Find CIIP — a pre-existing Z80 TCP/IP stack (TFTP/HTTP/DNS servers+clients; Simon Owen reportedly ported it to pyz80). Search ~/sam-archive + Pete's disks + online (GitHub etc.); do NOT scan Pete's photos (he checked — no source location, and it burns tokens). Assess license/portability and weigh 'adapt CIIP' vs continue i82/i83/i86. Report findings. | OPEN | agent |  |  |  |  |
| **i162** | **Version-control ~/bin agent tools in a private ~/git/claude-tools repo** — From q33 (Pete: yes, private). Create ~/git/claude-tools, move the ~/bin agent tools in (claude-chat-export incl. the i155 fix, claude-diary helpers, claude-chat-export SessionEnd hook target, etc.), scrub anything private/secret, push to github.com/petemoore/claude-tools as a PRIVATE repo (Pete can make public later). Gives the tools backup + history. | OPEN | agent |  |  |  |  |
| **i167** | **Determine the B-DOS save-buffer limit from first principles (read source; not hardware-only)** — q22 reframed (Pete 2026-06-20: 'only knowable on hardware' is a hand-wave — it's deterministic and we have the source). From first principles, read the B-DOS source (reference/bdos/) to find the max writable address / available save-buffer size deterministically, and replace the GUESSED FW_RECORD_CAP=4096 (src/netboot/http_main.asm, HTTP-streaming-sink path) with a source-derived value. Establish whether a hardware bench is truly required or (likely) the limit is computable from source. | OPEN | agent |  |  |  |  |
| **i168** | **Future: import a full source file into the editor (fail-with-line-number on bad line)** — Future feature (q24, Pete 2026-06-20). The SAM editor accepts ONE LINE AT A TIME (parsed/validated on entry) — no whole-file import today. If/when import is added: on a bad line, fail and report the line number + the error; the user fixes the source and retries (Pete's preferred behaviour). Gated on the editor groundwork (depends i3) — a normal backlog item, not part of current editor specs. | OPEN | agent |  | i3 |  |  |
| **i169** | **Adopt the 'needs Pete present' model (q32): ready --pete-present/--pete-away; decisions become questions** — q32 / needs-Pete model (Pete 2026-06-20). THREE kinds of work: (1) agent item = work the agent does alone (owner unmarked = agent); (2) question = a decision/input Pete gives (transient, deleted when answered); (3) needs-Pete-present = work gated on Pete's physical presence/action (press a key, hardware bench, Pi rig) = owner:pete.<br>Mechanism: 'build/registry ready' takes --pete-away (DEFAULT: excludes owner:pete) or --pete-present (includes AND prioritizes owner:pete, so when Pete is around the agent does presence-gated work first — don't waste his presence).<br>PRESENCE FILE = the single point of truth for whether Pete is around, surviving session handover / /clear / context-limit restart (the gap this closes). EITHER Pete OR an agent may create/delete it: Pete touches/rm's it; an agent creates it when Pete starts interacting and deletes it when Pete says he's stepping away (e.g. from his phone, where he can't touch files) — the agent TELLS Pete each time it creates/deletes on his behalf. The agent reads it at startup to default the --pete flag; monitor.sh can highlight presence from it.<br>Work: add the ready --pete-away/--pete-present flag defaulting from the presence file; convert the decision-shaped owner:pete items (i6, i50, i56, i66, i100c) to questions; backlog gate marker for owner:pete; document the model + presence file in CLAUDE.md + ROADMAP. Then i172 audits i133's dependents to reclassify presence-gated ones. | OPEN | agent |  |  |  |  |
| **i172** | **Audit i133's dependents: reclassify presence-gated ones as 'needs Pete' (or push i133 up)** — i133-dependents audit (Pete 2026-06-20): i133 (automate SAM testing via trinload) gates i118b, i135, i141, i143, i145, i152, i70, i88c, i93b — but many likely only need Pete PHYSICALLY PRESENT (a key-press / hardware action), not the full automation. Per the needs-Pete model (i169): go through each; for those that only need Pete present, mark owner:pete (needs-pete) + drop the i133 dep. Alternatively push i133 near the top so when it lands we verify it truly unblocks them. Depends on i169. | OPEN | agent |  | i169a |  |  |
| **i177** | **Publish trinboot: open-source EEPROM boot-config patcher (Python-over-trinload + bootable .mgt) for Trinity users** — Release the SAMBOOT EEPROM boot-config + stripes-restore patch as a standalone open-source tool for any Trinity owner: a Python program sent over trinload that patches the EEPROM, or a bootable .mgt. Gives Trinity users the configurable default-boot-record + opening-stripes feature for free. NOT a requirement for SAMBOOT -- depends on it (publication only). | OPEN | agent |  | i135, i176 |  | docs/specs/samboot.md |
| **i178** | **Registry: raise the item-title length limit from 120 to ~160 chars (validate.go + mutators help + test)** — The 120-char title cap forced abbreviating SAMBOOT item titles. Bump the limit in tools/registry/validate.go (the check + message + the §147 comment), the mutators.go add-flag help text, and update validate_test.go's title-length assertion. Keep titles single-line. Pete invited this (2026-06-20). | OPEN | agent |  |  |  |  |
| **i182** | **Bootable fetch-and-boot disk + real-Trinity run of client_fetch_boot** — i122c delivered the host-verified PXE-style fetch-and-boot orchestration entry (client_fetch_boot: EEPROM identity -> TFTP-fetch a .mgt straight into a scratch record via the raw_record_sink -> validate -> ALHK-boot). To exercise it on real hardware, build a bootable SAM disk whose autorun invokes client_fetch_boot (a netboot-fetch-boot-disk make target mirroring netboot-client-disk), decide the autorun entry vs client_main, and run it on real SAM+Trinity: serve a stamped 819200-byte .mgt, confirm it streams into the scratch record (1600 HWSAD sectors), validates, and ALHK-boots it. Hardware-gated (the whole point is the real SD write + real boot, unverifiable in emulation); entangled with the auto-boot/control-return autonomy (i132/i135). The full-size valid stream is also not driven through the bit-banged ENC in the host harness, so the real wire run is the first full-scale fetch-and-boot. | OPEN | agent |  | i122c |  |  |
| **i184** | **Harness prod-boot decode tests should serve a complete disk + StrictFileNotFound (emulation-first uniformity)** — Follow-up to i183. Several harness tests boot the PROD assembler with a minimal file set (align/org/fold guards, harness_test, compact_tbn) and rely on the legacy silent HLOAD no-op for the unconditional d15/sd13/zx013 boot loads. i183 made the faithful file-not-found longjmp OPT-IN (Config.StrictFileNotFound) to avoid breaking these. The emulation-first ideal (one layer, no carve-outs) is for every prod-boot test to serve a complete bootable disk and set StrictFileNotFound=true, so a future 'needed payload silently missing' bug fails loudly. Low priority, dev-tool test hygiene (not a CI gate). Refs: tools/z80-test-harness-go/harness.go Config.StrictFileNotFound, SCOPE.md i183 note. | OPEN | agent |  | i183 |  |  |
| **i189** | **SAM software catalogue: index every key binary, disassembly, investigation + how they relate** — Pete (2026-06-21): we now have many pieces of SAM software — some disassembled, some commented, some not — and no single overview, which has caused real confusion (e.g. SAMDOS vs B-DOS, where B-DOS replaces SAMDOS). Build a generic SAM software catalogue (a living reference doc) that for each piece records: what it is, where its binary lives, where its disassembly lives + whether commented, related investigations/reports, and how the pieces relate. Pieces include: the SAM system ROMs (untarnished, incl. startup stripes); Colin's forked ROM (fetches boot code from EEPROM -> how the forked-B-DOS bootblock is stored on EEPROM) — extracted 2026-06-21 (rom0.bin in ~/sam-archive/samboot-capture/, rom1 pending i87a-b2); the Trinity EEPROM contents + the Trinity disk's network-config program (defines the EEPROM layout) — eeprom.bin extracted 2026-06-21; trinload; SAMDOS; B-DOS; our own netboot programs + harness. Reference it from ARCHITECTURE.md. Record extraction artifacts so they are never lost. | OPEN | agent |  |  |  | docs/specs/samboot.md, docs/ARCHITECTURE.md |
| **i190** | **One shared koron-z80 SAM-emulation core used by both Go harnesses (no duplicated impl; post-SAMBOOT)** — PRINCIPLE (Pete, 2026-06-21, REFINED — read before implementing): the goal is NOT to merge the two harnesses into one tool. It is to STOP DUPLICATING the SAM-Coupé emulation IMPLEMENTATION. There must be ONE Go SAM-Coupé emulation core on the koron-go/z80 library — the CPU + memory/paging + IO model (the sampage package, created by i181, is the seed) — that BOTH harnesses USE rather than each carrying its own copy of the same features. The two harnesses MAY stay distinct codebases/packages where they are logically separate and benefit from independence: the assembler harness (tools/z80-test-harness-go) is assembler/disassembler-focused with DOS-hook + printer devices; the netboot harness (tools/netboot-oracle/z80) is netboot-focused with ENC28J60 + EEPROM + trinload. Do NOT force logically-separate tools together. Over-arching rule: don't duplicate implementations — if both share the same underlying emulation library, the problem is resolved.<br>CONCRETELY: the LMPR/HMPR paging + ROM write-protect currently exists TWICE — in the assembler harness (resolveRead/resolveWritePage) and, since i181, in sampage (used by the netboot harness). That duplication is what let the dumper's &C000 ROM-paging bug reach hardware (one harness had the model, the other didn't). i190 = make the assembler harness ALSO use the shared core (replace its inline pager with sampage; promote sampage to a location both modules can import) so the paging/memory model is implemented ONCE. Devices (ENC28J60/EEPROM/DOS-hooks/trinload) live where they are used — they are not duplicated, so no merge is needed. The pure-Go protocol oracle (frame/tftp/dhcp) is unaffected. ACCEPTANCE: the SAM CPU+memory+paging+ROM model exists in exactly one place that both harnesses import; both existing test suites pass; the harnesses may remain separate packages/modules. | OPEN | agent |  | i208 |  |  |
| **i192** | **SAMBOOT dumper: headless auto-RET after all regions served (single dump-and-return, no Esc)** — Split from i188 (2026-06-21). i188 fixed the rom1 crash + the clean Esc-driven trinload return; the dumper still serves forever until Esc. For the FULLY UNATTENDED push->run->return->re-push loop (i133-era), the dumper must auto-RET to trinload once the host has pulled all regions (rom0/rom1/eep0..7), with no human Esc. Implement: track per-region transfer completion (the final short-block ACK in serve_serve_once) and RET from dm_serve_loop once all 10 regions have been served once (Esc stays as the fallback). Emulation-verify via the i181 paged harness + the trinload push/return round-trip. NOT a blocker for i87a-b2 (rom1 recapture) — that uses the Esc return, already proven by the eeprom/rom0 captures. | OPEN | agent |  | i188 |  |  |
| **i194** | **Real-hardware disk push: stream all build/*.mgt to the SAM via the trinload serve vessel, into free Trinity records** — The i121 payoff on real hardware, autonomous: 'start trinload -> push the serve program (push-serve.py) -> tftp put each .mgt disk image (validated size==819200, streamed into a free record + claimed) -> tftp put tftp.done -> return to trinload', then read a stored record back (servable-raw) and byte-compare to confirm the store. Unblocked once the production CSD read lands (i145b, so the server finds free records) and the full experiment is emulation-proven (i145d). Agent-actionable: trinload runs on the SAM (192.168.2.75) over the LAN from the Pi; push via tools/trinload-push/push-serve.py + curl tftp://; the 19 build/*.mgt are valid + staged (/tmp/push-all-disks.sh batches them). NO Pete needed for the push+store+readback; booting a pushed record to watch it run is a downstream nicety (autonomous via i135 auto-boot, or a Pete-present spot-check) and NOT a blocker for this item. | OPEN | agent |  | i145a, i145b-b1, i145c, i145d, i145e, i121d, i195, i141, i198 |  |  |
| **i198** | **Real CMD24 list-sector WRITE + flip serve/client boot images to the real read+write list path** — Sibling/integration of i141 (which implemented the real CMD17 list READ, bd_list_read_hw, emulation-proven). For the real-hardware disk push (i194) to find AND claim free records on real Trinity, the boot images must use the real card-absolute list path end-to-end: (1) implement bd_list_write_hw — the raw SD CMD24 single-block WRITE of a list sector on ports &DC-&DF (the i145h model already covers CMD24, sdcard.go), mirroring bd_list_read_hw; (2) gate bdos_claim_record's list write behind the same NETBOOT_REAL_LISTREAD-style flag; (3) flip the serve/client boot images to define the real-list flag so bdos_read_list_sector/the claim write use the real SPI path instead of the BD_HOOK_LISTREAD/LISTWRITE harness scaffold; (4) update/extend the B3 boot-image tests (bdos_claim_name_test, bdos_picker_test) to seed the SD model (SeedSector) rather than the hook, so the claim read-modify-write round-trip is proven through real CMD17+CMD24. Read-only emulation precedent: i141. Hardware verification happens as part of i194's real push. owner:agent. | OPEN | agent |  | i141 | i194 |  |
| **i203** | **i48c-b8e-2: special-form encoders (shift/bitfield/bic-imm/csetm/barrier/ror/sysreg/mov-imm/ldr-lit/tbz)** — Second brick of i48c-b8e (see umbrella). Port the mnemonic-specific intercepts bypassing the form table (pass2.go:301-362 dispatch + handlers): encodeLSLSR (UBFM aliases, imm & reg), encodeBitfieldInst (bfi/bfxil/ubfx/bfc/sbfx -> BFM/UBFM/SBFM computed immr/imms), encodeBicImm (negate then logical-imm), encodeCsetm (csinv inverted cond), encodeBarrierInst (isb/dsb/dmb CRm bits 11:8), encodeRorImm (EXTR alias), encodeMrs/Msr/Dc/Tlbi (system-reg/SYS), tryEncodeMovImm (movz/movn/orr-imm autoselect, pass2.go:476-540), encodeLdrLitDirect (ldr Xt,label PC-rel19, pass2.go:546-569), encodeTbzTbnz (pass2.go:584-629). The hardest brick (~400-500 LOC). Faithful port (CLAUDE.md s6). Depends on i199 (reuses its self-test harness + form lookup). Extends the boot self-test with special-form fixtures, SimCoupe-gated. | OPEN | agent |  | i199 | i204, i48c-b8e |  |
| **i204** | **i48c-b8e-4: overlayClassify + literalWord (overlay mirror + PC-invariance probe)** — Fourth/final brick of i48c-b8e (see umbrella). Port the overlay/literal wiring that REUSES the b8e dispatcher. overlayClassify (overlay.go:82-163) mirrors encodeInst but returns {relocated-slot,expr,isLitpool,litWidth,rt} instead of a base word - classifyMem (overlay.go:177-220), classifyMovImm (overlay.go:230-254); special/compound symbolic forms with no overlay support return error. literalWord (compact.go:354-370) probes encodeInst at pcProbeA=0 and pcProbeB=0x40000: either errors -> keep symbolic; wA!=wB -> PC-dependent (keep symbolic); wA==wB -> bare 4-byte literal. ~100-150 LOC, decision trees over brick1/2/3 dispatcher. Depends on bricks 1-3. Faithful port (CLAUDE.md s6); Go authority overlay.go overlayClassify + compact.go literalWord. Completes b8e: text->.tbn can encode symbolic INST records on the SAM. SCOPE NOTE (from PR #561 review): the form-table path also has a bare Imm16 SlotKind (0x08, used only by udf/mnemonic 89) that brick-1 (i199) does NOT encode — encode_inst cleanly fail-tags it (ENC_TAG_BADSLOT) rather than mis-encoding. encode_imm_n caps at 7-bit width, so Imm16 needs its own 16-bit placement encoder. Ensure full SlotKind coverage (incl. plain Imm16) before closing b8e. | OPEN | agent |  | i199, i203, i201 | i48c-b8e |  |
| **i207** | **Harness fidelity: load boot payloads from the build-disk image, not via separate hardcoded page-loads** — The Go test harness (tools/z80-test-harness-go) pre-loads off-axis boot payloads (test_cluster, test_mem, paged_call, enc_fix, ...) directly into physical pages in its test setup, bypassing the real disk->HLOAD path SimCoupe exercises. Consequence: a payload missing from the SimCoupe fixture disk still passes the harness (it pre-loads the page), so harness-green != SimCoupe-green. This gap let i69 (PR #577) pass the harness while all four test-variant SimCoupe jobs hung (rc=124) because tools/run-roundtrip.sh did not put the enc_fix payload on the fixture disk. Per CLAUDE.md s7 (one emulation layer capturing every integration point): make the harness derive its loaded payloads from the SAME build-disk image SimCoupe boots (parse the .mgt directory / mirror build-disk's file list), OR at minimum assert the build-disk manifest contains every payload the boot loader HLOADs, so a missing-from-disk payload fails the harness too. Owner agent. | OPEN | agent |  |  |  |  |
| **i208** | **Review both Go SAM-emulation libraries: shared vs duplicated logic, merge recommendation** — Pete (2026-06-22, answering q41): the driving principle is do NOT reimplement the same SAM-Coupe emulation logic in two places — put shared logic in shared packages. Suspicion: we built two Go emulation libraries (tools/z80-test-harness-go and the netboot-oracle/z80 emulator, plus koron-z80) that overlap, where some accuracy features exist on one side only. ACTION: an agent reviews BOTH emulation cores (paging/pager, ROM-vs-RAM model, Trinity/SD/ENC seams, instruction coverage, what each tests that the other can't), and reports: which logic is duplicated, which features are one-sided, and whether ONE merged core would benefit both (more accuracy across more concerns) or whether they provide fundamentally different services where merging would just slow things. Output = a feedback/recommendation doc for Pete to decide the merge. This is the gating first step for i190 (the actual merge). See feedback_mirror_emulator_fixes (the i190-pending interim rule). | OPEN | agent |  |  | i190 |  |
| **i209** | **Assess on-SAM macro/preprocessor support (i31): scope, integration point, perf + size impact estimate** — Pete (2026-06-22, q38) actively uses macros developing on the Mac; losing them when development moves on-SAM would be a real problem, so on-SAM macros 'would be a huge win' — PROVIDED it doesn't blow up assemble-time performance or program size or force a redesign. ACTION (this item): an agent produces a REPORT, not an implementation: what .if build-constraint syntax + macro definition/expansion semantics are needed (survey what Pete's Mac-side sources actually use); where it integrates vs the existing host asmparse/text2bin preprocessing; the Go-authority-first plan; and crucially an ESTIMATE of the assemble-time performance cost + program-size cost + whether it 'slips in' or needs a redesign. q38 depends on this report; once it lands, Pete returns to q38 to give the go/no-go + scope. Does NOT implement i31 — only scopes it. | OPEN | agent |  |  |  |  |
| **i210** | **Assess linker-script parsing / SpectrumFourLayout extraction (i56/i20): touch points + cost estimate** — Pete (2026-06-22, q42): parsing the linker script 'would be awesome' — same caveat as macros: if it means rewriting a lot of code or a significant performance cost, flag it; otherwise good to do. ACTION (this item): an agent produces a REPORT, not an implementation — survey ALL the touch points where the hardcoded spectrum4 layout (the off-axis page assignments, section addresses, the build-disk/loader assumptions) would need to read from a parsed linker script / a -layout flag instead; identify what SpectrumFourLayout would abstract; and ESTIMATE the implementation cost + any assemble-time perf impact. q42 depends on this report; once it lands, Pete returns to q42 for the go/no-go. Does NOT implement i56 — only scopes it. (i20 also depends on i56.) | OPEN | agent |  |  |  |  |
| **i213** | **Trinity EEPROM capability signature + version (interfacing code auto-detects our patch, never assumes it)** — Trinity EEPROM/firmware version + identity stamp: when we customise the Trinity EEPROM (i135c — stripes restore, boot-a-record-on-startup), write a small magic signature + version byte at a stable offset, plus a read helper. JUSTIFICATION (Pete, 2026-06-23, refined): NOT for SD-capability negotiation (record access through the EEPROM B-DOS is transparent, so no capability check is needed). The real need: the B-DOS actually in control is NOT guaranteed to be the one we flashed — B-DOS can be loaded from FLOPPY (the traditional DOS-load path), which is the normal case for anyone WITHOUT our customised ROM chip (not universal). So our software can't assume the EEPROM's patched firmware is present; the stamp lets it DETECT which firmware/patch is actually running and handle a mismatch gracefully. SCOPE: just the stamp + read helper (not the heavyweight per-capability auto-detect tied to the WONTFIX i214). The write rides i135c; the stamp-format + read helper are owner:agent. Relatedly this is WHY i214 (fix BD_RECORDS in the EEPROM) was dropped — a floppy-loaded B-DOS bypasses any EEPROM fix, so our direct-SD record code (i141/i145, frozen-interface, version-independent) is the right layer, and it is already faithful-16-bit which is ample. | OPEN | agent |  |  |  |  |
| **i215** | **IDE page-persistence (spill): lazy-spill cold DOC blocks to Trinity or floppy/tape when the pool is full** — Downstream of i2 (Pete, 2026-06-22, q36): the i2 baseline refuses-with-message when the page pool is exhausted. This OPTIONAL feature makes alloc_page lazy-SPILL a cold DOC block to a persistence backend instead of refusing (design §4.5/§5/§7): serialize the block to disk, free its page, satisfy the alloc; reload on demand. Backend is PLUGGABLE — Trinity storage if the machine has it, else a physical floppy/tape — so a space-limited user on ANY machine gains document headroom beyond the ~144KB/256KB ceiling. HARD CONSTRAINT: the assembler must run fully on a floppy/tape-only machine with NO Trinity, so the spill backend is optional and must degrade to refuse-with-message when absent. Depends on i2 (the pool allocator + the page_owner[] discipline) but NEVER blocks it. | OPEN | agent |  | i2a |  |  |
| **i216** | **Audit + normalize SAMDOS vs B-DOS terminology across living docs and code comments** — Follow-on to i75/q10 (DONE: B-DOS AL 1.5a is the shipped default DOS; editor/assembler disks ship B-DOS). i75 reoriented the main living docs but was point-in-time; ~66 files still mention SAMDOS (many legitimately). This standing audit sweeps living docs + src comments and normalizes: (a) SAMDOS used as a generic stand-in for the loaded DOS (the hook/DOSFLG/DOSER mechanisms are DOS-agnostic) -> DOS-neutral ('the loaded DOS'); (b) SAMDOS meaning the SHIPPED DOS -> B-DOS; (c) PRESERVE legitimate SAMDOS references — the canonical-DOS source citations, genuinely SAMDOS-internal mechanisms (e.g. the (hksp) NMI snapshot, i185), and the verbatim upstream/ROM material. EXCLUDE reference/ + sam-archive/ (verbatim upstream) and docs/sam/ (canonical ROM disassembly + tech manual — SAMDOS is correct there). Single-source: the DOS-target decision lives canonically in i75/ARCHITECTURE.md; docs point to it, never restate. Known straggler captured 2026-06-22: docs/specs/ide-memory-model-design.md:47 ('SAMDOS occupies one page'). Raised by Pete (2026-06-22) while reviewing the q36 doc. | OPEN | agent |  |  |  |  |
