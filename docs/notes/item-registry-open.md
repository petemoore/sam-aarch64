<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/items.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Item registry — open

Work items with status `OPEN` or `IN_PROGRESS`. Items with status `DONE` or
`WONTFIX` live in `item-registry-closed.md`.

## Id conventions

`i<N>` · `i<N><letter>` · `i<N><letter>-b<M>` · `i<N><letter>-b<M><letter>`
(base → sub-item → brick → brick-part). Ids are immutable once used in a PR,
branch, or commit; never reused. Sort order is true-numeric (i5 < i41 < i237).

## Controlled status vocabulary (open file)

| token | meaning |
|---|---|
| `OPEN` | not yet started; or started but no agent has it in-hand |
| `IN_PROGRESS` | actively being worked right now |

"Blocked" is not a status token — it is a *derived* property of the dependency
graph. An item with an unsatisfied `depends_on` edge is still `OPEN`; its gating
item/question ids are listed in the `deps` column (the edges of the dependency DAG).

## Atomic-item rule

One row = one deliverable. If a row bundles independently completable parts, split
it into letter sub-ids (`i81a`/`i81b`…) so no finished part hides inside an
unfinished row.

## Dependency model

`depends_on` edges declare what must finish before this item can proceed, and are
shown in the `deps` column. A target may be an item (must reach `DONE`) or a
question (must be answered and deleted). Edges form a DAG; the validator rejects
cycles and dangling targets. The DAG is built from the `depends_on` fields in
`registry/items.yaml`; `grep depends_on` there for the machine-readable edge list.
The `dependents` column shows the reverse edges (items that depend on this one).

## Column layout

The generated table has 7 columns: `id | item | status | PR | deps | dependents |
refs/links`. `PR`, `deps`, `dependents`, and `refs/links` are each independent
first-class columns. The generated `.md` files are views of `registry/items.yaml`
— do not edit them by hand (hand edits fail CI).

<!-- The table below is generated — do not edit it by hand. -->


| **id** | item | status | owner | gate | PR | deps | dependents | refs/links |
|---|---|---|---|---|---|---|---|---|
| <a id="i3"></a>**i3** | **Editor groundwork (Phase 2) — full vision** — Phase-2 editor full vision umbrella. The editor groundwork encompasses all Phase-2 editor sub-features (see i43–i47 and sub-items). Full scope in ROADMAP. | OPEN | agent | ready |  |  | [i101](#i101), [i168](#i168), [i77](#i77), [i90](#i90) | docs/ROADMAP.md |
| <a id="i24"></a>**i24** | **16-bit OUT_LEN / 32 KB-output limit (OUT_LEN: defw 0 src/main_loop.asm; 32768-byte OUT ceiling src/encoder.asm)** — OUT-ceiling lift to support >32KB assemblies (q45 = option A, Pete 2026-07-01). The 16-bit OUT_LEN + 32768-byte ceiling (src/encoder.asm) cap assembled output at 32KB. Lift via option A (UNIFORM): the whole OUT buffer becomes ONE contiguous pool run (i2 page-pool), sized from the pass-1 total, and EVERY emit_byte brackets LMPR (page in the OUT page, write, restore) like today's high zone -- so HSAVE can save the >32KB output as one contiguous file (B-DOS HSAVE/HLOAD are contiguous-only). Loses today's free-low-zone fast path for the first 16KB (a one-shot ~150ms cost on a full assemble -- negligible per Pete). Simplest single code path; needs no section-C budget (unlike option B two-mode). The emit-hot-path perf optimisation is deferred to a separate lowest-priority item (measure by T-states to assemble release.s, once feature-complete). | OPEN | agent | ready |  | i2a, i2b, i218 | [i33](#i33) |  |
| <a id="i27"></a>**i27** | **Cortex-A53 errata workarounds (--fix-cortex-a53-*)** — Cortex-A53 errata workarounds (--fix-cortex-a53-835769 / -843419) as an EXPLICIT enable/disable feature, off by default for later silicon (Pi 4/400), on for Cortex-A53 / Pi 3 (Pete, q37). These are hard-to-debug latent bugs. Umbrella over the Go authority (i27a) + Z80 ports (i27b 835769, i27c 843419 + toggle). CORRECTION (i27a research vs Pete's q37 note): 835769 has NO page-boundary condition — it is a DATA-DEPENDENCY erratum (a memory op immediately followed by a 64-bit multiply-accumulate); the page-offset (0xff8/0xffc) condition belongs to 843419. Also note: in binutils 2.44 BOTH fixes are link-time (gas does not implement 835769; GCC's flag is compiler-side) — but for our single assemble+link tool 835769 is best done as NOP insertion in the instruction stream and 843419 as an in-place ADRP->ADR rewrite after final layout. CAVEAT (Pete): if it balloons in code size or assemble-time perf, flag and reconsider — assessment so far: tractable, NOT massive (linear scans + a re-layout only when 835769 hazards exist; off by default). Greenfield: Go-first authority then faithful Z80 port + binutils cross-check. | OPEN | agent | ready |  |  |  |  |
| <a id="i27b"></a>**i27b** | **i27b: Z80 port — Cortex-A53 835769 NOP insertion** — Faithful Z80 port (CLAUDE.md s6) of the i27a 835769 fix into the SAM-side assembler (src/). Detect a memory op immediately followed by a 64-bit multiply-accumulate (Ra!=XZR) and insert a NOP between them, off by default behind the SAM toggle. Mirror the Go authority's mask predicates + erratum_sequence RAW carve-out. NOP insertion shifts addresses, so it must be done in a way that PC-relative refs stay correct — detect in the record/IR stream (pass1) so the inserted NOP word is accounted for in PC assignment across both passes (the Go authority establishes the exact decision). SimCoupe-gated; add a boot self-test fixture. Go authority: errata.go (i27a). | OPEN | agent | ⏳ i234 |  | i27a, [i234](#i234) |  |  |
| <a id="i27c"></a>**i27c** | **i27c: Z80 port — Cortex-A53 843419 ADRP->ADR rewrite + SAM enable/disable toggle** — Faithful Z80 port (CLAUDE.md s6) of the i27a 843419 fix + the SAM-side enable/disable toggle UX (q37: explicit feature, off by default). Post-layout scan over the OUT buffer: for each ADRP at a final page-offset 0xff8/0xffc matching the sequence predicate (mem-op, optional 1 non-branch insn, then unsigned-imm ld/st whose base == ADRP Rd), rewrite ADRP->ADR in place (no address shift — the +/-1MB ADR range always fits our image). Mirror the Go authority masks. Wire the SAM toggle (directive or build option) covering BOTH 835769 (i27b) and 843419. SimCoupe-gated; boot self-test fixture. Go authority: errata.go (i27a). | OPEN | agent | ⏳ i234 |  | i27a, [i234](#i234) |  |  |
| <a id="i31"></a>**i31** | **On-SAM preprocessor (.if build-constraints + macros)** — Beyond-M7 / future idea. On-SAM preprocessor supporting .if build-constraints and macro expansion, reducing reliance on the host-side preprocess step. | OPEN | agent | ⏳ i48c-b8 |  | [i48c-b8](#i48c-b8) |  |  |
| <a id="i32"></a>**i32** | **Multiple source files + staged/partial loading** — Beyond-M7 / future idea. Support for assembling from multiple source files with staged or partial loading, enabling larger projects than currently fit in a single-file workflow. | OPEN | agent | 🔒 q46 |  | i2a, q46 |  |  |
| <a id="i33"></a>**i33** | **Trinity mass-storage: leverage B-DOS record seam for staged/overlaid assembly (i23/i24 ceiling lift)** — Use the proven Trinity B-DOS storage seam (src/netboot/bdos_seam.asm; spanning records in src/netboot/fw_span.asm) for staged/overlaid assembly, lifting the i23 96 KB IN / i24 32 KB OUT ceilings. Storage-access half proven by netboot work. Assembler still uses plain HSAVE/HLOAD for single-file IN→OUT; this item extends that to mass-storage-backed staging. Rescoped 2026-06-17: netboot proved records are accessible + writable; remaining deliverable is the assembler-scale integration. | OPEN | agent | ⏳ i24 |  | i2a, [i24](#i24) |  | memory/trinity_hardware.md, src/netboot/bdos_seam.asm, i23 |
| <a id="i39c"></a>**i39c** | **Phase 3 — bitfield-packing polish on the overlay slot bytes** — Optional second-order optimisation: pack the INSN_RUN overlay operand fields (reg=5b, kind-tag, shift-kind=2b, cond=4b, width=1b, mem-shape=3b) into sub-byte bitfields instead of one byte per field, per docs/specs/compact-tbn-nextgen-design.md §3.1 (which rates it "REFINE / accept narrowly" — real ~-4 KB but the least leverage per unit of Z80 complexity, a polish layered on the structural wins, not a headline). A SEPARATE concern from the comment/blank-run parsing brick (i48c-b7); the earlier "absorbed into i48c-b7" framing conflated two unrelated subsystems and pointed at a stale design ref (i48-syntactic-encoder-design.md has no §3.1). De-coupled 2026-06-21: gated on i48c-b8 (do the format-packing polish once the overlay encoder is functionally complete, so it isn't re-churned). Migrated to M9 at M8 close (2026-06-12). | OPEN | agent | ⏳ i48c-b8c, i48c-b8d, i48c-b8e |  | i48c-b8a, i48c-b8b, [i48c-b8c](#i48c-b8c), [i48c-b8d](#i48c-b8d), [i48c-b8e](#i48c-b8e) |  | docs/specs/compact-tbn-nextgen-design.md §3.1, docs/notes/m9-status.md, [i48c](#i48c) |
| <a id="i41"></a>**i41** | **Editor edit-buffer data structure — insertion performance for large source** — Architectural decision: the editor holds source in a separate insertion-friendly in-memory document model (paged block-list: block = page, intra-block gap buffer, record-id side-tables; worst-case edit ~30–80 ms at 400 KB) and serializes to .tbn on save/assemble. Unifies the editor document model with i48's IR (edit-model records = in-memory symbolic IR). Design agreed (opus, 2026-06-08), 5 key decisions in docs/specs/editor-edit-model-design.md §7. Decomposed into i41a–i41e. | OPEN | agent | ready |  |  | [i43](#i43), [i44](#i44), [i45](#i45), [i46](#i46), [i47](#i47), [i65](#i65) | docs/specs/editor-edit-model-design.md §7, docs/notes/m9-status.md, i41a, i41b, i41c, i41d, [i41e](#i41e) |
| <a id="i41e"></a>**i41e** | **Editor edit-model — i48-IR record payload + document symbol table + hybrid record model** — Replace i41a's raw-text line payload with the i48 in-memory symbolic IR (parsed format.Record): a document-global symbol table + name-id/record-id side-tables (§3), a hybrid committed-parsed / active-raw / invalid-error record type (§7.3), per-line text→IR entry. q24 resolved (2026-06-20): the editor validates ONE LINE AT A TIME — no whole-document import to serialize invalidly; the design point is how an in-progress/invalid LINE is held + round-tripped (whole-file import = i165). Open design (symbol-table lifecycle, hybrid sum type, per-line parse) — needs a design pass (§8/§5.2/§6/§7.3). | OPEN | agent | 🔒 q51 |  | i2a, q51 |  | docs/specs/editor-edit-model-design.md §8/§5.2/§6/§7.3, docs/specs/i48-syntactic-encoder-design.md, i41a, i41c, [i48c](#i48c) |
| <a id="i43"></a>**i43** | **Editor: register simulator with user-chosen seeds + replay-on-edit** — Phase-2 editor sub-feature. Register simulator with user-chosen seeds: step instructions, inject/randomise source regs, show dest regs/flags/memory, and replay-on-edit. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | docs/ROADMAP.md |
| <a id="i44"></a>**i44** | **Editor: inline instruction-explanation panel (prose semantics, flags, regs, bit-field layout)** — Phase-2 editor sub-feature. Inline instruction-explanation panel showing prose semantics, flags set, regs read/written, and bit-field layout. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | docs/ROADMAP.md |
| <a id="i45"></a>**i45** | **Editor: 'did you mean a simpler instruction?' result-equivalent rewrite hint** — Phase-2 editor sub-feature. Suggests result-equivalent but simpler instruction rewrites when a more concise encoding exists. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | docs/ROADMAP.md |
| <a id="i46"></a>**i46** | **Editor: system-register documentation panel (ARM MRA subset + editorial overlay)** — Phase-2 editor sub-feature. System-register documentation panel surfaced from the cursor line, embedding an ARM MRA subset with editorial overlay. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | docs/ROADMAP.md |
| <a id="i47"></a>**i47** | **Editor: inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations)** — Phase-2 editor sub-features. 'Why is this instruction here?' inverse-flow navigation plus retro UI affordances including chiptune audio, period fonts, and animations. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | docs/ROADMAP.md |
| <a id="i48"></a>**i48** | **Single serialized format + pass-free syntactic encoder (the design)** — Design for single serialized format + pass-free syntactic encoder. Decision A: the compact overlay is the only serialized .tbn; the symbolic kinds (KindInst/LabelDef/LocalDef/LitInsts) become an in-memory IR, never written. Decision B: text->overlay is a pure syntactic transform; value-dependent bits computed in the fold at assemble time; forego GNU silent rewrites; narrow assemble-time mov->movz/orr/movn fallback. Host side COMPLETE (i48a #144, i48b, i48d). i48c (Z80 text->overlay encoder) is the M9 carry-forward. | OPEN | agent | ready |  |  |  | docs/specs/i48-syntactic-encoder-design.md, https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md, [i48c](#i48c) |
| <a id="i48c"></a>**i48c** | **Z80 text→overlay encoder (editor input path)** — SAM-side mirror of the host front-end (tools/sam-aarch64/frontend: lexer→parser→records). Go i48b is the authority. Absorbs i39c (overlay slot bitfield-packing polish). Large multi-PR port decomposed into bricks B1–B8 (scoped 2026-06-17). B1–B7 are flat-memory-autonomous (koron-go/z80 harness, netboot-z80 CI). B8 is assembler-coupled, lands last. Host Preprocess (.include/.macro/.if) is out of scope (host-only bulk-import concern). | OPEN | agent | ready |  |  | [i109](#i109) | src/asmlex.asm, src/asmparse.asm, tools/netboot-oracle/z80/asmlex_test.go, tools/netboot-oracle/z80/asmparse_test.go, tools/sam-aarch64/frontend/lexer.go, tools/sam-aarch64/frontend/parser.go, docs/specs/i48-syntactic-encoder-design.md §2, docs/specs/i48-syntactic-encoder-design.md §7, docs/notes/m9-status.md, [i39c](#i39c), [i109](#i109) |
| <a id="i48c-b8"></a>**i48c-b8** | **Integration/fixture round-trip (assembler-coupled, lands last)** — Wire the completed text->overlay encoder (B1-B7) into the full assembler path so the SAM produces host-identical compact v2 .tbn from typed text, proving text->overlay byte-match over the fixture corpus. DISCOVERED SCOPE (2026-06-22): the SAM assembler reads label/local DEFINITIONS from the .tbn HEADER TABLES (reader.asm:151), not from the encoder's inline LABEL_DEF/LOCAL_DEF IR records, so a Z80 port of the host compact-serializer pipeline sits between encoder and assembler: assemble.Pass1 (PC assignment + label/local position capture + symbol resolution over the IR) -> assemble.Compact (IR -> INSN_RUN/LIT_DATA overlay records + COMMENT/BLANK_RUN->sidecar + .global->globals) -> headerRows + format.WriteFile (header tables w/ uvarint deltas + front-coded name table + editor region). Go authority: tools/sam-aarch64/assemble/{pass1,compact}.go + tools/sam-aarch64-format/writer.go. Umbrella; decomposed into b1-b4. | OPEN | agent | ready |  | i48c-b5b, i48c-b5c, i48c-b5d, i48c-b5e, i48c-b5f, i48c-b6, i48c-b5 | [i31](#i31) | src/assembler.asm, src/asmparse.asm |
| <a id="i48c-b8c"></a>**i48c-b8c** | **Z80 serialize: headerRows + WriteFile -> compact v2 .tbn bytes** — Port headerRows (compact.go:196) + format.WriteFile (tools/sam-aarch64-format/writer.go) to emit the full compact v2 .tbn: 12-byte header (magic SA64 + version 2 + flags + editor_region_offset u32), header label table ([count u16][name_id u16][delta uvarint] rows) and local table ([count u16][digit u8][delta uvarint] rows) built from b8a positions, the front-coded name table, the b8b compacted records, then the editor region (sidecar comment/blank rows + globals). Deltas are uvarint-encoded running offsets. Harness-test the serialized bytes vs host CompactTBNBytes/WriteFile over fixtures. | OPEN | agent | ⏳ i48c-b8e |  | [i48c-b8e](#i48c-b8e) | [i39c](#i39c), [i48c-b8d](#i48c-b8d) |  |
| <a id="i48c-b8d"></a>**i48c-b8d** | **Corpus round-trip integration: text -> compact .tbn on SAM byte-matches host (+optionally assemble to OUT)** — The lands-last wiring: drive the full SAM pipeline text -> lex(asmlex) -> parse(asmparse) -> Pass1-over-IR(b8a) -> Compact(b8b) -> serialize(b8c) over the fixture corpus and assert the produced compact v2 .tbn bytes byte-match the host CompactTBNBytes output (the m9 i48c done-criterion: text->overlay byte-match host for the corpus). Optionally feed the produced .tbn through the existing assembler reader+pass1/pass2 to OUT bytes and compare to the host-assembled output, closing text->bytes end-to-end. Wire a corpus-driven harness test (and, if it fits the boot budget, a SimCoupe self-test). ARCHITECTURE CONSTRAINT (found in b8a, PR #557): the parser tables (asmparse SYM_NAMES/PARSE_RECS) and the assembler pass-1 tables (assembler.asm OPVAL_ARRAY/PASS_PC@&C159/LITPOOL@&D200/LOCAL_LABEL_TABLE@&E280) BOTH claim the &C100-&EFFF scratch region, so the full text->.tbn chain cannot run in one flat binary with today's layout. b8a side-stepped this by producing the IR host-side; b8d (and the real SAM editor) must resolve the memory layout (relocate/page one stage's tables) so parse_run + pass1 + compact + serialize coexist. This is the integrated-build memory-model question (relates to q36). | OPEN | agent | ⏳ i48c-b8c |  | [i48c-b8c](#i48c-b8c) | [i39c](#i39c) |  |
| <a id="i48c-b8e"></a>**i48c-b8e** | **Compact encoder adapter: literal-probe + overlay-patch emission (paged, needs ENCTAB)** — Port the encoder-dependent half of assemble.Compact. SCOPE FINDING (2026-06-22 investigation): this is the LARGEST B8 brick and the architecturally most significant — the SAM gains its FIRST standalone instruction-encode capability. The Z80 has NEVER encoded a symbolic INST record from scratch; today it only FOLDS pre-computed base words (host produces base words via Go encodeInst; the Z80 insn_run.asm pass2 folds {slot,expr} patches). For text->.tbn on the SAM, the SAM must encode. So b8e ports ~800 LOC of NEW Z80: (1) encodeInst dispatch (pass2.go:214-373 + operandsToValues: operand-kinds -> form -> slot values -> base word, incl. the special-form intercepts lsl/lsr/bitfield/bic-imm/csetm/barriers/ror/mrs-msr/dc-tlbi + compound mem/shifted/extended forms); (2) overlayClassify (overlay.go:82-163: the encodeInst mirror that returns {relocated-slot, expr} instead of emitting); (3) literalWord (compact.go:354-370: encode at pcProbeA=0 and pcProbeB=0x40000; equal => bare 4-byte literal element). REUSES (already proven in pass2): the 13 slot folders (insn_run.asm fold_*), the per-slot encoders (src/slots/*.asm), form_lookup (form_lookup.asm), eval_expr_const, the symbol/local tables. HARNESS: flat is IMPOSSIBLE (needs the paged ENCTAB + form_lookup + slot encoders = the whole assembler). Use a BUILD_TESTS boot self-test inside the full paged assembler build (precedent: assembler.asm:357-468 self-tests; test_mem_offaxis.asm off-axis pattern), where ENCTAB is already mapped by main_assemble's enctab_map_in bracket; the Go harness synthesises a fixture, the self-test runs the encoder, the Go side compares vs host encodeInst/overlayClassify. SimCoupe-gated (it's in the assembler build). Likely deserves a short spec + possible further decomposition (encodeInst dispatch / overlayClassify / literalWord+wiring) at session start. Go authority: pass2.go encodeInst+operandsToValues, overlay.go overlayClassify, compact.go literalWord. | OPEN | agent | ⏳ i204 |  | i48c-b8b, i199, i203, i201, [i204](#i204) | [i39c](#i39c), [i48c-b8c](#i48c-b8c) |  |
| <a id="i50"></a>**i50** | **Host Go-tool CLI shape — deferred design question (one binary vs three vs pandoc-style)** — DECIDED (Pete, 2026-06-25): adopt the pandoc-style --from/--to CLI for the host Go tools (e.g. 'sam-aarch64 --from=asm --to=tbn foo.s'; --from=tbn --to=asm; future --from=gnu --to=tbn). Supersedes the earlier deferral (one front door vs three vs pandoc). Implementation is a thin presentation front-end: the three concerns are ALREADY cleanly separated as libraries (aarch64enc / aarch64dec / sam-aarch64-format+frontend), so this is CLI wiring over existing libs, not new functionality.<br><br>Scope notes (from a partial 2026-06-25 survey): the current integrated 'sam-aarch64' (tools/sam-aarch64/main.go) modes map onto from/to — assemble src->bin (--from=asm --to=bin/img), assemble .tbn->bin (--from=tbn --to=bin), --render .tbn->text (--from=tbn --to=asm), -E preprocess. Settle the format taxonomy (is 'gnu' distinct from 'asm'? our source IS gnu-syntax; --from=gnu may just alias, OR imply the future bulk GNU->.tbn import which does NOT exist yet — do NOT build that here unless scoped). Keep the existing CI/Makefile/run-*.sh invocations working (they use the current flags — see tools/run-disasm-roundtrip.sh, run-release-gate.sh, Makefile release targets). LOW priority: per memory project_go_tools_are_z80_scaffolding, the Go tools are Z80-port scaffolding + oracle — do not gold-plate; this is a presentation reshape, not a value-add. | OPEN | pete | 👤 pete |  |  |  |  |
| <a id="i55"></a>**i55** | **Multi-section / linker-script honouring in refenc** — Explicitly punted in favour of the text2bin -flatten approach. Multi-section / linker-script handling in refenc would only be needed if a non-spectrum4-shaped project surfaces. Deferred; from ROADMAP deferred checklist (retired in i52 PR 3). | OPEN | agent | ready |  |  |  |  |
| <a id="i65"></a>**i65** | **SAM-side save-without-comments option (editor phase)** — On-SAM counterpart to the host -strip-comments/-thin-comments flags. Saves a .tbn with the editor region stripped/thinned so a 256 KB SAM can assemble sources authored on a 512 KB machine. q9 resolved (Pete 2026-06-10): the tool targets both 256 and 512 KB SAMs; the full unstripped release .tbn is allowed to exceed 256 KB, but a 256 KB user must still be able to assemble via this stripped disk handoff. Host equivalent already exists. | OPEN | agent | ⏳ i41 |  | [i41](#i41) |  | i59 |
| <a id="i70"></a>**i70** | **Phase-3 firmware self-provisioning: SAM fetches Pi firmware blobs via HTTP and persists to Trinity storage** — SAM IP stack fetches Pi firmware (several MB) from cdn.githubraw.com over HTTP (q11 resolved: on-SAM HTTP via uIP, host-tool-free; q12 resolved: Model A only). Full host-verifiable HTTP-fetch path complete + host-verified across Bricks 1–6 (#245/#247/#254/#256/#257/#259 netboot_http.asm). Remaining: hardware-gated (EEPROM read + section-C paging + HSAVE RST-8 dispatch on real Trinity; emulation-verified != hardware-verified, CLAUDE.md §5). i82 TFTP client bridges provisioning meanwhile; HTTPS stretch is i88. | OPEN | agent | ⏳ i93b |  | i133, [i93b](#i93b) |  | docs/specs/phase3-delivery-design.md, docs/notes/trinity-capabilities.md, docs/notes/bdos-version-landscape.md, src/netboot/netboot_http.asm, docs/notes/netboot-trinity-testing.md, i61, i66, i84, [i88](#i88) |
| <a id="i73"></a>**i73** | **i17 review residuals — LOW-severity hardening and dead-code findings** — L12: .set/.equ REDEFINITION -> HARD FAIL (Pete 2026-07-01, q49). The Z80 assembler hard-fails on .set/.equ redefinition; make the Go authority (tools/sam-aarch64/assemble/pass1.go resolveEquDirective ~L382) ALSO hard-fail (it currently silently overwrites, matching GNU as). This is a DELIBERATE divergence from GNU as -- document it. Rationale (Pete): strict-input / byte-identical-output. We already reject inputs GNU accepts (e.g. SIMD), so there is precedent; if code assembles at all, the generated binary is byte-identical to GNU as, so there is NO divergence in emitted code -- the desired guarantee. WORK: (a) add a test asserting the hard-fail on redefinition (Go + Z80 oracle); (b) change the Go resolveEquDirective from silent-overwrite to hard-fail; (c) document the intentional GNU-as input divergence (near the align-with-binutils notes). See memory feedback_align_with_binutils (refined: match binutils on OUTPUT byte-identity, but we may be STRICTER on input). | OPEN | agent | ready |  |  |  | PR #185, i17 |
| <a id="i77"></a>**i77** | **spectrum4 source succinctification — comments + pathological constant expressions** — Agent-driven, section-by-section, human-reviewed (Pete 2026-06-12): (a) comments rewritten aggressively succinct but still readable; (b) pathological constant expressions replaced with named equates (e.g. the longest instruction in the corpus: `movk w0, (65536 \| (((BORDER_COLOUR >> 16) & 65535) & 65535)), lsl #16`). Coordinate with upstream spectrum4 (content originates there; release.s re-flattens). Ties to editor-tui spec D1 (all comments <256B → u16 record length can revert to u8). Parked (editor era). | OPEN | agent | ⏳ i3 |  | [i3](#i3) |  | docs/specs/editor-tui-prototype-design.md, i76 |
| <a id="i81"></a>**i81** | **Upstream SimCoupé issue triage — review open issues and assist where we can** — Umbrella for SimCoupé upstream community contributions. Triage complete (2026-06-13): produced contributions i81a (PNG-endian fix PR — BLOCKED:Pete), i81b (issue #28 comment — DONE PR #370), i81c (SDL headless fallback — BLOCKED:Pete). i81a and i81c remain open; i81b is done. | OPEN | agent | ready |  |  |  | tools/font-proof/simcoupe-local-capture.patch, i80 |
| <a id="i81a"></a>**i81a** | **PNG-endian one-line upstream SimCoupé PR: the __aarch64__ little-endian-guard fix in Base/SimCoupe.h** — hunk 2 of tools/font-proof/simcoupe-local-capture.patch; confirmed NOT in upstream HEAD; ready to offer standalone; community submission is Pete's call. | OPEN | pete | 👤 pete |  |  |  | tools/font-proof/simcoupe-local-capture.patch, [i81](#i81), i80 |
| <a id="i81c"></a>**i81c** | **Optionally offer the SDL dummy-driver headless renderer fallback upstream** — Lower priority than i81a; community submission is Pete's call. | OPEN | pete | 👤 pete |  |  |  | [i81](#i81), tools/font-proof/simcoupe-local-capture.patch |
| <a id="i88"></a>**i88** | **On-SAM HTTPS/TLS-1.3 client to fetch Pi firmware directly from GitHub** — Phase-3 stretch, LOWEST priority (Pete: only when nothing else is left). TLS-crypto-only: github.com-only, pinned public key, no CA store (content is SHA-256-pinned so cert verification is skipped). Decomposed into i88a (6a host-verifiable state machine), i88b (6b record-reassembler), and i88c (6b hardware-gated integration). | OPEN | agent | ready |  |  |  | docs/specs/phase3-delivery-design.md, src/netboot/tls_client.asm, tools/netboot-oracle/tls/, docs/specs/tls13-handshake.md, [i70](#i70), i84, [i100](#i100) |
| <a id="i88c"></a>**i88c** | **i88 6b — hardware-gated integration: wire tls_reasm into tcp_conn, paging layout, bootable disk, real RST 8 on Trinity** — Wire tls_reasm_feed into tcp_conn CONN_SINK_FILTER; finalise paging layout; build a bootable disk; exercise over real RST 8 on Trinity hardware. Prerequisite: TC_RX must grow from 1056 to 16645 B (TLS max) before a real Certificate record fits. Hardware-gated (CLAUDE.md §5). | OPEN | agent | ⏳ i93b |  | i133, [i93b](#i93b) |  | src/netboot/tls_reasm.asm, src/netboot/tls_client.asm, src/netboot/tcp_conn.asm, [i88](#i88), i88b |
| <a id="i89"></a>**i89** | **Capture a Pi 3 netboot exchange (Phase-3) — extend i83/i86 oracle to the older Pi family** — Confirm Pi 3 boot ROM's DHCP behaviour (may differ from Pi 4 PXE option-43 dance) and its TFTP file set (bootcode.bin/start.elf/fixup.dat). The oracle extension proceeds from the already-known Pi 3 file set (from the tftproot), so a fresh capture is not a blocker; an optional real-hardware confirmation (hub + Wireshark / trinload technique) can be run when convenient. | OPEN | agent | ready |  |  |  | docs/notes/pi-netboot-capture-analysis.md §4, i83, i86, i84 |
| <a id="i89b"></a>**i89b** | **Capture a real Pi 3 netboot exchange + generate Pi 3 golden wire vectors** — Hardware-gated confirmation tail of i89. On Pete's Pi 3 + SAM with the hub+Wireshark rig, capture the DHCP DISCOVER/OFFER/ACK + first TFTP probe sequence and generate masked Pi 3 golden vectors (as the Pi 4 vectors were). Decisive open detail: whether the Pi 3 boot ROM is byte-strict about the option-43 'Raspberry Pi Boot' menu string (trailing spaces). Findings to confirm are in pi-netboot-capture-analysis.md section 4. | OPEN | pete | 👤 pete |  |  |  |  |
| <a id="i90"></a>**i90** | **%nobits/BSS section bodies dropped by -flatten — source-structure loss** — When source is flattened (-flatten, the release path), the entire body of every %nobits/BSS section (bss_kernel/bss_roms/…) is discarded: contributes zero bytes, so blank lines/comments inside BSS don't round-trip (release.s: 132 of 1472 blank lines, all in NOBITS sections). Pre-existing flatten property, not an i78 bug — i78 blank/comment preservation is exact for PROGBITS. Only matters once the SAM editor edits a flattened source with BSS content. Captured only — decide whether to preserve NOBITS editor content if/when the flattened-edit path needs it. | OPEN | agent | ⏳ i3 |  | [i3](#i3) |  | docs/specs/source-structure-preservation-design.md, tools/sam-aarch64/frontend/flatten.go, i78 |
| <a id="i93"></a>**i93** | **Phase-3 netboot B-DOS storage seam (bdos_seam.asm) — name-to-record glue** — Name→record glue shared by i83 server and i82 client. Three routines from Go authority bdos/bdos.go, host-verified: bdos_name_to_uifa (name→48-byte UIFA), bdos_difa_to_size (DIFA→32-bit size for OACK tsize), bdos_fill_save_uifa (name+page+offset+size→HSAVE UIFA). Field conventions from src/io.asm/loader.asm/main_loop.asm + samdos-file-io.md. Seam arithmetic LANDED + host-verified. Remaining: RST 8 hook dispatch (HGTHD/HLOAD/HSAVE/HRECORD) stays unverified until real Trinity (hardware-gated, CLAUDE.md §5). | OPEN | agent | ready |  |  |  | src/netboot/bdos_seam.asm, tools/netboot-oracle/bdos/, docs/specs/samdos-file-io.md, docs/notes/bdos-version-landscape.md, i82, i83, i62, i71 |
| <a id="i93b"></a>**i93b** | **B-DOS RST 8 hook dispatch on real Trinity — hardware-gated (HGTHD/HLOAD/HSAVE/HRECORD)** | OPEN | agent | ready |  | i133, i295, i316 | [i70](#i70), [i88c](#i88c), [i95b](#i95b) |  |
| <a id="i95"></a>**i95** | **Phase-3 netboot integrated server — single dispatch loop (ARP+DHCP+TFTP)** — Headline Phase-3 program: SAM+Trinity netboots a Pi by answering ARP+DHCP+TFTP in one loop, serving boot files by name from the flat store. Go authority + Z80 netboot_serve_once LANDED + host-verified: full DISCOVER→OFFER→REQUEST→ACK→ARP→RRQ→OACK→DATA session vs Go byte-for-byte. Bootable disk shipped. Hardware first-light (2026-06-18): ARP+TFTP sub-responders real-Trinity-verified (i96). Remaining: DHCP server + B-DOS store provisioning (i93) + Pi-boot gated on real Trinity. | OPEN | agent | ready |  |  |  | tools/netboot-oracle/server/server.go, src/netboot/netboot_server.asm, docs/plans/phase3-netboot-implementation-plan.md, docs/notes/pi-netboot-capture-analysis.md, docs/notes/netboot-trinity-testing.md, i80, i83, i86, [i93](#i93), i94 |
| <a id="i95b"></a>**i95b** | **End-to-end Pi boot on real Trinity — hardware-gated: DHCP + B-DOS provisioning + real Pi boot** | OPEN | agent | ⏳ i93b |  | [i93b](#i93b) |  |  |
| <a id="i100"></a>**i100** | **Firmware-download provisioning UX — user-selectable revision and file subset** — On-SAM multi-file firmware downloader with user-selectable revision pin and file subset. Host-verified slice complete as i100a; bootable migration + real HSAVE store leaf complete as i100b (closed). Remaining: i100c picker UX (Pete-gated interface design). | OPEN | agent | ready |  |  |  | src/netboot/fw_source.asm, src/netboot/http_main.asm, docs/specs/phase3-delivery-design.md, i100a, i100b, [i100c](#i100c) |
| <a id="i100c"></a>**i100c** | **Picker UX — user-facing revision/file-selection interface for the firmware downloader** — User-facing revision/file-selection interface for the firmware downloader. Interface design is Pete's call (editor-era UX, no autonomous decision here). No specific qN gates it — bare Pete decision. | OPEN | pete | 👤 pete |  |  |  | docs/specs/phase3-delivery-design.md, [i100](#i100) |
| <a id="i101"></a>**i101** | **End-to-end deliverable: fetch RPi firmware → assemble spectrum4 release on SAM → TFTP-serve a full Pi 400 netboot** — The integrated SAM IDE on ONE bootable disk — editor + assembler/disassembler + TFTP server — an interactive USER-DRIVEN program (q23 resolved 2026-06-20: NOT a boot→fetch→assemble→serve auto-sequence). User loads it, starts/edits a project, assembles release.bin/kernel8.img from release.tbn (M6 byte-match), and TFTP-serves so a Pi 400 netboots — features exercised on demand. The three org-&8000 layers coexist via paging (implementation detail), not sequential overlays. NOT SimCoupé-testable (no Trinity ENC/SD); real E2E is Pete's hardware gate. Arch: docs/specs/phase3-delivery-design.md §13. | OPEN | agent | ⏳ i3 |  | [i3](#i3) |  | docs/specs/phase3-delivery-design.md §13, [i100](#i100), [i70](#i70), i83, i86, i99, i80 |
| <a id="i106"></a>**i106** | **SAM-as-terminal: SSH (or telnet) client to Pete's Mac over the physical LAN** — KEX-stub SSH the sweet spot: send constant X25519 basepoint as client ephemeral pubkey; use server's received pubkey as shared secret — zero EC math, instant connect, interoperates with stock OpenSSH sshd. Skip host-key verification (drops Ed25519+SHA-512). Per-byte: ChaCha20-Poly1305 ~few hundred B/s, usable for SAM MODE 3 64x24. Secure-mode toggle (real X25519, ~10-min) nearly free via existing i88 X25519. Telnet fallback: minimal SAM code but needs Homebrew inetutils on Mac. Reuses i88 crypto + i70 tcp_conn. | OPEN | agent | 🔒 q57 |  | q57 |  | [i88](#i88), [i70](#i70), docs/specs/tls13-handshake.md §Prior art |
| <a id="i109"></a>**i109** | **Collapse the now-unarmed encode_logical_imm soft-reject hook** — PR #371 (q20/i73-L9 sweep) deleted encode_logical_imm_try, the only caller that ever armed encode_logical_imm_soft; with no armer, encode_logical_imm_reject always jp fails. The soft branch + encode_logical_imm_soft/encode_logical_imm_rejected flags are vestigial. Collapsing back to a plain jp fail + dropping the two equates is behaviourally identical (flag never set) -> release.bin byte-identical. LOW priority (touches v2-production code). The hook may be re-armed by the future i48c editor encoder — decide when i48c's encoder is designed. | OPEN | agent | ⏳ i48c |  | [i48c](#i48c) |  | src/slots/logical_imm.asm, [i73](#i73) |
| <a id="i117"></a>**i117** | **Extract sam-aarch64 learnings into a reusable open-source autonomous Claude project framework** — Starter framework/template so a new project inherits: autonomous-loop monitor (i97), split iN/qN registries + controlled-status discipline (i115 validator), §3 pre-merge review gate, per-session retro (i104), prime-directive verification ethos, brick + Go-authority-oracle loop, delegate-to-subagents context discipline, and doc-lifecycle + single-source-of-truth rules. Must be self-contained (portable out of Pete's ~/.claude/CLAUDE.md). Delivery options: template repo / init scaffold or Claude Code plugin. Scope + sequencing + packaging are Pete's call; do not auto-pick-up. | OPEN | pete | 👤 pete |  |  |  | i97, i104, i115 |
| <a id="i117a"></a>**i117a** | **Prior-art survey: existing Claude Code autonomous-orchestration plugins/frameworks vs i117** — Preliminary 2026-06-18 scan: metaswarm, ccswarm, ruflo, claude-plan-orchestrator, autonomous-dev, closedloop-ai, barkain, Composio AO + Claude Code Agent Teams; plus awesome-agent-orchestrators, awesome-cli-coding-agents. Most emphasise PARALLELISM. i117's model differs: single serial orchestrator + deterministic non-LLM monitor (i97) + prime-directive culture + blameless retro + iN/qN registry backbone. Deliverable: feature-by-feature comparison to confirm differentiation and whether gap justifies building i117. Research-first; cite each tool. Pete's call; do not auto-pick-up. | OPEN | agent | ready |  |  |  | [i117](#i117) |
| <a id="i118"></a>**i118** | **i82 TFTP client — OACK handling / standard-TFTP-server interop** — The i82 client requests RRQ options (blksize/tsize/timeout/windowsize) but has no OACK (opcode 6) handling. A standards-compliant server (e.g. macOS tftpd) answers an optioned RRQ with OACK before DATA; the client ignores it, never ACKs block 0, and hangs. Fix: on OACK -> learn server endpoint + adopt negotiated blksize (512 default if absent) + send ACK block 0; on direct DATA -> 512 default (today's hard-coded 1428 truncates); drop windowsize from request. Mirrored in Go authority + host tests. PR #422 landed the Go/Z80/host side; hardware test pending. | OPEN | agent | ready |  |  |  | tools/netboot-oracle/tftp/tftp.go, tools/netboot-oracle/tftp/clientloop.go, tools/netboot-oracle/client/client.go, src/netboot/netboot_client.asm, src/netboot/tftp_client_front.asm, i82, [i119](#i119), i120 |
| <a id="i118b"></a>**i118b** | **hardware verification — OACK path on real Trinity hardware** — Confirm the OACK path works on real Trinity hardware: netboot client connects to a standards-compliant TFTP server (e.g. macOS tftpd), receives OACK, ACKs block 0, and receives the file. Gated on Trinity hardware access + a standards-compliant server in the test environment. | OPEN | agent | ⏳ i310 |  | i272, [i310](#i310) |  | [i119](#i119), i82 |
| <a id="i119"></a>**i119** | **i82 TFTP client — persistence write-out on real hardware** — Wire the i82 client's B-DOS persistence write-out; deferred by Pete (first test = receive only; persistence later). Gaps: (1) record-0-selection bug — client_main selects record 0 = floppy; (2) HRECORD-stamp question — whether a build-disk .mgt record is HRECORD-selectable; (3) large-file staging via the i99 fw_span port; (4) filename >10-char UIFA truncation. Free-record detection reads the on-card list directly per docs/specs/trinity-record-detection-design.md (Trinity SD is SHARED — show-name + confirm-before-overwrite mandatory). HSAVE write-out + raw-SD list read stay hardware-gated. | OPEN | agent | ready |  |  |  | src/netboot/netboot_client.asm, src/netboot/bdos_seam.asm, docs/specs/trinity-record-detection-design.md, docs/specs/samdos-file-io.md, i82, [i93](#i93), i99, i114, i134 |
| <a id="i121"></a>**i121** | **SAM TFTP server — WRQ (write / accept-in) support: push disk images to the SAM, auto-slotted** — PRIMARY netboot use case: boot SAM, 'tftp put' a disk image from the Mac, SAM writes it to a free Trinity record, boot+test. Combines with the i83/i95 serve-out server into one program (RRQ+WRQ). q30 resolved (2026-06-20): auto-pick is a DISK CONFIG — at launch the program reads it; if absent it PROMPTS (auto-pick a free record? y/n); test/shipped disks ship that config allowing any free record, so unattended push works with a pre-configured disk + never overwrites a named record. Reuses i82 receive loop + i119 write-out; adds WRQ parse + OACK/ACK-0 + free-slot alloc (i114). | OPEN | agent | ready |  |  |  | src/netboot/, tools/netboot-oracle/tftp/, i82, i83, [i95](#i95), i114, [i119](#i119), [i70](#i70) |
| <a id="i121j"></a>**i121j** | **SAM-side interactive strategy prompt when config is undefined (deferred)** — DEFERRED (Pete 2026-06-21: more complex to model, not needed now). Complementary to the host-launcher config: if the in-memory config block's strategy is undefined (the host launcher / disk config left it unset, e.g. user chose 'ask on the SAM'), the program prompts the user on the SAM keyboard for the placement strategy at launch. For now the host launcher always sets a concrete strategy (default highest-free), so this path is unused. Requires modelling a SAM keyboard prompt in emulation. | OPEN | agent | ready |  |  |  |  |
| <a id="i123"></a>**i123** | **Investigate migrating the build from make to tup** — tup tracks the full dependency graph and rebuilds exactly what is stale (and removes orphaned outputs), structurally preventing the dirty-build/stale-artifact class — same class i116 patches test-by-test; tup would make stale-.bin reads impossible by construction. Pete's ~/git/spectrum4 uses tup ('amazing'). Scope: evaluate tup against current Makefile (pyz80 .asm→.bin, Go builds, disk-image assembly, CI jobs), then if it wins, plan the migration. | OPEN | agent | ready |  |  |  | Makefile, i116, i104 |
| <a id="i123b"></a>**i123b** | **Perform the make->tup build migration (Pete-greenlit)** — Contingent on Pete's go-ahead per the i123a evaluation. Port the 167-target Makefile to per-dir Tupfiles + Tuprules.tup, reusing spectrum4's template. Order: (1) symbol-import cluster (enc_fix_payload->assembler->test_mem/test_cluster) first — it is the footgun; (2) netboot standalone binaries (parallel); (3) disk-pack + table-codegen edges. Keep SimCoupe/release-gate in ci.yml; byte-compare tup outputs vs make until parity; add the FUSE-privileged tup-under-docker CI launcher. owner:pete = the go/no-go decision is his; implementation is agent work once greenlit. | OPEN | pete | 👤 pete |  |  |  |  |
| <a id="i126"></a>**i126** | **One faithful SAM emulation layer used by every test — fix fragmented/inadequate emulation** — Root cause of repeated hardware-debug detours (i82 client_main; the 2026-06-23 yellow-screen): partial emulators, each modelling half the SAM. The flat Go netboot harness has Trinity seams (ENC/EEPROM/SD) + paging but NO screen/timing/sound/line-interrupts; SimCoupe is a faithful full SAM (real ROM, boots from address 0, screen, timing, sound, line-interrupt render) but has NO Trinity. So a routine that is a verbatim ROM copy can PASS the screenless Go harness and fail flat-yellow on hardware (the rainbow stripes are rendered by the live line-interrupt handlers, which the Go harness does not model). NORTH STAR (Pete 2026-06-23, feedback_comprehensive_emulation): ONE comprehensive emulation = fork SimCoupe (the most complete SAM) and PORT THE TRINITY MODELS IN (ENC28J60 + EEPROM + SD/B-DOS), so the single layer captures Trinity, network, storage, screen, timing, sound AND their interactions, boots from address 0 (i232), and is used by EVERY test. Then cross-subsystem surprises fail in emulation, not first on silicon, and the NETBOOT_HOSTTEST carve-outs (i231) are deleted. Stop the 'SimCoupe port is too much work' / 'merging the two emulators is overkill' shortcuts. Subsumes i124/i231/i232. | OPEN | agent | 🔒 q58 |  | q58 | [i232](#i232), [i254](#i254) | tools/z80-test-harness-go/harness.go, tools/netboot-oracle/z80/, src/netboot/, i124, i125, i116 |
| <a id="i143"></a>**i143** | **bdos_picker: hardware MODE-2 blit for picker_render (follow-up from i119d)** — picker_render in src/netboot/bdos_picker.asm is a stub ret (all decision/show-name/confirm logic is host-verified in i119d). This item wires the actual SAM MODE-2 screen blit so the picker display is visible on real hardware. Depends on i119d being merged. | OPEN | pete | 👤 pete |  | i119d |  |  |
| <a id="i168"></a>**i168** | **Future: import a full source file into the editor (fail-with-line-number on bad line)** — Future feature (q24, Pete 2026-06-20). The SAM editor accepts ONE LINE AT A TIME (parsed/validated on entry) — no whole-file import today. If/when import is added: on a bad line, fail and report the line number + the error; the user fixes the source and retries (Pete's preferred behaviour). Gated on the editor groundwork (depends i3) — a normal backlog item, not part of current editor specs. | OPEN | agent | ⏳ i3 |  | [i3](#i3) |  |  |
| <a id="i177"></a>**i177** | **Publish trinboot: open-source EEPROM boot-config patcher (Python-over-trinload + bootable .mgt) for Trinity users** — Release the SAMBOOT EEPROM boot-config + stripes-restore patch as a standalone open-source tool for any Trinity owner: a Python program sent over trinload that patches the EEPROM, or a bootable .mgt. Gives Trinity users the configurable default-boot-record + opening-stripes feature for free. NOT a requirement for SAMBOOT -- depends on it (publication only). | OPEN | agent | 🔒 q56 |  | i135, i176, q56 |  | docs/specs/samboot.md |
| <a id="i182"></a>**i182** | **Bootable fetch-and-boot disk + real-Trinity run of client_fetch_boot** — i122c delivered the host-verified PXE-style fetch-and-boot orchestration entry (client_fetch_boot: EEPROM identity -> TFTP-fetch a .mgt straight into a scratch record via the raw_record_sink -> validate -> ALHK-boot). To exercise it on real hardware, build a bootable SAM disk whose autorun invokes client_fetch_boot (a netboot-fetch-boot-disk make target mirroring netboot-client-disk), decide the autorun entry vs client_main, and run it on real SAM+Trinity: serve a stamped 819200-byte .mgt, confirm it streams into the scratch record (1600 HWSAD sectors), validates, and ALHK-boots it. Hardware-gated (the whole point is the real SD write + real boot, unverifiable in emulation); entangled with the auto-boot/control-return autonomy (i132/i135). The full-size valid stream is also not driven through the bit-banged ENC in the host harness, so the real wire run is the first full-scale fetch-and-boot. | OPEN | agent | ready |  | i122c |  |  |
| <a id="i182b"></a>**i182b** | **Real-Trinity run of client_fetch_boot via the fetch-and-boot disk** — Hardware-gated completion of i182 (unverifiable in emulation). Boot the i182a fetch-and-boot disk on real SAM+Trinity, serve a stamped 819200-byte .mgt, confirm it streams into the scratch record (1600 HWSAD sectors), validates, and ALHK-boots it. Entangled with auto-boot/control-return autonomy (i132/i135). The full-size valid stream is the first full-scale fetch-and-boot (not driven through the bit-banged ENC in the host harness). Needs i182a's disk. | OPEN | agent | ready |  | i182a, i272, i295 |  |  |
| <a id="i204"></a>**i204** | **i48c-b8e-4: overlayClassify + literalWord (overlay mirror + PC-invariance probe)** — Fourth/final brick of i48c-b8e (see umbrella). Port the overlay/literal wiring that REUSES the b8e dispatcher. overlayClassify (overlay.go:82-163) mirrors encodeInst but returns {relocated-slot,expr,isLitpool,litWidth,rt} instead of a base word - classifyMem (overlay.go:177-220), classifyMovImm (overlay.go:230-254); special/compound symbolic forms with no overlay support return error. literalWord (compact.go:354-370) probes encodeInst at pcProbeA=0 and pcProbeB=0x40000: either errors -> keep symbolic; wA!=wB -> PC-dependent (keep symbolic); wA==wB -> bare 4-byte literal. ~100-150 LOC, decision trees over brick1/2/3 dispatcher. Depends on bricks 1-3. Faithful port (CLAUDE.md s6); Go authority overlay.go overlayClassify + compact.go literalWord. Completes b8e: text->.tbn can encode symbolic INST records on the SAM. SCOPE NOTE (from PR #561 review): the form-table path also has a bare Imm16 SlotKind (0x08, used only by udf/mnemonic 89) that brick-1 (i199) does NOT encode — encode_inst cleanly fail-tags it (ENC_TAG_BADSLOT) rather than mis-encoding. encode_imm_n caps at 7-bit width, so Imm16 needs its own 16-bit placement encoder. Ensure full SlotKind coverage (incl. plain Imm16) before closing b8e. | OPEN | agent | ready |  | i199, i203, i201 | [i48c-b8e](#i48c-b8e) |  |
| <a id="i204b"></a>**i204b** | **i48c-b8e-4b: overlay_classify + literal_word + compact_inst (Z80 port + boot self-test)** — Faithful Z80 port of overlay.go::overlayClassify (+classifyMem/classifyMovImm/exprContextDep/foldSlotPCDependent), compact.go::literalWord + compactInst, litinsts.go::IsFullyLiteral, and aarch64enc::ZeroSlot, wired as a BUILD_TESTS boot self-test asserting Z80 output against Go-authority-computed values. The port is WRITTEN, Go-authority-verified, and staged (scratchpad i204b-insn_overlay.asm / i204b-test_overlay_classify.asm; Go authority test tools/sam-aarch64/assemble/overlay_classify_fixtures_test.go landed by the i204a PR). BLOCKED ON BUDGET: the routines are ~1300 B and, like encode_inst, MUST be inline in section C (they call encode_inst which reads ENCTAB in section A, so they cannot be evicted off-axis — the registry's i69/i205 analysis confirms lever-1 is exhausted for ENCTAB-coupled code). The test-variant assembler.bin has ~11 B of &C000 headroom; commoning recovers tens of bytes, not 1300. Depends on i234 (a budget-relief item) which must free section-C space (serial test bay / multiple-boot variant / page-pool memory model per q36/i2) before these can land + the boot self-test runs green. | OPEN | agent | ⏳ i234 |  | [i234](#i234) |  |  |
| <a id="i215"></a>**i215** | **IDE page-persistence (spill): lazy-spill cold DOC blocks to Trinity or floppy/tape when the pool is full** — Downstream of i2 (Pete, 2026-06-22, q36): the i2 baseline refuses-with-message when the page pool is exhausted. This OPTIONAL feature makes alloc_page lazy-SPILL a cold DOC block to a persistence backend instead of refusing (design §4.5/§5/§7): serialize the block to disk, free its page, satisfy the alloc; reload on demand. Backend is PLUGGABLE — Trinity storage if the machine has it, else a physical floppy/tape — so a space-limited user on ANY machine gains document headroom beyond the ~144KB/256KB ceiling. HARD CONSTRAINT: the assembler must run fully on a floppy/tape-only machine with NO Trinity, so the spill backend is optional and must degrade to refuse-with-message when absent. Depends on i2 (the pool allocator + the page_owner[] discipline) but NEVER blocks it. | OPEN | agent | ready |  | i2a |  |  |
| <a id="i215c"></a>**i215c** | **Spill manager: concrete pluggable backends (Trinity record / floppy-tape) + boot wiring** — Concrete SpillBackend implementations behind the i215a interface: Trinity SD-record store when present, else physical floppy/tape; selected at boot by capability detection. The assembler must run fully on a floppy/tape-only machine with NO Trinity (HARD constraint), so the backend is optional and degrades to refuse-with-message when absent. SimCoupe/boot integration. Depends on i215b. | OPEN | agent | 🔒 q60 |  | i215b, q60 |  |  |
| <a id="i231"></a>**i231** | **Systemically eliminate NETBOOT_HOSTTEST==0 emulation carve-outs + CI guard against reintroduction (emulation-first enforcement)** — Pete (2026-06-23), load-bearing + recurring: the 'if defined(NETBOOT_HOSTTEST)==0' pattern hides Z80 code from the emulation/host build so it ships to hardware UN-emulated — the exact CLAUDE.md §7 / feedback_emulation_first anti-pattern (the i82 client_main brick, the samboot_stripes stub). It keeps getting reintroduced (agents add new ones), so the fix must be SYSTEMIC, not per-stub. 34 sites across 13 src/netboot files (audit 2026-06-23). For each: determine what it gates + why the flat harness can't run it (ROM/RST 8/screen), then either (A) remove the gate (code runs safely in emulation as-is), (B) make the harness model what it needs, or (C) run that payload under SimCoupe (the ROM/screen-bearing CI gate). Plus a CI GUARD that FAILS the build if a NETBOOT_HOSTTEST==0 block is added beyond the shrinking allowlist (ratchet to zero), and crystal-clear CLAUDE.md §7 + memory wording so future agents never add one. First fix already landed: samboot_stripes (authoritative ROM port, no carve-out). Emulation-first: ANYTHING that runs on hardware is tested in emulation first, no exceptions. | OPEN | agent | ready |  |  |  |  |
| <a id="i231b"></a>**i231b** | **Systematically eliminate the 34 existing NETBOOT_HOSTTEST==0 carve-outs (ratchet the i231a allowlist to zero)** — The cleanup half of i231, enforced by the i231a ratchet guard. For each of the 34 sanctioned carve-out sites: determine what it gates + why the flat harness can't run it (ROM/RST 8/screen), then (A) remove the gate (runs safely in emulation as-is), (B) make the harness model what it needs, or (C) run that payload under SimCoupe (the ROM/screen CI gate); then ratchet the allowlist entry down. Large + subtle; split per file/cluster as taken on. Depends on i231a (the guard + allowlist exist first). | OPEN | agent | ready |  | i231a |  |  |
| <a id="i231b-b4"></a>**i231b-b4** | **Eliminate the harder carve-outs (post-i231b first pass), per file/cluster** — Umbrella for eliminating the carve-outs left after the i231b first pass, taken on per file/cluster (CLAUDE.md rule 7 / the i126 comprehensive-emulation north star). Children: i231b-b4a (smoke_test.asm, DONE) + i231b-b4b (the remaining 28 sites / 8 files). | OPEN | agent | ready |  |  |  |  |
| <a id="i231b-b4b"></a>**i231b-b4b** | **Eliminate the remaining carve-outs in netboot_serve.asm (11 sites / 1 file — the hard core)** — The last carve-out file after smoke_test.asm (b4a), csd_probe.asm (b4c), netboot_server.asm (b4d), netboot_dumper.asm (b4e), bdos_seam.asm (b4g), http_main.asm (b4i), with netboot_client.asm split out as [[i231b-b4h]] (blocked on a STAGING/high-address resolution). Live ledger 11 sites / 1 file: netboot_serve.asm — the (HOSTTEST==0)*(DUMPER==0) COMBINED gates (the serve loop's real-hardware body: EEPROM read, ENC init, the B-DOS record-serve + WRQ-store path via bdos_seam RST 8 hooks). This is the hard core: the gates are ANDed with NETBOOT_DUMPER==0 (the dumper shares the source), so collapsing needs untangling the serve-vs-dumper build split, and the serve WRQ-store path touches the B-DOS record hooks + the SD write path (watch [[i313]] high-address + the sensitive SD-write area, memory feedback_trinity_sd_write_settled_truths). Investigate the serve/dumper build variants + the combined-gate structure before collapsing. Nesting maxed at i231b-b4X: track completion as a NEW sibling leaf under i231b-b4. | OPEN | agent | ready |  |  |  |  |
| <a id="i231b-b4h"></a>**i231b-b4h** | **netboot_client.asm carve-outs eliminated (5 sites): collapse the flat HOSTTEST binary into the bootable; client_main/client_fetch_boot already harness-driven** — Eliminate the 5 NETBOOT_HOSTTEST==0 carve-outs in src/netboot/netboot_client.asm (boot entry :55; sink-mode selector :420; cl_sink_payload+raw_record_sink_leaf :448; the bootable body :754; the real-module includes bdos_picker/eeprom/raw_record_sink/sd_csd :1114). INVESTIGATED (see PR that recorded this): the naive collapse (delete the flat netboot_client.bin HOSTTEST build, retarget netboot_client_test.go at netboot_client_boot.bin, make the 5 blocks unconditional) does NOT land clean. Removing the carve-out directives ALONE is a boot-binary no-op (the ==0 blocks were already in the REAL_LISTREAD boot build; verified byte-identical). But retargeting the flat test REQUIRES enlarging STAGING: netboot_client_test.go streams up to 7240 bytes into STAGING via the client's own ldir (makeFile(cliBlksize*5+100), TestClientWindowedSession/GapRewind) — the flat build has STAGING=16384, the boot build 2048; a 2048 boot binary fails those tests (frame-builder code above STAGING gets overwritten). And enlarging STAGING in place relocates the SD routines above ~0xD000 into the [[i313]] harness high-address defect (boot tests then fail). Irreducible in-place conflict: flat needs STAGING>=7240; boot needs SD routines below ~0xCF00. TWO resolution paths: (A) LIGHTER — move the STAGING 'defs' to the END of the image (after all includes) so growing it extends into high DATA RAM without relocating any CODE; then STAGING=16384 works and the collapse proceeds (verify all boot tests + retargeted flat tests + the 32768 boot-fit-check). (B) fix i313 (the emulation-first path), then enlarge STAGING in place. Care points confirmed GREEN: the flat test never reaches the LISTREAD/csd_set_bd_records path (harness-hook vs REAL_LISTREAD CMD17 difference is moot); all 15 symbols it uses exist in netboot_client_boot.map (clean retarget). Makefile: delete the flat recipe + its .PHONY + netboot-z80-routines membership; netboot-client-disk already uses the boot bin. Fix netboot_client_test.go:49 stale 'make netboot-client' comment. Ledger 18->13 / 3->2 files. Recommend path A (no harness dep). | OPEN | agent | ready |  |  |  |  |
| <a id="i232"></a>**i232** | **All Go-harness tests boot the SAM from address 0 (real ROM cold-init) for faithful machine state, not Call-with-seeded-state** — Pete (2026-06-23), foundational fidelity principle (part of i126): booting from address 0 with the real ROM IS what a SAM does — running a routine in isolation with hand-seeded registers/sysvars (e.g. a hand-seeded PALTAB) is not emulating the machine, it is testing a fragment in a made-up state. The Go library boots from reset in milliseconds (samboot_real_boot_test.go already does it: load rom.bin, run from PC=0, the ROM cold-init MNINIT->NEW2->streams populates PALTAB/STREAMS/FLAGS/etc.), so every harness test should boot from 0 first and then exercise the code in the REAL post-boot state. Deliverable: a reusable 'boot the SAM from reset, then run X' harness helper, adopted as the default across the koron-go tests (the netboot leaf tests currently Load+Call without booting). Big refactor; FUTURE — must not block the current bootblock RAM-test work. Captured per Pete's instruction to record + move on. | OPEN | agent | 🔒 q58 |  | [i126](#i126) |  |  |
| <a id="i234"></a>**i234** | **Section-C test-variant budget relief for the encode_inst+overlay self-tests (&C000 cliff)** — Section-C test-variant budget relief via a SECOND BUILD_TESTS boot variant (Pete 2026-07-01, q54 = lever 2). The BUILD_TESTS binary cannot fit the i204b overlay_classify/literal_word/compact_inst routines (~1300B, which MUST stay in section-C -- they call encode_inst reading the section-A ENCTAB, so cannot be evicted off-axis) + their boot self-test ALONGSIDE the existing inline test suites (the &C000 cliff). FIX (lever 2, i69 'tests need not coexist'): a second BUILD_TESTS boot variant -- new -D flag + make target + harness boot test + CI job -- running the encode/overlay self-test suite WITHOUT the other inline suites, time-multiplexing section-C memory across two test runs. Same pattern as the existing off-axis test clusters. This is a TEST-BUILD budget issue ONLY (the production editor runtime fits, not affected). Option 4 (i2/q36 page-pool) rejected: i2 is DONE + q36 deleted (stale framing), AND the routines cannot page off-axis anyway. Unblocks i204b + the i27b/i27c errata ports. | OPEN | agent | ready |  |  | [i204b](#i204b), [i27b](#i27b), [i27c](#i27c) |  |
| <a id="i254"></a>**i254** | **Run ALL tests in CI: private binaries as repo secrets (SKIP_PRIVATE_TESTS default-true, our CI overrides) + add currently-excluded packages** — Pete (2026-06-25), follow-on to i253. GOAL: CI runs EVERY test; nothing silently excluded. (1) SKIP_PRIVATE_TESTS=true is the CI DEFAULT (forks get no secrets -> they skip the gated tests explicitly). (2) OUR repo's CI materialises the private binaries from encrypted GitHub repo secrets (rom.bin, eeprom.bin, B-DOS 1.5t -> the capture paths) and overrides SKIP_PRIVATE_TESTS=false so our CI runs them; GitHub withholds secrets from fork PRs. Pete is fine storing them as secrets. (3) ADD the currently-CI-excluded packages (z80-test-harness-go, zx0-greedy, editor-prototype) to CI, guarded by SKIP_PRIVATE_TESTS where they need private resources. TENSION RESOLVED (Pete 2026-06-25): the 'go harness is not a CI gate' rule predates Trinity emulation — SimCoupe was then strictly more complete so it gated CI; now the go harness emulates Trinity (ENC/SD/EEPROM) which SimCoupe does NOT, so gating Trinity-requiring tests on the go harness in CI is CORRECT (netboot-oracle/z80 already does). CONSOLIDATION: i126 (port Trinity INTO SimCoupe) -> once it lands CI can consolidate on SimCoupe. CAVEAT on 'private': the proprietary/non-redistributable status of Colin's binaries is a PRECAUTIONARY assumption by Pete, NOT confirmed fact — they may be freely redistributable. If confirmed redistributable, they could simply be committed/fetched and skip the secrets entirely (simpler). PETE-GATED: Pete creates the secrets. depends_on i253 + i126. Detail: docs/plans/i253-no-silent-skips.md. | OPEN | agent | 🔒 q58 |  | i253, [i126](#i126) |  |  |
| <a id="i264"></a>**i264** | **SAMBOOT selector firmware: hold-key-at-boot launches a record picker (choose boot record by number/name) instead of hardcoded record 3** — Today's forked bootloader (trinity-autoboot, flashed via i135c) auto-boots a HARDCODED record (3 = trinload). Make it configurable: at power-on, if a chosen key is held (e.g. F9 or ESC — pick one, note the alt), the bootloader launches a small RECORD-PICKER program that lists the available Trinity SD records and lets the user select one by NUMBER or NAME; the choice is persisted to the 'SAMBOOT Config' EEPROM chunk (the bootloader's read_config path already exists and is dormant behind the hardcode) so subsequent power-ons auto-boot the selected record. If the key is NOT held, boot the configured record (or fall to the opening screen + wait-key when none). Build in the PRIVATE sibling repo trinity-autoboot (q55: local, not in CI); reuse the by-name EEPROM chunk reader + record-list reader (trinity_storage_shared_resource: read the on-card record list directly, show name + confirm). Develop + verify in the netboot-oracle emulation (key-held vs not, picker lists records, selection persists to config chunk, boot dispatches the chosen record). HARDWARE flash (re-flash via eeprom_flash_chunk1) + on-SAM test GATED ON PETE PRESENT (TAPO smart-plug incoming will let the agent power-cycle for autonomous test). This + i194 (write disks to storage) are the two gaps between today's fixed-record auto-boot and full autonomous SAM control. | OPEN | agent | ready |  |  |  |  |
| <a id="i264b"></a>**i264b** | **Record picker UI (Increment 2): list available records, select by number/name, persist the SAMBOOT Config chunk** — Record-picker UI (Increment 2 of i264). HOME DECISION (Pete 2026-07-01, q59): the picker lives in the WRITABLE EEPROM as its OWN chunk(s), extending the bootblock code -- NOT the ~250-byte config chunk (A), not B-DOS scratch (B), not a separate SD record (C). The ~250B limit was the CONFIG chunk only. VERIFIED against ~/sam-archive/samboot-capture/eeprom.bin: 128KB total, 88.3% blank flash (0xFF) = ~113KB FREE, ~15KB used (~12KB is B-DOS 1.5t in the low ~12KB; the chunk directory is up at ~122-131KB; the middle ~110KB is empty). Ample. On a key-held boot the bootblock loads+runs the picker chunk (as it loads the B-DOS chunks), reads the on-card record list via the raw SD driver, renders a BIOS-style menu by number+name, takes a selection, persists to the SAMBOOT Config chunk (write_chunk). Develop+emulate in netboot-oracle; EEPROM flash hardware-gated (i266/Pete); build in the private trinity-autoboot repo (q55). BIOS-menu UI design = a future sprint. | OPEN | agent | ready |  |  |  |  |
| <a id="i284"></a>**i284** | **Push the CJ's Elephant playable disk (cjs-sam-remake boot_m2b.mgt) to the SAM over Trinity TFTP — the real-world end-to-end demo of the now-working SD write** — Real-world validation/demo (Pete): push the CJ's Elephant Antics playable disk (cj.mgt = ~/git/cjs-sam-remake boot_m2b.mgt, a SEPARATE Pete project, 819200 B) to a free Trinity SD record via the hardware-proven sd_push path (sd-push.py + src/netboot/sd_push.asm, incl. the i315 side-major reorder), then BOOT it on the SAM. Validation is SIZE-ONLY (i285): cj.mgt is accepted (no BDOS stamp; +232 is not a boot/select requirement - memory feedback_trinity_sd_write_settled_truths). Deps i289/i293/i295 all DONE. NOTE: a hardware-confirmed sd_push run already wrote record cjfixed and BOOTED CJ's Elephant on the real SAM (i295/i302, #784) - the mechanism is proven; this item is now the clean repeatable one-command push+boot demo/wrapper (drive boot via SAMBOOT autoboot i264b or a pushed boot-record). The .mgt-to-record ordering (D3, track-major vs side-major) is SOLVED (i294/i315) - do NOT re-list as open. Possibly-remaining: D1 (in-context HRECORD rst-8 crash) only if it recurs on the sd_push path. | OPEN | agent | ready |  | i289, i293, i295 |  |  |
| <a id="i288"></a>**i288** | **Emulate the ENC28J60 ereset + lighter re-arm options faithfully per the datasheet (so re-arm-after-SD recovery is testable in emulation)** — RESOLVED by the authority resolution + #771: the question what ENC re-arm is needed after an SD op is answered = NONE (only ereset disturbs RX; a mux-select cannot; per encdrv.asm + the ENC28J60 datasheet). The over-aggressive rxDisarmed-on-SD-select model was the bug, fixed in enc28j60.go by #771. Re-arm-after-SD is now faithfully modelled (= none), which was this item goal. | OPEN | agent | ready | [#771](https://github.com/petemoore/sam-aarch64/pull/771) |  | i289 |  |
| <a id="i291"></a>**i291** | **Measure the real ENC28J60/shared-PIC settle time on hardware (unattended ethernet-reporting bisection) to replace the conservative 1200-T-state emulator constant** — Follow-up to i289 (#755). The enc28j60.go sdInitSettling window is currently a conservative 1200-T-state constant (4x the documented ~50us) — a pure emulator-fidelity knob that never runs on real silicon, safely bounded so it catches the i242/i287 SD-before-ENC ordering bug without latching. This item MEASURES the real settle to make the model faithful rather than conservative, which also independently validates the shared-PIC settle model underpinning the §8ag serve-crash interpretation. Approach (Pete's, 2026-06-30): a trinload-pushed Z80 probe that does the &38 SD-init, waits N T-states, issues an identity probe, and reports stale-vs-fresh over ETHERNET (UDP markers, not screen — so it runs unattended; bisect N between a too-small lower bound and a too-large upper bound to converge on the real settle). Then set the emulator constant to the measured value (+ margin). owner agent: now self-serviceable (i266 TAPO power-cycle + i272 passwordless tcpdump). NOT required for correctness — purely a fidelity refinement. | OPEN | agent | ready |  |  |  |  |
| <a id="i297"></a>**i297** | **Investigate on-SAM RECORD-list hang after ~824 + DEF PROC error post-sd_push write** — After the 2026-06-30 sd_push HW write, an on-SAM RECORD listing hung after ~824 records + a DEF PROC not found BASIC error. Determine: card record-list state after the incomplete write, vs the RECORD command for many records, vs the SAM post-sd_push BASIC state (unclean exit). Relates to the sd_push-clean-exit item + the slowness. | OPEN | agent | ready |  |  |  |  |
| <a id="i304"></a>**i304** | **Reconstruct a labelled B-DOS 1.5t source from 1.5a + reassemble to byte-match** — Root-cause fix for the recurring 1.5a-vs-1.5t authority lure (Pete flagged it 2 sessions running; feedback_bdos_15t_not_15a). Start from reference/bdos/bdos15a.src.txt (the clean labelled 1.5a COMET source), identify the routines 1.5t changed (per bdos15t-beta6.annotated.dis: the SD init ladder, the 32-bit linear seek replacing hd.seek/set.chs/seek.base, the CSD/records math, record-base poke), splice the 1.5t versions in, and reassemble (pyz80) iterating until it produces the EXACT 1.5t binary (extracted/bdos15t-beta6.bin) -- the byte-match is the proof of correctness. Retain all 1.5a comments; finally annotate the new routines. Differences are minimal. Deliverable: a committed reference/bdos/bdos15t.src.txt (+ a make target asserting byte-identity) that becomes THE cited Trinity-HW B-DOS authority, and update the in-repo references/plan/code citations that currently point at 1.5a. NOTE: 1.5t is Colin's proprietary binary (kept out-of-repo, SKIP_PRIVATE_TESTS-gated) -- the reconstructed SOURCE is ours but the byte-match gate must guard against committing Colin's bytes; follow the existing private-artifact pattern (csd_decode_colin_test.go resolves the bin from ~/sam-archive). | OPEN | agent | ready |  |  |  |  |
| <a id="i305"></a>**i305** | **Retire superseded create-record implementations + stale branches (post #773)** — After PR #773 lands, remove the competing create-record scaffolding. STALE BRANCHES: i295-create-record (the #771 worktree at ~/git/sam-aarch64-771recover), origin/i293-sd-push, origin/i290-owncmd24-elim, origin/i194-own-cmd24-record-write, origin/fix-disk-record-validation-size-only, local i293-bdos-direct-pivot. IN-TREE dedupe: two record-write paths coexist -- sd_push.asm (own-LBA create-record, canonical) vs the serve WRQ path (netboot_serve.asm + raw_record_sink.asm + the bdos_seam HWSAD/HRECORD seam); decide canonical + remove or clearly scope the other. THIS OWNS the dead HWSAD/HRECORD seam still LINKED-but-unreachable in sd_push.bin via the wide bdos_seam.asm include (i303 scoping, 2026-07-01): NO seam routine is deletable (each is live in serve/client/http or test-driven -- incl. sd_push_faithful_test.go driving bdos_write_record/bdos_select_record against sd_push.bin), so the options are (a) gate the HWSAD/HRECORD block out of sd_push.bin behind a flag serve/client/http set but sd_push does not [requires retiring sd_push_faithful_test.go's HWSAD FACT-3/4 assertions first], or (b) leave it linked-dead (harmless, fits 16KB) -- agent's call when worked. Also review sd_push_faithful_test.go if it still asserts the old HWSAD path. | OPEN | agent | ready |  |  |  |  |
| <a id="i306"></a>**i306** | **L14 (record 14) first ~388 sectors corrupted by the pre-fix buggy push -- restore if needed** — The pre-fix sd_push used base=2438 (not B-DOS 2050), so cj 1600-sector write at LBA 21638-23237 spilled 388 sectors into B-DOS record 14 (catalogue L12/L14 region, body 22850-24449): LBA 22850-23237 overwritten. Pete data -- restore L14 from backup if it mattered. The base=2050 fix + the out-of-band write-guard (both in #773) prevent recurrence; future pushes only touch a genuinely free record. | OPEN | pete | 👤 pete |  |  |  |  |
| <a id="i309"></a>**i309** | **Makefile: several netboot .bin rules omit sd_csd.asm (and other transitive includes) from prereqs → stale local incremental builds** — csd_probe.bin (Makefile:933) and likely other netboot rules include sd_csd.asm transitively (via csd_probe.asm/netboot_serve.asm) but do NOT list it as a make prerequisite, so 'make' does not rebuild them when sd_csd.asm changes -> stale .bin -> false-green local tests (the i280b-class stale-binary trap; feedback_go_test_stale_binary_cache). CI is unaffected (clean checkout builds all fresh). Found during i301: had to 'make -B netboot-z80-routines' to test an sd_csd.asm change because 'make netboot-z80-routines' reported 'Nothing to be done'. Fix: audit every netboot .bin rule and ensure each lists sd_csd.asm + its transitive includes as prereqs (or auto-generate the dep list). Low-risk, mechanical. | OPEN | agent | ready |  |  |  |  |
| <a id="i310"></a>**i310** | **Provide a standards-compliant TFTP server in the test environment (for the i118b OACK-interop hardware test)** — i118b needs the netboot CLIENT to fetch from a STANDARDS-COMPLIANT third-party TFTP server (e.g. tftpd-hpa on the Pi, or macOS tftpd) that emits OACK for optioned RRQ. No such server is installed/reachable in the test env (no tftpd on this host; a standards-compliant server binds privileged port 69 -> needs apt install + config or a setcap/sudoers grant, which is Pete's host action, not covered by i272's tcpdump/arping caps). Once provided + reachable from the SAM (192.168.2.75), i118b unblocks and the agent runs the OACK-receive shot self-serve (power-cycle + push client + tcpdump). owner:pete = the one-time host setup. | OPEN | pete | 👤 pete |  |  | [i118b](#i118b) |  |
| <a id="i311"></a>**i311** | **Retire the AttachBDOS Go mock behind a shrink-to-zero ledger, now the faithful B-DOS rig has landed** — i274 P2 (retire the mock-substitutes-for-real anti-pattern). The AttachBDOS Go mock (bdos_store.go BDOSStore / Machine.AttachBDOS) and the armed-dispatch double stand in for real B-DOS and HID the serve/WRQ write-path failure across the SD-write saga (i274 axis A, 'mock-substitutes-for-real'). The faithful rig now exists (sd_push_faithful_test.go: real Colin ROM + B-DOS 1.5t via LoadROMImage), so B-DOS behaviour can be exercised authentically. TASK: migrate every AttachBDOS-based test to the faithful rig, and put any remaining AttachBDOS usages behind a shrink-to-zero ledger (name->reason, modelled on tools/hosttest-carveout-allowlist.txt) that only ratchets DOWN, so no NEW mock-based B-DOS test can be added and the mock is provably retired when the ledger hits zero. The over-aggressive-settle negative-control gate (the other half of P2) is already i289 (DONE). Authority: CLAUDE.md rule 8; memory feedback_trinity_sd_write_settled_truths ('never use the AttachBDOS mock to test B-DOS behaviour — always real Z80 B-DOS in emulation'). May be partially gated on the faithful rig covering each AttachBDOS use case (assess coverage when worked). | OPEN | agent | ready |  |  |  |  |
| <a id="i313"></a>**i313** | **netboot-oracle z80 harness mis-executes code relocated above ~0xD000 (blocks STAGING-enlarging / large-overlay collapses)** — Discovered during i231b-b4h (netboot_client carve-out collapse). When netboot binary code (the real SD re-blocking / ldir / 32-bit-division loops in raw_record_sink_leaf @ ~0xCF53+, bdos_read_sector, the sd_csd routines) is relocated ABOVE ~0xCF00-0xD000, the koron-go/z80 v0.10.2 harness mis-executes it: the CPU wild-jumps below the org (observed PC=&4E0D, 'raw_record_sink_leaf did not return after 5000000 steps') and/or decodes garbage ('no T-state timing for opcode DD FC at PC=&E16A'). Measured threshold (via real Make rebuild, boot tests = TestRawRecordSinkMatchesGo + TestClientFinalize*): raw_record_sink_leaf PASSES at 0xCB53 (STAGING=5120), FAILS at 0xCF53 (STAGING=6144) and above. pyz80 relocates correctly (every byte-diff is exactly a +high-byte delta on absolute operands; structurally identical code), and the CardModel HRSAD/HWSAD handlers are address-independent (bdos_store.go, keyed on PC==0x0008) — so the defect is in the harness's execution of high-address code, NOT the model or the assembly. Root-cause is UNCONFIRMED: could be a koron core decode/timing bug, a gap in the harness patch layer (see the INI/IND-style quirk already patched at harness.go:225-236 — a DD FC T-state entry may just be missing), or a subtler interaction. Task: root-cause + fix so high-address code runs faithfully (emulation-first, CLAUDE.md §7 'a gap is a defect to fix'). This unblocks the clean STAGING=16384 netboot_client collapse (i231b-b4h) and any future large section-D overlay. NOTE i231b-b4h has a LIGHTER alternative that dodges this bug (move the STAGING defs to the END of the image so growing it never relocates code) — so b4h is not hard-blocked on this, but fixing this is the emulation-first-correct path and de-risks all high-address netboot work. | OPEN | agent | ready |  |  |  |  |
| <a id="i314"></a>**i314** | **Reconcile the firmware-fetch record naming: Z80 storage_sink_leaf names records by hex(name[0:3]) but the Go authority bdos.SpanRecordName is content-addressed hex(hash[0:3])** — Discovered during i231b-b4i (#782 review). RULE-6 divergence in the hardware-gated HTTP firmware-fetch store path. The Go authority tools/netboot-oracle/bdos/span.go:52 SpanRecordName(blobHash [32]byte, index) returns a CONTENT-ADDRESSED name: fmt.Sprintf('%02x%02x%02x%03d', blobHash[0], blobHash[1], blobHash[2], index) — hex of the first 3 bytes of the blob's SHA-256 HASH + the zero-padded record index. But the Z80 src/netboot/http_main.asm storage_sink_leaf passes FW_STORE_NAME_PTR (the manifest NAME ptr set by store_begin), not a hash, to fw_span.asm fw_span_record_name — which hex-encodes the first 3 bytes of whatever HL points to. So the Z80 names records hex(NAME[0:3])+index, NOT hex(HASH[0:3])+index. fw_span.asm's own comment ('Mirrors bdos.SpanRecordName(blobHash...)', 'Emit hash[0], hash[1], hash[2]') says it should take a HASH — inconsistent with the caller passing the name. This is PRE-EXISTING (not introduced by #782; the bootable was byte-identical) and hardware-gated (the http_main store path is unverified on real hardware, CLAUDE.md §5), and #782's new tests accurately document the CURRENT name-based behavior. RECONCILE: (a) is the Z80 a port bug — storage_sink_leaf should pass a hash pointer (CONN_PINNED_HASH or the computed digest) to be content-addressed like the authority — OR (b) are the authority + fw_span.asm comment stale and name-based naming is the intended design? Check the serve side (netboot_serve reads 'the same convention' per http_main.asm) — does it expect name-hex or hash-hex? If the persist + serve sides agree (name-hex) the round-trip works but diverges from the authority; if they disagree, records can't be found. Per rule 6 the Go authority is the encoding truth unless there's a documented reason to diverge (like the plain-vs-suffixed SpanPlan choice). May escalate to a qN if the design intent needs Pete. Relates to i101 (serve record naming), the fw_span/http_main store leaf. | OPEN | agent | ready |  |  |  |  |
| <a id="i319"></a>**i319** | **Hardware-test our .mgt feature images on the real SAM (assembler/disassembler/editor/...) via the store+boot toolkit** — The payoff milestone. Once the SAM disk-testing toolkit works — store (sd_push + i315 reorder), boot (i316 boot-a-record), delete (i317) — push ALL our existing feature .mgt images to the real SAM over the network and test the full disk-boot + feature behaviour on real silicon: the aarch64 assembler, the disassembler, the editor, the netboot/TFTP demos, etc. This is what unblocks 'sam as a daily driver' verification on hardware instead of only in emulation. Umbrella: split per feature-image as we go. Gated on the toolkit (i315 + i316) + the cj boot proof (i295/i302) that validates the store->boot chain end to end. | OPEN | agent | ready |  | i315, i316, i317, i308, i318, i322 |  |  |
| <a id="i319a"></a>**i319a** | **Toolkit end-to-end hardware proof: list/push/boot/delete cycle on the real SAM with netboot_serve.mgt (network self-confirming)** — First i319 leaf: run the full toolkit cycle against the real SAM+Trinity -- list-records.py (i322 hardware validation, read-only), sd-push.py netboot_serve.mgt to the first free record (timing the push for i323; i308 record# report), boot-record.py that record (the i316 hardware follow-up; serve disk self-confirms via any TFTP response, which trinload cannot produce), power-cycle back to trinload, delete-record.py the record (the i317 hardware follow-up), re-list to confirm freed. serve_main is audit-HARDWARE-READY (all six fixes); B-DOS is resident under autoboot (samboot.md: the bootblock loads B-DOS to RAM before trinload). | OPEN | agent | ready |  |  |  |  |
| <a id="i319a-b2"></a>**i319a-b2** | **boot leg: boot_record a stored record on real hardware + TFTP-confirm the booted serve disk** — The unfinished i319a scope: boot_record bounces back to trinload on real hardware (i328 tracks the tool gap -- fix there first), so the serve-disk boot + its TFTP self-confirmation never ran. Once i328 lands: boot-record.py 186 (netboot_serve is stored+stamped, kept on the card), poll TFTP for any response (trinload speaks no TFTP, so any TFTP reply = the record booted), then optionally delete 186 to close the loop. | OPEN | agent | ⏳ i328 |  | [i328](#i328) |  |  |
| <a id="i319b"></a>**i319b** | **Per-feature-image hardware boots via the toolkit (assembler / disassembler / editor / demos)** — The remaining i319 scope after the i319a toolkit proof: push + boot + verify each feature .mgt on real silicon (release/assembler disk, disassembler, editor, netboot demos...). Split one leaf per image as each is attempted. | OPEN | agent | ⏳ i328 |  | [i328](#i328) |  |  |
| <a id="i321"></a>**i321** | **Verify +232 'BDOS' stamp-patch parity across SD-write paths (sd_push patches; serve-WRQ + client_finalize size-only)** — sd_push.asm sp_data_write patches the 4-byte 'BDOS' stamp (+ record name @+210) into the record's sector-0 on write - the path that hardware-booted cj.mgt (i295/i302/i315). The serve-WRQ path (raw_record_sink -> bdos_write_record) and client_finalize validate SIZE-ONLY (819200) and do NOT patch the stamp. Per the file-vs-SD-record distinction (memory feedback_bdos_record_header_vs_disk_body, Pete 2026-07-01): a persisted Trinity SD record needs 'BDOS'@232 for B-DOS 1.5t (get.label) to accept it, else rep81 'Invalid record'. So a stampless .mgt (e.g. a FRED disk) pushed via the serve-WRQ or client route would land a record B-DOS rejects, whereas sd_push handles it. VERIFY: real gap (those routes should patch like sd_push) or intended (incoming .mgt always pre-stamped / those routes superseded)? Likely relates to i319 (disk push). If a real gap, port sd_push's sector-0 patch into raw_record_sink finalize + client_finalize. | OPEN | agent | ready |  |  |  |  |
| <a id="i323"></a>**i323** | **Measure + assess sd_push network throughput on hardware (the ~10-13 KB/s question)** — Pete measured the last 800KB image push at well over a minute (~10-13 KB/s). VERIFIED in source that neither recalled culprit lingers: i301 dropped the per-sector SD init, and the ENC ereset is init/exit-only NOT per-block. MEASURED (i319a campaign, 2026-07-02, this Pi -> real SAM over the streamed stage-2 driver): 1600 sectors / 819200 B in 112.0s = 7.1 KB/s, window=4. Attribution INCOMPLETE: pure ENC bit-bang RX accounts for ~32ms/sector (~16 KB/s ceiling) but observed is ~70ms/sector -- roughly half the budget is elsewhere (candidates: the CMD24 write cost per sector, ack turnaround, HOST-side python window pacing). REMAINING TASK: attribute the non-RX ~38ms/sector (e.g. compare a host-side tight-loop pusher, or time the SAM-side write path via markers) and conclude bit-bang-bound vs residual. | OPEN | agent | ready |  |  |  |  |
| <a id="i324"></a>**i324** | **Mine the accumulated ~/claude-retro/ session retro reports for converging process-friction signal (one-off)** — One-off (Pete 2026-07-01): the per-session retro-report practice (i104, DONE) has accumulated a substantial corpus of blameless retros in ~/claude-retro/. Read them ALL and find the CONVERGING signal -- recurring process/workflow friction that surfaces INDEPENDENTLY across multiple sessions (the whole point of writing them without reading prior reports first, so agreement is real signal). Produce a prioritised recommendation set, each mapping to a follow-up item / CLAUDE.md rule / hook / skill change, mirroring the i274 hardware-fidelity retrospective method. Read-only analysis (no code change in this item). Output: a synthesis + filed follow-ups. Likely high-value now that many reports exist. | OPEN | agent | ready |  |  |  |  |
| <a id="i325"></a>**i325** | **Optimise the assembler emit hot path (T-state tuning) -- measure candidates by total T-states to assemble release.s** — Lowest-priority future perf item (Pete 2026-07-01). After i24 lands option A (uniform per-byte LMPR-bracketed emit into a pool run), the emit hot path pays ~1/4 of its per-byte cost on the page-swap bracket and ~3/4 on register push/pop (ABI), zone/full checks, and OUT_PC/OUT_LEN bookkeeping. Candidates to evaluate: (a) a small staging buffer (8-128 bytes) in an always-paged region, flushed via ONE bracket + ldir per N bytes (amortises the page-swap); (b) restructure so the per-byte hot path is just store-to-buffer + pointer-bump, moving zone/page-run advance + OUT_LEN + the bracket into the per-flush path (potentially ~3x); (c) alternative-register / reduced-stack-op tuning of emit_byte. METHOD: measure EACH candidate by TOTAL T-states to assemble release.s (a representative full source); best T-states that STILL fits the memory budget wins. DO WHEN CODE IS FEATURE-COMPLETE (so the free-memory / staging-buffer budget is known). One-shot cost -> lowest priority. | OPEN | agent | ready |  |  |  |  |
| <a id="i326"></a>**i326** | **Widen the record-list seam BD_LIST_SECTOR to 16-bit (records >8160 unreachable on >6.7GB cards)** — BD_LIST_SECTOR (bdos_seam.asm) is a 1-byte list-sector input, so every record-list consumer (find-free scan, claim, free/delete, list_records) reaches only list sectors 1..255 = records 1..8160; on cards over ~6.7GB the higher records are invisible to the whole SD toolkit. list_records clamps + refuses ('E') past the limit (i322 review finding, PR 799) and its launcher warns -- honest but partial. Widen the seam input to 16-bit (bd_list_read_hw already builds a 32-bit LBA) and lift the clamps. Low urgency: the current card decodes to 4809 records. | OPEN | agent | ready |  |  |  |  |
| <a id="i327"></a>**i327** | **Faithful-machine drv_init fails after a real-B-DOS boot (emulation artifact) — blocks a faithful come-up gate for pushable tools** — Under the faithful machine (Colin's ROM + B-DOS booted to WTKY2, ENC model via capIO), drv_init's chk_trinity identity probe FAILS for both sd_push and list_records -- yet both init fine on real hardware from the same trinload context (i319a session, 2026-07-02). The model's post-boot ENC/PIC state does not match real silicon after B-DOS's boot-time Trinity traffic -- an emulation gap in the OPPOSITE direction from usual (model stricter than hardware). It masked the i319a scroll-wedge diagnosis (a faithful end-to-end come-up test wedged at sp_fail_init instead of the real cause) and blocks committing TestSDPushComesUpOn64GBFaithful (written+parked in the i319a session -- see PR 801's description for the test body location in git history). Fix the model state, then land the faithful come-up test for every pushable tool (the class the flat harness cannot see: it stubs RST &10 and the ENC probe timing). | OPEN | agent | ready |  |  |  |  |
| <a id="i328"></a>**i328** | **boot_record fails on real hardware: HRECORD+ALHK boot bounces back to trinload (i316 hardware follow-up)** — ROOT CAUSE FOUND (2026-07-02, authority-verified vs bdos15t-beta6.annotated.dis + emulation-reproduced + fix emulation-validated): ALHK (B-DOS 1.5t HAUTO, real &9DBD) does NOT load/boot -- it scans the SELECTED record's dir for AUTO*, stages the file's UIFA/DIFA at &4B00/&4B50, and returns E=1; the actual load+exec is the ROM1 continuation at &E274+ that the ROM hook-exit vectors into. From a trinload-pushed context (LMPR bit6=0: NO ROM1 at section D) the vector lands in RAM garbage at &E274 -> crash -> real SAM re-autoboots -> trinload answers ~10s later = the observed bounce. BOOT-keyword and power-on-bootloader contexts work because ROM1 is already mapped when the hook fires. Faithful repro: rst8/ALHK from armed page-1 context = NOP-slide E274,E275,E276... -> wrap -> ROM0 reset path -> page 1 wiped -> editor; with LMPR\|=&40 pre-rst8 the SAME drive loads all 7 body sectors of the seeded record and execs it, caller page 1 intact. FIX: bdos_boot_record maps ROM1 at section D (LMPR bit6) before rst8/ALHK; correct its false 'ALHK never returns' contract comment; regression = faithful seeded-record boot test (composed AUTO-CODE mgt seeded side-major + BDOS stamp). Remaining: land fix + faithful test; full trinload-idle chain rig (started: trinload_idle_faithful_test.go) additionally blocked on a model bug -- cross-run tstate reset leaves sdInitSettling/settleUntilT stale (SetTState clears busyUntilT on backwards jump but not the i242 settle window) -> chk_trinity reads stale in any later run. | IN_PROGRESS | agent | 🛠 in-progress |  |  | [i319a-b2](#i319a-b2), [i319b](#i319b) |  |
| <a id="i329"></a>**i329** | **0xEDB0 verb ambiguity: launcher stage-1 can stream '@' binary blocks into a live sd_push (junk record claim)** — trinload and the pushable tools share port 0xEDB0 AND the '?'->'!' / '@' verbs, so a launcher cannot tell WHICH program answers discovery. Observed in the i319a campaign (2026-07-02): a stage-1 push retry hit a LIVE sd_push instead of trinload; the 6 '@' blocks (the binary) were interpreted as disk data -- sd_push claimed the first free record (145) under its default name 'cj' and wrote ~6 junk sectors. Data-safety HELD (only a free record written; deleted via delete-record in-session). FIX: make tool discovery replies distinctive (trinload answers bare '!'; tools answer '!'+tool-tag -- list_records already appends BD_RECORDS; standardise) and teach push_and_run to REFUSE stage-1 against a non-bare-'!' responder. | OPEN | agent | ready |  |  |  |  |
| <a id="i330"></a>**i330** | **sd-push.py stage-2 discovery window shorter than sd_push's real startup (~12s on the 64GB card)** — sd_push's startup on the real 64GB card (EEPROM read + ENC init + CSD ladder + free-record list scan) takes ~12s; sd-push.py's stage-2 discover() is 5 attempts x 2s = 10s, so the launcher declared failure while sd_push was still coming up (i319a, 2026-07-02; the manual stage-2 driver with attempts=10 then worked). Bump the stage-2 discovery attempts (e.g. 15) in sd-push.py; same for list-records.py discover_inventory. One-line-per-launcher robustness fix. | OPEN | agent | ready |  |  |  |  |

## Tree view — umbrella hierarchy

- [i3](#i3) — `OPEN` — Editor groundwork (Phase 2) — full vision
  - [i43](#i43) — `OPEN` — Editor: register simulator with user-chosen seeds + replay-on-edit
  - [i44](#i44) — `OPEN` — Editor: inline instruction-explanation panel (prose semantics, flags, regs, bit-field layout)
  - [i45](#i45) — `OPEN` — Editor: 'did you mean a simpler instruction?' result-equivalent rewrite hint
  - [i46](#i46) — `OPEN` — Editor: system-register documentation panel (ARM MRA subset + editorial overlay)
  - [i47](#i47) — `OPEN` — Editor: inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations)
- [i27](#i27) — `OPEN` — Cortex-A53 errata workarounds (--fix-cortex-a53-*)
  - [i27b](#i27b) — `OPEN` — i27b: Z80 port — Cortex-A53 835769 NOP insertion
  - [i27c](#i27c) — `OPEN` — i27c: Z80 port — Cortex-A53 843419 ADRP->ADR rewrite + SAM enable/disable toggle
- [i41](#i41) — `OPEN` — Editor edit-buffer data structure — insertion performance for large source
  - [i41e](#i41e) — `OPEN` — Editor edit-model — i48-IR record payload + document symbol table + hybrid record model
- [i48](#i48) — `OPEN` — Single serialized format + pass-free syntactic encoder (the design)
  - [i48c](#i48c) — `OPEN` — Z80 text→overlay encoder (editor input path)
    - [i39c](#i39c) — `OPEN` — Phase 3 — bitfield-packing polish on the overlay slot bytes
    - [i48c-b8](#i48c-b8) — `OPEN` — Integration/fixture round-trip (assembler-coupled, lands last)
      - [i48c-b8c](#i48c-b8c) — `OPEN` — Z80 serialize: headerRows + WriteFile -> compact v2 .tbn bytes
      - [i48c-b8d](#i48c-b8d) — `OPEN` — Corpus round-trip integration: text -> compact .tbn on SAM byte-matches host (+optionally assemble to OUT)
      - [i48c-b8e](#i48c-b8e) — `OPEN` — Compact encoder adapter: literal-probe + overlay-patch emission (paged, needs ENCTAB)
- [i81](#i81) — `OPEN` — Upstream SimCoupé issue triage — review open issues and assist where we can
  - [i81a](#i81a) — `OPEN` — PNG-endian one-line upstream SimCoupé PR: the __aarch64__ little-endian-guard fix in Base/SimCoupe.h
  - [i81c](#i81c) — `OPEN` — Optionally offer the SDL dummy-driver headless renderer fallback upstream
- [i88](#i88) — `OPEN` — On-SAM HTTPS/TLS-1.3 client to fetch Pi firmware directly from GitHub
  - [i88c](#i88c) — `OPEN` — i88 6b — hardware-gated integration: wire tls_reasm into tcp_conn, paging layout, bootable disk, real RST 8 on Trinity
- [i89](#i89) — `OPEN` — Capture a Pi 3 netboot exchange (Phase-3) — extend i83/i86 oracle to the older Pi family
  - [i89b](#i89b) — `OPEN` — Capture a real Pi 3 netboot exchange + generate Pi 3 golden wire vectors
- [i93](#i93) — `OPEN` — Phase-3 netboot B-DOS storage seam (bdos_seam.asm) — name-to-record glue
  - [i93b](#i93b) — `OPEN` — B-DOS RST 8 hook dispatch on real Trinity — hardware-gated (HGTHD/HLOAD/HSAVE/HRECORD)
- [i95](#i95) — `OPEN` — Phase-3 netboot integrated server — single dispatch loop (ARP+DHCP+TFTP)
  - [i95b](#i95b) — `OPEN` — End-to-end Pi boot on real Trinity — hardware-gated: DHCP + B-DOS provisioning + real Pi boot
- [i100](#i100) — `OPEN` — Firmware-download provisioning UX — user-selectable revision and file subset
  - [i100c](#i100c) — `OPEN` — Picker UX — user-facing revision/file-selection interface for the firmware downloader
- [i117](#i117) — `OPEN` — Extract sam-aarch64 learnings into a reusable open-source autonomous Claude project framework
  - [i117a](#i117a) — `OPEN` — Prior-art survey: existing Claude Code autonomous-orchestration plugins/frameworks vs i117
- [i118](#i118) — `OPEN` — i82 TFTP client — OACK handling / standard-TFTP-server interop
  - [i118b](#i118b) — `OPEN` — hardware verification — OACK path on real Trinity hardware
- [i119](#i119) — `OPEN` — i82 TFTP client — persistence write-out on real hardware
  - [i143](#i143) — `OPEN` — bdos_picker: hardware MODE-2 blit for picker_render (follow-up from i119d)
- [i121](#i121) — `OPEN` — SAM TFTP server — WRQ (write / accept-in) support: push disk images to the SAM, auto-slotted
  - [i121j](#i121j) — `OPEN` — SAM-side interactive strategy prompt when config is undefined (deferred)
- [i123](#i123) — `OPEN` — Investigate migrating the build from make to tup
  - [i123b](#i123b) — `OPEN` — Perform the make->tup build migration (Pete-greenlit)
- [i182](#i182) — `OPEN` — Bootable fetch-and-boot disk + real-Trinity run of client_fetch_boot
  - [i182b](#i182b) — `OPEN` — Real-Trinity run of client_fetch_boot via the fetch-and-boot disk
- [i204](#i204) — `OPEN` — i48c-b8e-4: overlayClassify + literalWord (overlay mirror + PC-invariance probe)
  - [i204b](#i204b) — `OPEN` — i48c-b8e-4b: overlay_classify + literal_word + compact_inst (Z80 port + boot self-test)
- [i215](#i215) — `OPEN` — IDE page-persistence (spill): lazy-spill cold DOC blocks to Trinity or floppy/tape when the pool is full
  - [i215c](#i215c) — `OPEN` — Spill manager: concrete pluggable backends (Trinity record / floppy-tape) + boot wiring
- [i231](#i231) — `OPEN` — Systemically eliminate NETBOOT_HOSTTEST==0 emulation carve-outs + CI guard against reintroduction (emulation-first enforcement)
  - [i231b](#i231b) — `OPEN` — Systematically eliminate the 34 existing NETBOOT_HOSTTEST==0 carve-outs (ratchet the i231a allowlist to zero)
    - [i231b-b4](#i231b-b4) — `OPEN` — Eliminate the harder carve-outs (post-i231b first pass), per file/cluster
      - [i231b-b4b](#i231b-b4b) — `OPEN` — Eliminate the remaining carve-outs in netboot_serve.asm (11 sites / 1 file — the hard core)
      - [i231b-b4h](#i231b-b4h) — `OPEN` — netboot_client.asm carve-outs eliminated (5 sites): collapse the flat HOSTTEST binary into the bootable; client_main/client_fetch_boot already harness-driven
- [i264](#i264) — `OPEN` — SAMBOOT selector firmware: hold-key-at-boot launches a record picker (choose boot record by number/name) instead of hardcoded record 3
  - [i264b](#i264b) — `OPEN` — Record picker UI (Increment 2): list available records, select by number/name, persist the SAMBOOT Config chunk
- [i319](#i319) — `OPEN` — Hardware-test our .mgt feature images on the real SAM (assembler/disassembler/editor/...) via the store+boot toolkit
  - [i319a](#i319a) — `OPEN` — Toolkit end-to-end hardware proof: list/push/boot/delete cycle on the real SAM with netboot_serve.mgt (network self-confirming)
    - [i319a-b2](#i319a-b2) — `OPEN` — boot leg: boot_record a stored record on real hardware + TFTP-confirm the booted serve disk
  - [i319b](#i319b) — `OPEN` — Per-feature-image hardware boots via the toolkit (assembler / disassembler / editor / demos)
