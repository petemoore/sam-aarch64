<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/items.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Item registry — open

Work items with status `OPEN` or `IN_PROGRESS`. Items with status `DONE` or
`WONTFIX` live in `item-registry-closed.md`.

## Id conventions

`i<N>` · `i<N><letter>` · `i<N><letter>-b<M>` · `i<N><letter>-b<M><letter>`
(base → sub-item → brick → brick-part). Ids are immutable once used in a PR,
branch, or commit; never reused. Sort order is true-numeric (i5 < i41 < i237).

## Controlled status vocabulary (open file)

| token | meaning |
|---|---|
| `OPEN` | not yet started; or started but no agent has it in-hand |
| `IN_PROGRESS` | actively being worked right now |

"Blocked" is not a status token — it is a *derived* property of the dependency
graph. An item with an unsatisfied `depends_on` edge is still `OPEN`; its gating
item/question ids are listed in the `deps` column (the edges of the dependency DAG).

## Atomic-item rule

One row = one deliverable. If a row bundles independently completable parts, split
it into letter sub-ids (`i81a`/`i81b`…) so no finished part hides inside an
unfinished row.

## Dependency model

`depends_on` edges declare what must finish before this item can proceed, and are
shown in the `deps` column. A target may be an item (must reach `DONE`) or a
question (must be answered and deleted). Edges form a DAG; the validator rejects
cycles and dangling targets. The DAG is built from the `depends_on` fields in
`registry/items.yaml`; `grep depends_on` there for the machine-readable edge list.
The `dependents` column shows the reverse edges (items that depend on this one).

## Column layout

The generated table has 7 columns: `id | item | status | PR | deps | dependents |
refs/links`. `PR`, `deps`, `dependents`, and `refs/links` are each independent
first-class columns. The generated `.md` files are views of `registry/items.yaml`
— do not edit them by hand (hand edits fail CI).

<!-- The table below is generated — do not edit it by hand. -->


| **id** | item | status | PR | deps | dependents | refs/links |
|---|---|---|---|---|---|---|
| **i2** | **On-SAM IDE memory model (edit buffer + IN/OUT paging; claim all free RAM, grow on demand)** — Scope row for the IDE memory model: the edit buffer and IN/OUT paging strategy. The design claim-all-free-RAM-grow-on-demand is the paged-buffer ceiling framing; full design deferred to editor work. | OPEN |  |  |  |  |
| **i3** | **Editor groundwork (Phase 2) — full vision** — Phase-2 editor full vision umbrella. The editor groundwork encompasses all Phase-2 editor sub-features (see i43–i47 and sub-items). Full scope in ROADMAP. | OPEN |  |  | i168 | docs/ROADMAP.md |
| **i4** | **Basic read-only listing/scroll viewer (centre-locked cursor; up/down only)** — SAM-side read-only viewer ported from the host prototype. The i76 P1 host viewer is the functional authority this SAM implementation will port. Carried forward by i76 Phase-1. | OPEN |  |  |  | i76, docs/specs/editor-tui-prototype-design.md §5 |
| **i5** | **UI visual prototyping via image generation (MODE 3 64x24 vs MODE 4 32x24 mockups)** — Mockup tooling for editor UI at SAM-accurate resolution. Carried forward by the i76 mockup backend. q1 resolved (2026-06-12): use a programmatic SAM-faithful renderer (PNG + SCREEN$ at exact mode geometry, real palette, correct pixel aspect) — generic AI image gen rejected; hand-authored retro art deferred to post-port. | OPEN |  |  |  | i76, docs/specs/editor-tui-prototype-design.md §2.4 |
| **i6** | **SAM screen-mode decision (MODE 3 vs 4, or user preference)** — Decide the default screen mode for the editor. To be settled empirically via i76 (geometry flag + font sheets + P3 memo). Design in editor-tui-prototype-design.md §3/§5 and ROADMAP Editor vision. | OPEN |  |  |  | i76, docs/specs/editor-tui-prototype-design.md §3, docs/specs/editor-tui-prototype-design.md §5, docs/ROADMAP.md |
| **i12** | **SimCoupé + test-harness tooling improvements** — Umbrella for SimCoupé emulator + editor-test-harness tooling improvements (version bumps, paste support, automated-editor input injection). Created 2026-06-20 (i153) so the i12a/b/c sub-items have their required umbrella parent — the children predate the parent-integrity rule. | OPEN |  |  |  | tools/Dockerfile.dev |
| **i12b** | **Editor-testing input injection (-keyin vs FLAGS/LASTK memory injection)** — New 2026-06-08. Determine and implement the right input-injection mechanism for automated editor tests in SimCoupé: the -keyin flag approach vs direct FLAGS/LASTK memory injection. | OPEN |  |  |  | tools/run-simcoupe.sh |
| **i16** | **Replace cls test instruction with a real spectrum4 one** — The cls instruction used in the test fixture is synthetic/placeholder; replace it with a real spectrum4 instruction. Tracked in the ROADMAP deferred checklist. (Note: id was tentatively i13 in conversation but locked as i16 to match shipped PR #107.) | OPEN |  |  |  | docs/ROADMAP.md |
| **i19** | **Subagent worktree-isolation leak — harden** — Harden subagent worktree isolation to prevent one subagent's filesystem changes from leaking into another's checkout. Scope row — specifics TBD. | OPEN |  |  |  |  |
| **i20** | **Linker-layout coupling (spectrum4.ld hardcoded in flatten)** — The flatten step has spectrum4.ld hardcoded; decouple the linker layout so other projects can use the toolchain without baking spectrum4-specific layout assumptions. Scope row. | OPEN |  |  |  |  |
| **i21** | **Go-harness fidelity follow-ups: make harness-sweep, USAGE.md, memory watchpoint** — Harness-sweep (one-shot build-all-deps + go test -count=1 ./...) and USAGE.md (full-suite run, stale-payload/test-cache gotchas, boot-self-test authoring patterns) DONE in PR #341. Remaining: memory watchpoint inspection feature — deferred until a debugging session needs it (agent-owned; add ad hoc per the README Evolving the harness). | OPEN |  |  |  |  |
| **i22** | **Per-fail-site diagnostic strings** — Add per-fail-site diagnostic strings to test failures so failures are more easily identified without a disassembler. Deferred-backlog item. | OPEN |  |  |  |  |
| **i23** | **Paged-IN HLOAD-ceiling lift (current ceiling: 96 KB = 6 pages 7..12, src/assembler.asm:49)** — The current IN-file ceiling is 96 KB (6 pages, 7–12, hardcoded src/assembler.asm line 49). Lift this ceiling to support larger source files via additional paged HLOAD slots. Deferred-backlog item. | OPEN |  |  |  |  |
| **i24** | **16-bit OUT_LEN / 32 KB-output limit (OUT_LEN: defw 0 src/main_loop.asm; 32768-byte OUT ceiling src/encoder.asm)** — The output length field is 16-bit and the output buffer ceiling is 32768 bytes (src/encoder.asm). Lift the OUT_LEN field and ceiling to support assemblies larger than 32 KB. Deferred-backlog item. | OPEN |  |  |  |  |
| **i25** | **(hksp) HSAVE/HLOAD error handler** — Add a proper error handler for HSAVE/HLOAD failures invoked via (hksp); currently errors are not surfaced to the user. Deferred-backlog item. | OPEN |  |  |  |  |
| **i27** | **Cortex-A53 errata workarounds (--fix-cortex-a53-*)** — Implement or wire errata workarounds for the Cortex-A53 (--fix-cortex-a53-* flags). Deferred-backlog item. | OPEN |  |  |  |  |
| **i29** | **Harden slot self-test PASS_PC dependency** — Deferred-backlog item. Hardens the slot self-test PASS_PC dependency to make it more robust. | OPEN |  |  |  |  |
| **i30** | **LDIR-fan-out for cross-page-shared blocks (smaller binary + faster boot)** — Deferred/possible future enhancement. Using LDIR fan-out for blocks shared across pages could reduce binary size and speed up boot. Noted in the strand-B PR4 disassembler plan under 'Future enhancements'. | OPEN |  |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-07-strand-b-pr4-z80-disassembler-port.md |
| **i31** | **On-SAM preprocessor (.if build-constraints + macros)** — Beyond-M7 / future idea. On-SAM preprocessor supporting .if build-constraints and macro expansion, reducing reliance on the host-side preprocess step. | OPEN |  |  |  |  |
| **i32** | **Multiple source files + staged/partial loading** — Beyond-M7 / future idea. Support for assembling from multiple source files with staged or partial loading, enabling larger projects than currently fit in a single-file workflow. | OPEN |  |  |  |  |
| **i33** | **Trinity mass-storage: leverage B-DOS record seam for staged/overlaid assembly (i23/i24 ceiling lift)** — Use the proven Trinity B-DOS storage seam (src/netboot/bdos_seam.asm; spanning records in src/netboot/fw_span.asm) for staged/overlaid assembly, lifting the i23 96 KB IN / i24 32 KB OUT ceilings. Storage-access half proven by netboot work. Assembler still uses plain HSAVE/HLOAD for single-file IN→OUT; this item extends that to mass-storage-backed staging. Rescoped 2026-06-17: netboot proved records are accessible + writable; remaining deliverable is the assembler-scale integration. | OPEN |  |  |  | memory/trinity_hardware.md, src/netboot/bdos_seam.asm, i23, i24 |
| **i39c** | **Phase 3 — bitfield-packing polish on the overlay slot bytes** — Bitfield-packing polish on the overlay slot bytes (design §3.1). Migrated to M9 at M8 close (2026-06-12). Folds into / absorbed into the i48c overlay-encoder work, specifically i48c-b7 (comments/blank-runs + overlay slot bitfield-packing brick). | OPEN |  |  |  | docs/specs/i48-syntactic-encoder-design.md §3.1, docs/notes/m9-status.md, i48c, i48c-b7 |
| **i41** | **Editor edit-buffer data structure — insertion performance for large source** — Architectural decision: the editor holds source in a separate insertion-friendly in-memory document model (paged block-list: block = page, intra-block gap buffer, record-id side-tables; worst-case edit ~30–80 ms at 400 KB) and serializes to .tbn on save/assemble. Unifies the editor document model with i48's IR (edit-model records = in-memory symbolic IR). Design agreed (opus, 2026-06-08), 5 key decisions in docs/specs/editor-edit-model-design.md §7. Decomposed into i41a–i41e. | OPEN |  |  |  | docs/specs/editor-edit-model-design.md §7, docs/notes/m9-status.md, i41a, i41b, i41c, i41d, i41e |
| **i41d** | **Editor edit-model — Z80 port of the paged block-list (on-SAM)** — Faithful Z80 port of the i41a Go authority (tools/sam-aarch64/editmodel), per CLAUDE.md §6. Scoped in design §8.1. Bricks 1+3+4a COMPLETE: insert/split/loc/goto (PR #387), delete/merge/free-list (PR #389), bounded ring-journal undo/redo (PR #391), EMDL serialize/load (PR #392), src/editmodel.asm, host-verified vs oracle. REMAINING: Brick 2 (paging onto i2 pool via OUT (251) — SimCoupé-gated), Brick 4b (real v2 .tbn path — couples to assembler build, deferred). | OPEN |  |  |  | src/editmodel.asm, tools/netboot-oracle/z80/editmodel_test.go, docs/specs/editor-edit-model-design.md §8.1/§4.2/§5, i41a |
| **i41e** | **Editor edit-model — i48-IR record payload + document symbol table + hybrid record model** — Replace i41a's raw-text line payload with the i48 in-memory symbolic IR (parsed format.Record): a document-global symbol table + name-id/record-id side-tables (§3), a hybrid committed-parsed / active-raw / invalid-error record type (§7.3), per-line text→IR entry. q24 resolved (2026-06-20): the editor validates ONE LINE AT A TIME — no whole-document import to serialize invalidly; the design point is how an in-progress/invalid LINE is held + round-tripped (whole-file import = i165). Open design (symbol-table lifecycle, hybrid sum type, per-line parse) — needs a design pass (§8/§5.2/§6/§7.3). | OPEN |  |  |  | docs/specs/editor-edit-model-design.md §8/§5.2/§6/§7.3, docs/specs/i48-syntactic-encoder-design.md, i41a, i41c, i48c |
| **i42** | **CRC / signature in the .tbn file header (integrity check)** — Deferred since M1. The v1 header has magic/version/flags but no integrity field. Add a CRC or signature field to the .tbn header for integrity checking. Referenced in the M1 status 'Known gaps' list. | OPEN |  |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/m1-status.md |
| **i43** | **Editor: register simulator with user-chosen seeds + replay-on-edit** — Phase-2 editor sub-feature. Register simulator with user-chosen seeds: step instructions, inject/randomise source regs, show dest regs/flags/memory, and replay-on-edit. Described in docs/ROADMAP.md 'Editor vision'. | OPEN |  |  |  | docs/ROADMAP.md |
| **i44** | **Editor: inline instruction-explanation panel (prose semantics, flags, regs, bit-field layout)** — Phase-2 editor sub-feature. Inline instruction-explanation panel showing prose semantics, flags set, regs read/written, and bit-field layout. Described in docs/ROADMAP.md 'Editor vision'. | OPEN |  |  |  | docs/ROADMAP.md |
| **i45** | **Editor: 'did you mean a simpler instruction?' result-equivalent rewrite hint** — Phase-2 editor sub-feature. Suggests result-equivalent but simpler instruction rewrites when a more concise encoding exists. Described in docs/ROADMAP.md 'Editor vision'. | OPEN |  |  |  | docs/ROADMAP.md |
| **i46** | **Editor: system-register documentation panel (ARM MRA subset + editorial overlay)** — Phase-2 editor sub-feature. System-register documentation panel surfaced from the cursor line, embedding an ARM MRA subset with editorial overlay. Described in docs/ROADMAP.md 'Editor vision'. | OPEN |  |  |  | docs/ROADMAP.md |
| **i47** | **Editor: inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations)** — Phase-2 editor sub-features. 'Why is this instruction here?' inverse-flow navigation plus retro UI affordances including chiptune audio, period fonts, and animations. Described in docs/ROADMAP.md 'Editor vision'. | OPEN |  |  |  | docs/ROADMAP.md |
| **i48** | **Single serialized format + pass-free syntactic encoder (the design)** — Design for single serialized format + pass-free syntactic encoder. Decision A: the compact overlay is the only serialized .tbn; the symbolic kinds (KindInst/LabelDef/LocalDef/LitInsts) become an in-memory IR, never written. Decision B: text->overlay is a pure syntactic transform; value-dependent bits computed in the fold at assemble time; forego GNU silent rewrites; narrow assemble-time mov->movz/orr/movn fallback. Host side COMPLETE (i48a #144, i48b, i48d). i48c (Z80 text->overlay encoder) is the M9 carry-forward. | OPEN |  |  |  | docs/specs/i48-syntactic-encoder-design.md, https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md, i48c |
| **i48c** | **Z80 text→overlay encoder (editor input path)** — SAM-side mirror of the host front-end (tools/sam-aarch64/frontend: lexer→parser→records). Go i48b is the authority. Absorbs i39c (overlay slot bitfield-packing polish). Large multi-PR port decomposed into bricks B1–B8 (scoped 2026-06-17). B1–B7 are flat-memory-autonomous (koron-go/z80 harness, netboot-z80 CI). B8 is assembler-coupled, lands last. Host Preprocess (.include/.macro/.if) is out of scope (host-only bulk-import concern). | OPEN |  |  |  | src/asmlex.asm, src/asmparse.asm, tools/netboot-oracle/z80/asmlex_test.go, tools/netboot-oracle/z80/asmparse_test.go, tools/sam-aarch64/frontend/lexer.go, tools/sam-aarch64/frontend/parser.go, docs/specs/i48-syntactic-encoder-design.md §2, docs/specs/i48-syntactic-encoder-design.md §7, docs/notes/m9-status.md, i39c, i109 |
| **i48c-b5b** | **movl pseudo (movz+movk expansion / :abs_g0_nc:+:abs_g1:)** — Implement the movl pseudo-instruction: expands to movz+movk pair; handles :abs_g0_nc: and :abs_g1: reloc operands. | OPEN |  |  |  | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b5c** | **Barriers dsb/dmb/isb (barrier-arg keyword→CRm)** — Parse barrier instructions dsb/dmb/isb: map barrier-arg keyword (e.g. sy, ish, nsh, ...) to the CRm encoding field. | OPEN |  |  |  | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b5d** | **mrs/msr — sysreg/PSTATE name→OpSysName** — Parse mrs/msr instructions: map sysreg and PSTATE register name strings to the OpSysName encoding (name lookup table). | OPEN |  |  |  | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b5e** | **dc/tlbi — op-name + optional Xt** — Parse dc/tlbi instructions: map operation-name string to encoding + accept optional Xt register operand. | OPEN |  |  |  | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b5f** | **ldr = literal-pool pseudo (OpLitPool)** — Parse ldr= pseudo-instruction: captures the pool-value expression into an OpLitPool operand for later literal-pool placement and PC-relative load emission. | OPEN |  |  |  | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b6** | **Directives parser** — Parse assembler directives (.set, .balign, .org, .skip, .ltorg, .inst, .section, etc.) into their respective overlay record types via src/asmparse.asm. | OPEN |  |  |  | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b7** | **Comments/blank-runs + i39c overlay slot bitfield-packing** — Parse comment and blank-run record types. Absorbs i39c: implement the overlay slot bitfield-packing polish (design §3.1) as part of the parser completion. | OPEN |  |  |  | src/asmparse.asm, docs/specs/i48-syntactic-encoder-design.md §3.1, i39c |
| **i48c-b8** | **Integration/fixture round-trip (assembler-coupled, lands last)** — Wire the completed text→overlay encoder into the full assembler path and verify end-to-end fixture round-trips. Assembler-coupled (like i41d Brick 4b); depends on B1–B7 being complete. Lands last in the i48c sequence. | OPEN |  |  |  | src/assembler.asm, src/asmparse.asm |
| **i50** | **Host Go-tool CLI shape — deferred design question (one binary vs three vs pandoc-style)** — Deferred design question (Pete 2026-06-09): whether host tools stay one format-aware front door (sam-aarch64, auto-detects SA64 magic), revert to three binaries (encode/decode/translate), or adopt pandoc-style --from/--to. Three concerns are already cleanly separated as libraries (aarch64enc / aarch64dec / sam-aarch64-format+frontend); CLI count is a thin presentation choice, orthogonal to the i48 in-memory-only-symbolic-form win. Revisit when bulk GNU→.tbn import and listings host features land. Go tools are Z80-port scaffolding + oracle (memory/project_go_tools_are_z80_scaffolding.md). | OPEN |  |  |  |  |
| **i55** | **Multi-section / linker-script honouring in refenc** — Explicitly punted in favour of the text2bin -flatten approach. Multi-section / linker-script handling in refenc would only be needed if a non-spectrum4-shaped project surfaces. Deferred; from ROADMAP deferred checklist (retired in i52 PR 3). | OPEN |  |  |  |  |
| **i56** | **SpectrumFourLayout extraction (-layout flag or linker-script parser)** — Not worth doing unless a second project surfaces. SpectrumFourLayout extraction via -layout flag or linker-script parser is a Pete-gated call; deferred. From ROADMAP deferred checklist (retired in i52 PR 3). | OPEN |  |  |  |  |
| **i65** | **SAM-side save-without-comments option (editor phase)** — On-SAM counterpart to the host -strip-comments/-thin-comments flags. Saves a .tbn with the editor region stripped/thinned so a 256 KB SAM can assemble sources authored on a 512 KB machine. q9 resolved (Pete 2026-06-10): the tool targets both 256 and 512 KB SAMs; the full unstripped release .tbn is allowed to exceed 256 KB, but a 256 KB user must still be able to assemble via this stripped disk handoff. Host equivalent already exists. | OPEN |  |  |  | i59 |
| **i66** | **Trinity page-persistence as a general persist-to-mass-storage technique** — Persist SAM pages to Trinity B-DOS records as a general storage technique. Deferred by Pete's decision (2026-06-10, q9 resolved): 'no value implementing right now'; revisit only if a concrete need appears. Conditional on i66's reactivation being Pete's call. | OPEN |  |  |  | docs/notes/bdos-version-landscape.md |
| **i69** | **Test-memory architecture: relieve section-C test-variant squeeze using test-coexistence principle** — Relieve section-C test-variant squeeze (principle: tests need not coexist in memory). Levers in order: (1) evict suites off-axis (PR #52 pattern); (2) serial test bay (page-13 time-multiplex generalised); (3) chunked payloads across more pages/disk; (4) multiple boots. Lever 1 partially applied (PR #180): test_symbols + test_local_labels evicted; test variant code_end &BFAA→&BCA3 (86B→861B headroom). Remaining levers ready for next pressure event. | OPEN |  |  |  | i68, src/README.md |
| **i70** | **Phase-3 firmware self-provisioning: SAM fetches Pi firmware blobs via HTTP and persists to Trinity storage** — SAM IP stack fetches Pi firmware (several MB) from cdn.githubraw.com over HTTP (q11 resolved: on-SAM HTTP via uIP, host-tool-free; q12 resolved: Model A only). Full host-verifiable HTTP-fetch path complete + host-verified across Bricks 1–6 (#245/#247/#254/#256/#257/#259 netboot_http.asm). Remaining: hardware-gated (EEPROM read + section-C paging + HSAVE RST-8 dispatch on real Trinity; emulation-verified != hardware-verified, CLAUDE.md §5). i82 TFTP client bridges provisioning meanwhile; HTTPS stretch is i88. | OPEN |  | i133 |  | docs/specs/phase3-delivery-design.md, docs/notes/trinity-capabilities.md, docs/notes/bdos-version-landscape.md, src/netboot/netboot_http.asm, docs/notes/netboot-trinity-testing.md, i61, i66, i84, i88 |
| **i73** | **i17 review residuals — LOW-severity hardening and dead-code findings** — Low-severity hardening items split from i17 (PR #185). Landed: L1 emit_byte off-by-one (PR #338), L6 .align>=16 silent no-op (PR #339), L7 pass-1 backward-.org check missing (PR #340), L9 retired symbolic-encoder path removed (PR #371, q20 sweep). Open: L3 symbol_abs_bit_ptr missing id<512 bound; L4 exactly-full-buffer conservative rejections; L5 zero-length LIT_INSTS BC underflow; L10 u8 segment-counter wrap; L11 load_in_file editor-offset wrap; L12 .set redefinition divergence. Follow-up i109 (vestigial soft-reject hook). | OPEN |  |  |  | PR #185, i17 |
| **i74** | **i7 phase D — at/ic/barrier operand-table codegen from the Go authority** — Codegen the at/ic/barrier operand tables from the Go authority (spec §6 Q3). Deferred by sequencing only (Pete 2026-06-12: tracked so it automatically gets done); queue after i7 phases A–C land. Gated on the aarch64dec/sys.go code→data refactor (i7 phase D prerequisite). | OPEN |  |  |  | docs/specs/codegen-tables-design.md, i7 |
| **i77** | **spectrum4 source succinctification — comments + pathological constant expressions** — Agent-driven, section-by-section, human-reviewed (Pete 2026-06-12): (a) comments rewritten aggressively succinct but still readable; (b) pathological constant expressions replaced with named equates (e.g. the longest instruction in the corpus: `movk w0, (65536 \| (((BORDER_COLOUR >> 16) & 65535) & 65535)), lsl #16`). Coordinate with upstream spectrum4 (content originates there; release.s re-flattens). Ties to editor-tui spec D1 (all comments <256B → u16 record length can revert to u8). Parked (editor era). | OPEN |  |  |  | docs/specs/editor-tui-prototype-design.md, i76 |
| **i81** | **Upstream SimCoupé issue triage — review open issues and assist where we can** — Umbrella for SimCoupé upstream community contributions. Triage complete (2026-06-13): produced contributions i81a (PNG-endian fix PR — BLOCKED:Pete), i81b (issue #28 comment — DONE PR #370), i81c (SDL headless fallback — BLOCKED:Pete). i81a and i81c remain open; i81b is done. | OPEN |  |  |  | tools/font-proof/simcoupe-local-capture.patch, i80 |
| **i81a** | **PNG-endian one-line upstream SimCoupé PR: the __aarch64__ little-endian-guard fix in Base/SimCoupe.h** — hunk 2 of tools/font-proof/simcoupe-local-capture.patch; confirmed NOT in upstream HEAD; ready to offer standalone; community submission is Pete's call. | OPEN |  |  |  | tools/font-proof/simcoupe-local-capture.patch, i81, i80 |
| **i81c** | **Optionally offer the SDL dummy-driver headless renderer fallback upstream** — Lower priority than i81a; community submission is Pete's call. | OPEN |  |  |  | i81, tools/font-proof/simcoupe-local-capture.patch |
| **i87** | **Dump + reverse-engineer Pete's patched SAM ROM (Phase-3)** — Pete's SAM runs a modified system ROM giving Trinity-interface access at power-on (no B-DOS floppy needed). When his SAM is next online he dumps the ROM to disk; we then diff it against the stock SAM ROM to document exactly how Trinity-at-boot is patched in, so the boot mechanism is reproducible/understood. Not a blocker — trinload can net-load the client/server in the interim, and trinity.mgt + reference/bdos cover the interface. Hardware-gated: artifact production needs Pete's hardware. | OPEN |  |  |  | docs/specs/phase3-delivery-design.md §3, i84 |
| **i87a** | **SAMBOOT: run the one-shot dumper on the SAM to capture rom.bin + eeprom.bin (hardware)** — Push the one-shot dumper onto the SAM via trinload and tftp-get rom.bin + eeprom.bin to the Pi/Mac. Hardware (Pete). Captures the artifacts that unblock the offline analysis; the EEPROM capture is the mandatory pre-flash backup. | OPEN |  | i173 | i87b | docs/specs/samboot.md |
| **i87b** | **SAMBOOT: document the SAM boot chain from the captured ROM/EEPROM + public source** — Diff rom.bin vs stock SAM ROM 3.0, confirm the EEPROM bootblock matches the public reproduction, and document the chip->bootblock->B-DOS handoff + where the injection hook attaches. | OPEN |  | i87a, i135a |  | docs/specs/samboot.md |
| **i88** | **On-SAM HTTPS/TLS-1.3 client to fetch Pi firmware directly from GitHub** — Phase-3 stretch, LOWEST priority (Pete: only when nothing else is left). TLS-crypto-only: github.com-only, pinned public key, no CA store (content is SHA-256-pinned so cert verification is skipped). Decomposed into i88a (6a host-verifiable state machine), i88b (6b record-reassembler), and i88c (6b hardware-gated integration). | OPEN |  |  |  | docs/specs/phase3-delivery-design.md, src/netboot/tls_client.asm, tools/netboot-oracle/tls/, docs/specs/tls13-handshake.md, i70, i84, i100 |
| **i88c** | **i88 6b — hardware-gated integration: wire tls_reasm into tcp_conn, paging layout, bootable disk, real RST 8 on Trinity** — Wire tls_reasm_feed into tcp_conn CONN_SINK_FILTER; finalise paging layout; build a bootable disk; exercise over real RST 8 on Trinity hardware. Prerequisite: TC_RX must grow from 1056 to 16645 B (TLS max) before a real Certificate record fits. Hardware-gated (CLAUDE.md §5). | OPEN |  | i133 |  | src/netboot/tls_reasm.asm, src/netboot/tls_client.asm, src/netboot/tcp_conn.asm, i88, i88b |
| **i89** | **Capture a Pi 3 netboot exchange (Phase-3) — extend i83/i86 oracle to the older Pi family** — Confirm Pi 3 boot ROM's DHCP behaviour (may differ from Pi 4 PXE option-43 dance) and its TFTP file set (bootcode.bin/start.elf/fixup.dat). Pete runs on real hardware (hub + Wireshark, trinload technique) when convenient — not a blocker (Pi 3 file set already known from his tftproot); agent provides setup steps at the time. | OPEN |  |  |  | docs/notes/pi-netboot-capture-analysis.md §4, i83, i86, i84 |
| **i90** | **%nobits/BSS section bodies dropped by -flatten — source-structure loss** — When source is flattened (-flatten, the release path), the entire body of every %nobits/BSS section (bss_kernel/bss_roms/…) is discarded: contributes zero bytes, so blank lines/comments inside BSS don't round-trip (release.s: 132 of 1472 blank lines, all in NOBITS sections). Pre-existing flatten property, not an i78 bug — i78 blank/comment preservation is exact for PROGBITS. Only matters once the SAM editor edits a flattened source with BSS content. Captured only — decide whether to preserve NOBITS editor content if/when the flattened-edit path needs it. | OPEN |  |  |  | docs/specs/source-structure-preservation-design.md, tools/sam-aarch64/frontend/flatten.go, i78 |
| **i93** | **Phase-3 netboot B-DOS storage seam (bdos_seam.asm) — name-to-record glue** — Name→record glue shared by i83 server and i82 client. Three routines from Go authority bdos/bdos.go, host-verified: bdos_name_to_uifa (name→48-byte UIFA), bdos_difa_to_size (DIFA→32-bit size for OACK tsize), bdos_fill_save_uifa (name+page+offset+size→HSAVE UIFA). Field conventions from src/io.asm/loader.asm/main_loop.asm + samdos-file-io.md. Seam arithmetic LANDED + host-verified. Remaining: RST 8 hook dispatch (HGTHD/HLOAD/HSAVE/HRECORD) stays unverified until real Trinity (hardware-gated, CLAUDE.md §5). | OPEN |  |  |  | src/netboot/bdos_seam.asm, tools/netboot-oracle/bdos/, docs/specs/samdos-file-io.md, docs/notes/bdos-version-landscape.md, i82, i83, i62, i71 |
| **i93b** | **B-DOS RST 8 hook dispatch on real Trinity — hardware-gated (HGTHD/HLOAD/HSAVE/HRECORD)** | OPEN |  | i133 | i95b |  |
| **i95** | **Phase-3 netboot integrated server — single dispatch loop (ARP+DHCP+TFTP)** — Headline Phase-3 program: SAM+Trinity netboots a Pi by answering ARP+DHCP+TFTP in one loop, serving boot files by name from the flat store. Go authority + Z80 netboot_serve_once LANDED + host-verified: full DISCOVER→OFFER→REQUEST→ACK→ARP→RRQ→OACK→DATA session vs Go byte-for-byte. Bootable disk shipped. Hardware first-light (2026-06-18): ARP+TFTP sub-responders real-Trinity-verified (i96). Remaining: DHCP server + B-DOS store provisioning (i93) + Pi-boot gated on real Trinity. | OPEN |  |  |  | tools/netboot-oracle/server/server.go, src/netboot/netboot_server.asm, docs/plans/phase3-netboot-implementation-plan.md, docs/notes/pi-netboot-capture-analysis.md, docs/notes/netboot-trinity-testing.md, i80, i83, i86, i93, i94 |
| **i95b** | **End-to-end Pi boot on real Trinity — hardware-gated: DHCP + B-DOS provisioning + real Pi boot** | OPEN |  | i93b |  |  |
| **i100** | **Firmware-download provisioning UX — user-selectable revision and file subset** — On-SAM multi-file firmware downloader with user-selectable revision pin and file subset. Host-verified slice complete as i100a; bootable migration + real HSAVE store leaf complete as i100b (closed). Remaining: i100c picker UX (Pete-gated interface design). | OPEN |  |  |  | src/netboot/fw_source.asm, src/netboot/http_main.asm, docs/specs/phase3-delivery-design.md, i100a, i100b, i100c |
| **i100c** | **Picker UX — user-facing revision/file-selection interface for the firmware downloader** — User-facing revision/file-selection interface for the firmware downloader. Interface design is Pete's call (editor-era UX, no autonomous decision here). No specific qN gates it — bare Pete decision. | OPEN |  |  |  | docs/specs/phase3-delivery-design.md, i100 |
| **i101** | **End-to-end deliverable: fetch RPi firmware → assemble spectrum4 release on SAM → TFTP-serve a full Pi 400 netboot** — The integrated SAM IDE on ONE bootable disk — editor + assembler/disassembler + TFTP server — an interactive USER-DRIVEN program (q23 resolved 2026-06-20: NOT a boot→fetch→assemble→serve auto-sequence). User loads it, starts/edits a project, assembles release.bin/kernel8.img from release.tbn (M6 byte-match), and TFTP-serves so a Pi 400 netboots — features exercised on demand. The three org-&8000 layers coexist via paging (implementation detail), not sequential overlays. NOT SimCoupé-testable (no Trinity ENC/SD); real E2E is Pete's hardware gate. Arch: docs/specs/phase3-delivery-design.md §13. | OPEN |  |  |  | docs/specs/phase3-delivery-design.md §13, i100, i70, i83, i86, i99, i80 |
| **i102** | **Aggressive T-state optimization of the netboot crypto / TLS-1.3 cipher-suite Z80** — Drive down T-states and code size of host-verified crypto leaves (sha256, hmac_sha256, hkdf, chacha20, poly1305, x25519, aead) using full Z80 opcode set including undocumented instructions; bit-exact reproducibility preserved (byte-for-byte host tests are the guardrail). Craft/fun exercise, no deadline. Landed as sub-items i102a–i102l + i102n–i102r (all DONE, closed registry): x25519 cumulative -50.4% (1.604G→0.796G), SHA-256 -56.8% (729K→315K T/block, fastest verified Z80 SHA-256). Remaining: i102m (sha256 throughput umbrella — sub-items mostly done, one small lever left). | OPEN |  |  |  | i88, src/netboot/, tools/netboot-oracle/z80/ |
| **i102m** | **i102 sha256 throughput rewrite for the firmware-verify path** — Increments i102n-i102r landed (closed registry): sha256 729,795->315,355 T/block (-56.8%) — fastest verified Z80 SHA-256. Decomposed into i102n–i102r (all DONE) + i102s (message-schedule extend loop lever, OPEN). Low priority (world-best already, no deadline). | OPEN |  |  |  | src/netboot/sha256.asm, tools/sha256-unroll-gen/, src/netboot/tcp_conn.asm, i100, i101, i102 |
| **i102s** | **i102 sha256 message-schedule extend loop — BCDE accumulate in add4_body (~2% gain)** — The message-schedule extend loop still does 3 term adds memory-to-memory (add4_body). Applying the BCDE register-accumulate pattern used in the round path to add4_body eliminates the write-back round-trips, netting an estimated ~2% further reduction. One clean lever remaining after i102r. Low priority (world-best already, no deadline). | OPEN |  |  |  | src/netboot/sha256.asm, i102m, i102r |
| **i103** | **Autonomous-loop monitor — activity-gated quiescence** — When non-Pete backlog is drained, the monitor keeps waking an idle session. Fix: agent writes a quiescence semaphore on confirming a drain; monitor stops nudging while the file exists AND session transcript has not grown past the recorded mark. The instant the transcript grows (Pete writes back or any new turn) the monitor deletes the file and resumes. Auto-expires on first sign of life — no manual restart, no permanently-deaf loop. Record the transcript mark when monitor first sees the file (not file mtime) to avoid spurious re-wake. Builds on i97. | OPEN |  |  |  | i97, tools/autonomous-loop/monitor.sh |
| **i104** | **Per-session retro reports** — At wind-down each session writes a short blameless retrospective to ~/claude-retro/ on genuine process/workflow friction, written independently (not reading other sessions' reports first) so converging signal across sessions is real, not echoed. Rule: say what genuinely happened; 'nothing notable' is a valid report; each friction grounded in a specific incident. Reviewed by hand every few sessions / at milestone close -> recurring frictions become actionable changes (CLAUDE.md / memory / qN). Distinct from the diary (~/claude-diary/). Practice started 2026-06-15. | OPEN |  |  |  | ~/claude-retro/README.md, docs/ROADMAP.md |
| **i106** | **SAM-as-terminal: SSH (or telnet) client to Pete's Mac over the physical LAN** — KEX-stub SSH the sweet spot: send constant X25519 basepoint as client ephemeral pubkey; use server's received pubkey as shared secret — zero EC math, instant connect, interoperates with stock OpenSSH sshd. Skip host-key verification (drops Ed25519+SHA-512). Per-byte: ChaCha20-Poly1305 ~few hundred B/s, usable for SAM MODE 3 64x24. Secure-mode toggle (real X25519, ~10-min) nearly free via existing i88 X25519. Telnet fallback: minimal SAM code but needs Homebrew inetutils on Mac. Reuses i88 crypto + i70 tcp_conn. | OPEN |  |  |  | i88, i70, docs/specs/tls13-handshake.md §Prior art |
| **i109** | **Collapse the now-unarmed encode_logical_imm soft-reject hook** — PR #371 (q20/i73-L9 sweep) deleted encode_logical_imm_try, the only caller that ever armed encode_logical_imm_soft; with no armer, encode_logical_imm_reject always jp fails. The soft branch + encode_logical_imm_soft/encode_logical_imm_rejected flags are vestigial. Collapsing back to a plain jp fail + dropping the two equates is behaviourally identical (flag never set) -> release.bin byte-identical. LOW priority (touches v2-production code). The hook may be re-armed by the future i48c editor encoder — decide when i48c's encoder is designed. | OPEN |  |  |  | src/slots/logical_imm.asm, i73, i48c |
| **i111** | **Memory-read coverage / generic dead-code finder** — Instrument the koron-go/z80 harness to record every byte read (opcode fetch + data load) while running the whole test corpus, then report bytes inside a build's code/data image never touched. Each unread region is a candidate: either genuinely dead (delete, reclaim code window) or a coverage gap (add a fixture). Phase 1 (flat payloads, paging-free): hook mac.Read/opcode-fetch + use .map symbol ranges. Phase 2 (paged binary): trace must record (physical page, offset) and hook the paging port. Exclude known-acceptable-unread ranges via MARKER SYMBOLS from the pyz80 .map — drift-proof. | OPEN |  |  |  | tools/z80-test-harness-go/, i109, i73 |
| **i112** | **Restore the SAM opening screen that Colin's Trinity boot-ROM patch tramples** — Colin's custom ROM boots straight from Trinity (no B-DOS floppy) but overwrites the normal power-on opening screen. Restore by appending code to end of B-DOS (Trinity flash) that redraws the opening screen. The screen is not a clean re-callable subroutine — it is the inline RAINBOW SCREEN block at ROM &ED1B: set MODE 3, init PALTAB, build LINICOLS rainbow table, print MGT banner. Restoration = reproduce that draw in appended Z80, not call a routine. Touches non-redistributable B-DOS 1.5t Trinity fork; modifying Colin's B-DOS may warrant his input (Pete's call). | OPEN |  | i135a |  | docs/sam/sam-coupe_rom-v3.0_annotated-disassembly.txt, i87, ~/sam-archive/bdos/analysis/ |
| **i113** | **Scrape, QA, and integrate the recovered Trinity documentation** — Pete recovered the full Trinity docs as 51 photographs (closes i61 gap: Quazar manual NOT found). Off-repo workspace ~/sam-archive/trinity-docs/ (third-party copyrighted; preservation-only). Three immutable layers: photos/ -> text/ (raw OCR via ocr_docs.py, Tesseract + rotation auto-detect) -> docs/notes/trinity-capabilities.md (verified facts only). Pipeline: OCR -> QA agent (prose errors + data-point inventory; wrong digits must be photo-verified) -> discovery agent (what's new vs current knowledge) -> photo-verify flagged data -> fold only photo-confirmed facts into trinity-capabilities.md. | OPEN |  |  |  | ~/sam-archive/trinity-docs/README.md, docs/notes/trinity-capabilities.md, i61, i80, i87 |
| **i113b** | **photo-verify flagged data + fold verified facts into trinity-capabilities.md** — QA report flags specific data points (port addresses, EEPROM offsets, hex values, diagrams) that require photo confirmation before committing to the repo. Deliverable: run vision-verify on flagged items, then fold only confirmed facts into docs/notes/trinity-capabilities.md via the established off-repo pipeline. | OPEN |  |  |  | ~/sam-archive/trinity-docs/VERIFY_CHECKLIST.md, docs/notes/trinity-capabilities.md |
| **i114** | **Netboot serve manifest + record-allocation strategy** — A manifest on the booted record maps TFTP names/paths -> B-DOS records, decoupling the served namespace from B-DOS 10-char flat filename limits. Entries carry {locator(s), span count, size, optional SHA-256}. Storage-allocation strategy in manifest header (first-free / highest-free / fixed-list; never silently steal a record). Multi-project use with shared-files-by-reference. Sub-choices settled: encoding=text, locator key=number+name, span naming=hash-derived, default=highest-free. Storage-class: trinity-sam-disks/ prefix = validated raw-record disk. Design at §6.5. | OPEN |  |  |  | docs/specs/netboot-storage-manifest-design.md §6.5, i93, i95, i99, i70, i100, i101, i119, i122 |
| **i114a** | **Serve manifest (Go authority): text format + parser + manifest-backed name→record resolve** — design §1-3: a human-editable line-based text manifest, read once from the boot record, mapping full Pi-facing TFTP names/paths -> internal B-DOS record locator(s) + span count + total size + optional SHA-256, with local vs remote entry kinds. Go authority: define the format, write the parser, back the tftp.Store/Resolve path so exact-match resolve returns record-list+size into the existing bdos.SpanPlan path. Host-verifiable, no hardware gate; Z80 port is a follow-on. | OPEN |  |  |  | docs/specs/netboot-storage-manifest-design.md, tools/netboot-oracle/tftp/server.go |
| **i114b** | **Netboot storage-allocation strategy: header policy (first-free / fixed-list / highest-free)** — design §4: storage-allocation policy in the manifest header (front section), user-chosen, default highest-free. s1 first-free (pack already-claimed records then lowest free), s2 fixed-list (overflow WARNS, never steals an unlisted record), s3 highest-free (keep low slots for the user's own disks). Invariants: overflow is an explicit warning, never a silent record-steal; the header records whether the manifest's own disk is in the pool; resume reuses already-claimed space. Go authority over a modelled store; host-verifiable. | OPEN |  |  |  | docs/specs/netboot-storage-manifest-design.md §4 |
| **i116** | **Harness measurement / byte-match tests must assert the build succeeded — no stale-.bin reads** — A failed pyz80/make build leaves the previous .bin in place (or a timestamp-keyed target no-ops after a source restore), so a T-state or byte-identical check reads the stale artifact and reports a false pass. Bit the project 4 sessions: sha256-fastest-z80, i88-tls-go-authority-arc, i48c-expr-parser-bricks, and a timestamp-keyed variant. Fix is structural (the i21 USAGE.md doc note did not take): make harness measurement/oracle tests depend on the .bin build target (or assert build exit-0 / a freshness check) so they cannot run against a stale artifact. | OPEN |  |  |  | tools/z80-test-harness-go/, i104, i21 |
| **i117** | **Extract sam-aarch64 learnings into a reusable open-source autonomous Claude project framework** — Starter framework/template so a new project inherits: autonomous-loop monitor (i97), split iN/qN registries + controlled-status discipline (i115 validator), §3 pre-merge review gate, per-session retro (i104), prime-directive verification ethos, brick + Go-authority-oracle loop, delegate-to-subagents context discipline, and doc-lifecycle + single-source-of-truth rules. Must be self-contained (portable out of Pete's ~/.claude/CLAUDE.md). Delivery options: template repo / init scaffold or Claude Code plugin. Scope + sequencing + packaging are Pete's call; do not auto-pick-up. | OPEN |  |  |  | i97, i104, i115 |
| **i117a** | **Prior-art survey: existing Claude Code autonomous-orchestration plugins/frameworks vs i117** — Preliminary 2026-06-18 scan: metaswarm, ccswarm, ruflo, claude-plan-orchestrator, autonomous-dev, closedloop-ai, barkain, Composio AO + Claude Code Agent Teams; plus awesome-agent-orchestrators, awesome-cli-coding-agents. Most emphasise PARALLELISM. i117's model differs: single serial orchestrator + deterministic non-LLM monitor (i97) + prime-directive culture + blameless retro + iN/qN registry backbone. Deliverable: feature-by-feature comparison to confirm differentiation and whether gap justifies building i117. Research-first; cite each tool. Pete's call; do not auto-pick-up. | OPEN |  |  |  | i117 |
| **i118** | **i82 TFTP client — OACK handling / standard-TFTP-server interop** — The i82 client requests RRQ options (blksize/tsize/timeout/windowsize) but has no OACK (opcode 6) handling. A standards-compliant server (e.g. macOS tftpd) answers an optioned RRQ with OACK before DATA; the client ignores it, never ACKs block 0, and hangs. Fix: on OACK -> learn server endpoint + adopt negotiated blksize (512 default if absent) + send ACK block 0; on direct DATA -> 512 default (today's hard-coded 1428 truncates); drop windowsize from request. Mirrored in Go authority + host tests. PR #422 landed the Go/Z80/host side; hardware test pending. | OPEN |  |  |  | tools/netboot-oracle/tftp/tftp.go, tools/netboot-oracle/tftp/clientloop.go, tools/netboot-oracle/client/client.go, src/netboot/netboot_client.asm, src/netboot/tftp_client_front.asm, i82, i119, i120 |
| **i118b** | **hardware verification — OACK path on real Trinity hardware** — Confirm the OACK path works on real Trinity hardware: netboot client connects to a standards-compliant TFTP server (e.g. macOS tftpd), receives OACK, ACKs block 0, and receives the file. Gated on Trinity hardware access + a standards-compliant server in the test environment. | OPEN |  | i133 |  | i119, i82 |
| **i119** | **i82 TFTP client — persistence write-out on real hardware** — Wire the i82 client's B-DOS persistence write-out; deferred by Pete (first test = receive only; persistence later). Gaps: (1) record-0-selection bug — client_main selects record 0 = floppy; (2) HRECORD-stamp question — whether a build-disk .mgt record is HRECORD-selectable; (3) large-file staging via the i99 fw_span port; (4) filename >10-char UIFA truncation. Free-record detection reads the on-card list directly per docs/specs/trinity-record-detection-design.md (Trinity SD is SHARED — show-name + confirm-before-overwrite mandatory). HSAVE write-out + raw-SD list read stay hardware-gated. | OPEN |  |  |  | src/netboot/netboot_client.asm, src/netboot/bdos_seam.asm, docs/specs/trinity-record-detection-design.md, docs/specs/samdos-file-io.md, i82, i93, i99, i114, i134 |
| **i120** | **Windowed TFTP (RFC 7440) for the netboot client/server — throughput optimization** — i118 drops windowsize from the client RRQ to stay a correct lock-step (one ACK per block) client. That costs round-trips on large transfers (an 800 KB .mgt at blksize 1428 ~574 lock-step round-trips over the SAM's slow ~20-80 KB/s SPI stack). Windowed delivery (window 4 -> ~4x fewer round-trips) is the throughput win. Requires the receiver to ACK only the last block of each window (+ handle lost-block/out-of-window rewind) on both client (i82) and server (i83/i95); re-add windowsize to ClientOptionSet only once windowed logic exists on both sides + is host-verified. | OPEN |  |  |  | tools/netboot-oracle/tftp/, src/netboot/netboot_client.asm, docs/notes/tftp-protocol-research.md, i118, i82, i83, i95 |
| **i121** | **SAM TFTP server — WRQ (write / accept-in) support: push disk images to the SAM, auto-slotted** — PRIMARY netboot use case: boot SAM, 'tftp put' a disk image from the Mac, SAM writes it to a free Trinity record, boot+test. Combines with the i83/i95 serve-out server into one program (RRQ+WRQ). q30 resolved (2026-06-20): auto-pick is a DISK CONFIG — at launch the program reads it; if absent it PROMPTS (auto-pick a free record? y/n); test/shipped disks ship that config allowing any free record, so unattended push works with a pre-configured disk + never overwrites a named record. Reuses i82 receive loop + i119 write-out; adds WRQ parse + OACK/ACK-0 + free-slot alloc (i114). | OPEN |  |  |  | src/netboot/, tools/netboot-oracle/tftp/, i82, i83, i95, i114, i119, i70 |
| **i121b** | **WRQ receive-to-staging (DATA->ACK loop)** — Wire tftp_recv_data (i82, tftp_client_loop.asm) into the WRQ transfer: DATA -> ACK each -> accumulate into STAGING -> short-block end. Resolve the SERVER_*/CLIENT_* var-name collision between tftp_client_loop.asm and netboot_serve.asm. Go authority: serverloop.go receive path. Host test: inject WRQ+DATA1..N, assert an ACK per block + STAGING holds the file. | OPEN |  | i121a | i121c |  |
| **i121c** | **WRQ auto-slot + write-out (write-to-free-only)** — Final block -> bdos_find_free_record (i119); none free -> reply TFTP ERROR(3,'no free record'); else select + bdos_fill_save_uifa (name from WRQ filename) + bdos_save_hook. q30 RESOLVED (write-to-free-only via disk-config auto-pick; prompt if config absent) so ungated. E2E mirrors TestClientE2EConfirm: assert HSAVE in the free record + right bytes; all-full -> ERROR(3) + zero HSAVEs. rst-8 HSAVE persist stays hardware-gated. | OPEN |  | i121b | i121d |  |
| **i121d** | **Combined RRQ+WRQ bootable program + disk/Makefile/CI wiring** — Fold WRQ into the RRQ serve-out program (netboot_serve.asm) so one bootable program handles both; wire into the disk image, Makefile, and CI. The real Mac->SAM 'tftp put' over the wire is the hardware final gate. | OPEN |  | i121c |  |  |
| **i122** | **SAM fetch-and-boot (PXE-style): TFTP-client-fetch a disk image and boot it directly** — SAM acts like a netbooting Pi: on boot, TFTP-fetches a named disk image, writes to a scratch/boot record, and boots into it (no control return). Design notes: (a) use reusable scratch record (no cleanup post-boot); (b) sha256-dedup before fetch — if content hash already in storage, skip fetch and boot existing copy (idempotent). Reuses i114/q26 content-addressed record naming. Builds on i82 receive loop + i119 write-out + a boot-a-record primitive (Trinity RECORD-then-BOOT model). | OPEN |  | i114c, i114d | i133 | src/netboot/netboot_client.asm, docs/notes/netboot-trinity-testing.md, i82, i114, i119 |
| **i122b** | **Raw-record streaming write: i99 streaming sink -> HWSAD into one 819200-byte record** — i122 brick 2: wire the i99 streaming sink to flush a fetched disk image into one raw Trinity record via i114c's HWSAD seam (bdos_write_record). The 819200-byte image far exceeds one STAGING buffer, so it must stream sector-by-sector as it arrives rather than buffer-then-save. Builds on i99 (bounded flush window) + i114c (HWSAD write seam). Host-verifiable via the BDOSStore SectorWrites capture; real SD write dispatch hardware-gated like HSAVE. Unblocks i122c. | OPEN |  | i114c, i99 | i122c | src/netboot/bdos_seam.asm, src/netboot/body_sink.asm, tools/netboot-oracle/z80/bdos_store.go, i122 |
| **i122c** | **Fetch->validate->raw-write->boot orchestration entry + harness E2E** — i122 brick 3: the integration entry that ties the bricks together — TFTP-fetch a named disk image (i82 receive loop), validate it as a raw Trinity disk record (i114c bdos_validate_disk_record: size==819200 AND BDOS stamp@232, reject on mismatch), stream-write it into a record (i122b), then ALHK-boot it (i122a). Plus a harness E2E test driving the whole path. No control return on success. Real SD write + boot dispatch hardware-gated; orchestration + validation host-verifiable. | OPEN |  | i122a, i122b | i122d | src/netboot/netboot_client.asm, docs/notes/netboot-trinity-testing.md, i122 |
| **i122d** | **sha256-dedup-before-fetch: skip fetch + boot existing copy if content hash already on card** — i122 brick 4: idempotent fetch. Before fetching, compute the image's content SHA-256, derive its record name via i114d (content-addressed lookup), and check the on-card record list (i119 list-read path) — if a record with that content already exists, skip the fetch and boot the existing copy (i122a). Closes the i122 design note (b). Builds on i114d naming + the i119 list-read. Host-verifiable; real boot hardware-gated. | OPEN |  | i114d, i122c |  | tools/netboot-oracle/z80/sha256.asm, src/netboot/fw_span.asm, i122 |
| **i123** | **Investigate migrating the build from make to tup** — tup tracks the full dependency graph and rebuilds exactly what is stale (and removes orphaned outputs), structurally preventing the dirty-build/stale-artifact class — same class i116 patches test-by-test; tup would make stale-.bin reads impossible by construction. Pete's ~/git/spectrum4 uses tup ('amazing'). Scope: evaluate tup against current Makefile (pyz80 .asm→.bin, Go builds, disk-image assembly, CI jobs), then if it wins, plan the migration. | OPEN |  |  |  | Makefile, i116, i104 |
| **i124** | **Emulate full Trinity boot path so netboot boot wrappers run end-to-end in the harness** — Close the emulate-before-hardware gap. PR #423 landed: EEPROM modeled + ROM1-read-only-at-&C000; smoke_main + client_main run end-to-end. i82 hang localized: client_main logic sound (reaches client_first, emits ARP); fault = proactive ARP TX before PHY link-up + no ARP retransmit (→ i127). Remaining: model PHY link-up (LSTAT + drop-TX-while-down) to reproduce the drop in emulation; SD/RST-8 stub for full client fetch; fold server/serve/http wrappers + delete NETBOOT_HOSTTEST carve-outs (→ i126). | OPEN |  |  |  | tools/netboot-oracle/z80/eeprom.go, tools/netboot-oracle/z80/harness.go, tools/netboot-oracle/z80/enc28j60.go, tools/netboot-oracle/z80/netboot_boot_test.go, src/netboot/eeprom.asm, src/netboot/bdos_seam.asm, i119, i125, i126, i127, i93 |
| **i126** | **One faithful SAM emulation layer used by every test — fix fragmented/inadequate emulation** — Root cause of i82 hardware-debug detour: three partial emulators — flat 64 KB netboot harness (no paging/ROM/EEPROM), full assembler harness (LMPR/HMPR but no ROM1-at-&C000-at-boot), SimCoupé (faithful SAM, no Trinity). Deliverable: consolidate to one layer that (a) models ROM1 read-only at &C000 at boot, (b) carries Trinity seams (ENC28J60 + EEPROM + SD/B-DOS), (c) used by every test, (d) runs netboot boot wrappers end-to-end so NETBOOT_HOSTTEST carve-outs are deleted. Reuses harness.go paging. Subsumes i124 netboot slice. | OPEN |  |  |  | tools/z80-test-harness-go/harness.go, tools/netboot-oracle/z80/, src/netboot/, i124, i125, i116 |
| **i133** | **Self-service autonomous hardware testing via trinload** — With trinload on SAM (recs 3/128) + control returning between programs (i132), run unattended experiments: push a program via trinload3.py, it runs+RETs, push the next; feedback via tcpdump + a net result channel. PRIORITIZED to top (Pete 2026-06-20): the crux is whether we can BOOTSTRAP our own code from EEPROM / Trinity storage WITHOUT a human typing BOOT or picking a record — if a power-cycled SAM auto-starts a service that boots a pushed image, hardware testing goes fully autonomous (no Pete). The sooner we build it, the sooner we know. Auto-boot = i135; i172 audits dependents. | OPEN |  | i135, i121a, i122 | i118b, i141, i143, i145, i152, i70, i88c, i93b | ~/git/trinload/test/trinload.py, src/netboot/, i132, i127 |
| **i135** | **Auto-boot a Trinity record on power-up — unsupervised end-to-end hardware automation** — Pete has a Tapo smart plug for programmatic power-cycling. If a record auto-boots on power-on (no manual BOOT), agent runs fully unsupervised: power-cycle → SAM boots chosen record → runs → results over net (i133) → repeat. Investigate: EEPROM/config default-boot-record setting, Trinity AutoBOOT ROM (Colin's patch loads B-DOS from 128K EEPROM at reset), or patch forked B-DOS for early boot-a-record hook. Research: ~/sam-archive/bdos/analysis, Trinity AutoBOOT ROM, EEPROM layout, ~/sam-archive/trinity-docs. Pairs with i133 for full unsupervised loop. | OPEN |  |  | i133, i177 | ~/sam-archive/bdos/analysis, i87, i133 |
| **i135c** | **SAMBOOT: flash the patched EEPROM with backup/restore and confirm unattended auto-boot (answers the crux)** — Flash the prototyped patched EEPROM to Pete's Trinity via write_chunk, keeping the captured EEPROM as the restore backup; power-cycle and confirm unattended auto-boot of the configured record. Hardware (Pete). | OPEN |  | i135d |  | docs/specs/samboot.md |
| **i135d** | **SAMBOOT: emulation-prototype the EEPROM bootblock injection (stripes + BIOS + boot-record); folds i112** — Patch the EEPROM bootblock in the koron-z80 harness (emulation-first): always redraw the MGT stripes, read the SAMBOOT BIOS config, auto-boot the configured Trinity record or fall through to BASIC. Folds i112 (opening-screen restore). | OPEN |  | i135a, i176 | i135c | docs/specs/samboot.md |
| **i137** | **build-disk netboot ceiling check doesn't distinguish section class (i125 follow-up)** — tools/build-disk/main.go rejects netboot image only if it loads past &10000 (≤32768 bytes). A section-C bootable (smoke/serve/server/client) must fit &8000-&BFFF (≤16384); above that it loads into ROM1 at boot and crashes. Mostly masked: the per-program make fit-check (i125, 16384 for section-C) fires first, so an over-section-C binary never reaches build-disk normally. Gap only reachable by calling build-disk -netboot directly on an over-section-C image. Fix needs a -netboot-section-c flag threaded through disk targets. Low priority (masked by i125 gate). | OPEN |  |  |  | tools/build-disk/main.go, tools/netboot-boot-fit-check.sh, i125, i119 |
| **i141** | **B3h — raw SD CMD17 card-absolute list-sector read on ports &DC-&DF (hardware gate for B3)** — Hardware implementation of the card-absolute list-sector read modelled in B3 by the harness hook BD_HOOK_LISTREAD. Implements the SPI command ladder (CMD17 single-block read) on Trinity ports &DC-&DF to read list sectors 1..base-1 directly off the SD card. Not required for B3 emulation; the harness models the returned bytes. Needs final confirmation of the exact SPI init/read sequence on real hardware before building (docs/specs/trinity-record-detection-design.md §8, open-verification-item-1 and §3 frozen-interface principle). | OPEN |  | i133 |  | docs/specs/trinity-record-detection-design.md |
| **i142** | **make registry should rebuild build/registry before mutations — stale-binary footgun** — The build/registry binary is a built artifact; running 'build/registry set-status/add' with a STALE binary (not rebuilt after a tool change) misbehaves silently — it caused the i138 DONE-but-in-priority inconsistency (a pre-#462 binary skipped the reconcilePriority step, stranding i138 in the queue). Fix: make 'registry-gen' a prerequisite of 'make registry' and the mutation paths (or have the CLI self-rebuild / a wrapper), so an agent cannot operate the registry with a stale binary. | OPEN |  |  |  |  |
| **i143** | **bdos_picker: hardware MODE-2 blit for picker_render (follow-up from i119d)** — picker_render in src/netboot/bdos_picker.asm is a stub ret (all decision/show-name/confirm logic is host-verified in i119d). This item wires the actual SAM MODE-2 screen blit so the picker display is visible on real hardware. Depends on i119d being merged. | OPEN |  | i119d, i133 |  |  |
| **i144** | **Harness HSAVE write-back model: persist captured bytes into CardModel sectors** — BDOSStore.handle captures the HSAVE UIFA (record+name+page+addr+size) but does NOT copy the Size bytes from the source page into CardModel.Sectors[record]. Add a write-back so a follow-up HRSAD can read the saved file back — enabling the i119e E2E to assert record CONTENTS (not just that the right record was selected + CONFIRMED). Not needed for the safety gate; useful for round-trip verification. | OPEN |  |  |  |  |
| **i145** | **SD CSD capacity read -> BD_RECORDS (hardware-gated)** — client_main's auto-pick needs BD_RECORDS (card record count = tot.sct/1600). tot.sct comes from the SD CSD capacity read over SPI (ports &DC-&DF), which is hardware-gated (no emulator). Until this lands, BD_RECORDS=0 on real hardware => picker finds no free record => safe decline (no write). The emulation E2E injects BD_RECORDS directly. Implement the CSD read so the bootable client can actually claim a record on hardware. | OPEN |  | i133 |  |  |
| **i147** | **Turn-end safety valve: make stop-after-merge structural, not discretionary** — The autonomous-loop context protection (touch task-done -> end turn -> monitor /clears for a fresh context) relies on the agent CHOOSING to stop after each merged PR — the easiest step to skip mid-flow. 2026-06-20: a session never stopped, accumulating ~10 merged PRs into one polluted context (judgment-picking + missing-edge blindness followed). Make end-turn-after-merge STRUCTURAL: e.g. the monitor detects 'new merge to main + no task-done while a turn is still running' and intervenes, and/or a gh-pr-merge wrapper auto-touches task-done. Design + own PR. | OPEN |  |  |  |  |
| **i152** | **Verify the A=drive-register contract for HRSAD/HWSAD raw-sector hooks on real Trinity** — bdos_read_sector (HRSAD, i119) and bdos_write_sector (HWSAD, i114c) both leave A = the sector value at the rst 8, but the B-DOS hook code reads A as the drive number (bdos15a.src.txt rwsad/xsad: LD A,(hk.a)). After HRECORD-select a Trinity record presents as drive 2. The koron-go harness models the selected record via HRECORD and ignores A, so it cannot catch a wrong-drive bug. Confirm on real Trinity whether these hooks need A explicitly set (e.g. A=2 or A=record) or whether HRECORD makes the record the active context regardless of A; fix both routines symmetrically if needed. | OPEN |  | i133 |  | src/netboot/bdos_seam.asm, reference/bdos/bdos15a.src.txt, i119, i114c |
| **i158** | **Padlock/gate marker in item-registry-open.md (mirror backlog.md gate column)** — Add the gate marker (ready / in-progress / 🔒 qN blocked-on-Pete / ⏳ iN blocked-on-work) to item-registry-open.md, mirroring the backlog.md gate column (computeGates), so the open-items view also shows at a glance what's actionable vs blocked-on-Pete. Pete request 2026-06-20. | OPEN |  |  |  |  |
| **i159** | **Hyperlink item refs to in-file anchors in the registry .md tables** — In item-registry-open/closed.md + backlog.md, render id references (deps, dependents, parent, id-shaped refs) as in-file anchor links (e.g. [i122](#i122)) so a reader can jump to the referenced row within the same doc. Needs per-row anchors (GFM heading/HTML anchors in a table cell) + ref→anchor rewriting in gen.go. Pete request 2026-06-20. | OPEN |  |  |  |  |
| **i160** | **Tree-view preview at the bottom of the registry .md docs** — Append a visual tree view (umbrellas → children → bricks) to item-registry-open.md / item-registry-closed.md / backlog.md, below the full table, so the hierarchy is visible at a glance. Derive from the parent edges in gen.go. Pete request 2026-06-20 (the corrupted '#3'). | OPEN |  |  |  |  |
| **i161** | **Research CIIP (Z80 TCP/IP stack): locate source, assess license + portability** — From q25 (Pete: yes, investigate). Find CIIP — a pre-existing Z80 TCP/IP stack (TFTP/HTTP/DNS servers+clients; Simon Owen reportedly ported it to pyz80). Search ~/sam-archive + Pete's disks + online (GitHub etc.); do NOT scan Pete's photos (he checked — no source location, and it burns tokens). Assess license/portability and weigh 'adapt CIIP' vs continue i82/i83/i86. Report findings. | OPEN |  |  |  |  |
| **i162** | **Version-control ~/bin agent tools in a private ~/git/claude-tools repo** — From q33 (Pete: yes, private). Create ~/git/claude-tools, move the ~/bin agent tools in (claude-chat-export incl. the i155 fix, claude-diary helpers, claude-chat-export SessionEnd hook target, etc.), scrub anything private/secret, push to github.com/petemoore/claude-tools as a PRIVATE repo (Pete can make public later). Gives the tools backup + history. | OPEN |  |  |  |  |
| **i167** | **Determine the B-DOS save-buffer limit from first principles (read source; not hardware-only)** — q22 reframed (Pete 2026-06-20: 'only knowable on hardware' is a hand-wave — it's deterministic and we have the source). From first principles, read the B-DOS source (reference/bdos/) to find the max writable address / available save-buffer size deterministically, and replace the GUESSED FW_RECORD_CAP=4096 (src/netboot/http_main.asm, HTTP-streaming-sink path) with a source-derived value. Establish whether a hardware bench is truly required or (likely) the limit is computable from source. | OPEN |  |  |  |  |
| **i168** | **Future: import a full source file into the editor (fail-with-line-number on bad line)** — Future feature (q24, Pete 2026-06-20). The SAM editor accepts ONE LINE AT A TIME (parsed/validated on entry) — no whole-file import today. If/when import is added: on a bad line, fail and report the line number + the error; the user fixes the source and retries (Pete's preferred behaviour). Gated on the editor groundwork (depends i3) — a normal backlog item, not part of current editor specs. | OPEN |  | i3 |  |  |
| **i169** | **Adopt the 'needs Pete present' model (q32): ready --pete-present/--pete-away; decisions become questions** — q32 / needs-Pete model (Pete 2026-06-20). THREE kinds of work: (1) agent item = work the agent does alone (owner unmarked = agent); (2) question = a decision/input Pete gives (transient, deleted when answered); (3) needs-Pete-present = work gated on Pete's physical presence/action (press a key, hardware bench, Pi rig) = owner:pete.<br>Mechanism: 'build/registry ready' takes --pete-away (DEFAULT: excludes owner:pete) or --pete-present (includes AND prioritizes owner:pete, so when Pete is around the agent does presence-gated work first — don't waste his presence).<br>PRESENCE FILE = the single point of truth for whether Pete is around, surviving session handover / /clear / context-limit restart (the gap this closes). EITHER Pete OR an agent may create/delete it: Pete touches/rm's it; an agent creates it when Pete starts interacting and deletes it when Pete says he's stepping away (e.g. from his phone, where he can't touch files) — the agent TELLS Pete each time it creates/deletes on his behalf. The agent reads it at startup to default the --pete flag; monitor.sh can highlight presence from it.<br>Work: add the ready --pete-away/--pete-present flag defaulting from the presence file; convert the decision-shaped owner:pete items (i6, i50, i56, i66, i100c) to questions; backlog gate marker for owner:pete; document the model + presence file in CLAUDE.md + ROADMAP. Then i172 audits i133's dependents to reclassify presence-gated ones. | OPEN |  |  | i172 |  |
| **i172** | **Audit i133's dependents: reclassify presence-gated ones as 'needs Pete' (or push i133 up)** — i133-dependents audit (Pete 2026-06-20): i133 (automate SAM testing via trinload) gates i118b, i135, i141, i143, i145, i152, i70, i88c, i93b — but many likely only need Pete PHYSICALLY PRESENT (a key-press / hardware action), not the full automation. Per the needs-Pete model (i169): go through each; for those that only need Pete present, mark owner:pete (needs-pete) + drop the i133 dep. Alternatively push i133 near the top so when it lands we verify it truly unblocks them. Depends on i169. | OPEN |  | i169 |  |  |
| **i173** | **SAMBOOT: build the one-shot ROM+EEPROM dumper (serves captured bytes via the proven TFTP server)** — Small Z80 program: read the patched system ROM (32 KB via SAM ROM paging) + the Trinity EEPROM chunks, and serve them via the proven TFTP server so the host pulls rom.bin/eeprom.bin. Pushed via trinload; reuses the host-verified serve loop + Colin's eeprom.asm. Emulation-verify EEPROM-read + serve; ROM-page read is hardware-first. | IN_PROGRESS |  | i135a | i87a | docs/specs/samboot.md |
| **i176** | **SAMBOOT BIOS: editable default-boot-record config in Trinity EEPROM (boot record N, or none) + editor** — A boot config in a Trinity EEPROM chunk (like the 'Trinity Network' chunk): a default-boot-record setting (boot record N at startup, or none -- any disk). The injection prototype reads it; a config editor sets it. The SAM 'BIOS'. | OPEN |  | i135a | i135d, i177 | docs/specs/samboot.md |
| **i177** | **Publish trinboot: open-source EEPROM boot-config patcher (Python-over-trinload + bootable .mgt) for Trinity users** — Release the SAMBOOT EEPROM boot-config + stripes-restore patch as a standalone open-source tool for any Trinity owner: a Python program sent over trinload that patches the EEPROM, or a bootable .mgt. Gives Trinity users the configurable default-boot-record + opening-stripes feature for free. NOT a requirement for SAMBOOT -- depends on it (publication only). | OPEN |  | i135, i176 |  | docs/specs/samboot.md |
| **i178** | **Registry: raise the item-title length limit from 120 to ~160 chars (validate.go + mutators help + test)** — The 120-char title cap forced abbreviating SAMBOOT item titles. Bump the limit in tools/registry/validate.go (the check + message + the §147 comment), the mutators.go add-flag help text, and update validate_test.go's title-length assertion. Keep titles single-line. Pete invited this (2026-06-20). | OPEN |  |  |  |  |
