<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/items.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Item registry — closed

Work items with status `DONE` or `WONTFIX`. Open items live in
`item-registry-open.md`.

## Controlled status vocabulary (closed file)

| token | meaning |
|---|---|
| `DONE` | completed; delivering PRs are in the `PR` column |
| `WONTFIX` | decided not to implement; reason is in the description |

A `DONE` leaf may have zero or more PRs — operational work (e.g. config changes)
needs no PR; items delivered across several PRs may list them all. Umbrellas never
appear as `DONE` here directly — they are `DONE` only when all leaf children are
`DONE`/`WONTFIX`, and the generator emits them here once that condition holds.

Git history is the archive for per-PR detail, rationale, and iteration history.
This table holds the stable structured fact — what was decided and where to find it.

The `PR`, `deps`, `dependents`, and `refs/links` columns are each independent
first-class columns. The generated `.md` files are the source-of-truth views of
`registry/items.yaml` — do not edit them by hand (hand edits fail CI).

<!-- The table below is generated — do not edit it by hand. -->


| **id** | item | status | owner | PR | deps | dependents | refs/links |
|---|---|---|---|---|---|---|---|
| **i1** | **Compact-.tbn format change (hybrid bytes/symbolic; KindLitInsts + KindLitData)** — Introduced KindLitInsts + KindLitData hybrid format reducing compact .tbn size from 88,644 to 51,117 B (-42.3%), 6 IN pages -> 4, byte-identical to GNU. Delivered across PRs #121, #122, #124. Next-gen redesign explored as i39. | DONE | agent | [#124](https://github.com/petemoore/sam-aarch64/pull/124) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/specs/2026-05-27-compact-tbn-and-disassembler-design.md, docs/specs/tbn-binary-format-reference.md (§7) |
| **i5** | **UI visual prototyping via image generation (MODE 3 64x24 vs MODE 4 32x24 mockups)** — Mockup tooling for editor UI at SAM-accurate resolution. Carried forward by the i76 mockup backend. q1 resolved (2026-06-12): use a programmatic SAM-faithful renderer (PNG + SCREEN$ at exact mode geometry, real palette, correct pixel aspect) — generic AI image gen rejected; hand-authored retro art deferred to post-port. | DONE | agent | [#200](https://github.com/petemoore/sam-aarch64/pull/200) |  |  | i76, docs/specs/editor-tui-prototype-design.md §2.4 |
| **i6** | **SAM screen-mode decision (MODE 3 vs 4, or user preference)** — Decide the default screen mode for the editor. DECIDED (Pete 2026-06-21): MODE 3 (64×24, mode3-8x8). Settled directly rather than via the empirical i76 sheets; recorded in editor-tui-prototype-design.md §8 (R2). The 85-col MODE 3 6-px variant stays available behind the §3 geometry flag (a font tweak, not a mode change). Design in editor-tui-prototype-design.md §3/§5/§8 and ROADMAP Editor vision. | DONE | pete | [#515](https://github.com/petemoore/sam-aarch64/pull/515) |  |  | i76, docs/specs/editor-tui-prototype-design.md §3, docs/specs/editor-tui-prototype-design.md §5, docs/ROADMAP.md |
| **i7** | **Codegen sysreg/mnemonic/form tables from Go authority** — Phases A+B+C done: A deleted ENCTAB_LEN; B renamed enctab-gen->tables-gen, added -sysreg-inc emitter + CI job sysreg-sync, src/sysreg_tables.inc generated from sysregs.go; C1 added -constants-inc + src/tbn_constants.inc (OP_KIND_*/REC_KIND_*/DIR_* equates); C2 added -mnemonic-ids-inc + src/mnemonic_ids.inc (MNEM_<NAME> equates, all bare-numeric cp dispatchers now named). PRs #184 (spec) + #189/#234/#235/#236 (phases A-C). Three sync tests guard all .inc files. Phase D = i74 (gated on aarch64dec/sys.go code->data refactor) is the only remaining work. | DONE | agent | [#184](https://github.com/petemoore/sam-aarch64/pull/184), [#189](https://github.com/petemoore/sam-aarch64/pull/189), [#234](https://github.com/petemoore/sam-aarch64/pull/234), [#235](https://github.com/petemoore/sam-aarch64/pull/235), [#236](https://github.com/petemoore/sam-aarch64/pull/236) |  |  | docs/specs/codegen-tables-design.md, i74 |
| **i8** | **Sysreg-table de-dup into shared src/sysreg_tables.inc** | DONE | agent | [#108](https://github.com/petemoore/sam-aarch64/pull/108) |  |  | src/sysreg_tables.inc |
| **i9** | **Parity robustness seeds (sysname full-Go-parity + untested-form empirical sweep)** — PR #114 pinned two found gaps as skipped tests; those gaps were subsequently fixed under i36/i37. | DONE | agent | [#114](https://github.com/petemoore/sam-aarch64/pull/114) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-z80-go-disasm-parity-i9.md |
| **i10** | **Go-vs-Z80 capability parity report** | DONE | agent | [#109](https://github.com/petemoore/sam-aarch64/pull/109) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-go-vs-z80-disasm-capability-parity.md |
| **i11** | **Full ARMv8.0-A A64 ISA footprint research** | DONE | agent | [#110](https://github.com/petemoore/sam-aarch64/pull/110) |  |  | docs/notes/a64-isa-footprint-research.md |
| **i12a** | **SimCoupé v1.2.16 bump (pin SHA to upstream, drop vendored -exitonhalt patch)** | DONE | agent | [#112](https://github.com/petemoore/sam-aarch64/pull/112) |  |  | tools/Dockerfile.dev |
| **i12b** | **Editor-testing input injection (SimCoupé -keyin end-to-end proof)** — DETERMINED (PR #502, Outcome B): SimCoupé's -keyin is a boot-prompt autotyper, NOT a general key feed for a running program; it cannot reliably deliver keystrokes to a post-boot SAM CODE program that polls FLAGS(&5C3B)/LASTK(&5C08) (the editor-input use case). Verified locally (SDL_VIDEODRIVER=offscreen, isolated HOME): a diagnostic stub booted with SIMCOUPE_KEYIN='BOOT\nABCABC\n', confirmed CanType() satisfied at the stub (read LMPR=&1F → Section A=ROM0, B=page0), and polled FLAGS for several seconds — bit5 never set and zero keys arrived. Root cause (SimCoupé Base/SAMIO.cpp + Base/Keyin.cpp): (1) the string loads ONCE at the WTFK startup screen with no re-arm; (2) keys stream only from the IMEXIT hook so the target needs interrupts ON; (3) Rst8Hook()'s `default: Keyin::Stop()` drops the pending string on any RST-8 code outside &00/&1d/&35/&13/&50, which DOS/BASIC trip during the boot→program handoff. The reliable, deterministic, CI-gated vehicle for editor INPUT tests is the Go harness InjectKeys (i138, keyboard_test.go) — the same LASTK/FLAGS mechanism driven directly; SimCoupé stays the boot/render gate via the existing fixture jobs. The full determination + reproduction lives in the -keyin comment block of tools/run-simcoupe.sh (single home). PR #502 also fixed a real run-simcoupe.sh bug: SimCoupé splits the parallel-printer capture across simc0000.txt, simc0001.txt… after inactivity gaps, and the wrapper globbed only the first file (silently dropping gapped output); it now concatenates all simc*.txt in numeric order. No flaky -keyin CI job was added. | DONE | agent | [#502](https://github.com/petemoore/sam-aarch64/pull/502) |  |  | tools/run-simcoupe.sh, src/netboot/key_read_test.asm, i138 |
| **i12c** | **Rebase/upstream macOS+Linux paste support** — RESOLVED/MOOT — upstreamed (87f2a69); arrives free with i12a. | DONE | agent | [#112](https://github.com/petemoore/sam-aarch64/pull/112) |  |  | ~/.claude/.../memory/project_simcoupe_sdl_paste_branch.md |
| **i13** | **gitignore in-tree Go build binaries** | DONE | agent | [#107](https://github.com/petemoore/sam-aarch64/pull/107) |  |  | .gitignore |
| **i14** | **Non-canonical logical-immediate decoder tests (decodeBitMasks immr>=esize reject)** | DONE | agent | [#113](https://github.com/petemoore/sam-aarch64/pull/113) |  |  | deferred-backlog row |
| **i15** | **adds 3-register form (Z80)** — adds Xd,Xn,Xm (and shifted-register form) already byte-exact via INSN_RUN overlay. The Go authority computes the base word; Z80 emits verbatim. Only absence is one optional self-test row in shifted_reg_table (src/slots/shifted_reg.asm) — not a product gap. Out of scope: extended-register form (adds ..., w2, uxtb) errors even in Go — a separate Go-side gap, unused in corpus. | WONTFIX | agent |  |  |  | src/insn_run.asm, src/slots/shifted_reg.asm, tools/sam-aarch64/assemble/pass2.go, CLAUDE.md §6 |
| **i16** | **Drop synthetic cls test instruction (resolved by PR #26)** — RESOLVED by PR #26 (merged 2026-05-27, commit 20ef1aa), which already closed the same ROADMAP deferred-checklist item this row tracks. The synthetic/placeholder cls was DROPPED ENTIRELY — from the inst_alu_single fixture and from MnemonicTable + manual_forms.go + data.go + every pass2 dispatch site — rather than replaced with a real instruction (Pete's call: "until we have a .tbn in the wild, no need to restrict ourselves"; cls was unused). That supersedes this row's original "replace it with a real spectrum4 one" framing while fully meeting its intent: zero cls instructions remain in any fixture or table (verified 2026-06-21). This row was created later (the i13->i16 renumbering era) to track work that PR #26 had already completed; closing it as stale-but-done. | DONE | agent | [#26](https://github.com/petemoore/sam-aarch64/pull/26) |  |  | docs/ROADMAP.md |
| **i17** | **Deep reviews of main_loop.asm + litpool.asm + SYMTAB_* equ sentinels** — Review delivered (PR #185): 1 CRITICAL / 3 MEDIUM / 12 LOW. Fixes: C1 (litpool .ltorg flush segment counters), M1 (.equ/.set value range check), M2 (imm9/imm7 range check), M3 (litpool pc-map comment), L2 (reader_at_end carry contract), L8 (load_in_file DI). SYMTAB sentinels verified collision-free. LOW residuals -> i73. | DONE | agent | [#185](https://github.com/petemoore/sam-aarch64/pull/185) |  |  | repo-audit §6 |
| **i18** | **Wider naming review (tests/m{N}, ci-m{N}, build-m3-disk, milestone fixtures)** | DONE | agent | [#159](https://github.com/petemoore/sam-aarch64/pull/159) |  |  | scope row "Directory naming" |
| **i19** | **Subagent worktree-isolation leak — verified mitigated** — Investigated 2026-06-21 (this row was a "specifics TBD" scope concern). The core risk — one subagent's worktree filesystem changes leaking into another checkout — is already PREVENTED by three existing mechanisms, so no new hardening was required: (1) `.claude/worktrees/` is gitignored (commit 84d0224), so harness-native worktree files cannot pollute the main checkout's `git status` or be accidentally committed; (2) the Agent tool's `isolation: "worktree"` gives each mutating subagent its own git worktree, and CLAUDE.md discipline rules 3/5 forbid two writers in one checkout; (3) the rule-5 `git clean -fdx`-vs-nested-worktree hazard is latent only — no tool/Makefile/CI/`tools/` step runs `git clean -fdx`/`-ffdx` in the main checkout, and plain `git clean` skips worktree `.git` boundaries without `-ff`. The one observed residual is harmless: fully-orphaned `.claude/worktrees/agent-*` directories accumulate when the harness auto-clean doesn't fire (a stale Jun-12 leftover was found and removed); these are gitignored cruft (no `.git`, no admin entry, no checkout-leak) clearable any time via `git worktree prune` + `rm -rf .claude/worktrees/agent-*`. Closing as verified-mitigated; orphaned-worktree-dir auto-pruning is a possible future convenience but not warranted now (harmless, gitignored). | DONE | agent | [#504](https://github.com/petemoore/sam-aarch64/pull/504) |  |  | .gitignore, tools/autonomous-loop/README.md |
| **i21** | **Go-harness fidelity follow-ups: make harness-sweep, USAGE.md, memory watchpoint** — Harness-sweep (one-shot build-all-deps + go test -count=1 ./...) and USAGE.md (full-suite run, stale-payload/test-cache gotchas, boot-self-test authoring patterns) DONE in PR #341. Remaining: memory watchpoint inspection feature — deferred until a debugging session needs it (agent-owned; add ad hoc per the README Evolving the harness). | DONE | agent |  |  |  |  |
| **i21a** | **Go-harness: harness-sweep target + USAGE.md** — One-shot build-all-deps + go test -count=1 ./... sweep target, and USAGE.md (full-suite run, stale-payload/test-cache gotchas, boot-self-test authoring patterns). | DONE | agent | [#341](https://github.com/petemoore/sam-aarch64/pull/341) |  |  |  |
| **i21b** | **Go-harness memory watchpoint** — Memory watchpoint inspection feature for the Z80 harness. Not a proactively-planned deliverable: per the harness README 'Evolving the harness', memory watchpoints are an ad-hoc improvement to PR without ceremony WHEN a debugging session needs one. Reopen if/when a concrete debugging need arises. | WONTFIX | agent |  |  |  |  |
| **i22** | **Per-fail-site diagnostic strings** — Add per-fail-site diagnostic strings to test failures so failures are more easily identified without a disassembler. Deferred-backlog item. | DONE | agent | [#506](https://github.com/petemoore/sam-aarch64/pull/506) |  |  |  |
| **i25a** | **Harness: model DOSER (&5BC0) post-hook dispatch (correct i183's (hksp) vector)** — Harness models the DOSER (&5BC0) post-hook dispatch (success + error, A = SAMDOS error number) per ROM DOSC, replacing i183's non-functional (hksp) model. doserDispatch fires after every file-I/O hook; with a handler installed it models JP (DOSER), else default-halt on error. Verified via micro-programs that install a DOSER handler at &5BC0. Emulation-first prerequisite for the SAM handler (i25b). | DONE | agent | [#510](https://github.com/petemoore/sam-aarch64/pull/510) |  | i25b |  |
| **i26** | **text2bin operand-kind validation (Task 21)** — text2bin tool deleted (replaced by integrated sam-aarch64 CLI per Makefile). Only surviving text2bin references are stale comments. If equivalent validation is wanted, it rescopes against sam-aarch64/frontend libs as a new item. | WONTFIX | agent |  |  |  | tools/README.md, Makefile, tools/sam-aarch64/ |
| **i28** | **Absolute .set/.equ high-word edge: negative absolute emitted wrong high word (0x00000000 vs 0xFFFFFFFF)** — Z80 stored only low 32 bits and reconstructed high word as zero. Fix: eval_push_sym_sign_high sign-extends bit 31 (add a,a / sbc a,a idiom). Correct for whole accepted set: absolute-classification already refuses values whose high word isn't 0x00000000/0xFFFFFFFF. Positive absolutes still get high 0; release.bin byte-identical. Pinned by tests/symbols/sources/set_neg_highword.s (.quad of -256, -1, 0x10000000). | DONE | agent | [#379](https://github.com/petemoore/sam-aarch64/pull/379) |  |  | src/expr_eval.asm (eval_push_sym_sign_high), src/main_loop.asm (main_dir_equ_mark_abs), tests/symbols/sources/set_neg_highword.s, CLAUDE.md §6 (Go authority) |
| **i29** | **Harden slot self-test PASS_PC dependency** — Deferred-backlog item. Hardens the slot self-test PASS_PC dependency to make it more robust. | DONE | agent | [#512](https://github.com/petemoore/sam-aarch64/pull/512) |  |  |  |
| **i30** | **LDIR-fan-out for cross-page-shared blocks (smaller binary + faster boot)** — Deferred/possible future enhancement. Using LDIR fan-out for blocks shared across pages could reduce binary size and speed up boot. Noted in the strand-B PR4 disassembler plan under 'Future enhancements'.<br><br>WONTFIX (audit, PR completing this item). The prescribed audit found the only data block embedded in >1 production page is sysreg_tables.inc (~833 B), in page 13 (sysreg_data.asm) and page 15 (sysreg_names.inc via disasm.asm); the other multi-included .inc files (zx0_comm, tbn_constants, disasm_comm) are equate-only (0 data bytes). The note's source-de-dup half is already done (sysreg_tables.inc is the shared source). LDIR fan-out delivers none of its hypothesised wins here: no RAM saved (bytes occupy both pages at runtime — and the project's binding constraint is the &C000 RAM budget, not disk); no boot-load saved (pages 13 and 15 are HLOAD'd for their own matcher/disassembler code regardless of the tables, so the "N loads -> 1" benefit does not apply); only ~833 B off an unconstrained 800 KB disk image, against boot-time indirection + a new silent-failure surface spanning two separately-built payloads. Over-optimisation for current table sizes, exactly as predicted in the note. Reopen with a fresh item if the tables grow by an order of magnitude or the disk image becomes constrained. | WONTFIX | agent | [#513](https://github.com/petemoore/sam-aarch64/pull/513) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-07-strand-b-pr4-z80-disassembler-port.md |
| **i34** | **Untrack accidentally-committed Go binaries** — enctab-gen untracked (PR #111); llist-normalise deleted with the LLIST cluster (i52 PR 4, q8 resolved 2026-06-10). | DONE | agent | [#111](https://github.com/petemoore/sam-aarch64/pull/111) |  |  | .gitignore, i52 |
| **i35** | **sdiv missing across the whole stack (no mnemonic/Form -> .inst; unencodable)** | DONE | agent | [#115](https://github.com/petemoore/sam-aarch64/pull/115) |  |  | deferred-backlog row; mirrored udiv ID 72 |
| **i36** | **Z80 disasm: ccmp/ccmn decode (was -> .inst) -- added decode + encoder (ccmn ID 100); binutils-aligned** | DONE | agent | [#119](https://github.com/petemoore/sam-aarch64/pull/119) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-z80-go-disasm-parity-i9.md |
| **i37** | **base csinv/csneg (Rn!=Rm): Go aarch64dec decodes them (IDs 101/102), encoder coverage added; aliases intact** — i9 sweep skip removed; csinv/csneg families now certified. Shared PR #119 with i36. | DONE | agent | [#119](https://github.com/petemoore/sam-aarch64/pull/119) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-z80-go-disasm-parity-i9.md |
| **i38** | **Audit for other skipped/excluded tests + PRs that left gaps rather than fixing them** — Audited 14 skip sites: 13 legitimate, only the i9 gap (now fixed by i36/i37); no other papered-over gaps found. | DONE | agent | [#118](https://github.com/petemoore/sam-aarch64/pull/118) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-skipped-tests-and-gaps-audit.md |
| **i39** | **Next-gen maximally-efficient compact .tbn encoding (the design)** — Instruction overlay (assembled word + zeroed bits + sparse expression overlay, unifying literal/symbolic INST into one run), header label/offset table, one .tbn v2 with a contiguous evictable editor region, name front-coding, bitfield-packing polish. Shipped as milestone M8 (closed 2026-06-12). Design agreed as Format B (Pete 2026-06-08; all 5 open Qs resolved, §7). Shipped via i39a (PR #131) + i39b (PRs #151, #153). Residual i39c migrated to M9 (folds into i48c; reparented to i48c — not a child of i39). | DONE | agent |  |  |  | docs/specs/compact-tbn-nextgen-design.md, i39a, i39b, i39c |
| **i39a** | **M8 Phase 1 — instruction overlay + header label/offset table; the v2 format flip** — v2 format (KindInsnRun) + overlay Fold/ZeroSlot (13 slots); refenc emit/decode + bin2text render + round-trip gate; Z80 v2 reader + INSN_RUN decoder (src/insn_run.asm) with the symbolic form-table path retired (net −370 B). The spectrum4 release byte-matches GNU on the SAM via the v2 compact .tbn (88,644→47,067 B; OUT 21,752 B) + all 53 M3–M6 fixtures. PR(d) done — header label/offset table; compact .tbn 47,067→45,189 B. i48b (FoldMovzAuto) co-shipped. Multi-PR delivery: PR(a)+(b)+i48b+(c)+(d) all part of PR #131. i48a split to a follow-up PR. | DONE | agent | [#131](https://github.com/petemoore/sam-aarch64/pull/131) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-08-i39-phase1-instruction-overlay-plan.md |
| **i39b** | **M8 Phase 2 — name-table front-coding + editor-region split for compact .tbn v2** — Delivered in two PRs. #151 (was id i39b-1, front-coding): each name-table entry becomes [shared_prefix_len][suffix_len][suffix] shared with the previous name in encounter order (no sort, so name_ids stay stable); .tbn 45,189->44,207 B. #153 (was id i39b-2, editor-region split): splits .tbn into an assembler-facing region and a trailing editor region bounded by editor_region_offset u32 at offset 8; -thin-comments host flag; enables i40 eviction. Assembled binary byte-identical (GNU==Go==Z80/SAM). Design §3.5-3.7. | DONE | agent | [#153](https://github.com/petemoore/sam-aarch64/pull/153) |  |  | docs/specs/compact-tbn-nextgen-design.md, https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-09-i39b-nametable-frontcoding-sidecars.md, https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md |
| **i40** | **Assembler-side editor-region eviction** — Load-path DONE (PR #181): SAM loads only the assembler-facing .tbn prefix (two-phase HGTHD+HLOAD — head read to discover editor_region_offset, then prefix HLOAD); the editor region never enters the IN buffer on any machine size. The conditional-residency/reload half is superseded by the comment-storage design (docs/specs/comment-storage-design.md): the editor holds comments as a compressed-resident block store with watermark math. Residual: page-aligned editor_region_offset normalisation verified against harness convention only — no page-aligned SimCoupé fixture yet (PR #181 review note). | DONE | agent | [#181](https://github.com/petemoore/sam-aarch64/pull/181) |  |  | https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md, docs/specs/comment-storage-design.md, i51 |
| **i41a** | **Editor edit-model — core paged block-list + cleanly-separable serialize/load seam** — First i41 brick; host-side Go authority for the eventual Z80 port (i41d). Package tools/sam-aarch64/editmodel: half-page (8 KB) blocks, u24 never-reused record-ids, a record-id to stable-block-pointer location table maintained O(half block) per split/merge (no global reindex — the bounded-cost property), insert/delete/goto-by-id/render, and a separable EMDL serialize/load seam (swapped for real v2 .tbn in i41c). Proven by property test (5 seeds x 7000 random ops vs oracle) + 15 unit tests + a teeth test (corrupted loc caught). CI-gated. SAM byte-layout and paging latency are i41d. | DONE | agent | [#383](https://github.com/petemoore/sam-aarch64/pull/383) |  |  | tools/sam-aarch64/editmodel/, docs/specs/editor-edit-model-design.md |
| **i41b** | **Editor edit-model — bounded ring-journal undo/redo** — Adds bounded ring-journal undo/redo to tools/sam-aarch64/editmodel (editEntry ring, maxUndoDepth=256, drop-oldest cap). Undo-of-insert deletes by IndexOf(id) (drift-robust); undo-of-delete re-inserts at the recorded index with the same RecordID (id-stability, anchors/comments stay valid); a fresh edit clears the redo stack. Proven by TestPropertyUndoRedo (5 seeds x 3000-step random walk vs reference state history) + 10 unit tests. CI-gated via go test + staticcheck. | DONE | agent | [#384](https://github.com/petemoore/sam-aarch64/pull/384) |  |  | tools/sam-aarch64/editmodel/, docs/specs/editor-edit-model-design.md |
| **i41c** | **Editor edit-model — real v2 .tbn serialize seam** — Adds SerializeTBN/LoadTBN to tools/sam-aarch64/editmodel (tbn.go): save = join lines + frontend.Translate + assemble.Pass1 + CompactTBNBytes; load = render.EmitLinesFromBytes + fresh editmodel lines. The EMDL seam is retained as the internal exact-round-trip seam the i41a/i41b property tests depend on. Deliberate limitation: SerializeTBN requires a complete valid assembly — an invalid/partial document fails loud; incremental/partial serialize is i41e. Proven by TestSerializeTBNRoundTrip + fail-loud + load round-trip tests. | DONE | agent | [#385](https://github.com/petemoore/sam-aarch64/pull/385) |  |  | tools/sam-aarch64/editmodel/, docs/specs/editor-edit-model-design.md |
| **i48a** | **Host front-end unification — factor encoder/assembler/renderer as shared Go libs** — Host front-end unification into shared libs. PR1 (#141): 3 host tools became thin wrappers over tools/sam-aarch64 with shared libs frontend/assemble/render. PR2 (#142): integrated sam-aarch64 binary (source->{binary,compact .tbn}, .tbn->binary, --render, -E); symbolic .tbn in-memory IR; 3 old modules deleted. PR3 (#144, final): symbolic records are pure in-memory []format.Record IR; Decision-B strictness (symbolic mem->error; lsl#12 syntactic; mov->movz default); q7 sweep resolved; 36 dead goldens removed. Post-#144 cleanup (#146): dead-code sweep. All byte-neutral; 14 CI checks green. | DONE | agent | [#144](https://github.com/petemoore/sam-aarch64/pull/144) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-09-i48a-host-frontend-unification.md |
| **i48b** | **Syntactic encoder + fold-time value-work** — Syntactic encoder + fold-time value-work. FoldMovzAuto (hw computed in the fold; ZeroSlot clears bits 22:21) landed in commit 0162f52 with i39a (PR #131). The non-byte-affecting strictness items (symbolic mem -> FoldMemImm12-or-error; add/sub lsl#12 syntactic via the fold + a split ZeroSlot; mov->movz default) + q7 sweep (no further GNU silent form-rewrites in corpus) landed in i48a PR3 (#144, final). Byte-neutral on the corpus (0 affected sites confirmed by q7 sweep). Two-PR delivery: first PR #131, final PR #144. | DONE | agent | [#144](https://github.com/petemoore/sam-aarch64/pull/144) |  |  | docs/specs/i48-syntactic-encoder-design.md |
| **i48c-b1** | **Lexer core — tokenizer kind/span/base + literal types + local-label refs + string literals** — Completes the editor lexer (src/asmlex.asm). B1: tokenizer kind/span/base. B1b: int64 literal value (parseIntInBase) + char literals (readCharLit). B1c: local-label refs Nf/Nb (readNumberOrLocal). B1d: string literals (readString — cpp line-directives out of scope, preprocessor artifact). Delivered across PRs #393 (B1) + #394 (B1b) + #395 (B1c) + #397 (B1d). | DONE | agent | [#397](https://github.com/petemoore/sam-aarch64/pull/397) |  |  | src/asmlex.asm, tools/netboot-oracle/z80/asmlex_test.go |
| **i48c-b2** | **Simple-inst parse → INST record (mnemonic table + reg/reg/reg + reg/reg/#imm)** — B2a: generated src/mnemonic_names.inc from Go authority + Z80 mnemonic_lookup (runtime-data twin of mnemonic_ids.inc). B2b: generic reg/reg/reg parse (matchReg + operand loop + emitInst → INST record). B2c: reg/reg/#imm (single-literal immediate: WriteImmExpr + folded WriteImm), host-verified vs format.ExprWriter/OperandWriter. Delivered across PRs #398 (B2a) + #399 (B2b) + #400 (B2c). | DONE | agent | [#400](https://github.com/petemoore/sam-aarch64/pull/400) |  |  | src/asmparse.asm, tools/netboot-oracle/z80/asmparse_test.go, tools/sam-aarch64/frontend/parser.go |
| **i48c-b3** | **Expression parser — precedence→expr bytecode (full arithmetic grammar + symbol-intern + PC/reloc primaries)** — B3a: scaffold (parseExprPrec/Primary/tokPrec) + constant-fold over + - & \| ^ / unary - ~ / parens. B3a2: shifts << >>. B3a3: * (64-bit shift-add multiply). B3a4: / (signed 64-bit restoring long division — truncate-toward-zero, MinInt64/-1→MinInt64, div0→0). B3b: symbol-intern + WriteSym (document symbol-table seam; per-document reset). B3c: PC . / local-ref Nf/Nb / :reloc: primaries. All host-verified vs format.ExprWriter+EvalConst. PRs #402+#403+#404+#406+#407+#412. | DONE | agent | [#412](https://github.com/petemoore/sam-aarch64/pull/412) |  |  | src/asmparse.asm, tools/netboot-oracle/z80/asmparse_test.go, tools/sam-aarch64/frontend/parser.go |
| **i48c-b4** | **Operand suffixes — memory operands (7 [...] shapes), register shift/extend suffix, condition-code operand** — B4: parse_operand_mem — all 7 [...] shapes; match_extend — ExtendKind table scan; MEM_SHAPE_*/EXT_* constants in tbn_constants.inc via tables-gen; host-verified vs Go parseMem/matchExtend. B4b: match_shift_kind + lookahead in parse_operand_reg; SHIFT_* constants; mutation-verified vs WriteShiftedReg/WriteExtendedReg. B4c: match_cond + COND_NAMES table (18 records: 16 canonical + 2 GNU aliases); mutation-verified. Delivered PRs #414 (B4) + #415 (B4b) + #417 (B4c). | DONE | agent | [#417](https://github.com/petemoore/sam-aarch64/pull/417) |  |  | src/asmparse.asm, tools/netboot-oracle/z80/asmparse_test.go, tools/sam-aarch64/frontend/parser.go |
| **i48c-b5** | **Label-def + local-def parse (foo: / 1: statement-leading)** — Port parseLine's label/local-def cases (parser.go:108-119): a TOK_IDENT followed by TOK_COLON interns the name (sym_intern) and emits a LABEL_DEF (REC_KIND 0x02, payload=symbol id u16 LE); a TOK_INT in [1,99] followed by TOK_COLON emits a LOCAL_DEF (REC_KIND 0x03, payload=digit u8). These in-memory IR records (compacted to header tables at serialize-time) are the last statement-leading record kind missing from the Z80 parser. Flat-memory-autonomous (koron-go/z80 harness vs host refParse); prerequisite for i48c-b8's fixture-corpus round-trip (nearly every fixture defines labels). | DONE | agent | [#555](https://github.com/petemoore/sam-aarch64/pull/555) |  | i48c-b8 |  |
| **i48c-b5a** | **movk/movz/movn special-form (#imm16 [, lsl #N], hw=N/16 folded into bits[17:16], range-checked)** — parse_movk dispatched on movk/movz/movn mnemonic ids before generic loop. #imm16 [, lsl #N] with hw=N/16 folded into bits[17:16]; constant imm range-checked to [0,65535] + folded to (hw<<16)\|imm; symbolic imm emits (hw<<16)\|(immExpr & 0xffff). Helpers: emit_imm_expr_operand/expr_buf_single_imm/expr_append_raw. Standalone build now includes mnemonic_ids.inc. 11 hand + 10 error + 5 fuzz seeds; host-verified vs format.ExprWriter/EvalConst/OperandWriter. | DONE | agent | [#435](https://github.com/petemoore/sam-aarch64/pull/435) |  |  | src/asmparse.asm, tools/netboot-oracle/z80/asmparse_test.go |
| **i48c-b5b** | **movl pseudo (movz+movk expansion / :abs_g0_nc:+:abs_g1:)** — Implement the movl pseudo-instruction: expands to movz+movk pair; handles :abs_g0_nc: and :abs_g1: reloc operands. | DONE | agent | [#547](https://github.com/petemoore/sam-aarch64/pull/547) |  | i48c-b8 | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b5c** | **Barriers dsb/dmb/isb (barrier-arg keyword→CRm)** — Parse barrier instructions dsb/dmb/isb: map barrier-arg keyword (e.g. sy, ish, nsh, ...) to the CRm encoding field. | DONE | agent | [#548](https://github.com/petemoore/sam-aarch64/pull/548) |  | i48c-b8 | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b5d** | **mrs/msr — sysreg/PSTATE name→OpSysName** — Parse mrs/msr instructions: map sysreg and PSTATE register name strings to the OpSysName encoding (name lookup table). | DONE | agent | [#549](https://github.com/petemoore/sam-aarch64/pull/549) |  | i48c-b8 | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b5e** | **dc/tlbi — op-name + optional Xt** — Parse dc/tlbi instructions: map operation-name string to encoding + accept optional Xt register operand. | DONE | agent | [#550](https://github.com/petemoore/sam-aarch64/pull/550) |  | i48c-b8 | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b5f** | **ldr = literal-pool pseudo (OpLitPool)** — Parse ldr= pseudo-instruction: captures the pool-value expression into an OpLitPool operand for later literal-pool placement and PC-relative load emission. | DONE | agent | [#552](https://github.com/petemoore/sam-aarch64/pull/552) |  | i48c-b8 | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b6** | **Directives parser** — Parse assembler directives (.set, .balign, .org, .skip, .ltorg, .inst, .section, etc.) into their respective overlay record types via src/asmparse.asm. | DONE | agent | [#553](https://github.com/petemoore/sam-aarch64/pull/553) |  | i48c-b8 | src/asmparse.asm, tools/sam-aarch64/frontend/parser.go |
| **i48c-b7** | **Parse comment and blank-run record types** — Parse COMMENT (0x05) and BLANK_RUN (0x06) record types in src/asmparse.asm, a faithful port of the Go authority (tools/sam-aarch64/frontend/parser.go: emitComment / emitBlankRun / parseLine). Comment placement (line-start vs after-statement) and blank-line-run coalescing mirror the Go exactly; byte-for-byte verified against the Go reference by the netboot-oracle harness (tools/netboot-oracle/z80/asmparse_test.go). The brick also makes the PARSE_RECS stream self-describing (each record now leads with its REC_KIND byte) so a mixed INST/COMMENT/BLANK_RUN stream is unambiguous (mnemonic ids 5/6 otherwise collide with kinds 0x05/0x06). The i39c overlay-slot bitfield-packing polish is a SEPARATE concern (different subsystem, optional second-order optimisation) split back out to the i39c item — it was never part of this parser brick's actual delivery. | DONE | agent | [#517](https://github.com/petemoore/sam-aarch64/pull/517) |  |  | src/asmparse.asm, tools/netboot-oracle/z80/asmparse_test.go, tools/sam-aarch64/frontend/parser.go |
| **i48c-b8a** | **Z80 Pass1-over-IR: PC assignment + label/local position capture + symbol resolution** — Port assemble.Pass1 (tools/sam-aarch64/assemble/pass1.go) to walk the encoder's in-memory IR record stream (INST/DIRECTIVE/LABEL_DEF/LOCAL_DEF/COMMENT/BLANK_RUN as emitted by parse_run) and compute: each record's output PC (INST=4 bytes; .word/.quad/.byte/.skip/.balign/.org/.ltorg PC effects; literal-pool placement), the absolute offset of every LABEL_DEF (name_id->PC) and LOCAL_DEF (digit->PC) for the header tables, and .set/.equ symbol values. Output: a label/local position table + origin VMA, the input headerRows + the assembler both consume. Flat-memory-autonomous; harness-test a new entry that feeds an IR record buffer and compares the resolved positions vs host Pass1. | DONE | agent | [#557](https://github.com/petemoore/sam-aarch64/pull/557) |  | i39c, i48c-b8b |  |
| **i48c-b8b** | **Compact non-encoder core: LIT_DATA + sidecar + globals + PC walk (flat harness)** — Port the non-encoder body of assemble.Compact (compact.go:46-188, excluding literalWord/encodeInstOverlay): drop LABEL_DEF/LOCAL_DEF (already in b8a Pass1); COMMENT/BLANK_RUN -> editor-region sidecar rows anchored to RecordPC; .global -> name_id list (globalNameIDs); same-directive constant data -> KindLitData runs split at the 1016-byte cap; emit INST elements as a skeleton (base/patches filled by the encoder adapter i48c-b8e). Flat-memory harness like b8a (reuse pass1_ir_test.go scaffold: serializeIR + Pass1 oracle + record read-back), verified vs host assemble.Compact's non-encoder outputs (sidecar/globals/LIT_DATA/PC) over fixtures. No ENCTAB/paging -> no memory collision. Go authority: compact.go. The encoder-dependent half (literal-probe + overlay-patch emission, needs paged ENCTAB) is the sibling i48c-b8e. | DONE | agent | [#559](https://github.com/petemoore/sam-aarch64/pull/559) | i48c-b8a | i39c, i48c-b8e |  |
| **i48d** | **Doc unification — bring tbn-binary-format-reference.md to v2 overlay-only format** — First brought v2-accurate (both profiles) in commit dc2b993 (part of i39a PR #131). Then fully rewritten overlay-only in i48a PR3 (#144, final) once the symbolic form became in-memory-only: tbn-binary-format-reference.md now documents a single on-disk format (compact overlay v2 — header + name table + label/local header tables + INSN_RUN/LIT_DATA/DIRECTIVE/COMMENT + 13 FoldSlots), with symbolic kinds noted as in-memory IR only. Also: historical banners on the M1 + compact-tbn design docs. Two-PR delivery: first PR #131, final PR #144. | DONE | agent | [#144](https://github.com/petemoore/sam-aarch64/pull/144) |  |  | docs/specs/tbn-binary-format-reference.md, docs/specs/i48-syntactic-encoder-design.md |
| **i49** | **tools/ cleanup — delete orphan tool dirs and prune stale .gitignore entries** — Delete two orphan tool dirs (flatten-s, build-screen-disk; off the build/CI/module path), prune 6 stale .gitignore binary-guard entries (4 for tools deleted in i48a/M0, 2 for the dirs removed here), drop their README rows, fix post-i48a doc-rot (harness README make text2bin -> make sam-aarch64; ci.yml m6-release comment). A follow-up commit also refreshed the last stale .gitignore text2bin comment. | DONE | agent | [#149](https://github.com/petemoore/sam-aarch64/pull/149) |  |  |  |
| **i51** | **Un-strip all comments on the SAM-side m6 gate** — Flow the full 335 KB of release.s comment text (7,502 // lines) through the SAM round-trip. DONE (PR #181): full unstripped compact .tbn (363,295 B; assembler-facing prefix 38,584 B / 3 IN pages) flows through the release gate; SAM-assembled OUT byte-matches GNU 21,752 B. Depends on i40 load-path. | DONE | agent | [#181](https://github.com/petemoore/sam-aarch64/pull/181) |  |  | https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md, i40 |
| **i52** | **Repo cleanup: docs restructure + tracking consolidation + tooling hygiene + functional renames** — Delivered across PRs #155-#159 (2026-06-10). Spec in git history at 2f4c898/docs/specs/2026-06-10-repo-cleanup-design.md. Includes: docs restructure (ARCHITECTURE.md, specs/, plans/), tracking consolidation (iN/qN registries), tooling hygiene, and functional renames (tests/format\|core\|symbols\|operands\|paged\|release, build-disk, CI jobs). i55/i56 were deferred sub-scope items. | DONE | agent | [#159](https://github.com/petemoore/sam-aarch64/pull/159) |  |  | https://github.com/petemoore/sam-aarch64/blob/2f4c898/docs/specs/2026-06-10-repo-cleanup-design.md |
| **i53** | **Bump-arena reclamation for the fixed section-D scratch arrays** — YAGNI per the risk census: every section-D table is SAFE under ~5x kernel growth and the real ceilings (IN/OUT paged buffers) are page-allocation bumps, not arena problems. Revisit only if the census §4 trigger fires (fixed section-D array overflows at >5x kernel scale). | WONTFIX | agent |  |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/specs/2026-05-29-bump-arena-risk-census.md |
| **i54** | **Synthetic-fixture oracle sweep for the on-SAM disassembler** — Synthetic-fixture oracle sweep for the on-SAM disassembler: 102-word corpus covering every family the Go decoder handles but release.img never exercises (cond-compare/select, extended-reg add/sub, signed multiplies, load/store option matrix — §B of the i10 capability-parity report). Converts §B from structurally-ported to empirically-certified. 11 new TestSyntheticParity_* functions. One genuine Z80 gap found and fixed: EXTR/ROR-immediate decoder missing from src/disasm.asm (ported from tryDecodeExtr, aliases.go:778). All 17 TestSyntheticParity_* + TestDisasmOracle 5438/5438 pass. | DONE | agent | [#183](https://github.com/petemoore/sam-aarch64/pull/183) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-go-vs-z80-disasm-capability-parity.md |
| **i57** | **Comment-compression experiment (host-side, Go): benchmark Z80-feasible schemes** — Benchmarked static word dictionary, digram/BPE, canonical Huffman, dictionary+Huffman hybrid, block-based LZ (ZX0/ZX7/LZSA-class) at 1/2/4/8 KB blocks, and flate as ceiling, against the real unstripped release editor region (335,461 B comment text, 7,502 comments). Result: best Z80-feasible = greedy LZ77/ZX0-style 85,526 B (ratio 0.287, 6 pages); no scheme fits 256 KB SAM; 512 KB SAM comfortable (15 pages free). Feeds i59 decision. Tool: tools/comment-bench/. | DONE | agent | [#161](https://github.com/petemoore/sam-aarch64/pull/161) |  |  | i59, docs/notes/comment-compression-research.md |
| **i58** | **Trinity interface capabilities research: storage, SPI access, Z80 driver feasibility** — Researched Quazar Trinity SD storage (port &DF/SPI VERIFIED; raw-sector 800 KB-record model; no public Z80 SD driver — source in SAM Revival 21 print; ethernet driver ready for Phase-3). Findings in docs/notes/trinity-capabilities.md. Cited sources required per item scope. | DONE | agent | [#162](https://github.com/petemoore/sam-aarch64/pull/162) |  |  | i59, docs/notes/trinity-capabilities.md |
| **i59** | **Full-fidelity source residency on SAM — decide comment-storage strategy** — Decision (Pete, 2026-06-10): compressed-resident on 512 KB; stripped-save (i65) covers 256 KB assembly; Trinity persistence deferred (i66). i40 urgency drops (eviction is no longer the 256 KB story). Reducing comments at source rejected (dev experience is the product goal). Consumes i57 + i58 results; informs i40 scope and i51 full-comment gate. Resolved as part of q9 closure (PR #168). | DONE | pete | [#168](https://github.com/petemoore/sam-aarch64/pull/168) |  |  | i57, i58, i60, i65, i66, docs/notes/comment-compression-research.md |
| **i60** | **On-SAM ZX0-format comment compression — measurement, compressor, and design** — Decision (Pete): greedy ZX0-bitstream compressor ON the SAM for self-hostedness. (a) harness T-states measured (PR #163); (b) Go-first greedy compressor (tools/zx0-greedy, H=512 D=16: ratio 0.3631 at 4 KB, 9246 B scratch) + Z80 port src/zx0_compress.asm (24/24 blocks byte-identical, post-i67 972 T/byte); (c) design approved PR #173: docs/specs/comment-storage-design.md — 4 KB blocks, turbo decoder, page-13 co-residency + page-14 staging, watermark R=2. Implementation: i68. | DONE | agent | [#173](https://github.com/petemoore/sam-aarch64/pull/173) |  |  | i59, docs/notes/comment-compression-research.md, docs/specs/comment-storage-design.md |
| **i61** | **Corpus sweep for Trinity/B-DOS/SAM-Revival artifacts (800 disk images)** — Found (2026-06-10): B-DOS 1.5t beta 6 binary + period Trinity utilities (SD select bytes recovered); B-DOS 1.5a complete Z80 source; 1.6c/1.7D/1.7J/1.7N binaries; a uIP TCP/IP stack for the SAM (Phase-3 relevant). NOT found: any SAM Revival disk, the Quazar manual. Detailed catalogue in private session records (publication policy). trinity-capabilities.md updated. | DONE | agent | [#166](https://github.com/petemoore/sam-aarch64/pull/166) |  |  | docs/notes/trinity-capabilities.md |
| **i62** | **B-DOS storage-backend investigation: prove SAMDOS hook-level portability end-to-end** — Boot B-DOS AL 1.5a + emulated Atom Lite under SimCoupé; exercised SAMDOS-hook file-io (DVAR-7 detect → HRECORD 1 → HSAVE/HGTHD/HLOAD round-trip) against a B-DOS record. Hook portability SAMDOS ↔ B-DOS AL: LIKELY → VERIFIED; Trinity/1.5t leg stays LIKELY (no emulation). Rig tools/i62-bdos-experiment/ is re-runnable. Findings: docs/notes/bdos-version-landscape.md §Empirical verification (i62). | DONE | agent | [#179](https://github.com/petemoore/sam-aarch64/pull/179) |  |  | docs/notes/bdos-version-landscape.md, tools/i62-bdos-experiment/README.md |
| **i63** | **Superpowers extraction: keep/adapt/drop skills, encode as repo-native rules** — Studied obra/superpowers (MIT); keeps encoded as native CLAUDE.md rules (PR #167, Pete-approved verbatim); plugin disabled for this project 2026-06-10. The bare-run test is every subsequent item. Rationale on the i63 PR. | DONE | agent | [#167](https://github.com/petemoore/sam-aarch64/pull/167) |  |  | ~/git/superpowers |
| **i64** | **Blog post: the 2026-06-10 session log (repo cleanup → compression → superpowers)** — First post published 2026-06-10 (petemoore.github.io: 'When the agent helps choose its own process'); drafted by agent with consent, privacy pass per publication policy before publishing. The blogging tradition continues per private memory. Closed in registry by PR #169 (session-close) alongside i63. | DONE | agent | [#169](https://github.com/petemoore/sam-aarch64/pull/169) |  |  |  |
| **i67** | **Night-shift T-state optimization of the Z80 ZX0 compression path** — Baseline then optimize src/zx0_compress.asm under a hard byte-identity invariant. Result (2026-06-11): compress 804.1M → 289.7M T (−64.0%) at 4 KB blocking; 134.0 s → 48.3 s at 6 MHz. 2698 → 972 T/byte; oracle byte-identical on all 110 whole-corpus blocks. Decode side: vendored + measured dzx0_fast (48.8 T/B) and dzx0_mega (46.6 T/B) vs standard 63.6 / turbo 50.9. Undocumented-opcode support matrix documented. PRs #170 + #171. | DONE | agent | [#171](https://github.com/petemoore/sam-aarch64/pull/171) |  |  | i60, docs/notes/comment-compression-research.md |
| **i68** | **ZX0 integration: promote decoder to src/, wire into boot path, boot self-tests** — i60c design implementation: (1) dzx0_turbo.asm promoted to src/ (org &8B00); (2) page-13 payload zx0.bin/zx0-test.bin, -zx0 build-disk flag, load_zx0_payload in both variants; (3) baked-bytes boot self-tests (1 KB comment fixture, tags &E1-&E3) — green in SimCoupé matrix, kosarev/z80 IXH/IXL proof; (4) release-unstripped-tbn re-pointed at vendored tests/release/release.s; (5) ARCHITECTURE §5 rewritten. Budget: test 141→86 B headroom, prod +38 B. | DONE | agent | [#176](https://github.com/petemoore/sam-aarch64/pull/176) |  |  | i60, docs/notes/comment-compression-research.md |
| **i69** | **Test-memory architecture: relieve section-C test-variant squeeze using test-coexistence principle** — Relieve section-C test-variant squeeze (principle: tests need not coexist in memory). Levers in order: (1) evict suites off-axis (PR #52 pattern); (2) serial test bay (page-13 time-multiplex generalised); (3) chunked payloads across more pages/disk; (4) multiple boots. Lever 1 partially applied (PR #180): test_symbols + test_local_labels evicted; test variant code_end &BFAA->&BCA3.<br><br>FINDING (2026-06-22, i69 investigation): Lever 1 (evict-to-cluster) is now EXHAUSTED for the remaining inline suites — they are inline PRECISELY because they are paging-coupled. test_emit_paged/test_reader_paged/test_sysreg_paged each do enctab_map_in / LMPR swaps that would page OUT the off-axis cluster's own executing code (cluster runs with LMPR mapping page 12 into section A; dispatch at assembler.asm:388, BEFORE load_enctab); test_sysname needs the section-B paged_call body (sysreg encoders); test_trampoline/test_paged_call are bootstrap-timed. So a naive cluster move hangs the boot.<br><br>VIABLE LEVER for the encode_inst pressure = lever 3: move enc_fix_table (528 B + all future i201/i204 fixture growth) + the fixture operand streams OFF-AXIS to a spare HLOAD'd page (test_cluster.bin pattern), read row-by-row by the INLINE driver (run_encode_inst_self_tests, must stay inline — encode_inst reads ENCTAB in section A) via an LMPR-bracketed copy: map fixture page into section A, copy row+stream into an inline scratch buffer (driver is in section C, stays mapped), remap ENCTAB, then call encode_inst on the scratch copy. ~30 B driver overhead, +~498 B now and SCALES with the strand. Files: new payload build target + loader fn + assembler.asm boot call + Makefile/build-disk filename + driver restructure + move the enc_fix_table data out of test_encode_inst.asm. Verify via TestBootSelfTestsPass (catches boot hang) + SimCoupe. i201a/i201b/i201c/i204 fixtures all depend on this relief. | DONE | agent | [#577](https://github.com/petemoore/sam-aarch64/pull/577) |  | i201a | i68, src/README.md |
| **i71** | **B-DOS reference + Trinity-fork deep-dive: vendor, disassemble, diff, re-point rig** — (a) Vendored reference/bdos/: Bdos15a.zip + bdos15a.src.txt + al-bdos15a.bin + provenance README (Edwin Blink FREEWARE; 1.5t NOT vendored — WoS 'Copyrights Declined'). (b) B-DOS 1.5t beta 6 disassembled + diffed vs 1.5a: hook surface VERIFIED-STATIC (all 39 vectors map 1:1; only sector-device layer swapped). Findings: docs/notes/bdos-trinity-fork-analysis.md. (c) i62 rig re-pointed at reference/bdos/al-bdos15a.bin. Runtime confirmation on real Trinity hardware remains outstanding (no emulator covers the ports). | DONE | agent | [#186](https://github.com/petemoore/sam-aarch64/pull/186) |  |  | docs/notes/bdos-trinity-fork-analysis.md, docs/notes/bdos-version-landscape.md, docs/notes/trinity-capabilities.md, reference/bdos/README.md |
| **i72** | **SAMDOS→B-DOS reorientation across living docs and build tooling** — B-DOS becomes the documented target DOS: samdos-file-io.md reframed (B-DOS = target, SAMDOS 2 = shipped boot DOS pending q10 swap); ARCHITECTURE.md + ROADMAP table reframed; src/sam_io.inc + src/trampoline.asm comments DOS-neutral (byte-identical built binaries); tools/build-disk gains -dos/-dos-name/-dos-load (default SAMDOS 2, byte-identical before). SAMDOS source citation trail restored (https://github.com/stefandrissen/samdos). Default/CI swap decision was q10 → executed in i75. | DONE | agent | [#187](https://github.com/petemoore/sam-aarch64/pull/187) |  |  | i62, docs/specs/samdos-file-io.md |
| **i75** | **B-DOS boot-disk swap execution: prove SimCoupé matrix green, flip shipped default** — tools/build-disk default DOS flipped from SAMDOS 2 to B-DOS AL 1.5a (reference/bdos/al-bdos15a.bin, name bdos, start address 32777); samdos2 byte-exact size guard re-keyed off reference path so SAMDOS 2 compatibility build stays fully functional. Local proof (plain floppy, no Atom Lite, SimCoupé v1.2.16): every gate passed. CI matrix boots B-DOS = formal proof. Resolved q10. | DONE | agent | [#191](https://github.com/petemoore/sam-aarch64/pull/191) |  |  | docs/notes/bdos-trinity-fork-analysis.md, docs/notes/m9-status.md |
| **i76** | **Host-side Go TUI editor prototype at SAM-faithful geometry — the functional UX authority for the Phase-2 editor** — Umbrella for the i76 TUI editor prototype phases: samscreen abstraction (tcell + PNG/SCREEN$ backends), i4-parity viewer, font-proof geometry evidence, config-lab, and rendering-rules spec. See docs/specs/editor-tui-prototype-design.md §8 for decisions. Children: i76a/b/c/d (all DONE). | DONE | agent |  |  |  | docs/specs/editor-tui-prototype-design.md, docs/specs/editor-rendering-rules-design.md, tools/editor-prototype/, tools/font-proof/, docs/notes/m9-status.md, i4, i5, i6, i41, i48 |
| **i76a** | **P1a: samscreen abstraction, tcell backend, PNG/SCREEN$ mockup, i4-parity viewer, -geometry matrix** — P1a of the i76 TUI editor prototype: samscreen abstraction + tcell terminal backend + PNG/SCREEN$ mockup backend + i4-parity read-only viewer + -geometry mode×font matrix (tools/editor-prototype). | DONE | agent | [#200](https://github.com/petemoore/sam-aarch64/pull/200) |  |  | tools/editor-prototype/, docs/specs/editor-tui-prototype-design.md, i4, i5, i6 |
| **i76b** | **P1b font-proof: five-pixel-font vendored; make font-proof screenshots 85×32 (6×6) vs 64×24 (ROM 8×8)** — P1b of the i76 TUI editor prototype: five-pixel-font vendored; make font-proof produces real-frame screenshots 85×32 (6×6) vs 64×24 (ROM 8×8) — the i6 geometry evidence. | DONE | agent | [#201](https://github.com/petemoore/sam-aarch64/pull/201) |  |  | tools/font-proof/, i6 |
| **i76c** | **P2 config-lab: tools/editor-prototype -config rendering lab over the real release .tbn at locked MODE 3 64×24** — P2 of the i76 prototype: interactive rendering lab (-config flag) over the real release .tbn at locked MODE 3 64×24. Delivered across PRs #202 (initial lab), #203 (v1.1 instruction truncation), #204 (v1.2 inline-if-fits + cursor-block demarcation); PR #204 is the completing increment. | DONE | agent | [#204](https://github.com/petemoore/sam-aarch64/pull/204) |  |  | tools/editor-prototype/configs/, q13 |
| **i76d** | **Rendering-rules spec landed; default-config recommendation tracked as q13** — docs/specs/editor-rendering-rules-design.md §3–§10 rendering-rules spec landed; default-config recommendation (tuned compressed.config) tracked as open question q13. | DONE | agent | [#217](https://github.com/petemoore/sam-aarch64/pull/217) |  |  | docs/specs/editor-rendering-rules-design.md, q13 |
| **i78** | **Source-structure preservation in the v2 editor region (blank lines, comment grouping, indentation)** — (a) IMPLEMENTED (PR #218): kind u8 sidecar discriminator (0=comment, 1=blank-run) + FlagTaggedSidecar header bit; KindBlankRun IR record; corpus round-trip assertion — all 109 fixtures green incl. release.s (1340 blank lines). Finding → i90: BSS section blanks not preserved through -flatten (pre-existing property). (b) Multi-line grouping DECIDED: one sidecar entry per source comment with embedded body newlines. (c) Indentation DEFERRED: gated downstream of i76 interface sign-off. | DONE | agent | [#218](https://github.com/petemoore/sam-aarch64/pull/218) |  |  | docs/specs/source-structure-preservation-design.md, docs/specs/comment-storage-design.md, i41, i48c, i39b, i85, i90 |
| **i79** | **samscreen.DefaultCLUT[5] palette fix: 18 (purple-blue) → 80 (true cyan)** — Found by PR #200 (P1a re-land) scoped review. Verified against SAM ROM power-on CLUT at 0x7d51 (two independent ROM copies): index 5 = 80 (true cyan), not 18; every other DefaultCLUT entry already matched the ROM. One-line fix in tools/editor-prototype/samscreen/palette.go; editor-prototype tests green. Affects MODE 4 mockup PNGs only; MODE 3 viewer (CLUT 0–3) unaffected. | DONE | agent | [#206](https://github.com/petemoore/sam-aarch64/pull/206) |  |  | tools/editor-prototype/samscreen/palette.go |
| **i80** | **Trinity network-interface emulation — ENC28J60 Ethernet SPI emulation for host-side Z80 netboot iteration** — Emulate Quazar Trinity ENC28J60 Ethernet (SPI via port &DE) so the SAM network stack can be iterated host-side. Go-first: enc28j60.go emulates Trinity microcontroller + ENC28J60 SPI (4 banks, 8KB RX/TX buffer, MAC/MII read-dummy). Real Z80 driver (src/netboot/encdrv.asm, trinload verbatim) runs against it; CI via netboot-z80 job. Landed: init/write/read/DHCP loopback verified (PR landed). Remaining: wire-I/O state machines (DHCP/TFTP transfer-loop) + end-to-end Pi boot on real Trinity (hardware-gated, CLAUDE.md §5). | DONE | agent | [#228](https://github.com/petemoore/sam-aarch64/pull/228) |  |  | docs/notes/trinity-capabilities.md, docs/specs/phase3-delivery-design.md, src/netboot/encdrv.asm, tools/netboot-oracle/z80/enc28j60.go, i58, i82, i83, i86, i92 |
| **i81b** | **Comment on upstream SimCoupé issue #28 offering our CI pass/fail idiom** — Offer our DI;HALT + -exitonhalt + printer-channel pass/fail detection idiom on simonowen/simcoupe#28. Note: our idiom routes results to the capturable printer port (src/print.asm), not a BC-return (SimCoupé cannot read BC externally). Pete posted the comment (simonowen/simcoupe#28 comment-4724317311). Tracking PR #370. | DONE | pete | [#370](https://github.com/petemoore/sam-aarch64/pull/370) |  |  | i81, tools/run-simcoupe.sh, src/print.asm |
| **i82** | **SAM TFTP client (RFC-conformant): RRQ/OACK/DATA/ACK client with ARP + RFC 1350/2347/2348/2349/7440 support** — trinload-evolved RFC-conformant TFTP client (RRQ pull). Closed by PR #244 (client boot disk netboot_client.asm), delivered across PR #210 (RFC research + trinload analysis), #225 (build_rrq/parse_oack via i92), #231 (transfer loop + SAS fix), #232 (origination front: ARP-for-server + RRQ send), #244. All host-verified over i80 emulation; HSAVE write-out is hardware-gated (Pete's Trinity test). RFC 2347 OACK compliance + the section-C fit gate were added post-close under i118 (PR #422). | DONE | agent | [#244](https://github.com/petemoore/sam-aarch64/pull/244) |  |  | docs/notes/tftp-protocol-research.md, docs/specs/phase3-delivery-design.md, src/netboot/netboot_client.asm, src/netboot/tftp_client_loop.asm, src/netboot/tftp_client_front.asm, tools/netboot-oracle/client/client.go, simonowen/trinload, i80, i83, i84, i86, i93, i118 |
| **i83** | **SAM TFTP server (Phase-3 PXE shipper) — serve spectrum4 kernel and Pi firmware over TFTP** — Phase-3 headline goal: SAM+Trinity netboots a Pi by serving boot files by name from one flat Trinity store. Design confirmed (Pete 2026-06-14): Model A (DHCP+TFTP server, model-general, any aarch64 Pi). Transfer loop landed + host-verified end-to-end (PR #230 — tftp_server_loop.asm vs Go ServerLoop, over i80 emulation; name→record seam is i93 bdos_seam). Pairs with i70 (firmware self-provisioning). Remaining: DHCP server (i86, mandatory for Pi-3+multi-client) + real B-DOS RST 8 HGTHD/HLOAD hook dispatch on Trinity (hardware-gated, Pete's test). | DONE | agent | [#230](https://github.com/petemoore/sam-aarch64/pull/230) |  |  | docs/plans/phase3-netboot-implementation-plan.md, docs/specs/phase3-delivery-design.md, docs/notes/pi-netboot-capture-analysis.md, src/netboot/tftp_server_loop.asm, tools/netboot-oracle/tftp/serverloop.go, i70, i80, i82, i84, i86, i89, i93 |
| **i84** | **Phase-3 end-to-end delivery design — integrating spec for the self-hosting boot/delivery loop** — Design confirmed (Pete 2026-06-14): docs/specs/phase3-delivery-design.md. Model A only: SAM = DHCP+TFTP server serving by name (all aarch64 TFTP-boot Pis). Bootstrap via Pete's patched SAM ROM (i87). q11 (Pi netboot mechanics) and q12 (delivery model) both RESOLVED as part of this design. New items spawned: i86 (DHCP), i87 (ROM dump). | DONE | pete | [#211](https://github.com/petemoore/sam-aarch64/pull/211) |  |  | docs/specs/phase3-delivery-design.md, docs/notes/tftp-protocol-research.md, i70, i80, i82, i83, i86, i87 |
| **i85** | **Whole-file ZX0 vs compact overlay — size measurement (one-shot)** — Measured: zx0(full interleaved release.s) = 201,587 B vs current hybrid 170,132 B (38,584 B overlay + 131,548 B zx0(comments)) → whole-file is +18.5% LARGER. Conclusion: do not rewrite for size; overlay earns its keep on assembly speed. Whitespace preservation tracked as i78. | DONE | pete | [#208](https://github.com/petemoore/sam-aarch64/pull/208) |  |  | docs/notes/comment-compression-research.md, i78, i57, i60 |
| **i86** | **Minimal SAM DHCP/bootp responder — netboot server's DHCP half** — Answer Pi boot ROM DHCP DISCOVER/REQUEST with OFFER/ACK: address from small pool, subnet, next-server (siaddr=SAM), PXEClient echo (opt 60), fixed 32-byte option-43 'Raspberry Pi Boot' blob, client UUID echo (opt 97); no opt-67. Confirmed from real capture (pi-netboot-capture-analysis.md). Responder loop LANDED + host-verified end-to-end: dhcp_loop.asm DORA (DISCOVER→OFFER, REQUEST→ACK, stable per-MAC lease) byte-for-byte vs Go authority dhcp/responder.go, over i80 emulated Trinity; wired to ci-netboot-z80. Remaining: end-to-end Pi boot on real Trinity (hardware-gated, CLAUDE.md §5). | DONE | agent | [#229](https://github.com/petemoore/sam-aarch64/pull/229) |  |  | docs/plans/phase3-netboot-implementation-plan.md, docs/specs/phase3-delivery-design.md, src/netboot/dhcp_loop.asm, tools/netboot-oracle/dhcp/responder.go, i80, i83, i84 |
| **i87a** | **SAMBOOT: run the one-shot dumper on the SAM to capture rom.bin + eeprom.bin** — Push the one-shot dumper onto the SAM via trinload and tftp-get rom.bin + eeprom.bin to the Pi/Mac. Agent-drivable: the capture is network-driven (trinload listener left running on the SAM; SAM reachable at 192.168.2.75) and read-only, so an agent runs it without a human in the loop — this is the same agent-driven loop samboot.md §1 describes. Captures the artifacts that unblock the offline analysis; the EEPROM capture is the mandatory pre-flash backup. | DONE | agent |  | i173 |  | docs/specs/samboot.md |
| **i87a-b1** | **Capture eeprom.bin (full 128 KB pre-flash backup) + rom0.bin from the real SAM** — DONE 2026-06-21: pushed the i173 dumper over trinload (SAM 192.168.2.75), TFTP-pulled eep0..eep7 -> eeprom.bin (131072 B, verified real: Trinity Network chunk + SAM MAC 02:54:52:49:4e:bc present) and rom0.bin (16384 B, low ROM). Stashed in ~/sam-archive/samboot-capture/. eeprom.bin is the mandatory pre-flash backup for i135c. | DONE | agent | [#520](https://github.com/petemoore/sam-aarch64/pull/520) |  | i135c, i87b |  |
| **i87a-b2** | **Recapture rom1.bin (patched ROM high 16 KB) after the dumper redesign** — rom1.bin was NOT captured 2026-06-21: the i173 dumper's dumper_read_rom1 scratch-page path crashed the SAM (page-0 clobber, see i188). Once the dumper is redesigned + emulation-verified (i188), re-run the capture to get rom1.bin so rom.bin = rom0.bin + rom1.bin is complete (unblocks i87b diff + i135c). | DONE | agent | [#528](https://github.com/petemoore/sam-aarch64/pull/528) | i188 | i190a, i87b |  |
| **i87b** | **SAMBOOT: document the SAM boot chain from the captured ROM/EEPROM + public source** — Diff rom.bin vs stock SAM ROM 3.0, confirm the EEPROM bootblock matches the public reproduction, and document the chip->bootblock->B-DOS handoff + where the injection hook attaches. | DONE | agent | [#543](https://github.com/petemoore/sam-aarch64/pull/543) | i87a-b1, i135a, i87a-b2 | i135c, i197 | docs/specs/samboot.md |
| **i88a** | **i88 6a — host-verifiable TLS-1.3 client: all crypto primitives + handshake bricks 1–5 + brick-6a state machine** — Full TLS-1.3 client host-verifiable slice: SHA-256, HMAC-SHA256, HKDF, HKDF-Expand-Label, ChaCha20, Poly1305, X25519, AEAD_CHACHA20_POLY1305, handshake bricks 1–5 (key-schedule, record-protection, transcript-hash, ClientHello, server-flight parser), Go authority, qsq extraction, 6a state machine. Delivered across PRs #269–#337; PR #335 completes 6a. All host-verified byte-for-byte vs Go authority. Brick 6b (bootable/hardware) stays in i88. | DONE | agent | [#335](https://github.com/petemoore/sam-aarch64/pull/335) |  |  | src/netboot/tls_client.asm, tools/netboot-oracle/tls/, docs/specs/tls13-handshake.md, i88 |
| **i88b** | **i88 6b — TLS-record reassembler: host-verifiable wire framing layer** — tls_reasm.asm (tls_reasm_feed) frames complete TLS records out of arbitrary TCP byte chunks (incremental consume; REASM_BUF sized to TLS-1.3 max 5+2^14+256). Go authority tools/netboot-oracle/tls/reassembler.go + tls_reasm_test.go: mis-aligned chunk sequences byte-exact. Remaining 6b (hardware-gated): wire into tcp_conn CONN_SINK_FILTER + paging layout + bootable disk + real RST 8 on Trinity. | DONE | agent | [#360](https://github.com/petemoore/sam-aarch64/pull/360) |  |  | src/netboot/tls_reasm.asm, tools/netboot-oracle/tls/reassembler.go, tools/netboot-oracle/z80/tls_reasm_test.go, i88 |
| **i91** | **Phase-3 netboot host oracle harness (tools/netboot-oracle/)** — Pure-Go DHCP + TFTP packet builders/parsers: frame (BuildUDPFrame), dhcp (OFFER/ACK + option-43 blob), tftp (RRQ/OACK/DATA/ACK/ERROR + serve-by-name + transfer-loop state machines), pcap (libpcap/pcapng reader), golden (masked committed vectors via cmd/gen-golden). Wired into CI as netboot-oracle + STATICCHECK_MODULES. Validates protocol logic only — Z80 execution, ENC28J60 I/O, and end-to-end Pi boot are not host-verifiable. | DONE | agent | [#219](https://github.com/petemoore/sam-aarch64/pull/219) |  |  | tools/netboot-oracle/, docs/plans/phase3-netboot-implementation-plan.md §6/§8.1, docs/notes/pi-netboot-capture-analysis.md, i82, i83, i86, i80 |
| **i92** | **Phase-3 netboot Z80 port of protocol logic (src/netboot/*.asm)** — SAM-side Z80 implementation of DHCP/TFTP packet builders/parsers, ported from i91 Go authority (tools/netboot-oracle/) and host-verified byte-for-byte via koron-go/z80 harness (ci-netboot-z80 job). Landed (all host-verified): build_udp_frame (#221), build_dhcp_reply (#222), build_oack/build_data/build_error, parse_request/resolve, build_rrq/parse_oack/find_option, build_arp_request. All packet build/parse logic ported. Remaining: wire-I/O state machines (ENC28J60 send/receive, DHCP/TFTP/transfer loops) — NOT host-verifiable; gated on i80/real-Trinity. | DONE | agent | [#221](https://github.com/petemoore/sam-aarch64/pull/221), [#222](https://github.com/petemoore/sam-aarch64/pull/222), [#223](https://github.com/petemoore/sam-aarch64/pull/223), [#224](https://github.com/petemoore/sam-aarch64/pull/224), [#225](https://github.com/petemoore/sam-aarch64/pull/225), [#226](https://github.com/petemoore/sam-aarch64/pull/226) |  |  | src/netboot/, tools/netboot-oracle/z80/, docs/plans/phase3-netboot-implementation-plan.md, docs/notes/pi-netboot-capture-analysis.md, i82, i83, i86, i91 |
| **i93a** | **B-DOS seam arithmetic: bdos_name_to_uifa / bdos_difa_to_size / bdos_fill_save_uifa — host-verified** | DONE | agent | [#233](https://github.com/petemoore/sam-aarch64/pull/233) |  |  |  |
| **i94** | **Phase-3 netboot bring-up smoke test (ARP responder)** — Tiny bootable program: drv_init on real Trinity then answers ARP requests for the SAM's IP. New build_arp_reply.asm primitive (port of frame.go::BuildARPReply) + smoke/smoke.go Go authority. Reads SAM MAC+IP from Trinity EEPROM via vendored eeprom.asm. Bootable disk via make netboot-smoke-disk. smoke_serve_once host-verified end-to-end over i80 emulation (5 cases incl. wrong-IP/non-ARP controls). EEPROM config read + real ENC28J60 timing + actual ARP round trip are hardware-gated. Delivery PRs #237 + #238; completing PR #238. | DONE | agent | [#238](https://github.com/petemoore/sam-aarch64/pull/238) |  |  | src/netboot/smoke_test.asm, src/netboot/build_arp_reply.asm, tools/netboot-oracle/smoke/smoke.go, docs/notes/netboot-trinity-testing.md, i80, i91, i92, i93 |
| **i95a** | **Integrated server dispatch loop (ARP+DHCP+TFTP) — Go authority + Z80 host-verified, bootable disk shipped** | DONE | agent | [#239](https://github.com/petemoore/sam-aarch64/pull/239), [#241](https://github.com/petemoore/sam-aarch64/pull/241) |  |  |  |
| **i96** | **Phase-3 serve-files TFTP demo (ARP + TFTP, no DHCP)** — Focused bootable demo server: ARP + TFTP only, testable from any stock tftp/curl client. Go authority serve/serve.go::Responder.OnFrame + Z80 serve_serve_once in netboot_serve.asm. Key addition over i95: bare RRQ (no options) answered per RFC 2347 with DATA block 1 directly; optioned RRQ takes OACK path. Demo files baked in (no B-DOS provisioning). Bootable disk via make netboot-serve-disk. Dispatch + bare/optioned RRQ paths host-verified; real ENC28J60 + stock TFTP client interop + on-hardware run are Pete's real-Trinity test. | DONE | agent | [#243](https://github.com/petemoore/sam-aarch64/pull/243) |  |  | tools/netboot-oracle/serve/serve.go, src/netboot/netboot_serve.asm, docs/notes/netboot-trinity-testing.md, i80, i83, i94, i95 |
| **i97** | **Deterministic autonomous-loop monitor (screen-driven, non-LLM)** — tools/autonomous-loop/monitor.sh: polls task-done semaphore, injects /context then a resume nudge (nudge is load-bearing — /context alone is local-only and does not invoke the model), agent picks next item or winds down at <20% free context. /clear+reprompt on wound-down for a clean reset. Hang timeout as residual watchdog. Screen window-0 targeting is load-bearing (screen -S <session> -p 0 -X stuff). PRs #251 (core), #252-#255 (robustness); marked DONE in PR #306 alongside q14 live-test confirmation. | DONE | agent | [#306](https://github.com/petemoore/sam-aarch64/pull/306) |  | i179 | tools/autonomous-loop/, docs/ROADMAP.md |
| **i98** | **Split the iN/qN registries by state (precursor idea, superseded by i105)** — Pete's idea (2026-06-15): restructure item + question registries so a fresh agent needn't read all-time history to see open work. Gated on a design-with-Pete session; that session happened and produced the agreed shape, executed as i105 (open/closed split + controlled status vocabulary + atomic items). PR #306 moved this row to DONE as part of the i105 registry restructure. | DONE | pete | [#306](https://github.com/petemoore/sam-aarch64/pull/306) |  |  | i105, docs/notes/item-registry-open.md, docs/notes/item-registry-closed.md |
| **i99** | **Stream netboot HTTP fetch body to storage (multi-MB firmware)** — Converts conn_accumulate into a bounded FLUSH_WINDOW buffer calling storage_sink_flush on threshold + FIN, never exceeding the window in RAM. Deletes the 4KB/16KB buffers + the overrun bug. q16 resolved (Pete): option (c) — multi-record spanning via bounded HSAVEs, sidesteps broken append hooks. Delivery sequence: PR #263 Go sink + Z80 port; #279 bodySink; #281 manifest sizes; #282 spanning Go authority; #283 fw_span Z80. Remaining (all hardware-gated): http_main migration onto streaming sink, serve-time reassembly, real RST 8 HSAVE/HLOAD — Pete's real-Trinity test. | DONE | agent | [#283](https://github.com/petemoore/sam-aarch64/pull/283) |  | i122b | src/netboot/netboot_http.asm, src/netboot/tcp_conn.asm, src/netboot/fw_span.asm, src/netboot/bdos_seam.asm, tools/netboot-oracle/tcp/conn.go, tools/netboot-oracle/bdos/span.go, docs/notes/sam-stub-audit.md, docs/specs/phase3-delivery-design.md, i70, i93, i24 |
| **i100a** | **i100 http_main host slice — Go authority http.Provisioner + Z80 Bricks 1-6** — Go authority http.Provisioner (PR #307) + Z80 http_main Bricks 1-6: Brick 1 compose + plan (#308), Brick 2 dynamic HTTP_PATH_PTR (#309), Brick 3 bodySink header-skip seam (#310), Brick 4 prov_start per-file re-init (#311), Brick 5 per-file store double store_begin/store_end (#313), Brick 6 prov_first/prov_onframe/prov_next download loop (#314). Every frame + verdict host-verified byte-for-byte vs Go Provisioner; bootable image byte-identical throughout. Bricks 1-6 PRs #307-#314; completing tracking PR #315. | DONE | agent | [#315](https://github.com/petemoore/sam-aarch64/pull/315) |  |  | tools/netboot-oracle/http/provision.go, src/netboot/http_main.asm, src/netboot/fw_source.asm |
| **i100b** | **i100 Brick 7 — bootable migration + real B-DOS HSAVE-per-record store leaf** — Un-guarded streaming sink + SHA-256 verify into the real bootable (NETBOOT_STREAM), added real storage_sink_leaf / store_begin / store_end in http_main.asm — each flushed FW_RECORD_CAP-byte window hashed then HSAVE'd to Trinity as record fw_span_record_name. Deleted legacy single-file bootable http_main + its 16KB STAGING. Bootable ends &ECED (27885 B, 4883 B free). Record cap = provisional 4096. Also fixed boot entry: jp http_main at &8000. HSAVE store leaf + EEPROM read ship unverified until Pete's Trinity test (CLAUDE.md §5). | DONE | agent | [#373](https://github.com/petemoore/sam-aarch64/pull/373) |  |  | src/netboot/http_main.asm, src/netboot/tcp_conn.asm, src/netboot/netboot_http.asm, src/netboot/fw_span.asm, tools/netboot-oracle/bdos/span.go, i100, i100a, i110 |
| **i102a** | **i102 measurement infra — cycle-exact T-state counter in koron-go/z80 harness** — Cycle-exact T-state counter in the koron-go/z80 harness (tools/netboot-oracle/z80/tstates.go, anchor-verified + error-on-unknown completeness) wired into CallResult.TStates, plus the recorded crypto baselines (tstates_baseline_test.go). The benchmark every later i102 opt measures against. | DONE | agent | [#316](https://github.com/petemoore/sam-aarch64/pull/316) |  |  | tools/netboot-oracle/z80/tstates.go, tstates_baseline_test.go, i102 |
| **i102b** | **i102 SHA-256 speedups — short-distance rotations and redundant push/pop removal** — Opt-1: short-distance rotations (whole-byte sha_rotr8/sha_rotl8 + a minimal bit residual + sha_shr8; deleted the bit-at-a-time sha_rotr_word_by); Opt-5: dropped the redundant push/pop in sha_add4 (callers already reload HL/DE). Documented opcodes only; NIST vectors byte-exact. Delivered in PRs #318 (−27.4%) + #322 (−2.7%); SHA-256 ~29.4% below baseline (6.45M→4.55M T). | DONE | agent | [#322](https://github.com/petemoore/sam-aarch64/pull/322) |  |  | src/netboot/sha256.asm, i102 |
| **i102c** | **i102 quarter-square 8x8 multiply — replaced shift-add loop with QSQ identity** — Replaced the mul8 shift-add loop with the quarter-square identity a*b = QSQ[a+b] - QSQ[\|a-b\|] (QSQ[n]=floor(n^2/4), a 1022-byte RAM table built by qsq_init via a pure-add recurrence). Opt-2: x25519; Opt-3: poly1305 (identical loop). Exhaustive TestMul8Exhaustive/TestPoly1305Mul8Exhaustive (all 65536 pairs) + RFC/e2e byte-exact. Delivered in PRs #319 (x25519 -23.0%) + #320 (poly1305 -9.5% / aead -11.5%); later DRY-extracted to qsq.asm. | DONE | agent | [#320](https://github.com/petemoore/sam-aarch64/pull/320) |  |  | src/netboot/qsq.asm, src/netboot/x25519.asm, src/netboot/poly1305.asm, i102 |
| **i102d** | **i102 chacha20 redundant push/pop removal — cc_add/cc_xor callers already reload HL/DE** — cc_add/cc_xor preserved HL/DE that every cc_qr caller reloads; dropping the push/pop also let cc_addstate's manual word-advance go (faster AND smaller). RFC 8439 + aead byte-exact. First i102 increment to shrink code. | DONE | agent | [#321](https://github.com/petemoore/sam-aarch64/pull/321) |  |  | src/netboot/chacha20.asm, i102 |
| **i102e** | **i102 x25519 structural opts — fe_sqr, fe_mul121665, IX/IY pointer hold** — Opt-6: dedicated symmetric squaring fe_sqr (496 off-diagonal products doubled + 32 diagonal terms, ~half a multiply); Opt-7: constant-aware fe_mul121665 (96 vs 1024 mul8 for the 3-byte a24); Opt-8: hold the schoolbook inner-loop pointers in IX/IY across mul8. Field pins + RFC 7748 + Go-curve25519 e2e byte-exact. Delivered in PRs #343 (-18.7%) + #345 (-8.6%) + #346 (-5.64%, -45 B); x25519 cumulative 1.604G->1.125G = -29.9%. | DONE | agent | [#346](https://github.com/petemoore/sam-aarch64/pull/346) |  |  | src/netboot/x25519.asm, i102 |
| **i102f** | **i102 x25519 Karatsuba field multiply — one level of Karatsuba over 32x32->64 schoolbook** — fe_schoolbook replaced its flat 1024-mul8 schoolbook with one level of Karatsuba: split at 16 bytes, three 16x16 sub-multiplies (kmul16) for z0/z2/middle (768 mul8), middle via the subtractive identity z1=z0+z2-(a_lo-a_hi)(b_lo-b_hi) with tracked sign bit, then multi-byte combine (kadd/ksub). fe_sqr left untouched. New TestFEMulRandom (1024 random pairs) + field pins + RFC 7748 + Go-curve25519 e2e all byte-exact. x25519 cumulative 1.604G->0.973G = -39.3%. | DONE | agent | [#354](https://github.com/petemoore/sam-aarch64/pull/354) |  |  | src/netboot/x25519.asm, tools/netboot-oracle/z80/x25519_field_test.go, i102 |
| **i102g** | **i102 mul8 micro-opts — drop internal push de/pop hl and dead ld d,0 before calls** — (1) drop mul8's internal push de/pop hl: saved operands B/C are dead once the difference is taken, so the second QSQ[\|a-b\|] lookup reuses BC for the table base+entry while QSQ[a+b] stays in DE; (2) remove the dead ld d,0 before every mul8 call across the x25519 + poly1305 byte-radix loops (mul8 reads only A and E). TestMul8Exhaustive + TestPoly1305Mul8Exhaustive (all 65536 pairs each) + RFC 7748 / RFC 8439 / Go e2e byte-exact. x25519 cumulative 1.604G->0.934G = -41.8%. | DONE | agent | [#355](https://github.com/petemoore/sam-aarch64/pull/355) |  |  | src/netboot/qsq.asm, src/netboot/x25519.asm, src/netboot/poly1305.asm, i102 |
| **i102h** | **i102 chacha20 register-based rotates — cc_rotl16/cc_rotl8 use registers, not cc_tmp4** — cc_rotl16/cc_rotl8 (the quarter-round's byte-permute rotates, each invoked ~80x per block: ROTL16 once + ROTL8 thrice via ROTL8/ROTL12/ROTL7) read the 4 word bytes into registers A/B/C/D and write them back permuted in place, instead of bouncing them through the cc_tmp4 memory temp via cc_load_tmp (now deleted — faster and smaller). RFC 8439 KAT + block/encrypt + AEAD + TLS-client composite byte-exact. chacha20_block -15.5% (345.5K->292.0K); chacha20_encrypt -15.3%; also speeds aead. | DONE | agent | [#356](https://github.com/petemoore/sam-aarch64/pull/356) |  |  | src/netboot/chacha20.asm, i102 |
| **i102i** | **i102 x25519 schoolbook carry in a register — ride carry in C through push bc/pop bc** — The multiply/square inner loops (kmul16, the Karatsuba 16x16; fe_sqr off-diagonal — together ~88% of x25519's mul8 calls) kept the running byte carry in memory (fe_car). The loops already push bc/pop bc around mul8 to preserve the djnz counter B; riding the carry in C through that same push/pop drops both memory accesses for free, and dest[i+j]+carry is folded into one add a,(iy+0). math/big field pins + TestFEMulRandom + TestFESqr* + RFC 7748 + Go-curve25519 e2e byte-exact. x25519 -6.6% (934.1M->872.4M). | DONE | agent | [#357](https://github.com/petemoore/sam-aarch64/pull/357) |  |  | src/netboot/x25519.asm, i102 |
| **i102j** | **i102 mul8 page-aligned table index — high-byte-only address formation for qsq_table** — qsq_table is page-aligned (low byte 0) and lookup index 2*(a+b) / 2*\|a-b\| is <= 1020, so each entry address is formed by adding only the high byte (ld a,qsq_table>>8; add a,h; ld h,a) instead of a full ld de,qsq_table; add hl,de — the index's low byte is already the address low byte. Saves ~12 T per mul8 call (~1.77M calls per x25519). Invariant documented at the qsq_table equ and guarded by exhaustive TestMul8Exhaustive/TestPoly1305Mul8Exhaustive (all 65536 pairs) — any non-page-aligned base fails CI. x25519 -2.7% (872.4M->848.7M); also speeds poly1305. | DONE | agent | [#358](https://github.com/petemoore/sam-aarch64/pull/358) |  |  | src/netboot/qsq.asm, i102 |
| **i102k** | **i102 reduction/constant-multiply carry in a register — fe_mul38_high and fe_121665_inner** — Applied the i102i carry-in-C pattern to the two remaining byte-radix multiply loops: fe_mul38_high (the x38 high-half fold, run on every fe_mul and fe_sqr reduction) and fe_121665_inner (the xa24 curve-constant multiply). Both kept carry in fe_car memory; now rides the existing push bc/pop bc in C, addend folded into one add. Completes carry-register conversion across all byte-radix multiply loops; fe_car scratch byte fully dead and removed. Field pins + TestFEMul121665 + RFC 7748 + Go-curve25519 e2e byte-exact. x25519 -0.5% (848.7M->844.5M). | DONE | agent | [#359](https://github.com/petemoore/sam-aarch64/pull/359) |  |  | src/netboot/x25519.asm, i102 |
| **i102l** | **i102 mul8 inlined at the two hottest x25519 sites — kmul16_inner and fe_sqr_inner** — The mul8 body (8x8->16 multiply, ~1.8M calls per x25519) was a called subroutine paying call+ret (27 T). Refactored into a pyz80 mul8_body MACRO (mul8 = mul8_body+ret) and expanded inline at kmul16_inner and fe_sqr_inner (~96% of calls). Branch over neg uses jr nc,$+4 so the macro carries no internal label. fe_sqr_diag + poly1305 + host field tests keep calling the subroutine. TestMul8Exhaustive (all 65536 pairs) + RFC 7748 + Go-curve25519 e2e + TLS-client composite headroom assertion byte-exact. x25519 -5.77% (844.5M->795.8M). | DONE | agent | [#363](https://github.com/petemoore/sam-aarch64/pull/363) |  |  | src/netboot/qsq.asm, src/netboot/x25519.asm, i102 |
| **i102n** | **i102 sha256 register-oriented rewrite — increment 1 (729,795->418,843 T/block, -42.6%)** — Replaced sha256_compress (memory-to-memory) with a register-resident design: inline add4/copy4 macros; sigma/Sigma rotates held in B/C/D/E (8T rr r, not 15T rr (hl)); simplified Ch/Maj; chained rotations; in-place W schedule. ABI byte-preserved. Fastest verified Z80 SHA-256 (timewave0/ZARC ~480-485k; oisee's 45k is fabricated AI code). q21 resolution: Pete 2026-06-16 decided to do the full rewrite + keep cryptographic SHA-256 (firmware from third-party CDN, SHA-256 pin is the integrity guarantee). NIST KAT + million-a + start.elf (2.98 MB) byte-exact. | DONE | agent | [#365](https://github.com/petemoore/sam-aarch64/pull/365) |  |  | src/netboot/sha256.asm, i102m, i100, i101 |
| **i102o** | **i102 sha256 8x circular-renaming unroll — increment 2 (418,843->377,371 T/block, -9.9%)** — Unrolls 64 rounds into an 8-phase group looped 8x: with circular renaming a..h never physically move (28-byte lddr shuffle gone — each phase hard-codes which wv_ slot is a..h). Generated flat (ordinary mnemonics, no pyz80 EQU FOR — sidesteps the EQU FOR*macro rewind bug) by tools/sha256-unroll-gen/gen_sha256_unrolled.py; drift-guarded byte-for-byte by regen_guard_test.go. Gated behind if defined(NETBOOT_TLS_CLIENT) — the TLS composite keeps the smaller rolled loop. Cumulative sha256 729,795->377,371 (-48.3%). NIST KAT + million-a + start.elf byte-exact. | DONE | agent | [#367](https://github.com/petemoore/sam-aarch64/pull/367) |  |  | src/netboot/sha256.asm, tools/sha256-unroll-gen/, i102m, i102n, i100, i101 |
| **i102p** | **i102 sha256 inline Ch/Maj — increment 3 (377,371->346,267 T/block, -8.2%)** — In the unrolled round path the operand slots for Ch(e,f,g) and Maj(a,b,c) are compile-time constants, so 8 generated phases now inline both instead of calling shared sha_ch/sha_maj subroutines. Inline: each operand loads with ld a,(nnnn) and the boolean is done in A with H,L,D as scratch (Ch 76 T/byte vs ~135; Maj 92 T/byte vs ~141). Emitted by gen_sha256_unrolled.py (ch_inline/maj_inline), drift-guarded by regen_guard_test.go. Shared subroutines untouched — the rolled TLS_CLIENT composite is byte-identical. Cumulative 729,795->346,267 (-52.6%). NIST KAT + million-a (15,625 blocks) byte-exact | DONE | agent | [#369](https://github.com/petemoore/sam-aarch64/pull/369) |  |  | src/netboot/sha256.asm, tools/sha256-unroll-gen/, i102m, i102n, i102o, i100, i101 |
| **i102q** | **i102 sha256 increment 4 — alternating sha_tmpa forward/backward walk (-1.0%)** — sha_bigsigma0/1/sha_sigma0/1 interleave st_bcde_tmpa + 1-2 xor_bcde_tmpa passes over the 4-byte sha_tmpa, each reloading ld hl,sha_tmpa (10 T). Since the intervening rotate macros preserve HL and XOR is per-byte order-independent, each pass after the first can chain from where the previous left HL: new xor_bcde_tmpa_bwd / xor_bcde_tmpa_fwd macros drop the reload. Saves 2 reloads each in Sigma functions (x64 rounds) + 1 each in sigma functions (x48 extend words) = -3520 T/block: 346,267->342,747 (-1.0%). NIST KAT byte-exact; 8x-unroll generator + regen guard untouched. | DONE | agent | [#375](https://github.com/petemoore/sam-aarch64/pull/375) |  |  | src/netboot/sha256.asm, tools/netboot-oracle/z80/tstates_baseline_test.go, i102m, i102p |
| **i102r** | **i102 sha256 increment 5 — register-accumulator round (342,747->315,355 T/block, -8.0%)** — T1/T2 accumulated in register quad B,C,D,E instead of memory-to-memory: each term folds in with BCDE += memory carry chain (~92 T, no write-back), so four intermediate T1 stores + both T2 stores are gone; only final new e/new a words touch memory. Ch rewritten to use A,H,L only; Maj parks in new sha_majs while S0 holds sha_tmpa. Emitted by gen_sha256_unrolled.py (rewritten phase_real), drift-guarded by regen_guard_test.go. Cumulative 729,795->315,355 (-56.8%). Also shrinks firmware-fetch bootable (http_main) by 478 B. Composite untouched. | DONE | agent | [#377](https://github.com/petemoore/sam-aarch64/pull/377) |  |  | src/netboot/sha256.asm, tools/sha256-unroll-gen/, tools/netboot-oracle/z80/tstates_baseline_test.go, i102m, i102q |
| **i105** | **Registry restructure — atomic items, status vocabulary, open/closed split** — Splits item + question registries into open/closed files so a fresh agent reads only the open work. Controlled status vocabulary (OPEN/DONE/BLOCKED:<what>/WONTFIX token-first). Atomic items: one row = one deliverable = one status; multi-deliverable rows split into sub-ids (i81→i81a/b, i76→i76a/b…). Resolving an item moves its row open→closed in the same PR. Codified in CLAUDE.md doc-lifecycle + ROADMAP handover pointers. Worked examples: i81 + i76. | DONE | agent | [#306](https://github.com/petemoore/sam-aarch64/pull/306) |  |  | docs/notes/item-registry-open.md, docs/notes/question-registry-open.md, CLAUDE.md, docs/ROADMAP.md |
| **i107** | **Registry status-cell discipline (process rule + retroactive sweep)** — Umbrella for the status-cell discipline work: the go-forward process rule (i107a) and the retroactive sweep splitting over-long rows into sub-items (i107b). Created 2026-06-20 (i153) so i107a/b have their required umbrella parent — the children predate the parent-integrity rule. | DONE | agent |  |  |  | CLAUDE.md, i105 |
| **i107a** | **Status-discipline process rule (go-forward codification)** — Documents that a status cell is the controlled-vocabulary token + a short phrase + an optional PR ref, never a multi-paragraph narrative. Multi-part work is split into sub-items the moment it is decided to be multi-part, not at a later audit. i105 had the rules but rows drifted into wall-of-text; this makes the timing + the anti-pattern explicit. Retroactive sweep is i107b. | DONE | pete | [#347](https://github.com/petemoore/sam-aarch64/pull/347) |  |  | docs/notes/item-registry-open.md, CLAUDE.md, i105, i107b |
| **i107b** | **Retroactive status-cell sweep — split over-long rows into sub-items** — Swept: i102 (Opt-1…Opt-8) → i102a-e in closed (PR #348); i70 (Brick 1-6) trimmed (PR #349; sequential stages, one outstanding part); i100 → i100a in closed (PR #350); i88 (TLS crypto + handshake bricks) → i88a in closed (PR #351). Each completing PR moves the done sub-part to the closed file and leaves the open umbrella terse. Final scan drained the 4 named walls; mild residuals (i73/i83) tracked as i108. Delivery PRs #348-#351; completing PR #351. | DONE | agent | [#351](https://github.com/petemoore/sam-aarch64/pull/351) |  |  | i107a, i108 |
| **i108** | **Light-trim mildly-accreted i73/i83 status cells (i107b scan residuals)** — Assessed both: i73 status was already terse (length lives in the item cell, not a status wall) — left as-is. i83 had a genuine moderate wall (design narrative + landed transfer loop) — trimmed ~2.9 KB to ~1.5 KB to a terse umbrella (Model A design-confirmed + landed loop PR #230 + i86-DHCP / hardware-RST 8 remainder), preserving every gating note. | DONE | agent | [#352](https://github.com/petemoore/sam-aarch64/pull/352) |  |  | i107b, i73, i83 |
| **i110** | **Fix netboot bootable boot-entry: jp *_main at &8000 in all four programs** — Each composed netboot program placed org &8000 at the first-included subroutine, not the *_main entry — so CALL 32768 landed on the wrong routine (smoke_test &8000=smoke_serve_once, netboot_server &8000=netboot_serve_once, netboot_serve &8000=serve_serve_once, netboot_client &8000=client_first). Fixed all four: jp <name>_main guarded if defined(NETBOOT_HOSTTEST)==0 placed right after org &8000. Host-test builds are byte-unchanged. HTTP bootable was fixed earlier in i100b. | DONE | agent | [#374](https://github.com/petemoore/sam-aarch64/pull/374) |  |  | src/netboot/smoke_test.asm, src/netboot/netboot_server.asm, src/netboot/netboot_serve.asm, src/netboot/netboot_client.asm, i100b |
| **i111** | **Memory-read coverage / generic dead-code finder** — Instrument the koron-go/z80 harness to record every byte read (opcode fetch + data load) while running the whole test corpus, then report bytes inside a build's code/data image never touched. Each unread region is a candidate: either genuinely dead (delete, reclaim code window) or a coverage gap (add a fixture). Phase 1 (flat payloads, paging-free): hook mac.Read/opcode-fetch + use .map symbol ranges. Phase 2 (paged binary): trace must record (physical page, offset) and hook the paging port. Exclude known-acceptable-unread ranges via MARKER SYMBOLS from the pyz80 .map — drift-proof. | DONE | agent | [#570](https://github.com/petemoore/sam-aarch64/pull/570) |  | i205 | tools/z80-test-harness-go/, i109, i73 |
| **i112** | **Restore the SAM opening screen that Colin's Trinity boot-ROM patch tramples** — Colin's custom ROM boots straight from Trinity (no B-DOS floppy) but overwrites the normal power-on opening screen. Restore by appending code to end of B-DOS (Trinity flash) that redraws the opening screen. The screen is not a clean re-callable subroutine — it is the inline RAINBOW SCREEN block at ROM &ED1B: set MODE 3, init PALTAB, build LINICOLS rainbow table, print MGT banner. Restoration = reproduce that draw in appended Z80, not call a routine. Touches non-redistributable B-DOS 1.5t Trinity fork; modifying Colin's B-DOS may warrant his input (Pete's call). | DONE | agent | [#494](https://github.com/petemoore/sam-aarch64/pull/494) | i135a |  | docs/sam/sam-coupe_rom-v3.0_annotated-disassembly.txt, i87, ~/sam-archive/bdos/analysis/ |
| **i113a** | **capture, OCR scrape, QA + discovery pipeline** — 51 photographs captured. OCR pipeline (ocr_docs.py, Tesseract + rotation auto-detect) run; text/ layer produced at 88–93% confidence. QA agent ran two-prong error-hunt (prose scan + data-point inventory). Discovery agent diffed against known facts and surfaced new findings. All three pipeline artifacts live in the off-repo ~/sam-archive/trinity-docs/ workspace (copyrighted; preservation-only). No repo PR — work is off-repo by policy. | DONE | agent |  |  |  | ~/sam-archive/trinity-docs/README.md, ~/sam-archive/trinity-docs/QA_REPORT.md, ~/sam-archive/trinity-docs/DISCOVERY_REPORT.md |
| **i114c** | **Netboot storage classes: flat-file vs trinity-sam-disks bootable raw-record (validate + HWSAD write seam)** — design §6.5 decision 5: store a fetched object as one of two classes by explicit namespace prefix (never inferred). default = flat-file archive (spanned per §3, never bootable). trinity-sam-disks/ prefix = SAM disk record: validate-before-commit (size==819200 AND BDOS stamp@byte232), reject on mismatch; a raw record is both bootable (HRECORD+boot) and servable-raw. Raw-record write seam: HWSAD (hook 149) per-sector write of the 1600-sector image, modelled in BDOSStore symmetric to HRSAD; SD write dispatch hardware-gated like HSAVE. Validation+dispatch host-verifiable. Unblocks i122. | DONE | agent | [#477](https://github.com/petemoore/sam-aarch64/pull/477) |  | i122, i122b | docs/specs/netboot-storage-manifest-design.md §6.5, docs/specs/samdos-file-io.md, reference/bdos/bdos15a.src.txt, tools/netboot-oracle/z80/bdos_store.go, i122 |
| **i114d** | **Content-addressed span-record naming + reference-sharing dedup** — design §6.3 decision 3: internal span-record naming is hash-derived (content-addressed) — a blob's records are keyed by its content SHA-256, so two projects fetching the identical file land on the same records and the §5 shared-files-by-reference dedup falls out for free (no separate dedup logic). Names are internal, never user-visible; the 7-char-prefix truncation is dropped. Go authority plus the content-hash record lookup that the i122 sha256-dedup-before-fetch uses. Host-verifiable. | DONE | agent | [#478](https://github.com/petemoore/sam-aarch64/pull/478) |  | i122, i122d | docs/specs/netboot-storage-manifest-design.md §6.3, tools/netboot-oracle/z80/sha256.asm, i122 |
| **i115** | **Registry as structured source — YAML-backed iN/qN registry with CLI validator + CI sync-check** — Registry as structured source — generate the iN/qN markdown views from validated YAML (registry/*.yaml) gated by a CLI validator + a registry-sync CI job, so a malformed edit is rejected at the iteration that introduces it (decay-proof, git-native tracking at scale). Model revised 2026-06-19: enum OPEN/IN_PROGRESS/DONE/WONTFIX (no BLOCKED — derived from depends_on edges), transient questions, dependency DAG core. | DONE | agent |  |  |  | docs/specs/registry-source-of-truth-design.md, i114, i117 |
| **i115a** | **Registry structured-source Phase 1 — tool skeleton** — Go tools/registry with validate + gen-to-buffer + round-trip test over a hand-written fixture. Dormant — no live source, no CI gate, cannot affect the live registry. Skeleton shipped (#437) on old schema; reworked to dependency-DAG model (no BLOCKED, depends_on, transient questions, invariants 11-13, 3 views). | DONE | agent | [#437](https://github.com/petemoore/sam-aarch64/pull/437), [#446](https://github.com/petemoore/sam-aarch64/pull/446), [#447](https://github.com/petemoore/sam-aarch64/pull/447), [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | tools/tables-gen, i115 |
| **i115b** | **Registry structured-source Phase 2 — generator + banner + header templates** — Emit the three registry .md views deterministically, pipe-escaped, with the GENERATED banner. The earlier parity-with-today's-.md proof is dropped — a no-information-loss round-trip replaces it. Generator built (PR #443, held) on the OLD schema + dropped parity test; reworked to the revised model (3 views, new status rendering, no BLOCKED). Delivered in #446/#451/#455. | DONE | agent | [#446](https://github.com/petemoore/sam-aarch64/pull/446), [#451](https://github.com/petemoore/sam-aarch64/pull/451), [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | i115 |
| **i115c** | **Registry structured-source Phase 3 — CLI mutators** — add / split / set-status / set-pr / dep / answer / next-id + the .id-ledger.txt no-reuse guard; the headless replacement for hand-editing giant table cells. CLI mutators landed in #447. | DONE | agent | [#447](https://github.com/petemoore/sam-aarch64/pull/447), [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | i115 |
| **i115d** | **Registry structured-source Phase 4 — migration + cutover** — Single atomic switchover: bounded-batch markdown→YAML migration (delete-as-you-go from .old registries, per-batch independent info-loss review), reshape wall-of-text rows into umbrella+leaves, generate the .md in place, wire the required registry-sync CI job. This work — the i115 family itself is the current batch. | DONE | agent | [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | i115 |
| **i115e** | **Registry structured-source Phase 5 — doc/automation rewiring** — Point CLAUDE.md (§3 + doc-lifecycle), ROADMAP (handover contract), the SessionStart hook, and the autonomous-loop docs at the YAML source; add the agent-guidance block. The completing PR deletes the plan. | DONE | agent | [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | i115 |
| **i115f** | **Registry structured-source — audited content reshape** — For each wall-of-text status/description cell: decompose into atomic single-status items OR relocate narrative history to PR/git/design-doc; independently audit for information loss (implementer ≠ reviewer). Under the 2026-06-19 model revision the reshape also converts BLOCKED→depends_on edges and retires the closed-questions view. q28 RESOLVED: it IS the bounded-batch migration (merged with i115d). | DONE | agent | [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | i115, i115d |
| **i115g** | **Registry structured-source — priority queue + ready/prioritize on top of the dependency DAG** — Open pullable items (leaves/childless; umbrellas excluded) become a committed priority order that agents pull from and sessions re-rank; plus depends_on edges as a cycle-free DAG whose topological extension the order must respect. A ready query returns open items with all deps satisfied, in priority order. Supersedes ROADMAP prose 'proposed sequence'. Later phase — builds on the DAG core in i115. | DONE | agent | [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | docs/specs/registry-source-of-truth-design.md, i115 |
| **i118a** | **OACK handling code — Go/Z80/host-side compliance (PR #422)** — RFC 2347 OACK compliance: on OACK learn server endpoint + adopt negotiated blksize (512 default if absent) + send ACK block 0; on direct DATA use 512 default; drop windowsize from RRQ. Mirrored in Go authority (tftp.go, clientloop.go) + host tests. Z80 side: netboot_client.asm, tftp_client_front.asm. All host tests green. | DONE | agent | [#422](https://github.com/petemoore/sam-aarch64/pull/422) |  |  | tools/netboot-oracle/tftp/tftp.go, tools/netboot-oracle/tftp/clientloop.go, src/netboot/netboot_client.asm, src/netboot/tftp_client_front.asm |
| **i119a** | **B1 — record-layer model + Z80 raw-sector read (harness CardModel)** — B1: established the record-layer model in the harness (CardModel) and implemented Z80 raw-sector read, the prerequisite for card-absolute list-sector access. | DONE | agent | [#440](https://github.com/petemoore/sam-aarch64/pull/440) |  |  |  |
| **i119b** | **B2 — per-record inspect primitive bdos_inspect_record (stamp@232 + disk-label@210)** — B2: implemented bdos_inspect_record — reads the BDOS stamp at byte 232 and the disk label at byte 210 from a record's own sector. Host-verified. The safety check backing the mandatory show-name + confirm-before-overwrite gate. | DONE | agent | [#442](https://github.com/petemoore/sam-aarch64/pull/442) |  |  |  |
| **i119c** | **B3 — card-absolute list-read: read list sectors 1..base-1, parse 16-byte entries, free ⇔ masked first-name-byte==0** — B3: implement the card-absolute list-read routine. Read list sectors 1..base-1 sequentially, parse each 16-byte entry per the frozen on-card layout (free ⇔ masked first-name byte == 0, per docs/specs/trinity-record-detection-design.md §3 and bdos15a.src.txt:946-948). Build against the harness CardModel first (emulation-first); no HRECORD, no error 81, no B-DOS internal addresses. | DONE | agent | [#461](https://github.com/petemoore/sam-aarch64/pull/461) |  |  | docs/specs/trinity-record-detection-design.md, i119 |
| **i119d** | **B4 — record-selection UX: list / manual-pick / auto-pick-free + show-name + overwrite-confirm** — B4: the user-facing record picker — enumerate records (using B3's list-read), show names, let the user manually pick or auto-select first free, then display the chosen record's name (or 'free/unnamed') and require confirmation before any write. Needs the i138 keyboard-sysvar stub to exercise the keypress flow in emulation. | DONE | agent | [#465](https://github.com/petemoore/sam-aarch64/pull/465) | i138 | i143 | docs/specs/trinity-record-detection-design.md, i119 |
| **i119e** | **B5 — wire into client_main (replace record-0=floppy bug) + full emulation E2E; HSAVE write-out hardware-gated** — B5: integrate B3 list-read + B4 record picker into client_main, replacing the record-0=floppy selection bug with the correct HRECORD record-n>=1 -> HSAVE flow. Run a full E2E test in emulation (harness CardModel + BDOS write-out model + i138 key injection). Also resolve the filename >10-char UIFA-name truncation (i119 gap 4). The HSAVE write-out to real hardware and the raw-SD list read stay hardware-gated. | DONE | agent | [#466](https://github.com/petemoore/sam-aarch64/pull/466) |  |  | src/netboot/netboot_client.asm, src/netboot/bdos_seam.asm, docs/specs/samdos-file-io.md, i119, i99 |
| **i121a** | **WRQ parse + handshake (ACK-0 / OACK)** — Opcode dispatch at netboot_serve.asm WRQ branch -> handle_wrq learns client endpoint; bare WRQ -> build_ack0 (new 4-byte 00 04 00 00 builder in tftp_build.asm); optioned WRQ -> OACK (reuse build_oack, echo blksize/tsize). Go authority: serverloop.go StartWrite + serve.go OnFrame WRQ branch. Host test: inject WRQ -> assert ACK-0/OACK byte-for-byte vs Go authority. Not gated; clean first deliverable. | DONE | agent | [#490](https://github.com/petemoore/sam-aarch64/pull/490) |  | i121b, i133 |  |
| **i121b** | **WRQ receive-to-staging (DATA->ACK loop)** — Wire tftp_recv_data (i82, tftp_client_loop.asm) into the WRQ transfer: DATA -> ACK each -> accumulate into STAGING -> short-block end. Resolve the SERVER_*/CLIENT_* var-name collision between tftp_client_loop.asm and netboot_serve.asm. Go authority: serverloop.go receive path. Host test: inject WRQ+DATA1..N, assert an ACK per block + STAGING holds the file. | DONE | agent | [#529](https://github.com/petemoore/sam-aarch64/pull/529) | i121a | i121c |  |
| **i121e** | **WRQ graceful termination — return control to trinload (sentinel + Esc escape)** — Escape hatch so the accept-in server hands control back to trinload (the only code-exec path until the auto-boot BIOS exists). A reserved sentinel filename pushed via 'tftp put' (e.g. tftp.done, empty) sets a stop flag -> sv_serve_loop breaks -> RET to trinload's pushed return addr; plus a non-blocking keyboard Esc poll (mirrors netboot_dumper.asm Esc-to-exit) for attended manual recovery. Needed so the next program can run after a batch of disk-image pushes. Emulation-gated (a hardware crash is unrecoverable). Home: netboot_serve.asm (i96). | DONE | agent | [#529](https://github.com/petemoore/sam-aarch64/pull/529) |  |  |  |
| **i121f** | **WRQ disk-record push path — stream a pushed .mgt into a free bootable Trinity record (PRIMARY use case)** — Pete's PRIMARY use case (2026-06-21): tftp put a .mgt disk image (819200 B) under the trinity-sam-disks/ namespace -> the SAM streams it into a free raw Trinity record (bootable + servable-raw, manifest design sec 6.5), validating size==819200 + BDOS stamp@232. Cannot stage whole (819200 B doesn't fit RAM), so the WRQ receive dispatch (i121b) feeds the i122b raw_record_sink (bdos_write_record / HWSAD) sector-by-sector; free record via i119 bdos_find_free_record; ERROR(3) if none free or validation fails. Reuses the i122 store side + i121a handshake; refactors handle_data to feed a pluggable sink. Distinct from i121c (the DEFAULT flat-file HSAVE archive path, secondary). Host-verifiable via BDOSStore SectorWrites; real SD write hardware-gated. | DONE | agent | [#529](https://github.com/petemoore/sam-aarch64/pull/529) |  |  |  |
| **i121g** | **WRQ record-claim — write the Trinity record-list name entry so a pushed record is marked used (enables BATCH pushes)** — BLOCKER for Pete's batch use case (push many .mgt images). i121f streams a pushed image into a FREE record but does NOT write the central record-list name entry, and bdos_find_free_record keys 'free' off the list name byte==0 -> a pushed-but-unnamed record still reads as free, so a SECOND push overwrites the FIRST (every push collides on the lowest free record). Each VALID push must CLAIM its record by writing a record-list name entry (mirror B-DOS new.rec/RENAME: LDIR a 10-char name from the WRQ filename into the 16-byte entry + write the list sector) AFTER validation passes (the clean commit point). Host-modelable logic (build entry + place name); the card-absolute list-sector WRITE dispatch is hardware-gated (ports &DC-&DF, same gate as the list read) -> add a BDOSStore list-write capture to host-verify. Without it only a SINGLE push-then-boot works. Discovered during i121f (2026-06-21). | DONE | agent | [#529](https://github.com/petemoore/sam-aarch64/pull/529) |  |  |  |
| **i121h** | **Config-block strategy: vessel-agnostic free-record placement (highest/lowest/explicit) read at launch** — Pete's design (2026-06-21): the WRQ server program is a vessel-agnostic library+entry that reads its record-placement STRATEGY from a small config block in memory, NOT hardcoded. The packaging vessel (trinload code-block or .mgt disk) populates that block; the program is identical either way. Config block (trailing, fixed label): magic/version + strategy byte (0=highest-free DEFAULT per s3 manifest-decision-4, 1=lowest-free, 2=explicit) + explicit-record word (0xFFFF=undefined -> future SAM prompt). bdos_find_free_record becomes strategy-aware (scan N..1 / 1..N / use named record). Resolves the i121f lowest-free vs documented s3 highest-free discrepancy by making it config-driven, default highest-free. Read once at launch (no routine disk re-read). Host-verifiable. | DONE | agent | [#529](https://github.com/petemoore/sam-aarch64/pull/529) |  |  |  |
| **i122a** | **Boot-a-record primitive: HRECORD-select + ALHK (hook 136) auto-load/run the record's AUTO file** — i122 brick 1: the boot-a-record primitive. bdos_boot_record HRECORD-selects a record then fires ALHK (hook 136, &88) to auto-load + run that record's AUTO file — the B-DOS BOOT "DOS resident" branch (KEYBOARD_BOOT_WORKAROUND.md §2: RST 8 / DEFB ALHK) / HAUTO (bdos15a.src.txt:463). ALHK modelled in BDOSStore symmetric to the existing hooks (Boots() accessor); host-verified via the real RST 8 dispatch (TestBDOSBootRecord). ALHK never returns on hardware (boots into the AUTO file); SD-boot dispatch hardware-gated like HSAVE (CLAUDE.md §5). Unblocks i122c. | DONE | agent | [#480](https://github.com/petemoore/sam-aarch64/pull/480) |  | i122c | src/netboot/bdos_seam.asm, tools/netboot-oracle/z80/bdos_store.go, reference/bdos/bdos15a.src.txt:463, i122 |
| **i122b** | **Raw-record streaming write: i99 streaming sink -> HWSAD into one 819200-byte record** — i122 brick 2: wire the i99 streaming sink to flush a fetched disk image into one raw Trinity record via i114c's HWSAD seam (bdos_write_record). The 819200-byte image far exceeds one STAGING buffer, so it must stream sector-by-sector as it arrives rather than buffer-then-save. Builds on i99 (bounded flush window) + i114c (HWSAD write seam). Host-verifiable via the BDOSStore SectorWrites capture; real SD write dispatch hardware-gated like HSAVE. Unblocks i122c. | DONE | agent | [#497](https://github.com/petemoore/sam-aarch64/pull/497) | i114c, i99 | i122c | src/netboot/bdos_seam.asm, src/netboot/body_sink.asm, tools/netboot-oracle/z80/bdos_store.go, i122 |
| **i122c** | **Fetch->validate->raw-write->boot orchestration entry + harness E2E** — i122 brick 3: the integration entry that ties the bricks together — TFTP-fetch a named disk image (i82 receive loop), validate it as a raw Trinity disk record (i114c bdos_validate_disk_record: size==819200 AND BDOS stamp@232, reject on mismatch), stream-write it into a record (i122b), then ALHK-boot it (i122a). Plus a harness E2E test driving the whole path. No control return on success. Real SD write + boot dispatch hardware-gated; orchestration + validation host-verifiable. | DONE | agent | [#498](https://github.com/petemoore/sam-aarch64/pull/498) | i122a, i122b | i122d, i182 | src/netboot/netboot_client.asm, docs/notes/netboot-trinity-testing.md, i122 |
| **i122d** | **sha256-dedup-before-fetch: skip fetch + boot existing copy if content hash already on card** — WONTFIX (Pete, 2026-06-23, q35): content-hash dedup of stored disk images is dropped as YAGNI. Rationale: the only benefit was SD-card SPACE — but a ~32GB card holds ~40,000 disk images at 819200 B each, while the entire published SAM catalogue is ~1,000 disks (<1GB). Space will never be the constraint, so the dedup machinery (content-addressed naming, name->hash manifest, streaming chunk-compare, copy-on-write on divergence, dedup-reference bookkeeping against the shared-storage confirm gate) is pure complexity for zero real benefit. The simple, safe model stands: fetch/receive -> find a free record (i119 detection) -> mandatory show-name + confirm-before-overwrite (trinity_storage_shared_resource) -> store. If re-fetch TIME ever becomes a real annoyance, a trivial 'already have this name, boot local' check could be added later without any content-addressed machinery — track then, not now. | WONTFIX | agent |  | i114d, i122c |  | tools/netboot-oracle/z80/sha256.asm, src/netboot/fw_span.asm, i122 |
| **i124a** | **boot-path emulation — EEPROM + ROM1 model; smoke_main + client_main end-to-end (PR #423)** — EEPROM modeled (eeprom.go), ROM1 mapped read-only at &C000 at boot, paging model in harness.go. smoke_main and client_main both run end-to-end in the harness: client_main logic confirmed sound (reaches client_first, emits ARP). Localized i82 hang to proactive ARP TX before PHY link-up (→ i127, hardware-fixed; → i131, emulation regression). | DONE | agent | [#423](https://github.com/petemoore/sam-aarch64/pull/423) |  |  | tools/netboot-oracle/z80/eeprom.go, tools/netboot-oracle/z80/harness.go, tools/netboot-oracle/z80/netboot_boot_test.go |
| **i124b** | **emulation completeness — PHY link-up model, SD/RST-8 stub, delete NETBOOT_HOSTTEST carve-outs** — Three remaining gaps in the emulation layer: (1) model PHY link-up (PHSTAT2.LSTAT + drop-TX-while-down) so the pre-link ARP drop is reproducible in emulation; (2) SD/RST-8 stub so full client fetch runs harness-end-to-end; (3) fold server/serve/http wrappers into the harness and delete NETBOOT_HOSTTEST carve-outs (→ i126). Together these make the emulator a complete, trusted gate for every netboot path. | DONE | agent | [#469](https://github.com/petemoore/sam-aarch64/pull/469) |  |  | tools/netboot-oracle/z80/enc28j60.go, tools/netboot-oracle/z80/harness.go, src/netboot/bdos_seam.asm, i126, i127, i131 |
| **i125** | **Section-C fit gate for every bootable netboot program (build + CI)** — A bootable netboot program loads at &8000; &C000-&FFFF is ROM1 at boot, so code/data above the budget is never written to RAM (the i119 over-size class). Generalised the down-payment client check to all *_boot.bin targets via shared script tools/netboot-boot-fit-check.sh: smoke/serve/server/client gated to 16384 (section C), http to 32768 (section-D overlay per phase3-delivery-design.md §13.1). Wired into CI by adding netboot-server-boot + netboot-serve-boot to ci-netboot-z80 so an over-size bootable image fails CI, not hardware. | DONE | agent | [#433](https://github.com/petemoore/sam-aarch64/pull/433) |  |  | tools/netboot-boot-fit-check.sh, Makefile, docs/specs/phase3-delivery-design.md §13.1, i119, i124 |
| **i127** | **netboot client: transmit the ARP only once the ENC PHY link is up** — client_main broadcast its proactive ARP immediately after drv_init, but drv_init never waited for PHY link-up; on real hardware the PHY silently dropped the first ARP, causing a livelock (flickering-border, silent-wire symptom). Fix (a): drv_wait_link (enc_link.asm) polls PHSTAT2.LSTAT over the MII until link is up, called in client_main between drv_init and client_first. Built + emulation-verified PR #424; HARDWARE-CONFIRMED 2026-06-19 rec 13. Fix (b) ARP-retransmit-on-timeout NOT implemented — (a) alone is sufficient. Emulator model + regression landed as i131 (PR #427). | DONE | agent | [#424](https://github.com/petemoore/sam-aarch64/pull/424) |  |  | src/netboot/enc_link.asm, src/netboot/netboot_client.asm, tools/netboot-oracle/z80/enc28j60.go, i124, i82, i119, i128, i131 |
| **i128** | **Apply drv_wait_link to http_main (the 2nd proactive netboot transmitter)** — One-line mirror of the hardware-confirmed i127 client fix: wait for PHY link after drv_init before prov_first's ARP, with a yellow-border (6) fail path. Emulation regression http_main_boot_test.go (TestHTTPMainBootReachesFirstTX + TestHTTPMainRecoversFromLinkDownStart, flat-loaded) mirrors the client's; the silent-wire model (i131) makes removing the fix go red. | DONE | agent | [#432](https://github.com/petemoore/sam-aarch64/pull/432) |  |  | src/netboot/http_main.asm, src/netboot/enc_link.asm, tools/netboot-oracle/z80/http_main_boot_test.go, i127, i70, i82 |
| **i129** | **trinload stage-one bootstrap disk + bring-up flow (de-risk disk-shipping)** — Bootable trinload.mgt (Simon Owen's trinload, ~/git/trinload, BSD-ish; encdrv.asm/eeprom.asm vendored from it). .mgt built + written to Trinity records 128 and 3; push→run→return HARDWARE-CONFIRMED 2026-06-19. Vendor-in-repo Q settled by i132. Two uses: (a) load+run netboot tools over wire for dev iteration; (b) fallback transport if our client/server stalls. Done via hardware bring-up — no work PR; #426 is the tracking proxy. NB: trinload runs code in RAM; persisting disk images still needs i119 write-out. | DONE | agent | [#426](https://github.com/petemoore/sam-aarch64/pull/426) |  |  | ~/git/trinload/trinload.asm, tools/build-disk/, i82, i119, i121, i130 |
| **i130** | **Name every unnamed-but-occupied Trinity record so none gets accidentally wiped** — Card (Atom Lite, 65535 records) holds Pete's collection — most occupied records are UNNAMED in samdisk list (present in samdisk dir), so a list-gap looks free when it isn't. Completed 2026-06-19: 805 records named via Python L<rec> bulk label write; method validated on rec 128 first, then bulk over 1-850, 0 data failures, all verified. Cosmetic: ~424 show a trailing leftover char (samdisk copy writes only 10-byte label; harmless). Completed as a direct Python bulk-rename on Pete's machine; PR #426 (tracking-consolidation) is the closest registry-update proxy. | DONE | pete | [#426](https://github.com/petemoore/sam-aarch64/pull/426) |  |  | ~/git/samdisk, i129 |
| **i131** | **Feed hardware-confirmed PHY-link-up behaviour back into the emulator (i127 rule-7 loop)** — enc28j60.go doTransmit drops a frame while PHY link is down (still signalling TXIF/TXRTS — the datasheet behaviour hiding the loss), atop the already-modelled PHSTAT2.LSTAT down-then-up read (PR #424). Tests: TestDrvWriteSilentWireWhileLinkDown (drv_write while link down egresses nothing, then egresses once up) + TestClientBootRecoversFromLinkDownStart (fixed client_main with link starting down egresses its one ARP; FirstTXOps self-calibrates so removing the fix → 0 frames → red). Whole transmit-before-link-up class caught in emulation; link-up timing stays hardware-gated (CLAUDE.md §5). | DONE | agent | [#427](https://github.com/petemoore/sam-aarch64/pull/427) |  |  | tools/netboot-oracle/z80/enc28j60.go, tools/netboot-oracle/z80/enc28j60_test.go, tools/netboot-oracle/z80/netboot_boot_test.go, tools/netboot-oracle/z80/README.md, i127, i124, i126 |
| **i132** | **Run trinload itself in the emulation harness — validate the push→run→return cycle** — Load trinload.bin (org &6000, vendored at src/netboot/trinload.asm) in the koron-z80 harness with the i124 ENC + EEPROM emulation; inject the trinload UDP protocol (? → ! reply; @+page+offset+data → LDIR; X+page+addr → pushed program executes + RETs to trinload start). Confirms control returns after the pushed program RETs (try_exec pushes hl=start before jp hl). De-risks every future trinload push. Ships: vendored trinload.asm, make netboot-trinload, TestTrinloadPushRunReturn + TestTrinloadDataWriteToOffset, LoadAt/StopPC/StopPCSkip in harness.go, HMPR snoop in enc28j60.go. | DONE | agent | [#431](https://github.com/petemoore/sam-aarch64/pull/431) |  |  | tools/netboot-oracle/z80/trinload_test.go, src/netboot/trinload.asm, i124, i126, i129, i133 |
| **i134** | **Harness SD/B-DOS write-out model: RST-8/HRECORD/HSAVE in emulation (prerequisite for emulation-testing i119)** — Generic RST-vector hook in harness.go (rstHandlers — PC intercept at RST target, runs handler, resumes past inline operand) + bdos_store.go (BDOSStore + Machine.AttachBDOS) modelling HRECORD (record select) + HSAVE (capture+decode 48-byte UIFA at IX). Tests: mechanism test (bdos_select_record/bdos_save_hook → UIFA captured) + TestClientBootWriteOutCaptured (real client_main through TFTP transfer to write-out). Reproduces i119 record-selection bug in emulation (record 0 = floppy selected). Finding: filename >10 chars truncates in SAM UIFA. Models digital dispatch only (CLAUDE.md §5). | DONE | agent | [#429](https://github.com/petemoore/sam-aarch64/pull/429) |  |  | tools/netboot-oracle/z80/bdos_store.go, tools/netboot-oracle/z80/bdos_store_test.go, tools/netboot-oracle/z80/harness.go, tools/netboot-oracle/z80/README.md, src/netboot/bdos_seam.asm, i119, i124, i126 |
| **i135a** | **SAMBOOT: understand the Trinity EEPROM bootblock from public source + locate the boot-a-record injection hook** — Read LongSteve/z80 boot.asm (Colin's SAM Revival 23 bootblock, public) + the forked-B-DOS EEPROM chunk map; document reset->bootblock->B-DOS load, the unfinished auto-load-file TODO as the boot-a-record hook, and the MGT-stripes redraw. No hardware. | DONE | agent | [#489](https://github.com/petemoore/sam-aarch64/pull/489) |  | i112, i135d, i173, i176, i87b | docs/specs/samboot.md, https://github.com/LongSteve/z80/blob/main/boot.asm |
| **i135d** | **SAMBOOT: emulation-prototype the EEPROM bootblock injection (stripes + BIOS + boot-record); folds i112** — Patch the EEPROM bootblock in the koron-z80 harness (emulation-first): always redraw the MGT stripes, read the SAMBOOT BIOS config, auto-boot the configured Trinity record or fall through to BASIC. Folds i112 (opening-screen restore). | DONE | agent | [#494](https://github.com/petemoore/sam-aarch64/pull/494) | i135a, i176 | i135c | docs/specs/samboot.md |
| **i136** | **Autonomous-loop monitor: resume nudge lost to /context TUI redraw** — Pete hit a ~1h stall 2026-06-19: only /context arrived after a merged item; the agent sat idle until woken by hand. TASK_DONE branch stuffed the RESUME_NUDGE immediately after /context, but /context is a local command that redraws the whole TUI — nudge keystrokes stuffed during the redraw were lost (same stall class as earlier /context-alone-never-resumed bug). Fix: sleep ALOOP_CONTEXT_SETTLE (default 5s) between the /context submit and the nudge stuff. The 1s SUBMIT_SETTLE inside submit() was too short to outlast the render. | DONE | agent | [#428](https://github.com/petemoore/sam-aarch64/pull/428) |  |  | tools/autonomous-loop/monitor.sh, tools/autonomous-loop/README.md |
| **i138** | **Emulator keyboard-input support: stub LASTK/FLAGS/KSTATE sysvars to feed keypresses without interrupts** — The Go/koron-z80 harness never fires interrupts or models the keyboard, so a program awaiting a keypress (the i119 record-selection UX) can't run in emulation. Cheap approach (Pete's pointer): stub the keyboard sysvars the interrupt populates — LASTK (0x5C08) / FLAGS bit 5 (new-key) / KSTATE — inject keys directly, bypassing 50 Hz interrupt + key-matrix decode. Precedent: BASIC ROM-emulation spike (blob c0f62fa) intercepts LASTK/FLAGS to feed a key queue; samfile's etunes uses a port-level stub. Full interrupt emulation is the heavier fallback. Foundation for editor-era input (i48c/i41). | DONE | agent | [#463](https://github.com/petemoore/sam-aarch64/pull/463), [#463](https://github.com/petemoore/sam-aarch64/pull/463) |  | i119d | tools/netboot-oracle/z80/, tools/z80-test-harness-go/, i119, i126, i48c, i41 |
| **i139** | **Autonomous-loop monitor: CONTEXT_SETTLE 5s too short → task-done resume nudge dropped** — After touch task-done, monitor stuffs /context then (after CONTEXT_SETTLE) stuffs RESUME_NUDGE. /context is a full-TUI redraw; with settle at 5s the nudge landed mid-render and was dropped, leaving agent idle until ~1h hang-timeout. Fix: bumped CONTEXT_SETTLE default 5→30 (matches proven CLEAR_SETTLE). PR #439 landed this fix. Superseded by i140: the longer sleep did not fully resolve the issue — root cause was burst-size, not a render race. A running monitor must be restarted to apply the CONTEXT_SETTLE change. | DONE | agent | [#439](https://github.com/petemoore/sam-aarch64/pull/439) |  |  | tools/autonomous-loop/monitor.sh, tools/autonomous-loop/README.md, i136, i140 |
| **i140** | **monitor.sh task-done nudge not delivered — second message after /context is dropped** — Root cause (reproduced in isolated test screen session): a long string stuffed as one screen -X stuff burst right after /context full-TUI redraw is dropped wholesale (0/4 delivered) — not a render race. Fixed: stuff now chunks every payload into small bursts; task-done branch uses nudge_until_turn (verify-and-retry, polls screen for 'esc to interrupt' turn marker). Verified end-to-end in isolated session ('nudge delivered (attempt 1, 1s)', agent took a real turn, twice). PR #444. Supersedes i139's CONTEXT_SETTLE approach. | DONE | agent | [#444](https://github.com/petemoore/sam-aarch64/pull/444) |  |  | tools/autonomous-loop/monitor.sh, tools/autonomous-loop/monitor-nudge-delivery.md, tools/autonomous-loop/README.md, i139, i97 |
| **i141** | **B3h — raw SD CMD17 card-absolute list-sector read on ports &DC-&DF (hardware gate for B3)** — Hardware implementation of the card-absolute list-sector read modelled in B3 by the harness hook BD_HOOK_LISTREAD. Implements the SPI command ladder (CMD17 single-block read) on Trinity ports &DC-&DF to read list sectors 1..base-1 directly off the SD card. Not required for B3 emulation; the harness models the returned bytes. Needs final confirmation of the exact SPI init/read sequence on real hardware before building (docs/specs/trinity-record-detection-design.md §8, open-verification-item-1 and §3 frozen-interface principle). | DONE | agent | [#546](https://github.com/petemoore/sam-aarch64/pull/546) | i193, i145c, i145h | i194, i198 | docs/specs/trinity-record-detection-design.md |
| **i145a** | **CSD-read probe (emulation): author the trinload-pushable probe + prove init->CMD9->decode->serve vs the model** — Author the trinload-pushable CSD probe (mirror netboot_dumper: read a region -> serve over TFTP), PORTING the SD interface from Colin's code per i193 (the &38 microcontroller init, the CMD ladder, CMD9 CSD read, the &DC/&DF busy-poll + &3F auto-null timing) — reuse the encdrv/eeprom SPI primitives. Decode CSD v1.0/v2.0 so ANY card size is inferred, never hardcoded (host-side decode ok). PROVE it end-to-end in EMULATION against the i145c model (which i145f has validated by running Colin's real driver against it) BEFORE any hardware run — the emulation-first gate (Pete 2026-06-22: a trinload crash on real HW is unrecoverable). The actual real-CSD capture on hardware is the separate i145g. Depends i193 (port authority) + i145c (model) + i145f (model validated). | DONE | agent | [#536](https://github.com/petemoore/sam-aarch64/pull/536) | i193, i145c, i145f | i145b, i145g, i194 |  |
| **i145b-b1** | **CSD->BD_RECORDS decode + emulation proof (gated module + test); ship pending the boot-budget page-out** — DONE this session. The production SD CSD read + the CSD->blocks->BD_RECORDS decode (src/netboot/sd_csd.asm: full SD-init ladder + CMD9 + the v1/v2 capacity decode + the 32-bit/16-bit records math), proven in EMULATION against the i145c model (csd_to_bd_records_test.go): BD_RECORDS is COMPUTED from the modelled CSD (NOT injected), matching the i145e-validated Go reference incl. Pete's 64GB -> 14581 (the q40 16-bit wrap). Card-init: the full ladder (netboot is a minimal env; the card cannot be assumed B-DOS-initialised). Wired into serve_main + client_main BUT gated behind -D NETBOOT_CSD_BDRECORDS=1 because it overflows the &C000 boot window by ~460-490 bytes (the ~630-byte module vs ~150 bytes free) — default boot builds are byte-for-byte UNCHANGED (no regression). The decode is correct + proven; SHIPPING it on hardware is i145b-b2. This is what ends the inject-BD_RECORDS shortcut at the logic level. | DONE | agent | [#539](https://github.com/petemoore/sam-aarch64/pull/539) |  | i145b-b2, i194 |  |
| **i145b-b2** | **Ship the CSD read in the boot image: page it out of the &C000 window (ENCTAB-style) so BD_RECORDS computes at boot** — The remaining work to actually CLOSE the hardware gap (BD_RECORDS=0 on real HW). i145b-b1's CSD-read+decode module (sd_csd.asm) is proven but gated off because it overflows the &C000 boot window by ~460-490 bytes. Ship it by paging the module out of section C (mirror the ENCTAB COMET-trampoline page-out, which already pages a payload out of the &C000 window), or a section-D overlay, so serve_main/client_main compute BD_RECORDS at boot within budget. Un-gate NETBOOT_CSD_BDRECORDS once it fits, and add an integration test that runs the un-gated serve_main path (not just the standalone fixture). The ENCTAB trampoline is the likely pattern to mirror (agent-actionable); raise a qN if the page-out approach needs a genuinely new design choice. The boot-budget is becoming a systemic constraint (multiple features need the window) — consider whether a general overlay mechanism is warranted. owner:agent. | DONE | agent | [#541](https://github.com/petemoore/sam-aarch64/pull/541) | i145b-b1 | i145d |  |
| **i145c** | **Harness SD-SPI/CSD model — model the DC/DF seam + SD command state machine + configurable CSD** — Model the Trinity &DC/&DF SD-SPI seam + the SD command state machine + a CONFIGURABLE CSD (v1.0/v2.0) in the netboot harness (mirror enc28j60.go/eeprom.go), FAITHFUL to i193 (the documented protocol is the spec — built from the doc, NOT derived from a hardware capture). Built FIRST in the emulation-first ladder: it is validated by running Colin's real B-DOS 1.5t driver against it (i145f / and the POC i145e) BEFORE any new code runs in it, and it ends the inject-BD_RECORDS shortcut that hid the i145 gap. The real CSD bytes captured on hardware (i145g) later re-parameterise it faithfully. Depends i193. | DONE | agent | [#535](https://github.com/petemoore/sam-aarch64/pull/535) | i193 | i141, i145a, i145b, i145d, i145f, i145h, i194, i195 |  |
| **i145d** | **Full E2E emulation test — trinload -> push server -> push .mgt -> store, BD_RECORDS from modelled CSD (not injected)** — The experiment that was never faithfully emulated: trinload load -> push serve program -> serve_main reads EEPROM + the modelled CSD -> BD_RECORDS computed (NOT injected) -> push a .mgt -> validate size==819200 -> stream to highest free record -> claim. Asserts the real-hardware path end to end. The test whose absence let the BD_RECORDS=0 hardware failure slip through. | DONE | agent | [#542](https://github.com/petemoore/sam-aarch64/pull/542) | i145c, i145f, i145h, i145b-b2 | i194 |  |
| **i145e** | **POC: run Colin's real 1.5t CSD-decode + records-math against a hand-filled CSD buffer (no SPI)** — POC-1 (DONE this session). Load Colin's B-DOS 1.5t fork binary (bdos15t-beta6.bin) into the netboot harness at the section-B aliases (the DOS page runs paged into &4000-&7FFF, so real &Axxx code = &6xxx) and run his REAL, hardware-proven CSD-decode (&A736 -> &6736) + records-math (&A45A -> &645A) against a HAND-FILLED 16-byte CSD buffer at &780F (no SPI). Asserts Colin's 32-bit block-count + BD_RECORDS match an independent Go reimpl of the i193 section-6 formula across v1/v2 cards incl. Pete's 64GB SDHC. Proves (a) the harness can execute Colin's code = the run/paging recipe for i145f, and (b) his capacity math. FINDING: Colin's BD_RECORDS is a 16-bit DVAR (&80C4) that WRAPS for cards >51GB (64GB -> 14581 not 80117 records) — the BD_RECORDS-width question. Test: tools/netboot-oracle/z80/csd_decode_colin_test.go (skips if the proprietary binary is absent via $BDOS15T_BIN). First rung of the emulation-first confidence ladder Pete defined 2026-06-22: validate the emulation with code we KNOW runs on hardware before running new code in it. | DONE | agent | [#534](https://github.com/petemoore/sam-aarch64/pull/534) | i193 | i145f, i194 |  |
| **i145f** | **Validate the SD-SPI model by running Colin's full 1.5t SD-init ladder (&A623 -> CMD9) against it** — POC-2 / model-validation gate. Run Colin's FULL SD-init ladder (DI; CALL &A623: the &38 microcontroller wake + CMD0/8/55+41/58/59/9/16 + the &FE-token 16-byte CSD read at &A711) against the i145c SD-SPI model — the model serves a synthetic CSD over the modelled &DC/&DF SPI, with NO hand-filled buffer, so the CSD must arrive via modelled SPI and decode to the correct BD_RECORDS. This is the confidence gate: it proves the SD-SPI emulation is FAITHFUL by running Colin's hardware-proven driver end-to-end BEFORE any new probe/serve code runs in the model (emulation-first ladder, Pete 2026-06-22; a trinload crash on real HW is unrecoverable, so the model must be trustworthy first). Builds on i145e's run/paging recipe + i145c's model. | DONE | agent | [#535](https://github.com/petemoore/sam-aarch64/pull/535) | i145c, i145e | i145a, i145d, i145g, i145h |  |
| **i145h** | **Extended validation: run Colin's write-record / select-record / read-back flow against the model** — Extended emulation validation (Pete's suggestion 2026-06-22): run Colin's write-record / select-record / read-back flow against the model — the fuller 'write a disk to storage, select that record, [boot it]' test, the strongest faithfulness check since it exercises CMD17/CMD24 single-block read+write + record selection (&A0A2) through Colin's REAL code. Needs the i145c model extended with CMD17/CMD24 + enough B-DOS scaffolding to drive HRECORD/HSAVE/HRSAD. Aspirational/larger than i145f, not on the critical CSD path, but the highest-confidence validation of the whole storage seam. Depends i145c (extended) + i145f. | DONE | agent | [#537](https://github.com/petemoore/sam-aarch64/pull/537) | i145c, i145f | i141, i145d, i195 |  |
| **i146** | **Priority-backlog robustness: a non-pullable ready tip = missing-edge bug (HW gates + prompt + CLI ordering-repair)** — Found 2026-06-20: the ready tip i141 (+ i118b/i93b/i133/i135) is hardware-blocked but has NO depends_on edge, so it falsely surfaces as pullable — a missing-edge bug (no-BLOCKED model). Fix: (1) model the hardware gate (gate node or owner:pete) so they leave ready; (2) prompt — a non-pullable tip means the agent ADDS the missing edge (check/create the gating item) first, never judgment-picks; (3) CLI auto-fixes backlog membership (#462) but not ordering — add a topological-repair pass; (4) fix the ROADMAP judgment-pick bullet. Absorbs q29. | DONE | agent | [#472](https://github.com/petemoore/sam-aarch64/pull/472) |  |  |  |
| **i148** | **registry CLI: a 'view' sub-command to print an item/question by id (replace ad-hoc YAML greps)** — No read command exists to inspect a single record, so agents resort to inline python/grep over registry/items.yaml (e.g. to read i133). Add 'registry view --id iN' (and qN) printing the record human-readably: id, title, status, owner, depends_on, dependents, parent, prs, refs, description — from the live registry. Optional --format yaml\|json for scripting. Read-only; pairs with ready/dependents/dag. | DONE | agent | [#473](https://github.com/petemoore/sam-aarch64/pull/473) |  |  |  |
| **i149** | **registry CLI: concurrency-safe mutation (file lock + atomic write) for concurrent/parallel use** — The CLI read-modify-writes the YAML with NO locking, so two concurrent mutations (Pete adds an item while an agent runs dep add) race -> last-writer-wins -> a change is silently lost; the .id-ledger append is likewise unguarded. Add an advisory file lock around load->mutate->write, atomic writes (temp+rename), and define contention behaviour. Enables Pete to add items while an agent works; prerequisite for parallel agent sessions (which also need git merge-conflict handling/recovery on the YAML — split if larger). | DONE | agent | [#474](https://github.com/petemoore/sam-aarch64/pull/474) |  |  |  |
| **i150** | **registry CLI: require an explicit data source — never guess, never fall back to test fixtures** — WONTFIX (q31 resolved 2026-06-20): keep the cwd-walk discovery as 'explicit by convention'. The catastrophic testdata fallback was removed (#472, errors if no live registry) and 'next-id' (which could read testdata and once handed Pete an 'i6') was removed (i153), so the danger is closed; a config-file / required-flag / wrapper is more machinery than the residual risk warrants. Revisit only if it bites. | WONTFIX | agent |  |  |  |  |
| **i151** | **Audit the whole registry for hardware/Pete-gated items missing a depends_on gate edge** — i146 added the i133 gate edge to the KNOWN hardware cluster (i141/i118b/i93b/i135). Systematically audit every open item for real-world blockers (hardware access, Pete-only setup) not yet expressed as a depends_on edge — the missing-edge class that makes a non-actionable item falsely surface as the ready tip. Candidates to check: i70/i93/i95/i101/i112/i87/i89/i113b/i127 and any others. For each truly-gated item add the edge to i133 (or the correct gate) so 'ready' surfaces only agent-actionable work. Generalises the i146 fix to prevent recurrence. | DONE | agent | [#475](https://github.com/petemoore/sam-aarch64/pull/475) |  |  |  |
| **i153** | **Registry CLI/model hardening: atomic tool-determined ids + blocked-on-Pete backlog marking + parent/orphan invariants** — Pete-requested registry tooling (2026-06-20). (1) backlog.md: drop rank col; add 'gate' col (ready / in-progress / blocked-on-Pete qN / blocked-on-work iN). (2) Atomic tool-determined ids: 'add' auto iN (+--space); 'split' auto child-id (iN->iNa, iNx->iNx-b1)+full fields; drop next-id+custom --id. (3) Multi IN_PROGRESS allowed; 'ready' EXCLUDES IN_PROGRESS; new 'in-progress' list cmd. (4) Invariants+tests: parent exists+umbrella, sub-id needs parent, no cycles, regexp conformance, exhaustion forbid>26. (5) Orphans: i12/i107 umbrellas, i39c->i48c, i117->umbrella. | DONE | agent | [#481](https://github.com/petemoore/sam-aarch64/pull/481) |  |  |  |
| **i154** | **Codify 'track-on-request' discipline + parallel-IN_PROGRESS session ledger** — Strengthen handover rule 1 from PR-time to REQUEST-time + embrace parallel IN_PROGRESS. When work is well-defined AND signed off, the agent creates the item LOCALLY (committed; no PR dance to capture), marks it IN_PROGRESS, and confirms to Pete ("logged as iNNNN, IN_PROGRESS, working"). Many asks/session => many IN_PROGRESS — a feature: registry = session completeness ledger. Wind-down: query IN_PROGRESS; each ends DONE or splits into done/not-done (untouched -> OPEN), so real partial state is captured + no ask lost in chat noise. Lands in CLAUDE.md + ROADMAP. Earned 2026-06-20. | DONE | agent | [#481](https://github.com/petemoore/sam-aarch64/pull/481) |  |  |  |
| **i155** | **Fix claude-chat-export dropping user messages mid-session** — ~/bin/claude-chat-export omits Pete's messages after a point in the per-session markdown export (2026-06-20 session: messages from 'you know what else is cool' onward are missing, though present in the .jsonl). Investigate the exporter's turn/role parsing (likely a message-shape or multi-part content-block handling bug, or a turn-boundary heuristic that stops early) and fix so every user turn is exported. Tool: ~/bin/claude-chat-export (renders .jsonl session logs to the Jekyll chat archive). | DONE | agent | [#482](https://github.com/petemoore/sam-aarch64/pull/482) |  |  |  |
| **i156** | **Run the registry Go unit tests in CI (add a ci-registry gate)** — CI has no go-test job for tools/registry: the registry-sync job validates the LIVE registry (id grammar inv.2 + parent inv.14) during gen, but the registry Go UNIT tests (subid_test, parent_invariant_test, gate_test, live_registry_conformance_test, mutators/priority/validate tests) never run in CI — they pass only locally, so #481 merged green without executing them. Add a ci-registry Makefile target + CI job (mirror ci-netboot-oracle) running 'go test ./...' on tools/registry so the test logic is gated. Closes the i153 audit gap (Pete checklist 8/12). | DONE | agent | [#483](https://github.com/petemoore/sam-aarch64/pull/483) |  |  |  |
| **i157** | **Document that one PR may complete multiple IN_PROGRESS items** — i154 made parallel IN_PROGRESS a feature, but the docs don't say a single PR may COMPLETE several items at once (CLAUDE.md 'Multi-PR work' still reads 'each leaf with exactly one completing PR', implying one-item-per-PR). Clarify in CLAUDE.md + ROADMAP rule 1: each item still has exactly ONE completing PR, but a PR MAY complete multiple items (as #481 completed i153+i154); when mid-flight work is interrupted, track every thread and either finish or split at wind-down. Closes the i153 audit gap (Pete checklist 16). | DONE | agent | [#483](https://github.com/petemoore/sam-aarch64/pull/483) |  |  |  |
| **i169a** | **ready --pete-away (default, excludes owner:pete) / --pete-present (includes + prioritizes)** — The core needs-Pete mechanism: 'build/registry ready' excludes owner:pete (needs-Pete-present) items by DEFAULT so the autonomous agent's tip is always agent-actionable; --pete-present includes them AND emits them first (don't waste Pete's presence). Fixes the autonomous-loop trap where an owner:pete item (e.g. i87a) is the ready tip. The presence-file auto-defaulting, decision-item->question conversions, backlog gate marker, and docs remain the i169 remainder. | DONE | agent | [#495](https://github.com/petemoore/sam-aarch64/pull/495) |  | i172 |  |
| **i173** | **SAMBOOT: build the one-shot ROM+EEPROM dumper (serves captured bytes via the proven TFTP server)** — Small Z80 program: read the patched system ROM (32 KB via SAM ROM paging) + the Trinity EEPROM chunks, and serve them via the proven TFTP server so the host pulls rom.bin/eeprom.bin. Pushed via trinload; reuses the host-verified serve loop + Colin's eeprom.asm. Emulation-verify EEPROM-read + serve; ROM-page read is hardware-first. | DONE | agent | [#492](https://github.com/petemoore/sam-aarch64/pull/492) | i135a | i87a | docs/specs/samboot.md |
| **i175** | **SAMBOOT charter: controlling overview doc + registry restructure under i133/i135** — Stand up the SAMBOOT controlling overview (docs/specs/samboot.md) + restructure the registry under it: de-conflate the ROM-chip/EEPROM-bootblock/forked-B-DOS naming, split i87, flip i135<-i133, the i135a-c chain, the dumper + BIOS items, fold i112. | DONE | agent | [#486](https://github.com/petemoore/sam-aarch64/pull/486) |  |  | docs/specs/samboot.md |
| **i176** | **SAMBOOT BIOS: editable default-boot-record config in Trinity EEPROM (boot record N, or none) + editor** — A boot config in a Trinity EEPROM chunk (like the 'Trinity Network' chunk): a default-boot-record setting (boot record N at startup, or none -- any disk). The injection prototype reads it; a config editor sets it. The SAM 'BIOS'. | DONE | agent | [#493](https://github.com/petemoore/sam-aarch64/pull/493) | i135a | i135d, i177 | docs/specs/samboot.md |
| **i179** | **monitor.sh: fix the prior-turn nudge-delivery race + add persistent delivery tracing (i97 follow-up)** — The task-done resume nudge is dropped when the monitor's TASK_DONE handler fires while the agent's PRIOR turn is still running (the agent touches task-done then keeps emitting, e.g. a summary). nudge_until_turn's 'if turn_running: return 0' then treats the still-running prior turn as nudge-success and never sends the nudge -> stall. Fix: wait_for_idle (sustained-idle confirm) before delivering /context + nudge; always send the nudge on attempt 1. Plus: persistent monitor.log tracing (every stuff/submit/turn_running/branch) so future delivery issues are visible, not guessed. Distinct from the i140 long-burst-drop fix (additive). | DONE | agent | [#487](https://github.com/petemoore/sam-aarch64/pull/487) | i97 |  | tools/autonomous-loop/monitor.sh, tools/autonomous-loop/monitor-nudge-delivery.md |
| **i180** | **Registry: make priority topo invariant + auto-repair umbrella-aware (expand umbrella deps to pullable leaves)** — Root cause (found while reshuffling SAMBOOT): validatePriority check 4 (validate.go:586) and topoRepairPriority (mutators.go:100) both SKIP any depends_on edge whose target is not itself a pullable queue entry. An umbrella is never pullable, so a leaf->umbrella edge (e.g. i133->i135) imposes ZERO ordering constraint and the umbrella's real prerequisite leaves (i135c/i135d) are never substituted in -> i133 can sit ahead of its prerequisites and validate (run by CI) stays green. Fix: expand a depends_on edge targeting an umbrella to its open pullable leaf descendants in BOTH validate check 4 and the topo-repair; add regression tests (leaf->umbrella ordering, currently uncovered). Then the auto-repair corrects priority.yaml. Spec: docs/specs/registry-source-of-truth-design.md (Priority queue + ready / validate). | DONE | agent | [#491](https://github.com/petemoore/sam-aarch64/pull/491) |  |  |  |
| **i181** | **Harness: add SAM paging + ROM write-protect to the netboot Go emulator (port the assembler emulator's pager)** — THE RECURRING-&C000 FIX, minimal form (Pete, 2026-06-21). Root of the i87a rom1 crash: the dumper needs BOTH paging and Ethernet but runs in the netboot emulator (tools/netboot-oracle/z80), which models ENC28J60 + EEPROM + trinload round-trip (i132) + a fixed ROM1-RO boot overlay (i124) but does NOT act on LMPR/HMPR (records, doesn't remap) — and the dumper's ROM-paging path is ifdef'd out (NETBOOT_HOSTTEST). The ASSEMBLER emulator (tools/z80-test-harness-go/harness.go) ALREADY has a faithful pager — resolveRead/resolveWritePage do full LMPR/HMPR mapping and SILENTLY DROP ROM writes (ROM0/ROM1 read-only, the &C000 wall). FIX (the immediate, SAMBOOT-critical step — the full two-into-one unification is POSTPONED to i190 per Pete): bring that proven pager (~40 lines) into the netboot emulator so LMPR/HMPR remap real pages and ROM writes drop; load ROM0/ROM1 fixtures + model trinload residency in its pages; un-ifdef the dumper ROM path; reproduce the i87a rom1 page-0-clobbers-trinload crash in emulation (red), then verify the i188 redesign green. Prefer extracting the pager into a SHARED package (a down-payment on i190) over a copy. Unblocks i188. | DONE | agent | [#523](https://github.com/petemoore/sam-aarch64/pull/523) |  | i188 |  |
| **i183** | **Harness: model the SAMDOS file-not-found / HSAVE error longjmp (currently a silent no-op)** — Emulation-first prerequisite for i25. The Go harness (tools/z80-test-harness-go/harness.go HGTHD/HLOAD/HSAVE cases) models an unregistered HGTHD file as a SILENT NO-OP (currentFile=nil), so a real SAMDOS file-not-found error manifests downstream as a cryptic 'jumped into garbage' trap rather than the actual derr -> (hksp) longjmp. To verify i25's (hksp) error handler in emulation, the harness must faithfully model the SAMDOS file-I/O error longjmp: read the emulated (hksp) vector, jump to an installed handler (or take the default halt when (hksp)=0), and expose injectable HGTHD/HSAVE failures. Design dimension to settle with i25: how faithfully to model (hksp)/SAMDOS-bank paging vs surface a clear error signal. Refs: docs/specs/samdos-file-io.md (derr/(hksp) at &410A; longjmp-on-error), src/main_loop.asm save_out_file/load_in_file, src/loader.asm. | DONE | agent | [#508](https://github.com/petemoore/sam-aarch64/pull/508) |  | i184, i25 |  |
| **i185** | **Correct SAMDOS file-I/O error references: app vector is DOSER (&5BC0), not (hksp)** — i25/i183 and the reference docs (docs/specs/samdos-file-io.md, docs/notes/sam-stub-audit.md) framed graceful file-I/O error handling around installing an (hksp) handler. That is wrong: the SAMDOS hook dispatcher zeros (hksp) on EVERY hook entry before the hook body runs (samdos/src/b.s:450-451), so an app-set (hksp) is never seen by derr (d.s:430-437) -- (hksp) is SAMDOS-internal (NMI snapshot only, d.s:606/745). The app-facing error vector is DOSER (&5BC0), a BASIC sysvar (always mapped, no paging dance) that ROM PTDOS dispatches via JP (DOSER) after every DOS hook (rom DOSC, docs/sam/sam-coupe_rom-v3.0_annotated-disassembly.txt:12977-12980,13003); COMET uses exactly this (comet.asm:1265-1382). samdos-file-io.md:174 already notes &5BC0 is COMET's error vector -- internally inconsistent with its (hksp) caveat. Scope of THIS item: fix the reference docs. Implementation (DOSER handler + harness DOSER model) is i25. | DONE | agent | [#509](https://github.com/petemoore/sam-aarch64/pull/509) |  | i25 |  |
| **i186** | **Show owner in all generated registry .md views + gate-column owner-awareness** — Add an 'owner' column to all four generated registry markdown views (item-registry-open.md, item-registry-closed.md, question-registry-open.md, backlog.md) so owner:pete vs owner:agent is visible at a glance. Also fix computeGates (tools/registry/gen.go) so the backlog 'gate' column reflects owner: an owner:pete item with satisfied deps should not read 'ready' (it is excluded from 'build/registry ready'); show it as pete-held instead. Motivation: i87a sat at the top of backlog labelled 'ready' all night but was owner:pete (hidden from the agent queue); the missing owner column + owner-blind gate made it look pullable when it was not. | DONE | agent | [#516](https://github.com/petemoore/sam-aarch64/pull/516) |  |  |  |
| **i188** | **SAMBOOT dumper: fix rom1 page-0 scratch clobber + restore paging for a clean trinload return** — The i173 dumper crashed real hardware capturing rom1.bin (i87a, 2026-06-21): dumper_read_rom1 picks scratch page P-1 = page 0 (P=1, the trinload push page), ldirs 16 KB of ROM1 into it, and never restores LMPR — clobbering low memory AND remapping section B (&6000) away from trinload (org &6000), killing trinload. COMPOUND defect, confirmed in emulation by i181's characterization test (dumper_rompaging_test.go): the routine ALSO clobbers its own register C — it stashes the intended post-copy LMPR (&20) in C, then `ld bc,REGION_BYTES; ldir` counts BC to 0, so the final `ld a,c; out (&FA),a` writes &00 (section A=ROM0, section B=page1), making the mis-paging total. Redesign per Pete: one program that reads ALL regions (ROM0/ROM1/EEPROM) with a genuinely-free scratch page + restored paging + registers not clobbered by the copy, streams them in one session, then RETs cleanly to trinload (fixes serve-forever/no-clean-return that blocks the autonomous push->run->return->re-push loop). When fixed, flip i181's rom1 characterization (page 0 untouched, LMPR==entry). Emulation-verify the full path (paging + trinload round-trip) before hardware via i181. EEPROM read + rom0 already hw-confirmed (i87a backup). SCOPE (this item): the rom1 read fix (genuinely-free scratch = STAGE's own page P+1, registers not clobbered, entry LMPR restored) + the clean Esc-driven trinload return it enables — this is all i87a-b2 (rom1 recapture) needs. The headless auto-RET-after-all-regions-served (the 'serve-forever' fix, only needed for the fully-unattended push->run->return loop, NOT the recapture) is split to i192; the eeprom/rom0 captures already returned via Esc, so it does not block i87a-b2. | DONE | agent | [#524](https://github.com/petemoore/sam-aarch64/pull/524) | i181 | i192, i87a-b2 | docs/specs/samboot.md, docs/notes/netboot-trinity-testing.md |
| **i190a** | **Shared emulation core loads the extracted real ROM patch + EEPROM code/config for authentic Trinity emulation** — Load the real captured ROM patch + Trinity EEPROM code/config (~/sam-archive/samboot-capture/rom.bin + eeprom.bin, from the DONE i87a capture) into the harness SAM emulation so the real boot can be traced authentically — the gating prerequisite for i197c (and thus SAMBOOT's i197/i135c). Pete un-postponed this (2026-06-22, q41): it is NOT blocked on the library merge (i190) — do it on the EXISTING harness emulator now; if the merge later happens it moves into the shared core. Depends only on i87a-b2 (DONE), so agent-actionable. No NEW hardware needed — the ROM/EEPROM were already captured (i87a). | DONE | agent | [#582](https://github.com/petemoore/sam-aarch64/pull/582) | i87a-b2 | i197c |  |
| **i191** | **Meta: root-cause recurring backlog-incoherence (untracked deps on unfinished work) + assess instruction/doc bloat** — RECURRING FAILURE (Pete, 2026-06-21, i87b incident): an item reached the ready-queue TIP while depending on unfinished work not tracked as a depends_on edge. i87a (capture rom.bin+eeprom.bin) was correctly split into i87a-b1 (eeprom+rom0, DONE) + i87a-b2 (rom1, OPEN), but i87b (study the captured data) depended on the DONE leaf i87a-b1, not the OPEN leaf i87a-b2 -> i87b surfaced at rank 1 though rom.bin (needs rom1) did not exist. CONFIRMED ROOT CAUSE: `registry split` ALREADY auto-rewrites a parent's dependents to its children (mutators.go:491), so the split was sound. The real bug: i87b was CREATED depending on the DONE leaf i87a-b1 (not the umbrella i87a, which the split-rewrite protects) — a done leaf reads as 'satisfied', so its open sibling was silently missing. A SEMANTIC wrong-target error at dependency-CREATION time; NOT a split bug, NOT a hand-edit-vs-CLI bug (the same wrong target via the CLI surfaces too). FIX MENU (escalating): (a) DONE here — CLAUDE.md rule 'depend on the umbrella for the whole, never a single done leaf' + the i87b example (the docs fix Pete asked for); (b) a CLI guard in dep-add/add that WARNS when the target is a DONE leaf of an umbrella with OPEN siblings (the mechanical at-source catch); (c) a registry SKILL so agents reach for the validated CLI (where the guard fires) AND to de-bloat CLAUDE.md by moving the registry workflow into an on-demand skill; (d) a CI registry-coherence lint (warning-level). b/c/d + the broader consolidation are Pete's design call. TODO: audit the open registry for OTHER tips depending on a done-leaf-with-open-siblings. | DONE | agent |  |  |  |  |
| **i191a** | **registry CLI guards: warn on dep to a done-leaf-with-open-siblings + warn on splitting a DONE item** | DONE | agent | [#526](https://github.com/petemoore/sam-aarch64/pull/526) |  |  |  |
| **i191b** | **registry CLI: set-desc + set-title commands (no hand-edit of items.yaml)** | DONE | agent | [#526](https://github.com/petemoore/sam-aarch64/pull/526) |  |  |  |
| **i191c** | **registry workflow as an on-demand skill + CLAUDE.md registry-section de-bloat** | DONE | agent | [#527](https://github.com/petemoore/sam-aarch64/pull/527) |  |  |  |
| **i193** | **Document the Trinity SD-card Z80 storage interface from Colin's disassembled B-DOS 1.5t fork** — Read Colin's annotated B-DOS 1.5t fork disassembly (~/sam-archive/bdos/analysis/bdos15t-beta6.annotated.dis, from i71) to extract + document the Trinity SD-card Z80 storage interface: the SPI command ladder (the &38 microcontroller SD-init, CMD0/8/55+41/58 init, CMD17 sector read, CMD24 write, and the CMD9/CSD read if present), the &DC-select / &DF-data protocol, the busy-poll (&DC bit3) + auto-null (&3F) timing, and how list-sector + record reads/writes map onto it. The AUTHORITY for i141 (raw list-sector read) and i145 (CSD capacity read) — port from Colin's proven code, not a reconstructed generic reference (a correct generic SD-SPI reference is in the chat archive as a cross-check). Offline analysis: agent-actionable, no hardware, filter-safe (reads existing disassembly). Output: a living doc (docs/notes/trinity-sd-z80-interface.md) the ports cite. | DONE | agent | [#533](https://github.com/petemoore/sam-aarch64/pull/533) |  | i141, i145a, i145c, i145e, i195 |  |
| **i195** | **Name a pushed record from its post-prefix WRQ name (full record-name field), so the 19 images are legible** — Pete (2026-06-21 night): push to trinity-sam-disks/NAME -> name the claimed Trinity RECORD from NAME (full record-name field, ~16 chars; i121g caps at 10 = the B-DOS file-name field) so the 19 images are legible via RECORD/dir. The claim is a record-LIST WRITE = a raw SD write with NO real B-DOS hook, so depends on i193 (the SD write interface) AND i145c (the harness modelling that write FAITHFULLY at raw-SD level, not hook-injected). SAFETY/ROBUSTNESS (Pete): the WRQ filename is ATTACKER-CONTROLLABLE, so the name derivation must be BOUNDED + SANITISED and must not be exploitable to corrupt a record: (1) confirm the legal SAM record-name charset + field length from the B-DOS source / docs/specs/trinity-record-detection-design.md; (2) derive the name from the post-prefix path (e.g. the final path segment), drop/replace illegal chars and extra separators/dots; (3) TRUNCATE to the field length so there is NO buffer overrun and NO illegal byte written, for ANY input (a 200-char name, trinity-sam-disks/abc/def.123, control bytes). Document the sanitisation rule in the spec. TESTED IN EMULATION vs i145c (incl. adversarial inputs asserting a safe + legible name + an intact, uncorrupted record); auto-hardware-tested when i194 pushes the 19 images. owner:agent, no Pete/question gate. | DONE | agent | [#538](https://github.com/petemoore/sam-aarch64/pull/538) | i193, i145c, i145h | i194 |  |
| **i197a** | **SAMBOOT: static RE of the captured on-card bootblock (real bootblock at EEPROM &0000; flat layout; candidate hook site)** — DONE. Reverse-engineered Colin's real bootblock from the i87b capture (offline, read-only). Findings in docs/notes/samboot-bootblock-analysis.md §7: (1) the real boot.asm-equivalent bootblock is at EEPROM offset &000000 (unique signature match), correcting §6.2's 'absent' claim — it is Colin's own build (execute-DOS &805F not &804C; no inline stripes/wait_for_key, redraws via ROM &06B5 + sysvar pokes; no TODO hook). (2) The EEPROM is a FLAT image, not the public 64-byte-header FS: chunk N at (28+4N)*256 = &2000+(N-1)*&400 (from get_chunk &412C); bootblock at &0000 pre-chunk; chunks 2-13 = B-DOS -> &8000-&AFFF. (3) Candidate injection hook site = between CALL &805F (&409E) and restore (&40A1), same logical point as boot.asm TODO; ~683 bytes free at &00015E-&000408. ROM fetch re-confirmed (reads &2000->&4000, JP &4000). | DONE | agent | [#545](https://github.com/petemoore/sam-aarch64/pull/545) |  |  |  |
| **i199** | **i48c-b8e-1: encodeInst skeleton + operandsToValues + form-table path (SAM standalone encoder, brick 1)** — First brick of the i48c-b8e encoder port (see i48c-b8e umbrella). Port the core encodeInst dispatch SKELETON (pass2.go:214-373) + operandsToValues (pass2.go:375-423) + the generic form-table path (ValidateOperandKinds -> operandsToValues -> enc.Encode). The FOUNDATION all other b8e bricks plug into: read the operand stream into a kinds/operands list; for instructions whose operands are all plain GPR/ImmExpr/Cond and whose mnemonic is NOT a compound/special form, look up the form via form_lookup (form_lookup.asm, reads paged ENCTAB), convert operands to slot values (operandsToValues: plain regs -> {reg}; ImmExpr -> eval_expr_const then PC-relative adjust for BranchImm26/19/14, AdrpImm 33-bit-wrap, AdrImm; Cond -> {cond}), and encode via the proven slot encoders (src/slots/*.asm) + insn_fold (insn_run.asm). Testable on add/sub-imm12, movz/movk imm16, logical-imm and/orr/eor, b/bl/b.cond/cbz branch-imm, adrp/adr, csel/csinc, ret/nop. Faithful port (CLAUDE.md s6), Go authority pass2.go encodeInst+operandsToValues. HARNESS: a BUILD_TESTS boot self-test inside the full paged assembler build (ENCTAB mapped by main_assemble; precedent assembler.asm:357-468 + test_mem_offaxis.asm off-axis). The Go harness synthesises fixtures, the self-test encodes, the Go side compares vs host encodeInst. SimCoupe-gated. This brick also builds the reusable self-test harness scaffold that bricks 2/3 extend. | DONE | agent | [#561](https://github.com/petemoore/sam-aarch64/pull/561) |  | i201, i201a, i201b, i201c, i203, i203a, i203b, i203c, i203d, i204, i48c-b8e |  |
| **i201a** | **i201a: shifted-reg + extended-reg + 3-reg/tst coercions (compound-form encoders)** — First sub-brick of i201 (i48c-b8e compound forms). Wire the shifted-register and extended-register compound-operand encoders into encode_inst's pre-form-table dispatch (pass2.go:235-240): encodeShiftedRegInst (pass2.go:1060-1122) + encodeExtendedRegInst (ADD/SUB only, pass2.go:1188-1225), plus the shifted-reg 3-register coercion (lsl #0, pass2.go:252-271) and 2-register tst coercion (pass2.go:273-290). REUSES the proven Z80 full-instruction encoders encode_shifted_reg_word + encode_extended_reg_word (src/slots/shifted_reg.asm / extended_reg.asm, already tested by test_shifted_reg/test_extended_reg) + encode_extend_op + eval_expr_const for the shift/amount expr. New work = parse the OpShiftedReg/OpExtendedReg operand wire format from the stream and set up the encoder inputs + the coercion synthesis. Faithful port (CLAUDE.md s6). Extends the test_encode_inst boot self-test + the Go authority fixtures. Depends on i199 (encode_inst skeleton + harness). SimCoupe-gated. | DONE | agent | [#578](https://github.com/petemoore/sam-aarch64/pull/578) | i199, i206, i69 | i201b, i201c |  |
| **i201b** | **i201b: memory compound-form encoder (encodeMemInst, 6 MemShape variants)** — Second sub-brick of i201. Port encodeMemInst (pass2.go:762-902): all MemShape variants - MemBase [xn], MemBaseOff/Pre/Post (scaled imm12 / signed imm9), pair imm7 (ldp/stp), stur/ldur unscaled, MemBaseIdx/Shifted/Extended (register offset with extend/shift). Wire into encode_inst's pre-form-table dispatch (pass2.go:232-234). REUSES encode_mem_word (src/slots/mem.asm) + fold_mem_imm12/fold_mem_imm9/fold_pair_imm7 (insn_run.asm) + eval_expr_const. New work = parse the OpMem operand wire format (shape byte + shape-dependent payload) and route each shape. Faithful port (CLAUDE.md s6). Depends on i199, i201a. Extends the boot self-test + Go authority fixtures. SimCoupe-gated. | DONE | agent | [#579](https://github.com/petemoore/sam-aarch64/pull/579) | i199, i201a |  |  |
| **i203a** | **i203a: shift/bitfield/ror special-form encoders (lsl/lsr/bfi/bfxil/ubfx/bfc/sbfx/ror)** — First sub-brick of i203 (i48c-b8e special forms). Port the self-contained bit-math special-form encoders that intercept before the form table (pass2.go:337-353 dispatch): encodeLSLSR (lsl=17/lsr=18 -> UBFM aliases with computed immr/imms; reg-shift form -> LSLV/LSRV, pass2.go:1253-1320), encodeBitfieldInst (bfi=49/bfxil=50/ubfx=51/bfc=83/sbfx=84 -> BFM/UBFM/SBFM with immr/imms from lsb+width; bfc has implicit XZR Rn, pass2.go:1334-1438), encodeRorImm (ror=70 -> EXTR Rd,Rn,Rn,#imm alias, pass2.go:1517-1561). Pure arithmetic over evaluated immediates (reuse eval_expr_const) + direct field placement; NO paged tables, NO form-table lookup. Faithful port (CLAUDE.md s6). Adds a special-form dispatch in encode_inst (checked before the form-table path, mirroring Go's switch rec.MnemonicID) + handlers. Extends the test_encode_inst boot self-test + the Go authority fixtures with shift/bitfield/ror cases. Depends on i199 (encode_inst skeleton + harness). SimCoupe-gated. | DONE | agent | [#563](https://github.com/petemoore/sam-aarch64/pull/563) | i199 | i203b, i203c, i203d |  |
| **i203b** | **i203b: bic-imm/csetm/barrier special-form encoders** — Second sub-brick of i203. Port encodeBicImm (bic=47 immediate -> negate the immediate then LogicalImm, pass2.go:1440-1480), encodeCsetm (csetm=52 -> csinv with inverted condition, pass2.go:1482-1515), encodeBarrierInst (isb=66/dsb=67/dmb=68 -> CRm operand into bits 11:8, pass2.go:1563-1599). Faithful port (CLAUDE.md s6). Reuses encode_inst dispatch (i203a) + eval_expr_const + the logical-imm encoder. Extends the boot self-test + Go authority fixtures. Depends on i199, i203a. | DONE | agent | [#564](https://github.com/petemoore/sam-aarch64/pull/564) | i199, i203a |  |  |
| **i203c** | **i203c: system-register special-form encoders (mrs/msr/dc/tlbi)** — Third sub-brick of i203. Wire the system-register encoders into encode_inst's special-form dispatch: mrs=76 (encodeMrs, pass2.go:1601), msr=77 (encodeMsr reg/PSTATE, pass2.go:1628), dc=78 (encodeDc, pass2.go:1673), tlbi=79 (encodeTlbi, pass2.go:1699). NOTE: the Z80 ALREADY has encode_mrs_word/encode_msr_word in src/sysname.asm (used by the .tbn path) + the paged sysreg name tables (sysreg_data.bin, load_page13_payload) — this brick is largely REUSE/wiring + dc/tlbi if not yet present, not new encoders. Verify the existing helpers match the Go authority; reuse them. Faithful port (CLAUDE.md s6). Depends on i199, i203a. Extends the boot self-test + Go authority fixtures (needs the paged sysreg tables mapped). SimCoupe-gated. | DONE | agent | [#565](https://github.com/petemoore/sam-aarch64/pull/565) | i199, i203a |  |  |
| **i203d** | **i203d: mov-imm autoselect + ldr-lit-direct + tbz/tbnz special-form encoders** — Fourth/final sub-brick of i203. Port tryEncodeMovImm (mov #imm -> movz/movn/orr-imm autoselect, pass2.go:476-540), encodeLdrLitDirect (ldr Xt,label -> PC-relative imm19, pass2.go:546-569), encodeTbzTbnz (tbz=22/tbnz=23 -> test-bit-and-branch, b5/b40 + imm14, pass2.go:584-629). These intercept before the form table (pass2.go:301-332). Faithful port (CLAUDE.md s6). Reuses encode_inst dispatch + eval_expr_const + insn_fold (imm14 branch via FSID_BRANCH14). Extends the boot self-test + Go authority fixtures. Depends on i199, i203a. | DONE | agent | [#573](https://github.com/petemoore/sam-aarch64/pull/573) | i199, i203a, i205 |  |  |
| **i205** | **encode_inst boot self-test: relieve the inline section-C budget (&C000 cliff)** — Relieve the test-variant assembler.bin &C000 inline-self-test budget (i203d overflows by 378 B; i201a/i204 add more) by RECOVERING CODE FOOTPRINT. METHOD (Pete steer 2026-06-22, q43): COMMONING-FIRST refactor, NOT dead-code deletion. The i111 coverage map (build/coverage-report.txt, ~1993 B untouched) was investigated and found to be almost entirely LIVE-but-untested features, NOT dead code (every top candidate is statically referenced; the biggest, sysname_parse_* 201 B, is the generic-sysreg-syntax fallback reached only on a table miss; .org/.word/.balign/ml_div/fail/litpool all live). So deletion won't recover the bytes — instead de-duplicate redundant LIVE routine families into generic data-driven forms, COVERAGE-FIRST per family (add fixtures so the family turns green in i111 coverage, THEN common with the test as guard), keeping every feature + every vector, full SimCoupe suite green + budget re-checked after each. CONCRETE FAMILIES (from the coverage map): (1) encode_logical_imm_repl_not32/16/8/4/2 (src/slots/logical_imm.asm:276-360) — five near-identical element-replication variants differing only by element size -> one size-parameterized replicate routine (~80-120 B); coverage needs logical immediates of element sizes 2/4/8/16/32 (0x5555.../0x1111.../0x0101.../0x0001.../0x00000001... — validate each vs the Go authority enc.EncodeLogicalImmPub). PRODUCTION ENCODER — refactor faithfully, this affects every real logical-imm assembly. (2) ml_div* division loop. (3) main_dir_org_pass1/pass2 + the directive handlers. Do families until headroom covers i203d (~+738 B from the current 360) + runway for i201a/i204. Then unpark i203d (draft PR #566) onto the roomier base, verify it fits + all green, merge. Tool: i111 (DONE). Improves prod too. | DONE | agent | [#572](https://github.com/petemoore/sam-aarch64/pull/572) | i111 | i203d |  |
| **i206** | **Commoning pass #2 — bank test-variant headroom before i201a compound-form encoders** — Follow-on to i205 (DONE, +386 B). The test-variant assembler.bin has only 87 B headroom to the &C000 cliff after i203d un-park, so i201a (compound-form encoders + fixtures) will overflow. Bank more headroom via coverage-first commoning of the un-commoned families flagged in build/coverage-report.txt: ml_div* (signed-divide helper), main_dir_org_* / directive handlers, and the sysname_parse_* generic-sysreg fallback paths. Keep the coverage-first rule (add a fixture so each family is green before/after commoning). Prod binary must stay byte-identical; test variant must regain headroom. Authority: existing Z80 sources + Go encoders where duplicated logic mirrors a Go fold/encoder. | DONE | agent | [#575](https://github.com/petemoore/sam-aarch64/pull/575) |  | i201a |  |
| **i214** | **Widen BD_RECORDS to 32-bit, gated behind the Trinity EEPROM large-disk capability flag (>51GB cards)** — WONTFIX (Pete, 2026-06-23): overkill. 32-bit BD_RECORDS only matters past ~51GB of disk images (65535 records x 800KB); the published SAM catalogue is <1GB, so faithful 16-bit (Colin's B-DOS design, ported as-is by i145b) is wildly sufficient — we will never approach the cap. Revisit only if a concrete >51GB need appears. | WONTFIX | agent |  |  |  |  |
