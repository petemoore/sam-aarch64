# Item index — closed items (`iN` registry, closed half)

This is the **resolved half** of the canonical, milestone-neutral `iN` registry.
Every row here carries a `DONE` or `WONTFIX` status token — it has been completed
or deliberately declined. Ids are **globally unique** with `item-registry-open.md`
and are never reused.

Governance, open backlog, and the shared registry discipline live in
`docs/notes/item-registry-open.md` (link it).

| id | item | status | pointer |
|----|------|--------|---------|
| **i1** | Compact-`.tbn` format change (hybrid bytes/symbolic; `KindLitInsts` + `KindLitData`) | DONE — ✅ PR1 #121 + PR2 #122 + PR3 #124 all merged — **88,644 → 51,117 B (−42.3%)**, 6 IN pages → 4, byte-identical to GNU. Next-gen redesign explored as **i39**. | `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/specs/2026-05-27-compact-tbn-and-disassembler-design.md`; `docs/specs/tbn-binary-format-reference.md` (§7) |
| **i8** | Sysreg-table de-dup into shared `src/sysreg_tables.inc` | DONE — ✅ (PR #108) | `src/sysreg_tables.inc` |
| **i9** | Parity robustness seeds (sysname full-Go-parity + untested-form empirical sweep) | DONE — ✅ (PR #114) — but it *pinned* two found gaps as skipped tests; fixed under **i36/i37** | `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-z80-go-disasm-parity-i9.md` |
| **i10** | Go-vs-Z80 capability parity report | DONE — ✅ (PR #109) | `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-go-vs-z80-disasm-capability-parity.md` |
| **i11** | Full ARMv8.0-A A64 ISA footprint research | DONE — ✅ (PR #110) | `docs/notes/a64-isa-footprint-research.md` |
| **i12a** | SimCoupé v1.2.16 bump (pin SHA to upstream, drop vendored `-exitonhalt` patch) | DONE — ✅ (PR #112) | `tools/Dockerfile.dev` |
| **i12c** | Rebase/upstream macOS+Linux paste support | DONE — ✅ RESOLVED/MOOT — upstreamed (`87f2a69`); arrives free with i12a | `~/.claude/.../memory/project_simcoupe_sdl_paste_branch.md` |
| **i13** | gitignore in-tree Go build binaries | DONE — ✅ (PR #107) | `.gitignore` |
| **i14** | Non-canonical logical-immediate decoder tests (`decodeBitMasks` `immr≥esize` reject) | DONE — ✅ (PR #113) | deferred-backlog row |
| **i17** | Deep reviews of `main_loop.asm` + `litpool.asm` + `SYMTAB_*` equ sentinels | DONE — ✅ (PR #185) — review delivered: 1 CRITICAL / 3 MEDIUM / 12 LOW; verified-clean coverage incl. SYMTAB sentinels collision-free. Fixes C1 (litpool `.ltorg` flush always advances segment counters — silent PC desync), M1 (`.equ`/`.set` value outside signed/unsigned 32-bit range now fails), M2 (imm9/imm7 deferred folds range-checked), M3 (litpool pc-map census "reset on every flush" comment corrected), L2 (`reader_at_end` pos-vs-end carry contract), L8 (`load_in_file` DI before each trampoline call) all in this PR. LOW residuals → i73. | repo-audit §6; PR #185 |
| **i18** | Wider naming review (`tests/m{N}`, `ci-m{N}`, `build-m3-disk`, milestone fixtures) | DONE — ✅ **DONE** — i52 PR 5/5 (branch `cleanup-5-renames`) | scope row "Directory naming" |
| **i34** | Untrack accidentally-committed Go binaries | DONE — ✅ `enctab-gen` untracked (PR #111); `llist-normalise` deleted with the LLIST cluster (i52 PR 4, q8 resolved 2026-06-10) | `.gitignore`; PR #111; i52 PR 4 |
| **i35** | `sdiv` missing across the whole stack (no mnemonic/Form → `.inst`; unencodable) | DONE — ✅ (PR #115) | deferred-backlog row; mirrored `udiv` ID 72 |
| **i36** | Z80 disasm: ccmp/ccmn decode (was → `.inst`) — added decode + encoder (ccmn ID 100); binutils-aligned | DONE — ✅ (PR #119) | `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-z80-go-disasm-parity-i9.md` |
| **i37** | base csinv/csneg (Rn≠Rm): Go `aarch64dec` now decodes them (IDs 101/102), encoder coverage added; aliases intact | DONE — ✅ (PR #119) | same analysis doc. The i9 sweep skip is removed; families now certified |
| **i38** | Audit for other skipped/excluded tests + PRs that left gaps rather than fixing them | DONE — ✅ (PR #118) — 14 skip sites, 13 legitimate, only the i9 gap (now fixed by i36/i37); no other papered-over gaps | `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-skipped-tests-and-gaps-audit.md` |
| **i39** | Next-gen maximally-efficient compact `.tbn` encoding (the design) — instruction overlay (assembled word + zeroed bits + sparse expression overlay, unifying literal/symbolic INST into one run), header label/offset table, one `.tbn` **v2** with a contiguous evictable editor region, name front-coding, bitfield-packing polish. **Shipped as milestone M8** (closed 2026-06-12). | DONE — ✅ **design agreed — Format B** (Pete 2026-06-08; all 5 open Qs resolved, §7) and shipped: i39a (#131) + i39b-1 (#151) + i39b-2 (#153). Residual i39c migrated to M9 (folds into i48c). | `docs/specs/compact-tbn-nextgen-design.md` §7; i39a, i39b, i39c |
| **i39a** | M8 Phase 1 — instruction overlay (unify literal/symbolic INST into one run) + header label/offset table; the v2 format flip | DONE — ✅ **PR(a)+(b)+i48b+(c) done — all 14 CI checks GREEN (incl. the SimCoupé matrix)** — v2 format (`KindInsnRun`) + overlay `Fold`/`ZeroSlot` (13 slots); refenc emit/decode + `bin2text` render + round-trip gate; **Z80 v2 reader + `INSN_RUN` decoder** (`src/insn_run.asm`, commit `5e3e8eb`) with the symbolic form-table path retired (net −370 B). The spectrum4 release **byte-matches GNU on the SAM** via the v2 compact `.tbn` (88,644→47,067 B; OUT 21752 B) + all 53 M3–M6 fixtures. **PR(d) DONE** (`f6d15ba` Go + `61f4a08` Z80) — header label/offset table; compact `.tbn` 47,067→45,189 B; harness byte-match; budget &BF68 (152 B). **i48d DONE** (`dc2b993`) — format reference → v2. **i48a split to a follow-up PR.** ✅ Complete on the branch; push + CI + §3 review pending, then merge. Branch `i39a-instruction-overlay`, **PR #131**. | `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-08-i39-phase1-instruction-overlay-plan.md`; PR #131 |
| **i39b** | M8 Phase 2 — name-table front-coding + comment/`.global`/base-hint editor sidecars (evictable region). **Split into i39b-1 (front-coding) + i39b-2 (editor-region split)** per the plan. | DONE — ✅ done — i39b-1 (PR #151) + i39b-2 (PR #153, merge `fbbd729`) | design §3.5/§3.6/§3.7, §5; `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-09-i39b-nametable-frontcoding-sidecars.md` |
| **i39b-1** | Name-table front-coding — each entry becomes `[shared_prefix_len uvarint][suffix_len uvarint][suffix bytes]` shared with the PREVIOUS name (encounter order, **no sort** so `name_id`s wired through every overlay patch + the header tables stay stable). Go `WriteFile`/`ReadFile` front-code/reconstruct; the Z80 `reader_init_skip_names` walks the front-coded entries via the existing LEB128 decoder + IN cursor (never decodes a name string). | DONE — ✅ **DONE** — PR #151 (merge `add0356`); all 15 CI checks green incl. the SimCoupé matrix + m6-release; §3 review = MERGE. Compact `.tbn` 45,189 → 44,207 B (−982 B, −2.2%); assembled binary byte-identical (GNU == Go(source) == Go(.tbn) == Z80/SAM, 21752 B); `TestCompactTbnAssembly`/`TestReleasePagedInLoad` + boot self-tests green; budget test `&BF94` (108 B headroom, skip +44 B). | PR #151; `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-09-i39b-nametable-frontcoding-sidecars.md` (i39b-1) |
| **i39b-2** | Editor-region split — the compact `.tbn` v2 splits into an **assembler-facing region** (header position tables + record stream) and a trailing **editor region** (front-coded name table + `.global` flags + comment sidecar) the assembler never reads, bounded by a new **`editor_region_offset` u32 section index at file offset 8**. Comments relocate to a PC-anchored sidecar (delta-varint + placement byte); `.global` becomes a name_id flag list; both leave the assembler record stream. Z80 `reader_init` reads the section index, sets `IN_END` to the editor boundary, and **deletes `reader_init_skip_names`** (the assembler stops mapping the name/comment page — the resident-RAM win). New host flag `-thin-comments=N`; a latent multi-line `/* */` block-comment render bug fixed. | DONE — ✅ **DONE** — PR **#153** (merge `fbbd729`). `release.s` comment text = **335,461 B** (7,502 `//`, ≈49% of source). Compact `.tbn`: **44,206 B** stripped (3 IN pages) / **59,539 B** `-thin-comments=20` (4 IN pages), both under the 96 KB/6-page ceiling; assembler-facing prefix byte-identical between them (`editor_region_offset` = 38,584); reclaimable editor region 5,622 B stripped / 20,955 B thinned. Budget test `&BF73` (141 B headroom, *more* than i39b-1's `&BF94`), prod `&B80E`. Assembled binary byte-identical (GNU == Go(source) == Go(.tbn) == Z80/SAM, 21752 B); disasm-roundtrip **105/105** (+ a per-fixture comment-count check + a host with-comments release leg, 7,014 `//`). Invariant: binary-identity + round-trip + `.tbn`-shrinks-or-holds. **Enables i40 eviction; does not evict.** | PR #153; `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-09-i39b-nametable-frontcoding-sidecars.md` (i39b-2); `https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md` |
| **i40** | Assembler-side editor-region eviction. **Load-path half ✅ DONE** (PR #181): the SAM loads only the assembler-facing `.tbn` prefix (two-phase HGTHD+HLOAD — head read to discover `editor_region_offset`, then prefix HLOAD); the editor region never enters the IN buffer on any machine size. The conditional-residency/reload half is superseded by the approved comment-storage design (`docs/specs/comment-storage-design.md` — the editor holds comments as a compressed-resident block store with watermark math and never re-loads the raw editor region), so no separate eviction mechanism remains to build beyond what that design already specifies (itself future editor-phase work). Residual: the page-aligned `editor_region_offset` normalisation branch (rem==0 → pages−1/16384) is verified against the harness convention + `samdos-file-io.md` only — no page-aligned fixture exercises it on SimCoupé yet (PR #181 review note). | DONE — ✅ load-path landed (PR #181) | `https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md`; design §7 (decision 1); i51 |
| **i48** | Single serialized format + pass-free syntactic encoder (the design). **A:** the compact overlay is the *only* serialized `.tbn`; the symbolic kinds (`KindInst`/`LabelDef`/`LocalDef`/`LitInsts`) become an in-memory IR, never written — old format buried, described in no head doc. **B:** text→overlay is a pure syntactic transform (no symbol pass); value-dependent bits computed in the fold at assemble time; forego GNU's silent `ldr→ldur`/`add lsl#12` rewrite (→ syntactic/error); narrow assemble-time `mov`→`movz`/`orr`/`movn` fallback. Makes the host mirror the eventual SAM flow (text→overlay must run on the SAM too). Refines i39/i39a. | DONE — ✅ **Host side COMPLETE** — strands i48a (#141/#142/#144) + i48b + i48d all merged; **i48c** (the Z80 text→overlay encoder) is future (editor phase). Decision A (overlay is the only serialized form; symbolic kinds are in-memory-only IR) and Decision B (syntactic encode, fold value-work) are both realised on the host. **i48c (the Z80 text→overlay encoder) is the M9 carry-forward.** | `docs/specs/i48-syntactic-encoder-design.md`; `https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md`; i48c |
| **i48a** | Host front-end unification — factor the three core capabilities (text→overlay encoder / overlay→bytes assembler / overlay→text renderer) as Go-authoritative shared libs, composed into **one integrated host tool** (q5; staged modes via flags); symbolic IR in-memory only; drop symbolic serialization. *(Absorbs the standalone `KindLitInsts`-removal item, the non-byte-affecting Decision-B strictness — symbolic mem → error, add/sub `lsl#12` syntactic, mov→movz fallback — and the q7 GNU-rewrite sweep.)* | DONE — ✅ **COMPLETE — all 3 phased PRs merged** (plan `https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-09-i48a-host-frontend-unification.md`). **PR1 (#141, `271b202`)** — byte-neutral library extraction: the 3 host tools became thin wrappers over one new module `tools/sam-aarch64` with shared libs `frontend`/`assemble`/`render`. **PR2 (#142, `1c2c0f9`)** — the integrated `sam-aarch64` binary (`source→{binary, compact .tbn}`, `.tbn→binary`, `--render`, `-E`; `SA64`-magic detection); symbolic `.tbn` became an in-memory IR (off disk, shape-a serialize-to-buffer); all ~150 call sites rewired; 3 old wrapper modules deleted. **PR3 (#144, `d07d627`)** — symbolic records are now a pure in-memory `[]format.Record` IR (`File.Records []byte`→`[]Record`; front-end builds structs directly; never serialized — shape-b); the symbolic-kind serialization (`WriteInst`/`WriteLabelDef`/`WriteLocalDef`/`WriteLitInsts` + the 4 reader decode-arms) is deleted from the format lib. Plus the Decision-B strictness (symbolic mem → `FoldMemImm12`-or-error; add/sub `lsl#12` syntactic; mov→movz default), the q7 sweep (resolved — no other GNU silent form-rewrite in the corpus), the i48d overlay-only doc rewrite, and the M1 string-goldens → re-assemble-and-byte-check vs GNU (36 dead goldens removed). All 3 PRs byte-neutral (GNU==Go==Z80/SAM via m6-release; all 14 CI checks green incl. the SimCoupé matrix; §3 reviews = MERGE). Post-merge cleanup (#146): the review's `WriteRaw` follow-up + a dead-code sweep dropped `WriteRaw` + 3 orphaned `Reset()` helpers + `SymbolTable.Name`/`Len`; a `staticcheck` (unused/U1000) CI gate now guards the core Go modules. | i48 spec §3, §7; q5 |
| **i48b** | Syntactic encoder + fold-time value-work — `FoldMovzAuto` computes `hw`; symbolic mem → `FoldMemImm12`-or-error; add/sub `lsl #12` syntactic; `mov` movz-default + assemble-time fallback. **Format-byte effect (movz-auto base word) → Go change must precede/accompany i39a PR(c).** | DONE — ✅ **DONE** — the byte-affecting `FoldMovzAuto` (`hw` computed in the fold; `ZeroSlot` clears bits 22:21) landed in commit `0162f52` with i39a; the non-byte-affecting strictness items (symbolic mem → `FoldMemImm12`-or-error, add/sub `lsl#12` syntactic via the fold + a split `ZeroSlot`, mov→movz default) + the **q7** sweep (resolved) landed in **i48a PR3 (#144)**. Byte-neutral on the corpus (0 affected sites, confirmed by the q7 sweep). | i48 spec §4, §7 |
| **i48d** | Doc unification — bring `tbn-binary-format-reference.md` to the current format; scrub head docs; historical banners on the M1/i1 design docs. | DONE — ✅ **DONE** — first brought v2-accurate (both profiles) in commit `dc2b993`, then **rewritten overlay-only in i48a PR3 (#144)** once the symbolic form became in-memory-only: `tbn-binary-format-reference.md` now documents a single on-disk format (the compact overlay v2 — header + name table + label/local header tables + INSN_RUN/LIT_DATA/DIRECTIVE/COMMENT + the 13 FoldSlots), with the symbolic kinds noted as in-memory IR only. Banners on the M1 + compact-tbn design docs. | i48 spec §6, §9 |
| **i49** | tools/ cleanup — delete two orphan tool dirs (`flatten-s`, `build-screen-disk`; off the build/CI/module path), prune 6 stale `.gitignore` binary-guard entries (4 for tools deleted in i48a/M0, 2 for the dirs removed here), drop their README rows, fix post-i48a doc-rot (harness README `make text2bin`→`make sam-aarch64`; ci.yml m6-release comment text2bin/refenc→sam-aarch64). | DONE — ✅ **DONE** — PR #149 (merge `fb9df4b`); a follow-up commit also refreshed the last stale `.gitignore` `text2bin` comment. | PR #149 |
| **i51** | Un-strip **all** comments on the SAM-side m6 gate — flow the full 335 KB of `release.s` comment text (7,502 `//` lines) through the SAM round-trip. **✅ DONE** (PR #181): full unstripped compact `.tbn` (363,295 B; assembler-facing prefix 38,584 B / 3 IN pages) flows through the release gate; SAM-assembled OUT byte-matches GNU 21,752 B. | DONE — ✅ DONE — PR #181 | `https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md`; depends on i40 load-path |
| **i52** | Repo cleanup: docs restructure + tracking consolidation + tooling hygiene + functional renames | DONE — ✅ DONE — PRs #155-#159 (2026-06-10) | spec in git history: https://github.com/petemoore/sam-aarch64/blob/2f4c898/docs/specs/2026-06-10-repo-cleanup-design.md |
| **i53** | Bump-arena reclamation for the fixed section-D scratch arrays | WONTFIX — ❌ YAGNI per the risk census — every section-D table is SAFE under ~5× kernel growth and the real ceilings (IN/OUT paged buffers) are page-allocation bumps, not arena problems. Revisit only if the census §4 trigger fires (fixed section-D array overflows at >5× kernel scale). | census (blob-pinned): <https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/specs/2026-05-29-bump-arena-risk-census.md> |
| **i54** | Synthetic-fixture oracle sweep for the on-SAM disassembler — a hand-built ~30-40-word corpus covering every family the Go decoder handles but `release.img` never exercises (§B of the i10 capability-parity report: cond-compare/select, extended-reg add/sub, signed multiplies, the load/store option matrix, …), run through the same per-word Z80-vs-Go oracle comparison as `TestDisasmOracle`. Converts §B from "structurally ported" to "empirically certified" at near-zero cost. | DONE — ✅ DONE — PR #183. 11 new `TestSyntheticParity_*` functions; 102 synthetic words across all §B families (neg/negs/cmn, eon/orn/bics, extr/ror-imm, variable-shift, bitfield-aliases, signed-loads, ldr-literal, reg-offset-mem, msr-pstate, bitmask-mov, movz/movn/movk-kept, mneg). One genuine Z80 gap found: EXTR/ROR-immediate decoder missing from `src/disasm.asm` — ported faithfully (cite: `tryDecodeExtr`, `aliases.go:778`). All 17 `TestSyntheticParity_*` (6 pre-existing + 11 new) + `TestDisasmOracle` 5438/5438 pass. Budget: prod 5762 B, test 3595 B. | report (blob-pinned): <https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-go-vs-z80-disasm-capability-parity.md> §"Recommended action" |
| **i57** | Comment-compression experiment (host-side, Go): benchmark Z80-feasible schemes against the real unstripped release editor region (335,461 B comment text, 7,502 comments, avg ~45 B) — static word dictionary, digram/BPE, canonical Huffman, dictionary+Huffman hybrid, block-based LZ (ZX0/ZX7/LZSA-class) at 1/2/4/8 KB blocks, `flate` as ceiling. Report ratio × decoder footprint × decode speed × random-access granularity × encode-on-SAM cost, plus a resident-pages capacity table (256 KB and 512 KB machines). Feeds the i59 decision. | DONE — ✅ DONE — best Z80-feasible: greedy LZ77/ZX0-style 85,526 B (ratio 0.287, 6 pages); no scheme fits 256 KB SAM (need ≤81,920 B = 5 pages); 512 KB SAM comfortable (15 pages free). Full results: `docs/notes/comment-compression-research.md`; tool: `tools/comment-bench/`. | i59; `docs/notes/comment-compression-research.md` |
| **i58** | Trinity interface capabilities research: what storage the Quazar Trinity offers (SD card? capacity? SPI access?), existing Z80 driver code in `trinload`, read/write feasibility as a comment-spill target and as the Phase-3 TFTP firmware source. Cited sources required. | DONE — ✅ DONE — findings in `docs/notes/trinity-capabilities.md` (SD slot via port `&DF`/SPI VERIFIED; raw-sector 800 KB-record model; no public Z80 SD driver — source in SAM Revival 21 print; ethernet driver ready for Phase-3) | i59; q9 |
| **i59** | Full-fidelity source residency on SAM — the unstripped `release.s` (`.tbn` + 335 KB comments) exceeds what the SAM can hold resident; decide the comment-storage strategy (compression multiplier × offload to disk/Trinity-SD × resident split). Umbrella decision: consumes i57 + i58 results; informs i40 scope (evict *compressed* blocks?) and the i51 full-comment gate ambition; Phase-2 editor prerequisite. Reducing comments at source is rejected (dev experience is the product goal). | DONE — ✅ RESOLVED via q9 (Pete 2026-06-10): compressed-resident on 512 KB; stripped-save (i65) covers 256 KB assembly; Trinity persistence deferred (i66). i40's urgency drops accordingly (eviction is no longer the 256 KB story). | q9; i57, i58, i60, i65, i66 |
| **i60** | On-SAM ZX0-format comment compression — measurement + design (**next work item**, Pete 2026-06-10). **Decision (Pete): option 2 — greedy ZX0-bitstream compressor ON the SAM**. Primary rationale: **self-hostedness — the development loop never leaves the SAM** (a host-recompression step would reintroduce a host dependency); also: compression only matters at save / memory-pressure time, and the assembler is unaffected (it never reads comments). Scope: (a) harness-measured decode T-states/byte for the real ZX0 standard+turbo Z80 decoders over real corpus blocks (replaces the note's unverified estimates); (b) Go-first greedy ZX0-format compressor with Z80-feasible structures, then Z80 port + harness speed measurement; (c) design: immutable compressed blocks + dirty-overlay + streaming save (O(dirty), not O(corpus)), per-block compressed? flag, **dirty-block watermark math** (never accumulate more dirty data than free RAM can recompress), **block-size sweet spot** (ratio × compress-time × scratch RAM — bigger blocks compress better but need more free memory to recompress), compressor placed **off-axis via paged_call** (audit co-residency on an existing prod page — 13 has ~15.5 KB free, 15 has ~6 KB — instead of burning a new page), and the `.tbn`-shrinks-or-holds carve-out for uncompressed-dirty blocks between host round-trips. | DONE — ✅ DONE — **(a)** (PR #163); **(b-1)** (`tools/zx0-greedy` Go module; H=512 D=16: greedy ratio 0.3631 at 4 KB, 9246 B scratch, 384/384 oracle checks passed); **(b-2)** (`src/zx0_compress.asm`, 24/24 blocks byte-identical + round-trip; post-i67 972 T/byte → 0.66 s per 4 KB block at 6 MHz); **(c)** design approved + merged (PR #173, Pete 2026-06-11): `docs/specs/comment-storage-design.md` — 4 KB blocks H=512 D=16, turbo decoder, page-13 co-residency + page-14 staging, watermark R=2, greedy-everywhere with the `.tbn`-shrinks-or-holds carve-out. Implementation: i68. | i59, q9; `docs/notes/comment-compression-research.md` |
| **i61** | Corpus sweep for Trinity/B-DOS/SAM-Revival artifacts (800 disk images) | DONE — ✅ DONE 2026-06-10 — found: B-DOS 1.5t beta 6 binary + period Trinity utilities (SD select bytes recovered → trinity-capabilities.md updated); B-DOS 1.5a complete Z80 source (the public fork-point source); 1.6c/1.7D/1.7J/1.7N binaries; a uIP TCP/IP stack for the SAM (Phase-3 relevant). NOT found: any SAM Revival disk, the Quazar manual. Detailed catalogue in private session records (publication policy). | q9; `docs/notes/trinity-capabilities.md` |
| **i62** | B-DOS storage-backend investigation: boot B-DOS AL 1.5a + emulated Atom Lite under SimCoupé, exercise our SAMDOS-hook file-io against a B-DOS record, prove the hook-level portability claim end-to-end (floppy/SAMDOS ↔ Atom-Lite/B-DOS ↔ Trinity/1.5t). Version plan + hook-compatibility analysis: `docs/notes/bdos-version-landscape.md`. Spill route for i40/i59 if proven. | DONE — ✅ DONE (2026-06-11, PR #179) — dual-run proof executed: ONE probe binary (DVAR-7 detect → HRECORD 1 → HSAVE/HGTHD/HLOAD round-trip) passed under SAMDOS 2 + floppy AND under B-DOS AL 1.5a + emulated Atom Lite (`DOS:BDOS V=05 R=000B`, all phases, OK; HSAVE'd bytes independently found in the HDF at the source-predicted record-1 sectors). Hook portability SAMDOS ↔ B-DOS AL: LIKELY → **VERIFIED**; Trinity/1.5t leg stays LIKELY (no emulation — real hardware needed). Rig (`tools/i62-bdos-experiment/`: probe + disk builder + programmatic B-DOS-formatted HDF generator) is re-runnable locally or in the CI container, not a CI gate (worldofsam images stay outside the repo). Findings + repro: `docs/notes/bdos-version-landscape.md` §"Empirical verification (i62)". | q9; `docs/notes/bdos-version-landscape.md`; `tools/i62-bdos-experiment/README.md` |
| **i63** | Superpowers extraction: study `obra/superpowers` (MIT), keep/adapt/drop per skill, encode keeps as repo-native rules, then disable the plugin for this project and run the next work item bare as the test. Rules landed in CLAUDE.md (PR #167, Pete-approved verbatim); plugin disabled for this project 2026-06-10. The bare-run test is simply: every item from here on. | DONE — ✅ DONE | `~/git/superpowers` (local clone) |
| **i64** | Blog post: the 2026-06-10 session log (repo cleanup → compression research → superpowers discussion), drafted by the agent with consent, privacy pass per the publication policy before publishing. Session snapshots preserved in private archive. | DONE — ✅ DONE — first post published 2026-06-10 (petemoore.github.io: "When the agent helps choose its own process"); the tradition continues per private memory. | publication policy (private memory) |
| **i67** | Night-shift T-state optimization of the Z80 ZX0 path. Baseline first: exact total T-states to compress AND decompress the full release comment corpus (all blocks, 4 KB + 8 KB) with the current code. Then optimize `src/zx0_compress.asm` (2557 T/byte measured — known headroom: absolute-address state vars, bit-writer call overhead) under a hard invariant: **identical output bytes** (the byte-identity-vs-Go + round-trip oracles are the proof; same algorithm, same (H,D), only faster code). Decode side: evaluate upstream ZX0's faster decoder variants beyond standard/turbo and vendor+measure if licence-clean. Includes a deliberate instruction-set recap (full Z80 repertoire incl. undocumented opcodes — IXH/IXL/IYH/IYL, SLL etc.: establish support in pyz80, koron-go/z80, SimCoupé, and real Z80B before relying on any; documented-only unless the win is large and support is proven). Deliverable: before/after totals (X→Y T-states; seconds at 6 MHz) + per-block table in the research note. | DONE — ✅ DONE (2026-06-11, night shift) — **compress 804.1M → 289.7M T (−64.0%) at 4 KB blocking; 134.0 s → 48.3 s at 6 MHz** (8 KB: 866.5M → 294.5M, −66.0%); 2698 → 972 T/byte; oracle byte-identical on all 110 whole-corpus blocks at every step. Decode side: vendored + measured dzx0_fast (187 B, 48.8 T/B) and dzx0_mega (673 B, 46.6 T/B) vs standard 63.6 / turbo 50.9. Full X→Y tables, optimization log, and undocumented-opcode support matrix in the research note (§i67). | i60; `docs/notes/comment-compression-research.md` |
| **i68** | ZX0 integration — the implementation companion to i60c's design: (1) promote the ZX0 decoder from harness testdata to `src/` as product code (variant per i60c's choice); (2) wire compressor + decompressor into the boot path (loader function, `build-disk` flag, page placement per i60c); (3) BUILD_TESTS boot self-tests for both — compress a fixed block and byte-compare against expected bytes baked at build time from the Go authority (deterministic, so CI's SimCoupé matrix becomes the empirical execution proof, incl. the IXH/IXL undocumented-opcode question); (4) re-point `comment-bench`/`release-unstripped-tbn` at the vendored `tests/release/release.s` so no spectrum4 checkout is needed; (5) ARCHITECTURE.md §5 rewrite opening with the BASIC-flat-address-space framing (Pete, 2026-06-11). Gate: full SimCoupé matrix green with the new self-tests in the test variant. | DONE — ✅ DONE (PR #176, 2026-06-11) — (1) dzx0_turbo.asm promoted to `src/` (org &8B00; harness keeps testdata copies for variant benchmarks); (2) combined page-13 payload `zx0.bin`/`zx0-test.bin` at &8400 (256 B-aligned) per design §5, `-zx0` build-disk flag ("zx013"), `load_zx0_payload` in BOTH variants via the new offset-bearing `load_payload_generic_at`, sysreg-style test_mem time-multiplex sequencing; (3) baked-bytes boot self-tests (1 KB release.s comment fixture, compress vs Go-authority bytes + turbo-decode vs raw, tags &E1-&E3) — green in the SimCoupé matrix, the standing kosarev/z80 IXH/IXL execution proof; (4) `release-unstripped-tbn` re-pointed at vendored `tests/release/release.s` — flatten reproduces the corpus exactly (7,012 rows / 291,038 B), zero delta; (5) ARCHITECTURE §5 rewritten with the flat-address-space framing. Budget: test 141→86 B headroom, prod +38 B. | i60; `docs/notes/comment-compression-research.md` |
| **i71** | B-DOS reference + Trinity-fork deep-dive: (a) vendor `reference/bdos/` — the B-DOS 1.5a Z80 source (the public fork-point source, from `Bdos15a.zip`, detokenised to readable text) + the AL 1.5a binary the i62 rig consumes, with a provenance README citing Edwin Blink's freeware/public-domain grants (worldofsam.org copyright pages); (b) disassemble B-DOS 1.5t beta 6 (private reference materials) and diff against 1.5a to document how the Trinity fork presents SD storage as Atom-compatible disks (sector-access layer, `&DC`/`&DF` SPI driver) and what Colin Piggot/Chris Pile changed — the commented fork disassembly stays in `~/sam-archive` (WoS marks Trinity software "Copyrights Declined"; ask-Colin is the route to publishing more), with a neutral findings doc in the repo; (c) re-point the i62 rig at the vendored reference copy. | DONE — ✅ DONE (2026-06-12, this PR) — (a) **vendored**: `reference/bdos/` now carries `Bdos15a.zip` (pristine), `bdos15a.src.txt` (detokenised `BDOS15a .S`), `al-bdos15a.bin` (the 10701-byte AL build the i62 rig boots), + a provenance/licence README (Edwin Blink's `BDOSINFO.T` FREEWARE statement + the WoS public-domain copyright grants). The 1.5t Trinity fork is deliberately NOT vendored (WoS "Copyrights Declined"). (b) **analysis done**: B-DOS 1.5t beta 6 disassembled + diffed against 1.5a at instruction level (annotated disassembly + `ANALYSIS.md` in `~/sam-archive/bdos/analysis/`, private); the publication-safe findings doc is `docs/notes/bdos-trinity-fork-analysis.md`. **Hook surface verdict: verified-static** — all 39 hook vectors map 1:1, dispatch + handler bodies are 1.5a's bytes under relocation; only the sector-device layer (Atom ATA → SD/SPI) is swapped. Corrections folded into `bdos-version-landscape.md` + `trinity-capabilities.md` (DVAR 7 = 5 under 1.5t; SD select/probe/status bytes confirmed; auto-null verified; filler-byte OQ closed). (c) **i62 rig re-pointed** at `reference/bdos/al-bdos15a.bin` (the `BDOS_BOOT_MGT` re-extract path kept as an optional override). Runtime confirmation on real Trinity hardware remains outstanding (no emulator covers the ports). | `docs/notes/bdos-trinity-fork-analysis.md`; `docs/notes/bdos-version-landscape.md`; `docs/notes/trinity-capabilities.md`; `reference/bdos/README.md`; q10 |
| **i72** | SAMDOS→B-DOS reorientation: B-DOS becomes the documented target DOS across the living docs (ARCHITECTURE, ROADMAP design-notes table, `samdos-file-io.md` — broadened with the i62-verified B-DOS compatibility preamble + the HRECORD extra, `reference/` READMEs, `src/sam_io.inc`/`src/trampoline.asm` comment framing), with SAMDOS references kept as redirects to the B-DOS story; assess + prepare the boot-disk/CI migration samdos2→B-DOS (a `-dos` flag on `tools/build-disk` mirroring the i62 `build-i62-disk` pattern; execution gated on q10); restore the SAMDOS source citation trail (the `~/git/samdos` clone comes from https://github.com/stefandrissen/samdos — now cited in `samdos-file-io.md`). | DONE — ✅ DONE (this PR) — **docs reorientation + `-dos` prep.** `samdos-file-io.md` reframed as the DOS hook-layer reference (B-DOS = target DOS, SAMDOS 2 = shipped boot DOS pending q10; SAMDOS 2 source stays authoritative for shared hook semantics; HRECORD &9C noted as the one backend-conditional step). ARCHITECTURE.md + the ROADMAP design-notes table reframed "the DOS hook layer (SAMDOS 2 / B-DOS)" where it matters. `src/sam_io.inc` + `src/trampoline.asm` comments reframed DOS-neutral (comment-only — built binaries byte-identical). `tools/build-disk` gains `-dos`/`-dos-name`/`-dos-load` (default SAMDOS 2 at 491529; the default `.mgt` is byte-identical to before; the 10000-byte hard check applies only to the default samdos2 path, a generous boot-region sanity bound otherwise). The default/CI swap decision lives in **q10**. | i62; q10; `docs/specs/samdos-file-io.md` |
| **i75** | B-DOS boot-disk swap execution (the q10 resolution, incremental): (1) build B-DOS boot-disk variants via `build-disk -dos` (prep landed i72/#187, binary vendored i71/#186); (2) prove the full SimCoupé gate suite green booting B-DOS — including AL 1.5a behaviour with no Atom Lite attached (the untested corner); (3) flip the shipped/CI default to B-DOS, retaining samdos2 build capability. | DONE — ✅ DONE (this PR) — **proof + flip.** `tools/build-disk`'s default DOS flipped from SAMDOS 2 to B-DOS AL 1.5a (`reference/bdos/al-bdos15a.bin`, name `bdos`, start address 32777, the i62-verified convention); the samdos2 byte-exact size guard re-keyed off the `reference/samdos/samdos2.bin` path so the SAMDOS 2 compatibility build (`-dos reference/samdos/samdos2.bin -dos-name samdos2 -dos-load 491529`) stays fully functional. **Local proof** (plain floppy machine, **no Atom Lite / no HDF**, SimCoupé v1.2.16, the i62 rig's headless build): every gate booted B-DOS and passed — core 9/9, symbols 5/5 + symbols-prod 5/5, operands 22/22 + operands-prod 22/22, paged + paged-prod, release-gate 3-way byte-match (GNU == Go == Z80); plus `go test ./...`, boot self-tests, `make check-budget` unchanged. Because every gate script calls `build-disk` with the default DOS, the PR's own CI matrix boots B-DOS — that matrix is the formal proof. | q10; `docs/notes/bdos-trinity-fork-analysis.md`; `docs/notes/m9-status.md` |
| **i76** | Host-side Go TUI editor prototype at SAM-faithful geometry — the functional UX authority for the Phase-2 editor; renders through a strict SAM-screen abstraction (terminal backend + PNG/SCREEN$ mockup backend = the i5/q1 renderer); P1 feeds i4, informs i5/i6. | DONE — host-side Go TUI editor prototype delivered (phases split to i76a–i76d); the on-SAM editor proper is the broader M9 work. | `docs/specs/editor-tui-prototype-design.md` (§8 decisions); `docs/specs/editor-rendering-rules-design.md`; `tools/editor-prototype/`; `tools/editor-prototype/configs/`; `tools/font-proof/`; `docs/notes/m9-status.md`; q13; i4, i5, i6, i41, i48 |
| **i76a** | P1a: samscreen abstraction + tcell terminal backend + PNG/SCREEN$ mockup backend + i4-parity read-only viewer + -geometry mode×font matrix | DONE — DONE — P1a: samscreen abstraction + tcell terminal backend + PNG/SCREEN$ mockup backend + i4-parity read-only viewer + `-geometry` mode×font matrix (tools/editor-prototype). | `docs/specs/editor-tui-prototype-design.md; tools/editor-prototype/; i4, i5, i6` |
| **i76b** | P1b font-proof (2026-06-12): five-pixel-font vendored; make font-proof real-frame screenshots 85×32 (6×6) vs 64×24 (ROM 8×8) | DONE — DONE — P1b font-proof (2026-06-12): five-pixel-font vendored; `make font-proof` real-frame screenshots 85×32 (6×6) vs 64×24 (ROM 8×8) — the i6 geometry evidence. | `tools/font-proof/; i6` |
| **i76c** | P2 config-lab (2026-06-12): tools/editor-prototype -config interactive rendering lab over the real release .tbn at locked MODE 3 64×24 | DONE — DONE — P2 config-lab (2026-06-12): `tools/editor-prototype -config` interactive rendering lab over the real release `.tbn` at locked MODE 3 64×24. | `tools/editor-prototype/configs/; q13` |
| **i76d** | Rendering-rules spec landed (docs/specs/editor-rendering-rules-design.md §3–§10); default-config recommendation tracked as q13 | DONE — DONE — rendering-rules spec landed (`docs/specs/editor-rendering-rules-design.md` §3–§10); default-config recommendation tracked as q13. | `docs/specs/editor-rendering-rules-design.md; q13` |
| **i78** | Source-structure preservation in the v2 editor region (i76 lab finding, Pete 2026-06-12): (a) blank lines — the corpus has 1,472 blanks in 888 runs (~11% of lines); encode as run-of-blanks editor-region rows (~3 KB) NOT as empty comments (214 textless `//` lines exist, 114 are paragraph separators inside comment blocks — conflating them would be ambiguous); (b) decide multi-line-comment grouping (currently one sidecar row per line; single-entry-with-user-line-breaks is open — interacts with comment anchoring + the comment-reader); (c) code/loop indentation — whether to retain indentation is an OUTPUT of the editor interface design / sign-off (at 64 cols screen estate may not justify it, and the compressed rendering already reclaims leading whitespace); capture the consideration now, but gate implementation DOWNSTREAM of interface sign-off — only if the interface keeps indentation do we preserve leading whitespace on code lines (same editor-region mechanism as the blank-line runs in (a)); do not build preservation for a feature that may be abandoned (Pete 2026-06-13). Invariant: render-to-text reproduces the source's blank/comment structure exactly. | DONE — ✅ **(a) IMPLEMENTED host-first + CI-gated** (PR #218) — the `kind u8` sidecar discriminator (`0`=comment, `1`=blank-run) + `FlagTaggedSidecar` header bit (legacy untagged files still read), the `KindBlankRun` in-memory IR record (parser emits, pass1 anchors, `Compact` relocates), blank emission in both renderers, and a **corpus round-trip blank-line assertion** (per-fixture exact; PROGBITS-only for the flattened release source). Verified: all 109 round-trip fixtures green incl. release.s (1340 blank lines round-tripped); binary byte-match untouched (editor-region only). Finding (→ i90): blanks inside `%nobits`/BSS sections are not preserved through `-flatten` (the whole section body is dropped — it has no flat-image PC), a pre-existing flatten property, not an i78 loss. (b) **multi-line-comment grouping DECIDED** (design §4) — one sidecar entry per source comment with the user's line-breaks as embedded body newlines (paragraph break = `\n\n`), consistent with the i39b-2 `/* */` render path; no code beyond what the format already does. (c) **indentation DEFERRED + gated** downstream of the i76 interface sign-off — same editor-region mechanism (`kind≥2` reserved) if the signed-off interface keeps it; captured, not built. Z80 half (parse/render in the editor) rides i48c. | `docs/specs/source-structure-preservation-design.md`; i76 lab findings; `docs/specs/comment-storage-design.md`; i41, i48c, i39b-2, i85, i90 |
| **i79** | `samscreen.DefaultCLUT[5]` palette fix — found by the #200 (P1a re-land) scoped review: it is `18` (purple-blue) but the SAM ROM power-on value at index 5 is `80` (true cyan, per the ROM CLUT table). Affects MODE 4 mockup PNGs / SCREEN$ colour 5 only; the MODE 3 viewer (CLUT 0–3) is unaffected. Verify the ROM value independently first (review findings are claims), then fix as a standalone follow-up PR. | DONE — ✅ DONE (this PR) — verified against the SAM ROM power-on CLUT at 0x7d51 (two independent ROM copies): index 5 = 80 (true cyan), not 18; every other DefaultCLUT entry already matched the ROM. One-line fix; editor-prototype tests green. | `tools/editor-prototype/samscreen/palette.go`; PR #200 review |
| **i81** | Upstream SimCoupé issue triage — review Simon Owen's open SimCoupé issues and assist where we can (we already build + patch SimCoupé locally; the aarch64 PNG-endian fix is one ready contribution). Community give-back + keeps the vendored build close to upstream. | DONE — SimCoupé upstream open-issue triage complete (2026-06-13); produced contributions i81a/i81b/i81c (now tracked as open rows). 🔬 **triage done (2026-06-13)** — submission is Pete's call (community contact). Findings: (1) the **PNG-endian fix** is `tools/font-proof/simcoupe-local-capture.patch` hunk 2 — one line in `Base/SimCoupe.h` adding `\|\| defined(__aarch64__)` to the little-endian platform guard; on 64-bit ARM Linux the `__arm__`-only guard falls through to big-endian, so PNG length/IHDR fields aren't byte-swapped and screenshots are corrupt. **Confirmed NOT in upstream HEAD** (`0e8a69f3` = v1.2.16 = our pin); genuine, reproducible, one-line — **ready to offer as a standalone upstream PR**. (2) The other two patch hunks (SDL `dummy`-driver headless renderer fallback; `-pngonhalt`) are project-specific, not standalone upstream candidates. (3) **Upstream open issues (8, snapshot)** — none touch ENC28J60/SPI/Trinity/network (no overlap with i80); only **#28** (unit-test support) is in our wheelhouse — our `DI;HALT`+BC-return+`-exitonhalt` idiom is exactly the pass/fail detection the thread asks about (and `-exitonhalt` is now on upstream main as `a65a16e`). **Recommendation for Pete (priority):** (a) PNG-endian one-line PR; (b) a short #28 comment on pass/fail detection; (c) maybe offer the SDL headless fallback. | the aarch64 endian patch (`tools/font-proof/simcoupe-local-capture.patch`); i80 |
| **i82** | SAM TFTP client (standard) — a trinload-evolved, RFC-conformant TFTP client so the SAM can fetch disk / firmware images from a standard TFTP server (e.g. Pete's Mac TFTP server that already serves Pi images). The near-term bootstrap-delivery enabler: with Colin Piggot's custom ROM giving Trinity-storage access at boot, a disk in Trinity storage running this client fetches images over the network, writes them to Trinity storage (B-DOS records), and boots them — closing the get-code-onto-SAM loop with no physical disk shuffling. Includes the TFTP RFC corpus research: RFC 1350 (core) + 2347 (option extension / OACK) + 2348 (blksize) + 2349 (timeout / tsize) + 7440 (windowsize). trinload can bootstrap the client onto the SAM. | DONE — ✅ RFC research + trinload analysis DONE — `docs/notes/tftp-protocol-research.md` (PR #210). RFCs 1350/2347/2348/2349/7440 fully documented with wire formats, option negotiation, SAS fix, windowsize/blksize trade-offs. trinload layer analysis complete: reusable routines catalogued, precise RRQ-client delta identified (~150–200 new Z80 lines). Recommended RRQ option set: blksize=1428, tsize=0, timeout=2, windowsize=4. **Transfer loop LANDED + host-verified end-to-end** (this PR) — `src/netboot/tftp_client_loop.asm` is the receive-side state machine: `tftp_recv_data` (`drv_read` → UDP dst == our TID? opcode DATA(3)? learn the server endpoint on the first DATA, validate the server TID thereafter; `block == acked+1` → accumulate the payload into STAGING + ACK; duplicate → re-ACK; future → ignore; short final block ends it; a wrong server TID → ERROR(5) to the *stray sender* per RFC 1350 §4) and `tftp_recv_timeout` (the Sorcerer's-Apprentice fix: retransmit the last ACK only, never the RRQ). It composes the host-verified `build_udp_frame`/`tftp_client` (parse_oack) + the real `encdrv.asm`, runs over the **i80 emulated Trinity**, and `tftp_client_loop_test.go` asserts the ACK frames + the two-block transfer (with the file bytes accumulated) + the unknown-TID ERROR(5) + the SAS retransmit on the virtual wire match the new Go authority `tftp/clientloop.go::ClientLoop` **byte-for-byte**. (Found + fixed an RFC 1350 §4 framing bug in the Go authority: the unknown-TID ERROR goes to the stray sender, not the learned server.) The request side (`build_rrq`/`parse_oack`) landed in i92 (#225). **Origination front LANDED + host-verified end-to-end** (this PR) — `src/netboot/tftp_client_front.asm` is the step *before* the receive loop: `tftp_send_arp` (`build_arp_request` → broadcast "who has SERVER_IP?" → `drv_write`), `tftp_recv_arp` (`drv_read` → is it an ARP reply (EtherType &0806, OPER=2) for SERVER_IP? learn the server MAC, BC=1; else BC=0 → keep reading), and `tftp_send_rrq` (`build_rrq` octet + the settled ClientOptionSet → wrap UDP our IP+TID → the learned server MAC+IP:69 → `drv_write`). It composes the host-verified `build_arp_request`/`build_rrq`/`build_udp_frame` + the real `encdrv.asm`, runs over the **i80 emulated Trinity**, and `tftp_client_front_test.go` asserts the ARP request + the RRQ frame on the virtual wire match the new Go authority `tftp/clientfront.go::ClientFront` (+ `frame.ParseARPReply`) **byte-for-byte**, plus the wrong-IP-reply-ignored control. The B-DOS write-out seam (the UIFA/DIFA arithmetic gluing the received-blocks→records write to the HSAVE hook) landed as **i93** (`bdos_seam.asm`'s `bdos_fill_save_uifa`, host-verified field arithmetic; the RST 8 HSAVE dispatch stays hardware-gated). **Client boot disk LANDED (increment 3, this PR):** the Go authority `client/client.go::Client` (the combined ARP→RRQ→DATA/ACK fetch phase machine, composing `ClientFront` + `ClientLoop`) + the Z80 port `src/netboot/netboot_client.asm` (`client_first` broadcasts the ARP; `client_run_once` steps PHASE_ARP → learn MAC + send RRQ → PHASE_XFER → accumulate DATA into STAGING / send ACKs → short final block ends it → stray-TID DATA → ERROR(5)), a *new* phase machine composing the host-verified `build_udp_frame`/`build_arp_request`/`tftp_client`/`bdos_seam` + the real `encdrv.asm` directly (NOT `include`ing the standalone front/loop files — symbol collisions). Host-verified over the i80 emulation byte-for-byte vs the Go authority (`netboot_client_test.go`: the ARP request + the RRQ + the ACK cadence + the accumulated STAGING bytes; full-fetch + wrong-IP-ARP-ignored + stray-TID-ERROR(5)). **Bootable disk shipped** (`make netboot-client-disk` → B-DOS boot + AUTO LOADs+CALLs `client_main` at &8000, which reads the EEPROM MAC/IP, fetches the configured file from the configured server, and HSAVEs it to Trinity storage) + the on-hardware guide (`netboot-trinity-testing.md` §"Increment 3"). The B-DOS HSAVE write-out (received bytes → records) is the real-hardware-only step (behind `NETBOOT_HOSTTEST`, not host-verifiable — no ROM/SAMDOS/RST 8 in the harness); the i93 field arithmetic it uses IS host-verified. Wired into `make ci-netboot-z80`. **Emulation-verified ≠ hardware-verified** — the HSAVE write-out + the end-to-end fetch with a real TFTP server stay Pete's real-Trinity test. | `docs/notes/tftp-protocol-research.md`; `docs/specs/phase3-delivery-design.md`; `src/netboot/netboot_client.asm`; `src/netboot/tftp_client_loop.asm`; `src/netboot/tftp_client_front.asm`; `tools/netboot-oracle/client/client.go`; `tools/netboot-oracle/tftp/clientloop.go`; `tools/netboot-oracle/tftp/clientfront.go`; `simonowen/trinload`; i80, i83, i84, i86, i93 |
| **i84** | Phase-3 end-to-end delivery design — the integrating spec for the self-hosting boot/delivery loop: Colin Piggot's custom ROM (Trinity-storage access at boot, no physical B-DOS disk needed) → a Trinity-storage disk running the TFTP client (i82) → fetch disk / firmware images from a standard TFTP server → persist to Trinity storage (B-DOS records) → boot them; plus the reverse PXE leg via the TFTP server (i83). Produce the plan + design doc (extend or supersede `phase3-tftp-design.md`); start with the RFC research + the trinload / Colin-ROM / Trinity-storage mechanics. No hardware needed to design. | DONE — ✅ **design CONFIRMED (Pete 2026-06-14)** — `docs/specs/phase3-delivery-design.md` (composes the i82 RFC/trinload research + trinity-capabilities into the self-hosting loop). q11+q12 resolved; doc revised — **Model A only**, SAM = **DHCP+TFTP server** serving **by name** (model-agnostic; all aarch64 TFTP-boot Pis, Pi 3 + Pi 400 first), **both topologies**, on-SAM **HTTP self-provisioning** (i70), bootstrap via Pete's **patched SAM ROM** (i87, dump pending), new DHCP item **i86**. Remaining gate = final go-ahead to implement; the only open work is execution research (Pi file list + DHCP conventions) settled by packet capture. Supersedes `phase3-tftp-design.md` on go-ahead. | `docs/specs/phase3-delivery-design.md`; `docs/notes/tftp-protocol-research.md`; q11, q12; i70, i80, i82, i83, i86, i87 |
| **i85** | Whole-file ZX0 vs the compact overlay — size measurement (Pete 2026-06-13, one-shot). Question: could storing `release.s` as one ZX0-compressed text blob (simpler, preserves ALL whitespace for free, and validate-on-entry is independent of storage so it's retained) beat the current hybrid (compact `.tbn` instruction overlay + ZX0-compressed comments)? MEASURED with the real 4 KB-block ZX0 (`tools/zx0-greedy`, H=512/D=16): zx0(full interleaved `release.s`) = 201,587 B vs the current hybrid 170,132 B (= 38,584 B instruction overlay + 131,548 B zx0(comments)) → whole-file is +18.5% LARGER. Key finding: the instruction overlay (38,584 B) beats zx0(code-as-text) (41,867 B) by only ~3.3 KB (8%); most of the 18.5% gap is comment/code interleaving within blocks (separating the streams recovers it → ~+3 KB vs current). So the overlay's value is assembly SPEED + the in-memory editable IR, NOT size. Conclusion: do NOT rewrite for size (wash-to-slightly-worse); the overlay earns its keep on speed; solve whitespace preservation within the current design (→ i78). Caveat: the measured comment figure 131,548 B exceeds i57's 85,526 B (i57 used a stronger scheme), but the relative comparison holds since comments cost the same on both sides. | DONE — ✅ measured — no rewrite (Pete 2026-06-13) | i78; `docs/notes/comment-compression-research.md`; i57, i60 |
| **i91** | Phase-3 netboot **host oracle harness** (`tools/netboot-oracle/`, new Go module — 2026-06-14, netboot impl plan §8.1 foundation). The pre-i80 verification net for the netboot protocol logic: pure-Go DHCP + TFTP packet builders/parsers (the byte-for-byte authority for the Z80 i82 client / i83 server / i86 DHCP responder) replayed against **masked golden vectors** extracted from Pete's real Pi 400 capture (`~/tftp-logs`, off-repo). Packages: `frame` (the §1.2 Ethernet/IPv4/UDP offset contract + `BuildUDPFrame`, the Go authority for the Z80 `build_udp_frame` fresh-frame primitive trinload lacks), `dhcp` (OFFER/ACK builder + the fixed option-43 "Raspberry Pi Boot" blob), `tftp` (RRQ/OACK/DATA/ACK/ERROR + serve-by-name resolve: 404 serial-subdir, ERROR(1)-on-miss-keep-serving + the client/server transfer-loop state machines: SAS retransmit fix, streamed send, short-final-block termination), `pcap` (dependency-free libpcap/pcapng reader), `golden` (committed masked vectors via `cmd/gen-golden`). Wired into CI as the `netboot-oracle` pure-Go job + `STATICCHECK_MODULES`. **Validates protocol logic only** — the Z80 execution, ENC28J60 I/O, and end-to-end Pi boot are NOT host-verifiable (gated on i80/real-Trinity). | DONE — ✅ **DONE** (PR #219) — module + golden vectors + 17 oracle tests green (incl. `BuildUDPFrame` round-trip + byte-exact RRQ/OACK builders vs the capture + the client/server transfer-loop state machines); CI-gated; no off-repo captures committed (real MACs/IPs/serials masked to documentation placeholders, option-43 blob byte-identical) | `tools/netboot-oracle/`; `docs/plans/phase3-netboot-implementation-plan.md` §6/§8.1; `docs/notes/pi-netboot-capture-analysis.md`; i82, i83, i86, i80 |
| **i94** | Phase-3 netboot **bring-up smoke test** — the first *bootable, runnable* netboot program (the components from i91/i92/i80/i93 were verified pieces; this is the first one Pete can boot on real Trinity). `src/netboot/smoke_test.asm`: a tiny program that runs `drv_init` on the real Trinity then loops answering **ARP requests for the SAM's IP** with an ARP reply — one observable network action confirming the Ethernet path comes up and talks (the "hello, the wire works" before the full server). New `build_arp_reply.asm` primitive (the inverse of `build_arp_request`; the port of the new Go authority `frame/frame.go::BuildARPReply`) + a new `smoke/smoke.go::Responder` Go authority. The bootable program reads the SAM's MAC + IP from the Trinity EEPROM "Trinity Network " chunk via the vendored `eeprom.asm` (Colin Piggot, simonowen/trinload, verbatim). Ships a **bootable SAM disk** (`make netboot-smoke-disk` → B-DOS boot + AUTO that LOADs+CALLs the program at &8000) built by a new `build-disk -netboot` mode, plus the "how to test on real Trinity" guide. **Verification (the honesty line, CLAUDE.md §5):** `smoke_serve_once` IS host-verified end-to-end over the i80 emulation — inject an ARP request, the real `encdrv.asm` + `build_arp_reply` run, and the reply on the virtual wire is byte-for-byte `smoke.Responder.OnFrame` (`smoke_test_test.go`, 5 cases incl. wrong-IP/non-ARP/empty-wire controls; `make ci-netboot-z80`). NOT host-verifiable: the EEPROM config read, the real ENC28J60 TX/RX silicon timing, and the actual ARP round trip with Pete's machine — gated on real Trinity (`smoke_main` + `eeprom.asm`). Emulation-verified ≠ hardware-verified. | DONE — ✅ **DONE** (this PR) — smoke program + ARP-reply primitive + Go authority + bootable disk + testing guide, all host-verified green + CI-gated; the on-hardware ARP round trip is Pete's test (`docs/notes/netboot-trinity-testing.md` §"Increment 1"). | `src/netboot/smoke_test.asm`; `src/netboot/build_arp_reply.asm`; `src/netboot/eeprom.asm`; `tools/netboot-oracle/smoke/smoke.go`; `tools/netboot-oracle/frame/frame.go`; `docs/notes/netboot-trinity-testing.md`; `tools/build-disk/main.go`; i80, i82, i83, i86, i91, i92, i93 |
| **i96** | Phase-3 **serve-files TFTP demo** — a focused, bootable "serve a few files to a plain TFTP client" demo server, distinct from the i95 Pi-netboot server: **ARP + TFTP only, no DHCP and no Pi PXE option-43 blob** — so it is testable from any machine with a stock `tftp`/`curl` client (no Raspberry Pi, no DHCP setup). Pete asked for it directly (2026-06-14): bake a couple of small demo files in, boot → it serves, Pete `tftp get`s them. **LANDED (this PR):** Go authority `tools/netboot-oracle/serve/serve.go::Responder.OnFrame` (ARP request for our IP → ARP reply; UDP:69 RRQ → serve-by-name; UDP:our-TID ACK → next DATA) + the Z80 port `src/netboot/netboot_serve.asm`'s `serve_serve_once` — a *new* single-dispatch state machine composing the host-verified `build_udp_frame`/`build_arp_reply`/`tftp_build`/`tftp_parse` + the real `encdrv.asm` directly (NOT `include`ing the standalone loops — symbol collisions). The **one behaviour beyond i95:** a bare RRQ with no options (what a classic `tftp get` sends) is answered per **RFC 2347** with **DATA block 1 directly** at the 512-byte default (no OACK); an optioned RRQ (e.g. `curl`'s `tsize`) takes the OACK path. Implemented by factoring `tftp.ServerLoop.StartTransfer(frame, sendOACK)` out of `OnRRQ` (the existing OACK path stays byte-identical — i83/i95 tests regress green) and a `PARSE_OPT_COUNT==0` branch in the Z80 `handle_rrq`. Host-verified over the i80 emulation byte-for-byte vs the Go authority (`netboot_serve_test.go`: ARP reply + bare-RRQ→DATA + optioned-RRQ→OACK + miss→ERROR(1)-keep-serving; `make ci-netboot-z80`). **Bootable disk shipped** (`make netboot-serve-disk` → B-DOS boot + AUTO LOADs+CALLs `serve_main` at &8000, which reads the EEPROM MAC/IP + provisions the baked-in `hello.txt`/`readme.txt` into STORE/SRC_TABLE) + the on-hardware guide (`netboot-trinity-testing.md` §"Serve-files demo"). The demo files are assembled into the binary (no B-DOS provisioning needed — that is what makes it plain-client-testable). **Verification line (CLAUDE.md §5):** the dispatch + the bare/optioned RRQ paths ARE host-verifiable; the real ENC28J60 silicon + a stock TFTP client interoperating + the on-hardware run stay Pete's real-Trinity test. Emulation-verified ≠ hardware-verified. | DONE — ✅ DONE — Go authority + Z80 loop + bootable disk + host tests landed; on-hardware run is Pete's. | `tools/netboot-oracle/serve/serve.go`; `src/netboot/netboot_serve.asm`; `tools/netboot-oracle/z80/netboot_serve_test.go`; `docs/notes/netboot-trinity-testing.md`; i80, i83, i94, i95 |
| **i97** | **Deterministic autonomous-loop monitor** — keep ONE interactive Claude Code session (in `screen`, with `/remote-control`) working autonomously across context limits, **losslessly** (never the lossy auto-compaction). A small deterministic watcher script + a *thin* protocol addendum the startup prompt points to; the proven `docs/ROADMAP.md`-driven orchestrator prompt/behaviour stays **unchanged** — no LLM supervisor, no headless `claude -p`. **Protocol (settled with Pete 2026-06-15):** the agent does one work item → writes a *task-done* semaphore → idles; the watcher injects the context readout (`/context`) **then a short resume nudge** (the nudge is load-bearing — `/context` is a *local* command that renders but does **not** invoke the model, so a real text prompt is required to wake the agent for its next turn; live-test finding, q14 failure mode 3); the agent then reads the readout and — if **<20% free** → **winds down** (writes the ROADMAP handover + a *wound-down* semaphore), else **picks the next item**; on *wound-down* the watcher stuffs **`/clear`** + the startup prompt → a genuine clean reset re-grounded from ROADMAP. Plus a **hang timeout** (the residual watchdog: no semaphore within T min while idle → nudge/escalate). **Why:** the sequencing / watchdog / clear-and-reprompt is deterministic plumbing — a script does it, not a model, which also keeps interactivity + `/remote-control` intact (a headless process would lose both). **Open before trusting overnight:** live-test that a *watcher-stuffed* `/context` reaches the agent the same way a hand-typed one does (proven hand-typed this session; same input path, expected identical). Per-task overshoot is bounded by the orchestrator delegating heavy work to subagents (its own context grows slowly) + the 20% reserve doubling as the overshoot cushion. **Screen targeting (Pete's setup, load-bearing):** his `screen` session has **multiple windows** (he opens shells with `ctrl-a c`); claude runs in the **first window** (window 0), launched via the `cl` alias. `screen -X stuff` defaults to the *active* window, so the watcher MUST target window 0 explicitly — `screen -S <session> -p 0 -X stuff "…"` — or keystrokes land in the wrong shell. | DONE — 🔨 **core BUILT** (this PR) — `tools/autonomous-loop/monitor.sh` + README; live test confirmed (q14). | `tools/autonomous-loop/`; `docs/ROADMAP.md` handover contract; `CLAUDE.md`; memory `feedback_autonomous_loop` + `feedback_implementation_autonomy` + `feedback_overnight_agent_orchestration`; q14 |
| **i98** | **Split the `iN`/`qN` registries by state** (Pete's idea, 2026-06-15) — restructure the item + question registries so an agent that wants only the *open* work needn't read the entire all-time history of every item ever created. The design-with-Pete session it was gated on happened (2026-06-15) and produced the agreed shape, executed as **i105** (open/closed split + controlled status vocabulary + atomic items). | DONE — superseded by i105 (this PR is i105); the "design WITH Pete in a dedicated session" gate it was waiting on is satisfied. | i105; `docs/notes/item-registry-open.md`; `docs/notes/item-registry-closed.md` |
| **i99** | **Stream the netboot HTTP fetch body to storage (multi-MB firmware)** — surfaced by the i70 brick-6 review (2026-06-15). The i70 HTTP fetch currently **buffers the whole response in RAM**: `tcp_conn.asm`'s `conn_accumulate` appends into a fixed **4096-byte `CONN_DATA`** (with **no bound check** — a >4 KB body overruns it), and `netboot_http.asm`'s `http_main` then copies the body into a **16 KB `STAGING`** buffer before one HSAVE. That is fine for the host tests + small files, but a real Pi firmware blob (`start4.elf` ≈ 3 MB; the `.deb` is several MB) **cannot** fit in the SAM's RAM, let alone a 4 KB/16 KB buffer. The real fetch must **stream**: write each block of body to Trinity storage **incrementally** as it arrives (append to a B-DOS record / span records via the `bdos_seam`/HSAVE-append surface) rather than accumulate-then-HSAVE, and `conn_accumulate` needs either a bound/flush hook or to be bypassed by the streaming path. **DESIGN PASS DONE (2026-06-15):** split into a host-verifiable RAM-side (autonomous-eligible NOW) + a hardware-gated B-DOS binding (a genuine Pete-decision = **q16**). **RAM-side (build now, behind an abstract sink):** convert `conn_accumulate` into a bounded `FLUSH_WINDOW` buffer that calls a `storage_sink_flush` on threshold + at the FIN, never exceeding the window in RAM (deletes the 4 KB/16 KB buffers + the overrun bug); **skip the HTTP header** before the first flush (the first window is "header+body-start"; `FLUSH_WINDOW` ≥ max header → no straddle case); **back-pressure = flush-before-ACK** (the receive loop is single-threaded `drv_read`→process→ACK, so flushing before `conn_build_and_send` withholds the ACK for free — no window arithmetic; an advertised-window shrink is a noted future-only refinement). Mirror the change in the Go authority (`tcp.Conn`/`Fetcher` route to a `Store` sink recording chunks) so the byte-for-byte oracle still holds; **host-verify** by streaming a multi-window body across ≥3 segments with a small `FLUSH_WINDOW` (≥2 flushes), a non-window-multiple length (partial-final), + the header-skip — asserting the sink's chunks are ordered/sized right + concatenate to the body (the streaming analogue of `tcp_conn_test`'s accumulated-body assert). **Hardware-gated (behind `NETBOOT_HOSTTEST`, q16-bound):** the real B-DOS record/sector write (`RST 8`), section-C paging, the end-to-end multi-MB fetch. **The B-DOS binding is q16** (the SAMDOS-2 audit says the append hooks `HOFLE`/`HSBYT` are broken for external callers, contradicting the fork analysis — Pete's call; agent leans the bounded-HSAVE-per-record convention). Not on the daily-loop critical path; blocks a *real* (vs toy-sized) on-hardware fetch. **FOUNDATION LANDED (2026-06-15):** the Go-authority streaming sink (`tcp.Sink`/`ChunkSink` + `Conn.SetSink`/`acceptPayload`/`flushFinal`, opt-in; nil sink = legacy) shipped in **PR #263**, and the **Z80 port** now mirrors it in `tcp_conn.asm` — an opt-in `CONN_SINK_ENABLED` flag (default 0 = byte-identical legacy `CONN_DATA` accumulate) that, when set, routes payload through a bounded `CONN_FLUSH_WINDOW`/`CONN_FLUSH_BUF`, flushing full windows to `storage_sink_flush` as it fills and the partial remainder at the FIN (`conn_flush_final`, before the FIN consumes its seq). `storage_sink_flush` is a recording test double (`CONN_SINK_OUT`/`CONN_SINK_CHUNKS`); host-verified by `tcp_conn_stream_test.go` (multi-window across straddling segments, ≥2 flushes, partial-final + FIN-flush, chunk boundaries cross-checked vs the Go `ChunkSink`), and the wire frames stay byte-for-byte the Go authority's (every existing oracle test green → the default path is unchanged). **q16 RESOLVED (Pete 2026-06-15): option (c)** — persist as a sequence of bounded files **spanning multiple Trinity records/disks** (~800 KB each; a multi-MB blob spans several), each a plain `HSAVE` (the *verified* hook), reassembled in order at serve time — **sidesteps the broken append hooks** (no `HOFLE`/`HSBYT`). **Correction (Pete):** the earlier "HSAVE caps at 64 KB" was WRONG — the UIFA size is pages-count×16384 + len-mod-16K (a **32-bit** size, ~4 MB/file max); the real limits are **RAM** (the source → stream) + **per-record capacity** (~800 KB → span records), not a 64 KB file ceiling (64 KB was a conflation with the 16-bit window / i24's assembler `OUT_LEN`). So the streaming window binds to a RAM-bounded chunk (the HSAVE source must be contiguous in RAM, so the per-flush chunk is bounded by free RAM — comfortably within a section-C window — NOT by the ~800 KB record/floppy capacity; "spanning records" = a sequence of bounded HSAVE'd files reassembled at serve time), each flushed as one bounded `HSAVE`. **Progress (2026-06-15):** the header-skip **`bodySink` Z80 port landed** (#279 — `src/netboot/body_sink.asm` vs `http.NewBodySink`); the **per-file `FW_MANIFEST` size landed** (the split count = size ÷ per-record-cap needs the size — now a manifest field, host-verified vs `http.RPiFirmware.Files[i].Size`). **Spanning convention — Go authority landed (2026-06-15):** `bdos.SpanPlan(name, size, cap) []SpanRecord{Name, Offset, Length}` (+ `SpanCount`, `SpanRecordName`) — the split + record-naming convention, the same plan for both the persist write (HSAVE each) and the serve read order (HLOAD each in order, concatenated). A non-spanned object (size ≤ cap) is a single record under its **plain** name (the kernel + small files keep their natural TFTP name through the existing flat store); a spanned object is `N = ceil(size/cap)` records named `<prefix7><NNN>`, the count derived from the **known size** (the manifest carries it) so **no on-disk index record is needed**. The per-record cap is **RAM-bounded** (the HSAVE source must be contiguous in paged RAM) and stays a **parameter** (pinned at hardware-build time — no speculative constant). Host-verified in `bdos/span_test.go` (boundaries, the real firmware sizes, + the reassembly invariants: records cover `[0,size)` contiguously, each ≤ cap, count == `SpanCount`, names == `SpanRecordName(0..N-1)`); spec §8 records the convention. **Z80 spanning primitives landed (2026-06-15):** `src/netboot/fw_span.asm` — `fw_span_chunk_len` (the per-record byte count `FW_SPAN_LEN = min(cap, remaining)`, 32-bit, the streaming-persist primitive; the record count falls out of the loop so no 32-bit divide) + `fw_span_record_name` (`<prefix7><NNN>`). Standalone leaf, bootable image byte-identical (`&FA52`); host-verified vs `bdos.SpanPlan`/`SpanRecordName` (`fw_span_test.go`: the streaming chunk-length sequence + the record names match byte-for-byte across non-spanned/spanned/exact-multiple/real-firmware sizes + indices to 999); `make netboot-fw-span`, in the `netboot-z80` CI job. With this, the **host-verifiable half** of the spanning convention is complete on both sides (Go authority + Z80 port). **Remaining (all hardware-gated):** the `http_main` migration onto the streaming sink (drives `fw_span_chunk_len` → HSAVE each record under `fw_span_record_name`), the serve-time reassembly read loop (HLOAD `<prefix>000..(N-1)` in order, `N` from the manifest size), and the real `RST 8` `HSAVE`/`HLOAD`-per-record — validated only on real Trinity (Pete's test). | DONE — ✅ host-verifiable half COMPLETE (#263 Go sink + Z80 port; q16 RESOLVED; bodySink #279; manifest size #281; spanning Go authority #282 + the Z80 `fw_span` port); remaining = the hardware-gated `http_main` migration / serve reassembly / real HSAVE-HLOAD (Pete's real-Trinity test) | `src/netboot/netboot_http.asm`; `src/netboot/tcp_conn.asm` (`conn_accumulate`/`CONN_WINDOW`); `src/netboot/fw_span.asm` (`fw_span_chunk_len`/`fw_span_record_name`); `src/netboot/bdos_seam.asm`; `tools/netboot-oracle/{tcp/conn.go,bdos/bdos.go,bdos/span.go}`; `docs/notes/sam-stub-audit.md`; `docs/specs/phase3-delivery-design.md` §8; i70, i93, i24, q15, q16 |
| **i105** | **Registry restructure — atomic items, status vocabulary, open/closed split** (Pete + agent, 2026-06-15). The item/question tables have grown large and their status cells drifted from a parseable token (early items: `✅ DONE (PR #N)`) into multi-part descriptive prose (e.g. i76's `P1a/P1b…`), so a startup agent must ingest the whole history and can't quickly answer "which items are open?"; worse, bundled multi-deliverable rows hide incomplete sub-parts (the SimCoupé PNG-endian fix sat unnoticed inside i81's prose). **Fix (agreed shape):** **(a)** split each registry into **`item-registry-open.md` + `item-registry-closed.md`** (and likewise for questions) — a fresh agent reads only the open file; resolving an item *moves* its row open→closed in the *same PR* that resolves it (mirrors "delete the plan in the completing PR"); ids stay globally unique across both files. **(b)** **Controlled status vocabulary** — every status cell starts with one token: `OPEN` / `DONE` / `BLOCKED:<what>` / `WONTFIX`, then any prose. **(c)** **Atomic items** — one item = one deliverable = one status; split genuine multi-deliverable rows into sub-ids (i81→i81a/b, i76→i76a/b…, extending the existing i12a/i48a-b/i74 convention), and *trim* (don't split) rows that are merely over-narrated for a single deliverable — the test: *must you query the parts' completion separately?* yes→split, no→trim. **(d)** Codify the convention in `CLAUDE.md` doc-lifecycle rules and update the ROADMAP handover pointers to the new filenames. **Execution discipline:** calibrate on **i81 + i76** as the worked examples, then **delegate the bulk rewrite to a subagent** (keeps the orchestrator lean — important under the 50% wind-down threshold), and the **pre-merge review MUST explicitly verify id-preservation**: every id present before is present after, none dropped, duplicated, or renumbered (a migration-specific gate beyond the standard §3 checklist). **Benefit beyond tidiness (correctness):** splitting bundled rows surfaces hidden long-tail sub-deliverables as their own `OPEN` rows, so "drained" finally means *actually* drained. | DONE — registry restructure (this PR): open/closed split + controlled status vocabulary + atomic items; see item-registry-open.md / item-registry-closed.md. | `docs/notes/item-registry-open.md`; `docs/notes/question-registry-open.md`; `CLAUDE.md` §doc-lifecycle; `docs/ROADMAP.md` handover protocol; i81, i76 (worked examples); i12a/i48a/i48b/i74 (existing sub-id precedent) |
| **i107a** | **Status-discipline process rule** (the go-forward half of i107, Pete 2026-06-16) — document that a status cell is the controlled-vocabulary token + a short phrase + an optional PR ref, **never** a multi-paragraph narrative of finished sub-results (`Opt-1…Opt-8`, `Brick 1…N`); and that multi-part work is **split into sub-items the moment it's decided multi-part, not at a later audit** (add the sub-items then, give the umbrella a terse status). i105 had the controlled-vocabulary + atomic-items rules but rows drifted into wall-of-text status anyway; this makes the timing + the anti-pattern explicit. | DONE — ✅ PR #347 — strengthened the Controlled-status-vocabulary + Atomic-items sections of `item-registry-open.md` and the matching `CLAUDE.md` doc-lifecycle line; the retroactive sweep of rows that already accreted is **i107b** | `docs/notes/item-registry-open.md` header; `CLAUDE.md` §doc-lifecycle; i105; i107b |
| **i102a** | i102 measurement infra — the cycle-exact T-state counter in the koron-go/z80 harness (`tools/netboot-oracle/z80/tstates.go`, anchor-verified + error-on-unknown completeness) wired into `CallResult.TStates`, plus the recorded crypto baselines (`tstates_baseline_test.go`). The benchmark every later i102 opt measures against. | DONE — ✅ PR #316 | `tools/netboot-oracle/z80/tstates.go`; `tstates_baseline_test.go`; i102 |
| **i102b** | i102 SHA-256 speedups — Opt-1: short-distance rotations (whole-byte `sha_rotr8`/`sha_rotl8` + a minimal bit residual + `sha_shr8`; deleted the bit-at-a-time `sha_rotr_word_by`); Opt-5: dropped the redundant push/pop in `sha_add4` (callers already reload HL/DE). Documented opcodes only; NIST vectors byte-exact. | DONE — ✅ PRs #318 (−27.4%) + #322 (−2.7%); SHA-256 ~29.4% below baseline (6.45M→4.55M T) | `src/netboot/sha256.asm`; i102 |
| **i102c** | i102 quarter-square 8×8 multiply — replaced the `mul8` shift-add loop with the quarter-square identity `a·b = QSQ[a+b] − QSQ[\|a−b\|]` (`QSQ[n]=⌊n²/4⌋`, a 1022-byte RAM table built by `qsq_init` via a pure-add recurrence). Opt-2: x25519; Opt-3: poly1305 (identical loop). Exhaustive `TestMul8Exhaustive`/`TestPoly1305Mul8Exhaustive` (all 65536 pairs) + RFC/e2e byte-exact. | DONE — ✅ PRs #319 (x25519 −23.0%) + #320 (poly1305 −9.5% / aead −11.5%); later DRY-extracted to `qsq.asm` | `src/netboot/qsq.asm`; `src/netboot/x25519.asm`; `src/netboot/poly1305.asm`; i102 |
| **i102d** | i102 chacha20 redundant push/pop — `cc_add`/`cc_xor` preserved HL/DE that every `cc_qr` caller reloads; dropping the push/pop also let `cc_addstate`'s manual word-advance go (faster AND smaller). RFC 8439 + aead byte-exact. | DONE — ✅ PR #321 (−7.5% chacha20_block; first i102 increment to shrink code) | `src/netboot/chacha20.asm`; i102 |
| **i102e** | i102 x25519 structural opts — Opt-6: dedicated symmetric squaring `fe_sqr` (496 off-diagonal products doubled + 32 diagonal terms, ~half a multiply); Opt-7: constant-aware `fe_mul121665` (96 vs 1024 mul8 for the 3-byte a24); Opt-8: hold the schoolbook inner-loop pointers in IX/IY across `mul8`. Field pins + RFC 7748 + Go-curve25519 e2e byte-exact. | DONE — ✅ PRs #343 (−18.7%) + #345 (−8.6%) + #346 (−5.64%, −45 B); x25519 cumulative 1.604G→1.125G = −29.9% | `src/netboot/x25519.asm`; i102 |
| **i102f** | i102 x25519 Karatsuba field multiply — `fe_schoolbook` (the 32×32→64-byte product behind every `fe_mul`) replaced its flat 1024-`mul8` schoolbook with one level of Karatsuba: split each operand at 16 bytes, three 16×16 sub-multiplies (`kmul16`) for z0/z2/middle (768 `mul8`), the middle coefficient via the subtractive identity `z1 = z0+z2 − (a_lo−a_hi)(b_lo−b_hi)` over absolute differences with a tracked sign bit, then a multi-byte combine (`kadd`/`ksub`). `fe_sqr` left untouched (its symmetric schoolbook already matches Karatsuba's `mul8` count). New `TestFEMulRandom` (1024 random asymmetric pairs) + the existing field pins + RFC 7748 + Go-curve25519 e2e all byte-exact. | DONE — ✅ PR #354 (x25519 −13.5%); x25519 cumulative 1.604G→0.973G = −39.3% | `src/netboot/x25519.asm`; `tools/netboot-oracle/z80/x25519_field_test.go`; i102 |
| **i100a** | i100 http_main host slice — Go-authority `http.Provisioner` (PR #307) + Z80 `http_main` Bricks 1–6: Brick 1 compose + plan (PR #308), Brick 2 dynamic `HTTP_PATH_PTR` (PR #309), Brick 3 bodySink header-skip seam (PR #310), Brick 4 `prov_start` per-file re-init (PR #311), Brick 5 per-file store double `store_begin`/`store_end` (PR #313), Brick 6 `prov_first`/`prov_onframe`/`prov_next` download loop (PR #314); every frame + verdict host-verified byte-for-byte vs Go `Provisioner`; bootable image byte-identical throughout (Brick 7 deferred to hardware, tracked in i100). | DONE — ✅ PRs #307–#314 + tracking PR #315 | `tools/netboot-oracle/http/provision.go`; `src/netboot/http_main.asm`; `src/netboot/fw_source.asm`; i100 (open umbrella) |
| **i107b** | Retroactive status-cell sweep — split the existing over-long rows into sub-items and collapse their status to the controlled vocabulary. Swept: **i102** (Opt-1…Opt-8) → i102a–e in closed (PR #348); **i70** (Brick 1…6) trimmed (PR #349; sequential stages, one outstanding part); **i100** → i100a in closed (PR #350); **i88** (TLS crypto + handshake bricks) → i88a in closed (PR #351). Each completing PR moves the done sub-part to the closed file and leaves the open umbrella terse. The final scan drained the 4 named walls; mild residuals (i73/i83, below the wall threshold) are tracked as the low-priority follow-up i108. | DONE — ✅ PRs #348, #349, #350, #351 | i107a (the rule, PR #347); i88a, i100a, i102a–e (closed sub-items); i108 (scan residuals) |
| **i108** | Light-trim the mildly-accreted i73 / i83 status cells (i107b scan residuals). | DONE — ✅ PR #352 — assessed both: **i73**'s status cell was already terse (its length lives in the *item* cell — a legitimate spec of the L1…L12 hardening residuals — not a status wall) → left as-is; **i83**'s status was a genuine moderate wall (design narrative + landed transfer loop) → trimmed ~2.9 KB → ~1.5 KB to a terse umbrella (Model A design-confirmed + landed loop PR #230 + the i86-DHCP / hardware-`RST 8` remainder), preserving every gating note. | i107b; i73; i83 |
| **i88a** | i88 **6a — host-verifiable TLS-1.3 client** (all crypto primitives + handshake bricks 1–5 + brick-6a record-level state machine). Building blocks: SHA-256 (PR #269), HMAC-SHA256 (PR #284), HKDF (PR #285), HKDF-Expand-Label (PR #286), ChaCha20 block+stream (PRs #287–#288), Poly1305 (PR #290), X25519 field+invert+ladder (PRs #291–#293), AEAD_CHACHA20_POLY1305 (PR #294), handshake spec (PR #295), brick 1 = key schedule (PR #296), brick 2 = record protection (PR #297), brick 5 = transcript hash (PR #298), brick 3 = ClientHello builder (PR #300), brick 4 = server-flight parser (PR #301), Go authority (PR #325), qsq extraction (PR #330), 6a composition + ClientHello sub-brick (PR #331), two-phase key schedule (PR #332), capture oracle (PR #333), q19 resolved (PR #334), 6a state machine COMPLETE (PR #335), wind-down (PR #337). All primitives and the composite `tls_client.asm` host-verified byte-for-byte vs the Go authority. Brick 6b (bootable/hardware) stays open in i88. | DONE — ✅ PR #335 (6a COMPLETE); full list PRs #269–#337 | `src/netboot/tls_client.asm`; `tools/netboot-oracle/tls/`; `docs/specs/tls13-handshake.md`; i88 (open umbrella for 6b) |
