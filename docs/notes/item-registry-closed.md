<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/items.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Item registry — closed

Work items with status `DONE` or `WONTFIX`. Open items live in
`item-registry-open.md`.

## Controlled status vocabulary (closed file)

| token | meaning |
|---|---|
| `DONE` | completed; delivering PRs are in the `PR` column |
| `WONTFIX` | decided not to implement; reason is in the description |

A `DONE` leaf may have zero or more PRs — operational work (e.g. config changes)
needs no PR; items delivered across several PRs may list them all. Umbrellas never
appear as `DONE` here directly — they are `DONE` only when all leaf children are
`DONE`/`WONTFIX`, and the generator emits them here once that condition holds.

Git history is the archive for per-PR detail, rationale, and iteration history.
This table holds the stable structured fact — what was decided and where to find it.

The `PR`, `deps`, `dependents`, and `refs/links` columns are each independent
first-class columns. The generated `.md` files are the source-of-truth views of
`registry/items.yaml` — do not edit them by hand (hand edits fail CI).

<!-- The table below is generated — do not edit it by hand. -->


| **id** | item | status | PR | deps | dependents | refs/links |
|---|---|---|---|---|---|---|
| **i1** | **Compact-.tbn format change (hybrid bytes/symbolic; KindLitInsts + KindLitData)** — Introduced KindLitInsts + KindLitData hybrid format reducing compact .tbn size from 88,644 to 51,117 B (-42.3%), 6 IN pages -> 4, byte-identical to GNU. Delivered across PRs #121, #122, #124. Next-gen redesign explored as i39. | DONE | [#124](https://github.com/petemoore/sam-aarch64/pull/124) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/specs/2026-05-27-compact-tbn-and-disassembler-design.md, docs/specs/tbn-binary-format-reference.md (§7) |
| **i7** | **Codegen sysreg/mnemonic/form tables from Go authority** — Phases A+B+C done: A deleted ENCTAB_LEN; B renamed enctab-gen->tables-gen, added -sysreg-inc emitter + CI job sysreg-sync, src/sysreg_tables.inc generated from sysregs.go; C1 added -constants-inc + src/tbn_constants.inc (OP_KIND_*/REC_KIND_*/DIR_* equates); C2 added -mnemonic-ids-inc + src/mnemonic_ids.inc (MNEM_<NAME> equates, all bare-numeric cp dispatchers now named). PRs #184 (spec) + #189/#234/#235/#236 (phases A-C). Three sync tests guard all .inc files. Phase D = i74 (gated on aarch64dec/sys.go code->data refactor) is the only remaining work. | DONE | [#184](https://github.com/petemoore/sam-aarch64/pull/184), [#189](https://github.com/petemoore/sam-aarch64/pull/189), [#234](https://github.com/petemoore/sam-aarch64/pull/234), [#235](https://github.com/petemoore/sam-aarch64/pull/235), [#236](https://github.com/petemoore/sam-aarch64/pull/236) |  |  | docs/specs/codegen-tables-design.md, i74 |
| **i8** | **Sysreg-table de-dup into shared src/sysreg_tables.inc** | DONE | [#108](https://github.com/petemoore/sam-aarch64/pull/108) |  |  | src/sysreg_tables.inc |
| **i9** | **Parity robustness seeds (sysname full-Go-parity + untested-form empirical sweep)** — PR #114 pinned two found gaps as skipped tests; those gaps were subsequently fixed under i36/i37. | DONE | [#114](https://github.com/petemoore/sam-aarch64/pull/114) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-z80-go-disasm-parity-i9.md |
| **i10** | **Go-vs-Z80 capability parity report** | DONE | [#109](https://github.com/petemoore/sam-aarch64/pull/109) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-go-vs-z80-disasm-capability-parity.md |
| **i11** | **Full ARMv8.0-A A64 ISA footprint research** | DONE | [#110](https://github.com/petemoore/sam-aarch64/pull/110) |  |  | docs/notes/a64-isa-footprint-research.md |
| **i12a** | **SimCoupé v1.2.16 bump (pin SHA to upstream, drop vendored -exitonhalt patch)** | DONE | [#112](https://github.com/petemoore/sam-aarch64/pull/112) |  |  | tools/Dockerfile.dev |
| **i12c** | **Rebase/upstream macOS+Linux paste support** — RESOLVED/MOOT — upstreamed (87f2a69); arrives free with i12a. | DONE | [#112](https://github.com/petemoore/sam-aarch64/pull/112) |  |  | ~/.claude/.../memory/project_simcoupe_sdl_paste_branch.md |
| **i13** | **gitignore in-tree Go build binaries** | DONE | [#107](https://github.com/petemoore/sam-aarch64/pull/107) |  |  | .gitignore |
| **i14** | **Non-canonical logical-immediate decoder tests (decodeBitMasks immr>=esize reject)** | DONE | [#113](https://github.com/petemoore/sam-aarch64/pull/113) |  |  | deferred-backlog row |
| **i15** | **adds 3-register form (Z80)** — adds Xd,Xn,Xm (and shifted-register form) already byte-exact via INSN_RUN overlay. The Go authority computes the base word; Z80 emits verbatim. Only absence is one optional self-test row in shifted_reg_table (src/slots/shifted_reg.asm) — not a product gap. Out of scope: extended-register form (adds ..., w2, uxtb) errors even in Go — a separate Go-side gap, unused in corpus. | WONTFIX |  |  |  | src/insn_run.asm, src/slots/shifted_reg.asm, tools/sam-aarch64/assemble/pass2.go, CLAUDE.md §6 |
| **i17** | **Deep reviews of main_loop.asm + litpool.asm + SYMTAB_* equ sentinels** — Review delivered (PR #185): 1 CRITICAL / 3 MEDIUM / 12 LOW. Fixes: C1 (litpool .ltorg flush segment counters), M1 (.equ/.set value range check), M2 (imm9/imm7 range check), M3 (litpool pc-map comment), L2 (reader_at_end carry contract), L8 (load_in_file DI). SYMTAB sentinels verified collision-free. LOW residuals -> i73. | DONE | [#185](https://github.com/petemoore/sam-aarch64/pull/185) |  |  | repo-audit §6 |
| **i18** | **Wider naming review (tests/m{N}, ci-m{N}, build-m3-disk, milestone fixtures)** | DONE | [#159](https://github.com/petemoore/sam-aarch64/pull/159) |  |  | scope row "Directory naming" |
| **i26** | **text2bin operand-kind validation (Task 21)** — text2bin tool deleted (replaced by integrated sam-aarch64 CLI per Makefile). Only surviving text2bin references are stale comments. If equivalent validation is wanted, it rescopes against sam-aarch64/frontend libs as a new item. | WONTFIX |  |  |  | tools/README.md, Makefile, tools/sam-aarch64/ |
| **i28** | **Absolute .set/.equ high-word edge: negative absolute emitted wrong high word (0x00000000 vs 0xFFFFFFFF)** — Z80 stored only low 32 bits and reconstructed high word as zero. Fix: eval_push_sym_sign_high sign-extends bit 31 (add a,a / sbc a,a idiom). Correct for whole accepted set: absolute-classification already refuses values whose high word isn't 0x00000000/0xFFFFFFFF. Positive absolutes still get high 0; release.bin byte-identical. Pinned by tests/symbols/sources/set_neg_highword.s (.quad of -256, -1, 0x10000000). | DONE | [#379](https://github.com/petemoore/sam-aarch64/pull/379) |  |  | src/expr_eval.asm (eval_push_sym_sign_high), src/main_loop.asm (main_dir_equ_mark_abs), tests/symbols/sources/set_neg_highword.s, CLAUDE.md §6 (Go authority) |
| **i34** | **Untrack accidentally-committed Go binaries** — enctab-gen untracked (PR #111); llist-normalise deleted with the LLIST cluster (i52 PR 4, q8 resolved 2026-06-10). | DONE | [#111](https://github.com/petemoore/sam-aarch64/pull/111) |  |  | .gitignore, i52 |
| **i35** | **sdiv missing across the whole stack (no mnemonic/Form -> .inst; unencodable)** | DONE | [#115](https://github.com/petemoore/sam-aarch64/pull/115) |  |  | deferred-backlog row; mirrored udiv ID 72 |
| **i36** | **Z80 disasm: ccmp/ccmn decode (was -> .inst) -- added decode + encoder (ccmn ID 100); binutils-aligned** | DONE | [#119](https://github.com/petemoore/sam-aarch64/pull/119) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-z80-go-disasm-parity-i9.md |
| **i37** | **base csinv/csneg (Rn!=Rm): Go aarch64dec decodes them (IDs 101/102), encoder coverage added; aliases intact** — i9 sweep skip removed; csinv/csneg families now certified. Shared PR #119 with i36. | DONE | [#119](https://github.com/petemoore/sam-aarch64/pull/119) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-z80-go-disasm-parity-i9.md |
| **i38** | **Audit for other skipped/excluded tests + PRs that left gaps rather than fixing them** — Audited 14 skip sites: 13 legitimate, only the i9 gap (now fixed by i36/i37); no other papered-over gaps found. | DONE | [#118](https://github.com/petemoore/sam-aarch64/pull/118) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-skipped-tests-and-gaps-audit.md |
| **i39** | **Next-gen maximally-efficient compact .tbn encoding (the design)** — Instruction overlay (assembled word + zeroed bits + sparse expression overlay, unifying literal/symbolic INST into one run), header label/offset table, one .tbn v2 with a contiguous evictable editor region, name front-coding, bitfield-packing polish. Shipped as milestone M8 (closed 2026-06-12). Design agreed as Format B (Pete 2026-06-08; all 5 open Qs resolved, §7). Shipped via i39a (PR #131) + i39b (PRs #151, #153). Residual i39c migrated to M9 (folds into i48c; reparented to i48c — not a child of i39). | DONE |  |  |  | docs/specs/compact-tbn-nextgen-design.md, i39a, i39b, i39c |
| **i39a** | **M8 Phase 1 — instruction overlay + header label/offset table; the v2 format flip** — v2 format (KindInsnRun) + overlay Fold/ZeroSlot (13 slots); refenc emit/decode + bin2text render + round-trip gate; Z80 v2 reader + INSN_RUN decoder (src/insn_run.asm) with the symbolic form-table path retired (net −370 B). The spectrum4 release byte-matches GNU on the SAM via the v2 compact .tbn (88,644→47,067 B; OUT 21,752 B) + all 53 M3–M6 fixtures. PR(d) done — header label/offset table; compact .tbn 47,067→45,189 B. i48b (FoldMovzAuto) co-shipped. Multi-PR delivery: PR(a)+(b)+i48b+(c)+(d) all part of PR #131. i48a split to a follow-up PR. | DONE | [#131](https://github.com/petemoore/sam-aarch64/pull/131) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-08-i39-phase1-instruction-overlay-plan.md |
| **i39b** | **M8 Phase 2 — name-table front-coding + editor-region split for compact .tbn v2** — Delivered in two PRs. #151 (was id i39b-1, front-coding): each name-table entry becomes [shared_prefix_len][suffix_len][suffix] shared with the previous name in encounter order (no sort, so name_ids stay stable); .tbn 45,189->44,207 B. #153 (was id i39b-2, editor-region split): splits .tbn into an assembler-facing region and a trailing editor region bounded by editor_region_offset u32 at offset 8; -thin-comments host flag; enables i40 eviction. Assembled binary byte-identical (GNU==Go==Z80/SAM). Design §3.5-3.7. | DONE | [#153](https://github.com/petemoore/sam-aarch64/pull/153) |  |  | docs/specs/compact-tbn-nextgen-design.md, https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-09-i39b-nametable-frontcoding-sidecars.md, https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md |
| **i40** | **Assembler-side editor-region eviction** — Load-path DONE (PR #181): SAM loads only the assembler-facing .tbn prefix (two-phase HGTHD+HLOAD — head read to discover editor_region_offset, then prefix HLOAD); the editor region never enters the IN buffer on any machine size. The conditional-residency/reload half is superseded by the comment-storage design (docs/specs/comment-storage-design.md): the editor holds comments as a compressed-resident block store with watermark math. Residual: page-aligned editor_region_offset normalisation verified against harness convention only — no page-aligned SimCoupé fixture yet (PR #181 review note). | DONE | [#181](https://github.com/petemoore/sam-aarch64/pull/181) |  |  | https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md, docs/specs/comment-storage-design.md, i51 |
| **i41a** | **Editor edit-model — core paged block-list + cleanly-separable serialize/load seam** — First i41 brick; host-side Go authority for the eventual Z80 port (i41d). Package tools/sam-aarch64/editmodel: half-page (8 KB) blocks, u24 never-reused record-ids, a record-id to stable-block-pointer location table maintained O(half block) per split/merge (no global reindex — the bounded-cost property), insert/delete/goto-by-id/render, and a separable EMDL serialize/load seam (swapped for real v2 .tbn in i41c). Proven by property test (5 seeds x 7000 random ops vs oracle) + 15 unit tests + a teeth test (corrupted loc caught). CI-gated. SAM byte-layout and paging latency are i41d. | DONE | [#383](https://github.com/petemoore/sam-aarch64/pull/383) |  |  | tools/sam-aarch64/editmodel/, docs/specs/editor-edit-model-design.md |
| **i41b** | **Editor edit-model — bounded ring-journal undo/redo** — Adds bounded ring-journal undo/redo to tools/sam-aarch64/editmodel (editEntry ring, maxUndoDepth=256, drop-oldest cap). Undo-of-insert deletes by IndexOf(id) (drift-robust); undo-of-delete re-inserts at the recorded index with the same RecordID (id-stability, anchors/comments stay valid); a fresh edit clears the redo stack. Proven by TestPropertyUndoRedo (5 seeds x 3000-step random walk vs reference state history) + 10 unit tests. CI-gated via go test + staticcheck. | DONE | [#384](https://github.com/petemoore/sam-aarch64/pull/384) |  |  | tools/sam-aarch64/editmodel/, docs/specs/editor-edit-model-design.md |
| **i41c** | **Editor edit-model — real v2 .tbn serialize seam** — Adds SerializeTBN/LoadTBN to tools/sam-aarch64/editmodel (tbn.go): save = join lines + frontend.Translate + assemble.Pass1 + CompactTBNBytes; load = render.EmitLinesFromBytes + fresh editmodel lines. The EMDL seam is retained as the internal exact-round-trip seam the i41a/i41b property tests depend on. Deliberate limitation: SerializeTBN requires a complete valid assembly — an invalid/partial document fails loud; incremental/partial serialize is i41e (q24). Proven by TestSerializeTBNRoundTrip + fail-loud + load round-trip tests. | DONE | [#385](https://github.com/petemoore/sam-aarch64/pull/385) |  |  | tools/sam-aarch64/editmodel/, docs/specs/editor-edit-model-design.md, q24 |
| **i48a** | **Host front-end unification — factor encoder/assembler/renderer as shared Go libs** — Host front-end unification into shared libs. PR1 (#141): 3 host tools became thin wrappers over tools/sam-aarch64 with shared libs frontend/assemble/render. PR2 (#142): integrated sam-aarch64 binary (source->{binary,compact .tbn}, .tbn->binary, --render, -E); symbolic .tbn in-memory IR; 3 old modules deleted. PR3 (#144, final): symbolic records are pure in-memory []format.Record IR; Decision-B strictness (symbolic mem->error; lsl#12 syntactic; mov->movz default); q7 sweep resolved; 36 dead goldens removed. Post-#144 cleanup (#146): dead-code sweep. All byte-neutral; 14 CI checks green. | DONE | [#144](https://github.com/petemoore/sam-aarch64/pull/144) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/plans/2026-06-09-i48a-host-frontend-unification.md |
| **i48b** | **Syntactic encoder + fold-time value-work** — Syntactic encoder + fold-time value-work. FoldMovzAuto (hw computed in the fold; ZeroSlot clears bits 22:21) landed in commit 0162f52 with i39a (PR #131). The non-byte-affecting strictness items (symbolic mem -> FoldMemImm12-or-error; add/sub lsl#12 syntactic via the fold + a split ZeroSlot; mov->movz default) + q7 sweep (no further GNU silent form-rewrites in corpus) landed in i48a PR3 (#144, final). Byte-neutral on the corpus (0 affected sites confirmed by q7 sweep). Two-PR delivery: first PR #131, final PR #144. | DONE | [#144](https://github.com/petemoore/sam-aarch64/pull/144) |  |  | docs/specs/i48-syntactic-encoder-design.md |
| **i48c-b1** | **Lexer core — tokenizer kind/span/base + literal types + local-label refs + string literals** — Completes the editor lexer (src/asmlex.asm). B1: tokenizer kind/span/base. B1b: int64 literal value (parseIntInBase) + char literals (readCharLit). B1c: local-label refs Nf/Nb (readNumberOrLocal). B1d: string literals (readString — cpp line-directives out of scope, preprocessor artifact). Delivered across PRs #393 (B1) + #394 (B1b) + #395 (B1c) + #397 (B1d). | DONE | [#397](https://github.com/petemoore/sam-aarch64/pull/397) |  |  | src/asmlex.asm, tools/netboot-oracle/z80/asmlex_test.go |
| **i48c-b2** | **Simple-inst parse → INST record (mnemonic table + reg/reg/reg + reg/reg/#imm)** — B2a: generated src/mnemonic_names.inc from Go authority + Z80 mnemonic_lookup (runtime-data twin of mnemonic_ids.inc). B2b: generic reg/reg/reg parse (matchReg + operand loop + emitInst → INST record). B2c: reg/reg/#imm (single-literal immediate: WriteImmExpr + folded WriteImm), host-verified vs format.ExprWriter/OperandWriter. Delivered across PRs #398 (B2a) + #399 (B2b) + #400 (B2c). | DONE | [#400](https://github.com/petemoore/sam-aarch64/pull/400) |  |  | src/asmparse.asm, tools/netboot-oracle/z80/asmparse_test.go, tools/sam-aarch64/frontend/parser.go |
| **i48c-b3** | **Expression parser — precedence→expr bytecode (full arithmetic grammar + symbol-intern + PC/reloc primaries)** — B3a: scaffold (parseExprPrec/Primary/tokPrec) + constant-fold over + - & \| ^ / unary - ~ / parens. B3a2: shifts << >>. B3a3: * (64-bit shift-add multiply). B3a4: / (signed 64-bit restoring long division — truncate-toward-zero, MinInt64/-1→MinInt64, div0→0). B3b: symbol-intern + WriteSym (document symbol-table seam; per-document reset). B3c: PC . / local-ref Nf/Nb / :reloc: primaries. All host-verified vs format.ExprWriter+EvalConst. PRs #402+#403+#404+#406+#407+#412. | DONE | [#412](https://github.com/petemoore/sam-aarch64/pull/412) |  |  | src/asmparse.asm, tools/netboot-oracle/z80/asmparse_test.go, tools/sam-aarch64/frontend/parser.go |
| **i48c-b4** | **Operand suffixes — memory operands (7 [...] shapes), register shift/extend suffix, condition-code operand** — B4: parse_operand_mem — all 7 [...] shapes; match_extend — ExtendKind table scan; MEM_SHAPE_*/EXT_* constants in tbn_constants.inc via tables-gen; host-verified vs Go parseMem/matchExtend. B4b: match_shift_kind + lookahead in parse_operand_reg; SHIFT_* constants; mutation-verified vs WriteShiftedReg/WriteExtendedReg. B4c: match_cond + COND_NAMES table (18 records: 16 canonical + 2 GNU aliases); mutation-verified. Delivered PRs #414 (B4) + #415 (B4b) + #417 (B4c). | DONE | [#417](https://github.com/petemoore/sam-aarch64/pull/417) |  |  | src/asmparse.asm, tools/netboot-oracle/z80/asmparse_test.go, tools/sam-aarch64/frontend/parser.go |
| **i48c-b5a** | **movk/movz/movn special-form (#imm16 [, lsl #N], hw=N/16 folded into bits[17:16], range-checked)** — parse_movk dispatched on movk/movz/movn mnemonic ids before generic loop. #imm16 [, lsl #N] with hw=N/16 folded into bits[17:16]; constant imm range-checked to [0,65535] + folded to (hw<<16)\|imm; symbolic imm emits (hw<<16)\|(immExpr & 0xffff). Helpers: emit_imm_expr_operand/expr_buf_single_imm/expr_append_raw. Standalone build now includes mnemonic_ids.inc. 11 hand + 10 error + 5 fuzz seeds; host-verified vs format.ExprWriter/EvalConst/OperandWriter. | DONE | [#435](https://github.com/petemoore/sam-aarch64/pull/435) |  |  | src/asmparse.asm, tools/netboot-oracle/z80/asmparse_test.go |
| **i48d** | **Doc unification — bring tbn-binary-format-reference.md to v2 overlay-only format** — First brought v2-accurate (both profiles) in commit dc2b993 (part of i39a PR #131). Then fully rewritten overlay-only in i48a PR3 (#144, final) once the symbolic form became in-memory-only: tbn-binary-format-reference.md now documents a single on-disk format (compact overlay v2 — header + name table + label/local header tables + INSN_RUN/LIT_DATA/DIRECTIVE/COMMENT + 13 FoldSlots), with symbolic kinds noted as in-memory IR only. Also: historical banners on the M1 + compact-tbn design docs. Two-PR delivery: first PR #131, final PR #144. | DONE | [#144](https://github.com/petemoore/sam-aarch64/pull/144) |  |  | docs/specs/tbn-binary-format-reference.md, docs/specs/i48-syntactic-encoder-design.md |
| **i49** | **tools/ cleanup — delete orphan tool dirs and prune stale .gitignore entries** — Delete two orphan tool dirs (flatten-s, build-screen-disk; off the build/CI/module path), prune 6 stale .gitignore binary-guard entries (4 for tools deleted in i48a/M0, 2 for the dirs removed here), drop their README rows, fix post-i48a doc-rot (harness README make text2bin -> make sam-aarch64; ci.yml m6-release comment). A follow-up commit also refreshed the last stale .gitignore text2bin comment. | DONE | [#149](https://github.com/petemoore/sam-aarch64/pull/149) |  |  |  |
| **i51** | **Un-strip all comments on the SAM-side m6 gate** — Flow the full 335 KB of release.s comment text (7,502 // lines) through the SAM round-trip. DONE (PR #181): full unstripped compact .tbn (363,295 B; assembler-facing prefix 38,584 B / 3 IN pages) flows through the release gate; SAM-assembled OUT byte-matches GNU 21,752 B. Depends on i40 load-path. | DONE | [#181](https://github.com/petemoore/sam-aarch64/pull/181) |  |  | https://github.com/petemoore/sam-aarch64/blob/d874a69/docs/notes/m8-status.md, i40 |
| **i52** | **Repo cleanup: docs restructure + tracking consolidation + tooling hygiene + functional renames** — Delivered across PRs #155-#159 (2026-06-10). Spec in git history at 2f4c898/docs/specs/2026-06-10-repo-cleanup-design.md. Includes: docs restructure (ARCHITECTURE.md, specs/, plans/), tracking consolidation (iN/qN registries), tooling hygiene, and functional renames (tests/format\|core\|symbols\|operands\|paged\|release, build-disk, CI jobs). i55/i56 were deferred sub-scope items. | DONE | [#159](https://github.com/petemoore/sam-aarch64/pull/159) |  |  | https://github.com/petemoore/sam-aarch64/blob/2f4c898/docs/specs/2026-06-10-repo-cleanup-design.md |
| **i53** | **Bump-arena reclamation for the fixed section-D scratch arrays** — YAGNI per the risk census: every section-D table is SAFE under ~5x kernel growth and the real ceilings (IN/OUT paged buffers) are page-allocation bumps, not arena problems. Revisit only if the census §4 trigger fires (fixed section-D array overflows at >5x kernel scale). | WONTFIX |  |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/specs/2026-05-29-bump-arena-risk-census.md |
| **i54** | **Synthetic-fixture oracle sweep for the on-SAM disassembler** — Synthetic-fixture oracle sweep for the on-SAM disassembler: 102-word corpus covering every family the Go decoder handles but release.img never exercises (cond-compare/select, extended-reg add/sub, signed multiplies, load/store option matrix — §B of the i10 capability-parity report). Converts §B from structurally-ported to empirically-certified. 11 new TestSyntheticParity_* functions. One genuine Z80 gap found and fixed: EXTR/ROR-immediate decoder missing from src/disasm.asm (ported from tryDecodeExtr, aliases.go:778). All 17 TestSyntheticParity_* + TestDisasmOracle 5438/5438 pass. | DONE | [#183](https://github.com/petemoore/sam-aarch64/pull/183) |  |  | https://github.com/petemoore/sam-aarch64/blob/c0f62fa/docs/notes/2026-06-08-go-vs-z80-disasm-capability-parity.md |
| **i57** | **Comment-compression experiment (host-side, Go): benchmark Z80-feasible schemes** — Benchmarked static word dictionary, digram/BPE, canonical Huffman, dictionary+Huffman hybrid, block-based LZ (ZX0/ZX7/LZSA-class) at 1/2/4/8 KB blocks, and flate as ceiling, against the real unstripped release editor region (335,461 B comment text, 7,502 comments). Result: best Z80-feasible = greedy LZ77/ZX0-style 85,526 B (ratio 0.287, 6 pages); no scheme fits 256 KB SAM; 512 KB SAM comfortable (15 pages free). Feeds i59 decision. Tool: tools/comment-bench/. | DONE | [#161](https://github.com/petemoore/sam-aarch64/pull/161) |  |  | i59, docs/notes/comment-compression-research.md |
| **i58** | **Trinity interface capabilities research: storage, SPI access, Z80 driver feasibility** — Researched Quazar Trinity SD storage (port &DF/SPI VERIFIED; raw-sector 800 KB-record model; no public Z80 SD driver — source in SAM Revival 21 print; ethernet driver ready for Phase-3). Findings in docs/notes/trinity-capabilities.md. Cited sources required per item scope. | DONE | [#162](https://github.com/petemoore/sam-aarch64/pull/162) |  |  | i59, docs/notes/trinity-capabilities.md |
| **i59** | **Full-fidelity source residency on SAM — decide comment-storage strategy** — Decision (Pete, 2026-06-10): compressed-resident on 512 KB; stripped-save (i65) covers 256 KB assembly; Trinity persistence deferred (i66). i40 urgency drops (eviction is no longer the 256 KB story). Reducing comments at source rejected (dev experience is the product goal). Consumes i57 + i58 results; informs i40 scope and i51 full-comment gate. Resolved as part of q9 closure (PR #168). | DONE | [#168](https://github.com/petemoore/sam-aarch64/pull/168) |  |  | i57, i58, i60, i65, i66, docs/notes/comment-compression-research.md |
| **i60** | **On-SAM ZX0-format comment compression — measurement, compressor, and design** — Decision (Pete): greedy ZX0-bitstream compressor ON the SAM for self-hostedness. (a) harness T-states measured (PR #163); (b) Go-first greedy compressor (tools/zx0-greedy, H=512 D=16: ratio 0.3631 at 4 KB, 9246 B scratch) + Z80 port src/zx0_compress.asm (24/24 blocks byte-identical, post-i67 972 T/byte); (c) design approved PR #173: docs/specs/comment-storage-design.md — 4 KB blocks, turbo decoder, page-13 co-residency + page-14 staging, watermark R=2. Implementation: i68. | DONE | [#173](https://github.com/petemoore/sam-aarch64/pull/173) |  |  | i59, docs/notes/comment-compression-research.md, docs/specs/comment-storage-design.md |
| **i61** | **Corpus sweep for Trinity/B-DOS/SAM-Revival artifacts (800 disk images)** — Found (2026-06-10): B-DOS 1.5t beta 6 binary + period Trinity utilities (SD select bytes recovered); B-DOS 1.5a complete Z80 source; 1.6c/1.7D/1.7J/1.7N binaries; a uIP TCP/IP stack for the SAM (Phase-3 relevant). NOT found: any SAM Revival disk, the Quazar manual. Detailed catalogue in private session records (publication policy). trinity-capabilities.md updated. | DONE | [#166](https://github.com/petemoore/sam-aarch64/pull/166) |  |  | docs/notes/trinity-capabilities.md |
| **i62** | **B-DOS storage-backend investigation: prove SAMDOS hook-level portability end-to-end** — Boot B-DOS AL 1.5a + emulated Atom Lite under SimCoupé; exercised SAMDOS-hook file-io (DVAR-7 detect → HRECORD 1 → HSAVE/HGTHD/HLOAD round-trip) against a B-DOS record. Hook portability SAMDOS ↔ B-DOS AL: LIKELY → VERIFIED; Trinity/1.5t leg stays LIKELY (no emulation). Rig tools/i62-bdos-experiment/ is re-runnable. Findings: docs/notes/bdos-version-landscape.md §Empirical verification (i62). | DONE | [#179](https://github.com/petemoore/sam-aarch64/pull/179) |  |  | docs/notes/bdos-version-landscape.md, tools/i62-bdos-experiment/README.md |
| **i63** | **Superpowers extraction: keep/adapt/drop skills, encode as repo-native rules** — Studied obra/superpowers (MIT); keeps encoded as native CLAUDE.md rules (PR #167, Pete-approved verbatim); plugin disabled for this project 2026-06-10. The bare-run test is every subsequent item. Rationale on the i63 PR. | DONE | [#167](https://github.com/petemoore/sam-aarch64/pull/167) |  |  | ~/git/superpowers |
| **i64** | **Blog post: the 2026-06-10 session log (repo cleanup → compression → superpowers)** — First post published 2026-06-10 (petemoore.github.io: 'When the agent helps choose its own process'); drafted by agent with consent, privacy pass per publication policy before publishing. The blogging tradition continues per private memory. Closed in registry by PR #169 (session-close) alongside i63. | DONE | [#169](https://github.com/petemoore/sam-aarch64/pull/169) |  |  |  |
| **i67** | **Night-shift T-state optimization of the Z80 ZX0 compression path** — Baseline then optimize src/zx0_compress.asm under a hard byte-identity invariant. Result (2026-06-11): compress 804.1M → 289.7M T (−64.0%) at 4 KB blocking; 134.0 s → 48.3 s at 6 MHz. 2698 → 972 T/byte; oracle byte-identical on all 110 whole-corpus blocks. Decode side: vendored + measured dzx0_fast (48.8 T/B) and dzx0_mega (46.6 T/B) vs standard 63.6 / turbo 50.9. Undocumented-opcode support matrix documented. PRs #170 + #171. | DONE | [#171](https://github.com/petemoore/sam-aarch64/pull/171) |  |  | i60, docs/notes/comment-compression-research.md |
| **i68** | **ZX0 integration: promote decoder to src/, wire into boot path, boot self-tests** — i60c design implementation: (1) dzx0_turbo.asm promoted to src/ (org &8B00); (2) page-13 payload zx0.bin/zx0-test.bin, -zx0 build-disk flag, load_zx0_payload in both variants; (3) baked-bytes boot self-tests (1 KB comment fixture, tags &E1-&E3) — green in SimCoupé matrix, kosarev/z80 IXH/IXL proof; (4) release-unstripped-tbn re-pointed at vendored tests/release/release.s; (5) ARCHITECTURE §5 rewritten. Budget: test 141→86 B headroom, prod +38 B. | DONE | [#176](https://github.com/petemoore/sam-aarch64/pull/176) |  |  | i60, docs/notes/comment-compression-research.md |
| **i71** | **B-DOS reference + Trinity-fork deep-dive: vendor, disassemble, diff, re-point rig** — (a) Vendored reference/bdos/: Bdos15a.zip + bdos15a.src.txt + al-bdos15a.bin + provenance README (Edwin Blink FREEWARE; 1.5t NOT vendored — WoS 'Copyrights Declined'). (b) B-DOS 1.5t beta 6 disassembled + diffed vs 1.5a: hook surface VERIFIED-STATIC (all 39 vectors map 1:1; only sector-device layer swapped). Findings: docs/notes/bdos-trinity-fork-analysis.md. (c) i62 rig re-pointed at reference/bdos/al-bdos15a.bin. Runtime confirmation on real Trinity hardware remains outstanding (no emulator covers the ports). | DONE | [#186](https://github.com/petemoore/sam-aarch64/pull/186) |  |  | docs/notes/bdos-trinity-fork-analysis.md, docs/notes/bdos-version-landscape.md, docs/notes/trinity-capabilities.md, reference/bdos/README.md |
| **i72** | **SAMDOS→B-DOS reorientation across living docs and build tooling** — B-DOS becomes the documented target DOS: samdos-file-io.md reframed (B-DOS = target, SAMDOS 2 = shipped boot DOS pending q10 swap); ARCHITECTURE.md + ROADMAP table reframed; src/sam_io.inc + src/trampoline.asm comments DOS-neutral (byte-identical built binaries); tools/build-disk gains -dos/-dos-name/-dos-load (default SAMDOS 2, byte-identical before). SAMDOS source citation trail restored (https://github.com/stefandrissen/samdos). Default/CI swap decision was q10 → executed in i75. | DONE | [#187](https://github.com/petemoore/sam-aarch64/pull/187) |  |  | i62, docs/specs/samdos-file-io.md |
| **i75** | **B-DOS boot-disk swap execution: prove SimCoupé matrix green, flip shipped default** — tools/build-disk default DOS flipped from SAMDOS 2 to B-DOS AL 1.5a (reference/bdos/al-bdos15a.bin, name bdos, start address 32777); samdos2 byte-exact size guard re-keyed off reference path so SAMDOS 2 compatibility build stays fully functional. Local proof (plain floppy, no Atom Lite, SimCoupé v1.2.16): every gate passed. CI matrix boots B-DOS = formal proof. Resolved q10. | DONE | [#191](https://github.com/petemoore/sam-aarch64/pull/191) |  |  | docs/notes/bdos-trinity-fork-analysis.md, docs/notes/m9-status.md |
| **i76** | **Host-side Go TUI editor prototype at SAM-faithful geometry — the functional UX authority for the Phase-2 editor** — Umbrella for the i76 TUI editor prototype phases: samscreen abstraction (tcell + PNG/SCREEN$ backends), i4-parity viewer, font-proof geometry evidence, config-lab, and rendering-rules spec. See docs/specs/editor-tui-prototype-design.md §8 for decisions. Children: i76a/b/c/d (all DONE). | DONE |  |  |  | docs/specs/editor-tui-prototype-design.md, docs/specs/editor-rendering-rules-design.md, tools/editor-prototype/, tools/font-proof/, docs/notes/m9-status.md, i4, i5, i6, i41, i48 |
| **i76a** | **P1a: samscreen abstraction, tcell backend, PNG/SCREEN$ mockup, i4-parity viewer, -geometry matrix** — P1a of the i76 TUI editor prototype: samscreen abstraction + tcell terminal backend + PNG/SCREEN$ mockup backend + i4-parity read-only viewer + -geometry mode×font matrix (tools/editor-prototype). | DONE | [#200](https://github.com/petemoore/sam-aarch64/pull/200) |  |  | tools/editor-prototype/, docs/specs/editor-tui-prototype-design.md, i4, i5, i6 |
| **i76b** | **P1b font-proof: five-pixel-font vendored; make font-proof screenshots 85×32 (6×6) vs 64×24 (ROM 8×8)** — P1b of the i76 TUI editor prototype: five-pixel-font vendored; make font-proof produces real-frame screenshots 85×32 (6×6) vs 64×24 (ROM 8×8) — the i6 geometry evidence. | DONE | [#201](https://github.com/petemoore/sam-aarch64/pull/201) |  |  | tools/font-proof/, i6 |
| **i76c** | **P2 config-lab: tools/editor-prototype -config rendering lab over the real release .tbn at locked MODE 3 64×24** — P2 of the i76 prototype: interactive rendering lab (-config flag) over the real release .tbn at locked MODE 3 64×24. Delivered across PRs #202 (initial lab), #203 (v1.1 instruction truncation), #204 (v1.2 inline-if-fits + cursor-block demarcation); PR #204 is the completing increment. | DONE | [#204](https://github.com/petemoore/sam-aarch64/pull/204) |  |  | tools/editor-prototype/configs/, q13 |
| **i76d** | **Rendering-rules spec landed; default-config recommendation tracked as q13** — docs/specs/editor-rendering-rules-design.md §3–§10 rendering-rules spec landed; default-config recommendation (tuned compressed.config) tracked as open question q13. | DONE | [#217](https://github.com/petemoore/sam-aarch64/pull/217) |  |  | docs/specs/editor-rendering-rules-design.md, q13 |
| **i78** | **Source-structure preservation in the v2 editor region (blank lines, comment grouping, indentation)** — (a) IMPLEMENTED (PR #218): kind u8 sidecar discriminator (0=comment, 1=blank-run) + FlagTaggedSidecar header bit; KindBlankRun IR record; corpus round-trip assertion — all 109 fixtures green incl. release.s (1340 blank lines). Finding → i90: BSS section blanks not preserved through -flatten (pre-existing property). (b) Multi-line grouping DECIDED: one sidecar entry per source comment with embedded body newlines. (c) Indentation DEFERRED: gated downstream of i76 interface sign-off. | DONE | [#218](https://github.com/petemoore/sam-aarch64/pull/218) |  |  | docs/specs/source-structure-preservation-design.md, docs/specs/comment-storage-design.md, i41, i48c, i39b, i85, i90 |
| **i79** | **samscreen.DefaultCLUT[5] palette fix: 18 (purple-blue) → 80 (true cyan)** — Found by PR #200 (P1a re-land) scoped review. Verified against SAM ROM power-on CLUT at 0x7d51 (two independent ROM copies): index 5 = 80 (true cyan), not 18; every other DefaultCLUT entry already matched the ROM. One-line fix in tools/editor-prototype/samscreen/palette.go; editor-prototype tests green. Affects MODE 4 mockup PNGs only; MODE 3 viewer (CLUT 0–3) unaffected. | DONE | [#206](https://github.com/petemoore/sam-aarch64/pull/206) |  |  | tools/editor-prototype/samscreen/palette.go |
| **i80** | **Trinity network-interface emulation — ENC28J60 Ethernet SPI emulation for host-side Z80 netboot iteration** — Emulate Quazar Trinity ENC28J60 Ethernet (SPI via port &DE) so the SAM network stack can be iterated host-side. Go-first: enc28j60.go emulates Trinity microcontroller + ENC28J60 SPI (4 banks, 8KB RX/TX buffer, MAC/MII read-dummy). Real Z80 driver (src/netboot/encdrv.asm, trinload verbatim) runs against it; CI via netboot-z80 job. Landed: init/write/read/DHCP loopback verified (PR landed). Remaining: wire-I/O state machines (DHCP/TFTP transfer-loop) + end-to-end Pi boot on real Trinity (hardware-gated, CLAUDE.md §5). | DONE | [#228](https://github.com/petemoore/sam-aarch64/pull/228) |  |  | docs/notes/trinity-capabilities.md, docs/specs/phase3-delivery-design.md, src/netboot/encdrv.asm, tools/netboot-oracle/z80/enc28j60.go, i58, i82, i83, i86, i92 |
| **i81b** | **Comment on upstream SimCoupé issue #28 offering our CI pass/fail idiom** — Offer our DI;HALT + -exitonhalt + printer-channel pass/fail detection idiom on simonowen/simcoupe#28. Note: our idiom routes results to the capturable printer port (src/print.asm), not a BC-return (SimCoupé cannot read BC externally). Pete posted the comment (simonowen/simcoupe#28 comment-4724317311). Tracking PR #370. | DONE | [#370](https://github.com/petemoore/sam-aarch64/pull/370) |  |  | i81, tools/run-simcoupe.sh, src/print.asm |
| **i82** | **SAM TFTP client (RFC-conformant): RRQ/OACK/DATA/ACK client with ARP + RFC 1350/2347/2348/2349/7440 support** — trinload-evolved RFC-conformant TFTP client (RRQ pull). Closed by PR #244 (client boot disk netboot_client.asm), delivered across PR #210 (RFC research + trinload analysis), #225 (build_rrq/parse_oack via i92), #231 (transfer loop + SAS fix), #232 (origination front: ARP-for-server + RRQ send), #244. All host-verified over i80 emulation; HSAVE write-out is hardware-gated (Pete's Trinity test). RFC 2347 OACK compliance + the section-C fit gate were added post-close under i118 (PR #422). | DONE | [#244](https://github.com/petemoore/sam-aarch64/pull/244) |  |  | docs/notes/tftp-protocol-research.md, docs/specs/phase3-delivery-design.md, src/netboot/netboot_client.asm, src/netboot/tftp_client_loop.asm, src/netboot/tftp_client_front.asm, tools/netboot-oracle/client/client.go, simonowen/trinload, i80, i83, i84, i86, i93, i118 |
| **i83** | **SAM TFTP server (Phase-3 PXE shipper) — serve spectrum4 kernel and Pi firmware over TFTP** — Phase-3 headline goal: SAM+Trinity netboots a Pi by serving boot files by name from one flat Trinity store. Design confirmed (Pete 2026-06-14): Model A (DHCP+TFTP server, model-general, any aarch64 Pi). Transfer loop landed + host-verified end-to-end (PR #230 — tftp_server_loop.asm vs Go ServerLoop, over i80 emulation; name→record seam is i93 bdos_seam). Pairs with i70 (firmware self-provisioning). Remaining: DHCP server (i86, mandatory for Pi-3+multi-client) + real B-DOS RST 8 HGTHD/HLOAD hook dispatch on Trinity (hardware-gated, Pete's test). | DONE | [#230](https://github.com/petemoore/sam-aarch64/pull/230) |  |  | docs/plans/phase3-netboot-implementation-plan.md, docs/specs/phase3-delivery-design.md, docs/notes/pi-netboot-capture-analysis.md, src/netboot/tftp_server_loop.asm, tools/netboot-oracle/tftp/serverloop.go, i70, i80, i82, i84, i86, i89, i93 |
| **i84** | **Phase-3 end-to-end delivery design — integrating spec for the self-hosting boot/delivery loop** — Design confirmed (Pete 2026-06-14): docs/specs/phase3-delivery-design.md. Model A only: SAM = DHCP+TFTP server serving by name (all aarch64 TFTP-boot Pis). Bootstrap via Pete's patched SAM ROM (i87). q11 (Pi netboot mechanics) and q12 (delivery model) both RESOLVED as part of this design. New items spawned: i86 (DHCP), i87 (ROM dump). | DONE | [#211](https://github.com/petemoore/sam-aarch64/pull/211) |  |  | docs/specs/phase3-delivery-design.md, docs/notes/tftp-protocol-research.md, i70, i80, i82, i83, i86, i87 |
| **i85** | **Whole-file ZX0 vs compact overlay — size measurement (one-shot)** — Measured: zx0(full interleaved release.s) = 201,587 B vs current hybrid 170,132 B (38,584 B overlay + 131,548 B zx0(comments)) → whole-file is +18.5% LARGER. Conclusion: do not rewrite for size; overlay earns its keep on assembly speed. Whitespace preservation tracked as i78. | DONE | [#208](https://github.com/petemoore/sam-aarch64/pull/208) |  |  | docs/notes/comment-compression-research.md, i78, i57, i60 |
| **i86** | **Minimal SAM DHCP/bootp responder — netboot server's DHCP half** — Answer Pi boot ROM DHCP DISCOVER/REQUEST with OFFER/ACK: address from small pool, subnet, next-server (siaddr=SAM), PXEClient echo (opt 60), fixed 32-byte option-43 'Raspberry Pi Boot' blob, client UUID echo (opt 97); no opt-67. Confirmed from real capture (pi-netboot-capture-analysis.md). Responder loop LANDED + host-verified end-to-end: dhcp_loop.asm DORA (DISCOVER→OFFER, REQUEST→ACK, stable per-MAC lease) byte-for-byte vs Go authority dhcp/responder.go, over i80 emulated Trinity; wired to ci-netboot-z80. Remaining: end-to-end Pi boot on real Trinity (hardware-gated, CLAUDE.md §5). | DONE | [#229](https://github.com/petemoore/sam-aarch64/pull/229) |  |  | docs/plans/phase3-netboot-implementation-plan.md, docs/specs/phase3-delivery-design.md, src/netboot/dhcp_loop.asm, tools/netboot-oracle/dhcp/responder.go, i80, i83, i84 |
| **i88a** | **i88 6a — host-verifiable TLS-1.3 client: all crypto primitives + handshake bricks 1–5 + brick-6a state machine** — Full TLS-1.3 client host-verifiable slice: SHA-256, HMAC-SHA256, HKDF, HKDF-Expand-Label, ChaCha20, Poly1305, X25519, AEAD_CHACHA20_POLY1305, handshake bricks 1–5 (key-schedule, record-protection, transcript-hash, ClientHello, server-flight parser), Go authority, qsq extraction, 6a state machine. Delivered across PRs #269–#337; PR #335 completes 6a. All host-verified byte-for-byte vs Go authority. Brick 6b (bootable/hardware) stays in i88. | DONE | [#335](https://github.com/petemoore/sam-aarch64/pull/335) |  |  | src/netboot/tls_client.asm, tools/netboot-oracle/tls/, docs/specs/tls13-handshake.md, i88 |
| **i88b** | **i88 6b — TLS-record reassembler: host-verifiable wire framing layer** — tls_reasm.asm (tls_reasm_feed) frames complete TLS records out of arbitrary TCP byte chunks (incremental consume; REASM_BUF sized to TLS-1.3 max 5+2^14+256). Go authority tools/netboot-oracle/tls/reassembler.go + tls_reasm_test.go: mis-aligned chunk sequences byte-exact. Remaining 6b (hardware-gated): wire into tcp_conn CONN_SINK_FILTER + paging layout + bootable disk + real RST 8 on Trinity. | DONE | [#360](https://github.com/petemoore/sam-aarch64/pull/360) |  |  | src/netboot/tls_reasm.asm, tools/netboot-oracle/tls/reassembler.go, tools/netboot-oracle/z80/tls_reasm_test.go, i88 |
| **i91** | **Phase-3 netboot host oracle harness (tools/netboot-oracle/)** — Pure-Go DHCP + TFTP packet builders/parsers: frame (BuildUDPFrame), dhcp (OFFER/ACK + option-43 blob), tftp (RRQ/OACK/DATA/ACK/ERROR + serve-by-name + transfer-loop state machines), pcap (libpcap/pcapng reader), golden (masked committed vectors via cmd/gen-golden). Wired into CI as netboot-oracle + STATICCHECK_MODULES. Validates protocol logic only — Z80 execution, ENC28J60 I/O, and end-to-end Pi boot are not host-verifiable. | DONE | [#219](https://github.com/petemoore/sam-aarch64/pull/219) |  |  | tools/netboot-oracle/, docs/plans/phase3-netboot-implementation-plan.md §6/§8.1, docs/notes/pi-netboot-capture-analysis.md, i82, i83, i86, i80 |
| **i92** | **Phase-3 netboot Z80 port of protocol logic (src/netboot/*.asm)** — SAM-side Z80 implementation of DHCP/TFTP packet builders/parsers, ported from i91 Go authority (tools/netboot-oracle/) and host-verified byte-for-byte via koron-go/z80 harness (ci-netboot-z80 job). Landed (all host-verified): build_udp_frame (#221), build_dhcp_reply (#222), build_oack/build_data/build_error, parse_request/resolve, build_rrq/parse_oack/find_option, build_arp_request. All packet build/parse logic ported. Remaining: wire-I/O state machines (ENC28J60 send/receive, DHCP/TFTP/transfer loops) — NOT host-verifiable; gated on i80/real-Trinity. | DONE | [#221](https://github.com/petemoore/sam-aarch64/pull/221), [#222](https://github.com/petemoore/sam-aarch64/pull/222), [#223](https://github.com/petemoore/sam-aarch64/pull/223), [#224](https://github.com/petemoore/sam-aarch64/pull/224), [#225](https://github.com/petemoore/sam-aarch64/pull/225), [#226](https://github.com/petemoore/sam-aarch64/pull/226) |  |  | src/netboot/, tools/netboot-oracle/z80/, docs/plans/phase3-netboot-implementation-plan.md, docs/notes/pi-netboot-capture-analysis.md, i82, i83, i86, i91 |
| **i93a** | **B-DOS seam arithmetic: bdos_name_to_uifa / bdos_difa_to_size / bdos_fill_save_uifa — host-verified** | DONE | [#233](https://github.com/petemoore/sam-aarch64/pull/233) |  |  |  |
| **i94** | **Phase-3 netboot bring-up smoke test (ARP responder)** — Tiny bootable program: drv_init on real Trinity then answers ARP requests for the SAM's IP. New build_arp_reply.asm primitive (port of frame.go::BuildARPReply) + smoke/smoke.go Go authority. Reads SAM MAC+IP from Trinity EEPROM via vendored eeprom.asm. Bootable disk via make netboot-smoke-disk. smoke_serve_once host-verified end-to-end over i80 emulation (5 cases incl. wrong-IP/non-ARP controls). EEPROM config read + real ENC28J60 timing + actual ARP round trip are hardware-gated. Delivery PRs #237 + #238; completing PR #238. | DONE | [#238](https://github.com/petemoore/sam-aarch64/pull/238) |  |  | src/netboot/smoke_test.asm, src/netboot/build_arp_reply.asm, tools/netboot-oracle/smoke/smoke.go, docs/notes/netboot-trinity-testing.md, i80, i91, i92, i93 |
| **i95a** | **Integrated server dispatch loop (ARP+DHCP+TFTP) — Go authority + Z80 host-verified, bootable disk shipped** | DONE | [#239](https://github.com/petemoore/sam-aarch64/pull/239), [#241](https://github.com/petemoore/sam-aarch64/pull/241) |  |  |  |
| **i96** | **Phase-3 serve-files TFTP demo (ARP + TFTP, no DHCP)** — Focused bootable demo server: ARP + TFTP only, testable from any stock tftp/curl client. Go authority serve/serve.go::Responder.OnFrame + Z80 serve_serve_once in netboot_serve.asm. Key addition over i95: bare RRQ (no options) answered per RFC 2347 with DATA block 1 directly; optioned RRQ takes OACK path. Demo files baked in (no B-DOS provisioning). Bootable disk via make netboot-serve-disk. Dispatch + bare/optioned RRQ paths host-verified; real ENC28J60 + stock TFTP client interop + on-hardware run are Pete's real-Trinity test. | DONE | [#243](https://github.com/petemoore/sam-aarch64/pull/243) |  |  | tools/netboot-oracle/serve/serve.go, src/netboot/netboot_serve.asm, docs/notes/netboot-trinity-testing.md, i80, i83, i94, i95 |
| **i97** | **Deterministic autonomous-loop monitor (screen-driven, non-LLM)** — tools/autonomous-loop/monitor.sh: polls task-done semaphore, injects /context then a resume nudge (nudge is load-bearing — /context alone is local-only and does not invoke the model), agent picks next item or winds down at <20% free context. /clear+reprompt on wound-down for a clean reset. Hang timeout as residual watchdog. Screen window-0 targeting is load-bearing (screen -S <session> -p 0 -X stuff). PRs #251 (core), #252-#255 (robustness); marked DONE in PR #306 alongside q14 live-test confirmation. | DONE | [#306](https://github.com/petemoore/sam-aarch64/pull/306) |  |  | tools/autonomous-loop/, docs/ROADMAP.md |
| **i98** | **Split the iN/qN registries by state (precursor idea, superseded by i105)** — Pete's idea (2026-06-15): restructure item + question registries so a fresh agent needn't read all-time history to see open work. Gated on a design-with-Pete session; that session happened and produced the agreed shape, executed as i105 (open/closed split + controlled status vocabulary + atomic items). PR #306 moved this row to DONE as part of the i105 registry restructure. | DONE | [#306](https://github.com/petemoore/sam-aarch64/pull/306) |  |  | i105, docs/notes/item-registry-open.md, docs/notes/item-registry-closed.md |
| **i99** | **Stream netboot HTTP fetch body to storage (multi-MB firmware)** — Converts conn_accumulate into a bounded FLUSH_WINDOW buffer calling storage_sink_flush on threshold + FIN, never exceeding the window in RAM. Deletes the 4KB/16KB buffers + the overrun bug. q16 resolved (Pete): option (c) — multi-record spanning via bounded HSAVEs, sidesteps broken append hooks. Delivery sequence: PR #263 Go sink + Z80 port; #279 bodySink; #281 manifest sizes; #282 spanning Go authority; #283 fw_span Z80. Remaining (all hardware-gated): http_main migration onto streaming sink, serve-time reassembly, real RST 8 HSAVE/HLOAD — Pete's real-Trinity test. | DONE | [#283](https://github.com/petemoore/sam-aarch64/pull/283) |  |  | src/netboot/netboot_http.asm, src/netboot/tcp_conn.asm, src/netboot/fw_span.asm, src/netboot/bdos_seam.asm, tools/netboot-oracle/tcp/conn.go, tools/netboot-oracle/bdos/span.go, docs/notes/sam-stub-audit.md, docs/specs/phase3-delivery-design.md, i70, i93, i24 |
| **i100a** | **i100 http_main host slice — Go authority http.Provisioner + Z80 Bricks 1-6** — Go authority http.Provisioner (PR #307) + Z80 http_main Bricks 1-6: Brick 1 compose + plan (#308), Brick 2 dynamic HTTP_PATH_PTR (#309), Brick 3 bodySink header-skip seam (#310), Brick 4 prov_start per-file re-init (#311), Brick 5 per-file store double store_begin/store_end (#313), Brick 6 prov_first/prov_onframe/prov_next download loop (#314). Every frame + verdict host-verified byte-for-byte vs Go Provisioner; bootable image byte-identical throughout. Bricks 1-6 PRs #307-#314; completing tracking PR #315. | DONE | [#315](https://github.com/petemoore/sam-aarch64/pull/315) |  |  | tools/netboot-oracle/http/provision.go, src/netboot/http_main.asm, src/netboot/fw_source.asm |
| **i100b** | **i100 Brick 7 — bootable migration + real B-DOS HSAVE-per-record store leaf** — Un-guarded streaming sink + SHA-256 verify into the real bootable (NETBOOT_STREAM), added real storage_sink_leaf / store_begin / store_end in http_main.asm — each flushed FW_RECORD_CAP-byte window hashed then HSAVE'd to Trinity as record fw_span_record_name. Deleted legacy single-file bootable http_main + its 16KB STAGING. Bootable ends &ECED (27885 B, 4883 B free). Record cap = provisional 4096. Also fixed boot entry: jp http_main at &8000. HSAVE store leaf + EEPROM read ship unverified until Pete's Trinity test (CLAUDE.md §5). | DONE | [#373](https://github.com/petemoore/sam-aarch64/pull/373) |  |  | src/netboot/http_main.asm, src/netboot/tcp_conn.asm, src/netboot/netboot_http.asm, src/netboot/fw_span.asm, tools/netboot-oracle/bdos/span.go, i100, i100a, i110 |
| **i102a** | **i102 measurement infra — cycle-exact T-state counter in koron-go/z80 harness** — Cycle-exact T-state counter in the koron-go/z80 harness (tools/netboot-oracle/z80/tstates.go, anchor-verified + error-on-unknown completeness) wired into CallResult.TStates, plus the recorded crypto baselines (tstates_baseline_test.go). The benchmark every later i102 opt measures against. | DONE | [#316](https://github.com/petemoore/sam-aarch64/pull/316) |  |  | tools/netboot-oracle/z80/tstates.go, tstates_baseline_test.go, i102 |
| **i102b** | **i102 SHA-256 speedups — short-distance rotations and redundant push/pop removal** — Opt-1: short-distance rotations (whole-byte sha_rotr8/sha_rotl8 + a minimal bit residual + sha_shr8; deleted the bit-at-a-time sha_rotr_word_by); Opt-5: dropped the redundant push/pop in sha_add4 (callers already reload HL/DE). Documented opcodes only; NIST vectors byte-exact. Delivered in PRs #318 (−27.4%) + #322 (−2.7%); SHA-256 ~29.4% below baseline (6.45M→4.55M T). | DONE | [#322](https://github.com/petemoore/sam-aarch64/pull/322) |  |  | src/netboot/sha256.asm, i102 |
| **i102c** | **i102 quarter-square 8x8 multiply — replaced shift-add loop with QSQ identity** — Replaced the mul8 shift-add loop with the quarter-square identity a*b = QSQ[a+b] - QSQ[\|a-b\|] (QSQ[n]=floor(n^2/4), a 1022-byte RAM table built by qsq_init via a pure-add recurrence). Opt-2: x25519; Opt-3: poly1305 (identical loop). Exhaustive TestMul8Exhaustive/TestPoly1305Mul8Exhaustive (all 65536 pairs) + RFC/e2e byte-exact. Delivered in PRs #319 (x25519 -23.0%) + #320 (poly1305 -9.5% / aead -11.5%); later DRY-extracted to qsq.asm. | DONE | [#320](https://github.com/petemoore/sam-aarch64/pull/320) |  |  | src/netboot/qsq.asm, src/netboot/x25519.asm, src/netboot/poly1305.asm, i102 |
| **i102d** | **i102 chacha20 redundant push/pop removal — cc_add/cc_xor callers already reload HL/DE** — cc_add/cc_xor preserved HL/DE that every cc_qr caller reloads; dropping the push/pop also let cc_addstate's manual word-advance go (faster AND smaller). RFC 8439 + aead byte-exact. First i102 increment to shrink code. | DONE | [#321](https://github.com/petemoore/sam-aarch64/pull/321) |  |  | src/netboot/chacha20.asm, i102 |
| **i102e** | **i102 x25519 structural opts — fe_sqr, fe_mul121665, IX/IY pointer hold** — Opt-6: dedicated symmetric squaring fe_sqr (496 off-diagonal products doubled + 32 diagonal terms, ~half a multiply); Opt-7: constant-aware fe_mul121665 (96 vs 1024 mul8 for the 3-byte a24); Opt-8: hold the schoolbook inner-loop pointers in IX/IY across mul8. Field pins + RFC 7748 + Go-curve25519 e2e byte-exact. Delivered in PRs #343 (-18.7%) + #345 (-8.6%) + #346 (-5.64%, -45 B); x25519 cumulative 1.604G->1.125G = -29.9%. | DONE | [#346](https://github.com/petemoore/sam-aarch64/pull/346) |  |  | src/netboot/x25519.asm, i102 |
| **i102f** | **i102 x25519 Karatsuba field multiply — one level of Karatsuba over 32x32->64 schoolbook** — fe_schoolbook replaced its flat 1024-mul8 schoolbook with one level of Karatsuba: split at 16 bytes, three 16x16 sub-multiplies (kmul16) for z0/z2/middle (768 mul8), middle via the subtractive identity z1=z0+z2-(a_lo-a_hi)(b_lo-b_hi) with tracked sign bit, then multi-byte combine (kadd/ksub). fe_sqr left untouched. New TestFEMulRandom (1024 random pairs) + field pins + RFC 7748 + Go-curve25519 e2e all byte-exact. x25519 cumulative 1.604G->0.973G = -39.3%. | DONE | [#354](https://github.com/petemoore/sam-aarch64/pull/354) |  |  | src/netboot/x25519.asm, tools/netboot-oracle/z80/x25519_field_test.go, i102 |
| **i102g** | **i102 mul8 micro-opts — drop internal push de/pop hl and dead ld d,0 before calls** — (1) drop mul8's internal push de/pop hl: saved operands B/C are dead once the difference is taken, so the second QSQ[\|a-b\|] lookup reuses BC for the table base+entry while QSQ[a+b] stays in DE; (2) remove the dead ld d,0 before every mul8 call across the x25519 + poly1305 byte-radix loops (mul8 reads only A and E). TestMul8Exhaustive + TestPoly1305Mul8Exhaustive (all 65536 pairs each) + RFC 7748 / RFC 8439 / Go e2e byte-exact. x25519 cumulative 1.604G->0.934G = -41.8%. | DONE | [#355](https://github.com/petemoore/sam-aarch64/pull/355) |  |  | src/netboot/qsq.asm, src/netboot/x25519.asm, src/netboot/poly1305.asm, i102 |
| **i102h** | **i102 chacha20 register-based rotates — cc_rotl16/cc_rotl8 use registers, not cc_tmp4** — cc_rotl16/cc_rotl8 (the quarter-round's byte-permute rotates, each invoked ~80x per block: ROTL16 once + ROTL8 thrice via ROTL8/ROTL12/ROTL7) read the 4 word bytes into registers A/B/C/D and write them back permuted in place, instead of bouncing them through the cc_tmp4 memory temp via cc_load_tmp (now deleted — faster and smaller). RFC 8439 KAT + block/encrypt + AEAD + TLS-client composite byte-exact. chacha20_block -15.5% (345.5K->292.0K); chacha20_encrypt -15.3%; also speeds aead. | DONE | [#356](https://github.com/petemoore/sam-aarch64/pull/356) |  |  | src/netboot/chacha20.asm, i102 |
| **i102i** | **i102 x25519 schoolbook carry in a register — ride carry in C through push bc/pop bc** — The multiply/square inner loops (kmul16, the Karatsuba 16x16; fe_sqr off-diagonal — together ~88% of x25519's mul8 calls) kept the running byte carry in memory (fe_car). The loops already push bc/pop bc around mul8 to preserve the djnz counter B; riding the carry in C through that same push/pop drops both memory accesses for free, and dest[i+j]+carry is folded into one add a,(iy+0). math/big field pins + TestFEMulRandom + TestFESqr* + RFC 7748 + Go-curve25519 e2e byte-exact. x25519 -6.6% (934.1M->872.4M). | DONE | [#357](https://github.com/petemoore/sam-aarch64/pull/357) |  |  | src/netboot/x25519.asm, i102 |
| **i102j** | **i102 mul8 page-aligned table index — high-byte-only address formation for qsq_table** — qsq_table is page-aligned (low byte 0) and lookup index 2*(a+b) / 2*\|a-b\| is <= 1020, so each entry address is formed by adding only the high byte (ld a,qsq_table>>8; add a,h; ld h,a) instead of a full ld de,qsq_table; add hl,de — the index's low byte is already the address low byte. Saves ~12 T per mul8 call (~1.77M calls per x25519). Invariant documented at the qsq_table equ and guarded by exhaustive TestMul8Exhaustive/TestPoly1305Mul8Exhaustive (all 65536 pairs) — any non-page-aligned base fails CI. x25519 -2.7% (872.4M->848.7M); also speeds poly1305. | DONE | [#358](https://github.com/petemoore/sam-aarch64/pull/358) |  |  | src/netboot/qsq.asm, i102 |
| **i102k** | **i102 reduction/constant-multiply carry in a register — fe_mul38_high and fe_121665_inner** — Applied the i102i carry-in-C pattern to the two remaining byte-radix multiply loops: fe_mul38_high (the x38 high-half fold, run on every fe_mul and fe_sqr reduction) and fe_121665_inner (the xa24 curve-constant multiply). Both kept carry in fe_car memory; now rides the existing push bc/pop bc in C, addend folded into one add. Completes carry-register conversion across all byte-radix multiply loops; fe_car scratch byte fully dead and removed. Field pins + TestFEMul121665 + RFC 7748 + Go-curve25519 e2e byte-exact. x25519 -0.5% (848.7M->844.5M). | DONE | [#359](https://github.com/petemoore/sam-aarch64/pull/359) |  |  | src/netboot/x25519.asm, i102 |
| **i102l** | **i102 mul8 inlined at the two hottest x25519 sites — kmul16_inner and fe_sqr_inner** — The mul8 body (8x8->16 multiply, ~1.8M calls per x25519) was a called subroutine paying call+ret (27 T). Refactored into a pyz80 mul8_body MACRO (mul8 = mul8_body+ret) and expanded inline at kmul16_inner and fe_sqr_inner (~96% of calls). Branch over neg uses jr nc,$+4 so the macro carries no internal label. fe_sqr_diag + poly1305 + host field tests keep calling the subroutine. TestMul8Exhaustive (all 65536 pairs) + RFC 7748 + Go-curve25519 e2e + TLS-client composite headroom assertion byte-exact. x25519 -5.77% (844.5M->795.8M). | DONE | [#363](https://github.com/petemoore/sam-aarch64/pull/363) |  |  | src/netboot/qsq.asm, src/netboot/x25519.asm, i102 |
| **i102n** | **i102 sha256 register-oriented rewrite — increment 1 (729,795->418,843 T/block, -42.6%)** — Replaced sha256_compress (memory-to-memory) with a register-resident design: inline add4/copy4 macros; sigma/Sigma rotates held in B/C/D/E (8T rr r, not 15T rr (hl)); simplified Ch/Maj; chained rotations; in-place W schedule. ABI byte-preserved. Fastest verified Z80 SHA-256 (timewave0/ZARC ~480-485k; oisee's 45k is fabricated AI code). q21 resolution: Pete 2026-06-16 decided to do the full rewrite + keep cryptographic SHA-256 (firmware from third-party CDN, SHA-256 pin is the integrity guarantee). NIST KAT + million-a + start.elf (2.98 MB) byte-exact. | DONE | [#365](https://github.com/petemoore/sam-aarch64/pull/365) |  |  | src/netboot/sha256.asm, i102m, i100, i101 |
| **i102o** | **i102 sha256 8x circular-renaming unroll — increment 2 (418,843->377,371 T/block, -9.9%)** — Unrolls 64 rounds into an 8-phase group looped 8x: with circular renaming a..h never physically move (28-byte lddr shuffle gone — each phase hard-codes which wv_ slot is a..h). Generated flat (ordinary mnemonics, no pyz80 EQU FOR — sidesteps the EQU FOR*macro rewind bug) by tools/sha256-unroll-gen/gen_sha256_unrolled.py; drift-guarded byte-for-byte by regen_guard_test.go. Gated behind if defined(NETBOOT_TLS_CLIENT) — the TLS composite keeps the smaller rolled loop. Cumulative sha256 729,795->377,371 (-48.3%). NIST KAT + million-a + start.elf byte-exact. | DONE | [#367](https://github.com/petemoore/sam-aarch64/pull/367) |  |  | src/netboot/sha256.asm, tools/sha256-unroll-gen/, i102m, i102n, i100, i101 |
| **i102p** | **i102 sha256 inline Ch/Maj — increment 3 (377,371->346,267 T/block, -8.2%)** — In the unrolled round path the operand slots for Ch(e,f,g) and Maj(a,b,c) are compile-time constants, so 8 generated phases now inline both instead of calling shared sha_ch/sha_maj subroutines. Inline: each operand loads with ld a,(nnnn) and the boolean is done in A with H,L,D as scratch (Ch 76 T/byte vs ~135; Maj 92 T/byte vs ~141). Emitted by gen_sha256_unrolled.py (ch_inline/maj_inline), drift-guarded by regen_guard_test.go. Shared subroutines untouched — the rolled TLS_CLIENT composite is byte-identical. Cumulative 729,795->346,267 (-52.6%). NIST KAT + million-a (15,625 blocks) byte-exact | DONE | [#369](https://github.com/petemoore/sam-aarch64/pull/369) |  |  | src/netboot/sha256.asm, tools/sha256-unroll-gen/, i102m, i102n, i102o, i100, i101 |
| **i102q** | **i102 sha256 increment 4 — alternating sha_tmpa forward/backward walk (-1.0%)** — sha_bigsigma0/1/sha_sigma0/1 interleave st_bcde_tmpa + 1-2 xor_bcde_tmpa passes over the 4-byte sha_tmpa, each reloading ld hl,sha_tmpa (10 T). Since the intervening rotate macros preserve HL and XOR is per-byte order-independent, each pass after the first can chain from where the previous left HL: new xor_bcde_tmpa_bwd / xor_bcde_tmpa_fwd macros drop the reload. Saves 2 reloads each in Sigma functions (x64 rounds) + 1 each in sigma functions (x48 extend words) = -3520 T/block: 346,267->342,747 (-1.0%). NIST KAT byte-exact; 8x-unroll generator + regen guard untouched. | DONE | [#375](https://github.com/petemoore/sam-aarch64/pull/375) |  |  | src/netboot/sha256.asm, tools/netboot-oracle/z80/tstates_baseline_test.go, i102m, i102p |
| **i102r** | **i102 sha256 increment 5 — register-accumulator round (342,747->315,355 T/block, -8.0%)** — T1/T2 accumulated in register quad B,C,D,E instead of memory-to-memory: each term folds in with BCDE += memory carry chain (~92 T, no write-back), so four intermediate T1 stores + both T2 stores are gone; only final new e/new a words touch memory. Ch rewritten to use A,H,L only; Maj parks in new sha_majs while S0 holds sha_tmpa. Emitted by gen_sha256_unrolled.py (rewritten phase_real), drift-guarded by regen_guard_test.go. Cumulative 729,795->315,355 (-56.8%). Also shrinks firmware-fetch bootable (http_main) by 478 B. Composite untouched. | DONE | [#377](https://github.com/petemoore/sam-aarch64/pull/377) |  |  | src/netboot/sha256.asm, tools/sha256-unroll-gen/, tools/netboot-oracle/z80/tstates_baseline_test.go, i102m, i102q |
| **i105** | **Registry restructure — atomic items, status vocabulary, open/closed split** — Splits item + question registries into open/closed files so a fresh agent reads only the open work. Controlled status vocabulary (OPEN/DONE/BLOCKED:<what>/WONTFIX token-first). Atomic items: one row = one deliverable = one status; multi-deliverable rows split into sub-ids (i81→i81a/b, i76→i76a/b…). Resolving an item moves its row open→closed in the same PR. Codified in CLAUDE.md doc-lifecycle + ROADMAP handover pointers. Worked examples: i81 + i76. | DONE | [#306](https://github.com/petemoore/sam-aarch64/pull/306) |  |  | docs/notes/item-registry-open.md, docs/notes/question-registry-open.md, CLAUDE.md, docs/ROADMAP.md |
| **i107a** | **Status-discipline process rule (go-forward codification)** — Documents that a status cell is the controlled-vocabulary token + a short phrase + an optional PR ref, never a multi-paragraph narrative. Multi-part work is split into sub-items the moment it is decided to be multi-part, not at a later audit. i105 had the rules but rows drifted into wall-of-text; this makes the timing + the anti-pattern explicit. Retroactive sweep is i107b. | DONE | [#347](https://github.com/petemoore/sam-aarch64/pull/347) |  |  | docs/notes/item-registry-open.md, CLAUDE.md, i105, i107b |
| **i107b** | **Retroactive status-cell sweep — split over-long rows into sub-items** — Swept: i102 (Opt-1…Opt-8) → i102a-e in closed (PR #348); i70 (Brick 1-6) trimmed (PR #349; sequential stages, one outstanding part); i100 → i100a in closed (PR #350); i88 (TLS crypto + handshake bricks) → i88a in closed (PR #351). Each completing PR moves the done sub-part to the closed file and leaves the open umbrella terse. Final scan drained the 4 named walls; mild residuals (i73/i83) tracked as i108. Delivery PRs #348-#351; completing PR #351. | DONE | [#351](https://github.com/petemoore/sam-aarch64/pull/351) |  |  | i107a, i108 |
| **i108** | **Light-trim mildly-accreted i73/i83 status cells (i107b scan residuals)** — Assessed both: i73 status was already terse (length lives in the item cell, not a status wall) — left as-is. i83 had a genuine moderate wall (design narrative + landed transfer loop) — trimmed ~2.9 KB to ~1.5 KB to a terse umbrella (Model A design-confirmed + landed loop PR #230 + i86-DHCP / hardware-RST 8 remainder), preserving every gating note. | DONE | [#352](https://github.com/petemoore/sam-aarch64/pull/352) |  |  | i107b, i73, i83 |
| **i110** | **Fix netboot bootable boot-entry: jp *_main at &8000 in all four programs** — Each composed netboot program placed org &8000 at the first-included subroutine, not the *_main entry — so CALL 32768 landed on the wrong routine (smoke_test &8000=smoke_serve_once, netboot_server &8000=netboot_serve_once, netboot_serve &8000=serve_serve_once, netboot_client &8000=client_first). Fixed all four: jp <name>_main guarded if defined(NETBOOT_HOSTTEST)==0 placed right after org &8000. Host-test builds are byte-unchanged. HTTP bootable was fixed earlier in i100b. | DONE | [#374](https://github.com/petemoore/sam-aarch64/pull/374) |  |  | src/netboot/smoke_test.asm, src/netboot/netboot_server.asm, src/netboot/netboot_serve.asm, src/netboot/netboot_client.asm, i100b |
| **i113a** | **capture, OCR scrape, QA + discovery pipeline** — 51 photographs captured. OCR pipeline (ocr_docs.py, Tesseract + rotation auto-detect) run; text/ layer produced at 88–93% confidence. QA agent ran two-prong error-hunt (prose scan + data-point inventory). Discovery agent diffed against known facts and surfaced new findings. All three pipeline artifacts live in the off-repo ~/sam-archive/trinity-docs/ workspace (copyrighted; preservation-only). No repo PR — work is off-repo by policy. | DONE |  |  |  | ~/sam-archive/trinity-docs/README.md, ~/sam-archive/trinity-docs/QA_REPORT.md, ~/sam-archive/trinity-docs/DISCOVERY_REPORT.md |
| **i115** | **Registry as structured source — YAML-backed iN/qN registry with CLI validator + CI sync-check** — Registry as structured source — generate the iN/qN markdown views from validated YAML (registry/*.yaml) gated by a CLI validator + a registry-sync CI job, so a malformed edit is rejected at the iteration that introduces it (decay-proof, git-native tracking at scale). Model revised 2026-06-19: enum OPEN/IN_PROGRESS/DONE/WONTFIX (no BLOCKED — derived from depends_on edges), transient questions, dependency DAG core. | DONE |  |  |  | docs/specs/registry-source-of-truth-design.md, i114, i117 |
| **i115a** | **Registry structured-source Phase 1 — tool skeleton** — Go tools/registry with validate + gen-to-buffer + round-trip test over a hand-written fixture. Dormant — no live source, no CI gate, cannot affect the live registry. Skeleton shipped (#437) on old schema; reworked to dependency-DAG model (no BLOCKED, depends_on, transient questions, invariants 11-13, 3 views). | DONE | [#437](https://github.com/petemoore/sam-aarch64/pull/437), [#446](https://github.com/petemoore/sam-aarch64/pull/446), [#447](https://github.com/petemoore/sam-aarch64/pull/447), [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | tools/tables-gen, i115 |
| **i115b** | **Registry structured-source Phase 2 — generator + banner + header templates** — Emit the three registry .md views deterministically, pipe-escaped, with the GENERATED banner. The earlier parity-with-today's-.md proof is dropped — a no-information-loss round-trip replaces it. Generator built (PR #443, held) on the OLD schema + dropped parity test; reworked to the revised model (3 views, new status rendering, no BLOCKED). Delivered in #446/#451/#455. | DONE | [#446](https://github.com/petemoore/sam-aarch64/pull/446), [#451](https://github.com/petemoore/sam-aarch64/pull/451), [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | i115 |
| **i115c** | **Registry structured-source Phase 3 — CLI mutators** — add / split / set-status / set-pr / dep / answer / next-id + the .id-ledger.txt no-reuse guard; the headless replacement for hand-editing giant table cells. CLI mutators landed in #447. | DONE | [#447](https://github.com/petemoore/sam-aarch64/pull/447), [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | i115 |
| **i115d** | **Registry structured-source Phase 4 — migration + cutover** — Single atomic switchover: bounded-batch markdown→YAML migration (delete-as-you-go from .old registries, per-batch independent info-loss review), reshape wall-of-text rows into umbrella+leaves, generate the .md in place, wire the required registry-sync CI job. This work — the i115 family itself is the current batch. | DONE | [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | i115 |
| **i115e** | **Registry structured-source Phase 5 — doc/automation rewiring** — Point CLAUDE.md (§3 + doc-lifecycle), ROADMAP (handover contract), the SessionStart hook, and the autonomous-loop docs at the YAML source; add the agent-guidance block. The completing PR deletes the plan. | DONE | [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | i115 |
| **i115f** | **Registry structured-source — audited content reshape** — For each wall-of-text status/description cell: decompose into atomic single-status items OR relocate narrative history to PR/git/design-doc; independently audit for information loss (implementer ≠ reviewer). Under the 2026-06-19 model revision the reshape also converts BLOCKED→depends_on edges and retires the closed-questions view. q28 RESOLVED: it IS the bounded-batch migration (merged with i115d). | DONE | [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | i115, i115d |
| **i115g** | **Registry structured-source — priority queue + ready/prioritize on top of the dependency DAG** — Open pullable items (leaves/childless; umbrellas excluded) become a committed priority order that agents pull from and sessions re-rank; plus depends_on edges as a cycle-free DAG whose topological extension the order must respect. A ready query returns open items with all deps satisfied, in priority order. Supersedes ROADMAP prose 'proposed sequence'. Later phase — builds on the DAG core in i115. | DONE | [#455](https://github.com/petemoore/sam-aarch64/pull/455) |  |  | docs/specs/registry-source-of-truth-design.md, i115 |
| **i118a** | **OACK handling code — Go/Z80/host-side compliance (PR #422)** — RFC 2347 OACK compliance: on OACK learn server endpoint + adopt negotiated blksize (512 default if absent) + send ACK block 0; on direct DATA use 512 default; drop windowsize from RRQ. Mirrored in Go authority (tftp.go, clientloop.go) + host tests. Z80 side: netboot_client.asm, tftp_client_front.asm. All host tests green. | DONE | [#422](https://github.com/petemoore/sam-aarch64/pull/422) |  |  | tools/netboot-oracle/tftp/tftp.go, tools/netboot-oracle/tftp/clientloop.go, src/netboot/netboot_client.asm, src/netboot/tftp_client_front.asm |
| **i119a** | **B1 — record-layer model + Z80 raw-sector read (harness CardModel)** — B1: established the record-layer model in the harness (CardModel) and implemented Z80 raw-sector read, the prerequisite for card-absolute list-sector access. | DONE | [#440](https://github.com/petemoore/sam-aarch64/pull/440) |  |  |  |
| **i119b** | **B2 — per-record inspect primitive bdos_inspect_record (stamp@232 + disk-label@210)** — B2: implemented bdos_inspect_record — reads the BDOS stamp at byte 232 and the disk label at byte 210 from a record's own sector. Host-verified. The safety check backing the mandatory show-name + confirm-before-overwrite gate. | DONE | [#442](https://github.com/petemoore/sam-aarch64/pull/442) |  |  |  |
| **i124a** | **boot-path emulation — EEPROM + ROM1 model; smoke_main + client_main end-to-end (PR #423)** — EEPROM modeled (eeprom.go), ROM1 mapped read-only at &C000 at boot, paging model in harness.go. smoke_main and client_main both run end-to-end in the harness: client_main logic confirmed sound (reaches client_first, emits ARP). Localized i82 hang to proactive ARP TX before PHY link-up (→ i127, hardware-fixed; → i131, emulation regression). | DONE | [#423](https://github.com/petemoore/sam-aarch64/pull/423) |  |  | tools/netboot-oracle/z80/eeprom.go, tools/netboot-oracle/z80/harness.go, tools/netboot-oracle/z80/netboot_boot_test.go |
| **i125** | **Section-C fit gate for every bootable netboot program (build + CI)** — A bootable netboot program loads at &8000; &C000-&FFFF is ROM1 at boot, so code/data above the budget is never written to RAM (the i119 over-size class). Generalised the down-payment client check to all *_boot.bin targets via shared script tools/netboot-boot-fit-check.sh: smoke/serve/server/client gated to 16384 (section C), http to 32768 (section-D overlay per phase3-delivery-design.md §13.1). Wired into CI by adding netboot-server-boot + netboot-serve-boot to ci-netboot-z80 so an over-size bootable image fails CI, not hardware. | DONE | [#433](https://github.com/petemoore/sam-aarch64/pull/433) |  |  | tools/netboot-boot-fit-check.sh, Makefile, docs/specs/phase3-delivery-design.md §13.1, i119, i124 |
| **i127** | **netboot client: transmit the ARP only once the ENC PHY link is up** — client_main broadcast its proactive ARP immediately after drv_init, but drv_init never waited for PHY link-up; on real hardware the PHY silently dropped the first ARP, causing a livelock (flickering-border, silent-wire symptom). Fix (a): drv_wait_link (enc_link.asm) polls PHSTAT2.LSTAT over the MII until link is up, called in client_main between drv_init and client_first. Built + emulation-verified PR #424; HARDWARE-CONFIRMED 2026-06-19 rec 13. Fix (b) ARP-retransmit-on-timeout NOT implemented — (a) alone is sufficient. Emulator model + regression landed as i131 (PR #427). | DONE | [#424](https://github.com/petemoore/sam-aarch64/pull/424) |  |  | src/netboot/enc_link.asm, src/netboot/netboot_client.asm, tools/netboot-oracle/z80/enc28j60.go, i124, i82, i119, i128, i131 |
| **i128** | **Apply drv_wait_link to http_main (the 2nd proactive netboot transmitter)** — One-line mirror of the hardware-confirmed i127 client fix: wait for PHY link after drv_init before prov_first's ARP, with a yellow-border (6) fail path. Emulation regression http_main_boot_test.go (TestHTTPMainBootReachesFirstTX + TestHTTPMainRecoversFromLinkDownStart, flat-loaded) mirrors the client's; the silent-wire model (i131) makes removing the fix go red. | DONE | [#432](https://github.com/petemoore/sam-aarch64/pull/432) |  |  | src/netboot/http_main.asm, src/netboot/enc_link.asm, tools/netboot-oracle/z80/http_main_boot_test.go, i127, i70, i82 |
| **i129** | **trinload stage-one bootstrap disk + bring-up flow (de-risk disk-shipping)** — Bootable trinload.mgt (Simon Owen's trinload, ~/git/trinload, BSD-ish; encdrv.asm/eeprom.asm vendored from it). .mgt built + written to Trinity records 128 and 3; push→run→return HARDWARE-CONFIRMED 2026-06-19. Vendor-in-repo Q settled by i132. Two uses: (a) load+run netboot tools over wire for dev iteration; (b) fallback transport if our client/server stalls. Done via hardware bring-up — no work PR; #426 is the tracking proxy. NB: trinload runs code in RAM; persisting disk images still needs i119 write-out. | DONE | [#426](https://github.com/petemoore/sam-aarch64/pull/426) |  |  | ~/git/trinload/trinload.asm, tools/build-disk/, i82, i119, i121, i130 |
| **i130** | **Name every unnamed-but-occupied Trinity record so none gets accidentally wiped** — Card (Atom Lite, 65535 records) holds Pete's collection — most occupied records are UNNAMED in samdisk list (present in samdisk dir), so a list-gap looks free when it isn't. Completed 2026-06-19: 805 records named via Python L<rec> bulk label write; method validated on rec 128 first, then bulk over 1-850, 0 data failures, all verified. Cosmetic: ~424 show a trailing leftover char (samdisk copy writes only 10-byte label; harmless). Completed as a direct Python bulk-rename on Pete's machine; PR #426 (tracking-consolidation) is the closest registry-update proxy. | DONE | [#426](https://github.com/petemoore/sam-aarch64/pull/426) |  |  | ~/git/samdisk, i129 |
| **i131** | **Feed hardware-confirmed PHY-link-up behaviour back into the emulator (i127 rule-7 loop)** — enc28j60.go doTransmit drops a frame while PHY link is down (still signalling TXIF/TXRTS — the datasheet behaviour hiding the loss), atop the already-modelled PHSTAT2.LSTAT down-then-up read (PR #424). Tests: TestDrvWriteSilentWireWhileLinkDown (drv_write while link down egresses nothing, then egresses once up) + TestClientBootRecoversFromLinkDownStart (fixed client_main with link starting down egresses its one ARP; FirstTXOps self-calibrates so removing the fix → 0 frames → red). Whole transmit-before-link-up class caught in emulation; link-up timing stays hardware-gated (CLAUDE.md §5). | DONE | [#427](https://github.com/petemoore/sam-aarch64/pull/427) |  |  | tools/netboot-oracle/z80/enc28j60.go, tools/netboot-oracle/z80/enc28j60_test.go, tools/netboot-oracle/z80/netboot_boot_test.go, tools/netboot-oracle/z80/README.md, i127, i124, i126 |
| **i132** | **Run trinload itself in the emulation harness — validate the push→run→return cycle** — Load trinload.bin (org &6000, vendored at src/netboot/trinload.asm) in the koron-z80 harness with the i124 ENC + EEPROM emulation; inject the trinload UDP protocol (? → ! reply; @+page+offset+data → LDIR; X+page+addr → pushed program executes + RETs to trinload start). Confirms control returns after the pushed program RETs (try_exec pushes hl=start before jp hl). De-risks every future trinload push. Ships: vendored trinload.asm, make netboot-trinload, TestTrinloadPushRunReturn + TestTrinloadDataWriteToOffset, LoadAt/StopPC/StopPCSkip in harness.go, HMPR snoop in enc28j60.go. | DONE | [#431](https://github.com/petemoore/sam-aarch64/pull/431) |  |  | tools/netboot-oracle/z80/trinload_test.go, src/netboot/trinload.asm, i124, i126, i129, i133 |
| **i134** | **Harness SD/B-DOS write-out model: RST-8/HRECORD/HSAVE in emulation (prerequisite for emulation-testing i119)** — Generic RST-vector hook in harness.go (rstHandlers — PC intercept at RST target, runs handler, resumes past inline operand) + bdos_store.go (BDOSStore + Machine.AttachBDOS) modelling HRECORD (record select) + HSAVE (capture+decode 48-byte UIFA at IX). Tests: mechanism test (bdos_select_record/bdos_save_hook → UIFA captured) + TestClientBootWriteOutCaptured (real client_main through TFTP transfer to write-out). Reproduces i119 record-selection bug in emulation (record 0 = floppy selected). Finding: filename >10 chars truncates in SAM UIFA. Models digital dispatch only (CLAUDE.md §5). | DONE | [#429](https://github.com/petemoore/sam-aarch64/pull/429) |  |  | tools/netboot-oracle/z80/bdos_store.go, tools/netboot-oracle/z80/bdos_store_test.go, tools/netboot-oracle/z80/harness.go, tools/netboot-oracle/z80/README.md, src/netboot/bdos_seam.asm, i119, i124, i126 |
| **i136** | **Autonomous-loop monitor: resume nudge lost to /context TUI redraw** — Pete hit a ~1h stall 2026-06-19: only /context arrived after a merged item; the agent sat idle until woken by hand. TASK_DONE branch stuffed the RESUME_NUDGE immediately after /context, but /context is a local command that redraws the whole TUI — nudge keystrokes stuffed during the redraw were lost (same stall class as earlier /context-alone-never-resumed bug). Fix: sleep ALOOP_CONTEXT_SETTLE (default 5s) between the /context submit and the nudge stuff. The 1s SUBMIT_SETTLE inside submit() was too short to outlast the render. | DONE | [#428](https://github.com/petemoore/sam-aarch64/pull/428) |  |  | tools/autonomous-loop/monitor.sh, tools/autonomous-loop/README.md |
| **i139** | **Autonomous-loop monitor: CONTEXT_SETTLE 5s too short → task-done resume nudge dropped** — After touch task-done, monitor stuffs /context then (after CONTEXT_SETTLE) stuffs RESUME_NUDGE. /context is a full-TUI redraw; with settle at 5s the nudge landed mid-render and was dropped, leaving agent idle until ~1h hang-timeout. Fix: bumped CONTEXT_SETTLE default 5→30 (matches proven CLEAR_SETTLE). PR #439 landed this fix. Superseded by i140: the longer sleep did not fully resolve the issue — root cause was burst-size, not a render race. A running monitor must be restarted to apply the CONTEXT_SETTLE change. | DONE | [#439](https://github.com/petemoore/sam-aarch64/pull/439) |  |  | tools/autonomous-loop/monitor.sh, tools/autonomous-loop/README.md, i136, i140 |
| **i140** | **monitor.sh task-done nudge not delivered — second message after /context is dropped** — Root cause (reproduced in isolated test screen session): a long string stuffed as one screen -X stuff burst right after /context full-TUI redraw is dropped wholesale (0/4 delivered) — not a render race. Fixed: stuff now chunks every payload into small bursts; task-done branch uses nudge_until_turn (verify-and-retry, polls screen for 'esc to interrupt' turn marker). Verified end-to-end in isolated session ('nudge delivered (attempt 1, 1s)', agent took a real turn, twice). PR #444. Supersedes i139's CONTEXT_SETTLE approach. | DONE | [#444](https://github.com/petemoore/sam-aarch64/pull/444) |  |  | tools/autonomous-loop/monitor.sh, tools/autonomous-loop/monitor-nudge-delivery.md, tools/autonomous-loop/README.md, i139, i97 |
