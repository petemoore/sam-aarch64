<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/questions.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Question registry — open

Open questions (`qN`) that gate one or more work items. There is no closed view:
a question is deleted once answered (see lifecycle below).

## Id conventions

`q<N>` or `q<N><letter>`. True-numeric sort. Ids are globally unique across the
item and question spaces; never reused.

## Transient-question lifecycle

1. A question is raised when an item cannot proceed without a decision.
   The gated item declares `depends_on: [qN]`.
2. Pete (or whoever is `owner`) answers the question.
3. An agent curates every dependent item — applying the decision: redefine,
   split, mark `WONTFIX`, spawn new items, raise follow-up questions.
4. Once every `depends_on: [qN]` edge has been removed from all items, the
   question is **deleted** from this file. Git history is the archive.

The validator enforces the delete-gate: a question with live `depends_on`
dependents cannot be deleted (the dangling edge would fail inv 11).

Standalone questions (not gating any specific item) resolve the same way:
the answer either spawns a new work item or the question is simply deleted
with the rationale in the git commit message.

<!-- The table below is generated — do not edit it by hand. -->


| **id** | body | owner |
|---|---|---|
| **q13** | **Editor default rendering config** (i76 / editor-rendering-rules-design.md §10) — which look does the on-SAM editor ship as its default (user-overridable)? Rules §3–§9 specified (config lab is authority); 64 cols fits only 76% of comments uncompressed. Choosing baseline-clean vs compressed-dense vs chromeless is a taste call against real PNGs / SimCoupé SCREEN$. Agent recommends tuned compressed.config: adaptive mnemonic col, &-immediates, stripped registers, label-truncate 16, sameline comments, wrap:3, max-width 40. Non-blocking — any answer is a one-line resolution + default config file. | pete |
| **q45** | i24 OUT-ceiling lift — emit_byte hot-path design fork. HSAVE/HLOAD are contiguous-only (samdos c.s:575/318-374), so lifting OUT past 32KB forces the WHOLE OUT buffer into ONE contiguous pool run sized from the pass-1 total. That collides with the 'free low-zone' optimization: today page 5 = LMPR_ENCTAB+1, so the first 16KB of OUT writes land in section B with NO LMPR bracket (paged-out-design.md); a large pool run can't start at page 5 (it would run into IN at 7+), so it loses that free path. OPTIONS: (A) UNIFORM — whole OUT is a pool run, every emit_byte brackets LMPR like today's high-zone (simplest; loses free-low-zone for the common <=16KB case; per-byte bracket = a few T-states/byte, negligible on a one-shot assemble). (B) TWO-MODE — keep the static page5/6 32KB scheme (free-low) for outputs <=32KB and a pool run only for >32KB (preserves common-case perf but doubles the emit_byte path + needs section-C budget we lack). Agent recommends (A). Pete cares about T-states (the OUT path) — hence the ask. NOTE: i24 is ALSO budget-blocked (i23 left 13 B test-variant headroom); see the commoning-pass item. | pete |
| **q46** | i32 (multiple source files + staged/partial loading) — what is the intended SHAPE? This is a 'beyond-M7 future idea' with no settled approach, and several design forks need your call before autonomous implementation: (a) how do multiple files combine — textual concatenation in a fixed order, an .include-driven single translation unit (we already have .include), or separate compilation + a link step over multiple object/.tbn units? (b) 'staged/partial loading' — is this about assembling a project too big to hold all source in RAM at once (stream files through the i2 pool), or about incremental re-assembly of only changed files? (c) does this supersede or overlap i33 (Trinity-backed staged/overlaid assembly) — should they merge? Recommend scoping once the IDE/pool (i2) lands, since staged loading depends on the pool's spill story (i215). No agent design until the shape is chosen. | pete |
| **q48** | OPEN (Pete still considering, 2026-06-23): how should the SAM assembler treat .section / named-section layout? FINDING (verified this session): on the SAM/non-flatten path, .section (and .text/.data section-switches) are zero-byte directives — they ROUND-TRIP faithfully (text->.tbn->text via render/pc.go:200; the .tbn stores them as DIRECTIVE records) but have NO effect on byte placement, because the SAM target is flat single-.org with no linker (the host -flatten path consumes/strips sections per flatten.go:86-102). So a from-scratch on-SAM project is NOT locked to spectrum4's layout (place with .org), but a .section directive looks meaningful while being placement-inert. DECISION FOR PETE: (a) keep 'preserved-but-inert' as-is (nothing broken; .section round-trips like a comment); (b) make the authoring path WARN when a .section switch appears so the author knows it won't relocate (preserve for round-trip, just surface it); or (c) add REAL named-section / linker-style placement on the SAM so .section becomes load-bearing (large feature; overlaps q46/i32 multi-source). Pete asked to keep this open; he will revisit. Do NOT change .section behaviour until Pete decides — erroring/stripping would break the existing round-trip. | pete |
| **q49** | i73 L12: .set/.equ redefinition semantics diverge between the Go authority and the Z80 assembler. The Go authority (tools/sam-aarch64/assemble/pass1.go resolveEquDirective, ~line 382) SILENTLY OVERWRITES on redefinition — res.Symbols[name]=v unconditionally, only skipping the usage-counter bump on a dup, matching GNU as where .set may reassign a symbol's value. The Z80 side (main_loop.asm main_dir_equ_do_insert -> symbols.asm symbol_insert) does jp fail on any duplicate id. Making the Z80 faithful to Go is a real BEHAVIOUR change, not a decision-free guard: the paged SYMTAB symbol_insert is insert-only (a redefine needs a lookup-and-overwrite path), and the two-pass model raises a genuine question — which value wins across pass 1 vs pass 2 for a symbol redefined mid-file, and must a forward reference between two .set's of the same name resolve to the pre- or post-redefinition value? No current release/fixture redefines a .set (the release byte-match is unaffected either way), so there is no test forcing the answer. Decision needed: (a) keep the hard duplicate-fail (current Z80 behaviour; diverges from Go/GNU but is safe), or (b) implement last-wins .set redefinition faithful to Go, defining the two-pass tie-break. Until decided, L12 is left unfixed; the other five i17 L-residuals (L3/L4/L5/L10/L11) are fixed in the i73 hardening PR. | pete |
| **q51** | i41e editor edit-model — partial/invalid-document serialize UX (the 'sharpest' open point, flagged '→ qN' in docs/specs/editor-edit-model-design.md §8.4 but never tracked). When the edit-model holds the i48-IR record payload, a document can contain raw/in-progress or invalid/error lines. The existing Compact/Pass1 encoder assumes a FULLY-resolvable assembly and errors otherwise (i41c ships fail-loud: SerializeTBN refuses any incomplete/invalid doc). i41e needs your product-UX call on how a document containing raw/error lines saves to .tbn: (a) REFUSE to save until the whole document is valid (the i41c fail-loud default — simplest, safest, but can't save WIP); (b) save VALID-LINES-ONLY (drop/skip raw/error lines — lossy, surprising); (c) save raw TEXT + error records alongside the encoded valid records (lossless WIP round-trip, needs a .tbn raw/error record variant + a format.Record sum-type wrapper — most work). This is an editor-UX product choice, not a mechanical port, so it gates i41e (CLAUDE.md spec-gate + 'Go-already-implements-it' does NOT apply — there is no Go authority for this). Related design surface that follows from the choice (agent design, not Pete): the hybrid record sum type (§8.2/§7.3), incremental symbol-table name-id lifecycle + name-id<->record-id side-tables (§8.1/§8.5), and per-line text->IR validation semantics (§8.3). Recommend (a) fail-loud as the interim and likely default unless you want WIP-save. Context: docs/specs/editor-edit-model-design.md §8. | pete |
| **q52** | i42 .tbn integrity field — format-evolution decision (on the v2 'Format B' format you designed via i39). Add a CRC/checksum/signature so a corrupted .tbn is detected, not silently mis-assembled. Your calls before autonomous implementation: (a) PRIMITIVE — CRC-16 (cheap on Z80, ~adequate for accidental corruption), CRC-32 (stronger, more T-states + 4 bytes), or a real signature (overkill for accidental corruption; only if tamper-detection matters)? (b) PLACEMENT + VERSION — a new fixed field in the v2 header (bumps the format version + touches every fixture + the release-gate/disasm-roundtrip byte-match gates), or an optional/appended trailer that old readers ignore (back-compatible, no version bump)? (c) COVERAGE — checksum the whole file, or just the record stream (excluding the integrity field itself)? (d) SAM-SIDE COST — verify-on-load costs T-states proportional to file size on real hardware; is detection worth that on every load, or verify-on-demand/optional? Recommend: CRC-32 over the record stream in an appended trailer (back-compatible, no fixture/gate churn), verified on load (the reader already streams the file). Context: docs/specs/tbn-binary-format-reference.md, docs/specs/compact-tbn-nextgen-design.md (i39 Format B). | pete |
| **q54** | i234 must free section-C space for the i204b overlay_classify/literal_word/compact_inst routines (~1300 B) + their boot self-test. These call encode_inst (reads ENCTAB in section A) so they CANNOT be evicted off-axis, and the commoning lever (i205/q43 steer, i206/i218) is EXHAUSTED for ENCTAB-coupled code (recovers tens of bytes; test variant has ~11 B headroom vs a 1300 B need). Remaining viable levers (i69/i205 analysis): (2) a SECOND BUILD_TESTS boot variant (new -D flag + make target + harness boot test + CI job) running the encode/overlay suite in a boot WITHOUT the other inline test suites — time-multiplexing memory per i69's 'tests need not coexist'; (3) chunked payloads; (4) the i2/q36 dynamic page-pool memory model (the long-term fix that lifts the whole section-C/scratch contention). Lever 2 adds a permanent CI job + build variant (a standing cost); lever 4 is a multi-session refactor. Precedent: the analogous i205 budget-relief was Pete-steered via q43. Pete's call on which lever before the agent builds i234. | pete |
| **q56** | Publish the SAMBOOT/trinboot patch (configurable default-boot-record + opening-stripes restore) as an open-source tool for Trinity owners (i177)? q55 (2026-06-26) decided to KEEP IT PRIVATE for now — the bootloader is Colin Piggot's boot block (private bytes) + our patches, built locally, out of CI. If you DO want to share it, the clean route per q55 is an upstream PATCH to LongSteve's public Trinity-bootloader repo (our diffs only, never Colin's bytes) + a link from our project, NOT a redistribution. Until you decide to publish, i177 stays blocked (no autonomous publication of Colin-derived work). Answer to unblock or WONTFIX i177. | pete |
| **q57** | i106 (SAM-as-terminal: SSH/telnet client to your Mac over the LAN) -- go/no-go + security posture before any autonomous implementation. No Go authority implements SSH, so this is a NEW design+security+product decision, not a port. The sketched 'sweet spot' is a deliberately-WEAK KEX-stub SSH: send a constant X25519 basepoint as the client ephemeral pubkey and use the server's received pubkey as the shared secret (zero EC math, instant connect, interoperates with stock OpenSSH sshd), SKIPPING host-key verification. That trades away essentially all confidentiality/integrity vs an active attacker (MITM-able, no forward secrecy) for a tiny Z80 footprint -- putting a knowingly-weak SSH client on your home LAN talking to your Mac's sshd. Decisions: (a) PURSUE i106 at all, or is a SAM SSH terminal too niche to spend the budget on right now? (b) SECURITY POSTURE if yes -- (i) the weak KEX-stub as sketched (fast, insecure, fine for a trusted home LAN), (ii) real X25519 from the start (reuses i88 X25519; ~10-min handshake on Z80 per the i106 note but actually secure), with a secure-mode toggle between them, or (iii) TELNET instead (minimal SAM code but needs Homebrew inetutils telnetd on the Mac and is cleartext)? (c) SCOPE -- MODE 3 64x24 terminal, ChaCha20-Poly1305 per-byte at a few hundred B/s; acceptable? Reuses i88 crypto + i70 tcp_conn. Answer to unblock i106 (and a follow-up design spec). | pete |
| **q58** | i126 (ONE comprehensive SAM emulation: fork SimCoupe + port the Trinity device models in) -- strategic go-ahead + approach before the multi-increment implementation begins. Direction is blessed (feedback_comprehensive_emulation, 2026-06-23); these are the open calls. Decision spec: docs/specs/comprehensive-emulation-design.md (grounded current-state map; ~2,600 LOC Go Trinity models -> C++; ~500-1000 hr effort). Decisions: (a) FORK STRATEGY -- a private maintained fork (~/git/simcoupe-trinity, rebased on upstream), an in-repo vendored copy + patch series, or UPSTREAMING a Trinity peripheral to simonowen/simcoupe so it lives in mainline (you know Simon personally)? Determines maintenance cost, how CI builds the emulator, and whether the work is public. (b) TIMING -- start now, or after the current bootblock/netboot-autonomy push (i133/i194/i264)? Today the project treats i126 as FUTURE: i232 is 'must not block current work', i241-i254 are 'gated on the i126 north star'. (c) RETIRE the flat Go harness or keep it for ~1ms dev iteration (recommend KEEP, not a gate; unified SimCoupe+Trinity becomes the CI authority). Agent's recommended default: upstream-first (offer Simon a Trinity peripheral, fall back to a private fork), DEFER until the netboot-autonomy line is past its push, KEEP the Go harness. Answer to unblock i126 implementation (then the spec's increment sketch becomes a registry umbrella + ordered children). | pete |
| **q59** | i264b record-picker UI placement fork: the SAMBOOT bootblock 'SAMBOOT Config' chunk has only ~250 free bytes — too small for a full record-picker UI (read on-card Trinity record list, render by number+name, accept selection, persist choice to the EEPROM config chunk). Three candidate homes: (A) squeeze a minimal picker in-chunk within the ~250 B; (B) a B-DOS-resident scratch page after &805F (more room, but needs B-DOS resident state at picker time); (C) boot a dedicated 'setup' Trinity record that contains the picker program (most room, cleanest, but adds a record to provision + a boot-time branch into it). Which home for the i264b picker? Gates i264b. Develop+emulate in netboot-oracle either way; hardware flash gated on i266/Pete. | pete |
| **q60** | i215c spill-backend STORAGE DESIGN — the concrete SpillBackend (Store/Load/Discard a serialized DOC block by id) needs a storage architecture the spec (ide-memory-model-design.md §4.5/§5/§7) explicitly DEFERS ('spill is a downstream feature, not i2'). i215a modelled only an in-memory FAKE backend; i215b ported only an in-RAM SP_BACKSTORE placeholder. Before the concrete Trinity/floppy backend (i215c) can be implemented faithfully, please decide (several have DATA-SAFETY implications, so I won't guess — prime directive + the shared-resource 'never clobber' invariant): (a) TRINITY RECORD SELECTION — which record(s) does spill use? A free record claimed via bdos_find_free_record (safe: never touches named records; but competes with TFTP push for free slots), highest-free like push, or a dedicated reserved 'SPILL' record? (b) BLOCK->LOCATION MAPPING — how are many DOC blocks (BlockID uint32) laid out in backend storage? One spill record holding blocks at fixed sector offsets by slot (block slot i -> sectors [i*k..]; needs a slot table), or one record per block (uses many records)? (c) SESSION PERSISTENCE — does the spill store persist across boots/sessions or is it scratch reclaimed each boot? Does spill name/claim its record (B-DOS list entry) or use it unnamed-scratch? (d) SPILL vs PUSH CONTENTION — if both use free records, what's the policy (e.g. push lowest-free, spill highest-free; or spill reserves one record up front)? (e) FLOPPY/TAPE BACKEND — the fallback when no Trinity is UNSPECIFIED entirely: file format/naming (one SAVE'd file per block named by id? a tape catalog?), or is floppy spill out-of-scope for v1 (Trinity-or-refuse MVP — the HARD constraint is only 'runs fully WITHOUT Trinity', i.e. refuse-with-message, not 'spills to floppy')? Recommendation if you want a default: v1 = Trinity-only, claim ONE free record up front as a named 'SPILL' scratch (shared-resource-safe), blocks at slot*K sector offsets, scratch (not persisted across boots); floppy deferred to a later item; refuse-with-message when no Trinity. Blocks i215c (and its split shape depends on the answers). | pete |
| **q64** | i274 P3+P4 -- process-rule recommendations from the hardware-fidelity retrospective that need Pete go/no-go before adoption into CLAUDE.md (they change how all future sessions work). P3 (the trust-damage fix): add a standing rule to LABEL unverified claims, and after a compaction/revival TREAT un-cited behavioural prose as unverified until re-checked against a primary source -- the post-compaction false-prose failure mode that re-seeded the SD-write saga. P4: add an explicit authority-diff-FIRST for port / Trinity-HW changes (emulator-green != fidelity) item to the section-3 pre-merge review checklist -- CLAUDE.md rule 8 + memory feedback_port_diff_authority_first already state the principle in prose, but P4 wants it ALSO as a checklist gate the reviewer must tick. RELATED (also awaiting your go/no-go): the broader saga section-F controls -- an append-only settled-facts register (docs/SETTLED.md or registry/settled.yaml), a findings-must-cite-a-primary-source review gate, an escalate-don't-spin clause for not-emulation-reproducible problems (from the SD-write saga post-mortem section F, now in git history). Decide which controls to adopt; each adopted one becomes a follow-up item / rule edit. | pete |
| **q68** | FUTURE write-path design for the TFTP-backing-storage feature (serve-WRQ / SAM-as-backing-storage for Pi PXE) -- captured 2026-07-01, NOT to be redesigned now (Pete). Two related options for when the TFTP-upload feature is next worked: (a) CONVERGE the serve-WRQ record-write onto the PROVEN own-CMD24 engine (sd_push / bd_record_write_hw) instead of the current unproven B-DOS-HWSAD path -- unifies the write engine and makes the feature actually work on hardware; (b) RAW-SD-SPACE storage -- for large/spanning firmware files (bigger than an 800KB record), consider raw read/write into the card wasted space (~10GB) OUTSIDE the BDOS 800KB-record structure, bypassing the record + manifest-spanning complexity (q16/i99). Decide the direction before reworking the serve-WRQ write path (i93b/i182b) or the firmware-spanning items. The feature itself is WANTED (Pi PXE firmware serving); this is purely about the storage mechanism. | pete |
