# Question index — open questions (`qN` registry, open half)

This is the **open half** of the canonical, milestone-neutral `qN` registry. It
holds every **unresolved decision for Pete** (things an agent cannot settle itself,
or chose to defer rather than guess). The sibling item registry is
`docs/notes/item-registry-open.md`.

**Governance is identical to the item registry — see `docs/notes/item-registry-open.md`
for the shared registry discipline** (stable ids locked once assigned, never
renumbered, ids globally unique across open + closed halves, marked-resolved by
moving the row to `question-registry-closed.md` rather than deleting it). In one
line for questions: `qN` ids; this table is the **single sure-fire list of what's
still open**; add the moment a question arises (not just in chat — chat questions
get lost in simultaneous-edit races); when answered, move the row to
`question-registry-closed.md` **in the same PR that resolves it**; land on `main`
via PR so Pete sees it where he reads.

## Controlled status vocabulary

Every status cell starts with exactly one token:
- `OPEN` — awaiting Pete's answer, no additional blocker
- `BLOCKED:<what>` — additionally gated on the named prerequisite or person

| id | question | status | pointer |
|----|----------|--------|---------|
| **q13** | **Editor default rendering config** (i76 / `editor-rendering-rules-design.md` §10) — which look does the on-SAM editor ship as its *default* (user-overridable)? The rendering *rules* are specified (§3–§9, the config lab is the authority) and the corpus measurements justify *some* compression (64 cols fits only 76% of comments uncompressed), but choosing the default *style* — baseline-clean (`binutils-baseline`) vs compressed-dense (a tuned `compressed`) vs chromeless (`comet-minimal`) — is a lived-use taste call against the real PNGs / SimCoupé SCREEN$. The agent **recommends** a tuned `compressed.config` (adaptive mnemonic column, SAM-native `&`-immediates, stripped registers + marked *immediates* not registers per the iteration-2 insight, label-truncate 16, sameline comments at the commented-lines-only column, `wrap:3` cursor-line expansion, max-width 40). Not a blocker — any answer is a one-line resolution + a default config file, and the renderer port is independent of it. | OPEN — agent recommendation on the table; Pete's sign-off — A/B the four starter configs | `docs/specs/editor-rendering-rules-design.md` §10; `tools/editor-prototype/configs/`; item registry i76; i6 (geometry/font, separately gated on the P3 memo) |
| **q22** | **Pin the Brick-7 record cap (the HSAVE staging-buffer RAM budget) on real Trinity hardware.** q18 resolved (Pete: build Brick 7 speculatively), so the speculative build uses a **provisional named `equ` constant** for the record cap, marked *"confirm on hardware."* The Go authority `bdos/span.go` deliberately refused to bake this ("a hardware detail pinned when the real persist is built"). When you bench the real B-DOS HSAVE persist on Trinity, confirm/adjust the cap to your actual RAM budget — a one-line change to the constant (decoupled by design). **Shipped value: `FW_RECORD_CAP = 4096`** (`src/netboot/http_main.asm`, i100b/PR #373) — chosen so the largest file (start.elf ~2.98 MB) fits the 1000-record/3-digit-index bound and the bootable fits under &10000. | BLOCKED:Pete-hardware — provisional constant (4096) shipped with i100b; real value pinned at the hardware-persist test | `src/netboot/http_main.asm` (`FW_RECORD_CAP`); `src/netboot/fw_span.asm`; `tools/netboot-oracle/bdos/span.go`; item registry i100b; q18 (resolved) |
| **q23** | **The i101 capstone orchestration UX — how are the boot→fetch→assemble→serve phases sequenced for the user?** The capstone's three layers (fetch / assemble / serve) all org `&8000` and cannot co-reside, so they run as sequential overlays handing off through the Trinity B-DOS store (design §13.1–13.2). The genuinely-new product choice is the *sequencing model*: **(a)** one auto-sequencing bootable disk that detects provisioning state (markers in the store) and runs fetch → assemble → serve automatically (needs a new resident orchestrator + a chain-load mechanism — most code, most "beautiful single disk"); **(b)** three separate single-purpose disks the user boots in order (no orchestrator — just the shared store + a documented sequence; least new code, least magic); **(c)** one disk with a boot menu (manual phase selection). This shapes the i101 sub-decomposition (whether an `i101a` orchestrator exists) and is an editor-era-style UX taste call Pete owns (cf. i100c). **Non-blocking:** the per-component logic is built + verified and the real chained boot is hardware-gated regardless (design §13.3); this only decides the resident-disk shape, and the hardcoded-pin fetch means **no i100c picker is needed** under any model. Agent leans **(a)** (it is the "one bootable disk" the goal names) with **(b)** as the zero-new-code fallback. **Awaiting Pete.** | BLOCKED:Pete — capstone UX/product call; non-blocking (components built + verified, real E2E hardware-gated either way) | `docs/specs/phase3-delivery-design.md` §13; item registry i101; i100c (sibling UX call); i80 (the absent SimCoupé-Trinity emulation that would otherwise allow a chained E2E test) |
| **q24** | **How should the on-SAM editor serialize a document that is NOT a complete valid assembly?** (editor edit-model, i41e — design `editor-edit-model-design.md` §8.4.) The save path through the assembler encoder (`frontend.Translate`→`Pass1`→`Compact`) requires a fully-resolvable assembly: every symbol defined, every PC computable. A document being *edited* will often be transiently invalid (a half-typed line, a forward reference to a label not yet typed, a syntax error). Three product behaviours: **(a)** refuse to save/assemble until the whole document is valid, surfacing the first error to the user (the i41c **fail-loud** interim default — simplest, but loses work-in-progress on a crash unless paired with the EMDL exact-serialize as an autosave); **(b)** save valid lines only, dropping/flagging invalid ones (data-lossy, surprising); **(c)** persist raw text + error records for invalid lines (the §7.3 hybrid — the document round-trips losslessly through edits, and only *assembly* requires validity). This is an **editor-UX taste call** Pete owns (cf. q13/q23) and it shapes i41e's hybrid-record + serialize design. Agent leans **(c)** for the *document* persistence (lossless editing) with **(a)**'s fail-loud reserved for the *assemble-to-`.tbn`-for-the-assembler* step — i.e. the editor always saves your raw text, but "assemble" refuses until valid. **Non-blocking:** i41c ships fail-loud (safe); i41e needs this answer before its hybrid-record design. | BLOCKED:Pete — editor-UX product call; non-blocking (i41c fail-loud is the safe interim) | `docs/specs/editor-edit-model-design.md` §8 (item 4), §7.3; item registry i41e, i41c |
| **q25** | **Should we investigate "CIIP" — a second, pre-existing Z80 TCP/IP stack — as prior art / a building block for the SAM netboot stack (i83 TFTP server, i70 HTTP fetch, i86 DHCP)?** Surfaced by the i113 discovery pass over the recovered Trinity docs (2026-06-17): the docs reference **CIIP**, a CPC-originated Z80 TCP/IP stack by "Mark Hi…" (author name OCR-damaged — needs photo-verify, `IMG_20260617_163218`) that reportedly already provides a **TFTP server, HTTP server, DNS server**, plus telnet/finger/host/ping clients and an ifconfig module — and **Simon Owen ported it to pyz80**. This matters because Phase-3 is building exactly those servers from scratch in Z80; an existing, working stack could be major prior art (design reference, fidelity oracle, or even a base to adapt — cf. how trinload's `encdrv`/`eeprom` were vendored). The question is whether to pursue it, and how: locate the actual CIIP code (Simon's pyz80 port; CPC archives), assess license/portability, and weigh "adapt CIIP" vs "continue our from-scratch i82/i83/i86 path" (which is already host-verified and well underway). **UNVERIFIED** — all detail is OCR-derived pending photo confirmation; do not treat CIIP's capabilities as established until `IMG_20260617_163218` is read. Pete may already know this artifact (community contact). | OPEN — 🔎 lead to assess; UNVERIFIED (OCR, pending photo `IMG_20260617_163218`); Pete's call whether to chase | `~/sam-archive/trinity-docs/DISCOVERY_REPORT.md`; item registry i113 (source), i83, i70, i82, i86; `memory/project_sam_community_contacts` (Simon Owen) |
| **q27** | **i119 free-record detection/selection policy — what counts as "claimable", given the central record list is not user-reachable?** (Surfaced 2026-06-19 by the B2 emulation work; the fork RE proves no RST-8 hook reads the card's central record list or a card-absolute sector — `bdos15a.src.txt:907-919,1243-1297` — so a user program can only inspect a record it has HRECORD-**selected**, via that record's own first sector: stamp@232 + label@210.) The reachable free-detection *signal* is therefore coarse: HRECORD **succeeds** (record is a valid, "BDOS"-stamped B-DOS record) or raises **error 81** (no stamp — blank OR a foreign-DOS disk we can't read). Two policies: **(a) faithful / hardware-robust (agent default, building toward this):** operate only on records the user designates that HRECORD-*succeeds* on — read the record's own label + emptiness, **show the name + confirm before overwrite**; "auto-pick" scans for an *empty B-DOS record*. No error-trap, no version-specific addresses. *Cost:* cannot auto-claim a **never-formatted blank** record — the user must B-DOS-`FORMAT` a record first to make it claimable (a reasonable "this slot is for B-DOS use" act on a shared card). **(b) more capable / brittle:** additionally arm the **B-DOS-internal `nmi.sp` error trap** (`bdos15a.src.txt:4848-4886`) to survive error 81 and classify/skip **non-B-DOS** records, enabling true "any free record" auto-pick — but `nmi.sp` is an undocumented internal whose address relocated 1.5a→1.5t, is shared with NMI/snapshot state, and is unverifiable without hardware. Pete's recorded UX intent ("prompt OR auto-pick; show name; confirm before overwrite; never silently trash a record") is satisfied by **both**; the only product-visible difference is whether a *never-formatted* record can be auto-claimed. **Non-blocking:** the agent is building (a) (the inspect primitive landed B2, host-verified); (b) is additive later if Pete wants it. The confirm-before-overwrite gate is mandatory either way (the "is this slot truly blank?" marker is not user-reachable). | OPEN — agent default is (a); Pete confirms the policy + whether (b)'s brittle trap-probe is worth pursuing for never-formatted-record auto-claim | `src/netboot/bdos_seam.asm` (`bdos_inspect_record`); `docs/notes/item-registry-open.md` i119; `memory/trinity_storage_shared_resource`; `~/sam-archive/bdos/analysis/bdos15a.src.txt`; i119, i114, i121, i122 |
| **q28** | **i115f reshape — land it in the markdown first, or directly in validated YAML at migration?** (Surfaced 2026-06-19 during the registry model revision.) You said "i115f runs before any other i115 part." Under the revised model (dependencies are structural), the audited reshape (atomic rows; `BLOCKED`→`depends_on` edges; history→git; rationale→cited design docs; retire the closed-questions view) can be done either: **(a) on the live markdown first** — honours "literally first," but hand-editing ~100 rows has no validator safety net (the exact decay this whole effort eliminates); or **(b) as the migration into validated YAML** — the validator + DAG checks back the reshape, it reshapes once directly into the target structure, but the tool is built first. Agent leans **(b)**: same audited, information-preserving reshape, validator-backed, no double-work; "i115f first" is honoured as "the audited reshape is the first *content* step, before the cutover." **Non-blocking** — the tool rework is needed under either reading and building it does not foreclose the choice. | OPEN — agent leans (b) (validator-backed reshape); Pete confirms the ordering reading | `docs/specs/registry-source-of-truth-design.md`; `docs/plans/registry-structured-source.md`; i115, i115f, i115d |