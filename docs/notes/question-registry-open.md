<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/questions.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Question registry — open

Open questions (`qN`) that gate one or more work items. There is no closed view:
a question is deleted once answered (see lifecycle below).

## Id conventions

`q<N>` or `q<N><letter>`. True-numeric sort. Ids are globally unique across the
item and question spaces; never reused.

## Transient-question lifecycle

1. A question is raised when an item cannot proceed without a decision.
   The gated item declares `depends_on: [qN]`.
2. Pete (or whoever is `owner`) answers the question.
3. An agent curates every dependent item — applying the decision: redefine,
   split, mark `WONTFIX`, spawn new items, raise follow-up questions.
4. Once every `depends_on: [qN]` edge has been removed from all items, the
   question is **deleted** from this file. Git history is the archive.

The validator enforces the delete-gate: a question with live `depends_on`
dependents cannot be deleted (the dangling edge would fail inv 11).

Standalone questions (not gating any specific item) resolve the same way:
the answer either spawns a new work item or the question is simply deleted
with the rationale in the git commit message.

<!-- The table below is generated — do not edit it by hand. -->


| **id** | body | owner |
|---|---|---|
| **q13** | **Editor default rendering config** (i76 / editor-rendering-rules-design.md §10) — which look does the on-SAM editor ship as its default (user-overridable)? Rules §3–§9 specified (config lab is authority); 64 cols fits only 76% of comments uncompressed. Choosing baseline-clean vs compressed-dense vs chromeless is a taste call against real PNGs / SimCoupé SCREEN$. Agent recommends tuned compressed.config: adaptive mnemonic col, &-immediates, stripped registers, label-truncate 16, sameline comments, wrap:3, max-width 40. Non-blocking — any answer is a one-line resolution + default config file. | pete |
| **q22** | **Pin the Brick-7 record cap (HSAVE staging-buffer RAM budget) on real Trinity hardware.** q18 resolved (Pete: build Brick 7 speculatively), so the build uses a provisional equ for the record cap, marked 'confirm on hardware.' The Go authority bdos/span.go deliberately refused to bake this. Bench the real B-DOS HSAVE persist on Trinity, confirm/adjust the cap — a one-line change (decoupled by design). Shipped value: FW_RECORD_CAP = 4096 (src/netboot/http_main.asm, i100b/PR #373) — largest file fits 1000-record/3-digit-index bound, bootable fits under &10000. | pete |
| **q23** | **The i101 capstone orchestration UX — how are the boot→fetch→assemble→serve phases sequenced?** The three layers all org &8000 and cannot co-reside; they run as sequential overlays via the Trinity B-DOS store (design §13.1–13.2). Product choice: (a) auto-sequencing disk detecting provisioning state, chains fetch→assemble→serve (needs orchestrator + chain-load); (b) three separate single-purpose disks in order; (c) one disk with boot menu. Agent leans (a) with (b) as fallback. Non-blocking: components built; real chained boot is hardware-gated (§13.3); no i100c picker needed under any model. | pete |
| **q24** | **How should the on-SAM editor serialize a document that is NOT a complete valid assembly?** (i41e — editor-edit-model-design.md §8.4.) Three product behaviours: (a) refuse to save until valid (i41c fail-loud — loses WIP on crash); (b) save valid lines only, dropping invalid (data-lossy); (c) persist raw text + error records for invalid lines (§7.3 hybrid — lossless round-trips; only assembly requires validity). Agent leans (c) for document persistence with (a) fail-loud for assemble-to-.tbn. Non-blocking: i41c ships fail-loud; i41e needs this answer before its hybrid-record design. | pete |
| **q25** | **Should we investigate CIIP — a pre-existing Z80 TCP/IP stack — as prior art for the SAM netboot stack (i83 TFTP, i70 HTTP, i86 DHCP)?** Surfaced by i113 discovery (2026-06-17): CIIP is CPC-originated (author OCR-damaged — photo-verify IMG_20260617_163218) reportedly providing TFTP/HTTP/DNS servers + clients; Simon Owen ported it to pyz80. Phase-3 is building exactly those servers; CIIP could be prior art. Task: locate code, assess license/portability, weigh 'adapt CIIP' vs 'continue i82/i83/i86 path'. UNVERIFIED — OCR-derived; do not treat capabilities as established until photo-confirmed. | pete |
| **q30** | Remote-push (i121 WRQ) safety model: i119 makes an interactive show-name + y/n confirm MANDATORY before any HSAVE, but an unattended 'tftp put' has no operator at the SAM keyboard (the confirm spins forever). Is 'write-to-FREE-record-only' sufficient safety (auto-pick a free record; TFTP ERROR(3) if none free; never overwrite a named record - satisfies the shared-resource invariant without a keyboard), or must remote pushes be operator-armed (boot-time 'pushes allowed' pre-confirm)? Gates i121c (the write brick). Plan: docs/plans/i121-wrq-server.md. | pete |
