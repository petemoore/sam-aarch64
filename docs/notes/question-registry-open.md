<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/questions.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Question registry — open

Open questions (`qN`) that gate one or more work items. There is no closed view:
a question is deleted once answered (see lifecycle below).

## Id conventions

`q<N>` or `q<N><letter>`. True-numeric sort. Ids are globally unique across the
item and question spaces; never reused.

## Transient-question lifecycle

1. A question is raised when an item cannot proceed without a decision.
   The gated item declares `depends_on: [qN]`.
2. Pete (or whoever is `owner`) answers the question.
3. An agent curates every dependent item — applying the decision: redefine,
   split, mark `WONTFIX`, spawn new items, raise follow-up questions.
4. Once every `depends_on: [qN]` edge has been removed from all items, the
   question is **deleted** from this file. Git history is the archive.

The validator enforces the delete-gate: a question with live `depends_on`
dependents cannot be deleted (the dangling edge would fail inv 11).

Standalone questions (not gating any specific item) resolve the same way:
the answer either spawns a new work item or the question is simply deleted
with the rationale in the git commit message.

<!-- The table below is generated — do not edit it by hand. -->


| **id** | body | owner |
|---|---|---|
| **q13** | **Editor default rendering config** (i76 / editor-rendering-rules-design.md §10) — which look does the on-SAM editor ship as its default (user-overridable)? Rules §3–§9 specified (config lab is authority); 64 cols fits only 76% of comments uncompressed. Choosing baseline-clean vs compressed-dense vs chromeless is a taste call against real PNGs / SimCoupé SCREEN$. Agent recommends tuned compressed.config: adaptive mnemonic col, &-immediates, stripped registers, label-truncate 16, sameline comments, wrap:3, max-width 40. Non-blocking — any answer is a one-line resolution + default config file. | pete |
| **q35** | i122 brick 4 wants 'before fetching, if the content hash is already stored, skip the fetch and boot the existing copy.' Two tensions block a faithful build:<br><br>(1) HASH SOURCE: the client fetches by FILENAME over TFTP and has no content hash until it has fetched the bytes; to SKIP a fetch you need the hash FIRST. So it must come from a boot-config field or a name->hash manifest lookup. Which?<br><br>(2) STORAGE MODEL: i122c (PR #498) writes the image to a FIXED reused scratch record (i122 note a), NOT content-addressed. i114d's <hash6><NNN> naming is for SPANNED FLAT-FILES, not raw disk records, so a prior fetch-and-boot leaves nothing dedup-findable by hash. Options: (A) store disk images content-addressed (supersedes scratch) -> records ACCUMULATE, needing free-record alloc + the mandatory shared-resource show-name+confirm gate; (B) keep scratch, dedup only against images PROVISIONED via the content-addressed path (i70/i100+manifest) -> boot a provisioned copy directly, else scratch-fetch; (C) a minimal last-fetched-hash cache that only short-circuits an immediate re-boot of the same image.<br><br>RECOMMEND (B): dedup checks if the requested image is already on-card under its content-addressed name (prior provisioning, via the i119 list-read scan + i114d naming), boot it without re-fetch; else fetch into scratch (preserves note a). Hash from a name->hash manifest entry (i114). Needs Pete: touches the shared-storage policy he has corrected repeatedly (trinity_storage_shared_resource) and decides whether i122c's scratch model stands. Until answered, any i122d choice is a guess that may contradict storage policy. | pete |
| **q36** | IDE memory-model architecture (i2; design proposal docs/specs/ide-memory-model-design.md). HEADLINE: does the on-SAM IDE adopt ONE dynamic page pool shared by the editor AND the assembler -- a resident page_owner[] allocator + boot-time sizing from PRAMTP that supersedes the assembler hardcoded off-axis pages 4-15 and LIFTS the fixed 96KB-IN (i23) / 32KB-OUT (i24) ceilings -- or does the editor layer a SEPARATE pool on top of the assembler existing static layout (smaller change, but two memory regimes and no IN/OUT lift)? Sub-decisions riding the same answer: (1) minimum target machine -- is 256KB first-class (~144KB document ceiling, hard reliance on evict-on-assemble) or is 512KB the floor? (2) full-document pool exhaustion -- refuse-with-message now + disk-spill deferred (recommended), or is disk-spill in scope for i2? (3) ALLOCT coexistence -- IDE owns allocation outright for its session (rebuild BASIC view only on exit), or keep BASIC ALLOCT consistent for clean mid-session hand-back? RECOMMEND: the unified pool (proposal section 4), 256KB supported but documents smaller, refuse-with-message exhaustion (disk-spill deferred to a tracked follow-up), IDE owns allocation for its session. Rationale: it is the model i41 section 5.2 already assumes (per-block page-list claimed from the i2 pool; i40 evict/reload = free/reclaim), delivers the i23/i24 lifts for free, and matches claim-all-free-RAM-grow-on-demand directly. Needs Pete: this is the foundational editor-era memory direction the handover reserved for his steer before an agent commits foundational decisions. | pete |
| **q38** | AWAITING the assessment report i209 (Pete, 2026-06-22). Pete actively uses macros on the Mac; on-SAM macros 'would be a huge win' provided no big perf/size cost or redesign. i209 (agent) produces a scope + perf/size-impact report; once it lands, Pete returns HERE to give the go/no-go + scope for i31 (the on-SAM .if + macro implementation). Original: pursue an on-SAM preprocessor now, at what scope? Host-side asmparse/text2bin already covers this; moving on-SAM is a new beyond-M7 feature with real scope choices (.if syntax; macro semantics; integration vs host preprocess; Go-authority side). Gates i31. | pete |
| **q42** | AWAITING the assessment report i210 (Pete, 2026-06-22). Pete: linker-script parsing 'would be awesome' if not a large rewrite/perf cost. i210 (agent) reports the touch points + cost estimate; once it lands, Pete returns HERE for the go/no-go on i56 (extract SpectrumFourLayout / a -layout flag or linker-script parser). i20 depends on i56. | pete |
