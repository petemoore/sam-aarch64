<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/priority.yaml + registry/items.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Backlog — priority queue

The curated order of open pullable work items (leaves / childless items;
umbrellas are spanned by their leaves). Highest priority first. The order is
stored in `registry/priority.yaml` and validated in CI as a strict permutation
of all open pullable items + a topological extension of the dependency DAG
(nothing ranked before an item it depends on).

**Agents pull from the top.** The `registry ready` command returns the unblocked
tip — items whose dependencies are all satisfied — in this priority order. Pull
from the first `ready` item to ensure the highest-priority unblocked work is
always picked up.

**Re-rank with:**

```
registry prioritize --id iN --to-top
registry move --id iN --before iM
registry move --id iN --after  iM
```

This view is the **single source of work ordering** — the ROADMAP `Current State` block is state/context only, never a work queue.
Source of truth: `registry/priority.yaml`. Spec: `docs/specs/registry-source-of-truth-design.md §"Priority queue + ready"`.

<!-- The table below is generated — do not edit it by hand. -->


| **id** | item | status | owner | gate | deps | dependents |
|---|---|---|---|---|---|---|
| <a id="i362"></a>**i362** | **Record-body read-back tool: expose a served record-relative sector read so a shot can confirm what was written INTO a Trinity record's disk image (not just the card record-LIST)** — The i70b HTTP-store smoke build OMITS bdos_claim_record (i360 residual 2, verified http_main.asm:758-760): a successful store writes the record's disk BODY (store_format_record's +232 'BDOS' stamp; HSAVE's LICENCE.broadcom dir entry) but never names the record-LIST entry — so list_records reports the record FREE and is STRUCTURALLY BLIND to the store. Needed: a read-only served command (extend list_records or a sibling program on the 0xEDB0 protocol) that, given a record number + a record-relative sector index, computes the record base sector (port sd_push.asm record-base arithmetic; RECORD_SECTORS=1600) and CMD17-reads that sector back over the net. The classify primitive bdos_inspect_record (bdos_seam.asm:255 — +232 'BDOS' stamp + +210 label) already exists. Launcher reads record N's track-0 directory range and scans for the 'BDOS' stamp + the stored filename. This confirms i70b's HSAVE store from the ON-CARD evidence WITHOUT adding the ~200 B claim (doesn't fit the 32741/32768 smoke build) and WITHOUT a re-shoot. Read-only: build without any list-write primitive (trinity_storage_shared_resource). Emulation-first: add a faithful/host test that reads back a known record body. | OPEN | agent | ready |  | [i70b](#i70b) |
| <a id="i359b"></a>**i359b** | **Store (sd_push) + boot-verify every built .mgt disk to a Trinity record; fill the record->disk mapping** — Store the built .mgt disks as Trinity records + fill the record->disk mapping. SCOPE REFINED by the i359a catalogue (docs/disk-catalogue.md): of the 27 disks in build/, MOST are NOT record-worthy — the 12 per-fixture corpus disks (inst_*/dir_*/expr_*/empty/set_neg_highword) are transient test residue regenerated each test-run, and okstub/dbgstub/keyin-proof are deletable i12b one-offs (no Makefile target). PRODUCT disks warranting a permanent bootable record: test_record.mgt (rec 186, DONE), netboot_server_record.mgt (187, DONE), netboot_serve_record.mgt (145, DONE), release.mgt (no -code-auto record vessel built yet). Disassembler+editor are NOT standalone disks (disasm ships as the d15 payload inside the assembler disk; editor = font-proof prototype) so there is nothing to push for them. NET new push is small. AWAITING PETE STEER (coverage): only the product disks, or a BROADER set (incl. corpus) pushed for on-hardware test coverage (the i319b per-image-boot goal)? SAFE sub-step now: run list-records (read-only) against the live SAM (reachable 2026-07-04) to CONFIRM the record->disk mapping (documented->confirmed) in the catalogue. Bulk unattended pushing to the shared card deliberately NOT done unattended (no-retransmit i348 fragility + shared-resource discipline). | OPEN | agent | 🔒 q73 | q73 |  |
| <a id="i41e"></a>**i41e** | **Editor edit-model — i48-IR record payload + document symbol table + hybrid record model** — Replace i41a's raw-text line payload with the i48 in-memory symbolic IR (parsed format.Record): a document-global symbol table + name-id/record-id side-tables (§3), a hybrid committed-parsed / active-raw / invalid-error record type (§7.3), per-line text→IR entry. q24 resolved (2026-06-20): the editor validates ONE LINE AT A TIME — no whole-document import to serialize invalidly; the design point is how an in-progress/invalid LINE is held + round-tripped (whole-file import = i165). Open design (symbol-table lifecycle, hybrid sum type, per-line parse) — needs a design pass (§8/§5.2/§6/§7.3). | OPEN | agent | 🔒 q51 | i2a, q51 |  |
| <a id="i43"></a>**i43** | **Editor: register simulator with user-chosen seeds + replay-on-edit** — Phase-2 editor sub-feature. Register simulator with user-chosen seeds: step instructions, inject/randomise source regs, show dest regs/flags/memory, and replay-on-edit. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 | i41 |  |
| <a id="i44"></a>**i44** | **Editor: inline instruction-explanation panel (prose semantics, flags, regs, bit-field layout)** — Phase-2 editor sub-feature. Inline instruction-explanation panel showing prose semantics, flags set, regs read/written, and bit-field layout. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 | i41 |  |
| <a id="i45"></a>**i45** | **Editor: 'did you mean a simpler instruction?' result-equivalent rewrite hint** — Phase-2 editor sub-feature. Suggests result-equivalent but simpler instruction rewrites when a more concise encoding exists. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 | i41 |  |
| <a id="i46"></a>**i46** | **Editor: system-register documentation panel (ARM MRA subset + editorial overlay)** — Phase-2 editor sub-feature. System-register documentation panel surfaced from the cursor line, embedding an ARM MRA subset with editorial overlay. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 | i41 |  |
| <a id="i47"></a>**i47** | **Editor: inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations)** — Phase-2 editor sub-features. 'Why is this instruction here?' inverse-flow navigation plus retro UI affordances including chiptune audio, period fonts, and animations. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 | i41 |  |
| <a id="i177"></a>**i177** | **Publish trinboot: open-source EEPROM boot-config patcher (Python-over-trinload + bootable .mgt) for Trinity users** — Release the SAMBOOT EEPROM boot-config + stripes-restore patch as a standalone open-source tool for any Trinity owner: a Python program sent over trinload that patches the EEPROM, or a bootable .mgt. Gives Trinity users the configurable default-boot-record + opening-stripes feature for free. NOT a requirement for SAMBOOT -- depends on it (publication only). | OPEN | agent | 🔒 q56 | i135, i176, q56 |  |
| <a id="i345"></a>**i345** | **Netboot-target Pi rig: hub-isolated network segment with a netboot-enabled Pi wired to the SAM (Pete hardware setup)** — Physical prerequisite for any REAL-Pi netboot exchange against the SAM (i95b real-Pi boot; i89b Pi-3 capture): a netboot-enabled target Pi (Pi 400 per the existing captures, or Pi 3 for i89b) wired to the SAM on an ISOLATED segment (the hub+Wireshark rig from the trinload captures, pi-netboot-capture-analysis.md). Isolation is load-bearing: the SAM's DHCP server answering DISCOVERs on the shared house LAN would be a rogue DHCP server handing broken leases to other devices - the reference dnsmasq capture setup used a separate USB-NIC subnet (192.168.50.x) for exactly this reason. Pete-owned: physically wiring/enabling the rig; once it exists the exchanges themselves can run agent-side. | OPEN | pete | 👤 pete |  | [i89b](#i89b), [i95b-b2](#i95b-b2) |
| <a id="i95b-b2"></a>**i95b-b2** | **Real Pi netboots from the SAM end-to-end (rig-gated): DHCP + TFTP boot of the physical target Pi** — The physical-client tail of the end-to-end Pi boot: with the i345 hub-isolated rig in place and the i95b-b1 simulated-client shot proven, power the netboot-enabled target Pi and confirm it DHCP-leases from the SAM, TFTP-fetches its boot file set from the Trinity store, and boots. Multi-MB firmware provisioning/serving (spanning) rides the i70 residuals. OPEN RISK (Pete, 2026-07-02): at the measured bit-bang throughput (~5-9 KB/s) a full Pi 400 fetch (start4.elf ~2.9 MB + kernel) plausibly takes 10-20 min; TFTP's per-block lock-step keeps per-block gaps ~60-110 ms (fine for retransmit timeouts), but whether the Pi 4/400 EEPROM bootloader has a GLOBAL watchdog that aborts a multi-minute TFTP phase is unknown - this shot is what answers it. Use the i95b-b1 measured serve throughput to set expectations before the rig session. | OPEN | agent | ⏳ i345 | [i345](#i345), i95b-b1, i346 |  |
| <a id="i310"></a>**i310** | **Provide a standards-compliant TFTP server in the test environment (for the i118b OACK-interop hardware test)** — i118b needs the netboot CLIENT to fetch from a STANDARDS-COMPLIANT third-party TFTP server (e.g. tftpd-hpa on the Pi, or macOS tftpd) that emits OACK for optioned RRQ. No such server is installed/reachable in the test env (no tftpd on this host; a standards-compliant server binds privileged port 69 -> needs apt install + config or a setcap/sudoers grant, which is Pete's host action, not covered by i272's tcpdump/arping caps). Once provided + reachable from the SAM (192.168.2.75), i118b unblocks and the agent runs the OACK-receive shot self-serve (power-cycle + push client + tcpdump). owner:pete = the one-time host setup. | OPEN | pete | 👤 pete |  | [i118b](#i118b) |
| <a id="i118b"></a>**i118b** | **hardware verification — OACK path on real Trinity hardware** — Confirm the OACK path works on real Trinity hardware: netboot client connects to a standards-compliant TFTP server (e.g. macOS tftpd), receives OACK, ACKs block 0, and receives the file. Gated on Trinity hardware access + a standards-compliant server in the test environment. | OPEN | agent | ⏳ i310 | i272, [i310](#i310) |  |
| <a id="i32"></a>**i32** | **Multiple source files + staged/partial loading** — Beyond-M7 / future idea. Support for assembling from multiple source files with staged or partial loading, enabling larger projects than currently fit in a single-file workflow. | OPEN | agent | 🔒 q46 | i2a, q46 |  |
| <a id="i50"></a>**i50** | **Host Go-tool CLI shape — deferred design question (one binary vs three vs pandoc-style)** — DECIDED (Pete, 2026-06-25): adopt the pandoc-style --from/--to CLI for the host Go tools (e.g. 'sam-aarch64 --from=asm --to=tbn foo.s'; --from=tbn --to=asm; future --from=gnu --to=tbn). Supersedes the earlier deferral (one front door vs three vs pandoc). Implementation is a thin presentation front-end: the three concerns are ALREADY cleanly separated as libraries (aarch64enc / aarch64dec / sam-aarch64-format+frontend), so this is CLI wiring over existing libs, not new functionality.<br><br>Scope notes (from a partial 2026-06-25 survey): the current integrated 'sam-aarch64' (tools/sam-aarch64/main.go) modes map onto from/to — assemble src->bin (--from=asm --to=bin/img), assemble .tbn->bin (--from=tbn --to=bin), --render .tbn->text (--from=tbn --to=asm), -E preprocess. Settle the format taxonomy (is 'gnu' distinct from 'asm'? our source IS gnu-syntax; --from=gnu may just alias, OR imply the future bulk GNU->.tbn import which does NOT exist yet — do NOT build that here unless scoped). Keep the existing CI/Makefile/run-*.sh invocations working (they use the current flags — see tools/run-disasm-roundtrip.sh, run-release-gate.sh, Makefile release targets). LOW priority: per memory project_go_tools_are_z80_scaffolding, the Go tools are Z80-port scaffolding + oracle — do not gold-plate; this is a presentation reshape, not a value-add. | OPEN | pete | 👤 pete |  |  |
| <a id="i65"></a>**i65** | **SAM-side save-without-comments option (editor phase)** — On-SAM counterpart to the host -strip-comments/-thin-comments flags. Saves a .tbn with the editor region stripped/thinned so a 256 KB SAM can assemble sources authored on a 512 KB machine. q9 resolved (Pete 2026-06-10): the tool targets both 256 and 512 KB SAMs; the full unstripped release .tbn is allowed to exceed 256 KB, but a 256 KB user must still be able to assemble via this stripped disk handoff. Host equivalent already exists. | OPEN | agent | ⏳ i41 | i41 |  |
| <a id="i70b"></a>**i70b** | **HTTP-fetch smoke hardware shot: EEPROM read + section-C staging + HSAVE RST-8 verified on real Trinity (single small file from a LAN server)** — The scoped hardware validation: boot the smoke-variant fetcher on the real SAM, fetch LICENCE.broadcom (1594 B) from a LAN HTTP mirror, persist via HSAVE RST-8 into the first-FREE SD record. SHOT 4 (2026-07-04, plain smoke build @ server 192.168.2.44:8080, first-free=REC 199): the FULL HTTP fetch is now HARDWARE-PROVEN end-to-end — ARP -> TCP handshake -> GET the exact cdn path -> ALL 1594 body bytes received -> clean 4-way FIN close (SAM acked the complete body + server FIN and sent its own FIN); the SAM then returned CLEANLY to trinload (list_records pushed fine afterward, NO wedge, no power-cycle) — distinct from shots 2/3's store-leg hang, confirming i357 removed the HRECORD-into-DI'd-driver wedge. Store UNCONFIRMED but likely-succeeded: post-cycle list_records shows REC 199 still FREE (811 used before AND after) which is EXPECTED even on success because the smoke build OMITS bdos_claim_record (i360 residual 2, verified http_main.asm:758-760): store_format_record writes only the record DISK BODY (+232 'BDOS' stamp) and never the record-LIST name entry that list_records tests. So list_records is STRUCTURALLY BLIND to the smoke store; confirmation needs reading REC 199's disk body (BDOS stamp + LICENCE.broadcom dir entry) — evidence persists on the card. Gated on i362 (record-body read tool). pcap saved in session scratch/shot.pcap. Earlier: SHOT 2/3 got partial body then a store-leg wedge (i355/i356/i357 root-caused + fixed). i347-class IM2 exposure assessed-low for the seconds-long smoke. | OPEN | agent | ⏳ i362 | i70a, i70d, i70e, i355, i357, [i362](#i362) | [i70c](#i70c) |
| <a id="i70c"></a>**i70c** | **Directory-safe multi-blob persist + full 6-file cdn manifest on hardware (start.elf-scale spanning)** — The full i70 payoff, blocked on a persist design reconciliation: at FW_RECORD_CAP=6144 (http_main.asm:333) start.elf alone becomes 486 HSAVE files in ONE B-DOS record directory (~80-slot limit) — the fw_span record-spanning design (src/netboot/fw_span.asm, phase3-delivery-design.md) must be reconciled with directory capacity (span across multiple first-FREE records or raise the per-file window) before any multi-MB persist. Also assess the i347 IM2 exposure for the 10-18+ min full fetch (Z80 SHA-256 in the loop dominates; measured bands in trinity-capabilities.md), and decide the cdn reachability story (LAN mirror of the pinned a43df3a firmware blobs vs resolved cdn IP; blobs verified live 2026-07-03, SHA-256-pinned so an untrusted proxy is fine). | OPEN | agent | ⏳ i70b | [i70b](#i70b) |  |
| <a id="i77"></a>**i77** | **spectrum4 source succinctification — comments + pathological constant expressions** — Agent-driven, section-by-section, human-reviewed (Pete 2026-06-12): (a) comments rewritten aggressively succinct but still readable; (b) pathological constant expressions replaced with named equates (e.g. the longest instruction in the corpus: `movk w0, (65536 \| (((BORDER_COLOUR >> 16) & 65535) & 65535)), lsl #16`). Coordinate with upstream spectrum4 (content originates there; release.s re-flattens). Ties to editor-tui spec D1 (all comments <256B → u16 record length can revert to u8). Parked (editor era). | OPEN | agent | ⏳ i3 | i3 |  |
| <a id="i81a"></a>**i81a** | **PNG-endian one-line upstream SimCoupé PR: the __aarch64__ little-endian-guard fix in Base/SimCoupe.h** — hunk 2 of tools/font-proof/simcoupe-local-capture.patch; confirmed NOT in upstream HEAD; ready to offer standalone; community submission is Pete's call. | OPEN | pete | 👤 pete |  |  |
| <a id="i81c"></a>**i81c** | **Optionally offer the SDL dummy-driver headless renderer fallback upstream** — Lower priority than i81a; community submission is Pete's call. | OPEN | pete | 👤 pete |  |  |
| <a id="i88c-b1"></a>**i88c-b1** | **i88c-b1 — wire tls_reasm into CONN_SINK_FILTER: sink+emit shims, bounded emulation integration test** — The wiring: new src/netboot/tls_main.asm composes tls_reasm + tls_client + tcp_conn's CONN_SINK_FILTER dispatch with two shims — tls_sink_feed (CONN_SINK_FILTER target: HL=window,BC=len -> jp tls_reasm_feed) and tls_record_shim (REASM_EMIT_PTR: copy the emitted record REASM_BUF->TC_RX, set TC_RX_LEN, call tls_client_on_record). Emulation-first: a new tools/netboot-oracle/z80/tls_integration_test.go replays the existing captured handshake flight (records <=TC_RX=1056, flat-buildable) chunked at mis-aligned TCP-segment sizes THROUGH storage_sink_flush (the real dispatch) and asserts TC_PHASE reaches DONE / secrets match the Go authority. REASM_BUF placed in low memory so the composite fits flat. Land via PR. body_sink_write is the precedent. | OPEN | agent | ready |  | [i88c-b2](#i88c-b2) |
| <a id="i88c-b2"></a>**i88c-b2** | **i88c-b2 — grow TC_RX 1056->16645 + finalise paged buffer layout for full-size TLS records** — Grow TC_RX to REASM_MAX (16645) so a real Certificate record fits, and finalise the paged layout: measured, REASM_BUF(16645)+TC_RX(16645)+crypto code cannot coexist in a flat build (tls_client composite already 30629 B at &8000), so a multi-page arrangement is required. No Go authority (a Z80 paging constraint) -> design decision q72. | OPEN | agent | 🔒 q72 | [i88c-b1](#i88c-b1), q72 | [i88c-b3](#i88c-b3) |
| <a id="i88c-b3"></a>**i88c-b3** | **i88c-b3 — bootable TLS disk: tls_main.asm as NETBOOT_STREAM boot binary + make target + build-disk wiring** — Build the bootable TLS artifact: tls_main.asm assembled with NETBOOT_STREAM, org &8000 (jp entry), a make netboot-tls-boot/-disk target and build-disk wiring, mirroring netboot-http-boot/-disk. depends_on the finalised paged layout (b2). | OPEN | agent | 🔒 q72 | [i88c-b2](#i88c-b2) | [i88c-b4](#i88c-b4) |
| <a id="i88c-b4"></a>**i88c-b4** | **i88c-b4 — real RST-8 TLS shot on Trinity hardware (hardware-gated)** — Exercise the composed TLS build over real RST 8 on the Trinity SAM. Gated on the b3 bootable disk + q72 (hardware-leg scope: live-internet TLS vs captured replay; what it fetches/stores). If the q72 answer makes the shot store via HSAVE, additionally gate on i357 (the free-record store-leg wedge, itself q71-gated) — add that edge at decision time. Emulation-first: no shot until the software legs are emulation-green and Pete signs off q72. | OPEN | agent | 🔒 q72 | [i88c-b3](#i88c-b3), q72 |  |
| <a id="i90"></a>**i90** | **%nobits/BSS section bodies dropped by -flatten — source-structure loss** — When source is flattened (-flatten, the release path), the entire body of every %nobits/BSS section (bss_kernel/bss_roms/…) is discarded: contributes zero bytes, so blank lines/comments inside BSS don't round-trip (release.s: 132 of 1472 blank lines, all in NOBITS sections). Pre-existing flatten property, not an i78 bug — i78 blank/comment preservation is exact for PROGBITS. Only matters once the SAM editor edits a flattened source with BSS content. Captured only — decide whether to preserve NOBITS editor content if/when the flattened-edit path needs it. | OPEN | agent | ⏳ i3 | i3 |  |
| <a id="i100c"></a>**i100c** | **Picker UX — user-facing revision/file-selection interface for the firmware downloader** — User-facing revision/file-selection interface for the firmware downloader. Interface design is Pete's call (editor-era UX, no autonomous decision here). No specific qN gates it — bare Pete decision. | OPEN | pete | 👤 pete |  |  |
| <a id="i101"></a>**i101** | **End-to-end deliverable: fetch RPi firmware → assemble spectrum4 release on SAM → TFTP-serve a full Pi 400 netboot** — The integrated SAM IDE on ONE bootable disk — editor + assembler/disassembler + TFTP server — an interactive USER-DRIVEN program (q23 resolved 2026-06-20: NOT a boot→fetch→assemble→serve auto-sequence). User loads it, starts/edits a project, assembles release.bin/kernel8.img from release.tbn (M6 byte-match), and TFTP-serves so a Pi 400 netboots — features exercised on demand. The three org-&8000 layers coexist via paging (implementation detail), not sequential overlays. NOT SimCoupé-testable (no Trinity ENC/SD); real E2E is Pete's hardware gate. Arch: docs/specs/phase3-delivery-design.md §13. | OPEN | agent | ⏳ i3 | i3 |  |
| <a id="i106"></a>**i106** | **SAM-as-terminal: SSH (or telnet) client to Pete's Mac over the physical LAN** — KEX-stub SSH the sweet spot: send constant X25519 basepoint as client ephemeral pubkey; use server's received pubkey as shared secret — zero EC math, instant connect, interoperates with stock OpenSSH sshd. Skip host-key verification (drops Ed25519+SHA-512). Per-byte: ChaCha20-Poly1305 ~few hundred B/s, usable for SAM MODE 3 64x24. Secure-mode toggle (real X25519, ~10-min) nearly free via existing i88 X25519. Telnet fallback: minimal SAM code but needs Homebrew inetutils on Mac. Reuses i88 crypto + i70 tcp_conn. | OPEN | agent | 🔒 q57 | q57 |  |
| <a id="i126"></a>**i126** | **One faithful SAM emulation layer used by every test — fix fragmented/inadequate emulation** — Root cause of repeated hardware-debug detours (i82 client_main; the 2026-06-23 yellow-screen): partial emulators, each modelling half the SAM. The flat Go netboot harness has Trinity seams (ENC/EEPROM/SD) + paging but NO screen/timing/sound/line-interrupts; SimCoupe is a faithful full SAM (real ROM, boots from address 0, screen, timing, sound, line-interrupt render) but has NO Trinity. So a routine that is a verbatim ROM copy can PASS the screenless Go harness and fail flat-yellow on hardware (the rainbow stripes are rendered by the live line-interrupt handlers, which the Go harness does not model). NORTH STAR (Pete 2026-06-23, feedback_comprehensive_emulation): ONE comprehensive emulation = fork SimCoupe (the most complete SAM) and PORT THE TRINITY MODELS IN (ENC28J60 + EEPROM + SD/B-DOS), so the single layer captures Trinity, network, storage, screen, timing, sound AND their interactions, boots from address 0 (i232), and is used by EVERY test. Then cross-subsystem surprises fail in emulation, not first on silicon, and the NETBOOT_HOSTTEST carve-outs (i231) are deleted. Stop the 'SimCoupe port is too much work' / 'merging the two emulators is overkill' shortcuts. Subsumes i124/i231/i232. | OPEN | agent | 🔒 q58 | q58 | [i232](#i232), [i254](#i254) |
| <a id="i143"></a>**i143** | **bdos_picker: hardware MODE-2 blit for picker_render (follow-up from i119d)** — picker_render in src/netboot/bdos_picker.asm is a stub ret (all decision/show-name/confirm logic is host-verified in i119d). This item wires the actual SAM MODE-2 screen blit so the picker display is visible on real hardware. Depends on i119d being merged. | OPEN | pete | 👤 pete | i119d |  |
| <a id="i168"></a>**i168** | **Future: import a full source file into the editor (fail-with-line-number on bad line)** — Future feature (q24, Pete 2026-06-20). The SAM editor accepts ONE LINE AT A TIME (parsed/validated on entry) — no whole-file import today. If/when import is added: on a bad line, fail and report the line number + the error; the user fixes the source and retries (Pete's preferred behaviour). Gated on the editor groundwork (depends i3) — a normal backlog item, not part of current editor specs. | OPEN | agent | ⏳ i3 | i3 |  |
| <a id="i232"></a>**i232** | **All Go-harness tests boot the SAM from address 0 (real ROM cold-init) for faithful machine state, not Call-with-seeded-state** — Pete (2026-06-23), foundational fidelity principle (part of i126): booting from address 0 with the real ROM IS what a SAM does — running a routine in isolation with hand-seeded registers/sysvars (e.g. a hand-seeded PALTAB) is not emulating the machine, it is testing a fragment in a made-up state. The Go library boots from reset in milliseconds (samboot_real_boot_test.go already does it: load rom.bin, run from PC=0, the ROM cold-init MNINIT->NEW2->streams populates PALTAB/STREAMS/FLAGS/etc.), so every harness test should boot from 0 first and then exercise the code in the REAL post-boot state. Deliverable: a reusable 'boot the SAM from reset, then run X' harness helper, adopted as the default across the koron-go tests (the netboot leaf tests currently Load+Call without booting). Big refactor; FUTURE — must not block the current bootblock RAM-test work. Captured per Pete's instruction to record + move on. | OPEN | agent | 🔒 q58 | [i126](#i126) |  |
| <a id="i89b"></a>**i89b** | **Capture a real Pi 3 netboot exchange + generate Pi 3 golden wire vectors** — Hardware-gated confirmation tail of i89. On Pete's Pi 3 + SAM with the hub+Wireshark rig, capture the DHCP DISCOVER/OFFER/ACK + first TFTP probe sequence and generate masked Pi 3 golden vectors (as the Pi 4 vectors were). Decisive open detail: whether the Pi 3 boot ROM is byte-strict about the option-43 'Raspberry Pi Boot' menu string (trailing spaces). Findings to confirm are in pi-netboot-capture-analysis.md section 4. | OPEN | pete | ⏳ i345 | [i345](#i345) |  |
| <a id="i123b"></a>**i123b** | **Perform the make->tup build migration (Pete-greenlit)** — Contingent on Pete's go-ahead per the i123a evaluation. Port the 167-target Makefile to per-dir Tupfiles + Tuprules.tup, reusing spectrum4's template. Order: (1) symbol-import cluster (enc_fix_payload->assembler->test_mem/test_cluster) first — it is the footgun; (2) netboot standalone binaries (parallel); (3) disk-pack + table-codegen edges. Keep SimCoupe/release-gate in ci.yml; byte-compare tup outputs vs make until parity; add the FUSE-privileged tup-under-docker CI launcher. owner:pete = the go/no-go decision is his; implementation is agent work once greenlit. | OPEN | pete | 👤 pete |  |  |
| <a id="i254"></a>**i254** | **Run ALL tests in CI: private binaries as repo secrets (SKIP_PRIVATE_TESTS default-true, our CI overrides) + add currently-excluded packages** — Pete (2026-06-25), follow-on to i253. GOAL: CI runs EVERY test; nothing silently excluded. (1) SKIP_PRIVATE_TESTS=true is the CI DEFAULT (forks get no secrets -> they skip the gated tests explicitly). (2) OUR repo's CI materialises the private binaries from encrypted GitHub repo secrets (rom.bin, eeprom.bin, B-DOS 1.5t -> the capture paths) and overrides SKIP_PRIVATE_TESTS=false so our CI runs them; GitHub withholds secrets from fork PRs. Pete is fine storing them as secrets. (3) ADD the currently-CI-excluded packages (z80-test-harness-go, zx0-greedy, editor-prototype) to CI, guarded by SKIP_PRIVATE_TESTS where they need private resources. TENSION RESOLVED (Pete 2026-06-25): the 'go harness is not a CI gate' rule predates Trinity emulation — SimCoupe was then strictly more complete so it gated CI; now the go harness emulates Trinity (ENC/SD/EEPROM) which SimCoupe does NOT, so gating Trinity-requiring tests on the go harness in CI is CORRECT (netboot-oracle/z80 already does). CONSOLIDATION: i126 (port Trinity INTO SimCoupe) -> once it lands CI can consolidate on SimCoupe. CAVEAT on 'private': the proprietary/non-redistributable status of Colin's binaries is a PRECAUTIONARY assumption by Pete, NOT confirmed fact — they may be freely redistributable. If confirmed redistributable, they could simply be committed/fetched and skip the secrets entirely (simpler). PETE-GATED: Pete creates the secrets. depends_on i253 + i126. Detail: docs/plans/i253-no-silent-skips.md. | OPEN | agent | 🔒 q58 | i253, [i126](#i126) |  |
| <a id="i264b"></a>**i264b** | **Record picker UI (Increment 2): list available records, select by number/name, persist the SAMBOOT Config chunk** — Record-picker UI (Increment 2 of i264). HOME DECISION (Pete 2026-07-01, q59): the picker lives in the WRITABLE EEPROM as its OWN chunk(s), extending the bootblock code -- NOT the ~250-byte config chunk (A), not B-DOS scratch (B), not a separate SD record (C). The ~250B limit was the CONFIG chunk only. VERIFIED against ~/sam-archive/samboot-capture/eeprom.bin: 128KB total, 88.3% blank flash (0xFF) = ~113KB FREE, ~15KB used (~12KB is B-DOS 1.5t in the low ~12KB; the chunk directory is up at ~122-131KB; the middle ~110KB is empty). Ample. On a key-held boot the bootblock loads+runs the picker chunk (as it loads the B-DOS chunks), reads the on-card record list via the raw SD driver, renders a BIOS-style menu by number+name, takes a selection, persists to the SAMBOOT Config chunk (write_chunk). Develop+emulate in netboot-oracle; EEPROM flash hardware-gated (i266/Pete); build in the private trinity-autoboot repo (q55). BIOS-menu UI design = a future sprint. | OPEN | agent | ready |  |  |
| <a id="i182b"></a>**i182b** | **Real-Trinity run of client_fetch_boot via the fetch-and-boot disk** — Hardware-gated completion of i182 (unverifiable in emulation). Boot the i182a fetch-and-boot disk on real SAM+Trinity, serve a stamped 819200-byte .mgt, confirm it streams into the scratch record (1600 HWSAD sectors), validates, and ALHK-boots it. Entangled with auto-boot/control-return autonomy (i132/i135). The full-size valid stream is the first full-scale fetch-and-boot (not driven through the bit-banged ENC in the host harness). Needs i182a's disk. | OPEN | agent | ready | i182a, i272, i295 |  |
| <a id="i215c"></a>**i215c** | **Spill manager: concrete pluggable backends (Trinity record / floppy-tape) + boot wiring** — Concrete SpillBackend implementations behind the i215a interface: Trinity SD-record store when present, else physical floppy/tape; selected at boot by capability detection. The assembler must run fully on a floppy/tape-only machine with NO Trinity (HARD constraint), so the backend is optional and degrades to refuse-with-message when absent. SimCoupe/boot integration. Depends on i215b. | OPEN | agent | 🔒 q60 | i215b, q60 |  |
| <a id="i284"></a>**i284** | **Push the CJ's Elephant playable disk (cjs-sam-remake boot_m2b.mgt) to the SAM over Trinity TFTP — the real-world end-to-end demo of the now-working SD write** — Real-world validation/demo (Pete): push the CJ's Elephant Antics playable disk (cj.mgt = ~/git/cjs-sam-remake boot_m2b.mgt, a SEPARATE Pete project, 819200 B) to a free Trinity SD record via the hardware-proven sd_push path (sd-push.py + src/netboot/sd_push.asm, incl. the i315 side-major reorder), then BOOT it on the SAM. Validation is SIZE-ONLY (i285): cj.mgt is accepted (no BDOS stamp; +232 is not a boot/select requirement - memory feedback_trinity_sd_write_settled_truths). Deps i289/i293/i295 all DONE. NOTE: a hardware-confirmed sd_push run already wrote record cjfixed and BOOTED CJ's Elephant on the real SAM (i295/i302, #784) - the mechanism is proven; this item is now the clean repeatable one-command push+boot demo/wrapper (drive boot via SAMBOOT autoboot i264b or a pushed boot-record). The .mgt-to-record ordering (D3, track-major vs side-major) is SOLVED (i294/i315) - do NOT re-list as open. Possibly-remaining: D1 (in-context HRECORD rst-8 crash) only if it recurs on the sd_push path. | OPEN | agent | ready | i289, i293, i295 |  |
| <a id="i231b-b4b"></a>**i231b-b4b** | **Eliminate the remaining carve-outs in netboot_serve.asm (11 sites / 1 file — the hard core)** — The last carve-out file after smoke_test.asm (b4a), csd_probe.asm (b4c), netboot_server.asm (b4d), netboot_dumper.asm (b4e), bdos_seam.asm (b4g), http_main.asm (b4i), with netboot_client.asm split out as [[i231b-b4h]] (blocked on a STAGING/high-address resolution). Live ledger 11 sites / 1 file: netboot_serve.asm — the (HOSTTEST==0)*(DUMPER==0) COMBINED gates (the serve loop's real-hardware body: EEPROM read, ENC init, the B-DOS record-serve + WRQ-store path via bdos_seam RST 8 hooks). This is the hard core: the gates are ANDed with NETBOOT_DUMPER==0 (the dumper shares the source), so collapsing needs untangling the serve-vs-dumper build split, and the serve WRQ-store path touches the B-DOS record hooks + the SD write path (watch [[i313]] high-address + the sensitive SD-write area, memory feedback_trinity_sd_write_settled_truths). Investigate the serve/dumper build variants + the combined-gate structure before collapsing. Nesting maxed at i231b-b4X: track completion as a NEW sibling leaf under i231b-b4. | OPEN | agent | ready |  |  |
| <a id="i291"></a>**i291** | **Measure the real ENC28J60/shared-PIC settle time on hardware (unattended ethernet-reporting bisection) to replace the conservative 1200-T-state emulator constant** — Follow-up to i289 (#755). The enc28j60.go sdInitSettling window is currently a conservative 1200-T-state constant (4x the documented ~50us) — a pure emulator-fidelity knob that never runs on real silicon, safely bounded so it catches the i242/i287 SD-before-ENC ordering bug without latching. This item MEASURES the real settle to make the model faithful rather than conservative, which also independently validates the shared-PIC settle model underpinning the §8ag serve-crash interpretation. Approach (Pete's, 2026-06-30): a trinload-pushed Z80 probe that does the &38 SD-init, waits N T-states, issues an identity probe, and reports stale-vs-fresh over ETHERNET (UDP markers, not screen — so it runs unattended; bisect N between a too-small lower bound and a too-large upper bound to converge on the real settle). Then set the emulator constant to the measured value (+ margin). owner agent: now self-serviceable (i266 TAPO power-cycle + i272 passwordless tcpdump). NOT required for correctness — purely a fidelity refinement. ALSO fold in (PR 820 §3 review residual, 2026-07-02): a window still OPEN at a run boundary carries its old-timeline ABSOLUTE deadline unclamped into the new run, so a run stopping within 1200T of its last SD byte could pin the entire next run's probes stale (the i327 artifact in a narrower shape); clamp on survival to t + (settleUntilT - tNow_old) — the remaining budget — which preserves both committed boundary pins (arithmetic checked in the PR 820 review). | OPEN | agent | ready |  |  |
| <a id="i297"></a>**i297** | **Investigate on-SAM RECORD-list hang after ~824 + DEF PROC error post-sd_push write** — After the 2026-06-30 sd_push HW write, an on-SAM RECORD listing hung after ~824 records + a DEF PROC not found BASIC error. Determine: card record-list state after the incomplete write, vs the RECORD command for many records, vs the SAM post-sd_push BASIC state (unclean exit). Relates to the sd_push-clean-exit item + the slowness. | OPEN | agent | ready |  |  |
| <a id="i304"></a>**i304** | **Reconstruct a labelled B-DOS 1.5t source from 1.5a + reassemble to byte-match** — Root-cause fix for the recurring 1.5a-vs-1.5t authority lure (Pete flagged it 2 sessions running; feedback_bdos_15t_not_15a). Start from reference/bdos/bdos15a.src.txt (the clean labelled 1.5a COMET source), identify the routines 1.5t changed (per bdos15t-beta6.annotated.dis: the SD init ladder, the 32-bit linear seek replacing hd.seek/set.chs/seek.base, the CSD/records math, record-base poke), splice the 1.5t versions in, and reassemble (pyz80) iterating until it produces the EXACT 1.5t binary (extracted/bdos15t-beta6.bin) -- the byte-match is the proof of correctness. Retain all 1.5a comments; finally annotate the new routines. Differences are minimal. Deliverable: a committed reference/bdos/bdos15t.src.txt (+ a make target asserting byte-identity) that becomes THE cited Trinity-HW B-DOS authority, and update the in-repo references/plan/code citations that currently point at 1.5a. NOTE: 1.5t is Colin's proprietary binary (kept out-of-repo, SKIP_PRIVATE_TESTS-gated) -- the reconstructed SOURCE is ours but the byte-match gate must guard against committing Colin's bytes; follow the existing private-artifact pattern (csd_decode_colin_test.go resolves the bin from ~/sam-archive). | OPEN | agent | ready |  |  |
| <a id="i305"></a>**i305** | **Retire superseded create-record implementations + stale branches (post #773)** — After PR #773 lands, remove the competing create-record scaffolding. STALE BRANCHES: i295-create-record (the #771 worktree at ~/git/sam-aarch64-771recover), origin/i293-sd-push, origin/i290-owncmd24-elim, origin/i194-own-cmd24-record-write, origin/fix-disk-record-validation-size-only, local i293-bdos-direct-pivot. IN-TREE dedupe: two record-write paths coexist -- sd_push.asm (own-LBA create-record, canonical) vs the serve WRQ path (netboot_serve.asm + raw_record_sink.asm + the bdos_seam HWSAD/HRECORD seam); decide canonical + remove or clearly scope the other. THIS OWNS the dead HWSAD/HRECORD seam still LINKED-but-unreachable in sd_push.bin via the wide bdos_seam.asm include (i303 scoping, 2026-07-01): NO seam routine is deletable (each is live in serve/client/http or test-driven -- incl. sd_push_faithful_test.go driving bdos_write_record/bdos_select_record against sd_push.bin), so the options are (a) gate the HWSAD/HRECORD block out of sd_push.bin behind a flag serve/client/http set but sd_push does not [requires retiring sd_push_faithful_test.go's HWSAD FACT-3/4 assertions first], or (b) leave it linked-dead (harmless, fits 16KB) -- agent's call when worked. Also review sd_push_faithful_test.go if it still asserts the old HWSAD path. | OPEN | agent | ready |  |  |
| <a id="i306"></a>**i306** | **L14 (record 14) first ~388 sectors corrupted by the pre-fix buggy push -- restore if needed** — The pre-fix sd_push used base=2438 (not B-DOS 2050), so cj 1600-sector write at LBA 21638-23237 spilled 388 sectors into B-DOS record 14 (catalogue L12/L14 region, body 22850-24449): LBA 22850-23237 overwritten. Pete data -- restore L14 from backup if it mattered. The base=2050 fix + the out-of-band write-guard (both in #773) prevent recurrence; future pushes only touch a genuinely free record. | OPEN | pete | 👤 pete |  |  |
| <a id="i311"></a>**i311** | **Retire the AttachBDOS Go mock behind a shrink-to-zero ledger, now the faithful B-DOS rig has landed** — i274 P2 (retire the mock-substitutes-for-real anti-pattern). The AttachBDOS Go mock (bdos_store.go BDOSStore / Machine.AttachBDOS) and the armed-dispatch double stand in for real B-DOS and HID the serve/WRQ write-path failure across the SD-write saga (i274 axis A, 'mock-substitutes-for-real'). The faithful rig now exists (sd_push_faithful_test.go: real Colin ROM + B-DOS 1.5t via LoadROMImage), so B-DOS behaviour can be exercised authentically. TASK: migrate every AttachBDOS-based test to the faithful rig, and put any remaining AttachBDOS usages behind a shrink-to-zero ledger (name->reason, modelled on tools/hosttest-carveout-allowlist.txt) that only ratchets DOWN, so no NEW mock-based B-DOS test can be added and the mock is provably retired when the ledger hits zero. The over-aggressive-settle negative-control gate (the other half of P2) is already i289 (DONE). Authority: CLAUDE.md rule 8; memory feedback_trinity_sd_write_settled_truths ('never use the AttachBDOS mock to test B-DOS behaviour — always real Z80 B-DOS in emulation'). May be partially gated on the faithful rig covering each AttachBDOS use case (assess coverage when worked). | OPEN | agent | ready |  |  |
| <a id="i231b-b4h"></a>**i231b-b4h** | **netboot_client.asm carve-outs eliminated (5 sites): collapse the flat HOSTTEST binary into the bootable; client_main/client_fetch_boot already harness-driven** — Eliminate the 5 NETBOOT_HOSTTEST==0 carve-outs in src/netboot/netboot_client.asm (boot entry :55; sink-mode selector :420; cl_sink_payload+raw_record_sink_leaf :448; the bootable body :754; the real-module includes bdos_picker/eeprom/raw_record_sink/sd_csd :1114). INVESTIGATED (see PR that recorded this): the naive collapse (delete the flat netboot_client.bin HOSTTEST build, retarget netboot_client_test.go at netboot_client_boot.bin, make the 5 blocks unconditional) does NOT land clean. Removing the carve-out directives ALONE is a boot-binary no-op (the ==0 blocks were already in the REAL_LISTREAD boot build; verified byte-identical). But retargeting the flat test REQUIRES enlarging STAGING: netboot_client_test.go streams up to 7240 bytes into STAGING via the client's own ldir (makeFile(cliBlksize*5+100), TestClientWindowedSession/GapRewind) — the flat build has STAGING=16384, the boot build 2048; a 2048 boot binary fails those tests (frame-builder code above STAGING gets overwritten). And enlarging STAGING in place relocates the SD routines above ~0xD000 into the [[i313]] harness high-address defect (boot tests then fail). Irreducible in-place conflict: flat needs STAGING>=7240; boot needs SD routines below ~0xCF00. TWO resolution paths: (A) LIGHTER — move the STAGING 'defs' to the END of the image (after all includes) so growing it extends into high DATA RAM without relocating any CODE; then STAGING=16384 works and the collapse proceeds (verify all boot tests + retargeted flat tests + the 32768 boot-fit-check). (B) fix i313 (the emulation-first path), then enlarge STAGING in place. Care points confirmed GREEN: the flat test never reaches the LISTREAD/csd_set_bd_records path (harness-hook vs REAL_LISTREAD CMD17 difference is moot); all 15 symbols it uses exist in netboot_client_boot.map (clean retarget). Makefile: delete the flat recipe + its .PHONY + netboot-z80-routines membership; netboot-client-disk already uses the boot bin. Fix netboot_client_test.go:49 stale 'make netboot-client' comment. Ledger 18->13 / 3->2 files. Recommend path A (no harness dep). | OPEN | agent | ready |  |  |
| <a id="i313"></a>**i313** | **netboot-oracle z80 harness mis-executes code relocated above ~0xD000 (blocks STAGING-enlarging / large-overlay collapses)** — Discovered during i231b-b4h (netboot_client carve-out collapse). When netboot binary code (the real SD re-blocking / ldir / 32-bit-division loops in raw_record_sink_leaf @ ~0xCF53+, bdos_read_sector, the sd_csd routines) is relocated ABOVE ~0xCF00-0xD000, the koron-go/z80 v0.10.2 harness mis-executes it: the CPU wild-jumps below the org (observed PC=&4E0D, 'raw_record_sink_leaf did not return after 5000000 steps') and/or decodes garbage ('no T-state timing for opcode DD FC at PC=&E16A'). Measured threshold (via real Make rebuild, boot tests = TestRawRecordSinkMatchesGo + TestClientFinalize*): raw_record_sink_leaf PASSES at 0xCB53 (STAGING=5120), FAILS at 0xCF53 (STAGING=6144) and above. pyz80 relocates correctly (every byte-diff is exactly a +high-byte delta on absolute operands; structurally identical code), and the CardModel HRSAD/HWSAD handlers are address-independent (bdos_store.go, keyed on PC==0x0008) — so the defect is in the harness's execution of high-address code, NOT the model or the assembly. Root-cause is UNCONFIRMED: could be a koron core decode/timing bug, a gap in the harness patch layer (see the INI/IND-style quirk already patched at harness.go:225-236 — a DD FC T-state entry may just be missing), or a subtler interaction. Task: root-cause + fix so high-address code runs faithfully (emulation-first, CLAUDE.md §7 'a gap is a defect to fix'). This unblocks the clean STAGING=16384 netboot_client collapse (i231b-b4h) and any future large section-D overlay. NOTE i231b-b4h has a LIGHTER alternative that dodges this bug (move the STAGING defs to the END of the image so growing it never relocates code) — so b4h is not hard-blocked on this, but fixing this is the emulation-first-correct path and de-risks all high-address netboot work. | OPEN | agent | ready |  |  |
| <a id="i314"></a>**i314** | **Reconcile the firmware-fetch record naming: Z80 storage_sink_leaf names records by hex(name[0:3]) but the Go authority bdos.SpanRecordName is content-addressed hex(hash[0:3])** — Discovered during i231b-b4i (#782 review). RULE-6 divergence in the hardware-gated HTTP firmware-fetch store path. The Go authority tools/netboot-oracle/bdos/span.go:52 SpanRecordName(blobHash [32]byte, index) returns a CONTENT-ADDRESSED name: fmt.Sprintf('%02x%02x%02x%03d', blobHash[0], blobHash[1], blobHash[2], index) — hex of the first 3 bytes of the blob's SHA-256 HASH + the zero-padded record index. But the Z80 src/netboot/http_main.asm storage_sink_leaf passes FW_STORE_NAME_PTR (the manifest NAME ptr set by store_begin), not a hash, to fw_span.asm fw_span_record_name — which hex-encodes the first 3 bytes of whatever HL points to. So the Z80 names records hex(NAME[0:3])+index, NOT hex(HASH[0:3])+index. fw_span.asm's own comment ('Mirrors bdos.SpanRecordName(blobHash...)', 'Emit hash[0], hash[1], hash[2]') says it should take a HASH — inconsistent with the caller passing the name. This is PRE-EXISTING (not introduced by #782; the bootable was byte-identical) and hardware-gated (the http_main store path is unverified on real hardware, CLAUDE.md §5), and #782's new tests accurately document the CURRENT name-based behavior. RECONCILE: (a) is the Z80 a port bug — storage_sink_leaf should pass a hash pointer (CONN_PINNED_HASH or the computed digest) to be content-addressed like the authority — OR (b) are the authority + fw_span.asm comment stale and name-based naming is the intended design? Check the serve side (netboot_serve reads 'the same convention' per http_main.asm) — does it expect name-hex or hash-hex? If the persist + serve sides agree (name-hex) the round-trip works but diverges from the authority; if they disagree, records can't be found. Per rule 6 the Go authority is the encoding truth unless there's a documented reason to diverge (like the plain-vs-suffixed SpanPlan choice). May escalate to a qN if the design intent needs Pete. Relates to i101 (serve record naming), the fw_span/http_main store leaf. | OPEN | agent | ready |  |  |
| <a id="i321"></a>**i321** | **Verify +232 'BDOS' stamp-patch parity across SD-write paths (sd_push patches; serve-WRQ + client_finalize size-only)** — sd_push.asm sp_data_write patches the 4-byte 'BDOS' stamp (+ record name @+210) into the record's sector-0 on write - the path that hardware-booted cj.mgt (i295/i302/i315). The serve-WRQ path (raw_record_sink -> bdos_write_record) and client_finalize validate SIZE-ONLY (819200) and do NOT patch the stamp. Per the file-vs-SD-record distinction (memory feedback_bdos_record_header_vs_disk_body, Pete 2026-07-01): a persisted Trinity SD record needs 'BDOS'@232 for B-DOS 1.5t (get.label) to accept it, else rep81 'Invalid record'. So a stampless .mgt (e.g. a FRED disk) pushed via the serve-WRQ or client route would land a record B-DOS rejects, whereas sd_push handles it. VERIFY: real gap (those routes should patch like sd_push) or intended (incoming .mgt always pre-stamped / those routes superseded)? Likely relates to i319 (disk push). If a real gap, port sd_push's sector-0 patch into raw_record_sink finalize + client_finalize. | OPEN | agent | ready |  |  |
| <a id="i117a"></a>**i117a** | **Prior-art survey: existing Claude Code autonomous-orchestration plugins/frameworks vs i117** — Preliminary 2026-06-18 scan: metaswarm, ccswarm, ruflo, claude-plan-orchestrator, autonomous-dev, closedloop-ai, barkain, Composio AO + Claude Code Agent Teams; plus awesome-agent-orchestrators, awesome-cli-coding-agents. Most emphasise PARALLELISM. i117's model differs: single serial orchestrator + deterministic non-LLM monitor (i97) + prime-directive culture + blameless retro + iN/qN registry backbone. Deliverable: feature-by-feature comparison to confirm differentiation and whether gap justifies building i117. Research-first; cite each tool. Pete's call; do not auto-pick-up. | OPEN | pete | 👤 pete |  |  |
| <a id="i325"></a>**i325** | **Optimise the assembler emit hot path (T-state tuning) -- measure candidates by total T-states to assemble release.s** — Lowest-priority future perf item (Pete 2026-07-01). After i24 lands option A (uniform per-byte LMPR-bracketed emit into a pool run), the emit hot path pays ~1/4 of its per-byte cost on the page-swap bracket and ~3/4 on register push/pop (ABI), zone/full checks, and OUT_PC/OUT_LEN bookkeeping. Candidates to evaluate: (a) a small staging buffer (8-128 bytes) in an always-paged region, flushed via ONE bracket + ldir per N bytes (amortises the page-swap); (b) restructure so the per-byte hot path is just store-to-buffer + pointer-bump, moving zone/page-run advance + OUT_LEN + the bracket into the per-flush path (potentially ~3x); (c) alternative-register / reduced-stack-op tuning of emit_byte. METHOD: measure EACH candidate by TOTAL T-states to assemble release.s (a representative full source); best T-states that STILL fits the memory budget wins. DO WHEN CODE IS FEATURE-COMPLETE (so the free-memory / staging-buffer budget is known). One-shot cost -> lowest priority. | OPEN | agent | ready |  |  |
| <a id="i326"></a>**i326** | **Widen the record-list seam BD_LIST_SECTOR to 16-bit (records >8160 unreachable on >6.7GB cards)** — BD_LIST_SECTOR (bdos_seam.asm) is a 1-byte list-sector input, so every record-list consumer (find-free scan, claim, free/delete, list_records) reaches only list sectors 1..255 = records 1..8160; on cards over ~6.7GB the higher records are invisible to the whole SD toolkit. list_records clamps + refuses ('E') past the limit (i322 review finding, PR 799) and its launcher warns -- honest but partial. Widen the seam input to 16-bit (bd_list_read_hw already builds a 32-bit LBA) and lift the clamps. Low urgency: the current card decodes to 4809 records. | OPEN | agent | ready |  |  |
| <a id="i347"></a>**i347** | **netboot_serve (i332 vessel) shares the EI'd-loop / ROM-IM1-chain exposure on unbounded serve runs** — Found during i95b-b1 stage 2: the vendored ENC driver EIs on every exit, so a record-booted serve program running its loop indefinitely gets ROM IM1 frame interrupts + the B-DOS frame hook against a frozen boot context, with shared-&DC SD/ENC traffic - netboot_server (i95b-b1) now arms an IM 2 takeover after its last B-DOS hook to own the vector for the serve phase; netboot_serve (the i332 TFTP-record vessel, record 145) has NO such takeover and its faithful tests only drive short windows, so the exposure is theoretical-but-real for long-lived serves on hardware. Assess and, if confirmed, port the IM 2 takeover pattern from netboot_server.asm to netboot_serve.asm. NOTE (2026-07-03 i70 pre-flight): http_main (the i70 fetcher) has the SAME exposure — di once at entry, no IM2 takeover, EI d by every ENC-driver exit and RST-8 HSAVE — and its full-manifest fetch runs 10-18+ min, more acute than the serve vessels; the i70c full fetch is gated on assessing this (seconds-long i70b smoke is low-risk). | OPEN | agent | ready |  |  |
| <a id="i348"></a>**i348** | **sd-push has no block retransmit: one lost UDP frame in a 1600-sector stream fails the whole push** — Observed twice back-to-back on the i95b-b1 vessel push (2026-07-02): the pusher windows 4 outstanding acks but an ack timeout only WARNs and moves on (sd-push.py stream loop), and sd_push counts RECEIVED blocks (sp_recv_count) with finalize requiring exactly 1600 - so a single lost '@' frame (or several) anywhere in an ~87 s stream yields finalize 'E' and the record must be deleted and re-pushed from scratch (3rd attempt succeeded). Fix needs BOTH sides: pusher retransmits un-acked sectors, and sd_push counts UNIQUE sectors (or an in-order watermark) so retransmitted duplicates (data landed, ack lost) do not overcount past 1600. Writes are LBA-addressed so duplicate rewrites are idempotent and safe. Until fixed, the workaround is delete + retry. | OPEN | agent | ready |  |  |
| <a id="i31b"></a>**i31b** | **On-SAM preprocessor — implementation (Z80 port of preprocess.go per the approved spec)** — Implement the on-SAM preprocessor per docs/specs/on-sam-preprocessor-design.md (spec APPROVED by Pete 2026-07-04, q70 closed): Go-authority-first bricks (1: .set table + .if/.else/.endif stack; 2: .macro/.endm + \param substitution + invocation with recursion guard; 3: .include + # line emission via a pluggable reader vector, real reader = UIFA+HGTHD+trampoline-HLOAD; 4: prep_run wired in front of b8d_chain_paged + corpus gate). Oracle gates: byte-equal vs host sam-aarch64 -E on ported preprocess_test.go fixtures + new preprocessor-bearing corpus fixtures (spectrum4 slice), then end-to-end text->.tbn vs CompactTBNBytes. Buffers from the i2 pool (new PP_PREP tag); own paged image (b8d two-image pattern). The expanded-text LEX_SRC ceiling lift is explicitly NOT this item (spec par.3). | OPEN | agent | ready |  |  |
| <a id="i352"></a>**i352** | **Local SimCoupé disk-boot broken on the Pi host (baseline 107 File-not-found at boot)** — Found 2026-07-03 during i39c: EVERY .mgt boot under local SimCoupé fails — the disk auto-boots, AUTO runs, and the boot loader's first HLOAD dies with SAM-screen '107 File not found, 30:1' — on a FRESH origin/main worktree build (baseline, pre-i39c) and with a factory-default SimCoupe.cfg, while the same commit's CI SimCoupé matrix is green. So local disk-boot confirmation (the 'SimCoupé locally before pushing' inner-loop step) is unavailable on this host; the Go harness remains the local loop and CI the gate. Evidence gathered: all 10 files present in the .mgt (samfile ls); ROM identical between /usr/local/share/simcoupe and the i62-local Resource dir (md5 ad08ed47...); default cfg (deleted + regenerated) reproduces; DISPLAY=:0 (labwc/Xwayland) + SDL_VIDEODRIVER=x11 + SDL_AUDIODRIVER=dummy is the working launch env (dummy video fails, offscreen emulates but same error; screenshots via grim). PRIME SUSPECT: local /usr/local/bin/simcoupe is a newer main-branch build (strings show 1.3.1, built Jun 13) vs CI's pinned v1.2.16 (0e8a69f, docs/notes/headless-simcoupe.md) — a boot/autoload behaviour change upstream would explain baseline-red/CI-green. NEXT ACTION: build+install the pinned v1.2.16 locally (the headless doc has the recipe) and re-run tools/run-simcoupe.sh on a make-disk test.mgt; if green, pin the local install and note the env (DISPLAY=:0, SDL vars) in the headless doc's local-host section. Note: ~/.simcoupe/SimCoupe.cfg was consumed/rewritten during diagnosis (pre-existing backup: SimCoupe.cfg.claude-backup-i24; today's: .claude-backup-i39c); Pete's outpath default may need restoring per feedback_agent_simcoupe_config_pollution. | OPEN | agent | ready |  |  |
| <a id="i353"></a>**i353** | **b8d corpus fixture pinning the packed-header length-escape EMIT branch (15+-byte patch expr)** — PR 837 §3-review residual (non-blocking note recorded on the PR): the Z80 packed patch-header DECODE escape branch (insn_patch_header, len nibble 15 -> real u8) is CI-proven via the release gate (the release .tbn carries 4 escaped patches and the SAM leg byte-matches), but the Z80 EMIT escape branch (compact_emit.asm cemit_ai_hdr_esc) has no pinned fixture — no b8d corpus source produces a 15+-byte patch expression, so that branch's only proof is code review. Add a corpus fixture (e.g. a symbol-diff expression long enough to exceed 14 bytes of expr bytecode) under tests/*/sources/ so TestChainPagedB8dCorpus byte-compares the emit escape path against host CompactTBNBytes. | OPEN | agent | ready |  |  |
| <a id="i354"></a>**i354** | **Delete the gated synthetic list-hook machinery (BD_HOOK_LISTREAD/LISTWRITE 161/162 + oracle cases)** — i70e follow-up hygiene: after PR 838, NOTHING uses the synthetic RST-8 list hooks — every image builds with NETBOOT_REAL_LISTREAD (raw CMD17/CMD24) and no test sets AllowSyntheticListHooks. Delete the dead machinery end-to-end: the NETBOOT_SYNTHETIC_LIST_HOOKS branches + BD_HOOK_LISTREAD/LISTWRITE equs in src/netboot/bdos_seam.asm (the scan-cluster guard then collapses to "if defined(NETBOOT_REAL_LISTREAD)"); the oracle bdHookListRead/bdHookListWrite cases, AllowSyntheticListHooks, requireSyntheticListHooks, the hook-read usedRecords overlay (the SPI mirror via MarkRecordListUsed supersedes it — check whether usedRecords itself is then write-only and deletable), applyListWrite/ListWrite/ListWrites() (sole remaining consumer: netboot_serve_terminate_test len==0 assert — re-point it at the SD model captured writes), and CardModel.ListSector if its last consumer goes. Keep CardModel itself (HRSAD/HSAVE still use it). ALSO sweep the two stale comments the PR-838 review flagged (recorded on the PR): sd_csd.asm:323-327 ("production serve + http_main use the RST 8 hook" — false; that block collapses with the fork) and the "With BD_RECORDS=10 ... usedRecords overlay" comment inside TestProvStoreDemarcation (~line 176). Pure deletion + test re-pointing + comment sweep; no behaviour change. | OPEN | agent | ready |  |  |
| <a id="i358"></a>**i358** | **Clean up working-note narrative in test_erratum843419.asm Test 2 comment block** — The Test 2 comment block in src/test_erratum843419.asm (from the original i27c commit 872e52e4, NOT the fix) carries working-note narrative with wrong intermediate values ('0x1fff8040', 'something is wrong', 'let me recompute properly'). The final conclusion + the test assertions are correct (0x10,0x40,0x80,0xFF = 0x10FF8040), but the narrative violates the present-tense/no-history comment rule. Trivial: rewrite the comment to state the correct ADR re-encode derivation in present tense. Flagged by the #846 §3 re-review as a non-blocking YELLOW. LOW priority (comment-only, no behaviour change). | OPEN | agent | ready |  |  |
| <a id="i360"></a>**i360** | **http store leaf: fit the record-write cluster + record-list claim into the debug/full 6-file http builds, and claim the record so multi-file fetch doesn't re-pick** — i357/#847 landed store_format_record (free-record pre-format before HRECORD/HSAVE) but ONLY in the smoke build (32741/32768). Two residuals: (1) BUDGET — the ~512 B raw-CMD24 record-write cluster (NETBOOT_WANT_RECORD_WRITE) does not fit the debug (marker) build or the full 6-file netboot_http_boot in the 32 KB boot window, so the marker-instrumented free-record re-shoot (i70b diagnosis path) and the full firmware fetch's store leaf still lack the fix (they wedge on a free record). Needs a byte-budget lift: leaner cluster, smaller/paged manifest, or a section-D overlay for the write code. (2) CLAIM — http_main omits bdos_claim_record (the record-LIST name write, +~200 B), so a second file's store_begin (bdos_find_free_record) RE-PICKS the same record (selection mismatch: find_free tests the list-entry blank, our format sets only the +232 dir stamp), overwriting the first file. Single-file smoke is unaffected; multi-file fetch (the full build) needs the claim OR a bdos_find_free_record that also rejects a dir-stamped record. Both are only reachable once the budget is lifted (the full build is the multi-file one). Not blocking i70b (its re-shoot uses the plain smoke build's border-colour outcome). | OPEN | agent | ready |  |  |
| <a id="i361"></a>**i361** | **Local incremental build: TestBootRecordAssemblerRecordVessel fails on stale build/ (enctab.enc vs vessel out of sync); CI-green** — On the Pi host, TestBootRecordAssemblerRecordVessel (the i319b-b2 gate) fails locally: 'enctab.enc page 4 bytes differ ... got &40 want &45 — the boot's HLOAD from the record did not deliver it'. It FAILS on clean origin/main (proven by a stash test during i357) and is UNRELATED to any source change — a stale/inconsistent local build/ dir. CI is green (the netboot-oracle job clean-builds), so this is local-only, i352-class. Partial diagnosis (i357 session): make enctab regenerated build/enctab.enc (it was itself stale); make assembler said 'nothing to do'; make disk-record rebuilt build/test_record.mgt but the vessel STILL delivered the old enctab bytes — so the vessel's embedded enctab did not track a regenerated build/enctab.enc. Likely an incremental-build dependency gap: the assembler/vessel pipeline does not mark itself stale when enctab.enc changes. NEXT: confirm whether a clean rebuild clears it (=> pure cruft, document the clean-rebuild fix), or whether a Makefile prereq is genuinely missing (=> add the dep). Low priority; saves the next local session the re-diagnosis this cost. | OPEN | agent | ready |  |  |

## Tree view — umbrella hierarchy

- i3 — `OPEN` — Editor groundwork (Phase 2) — full vision
  - [i43](#i43) — `OPEN` — Editor: register simulator with user-chosen seeds + replay-on-edit
  - [i44](#i44) — `OPEN` — Editor: inline instruction-explanation panel (prose semantics, flags, regs, bit-field layout)
  - [i45](#i45) — `OPEN` — Editor: 'did you mean a simpler instruction?' result-equivalent rewrite hint
  - [i46](#i46) — `OPEN` — Editor: system-register documentation panel (ARM MRA subset + editorial overlay)
  - [i47](#i47) — `OPEN` — Editor: inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations)
- i31 — `OPEN` — On-SAM preprocessor (.if build-constraints + macros)
  - [i31b](#i31b) — `OPEN` — On-SAM preprocessor — implementation (Z80 port of preprocess.go per the approved spec)
- i41 — `OPEN` — Editor edit-buffer data structure — insertion performance for large source
  - [i41e](#i41e) — `OPEN` — Editor edit-model — i48-IR record payload + document symbol table + hybrid record model
- i70 — `OPEN` — Phase-3 firmware self-provisioning: SAM fetches Pi firmware blobs via HTTP and persists to Trinity storage
  - [i70b](#i70b) — `OPEN` — HTTP-fetch smoke hardware shot: EEPROM read + section-C staging + HSAVE RST-8 verified on real Trinity (single small file from a LAN server)
  - [i70c](#i70c) — `OPEN` — Directory-safe multi-blob persist + full 6-file cdn manifest on hardware (start.elf-scale spanning)
- i81 — `OPEN` — Upstream SimCoupé issue triage — review open issues and assist where we can
  - [i81a](#i81a) — `OPEN` — PNG-endian one-line upstream SimCoupé PR: the __aarch64__ little-endian-guard fix in Base/SimCoupe.h
  - [i81c](#i81c) — `OPEN` — Optionally offer the SDL dummy-driver headless renderer fallback upstream
- i88 — `OPEN` — On-SAM HTTPS/TLS-1.3 client to fetch Pi firmware directly from GitHub
  - i88c — `OPEN` — i88 6b — hardware-gated integration: wire tls_reasm into tcp_conn, paging layout, bootable disk, real RST 8 on Trinity
    - [i88c-b1](#i88c-b1) — `OPEN` — i88c-b1 — wire tls_reasm into CONN_SINK_FILTER: sink+emit shims, bounded emulation integration test
    - [i88c-b2](#i88c-b2) — `OPEN` — i88c-b2 — grow TC_RX 1056->16645 + finalise paged buffer layout for full-size TLS records
    - [i88c-b3](#i88c-b3) — `OPEN` — i88c-b3 — bootable TLS disk: tls_main.asm as NETBOOT_STREAM boot binary + make target + build-disk wiring
    - [i88c-b4](#i88c-b4) — `OPEN` — i88c-b4 — real RST-8 TLS shot on Trinity hardware (hardware-gated)
- i89 — `OPEN` — Capture a Pi 3 netboot exchange (Phase-3) — extend i83/i86 oracle to the older Pi family
  - [i89b](#i89b) — `OPEN` — Capture a real Pi 3 netboot exchange + generate Pi 3 golden wire vectors
- i95 — `OPEN` — Phase-3 netboot integrated server — single dispatch loop (ARP+DHCP+TFTP)
  - i95b — `OPEN` — End-to-end Pi boot on real Trinity — hardware-gated: DHCP + B-DOS provisioning + real Pi boot
    - [i95b-b2](#i95b-b2) — `OPEN` — Real Pi netboots from the SAM end-to-end (rig-gated): DHCP + TFTP boot of the physical target Pi
- i100 — `OPEN` — Firmware-download provisioning UX — user-selectable revision and file subset
  - [i100c](#i100c) — `OPEN` — Picker UX — user-facing revision/file-selection interface for the firmware downloader
- i117 — `OPEN` — Extract sam-aarch64 learnings into a reusable open-source autonomous Claude project framework
  - [i117a](#i117a) — `OPEN` — Prior-art survey: existing Claude Code autonomous-orchestration plugins/frameworks vs i117
- i118 — `OPEN` — i82 TFTP client — OACK handling / standard-TFTP-server interop
  - [i118b](#i118b) — `OPEN` — hardware verification — OACK path on real Trinity hardware
- i119 — `OPEN` — i82 TFTP client — persistence write-out on real hardware
  - [i143](#i143) — `OPEN` — bdos_picker: hardware MODE-2 blit for picker_render (follow-up from i119d)
- i123 — `OPEN` — Investigate migrating the build from make to tup
  - [i123b](#i123b) — `OPEN` — Perform the make->tup build migration (Pete-greenlit)
- i182 — `OPEN` — Bootable fetch-and-boot disk + real-Trinity run of client_fetch_boot
  - [i182b](#i182b) — `OPEN` — Real-Trinity run of client_fetch_boot via the fetch-and-boot disk
- i215 — `OPEN` — IDE page-persistence (spill): lazy-spill cold DOC blocks to Trinity or floppy/tape when the pool is full
  - [i215c](#i215c) — `OPEN` — Spill manager: concrete pluggable backends (Trinity record / floppy-tape) + boot wiring
- i231 — `OPEN` — Systemically eliminate NETBOOT_HOSTTEST==0 emulation carve-outs + CI guard against reintroduction (emulation-first enforcement)
  - i231b — `OPEN` — Systematically eliminate the 34 existing NETBOOT_HOSTTEST==0 carve-outs (ratchet the i231a allowlist to zero)
    - i231b-b4 — `OPEN` — Eliminate the harder carve-outs (post-i231b first pass), per file/cluster
      - [i231b-b4b](#i231b-b4b) — `OPEN` — Eliminate the remaining carve-outs in netboot_serve.asm (11 sites / 1 file — the hard core)
      - [i231b-b4h](#i231b-b4h) — `OPEN` — netboot_client.asm carve-outs eliminated (5 sites): collapse the flat HOSTTEST binary into the bootable; client_main/client_fetch_boot already harness-driven
- i264 — `OPEN` — SAMBOOT selector firmware: hold-key-at-boot launches a record picker (choose boot record by number/name) instead of hardcoded record 3
  - [i264b](#i264b) — `OPEN` — Record picker UI (Increment 2): list available records, select by number/name, persist the SAMBOOT Config chunk
- i359 — `OPEN` — Disk-image catalogue + store all built .mgt images as Trinity records (with in-repo record->disk mapping)
  - [i359b](#i359b) — `OPEN` — Store (sd_push) + boot-verify every built .mgt disk to a Trinity record; fill the record->disk mapping
