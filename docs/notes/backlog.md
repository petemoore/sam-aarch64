<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/priority.yaml + registry/items.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Backlog — priority queue

The curated order of open pullable work items (leaves / childless items;
umbrellas are spanned by their leaves). Highest priority first. The order is
stored in `registry/priority.yaml` and validated in CI as a strict permutation
of all open pullable items + a topological extension of the dependency DAG
(nothing ranked before an item it depends on).

**Agents pull from the top.** The `registry ready` command returns the unblocked
tip — items whose dependencies are all satisfied — in this priority order. Pull
from the first `ready` item to ensure the highest-priority unblocked work is
always picked up.

**Re-rank with:**

```
registry prioritize --id iN --to-top
registry move --id iN --before iM
registry move --id iN --after  iM
```

This view supersedes the prose "proposed sequence" in `docs/ROADMAP.md`.
Source of truth: `registry/priority.yaml`. Spec: `docs/specs/registry-source-of-truth-design.md §"Priority queue + ready"`.

<!-- The table below is generated — do not edit it by hand. -->


| rank | **id** | item | status | deps | dependents |
|---|---|---|---|---|---|
| 1 | **i119d** | **B4 — record-selection UX: list / manual-pick / auto-pick-free + show-name + overwrite-confirm** — B4: the user-facing record picker — enumerate records (using B3's list-read), show names, let the user manually pick or auto-select first free, then display the chosen record's name (or 'free/unnamed') and require confirmation before any write. Needs the i138 keyboard-sysvar stub to exercise the keypress flow in emulation. | IN_PROGRESS | i138 |  |
| 2 | **i119e** | **B5 — wire into client_main (replace record-0=floppy bug) + full emulation E2E; HSAVE write-out hardware-gated** — B5: integrate B3 list-read + B4 record picker into client_main, replacing the record-0=floppy selection bug with the correct HRECORD record-n>=1 -> HSAVE flow. Run a full E2E test in emulation (harness CardModel + BDOS write-out model + i138 key injection). Also resolve the filename >10-char UIFA-name truncation (i119 gap 4). The HSAVE write-out to real hardware and the raw-SD list read stay hardware-gated. | OPEN |  |  |
| 3 | **i141** | **B3h — raw SD CMD17 card-absolute list-sector read on ports &DC-&DF (hardware gate for B3)** — Hardware implementation of the card-absolute list-sector read modelled in B3 by the harness hook BD_HOOK_LISTREAD. Implements the SPI command ladder (CMD17 single-block read) on Trinity ports &DC-&DF to read list sectors 1..base-1 directly off the SD card. Not required for B3 emulation; the harness models the returned bytes. Needs final confirmation of the exact SPI init/read sequence on real hardware before building (docs/specs/trinity-record-detection-design.md §8, open-verification-item-1 and §3 frozen-interface principle). | OPEN |  |  |
| 4 | **i121** | **SAM TFTP server — WRQ (write / accept-in) support: push disk images to the SAM, auto-slotted** — Pete's PRIMARY netboot use case: boot SAM, 'tftp put' a disk image from the Mac, the SAM writes it to the next free Trinity record, then boot+test immediately. A standard TFTP server handles both RRQ (serve-out) and WRQ (accept-in), so this combines with the existing i83/i95 serve-out server into one program. Reuses the i82 receive loop (DATA accumulate + ACK) + i119 storage write-out; adds WRQ parsing + server-side OACK/ACK-0 (the i83/i95 server already builds OACKs for serve-out) + auto-slot allocation (i114 manifest / highest-free strategy). | OPEN |  |  |
| 5 | **i122** | **SAM fetch-and-boot (PXE-style): TFTP-client-fetch a disk image and boot it directly** — SAM acts like a netbooting Pi: on boot, TFTP-fetches a named disk image, writes to a scratch/boot record, and boots into it (no control return). Design notes: (a) use reusable scratch record (no cleanup post-boot); (b) sha256-dedup before fetch — if content hash already in storage, skip fetch and boot existing copy (idempotent). Reuses i114/q26 content-addressed record naming. Builds on i82 receive loop + i119 write-out + a boot-a-record primitive (Trinity RECORD-then-BOOT model). | OPEN |  |  |
| 6 | **i118b** | **hardware verification — OACK path on real Trinity hardware** — Confirm the OACK path works on real Trinity hardware: netboot client connects to a standards-compliant TFTP server (e.g. macOS tftpd), receives OACK, ACKs block 0, and receives the file. Gated on Trinity hardware access + a standards-compliant server in the test environment. | OPEN |  |  |
| 7 | **i124b** | **emulation completeness — PHY link-up model, SD/RST-8 stub, delete NETBOOT_HOSTTEST carve-outs** — Three remaining gaps in the emulation layer: (1) model PHY link-up (PHSTAT2.LSTAT + drop-TX-while-down) so the pre-link ARP drop is reproducible in emulation; (2) SD/RST-8 stub so full client fetch runs harness-end-to-end; (3) fold server/serve/http wrappers into the harness and delete NETBOOT_HOSTTEST carve-outs (→ i126). Together these make the emulator a complete, trusted gate for every netboot path. | OPEN |  |  |
| 8 | **i93b** | **B-DOS RST 8 hook dispatch on real Trinity — hardware-gated (HGTHD/HLOAD/HSAVE/HRECORD)** | OPEN |  | i95b |
| 9 | **i95b** | **End-to-end Pi boot on real Trinity — hardware-gated: DHCP + B-DOS provisioning + real Pi boot** | OPEN | i93b |  |
| 10 | **i133** | **Self-service autonomous hardware testing via trinload** — With trinload on SAM (recs 3/128) and control returning between programs (i132 confirms mechanism), run unattended experiments: push a program via trinload3.py, it runs + RETs, push the next. Feedback: (a) sniff the wire (tcpdump); (b) network result channel — test program writes results over the net so agent debugs real-hardware unattended. Needs RET-not-halt program variants (re-inits ENC at start, RETs at end, emits results over net) + i132 pre-flight. A crash needs Pete reboot until i135 (auto-boot + Tapo) closes the gap. | OPEN |  |  |
| 11 | **i135** | **Auto-boot a Trinity record on power-up — unsupervised end-to-end hardware automation** — Pete has a Tapo smart plug for programmatic power-cycling. If a record auto-boots on power-on (no manual BOOT), agent runs fully unsupervised: power-cycle → SAM boots chosen record → runs → results over net (i133) → repeat. Investigate: EEPROM/config default-boot-record setting, Trinity AutoBOOT ROM (Colin's patch loads B-DOS from 128K EEPROM at reset), or patch forked B-DOS for early boot-a-record hook. Research: ~/sam-archive/bdos/analysis, Trinity AutoBOOT ROM, EEPROM layout, ~/sam-archive/trinity-docs. Pairs with i133 for full unsupervised loop. | OPEN |  |  |
| 12 | **i2** | **On-SAM IDE memory model (edit buffer + IN/OUT paging; claim all free RAM, grow on demand)** — Scope row for the IDE memory model: the edit buffer and IN/OUT paging strategy. The design claim-all-free-RAM-grow-on-demand is the paged-buffer ceiling framing; full design deferred to editor work. | OPEN |  |  |
| 13 | **i4** | **Basic read-only listing/scroll viewer (centre-locked cursor; up/down only)** — SAM-side read-only viewer ported from the host prototype. The i76 P1 host viewer is the functional authority this SAM implementation will port. Carried forward by i76 Phase-1. | OPEN |  |  |
| 14 | **i5** | **UI visual prototyping via image generation (MODE 3 64x24 vs MODE 4 32x24 mockups)** — Mockup tooling for editor UI at SAM-accurate resolution. Carried forward by the i76 mockup backend. q1 resolved (2026-06-12): use a programmatic SAM-faithful renderer (PNG + SCREEN$ at exact mode geometry, real palette, correct pixel aspect) — generic AI image gen rejected; hand-authored retro art deferred to post-port. | OPEN |  |  |
| 15 | **i6** | **SAM screen-mode decision (MODE 3 vs 4, or user preference)** — Decide the default screen mode for the editor. To be settled empirically via i76 (geometry flag + font sheets + P3 memo). Design in editor-tui-prototype-design.md §3/§5 and ROADMAP Editor vision. | OPEN |  |  |
| 16 | **i12b** | **Editor-testing input injection (-keyin vs FLAGS/LASTK memory injection)** — New 2026-06-08. Determine and implement the right input-injection mechanism for automated editor tests in SimCoupé: the -keyin flag approach vs direct FLAGS/LASTK memory injection. | OPEN |  |  |
| 17 | **i16** | **Replace cls test instruction with a real spectrum4 one** — The cls instruction used in the test fixture is synthetic/placeholder; replace it with a real spectrum4 instruction. Tracked in the ROADMAP deferred checklist. (Note: id was tentatively i13 in conversation but locked as i16 to match shipped PR #107.) | OPEN |  |  |
| 18 | **i19** | **Subagent worktree-isolation leak — harden** — Harden subagent worktree isolation to prevent one subagent's filesystem changes from leaking into another's checkout. Scope row — specifics TBD. | OPEN |  |  |
| 19 | **i20** | **Linker-layout coupling (spectrum4.ld hardcoded in flatten)** — The flatten step has spectrum4.ld hardcoded; decouple the linker layout so other projects can use the toolchain without baking spectrum4-specific layout assumptions. Scope row. | OPEN |  |  |
| 20 | **i21** | **Go-harness fidelity follow-ups: make harness-sweep, USAGE.md, memory watchpoint** — Harness-sweep (one-shot build-all-deps + go test -count=1 ./...) and USAGE.md (full-suite run, stale-payload/test-cache gotchas, boot-self-test authoring patterns) DONE in PR #341. Remaining: memory watchpoint inspection feature — deferred until a debugging session needs it (agent-owned; add ad hoc per the README Evolving the harness). | OPEN |  |  |
| 21 | **i22** | **Per-fail-site diagnostic strings** — Add per-fail-site diagnostic strings to test failures so failures are more easily identified without a disassembler. Deferred-backlog item. | OPEN |  |  |
| 22 | **i23** | **Paged-IN HLOAD-ceiling lift (current ceiling: 96 KB = 6 pages 7..12, src/assembler.asm:49)** — The current IN-file ceiling is 96 KB (6 pages, 7–12, hardcoded src/assembler.asm line 49). Lift this ceiling to support larger source files via additional paged HLOAD slots. Deferred-backlog item. | OPEN |  |  |
| 23 | **i24** | **16-bit OUT_LEN / 32 KB-output limit (OUT_LEN: defw 0 src/main_loop.asm; 32768-byte OUT ceiling src/encoder.asm)** — The output length field is 16-bit and the output buffer ceiling is 32768 bytes (src/encoder.asm). Lift the OUT_LEN field and ceiling to support assemblies larger than 32 KB. Deferred-backlog item. | OPEN |  |  |
| 24 | **i25** | **(hksp) HSAVE/HLOAD error handler** — Add a proper error handler for HSAVE/HLOAD failures invoked via (hksp); currently errors are not surfaced to the user. Deferred-backlog item. | OPEN |  |  |
| 25 | **i27** | **Cortex-A53 errata workarounds (--fix-cortex-a53-*)** — Implement or wire errata workarounds for the Cortex-A53 (--fix-cortex-a53-* flags). Deferred-backlog item. | OPEN |  |  |
| 26 | **i29** | **Harden slot self-test PASS_PC dependency** — Deferred-backlog item. Hardens the slot self-test PASS_PC dependency to make it more robust. | OPEN |  |  |
| 27 | **i30** | **LDIR-fan-out for cross-page-shared blocks (smaller binary + faster boot)** — Deferred/possible future enhancement. Using LDIR fan-out for blocks shared across pages could reduce binary size and speed up boot. Noted in the strand-B PR4 disassembler plan under 'Future enhancements'. | OPEN |  |  |
| 28 | **i31** | **On-SAM preprocessor (.if build-constraints + macros)** — Beyond-M7 / future idea. On-SAM preprocessor supporting .if build-constraints and macro expansion, reducing reliance on the host-side preprocess step. | OPEN |  |  |
| 29 | **i32** | **Multiple source files + staged/partial loading** — Beyond-M7 / future idea. Support for assembling from multiple source files with staged or partial loading, enabling larger projects than currently fit in a single-file workflow. | OPEN |  |  |
| 30 | **i33** | **Trinity mass-storage: leverage B-DOS record seam for staged/overlaid assembly (i23/i24 ceiling lift)** — Use the proven Trinity B-DOS storage seam (src/netboot/bdos_seam.asm; spanning records in src/netboot/fw_span.asm) for staged/overlaid assembly, lifting the i23 96 KB IN / i24 32 KB OUT ceilings. Storage-access half proven by netboot work. Assembler still uses plain HSAVE/HLOAD for single-file IN→OUT; this item extends that to mass-storage-backed staging. Rescoped 2026-06-17: netboot proved records are accessible + writable; remaining deliverable is the assembler-scale integration. | OPEN |  |  |
| 31 | **i39c** | **Phase 3 — bitfield-packing polish on the overlay slot bytes** — Bitfield-packing polish on the overlay slot bytes (design §3.1). Migrated to M9 at M8 close (2026-06-12). Folds into / absorbed into the i48c overlay-encoder work, specifically i48c-b7 (comments/blank-runs + overlay slot bitfield-packing brick). | OPEN |  |  |
| 32 | **i41d** | **Editor edit-model — Z80 port of the paged block-list (on-SAM)** — Faithful Z80 port of the i41a Go authority (tools/sam-aarch64/editmodel), per CLAUDE.md §6. Scoped in design §8.1. Bricks 1+3+4a COMPLETE: insert/split/loc/goto (PR #387), delete/merge/free-list (PR #389), bounded ring-journal undo/redo (PR #391), EMDL serialize/load (PR #392), src/editmodel.asm, host-verified vs oracle. REMAINING: Brick 2 (paging onto i2 pool via OUT (251) — SimCoupé-gated), Brick 4b (real v2 .tbn path — couples to assembler build, deferred). | OPEN |  |  |
| 33 | **i41e** | **Editor edit-model — i48-IR record payload + document symbol table + hybrid record model** — Replace i41a's raw-text line payload with the i48 in-memory symbolic IR (parsed format.Record), giving the edit-model an incremental document-global symbol table + name-id/record-id side-tables (§3), a hybrid committed-parsed / active-raw / invalid-error record type (§7.3), and a per-line text→IR entry path. Genuinely-open design — not a mechanical port: symbol-table ownership/lifecycle, the hybrid sum type, per-line parse semantics, and partial/invalid-document serialize (q24). Needs q24 answer + a design pass before building (design §8/§5.2/§6/§7.3). | OPEN | q24 |  |
| 34 | **i42** | **CRC / signature in the .tbn file header (integrity check)** — Deferred since M1. The v1 header has magic/version/flags but no integrity field. Add a CRC or signature field to the .tbn header for integrity checking. Referenced in the M1 status 'Known gaps' list. | OPEN |  |  |
| 35 | **i43** | **Editor: register simulator with user-chosen seeds + replay-on-edit** — Phase-2 editor sub-feature. Register simulator with user-chosen seeds: step instructions, inject/randomise source regs, show dest regs/flags/memory, and replay-on-edit. Described in docs/ROADMAP.md 'Editor vision'. | OPEN |  |  |
| 36 | **i44** | **Editor: inline instruction-explanation panel (prose semantics, flags, regs, bit-field layout)** — Phase-2 editor sub-feature. Inline instruction-explanation panel showing prose semantics, flags set, regs read/written, and bit-field layout. Described in docs/ROADMAP.md 'Editor vision'. | OPEN |  |  |
| 37 | **i45** | **Editor: 'did you mean a simpler instruction?' result-equivalent rewrite hint** — Phase-2 editor sub-feature. Suggests result-equivalent but simpler instruction rewrites when a more concise encoding exists. Described in docs/ROADMAP.md 'Editor vision'. | OPEN |  |  |
| 38 | **i46** | **Editor: system-register documentation panel (ARM MRA subset + editorial overlay)** — Phase-2 editor sub-feature. System-register documentation panel surfaced from the cursor line, embedding an ARM MRA subset with editorial overlay. Described in docs/ROADMAP.md 'Editor vision'. | OPEN |  |  |
| 39 | **i47** | **Editor: inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations)** — Phase-2 editor sub-features. 'Why is this instruction here?' inverse-flow navigation plus retro UI affordances including chiptune audio, period fonts, and animations. Described in docs/ROADMAP.md 'Editor vision'. | OPEN |  |  |
| 40 | **i48c-b5b** | **movl pseudo (movz+movk expansion / :abs_g0_nc:+:abs_g1:)** — Implement the movl pseudo-instruction: expands to movz+movk pair; handles :abs_g0_nc: and :abs_g1: reloc operands. | OPEN |  |  |
| 41 | **i48c-b5c** | **Barriers dsb/dmb/isb (barrier-arg keyword→CRm)** — Parse barrier instructions dsb/dmb/isb: map barrier-arg keyword (e.g. sy, ish, nsh, ...) to the CRm encoding field. | OPEN |  |  |
| 42 | **i48c-b5d** | **mrs/msr — sysreg/PSTATE name→OpSysName** — Parse mrs/msr instructions: map sysreg and PSTATE register name strings to the OpSysName encoding (name lookup table). | OPEN |  |  |
| 43 | **i48c-b5e** | **dc/tlbi — op-name + optional Xt** — Parse dc/tlbi instructions: map operation-name string to encoding + accept optional Xt register operand. | OPEN |  |  |
| 44 | **i48c-b5f** | **ldr = literal-pool pseudo (OpLitPool)** — Parse ldr= pseudo-instruction: captures the pool-value expression into an OpLitPool operand for later literal-pool placement and PC-relative load emission. | OPEN |  |  |
| 45 | **i48c-b6** | **Directives parser** — Parse assembler directives (.set, .balign, .org, .skip, .ltorg, .inst, .section, etc.) into their respective overlay record types via src/asmparse.asm. | OPEN |  |  |
| 46 | **i48c-b7** | **Comments/blank-runs + i39c overlay slot bitfield-packing** — Parse comment and blank-run record types. Absorbs i39c: implement the overlay slot bitfield-packing polish (design §3.1) as part of the parser completion. | OPEN |  |  |
| 47 | **i48c-b8** | **Integration/fixture round-trip (assembler-coupled, lands last)** — Wire the completed text→overlay encoder into the full assembler path and verify end-to-end fixture round-trips. Assembler-coupled (like i41d Brick 4b); depends on B1–B7 being complete. Lands last in the i48c sequence. | OPEN |  |  |
| 48 | **i50** | **Host Go-tool CLI shape — deferred design question (one binary vs three vs pandoc-style)** — Deferred design question (Pete 2026-06-09): whether host tools stay one format-aware front door (sam-aarch64, auto-detects SA64 magic), revert to three binaries (encode/decode/translate), or adopt pandoc-style --from/--to. Three concerns are already cleanly separated as libraries (aarch64enc / aarch64dec / sam-aarch64-format+frontend); CLI count is a thin presentation choice, orthogonal to the i48 in-memory-only-symbolic-form win. Revisit when bulk GNU→.tbn import and listings host features land. Go tools are Z80-port scaffolding + oracle (memory/project_go_tools_are_z80_scaffolding.md). | OPEN |  |  |
| 49 | **i55** | **Multi-section / linker-script honouring in refenc** — Explicitly punted in favour of the text2bin -flatten approach. Multi-section / linker-script handling in refenc would only be needed if a non-spectrum4-shaped project surfaces. Deferred; from ROADMAP deferred checklist (retired in i52 PR 3). | OPEN |  |  |
| 50 | **i56** | **SpectrumFourLayout extraction (-layout flag or linker-script parser)** — Not worth doing unless a second project surfaces. SpectrumFourLayout extraction via -layout flag or linker-script parser is a Pete-gated call; deferred. From ROADMAP deferred checklist (retired in i52 PR 3). | OPEN |  |  |
| 51 | **i65** | **SAM-side save-without-comments option (editor phase)** — On-SAM counterpart to the host -strip-comments/-thin-comments flags. Saves a .tbn with the editor region stripped/thinned so a 256 KB SAM can assemble sources authored on a 512 KB machine. q9 resolved (Pete 2026-06-10): the tool targets both 256 and 512 KB SAMs; the full unstripped release .tbn is allowed to exceed 256 KB, but a 256 KB user must still be able to assemble via this stripped disk handoff. Host equivalent already exists. | OPEN |  |  |
| 52 | **i66** | **Trinity page-persistence as a general persist-to-mass-storage technique** — Persist SAM pages to Trinity B-DOS records as a general storage technique. Deferred by Pete's decision (2026-06-10, q9 resolved): 'no value implementing right now'; revisit only if a concrete need appears. Conditional on i66's reactivation being Pete's call. | OPEN |  |  |
| 53 | **i69** | **Test-memory architecture: relieve section-C test-variant squeeze using test-coexistence principle** — Relieve section-C test-variant squeeze (principle: tests need not coexist in memory). Levers in order: (1) evict suites off-axis (PR #52 pattern); (2) serial test bay (page-13 time-multiplex generalised); (3) chunked payloads across more pages/disk; (4) multiple boots. Lever 1 partially applied (PR #180): test_symbols + test_local_labels evicted; test variant code_end &BFAA→&BCA3 (86B→861B headroom). Remaining levers ready for next pressure event. | OPEN |  |  |
| 54 | **i70** | **Phase-3 firmware self-provisioning: SAM fetches Pi firmware blobs via HTTP and persists to Trinity storage** — SAM IP stack fetches Pi firmware (several MB) from cdn.githubraw.com over HTTP (q11 resolved: on-SAM HTTP via uIP, host-tool-free; q12 resolved: Model A only). Full host-verifiable HTTP-fetch path complete + host-verified across Bricks 1–6 (#245/#247/#254/#256/#257/#259 netboot_http.asm). Remaining: hardware-gated (EEPROM read + section-C paging + HSAVE RST-8 dispatch on real Trinity; emulation-verified != hardware-verified, CLAUDE.md §5). i82 TFTP client bridges provisioning meanwhile; HTTPS stretch is i88. | OPEN |  |  |
| 55 | **i73** | **i17 review residuals — LOW-severity hardening and dead-code findings** — Low-severity hardening items split from i17 (PR #185). Landed: L1 emit_byte off-by-one (PR #338), L6 .align>=16 silent no-op (PR #339), L7 pass-1 backward-.org check missing (PR #340), L9 retired symbolic-encoder path removed (PR #371, q20 sweep). Open: L3 symbol_abs_bit_ptr missing id<512 bound; L4 exactly-full-buffer conservative rejections; L5 zero-length LIT_INSTS BC underflow; L10 u8 segment-counter wrap; L11 load_in_file editor-offset wrap; L12 .set redefinition divergence. Follow-up i109 (vestigial soft-reject hook). | OPEN |  |  |
| 56 | **i74** | **i7 phase D — at/ic/barrier operand-table codegen from the Go authority** — Codegen the at/ic/barrier operand tables from the Go authority (spec §6 Q3). Deferred by sequencing only (Pete 2026-06-12: tracked so it automatically gets done); queue after i7 phases A–C land. Gated on the aarch64dec/sys.go code→data refactor (i7 phase D prerequisite). | OPEN |  |  |
| 57 | **i77** | **spectrum4 source succinctification — comments + pathological constant expressions** — Agent-driven, section-by-section, human-reviewed (Pete 2026-06-12): (a) comments rewritten aggressively succinct but still readable; (b) pathological constant expressions replaced with named equates (e.g. the longest instruction in the corpus: `movk w0, (65536 \| (((BORDER_COLOUR >> 16) & 65535) & 65535)), lsl #16`). Coordinate with upstream spectrum4 (content originates there; release.s re-flattens). Ties to editor-tui spec D1 (all comments <256B → u16 record length can revert to u8). Parked (editor era). | OPEN |  |  |
| 58 | **i81a** | **PNG-endian one-line upstream SimCoupé PR: the __aarch64__ little-endian-guard fix in Base/SimCoupe.h** — hunk 2 of tools/font-proof/simcoupe-local-capture.patch; confirmed NOT in upstream HEAD; ready to offer standalone; community submission is Pete's call. | OPEN |  |  |
| 59 | **i81c** | **Optionally offer the SDL dummy-driver headless renderer fallback upstream** — Lower priority than i81a; community submission is Pete's call. | OPEN |  |  |
| 60 | **i87** | **Dump + reverse-engineer Pete's patched SAM ROM (Phase-3)** — Pete's SAM runs a modified system ROM giving Trinity-interface access at power-on (no B-DOS floppy needed). When his SAM is next online he dumps the ROM to disk; we then diff it against the stock SAM ROM to document exactly how Trinity-at-boot is patched in, so the boot mechanism is reproducible/understood. Not a blocker — trinload can net-load the client/server in the interim, and trinity.mgt + reference/bdos cover the interface. Hardware-gated: artifact production needs Pete's hardware. | OPEN |  | i112 |
| 61 | **i88c** | **i88 6b — hardware-gated integration: wire tls_reasm into tcp_conn, paging layout, bootable disk, real RST 8 on Trinity** — Wire tls_reasm_feed into tcp_conn CONN_SINK_FILTER; finalise paging layout; build a bootable disk; exercise over real RST 8 on Trinity hardware. Prerequisite: TC_RX must grow from 1056 to 16645 B (TLS max) before a real Certificate record fits. Hardware-gated (CLAUDE.md §5). | OPEN |  |  |
| 62 | **i89** | **Capture a Pi 3 netboot exchange (Phase-3) — extend i83/i86 oracle to the older Pi family** — Confirm Pi 3 boot ROM's DHCP behaviour (may differ from Pi 4 PXE option-43 dance) and its TFTP file set (bootcode.bin/start.elf/fixup.dat). Pete runs on real hardware (hub + Wireshark, trinload technique) when convenient — not a blocker (Pi 3 file set already known from his tftproot); agent provides setup steps at the time. | OPEN |  |  |
| 63 | **i90** | **%nobits/BSS section bodies dropped by -flatten — source-structure loss** — When source is flattened (-flatten, the release path), the entire body of every %nobits/BSS section (bss_kernel/bss_roms/…) is discarded: contributes zero bytes, so blank lines/comments inside BSS don't round-trip (release.s: 132 of 1472 blank lines, all in NOBITS sections). Pre-existing flatten property, not an i78 bug — i78 blank/comment preservation is exact for PROGBITS. Only matters once the SAM editor edits a flattened source with BSS content. Captured only — decide whether to preserve NOBITS editor content if/when the flattened-edit path needs it. | OPEN |  |  |
| 64 | **i100c** | **Picker UX — user-facing revision/file-selection interface for the firmware downloader** — User-facing revision/file-selection interface for the firmware downloader. Interface design is Pete's call (editor-era UX, no autonomous decision here). No specific qN gates it — bare Pete decision. | OPEN |  |  |
| 65 | **i101** | **End-to-end deliverable: fetch RPi firmware → assemble spectrum4 release on SAM → TFTP-serve a full Pi 400 netboot** — Single bootable SAM disk: (1) fetch firmware from cdn.githubraw.com (SHA-256-verified, i100 downloader) → Trinity storage; (2) assemble release.bin/kernel8.img on SAM from release.tbn (M6 byte-match); (3) DHCP/TFTP netboot serve so Pi 400 netboots. New work = sequential overlay orchestration (fetch-once→assemble-once→serve-daily via Trinity B-DOS). NOT SimCoupé-testable (no Trinity ENC/SD in emulator); real E2E is Pete's hardware gate. Architecture: docs/specs/phase3-delivery-design.md §13. q23 decision (orchestration UX) shapes sub-decomposition. | OPEN | q23 |  |
| 66 | **i102s** | **i102 sha256 message-schedule extend loop — BCDE accumulate in add4_body (~2% gain)** — The message-schedule extend loop still does 3 term adds memory-to-memory (add4_body). Applying the BCDE register-accumulate pattern used in the round path to add4_body eliminates the write-back round-trips, netting an estimated ~2% further reduction. One clean lever remaining after i102r. Low priority (world-best already, no deadline). | OPEN |  |  |
| 67 | **i103** | **Autonomous-loop monitor — activity-gated quiescence** — When non-Pete backlog is drained, the monitor keeps waking an idle session. Fix: agent writes a quiescence semaphore on confirming a drain; monitor stops nudging while the file exists AND session transcript has not grown past the recorded mark. The instant the transcript grows (Pete writes back or any new turn) the monitor deletes the file and resumes. Auto-expires on first sign of life — no manual restart, no permanently-deaf loop. Record the transcript mark when monitor first sees the file (not file mtime) to avoid spurious re-wake. Builds on i97. | OPEN |  |  |
| 68 | **i104** | **Per-session retro reports** — At wind-down each session writes a short blameless retrospective to ~/claude-retro/ on genuine process/workflow friction, written independently (not reading other sessions' reports first) so converging signal across sessions is real, not echoed. Rule: say what genuinely happened; 'nothing notable' is a valid report; each friction grounded in a specific incident. Reviewed by hand every few sessions / at milestone close -> recurring frictions become actionable changes (CLAUDE.md / memory / qN). Distinct from the diary (~/claude-diary/). Practice started 2026-06-15. | OPEN |  |  |
| 69 | **i106** | **SAM-as-terminal: SSH (or telnet) client to Pete's Mac over the physical LAN** — KEX-stub SSH the sweet spot: send constant X25519 basepoint as client ephemeral pubkey; use server's received pubkey as shared secret — zero EC math, instant connect, interoperates with stock OpenSSH sshd. Skip host-key verification (drops Ed25519+SHA-512). Per-byte: ChaCha20-Poly1305 ~few hundred B/s, usable for SAM MODE 3 64x24. Secure-mode toggle (real X25519, ~10-min) nearly free via existing i88 X25519. Telnet fallback: minimal SAM code but needs Homebrew inetutils on Mac. Reuses i88 crypto + i70 tcp_conn. | OPEN |  |  |
| 70 | **i109** | **Collapse the now-unarmed encode_logical_imm soft-reject hook** — PR #371 (q20/i73-L9 sweep) deleted encode_logical_imm_try, the only caller that ever armed encode_logical_imm_soft; with no armer, encode_logical_imm_reject always jp fails. The soft branch + encode_logical_imm_soft/encode_logical_imm_rejected flags are vestigial. Collapsing back to a plain jp fail + dropping the two equates is behaviourally identical (flag never set) -> release.bin byte-identical. LOW priority (touches v2-production code). The hook may be re-armed by the future i48c editor encoder — decide when i48c's encoder is designed. | OPEN |  |  |
| 71 | **i111** | **Memory-read coverage / generic dead-code finder** — Instrument the koron-go/z80 harness to record every byte read (opcode fetch + data load) while running the whole test corpus, then report bytes inside a build's code/data image never touched. Each unread region is a candidate: either genuinely dead (delete, reclaim code window) or a coverage gap (add a fixture). Phase 1 (flat payloads, paging-free): hook mac.Read/opcode-fetch + use .map symbol ranges. Phase 2 (paged binary): trace must record (physical page, offset) and hook the paging port. Exclude known-acceptable-unread ranges via MARKER SYMBOLS from the pyz80 .map — drift-proof. | OPEN |  |  |
| 72 | **i112** | **Restore the SAM opening screen that Colin's Trinity boot-ROM patch tramples** — Colin's custom ROM boots straight from Trinity (no B-DOS floppy) but overwrites the normal power-on opening screen. Restore by appending code to end of B-DOS (Trinity flash) that redraws the opening screen. The screen is not a clean re-callable subroutine — it is the inline RAINBOW SCREEN block at ROM &ED1B: set MODE 3, init PALTAB, build LINICOLS rainbow table, print MGT banner. Restoration = reproduce that draw in appended Z80, not call a routine. Touches non-redistributable B-DOS 1.5t Trinity fork; modifying Colin's B-DOS may warrant his input (Pete's call). | OPEN | i87 |  |
| 73 | **i113b** | **photo-verify flagged data + fold verified facts into trinity-capabilities.md** — QA report flags specific data points (port addresses, EEPROM offsets, hex values, diagrams) that require photo confirmation before committing to the repo. Deliverable: run vision-verify on flagged items, then fold only confirmed facts into docs/notes/trinity-capabilities.md via the established off-repo pipeline. | OPEN |  |  |
| 74 | **i114** | **Netboot serve manifest + record-allocation strategy** — A manifest on the booted record maps TFTP names/paths -> B-DOS records, decoupling the served namespace from B-DOS 10-char flat filename limits. Entries carry {locator(s), span count, size, optional SHA-256}. Storage-allocation strategy in manifest header (first-free / highest-free / fixed-list; never silently steal a record). Multi-project use with shared-files-by-reference. Sub-choices settled: encoding=text, locator key=number+name, span naming=hash-derived, default=highest-free. Storage-class: trinity-sam-disks/ prefix = validated raw-record disk. Design at §6.5. | OPEN |  |  |
| 75 | **i116** | **Harness measurement / byte-match tests must assert the build succeeded — no stale-.bin reads** — A failed pyz80/make build leaves the previous .bin in place (or a timestamp-keyed target no-ops after a source restore), so a T-state or byte-identical check reads the stale artifact and reports a false pass. Bit the project 4 sessions: sha256-fastest-z80, i88-tls-go-authority-arc, i48c-expr-parser-bricks, and a timestamp-keyed variant. Fix is structural (the i21 USAGE.md doc note did not take): make harness measurement/oracle tests depend on the .bin build target (or assert build exit-0 / a freshness check) so they cannot run against a stale artifact. | OPEN |  |  |
| 76 | **i117** | **Extract sam-aarch64 learnings into a reusable open-source autonomous Claude project framework** — Starter framework/template so a new project inherits: autonomous-loop monitor (i97), split iN/qN registries + controlled-status discipline (i115 validator), §3 pre-merge review gate, per-session retro (i104), prime-directive verification ethos, brick + Go-authority-oracle loop, delegate-to-subagents context discipline, and doc-lifecycle + single-source-of-truth rules. Must be self-contained (portable out of Pete's ~/.claude/CLAUDE.md). Delivery options: template repo / init scaffold or Claude Code plugin. Scope + sequencing + packaging are Pete's call; do not auto-pick-up. | OPEN |  |  |
| 77 | **i117a** | **Prior-art survey: existing Claude Code autonomous-orchestration plugins/frameworks vs i117** — Preliminary 2026-06-18 scan: metaswarm, ccswarm, ruflo, claude-plan-orchestrator, autonomous-dev, closedloop-ai, barkain, Composio AO + Claude Code Agent Teams; plus awesome-agent-orchestrators, awesome-cli-coding-agents. Most emphasise PARALLELISM. i117's model differs: single serial orchestrator + deterministic non-LLM monitor (i97) + prime-directive culture + blameless retro + iN/qN registry backbone. Deliverable: feature-by-feature comparison to confirm differentiation and whether gap justifies building i117. Research-first; cite each tool. Pete's call; do not auto-pick-up. | OPEN |  |  |
| 78 | **i120** | **Windowed TFTP (RFC 7440) for the netboot client/server — throughput optimization** — i118 drops windowsize from the client RRQ to stay a correct lock-step (one ACK per block) client. That costs round-trips on large transfers (an 800 KB .mgt at blksize 1428 ~574 lock-step round-trips over the SAM's slow ~20-80 KB/s SPI stack). Windowed delivery (window 4 -> ~4x fewer round-trips) is the throughput win. Requires the receiver to ACK only the last block of each window (+ handle lost-block/out-of-window rewind) on both client (i82) and server (i83/i95); re-add windowsize to ClientOptionSet only once windowed logic exists on both sides + is host-verified. | OPEN |  |  |
| 79 | **i123** | **Investigate migrating the build from make to tup** — tup tracks the full dependency graph and rebuilds exactly what is stale (and removes orphaned outputs), structurally preventing the dirty-build/stale-artifact class — same class i116 patches test-by-test; tup would make stale-.bin reads impossible by construction. Pete's ~/git/spectrum4 uses tup ('amazing'). Scope: evaluate tup against current Makefile (pyz80 .asm→.bin, Go builds, disk-image assembly, CI jobs), then if it wins, plan the migration. | OPEN |  |  |
| 80 | **i126** | **One faithful SAM emulation layer used by every test — fix fragmented/inadequate emulation** — Root cause of i82 hardware-debug detour: three partial emulators — flat 64 KB netboot harness (no paging/ROM/EEPROM), full assembler harness (LMPR/HMPR but no ROM1-at-&C000-at-boot), SimCoupé (faithful SAM, no Trinity). Deliverable: consolidate to one layer that (a) models ROM1 read-only at &C000 at boot, (b) carries Trinity seams (ENC28J60 + EEPROM + SD/B-DOS), (c) used by every test, (d) runs netboot boot wrappers end-to-end so NETBOOT_HOSTTEST carve-outs are deleted. Reuses harness.go paging. Subsumes i124 netboot slice. | OPEN |  |  |
| 81 | **i137** | **build-disk netboot ceiling check doesn't distinguish section class (i125 follow-up)** — tools/build-disk/main.go rejects netboot image only if it loads past &10000 (≤32768 bytes). A section-C bootable (smoke/serve/server/client) must fit &8000-&BFFF (≤16384); above that it loads into ROM1 at boot and crashes. Mostly masked: the per-program make fit-check (i125, 16384 for section-C) fires first, so an over-section-C binary never reaches build-disk normally. Gap only reachable by calling build-disk -netboot directly on an over-section-C image. Fix needs a -netboot-section-c flag threaded through disk targets. Low priority (masked by i125 gate). | OPEN |  |  |
| 82 | **i142** | **make registry should rebuild build/registry before mutations — stale-binary footgun** — The build/registry binary is a built artifact; running 'build/registry set-status/add' with a STALE binary (not rebuilt after a tool change) misbehaves silently — it caused the i138 DONE-but-in-priority inconsistency (a pre-#462 binary skipped the reconcilePriority step, stranding i138 in the queue). Fix: make 'registry-gen' a prerequisite of 'make registry' and the mutation paths (or have the CLI self-rebuild / a wrapper), so an agent cannot operate the registry with a stale binary. | OPEN |  |  |
