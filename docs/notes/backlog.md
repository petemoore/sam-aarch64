<!--
  GENERATED by `registry gen` (tools/registry) — DO NOT EDIT BY HAND.
  Source of truth: registry/priority.yaml + registry/items.yaml
  Regenerate:      make registry
  Validated in CI by the `registry-sync` job. Hand edits FAIL CI.
-->

# Backlog — priority queue

The curated order of open pullable work items (leaves / childless items;
umbrellas are spanned by their leaves). Highest priority first. The order is
stored in `registry/priority.yaml` and validated in CI as a strict permutation
of all open pullable items + a topological extension of the dependency DAG
(nothing ranked before an item it depends on).

**Agents pull from the top.** The `registry ready` command returns the unblocked
tip — items whose dependencies are all satisfied — in this priority order. Pull
from the first `ready` item to ensure the highest-priority unblocked work is
always picked up.

**Re-rank with:**

```
registry prioritize --id iN --to-top
registry move --id iN --before iM
registry move --id iN --after  iM
```

This view supersedes the prose "proposed sequence" in `docs/ROADMAP.md`.
Source of truth: `registry/priority.yaml`. Spec: `docs/specs/registry-source-of-truth-design.md §"Priority queue + ready"`.

<!-- The table below is generated — do not edit it by hand. -->


| **id** | item | status | owner | gate | deps | dependents |
|---|---|---|---|---|---|---|
| **i41e** | **Editor edit-model — i48-IR record payload + document symbol table + hybrid record model** — Replace i41a's raw-text line payload with the i48 in-memory symbolic IR (parsed format.Record): a document-global symbol table + name-id/record-id side-tables (§3), a hybrid committed-parsed / active-raw / invalid-error record type (§7.3), per-line text→IR entry. q24 resolved (2026-06-20): the editor validates ONE LINE AT A TIME — no whole-document import to serialize invalidly; the design point is how an in-progress/invalid LINE is held + round-tripped (whole-file import = i165). Open design (symbol-table lifecycle, hybrid sum type, per-line parse) — needs a design pass (§8/§5.2/§6/§7.3). | OPEN | agent | 🔒 q51 | i2a, q51 |  |
| **i43** | **Editor: register simulator with user-chosen seeds + replay-on-edit** — Phase-2 editor sub-feature. Register simulator with user-chosen seeds: step instructions, inject/randomise source regs, show dest regs/flags/memory, and replay-on-edit. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 | i41 |  |
| **i44** | **Editor: inline instruction-explanation panel (prose semantics, flags, regs, bit-field layout)** — Phase-2 editor sub-feature. Inline instruction-explanation panel showing prose semantics, flags set, regs read/written, and bit-field layout. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 | i41 |  |
| **i45** | **Editor: 'did you mean a simpler instruction?' result-equivalent rewrite hint** — Phase-2 editor sub-feature. Suggests result-equivalent but simpler instruction rewrites when a more concise encoding exists. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 | i41 |  |
| **i46** | **Editor: system-register documentation panel (ARM MRA subset + editorial overlay)** — Phase-2 editor sub-feature. System-register documentation panel surfaced from the cursor line, embedding an ARM MRA subset with editorial overlay. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 | i41 |  |
| **i47** | **Editor: inverse-flow navigation + retro UI affordances (chiptune, period fonts, animations)** — Phase-2 editor sub-features. 'Why is this instruction here?' inverse-flow navigation plus retro UI affordances including chiptune audio, period fonts, and animations. Described in docs/ROADMAP.md 'Editor vision'. | OPEN | agent | ⏳ i41 | i41 |  |
| **i229** | **Build + emulation-verify the combined patched-bootblock image (Colin original + samboot_inject spliced, <=1KB chunk-1)** — TOP PRIORITY next session, gated on Pete being present (culminates in a hardware stripes test, i230, that needs Pete at the SAM). Stage 1 of the i135c no-brick plan. Build the patched bootblock with a WORKING boot screen and emulation-verify it. The full implementation recipe (from the 2026-06-23 ROM research) is in docs/plans/i229-combined-bootblock.md: the rainbow stripes are rendered LIVE by the ROM line-interrupt ISRs (FRAMINT/LINEINT), not by the &ED1B data write — so the routine must establish IM1+EI + arm STATPORT, clear paper+border to CLUT index 0 (LINEINT drives CLUT reg 0 per scan line; border is a CLUT-index lookup so both track it), build LINICOLS (the verbatim &ED1B port already in samboot_stripes), set the print position via CLSLOWER &06B5 (channel K — fixes the top-left banner), print the banner (UTMSG &3DB0 + the &0F7F é/size tail), then EI, wait-for-key, teardown (LINICOLS[0]=&FF), and RETURN CLEANLY (never di;halt — trinload's try_exec pushed its own start, so a clean RET restarts trinload; the prior yellow-screen run hung because the di;halt path / wrong paging executed). STATE 2026-06-23: a stripes+banner DEMO (mgt_screen_demo, the &ED1B DATA write only) is committed + emulation-verified but it showed FLAT YELLOW on hardware precisely because it omits the interrupt setup — that is the gap this item closes. Also assemble the full chunk-1 image (Colin's proprietary bootblock from the local eeprom.bin + the inject, via a splice tool, NOT committed bytes) and reset-chain-verify (samboot_real_boot_test.go boots the real rom.bin from address 0). Build with fresh context; do not rush boot code (prime directive). | OPEN | agent | 🔒 q50 | q50, i239 | i230 |
| **i230** | **RAM-test the patched bootblock on real hardware (inject + stripes + ALHK auto-boot) before any EEPROM flash** — Stage 2 of the i135c no-brick plan. HARDWARE — GATED ON PETE BEING PRESENT (no set-owner cmd, so this is the gate: the agent must NOT run this when Pete is away; it needs Pete at the SAM to reload trinload + watch the screen). Push the combined patched-bootblock (or the boot-screen demo) to RAM via trinload and confirm on the real SAM: the rainbow stripes + copyright banner RENDER (the i229 recipe makes them work — the bare-data demo showed flat yellow), the config-fetch + record-boot / no-boot decision works, and it returns cleanly to trinload (clean RET, never di;halt). The stripes MUST work here BEFORE i135c patches the EEPROM (the i135c->i230 dep enforces it). Nothing here touches the boot EEPROM. Depends i229. | OPEN | agent | 🔒 q50 | i229 | i135c |
| **i135c** | **SAMBOOT: flash the patched EEPROM with backup/restore and confirm unattended auto-boot (answers the crux)** — Flash the prototyped patched EEPROM to Pete's Trinity via write_chunk, keeping the captured EEPROM as the restore backup; power-cycle and confirm unattended auto-boot of the configured record. Hardware (Pete). | OPEN | pete | 🔒 q50 | i135d, i87a-b1, i87b, i197c, i221, i226, i230 |  |
| **i133** | **Self-service autonomous hardware testing via trinload** — With trinload on SAM (recs 3/128) + control returning between programs (i132), run unattended experiments: push a program via trinload3.py, it runs+RETs, push the next; feedback via tcpdump + a net result channel. PRIORITIZED to top (Pete 2026-06-20): the crux is whether we can BOOTSTRAP our own code from EEPROM / Trinity storage WITHOUT a human typing BOOT or picking a record — if a power-cycled SAM auto-starts a service that boots a pushed image, hardware testing goes fully autonomous (no Pete). The sooner we build it, the sooner we know. Auto-boot = i135; i172 audits dependents. | OPEN | agent | ⏳ i135 | i135, i121a, i122 | i118b, i143, i152, i70, i88c, i93b |
| **i177** | **Publish trinboot: open-source EEPROM boot-config patcher (Python-over-trinload + bootable .mgt) for Trinity users** — Release the SAMBOOT EEPROM boot-config + stripes-restore patch as a standalone open-source tool for any Trinity owner: a Python program sent over trinload that patches the EEPROM, or a bootable .mgt. Gives Trinity users the configurable default-boot-record + opening-stripes feature for free. NOT a requirement for SAMBOOT -- depends on it (publication only). | OPEN | agent | ⏳ i135 | i135, i176 |  |
| **i93b** | **B-DOS RST 8 hook dispatch on real Trinity — hardware-gated (HGTHD/HLOAD/HSAVE/HRECORD)** | OPEN | agent | ⏳ i133 | i133 | i95b |
| **i95b** | **End-to-end Pi boot on real Trinity — hardware-gated: DHCP + B-DOS provisioning + real Pi boot** | OPEN | agent | ⏳ i93b | i93b |  |
| **i118b** | **hardware verification — OACK path on real Trinity hardware** — Confirm the OACK path works on real Trinity hardware: netboot client connects to a standards-compliant TFTP server (e.g. macOS tftpd), receives OACK, ACKs block 0, and receives the file. Gated on Trinity hardware access + a standards-compliant server in the test environment. | OPEN | agent | ⏳ i133 | i133 |  |
| **i24** | **16-bit OUT_LEN / 32 KB-output limit (OUT_LEN: defw 0 src/main_loop.asm; 32768-byte OUT ceiling src/encoder.asm)** — The output length field is 16-bit and the output buffer ceiling is 32768 bytes (src/encoder.asm). Lift the OUT_LEN field and ceiling to support assemblies larger than 32 KB. Deferred-backlog item. | OPEN | agent | 🔒 q45 | i2a, i2b, q45, i218 | i33 |
| **i234** | **Section-C test-variant budget relief for the encode_inst+overlay self-tests (&C000 cliff)** — Free section-C space so the i204b overlay_classify/literal_word/compact_inst routines (~1300 B) + their boot self-test can land inline. These routines call encode_inst (reads ENCTAB in section A) so they CANNOT be evicted off-axis — the established lever-1 (evict-to-cluster, PR #52/#180) and commoning passes (i205/i206/i218) are EXHAUSTED for ENCTAB-coupled code and recover only tens of bytes (the test variant has ~11 B headroom vs a 1300 B need). Remaining viable levers (registry i69/i205 analysis): (2) serial test bay / a SECOND BUILD_TESTS boot variant (a new -D flag + make target + harness boot test + CI job) that runs the encode/overlay suite in a boot WITHOUT the other inline test suites, time-multiplexing memory ('tests need not coexist'); (3) chunked payloads; (4) the i2/q36 dynamic page-pool memory model (the long-term fix that lifts the whole section-C/scratch contention). Pick a lever (Pete steer recommended, as i205 was steered via q43), implement it, then unblock i204b: include insn_overlay.asm + test_overlay_classify.asm, wire run_overlay_classify_self_tests, and verify TestBootSelfTestsPass + the SimCoupe matrix green. Staged work: scratchpad/i204b-*.asm. | OPEN | agent | 🔒 q54 | q54 | i204b, i27b, i27c |
| **i204b** | **i48c-b8e-4b: overlay_classify + literal_word + compact_inst (Z80 port + boot self-test)** — Faithful Z80 port of overlay.go::overlayClassify (+classifyMem/classifyMovImm/exprContextDep/foldSlotPCDependent), compact.go::literalWord + compactInst, litinsts.go::IsFullyLiteral, and aarch64enc::ZeroSlot, wired as a BUILD_TESTS boot self-test asserting Z80 output against Go-authority-computed values. The port is WRITTEN, Go-authority-verified, and staged (scratchpad i204b-insn_overlay.asm / i204b-test_overlay_classify.asm; Go authority test tools/sam-aarch64/assemble/overlay_classify_fixtures_test.go landed by the i204a PR). BLOCKED ON BUDGET: the routines are ~1300 B and, like encode_inst, MUST be inline in section C (they call encode_inst which reads ENCTAB in section A, so they cannot be evicted off-axis — the registry's i69/i205 analysis confirms lever-1 is exhausted for ENCTAB-coupled code). The test-variant assembler.bin has ~11 B of &C000 headroom; commoning recovers tens of bytes, not 1300. Depends on i234 (a budget-relief item) which must free section-C space (serial test bay / multiple-boot variant / page-pool memory model per q36/i2) before these can land + the boot self-test runs green. | OPEN | agent | 🔒 q54 | i234 |  |
| **i48c-b8e** | **Compact encoder adapter: literal-probe + overlay-patch emission (paged, needs ENCTAB)** — Port the encoder-dependent half of assemble.Compact. SCOPE FINDING (2026-06-22 investigation): this is the LARGEST B8 brick and the architecturally most significant — the SAM gains its FIRST standalone instruction-encode capability. The Z80 has NEVER encoded a symbolic INST record from scratch; today it only FOLDS pre-computed base words (host produces base words via Go encodeInst; the Z80 insn_run.asm pass2 folds {slot,expr} patches). For text->.tbn on the SAM, the SAM must encode. So b8e ports ~800 LOC of NEW Z80: (1) encodeInst dispatch (pass2.go:214-373 + operandsToValues: operand-kinds -> form -> slot values -> base word, incl. the special-form intercepts lsl/lsr/bitfield/bic-imm/csetm/barriers/ror/mrs-msr/dc-tlbi + compound mem/shifted/extended forms); (2) overlayClassify (overlay.go:82-163: the encodeInst mirror that returns {relocated-slot, expr} instead of emitting); (3) literalWord (compact.go:354-370: encode at pcProbeA=0 and pcProbeB=0x40000; equal => bare 4-byte literal element). REUSES (already proven in pass2): the 13 slot folders (insn_run.asm fold_*), the per-slot encoders (src/slots/*.asm), form_lookup (form_lookup.asm), eval_expr_const, the symbol/local tables. HARNESS: flat is IMPOSSIBLE (needs the paged ENCTAB + form_lookup + slot encoders = the whole assembler). Use a BUILD_TESTS boot self-test inside the full paged assembler build (precedent: assembler.asm:357-468 self-tests; test_mem_offaxis.asm off-axis pattern), where ENCTAB is already mapped by main_assemble's enctab_map_in bracket; the Go harness synthesises a fixture, the self-test runs the encoder, the Go side compares vs host encodeInst/overlayClassify. SimCoupe-gated (it's in the assembler build). Likely deserves a short spec + possible further decomposition (encodeInst dispatch / overlayClassify / literalWord+wiring) at session start. Go authority: pass2.go encodeInst+operandsToValues, overlay.go overlayClassify, compact.go literalWord. | OPEN | agent | ⏳ i204 | i48c-b8b, i199, i203, i201, i204 | i39c, i48c-b8c |
| **i48c-b8c** | **Z80 serialize: headerRows + WriteFile -> compact v2 .tbn bytes** — Port headerRows (compact.go:196) + format.WriteFile (tools/sam-aarch64-format/writer.go) to emit the full compact v2 .tbn: 12-byte header (magic SA64 + version 2 + flags + editor_region_offset u32), header label table ([count u16][name_id u16][delta uvarint] rows) and local table ([count u16][digit u8][delta uvarint] rows) built from b8a positions, the front-coded name table, the b8b compacted records, then the editor region (sidecar comment/blank rows + globals). Deltas are uvarint-encoded running offsets. Harness-test the serialized bytes vs host CompactTBNBytes/WriteFile over fixtures. | OPEN | agent | ⏳ i48c-b8e | i48c-b8e | i39c, i48c-b8d |
| **i48c-b8d** | **Corpus round-trip integration: text -> compact .tbn on SAM byte-matches host (+optionally assemble to OUT)** — The lands-last wiring: drive the full SAM pipeline text -> lex(asmlex) -> parse(asmparse) -> Pass1-over-IR(b8a) -> Compact(b8b) -> serialize(b8c) over the fixture corpus and assert the produced compact v2 .tbn bytes byte-match the host CompactTBNBytes output (the m9 i48c done-criterion: text->overlay byte-match host for the corpus). Optionally feed the produced .tbn through the existing assembler reader+pass1/pass2 to OUT bytes and compare to the host-assembled output, closing text->bytes end-to-end. Wire a corpus-driven harness test (and, if it fits the boot budget, a SimCoupe self-test). ARCHITECTURE CONSTRAINT (found in b8a, PR #557): the parser tables (asmparse SYM_NAMES/PARSE_RECS) and the assembler pass-1 tables (assembler.asm OPVAL_ARRAY/PASS_PC@&C159/LITPOOL@&D200/LOCAL_LABEL_TABLE@&E280) BOTH claim the &C100-&EFFF scratch region, so the full text->.tbn chain cannot run in one flat binary with today's layout. b8a side-stepped this by producing the IR host-side; b8d (and the real SAM editor) must resolve the memory layout (relocate/page one stage's tables) so parse_run + pass1 + compact + serialize coexist. This is the integrated-build memory-model question (relates to q36). | OPEN | agent | ⏳ i48c-b8c | i48c-b8c | i39c |
| **i31** | **On-SAM preprocessor (.if build-constraints + macros)** — Beyond-M7 / future idea. On-SAM preprocessor supporting .if build-constraints and macro expansion, reducing reliance on the host-side preprocess step. | OPEN | agent | ⏳ i48c-b8 | i48c-b8 |  |
| **i32** | **Multiple source files + staged/partial loading** — Beyond-M7 / future idea. Support for assembling from multiple source files with staged or partial loading, enabling larger projects than currently fit in a single-file workflow. | OPEN | agent | 🔒 q46 | i2a, q46 |  |
| **i33** | **Trinity mass-storage: leverage B-DOS record seam for staged/overlaid assembly (i23/i24 ceiling lift)** — Use the proven Trinity B-DOS storage seam (src/netboot/bdos_seam.asm; spanning records in src/netboot/fw_span.asm) for staged/overlaid assembly, lifting the i23 96 KB IN / i24 32 KB OUT ceilings. Storage-access half proven by netboot work. Assembler still uses plain HSAVE/HLOAD for single-file IN→OUT; this item extends that to mass-storage-backed staging. Rescoped 2026-06-17: netboot proved records are accessible + writable; remaining deliverable is the assembler-scale integration. | OPEN | agent | 🔒 q45 | i2a, i24 |  |
| **i39c** | **Phase 3 — bitfield-packing polish on the overlay slot bytes** — Optional second-order optimisation: pack the INSN_RUN overlay operand fields (reg=5b, kind-tag, shift-kind=2b, cond=4b, width=1b, mem-shape=3b) into sub-byte bitfields instead of one byte per field, per docs/specs/compact-tbn-nextgen-design.md §3.1 (which rates it "REFINE / accept narrowly" — real ~-4 KB but the least leverage per unit of Z80 complexity, a polish layered on the structural wins, not a headline). A SEPARATE concern from the comment/blank-run parsing brick (i48c-b7); the earlier "absorbed into i48c-b7" framing conflated two unrelated subsystems and pointed at a stale design ref (i48-syntactic-encoder-design.md has no §3.1). De-coupled 2026-06-21: gated on i48c-b8 (do the format-packing polish once the overlay encoder is functionally complete, so it isn't re-churned). Migrated to M9 at M8 close (2026-06-12). | OPEN | agent | ⏳ i48c-b8c, i48c-b8d, i48c-b8e | i48c-b8a, i48c-b8b, i48c-b8c, i48c-b8d, i48c-b8e |  |
| **i42** | **CRC / signature in the .tbn file header (integrity check)** — STALE FRAMING (this row predates the v2 format): it says 'the v1 header has magic/version/flags but no integrity field' — but the .tbn format was redesigned to v2 ('Format B', shipped via i39/M8 2026-06-12, designed WITH Pete). Re-scope to v2. Intent stands: add a CRC/checksum/signature integrity field so a corrupted .tbn (bad disk sector, truncated transfer) is detected rather than silently mis-assembled. This is a FORMAT EVOLUTION of Pete's carefully-designed v2 format and touches the byte-match CI gates (release-gate, disasm-roundtrip) + the SAM-side reader (verify-on-load costs T-states). Genuine design choices -> q52 (Pete's format call): which integrity primitive, where in the v2 header, version strategy/back-compat, what bytes are covered, and whether the SAM-side verify cost is worth it. Gated on q52. | OPEN | agent | 🔒 q52 | q52 |  |
| **i50** | **Host Go-tool CLI shape — deferred design question (one binary vs three vs pandoc-style)** — Deferred design question (Pete 2026-06-09): whether host tools stay one format-aware front door (sam-aarch64, auto-detects SA64 magic), revert to three binaries (encode/decode/translate), or adopt pandoc-style --from/--to. Three concerns are already cleanly separated as libraries (aarch64enc / aarch64dec / sam-aarch64-format+frontend); CLI count is a thin presentation choice, orthogonal to the i48 in-memory-only-symbolic-form win. Revisit when bulk GNU→.tbn import and listings host features land. Go tools are Z80-port scaffolding + oracle (memory/project_go_tools_are_z80_scaffolding.md). | OPEN | pete | 👤 pete |  |  |
| **i55** | **Multi-section / linker-script honouring in refenc** — Explicitly punted in favour of the text2bin -flatten approach. Multi-section / linker-script handling in refenc would only be needed if a non-spectrum4-shaped project surfaces. Deferred; from ROADMAP deferred checklist (retired in i52 PR 3). | OPEN | agent | 🔒 q53 | q53 |  |
| **i65** | **SAM-side save-without-comments option (editor phase)** — On-SAM counterpart to the host -strip-comments/-thin-comments flags. Saves a .tbn with the editor region stripped/thinned so a 256 KB SAM can assemble sources authored on a 512 KB machine. q9 resolved (Pete 2026-06-10): the tool targets both 256 and 512 KB SAMs; the full unstripped release .tbn is allowed to exceed 256 KB, but a 256 KB user must still be able to assemble via this stripped disk handoff. Host equivalent already exists. | OPEN | agent | ⏳ i41 | i41 |  |
| **i66** | **Trinity page-persistence as a general persist-to-mass-storage technique** — Persist SAM pages to Trinity B-DOS records as a general storage technique. Deferred by Pete's decision (2026-06-10, q9 resolved): 'no value implementing right now'; revisit only if a concrete need appears. Conditional on i66's reactivation being Pete's call. | OPEN | agent | 🔒 q53 | q53 |  |
| **i70** | **Phase-3 firmware self-provisioning: SAM fetches Pi firmware blobs via HTTP and persists to Trinity storage** — SAM IP stack fetches Pi firmware (several MB) from cdn.githubraw.com over HTTP (q11 resolved: on-SAM HTTP via uIP, host-tool-free; q12 resolved: Model A only). Full host-verifiable HTTP-fetch path complete + host-verified across Bricks 1–6 (#245/#247/#254/#256/#257/#259 netboot_http.asm). Remaining: hardware-gated (EEPROM read + section-C paging + HSAVE RST-8 dispatch on real Trinity; emulation-verified != hardware-verified, CLAUDE.md §5). i82 TFTP client bridges provisioning meanwhile; HTTPS stretch is i88. | OPEN | agent | ⏳ i133 | i133 |  |
| **i73** | **i17 review residuals — LOW-severity hardening and dead-code findings** — Low-severity hardening items split from i17 (PR #185). Landed: L1 emit_byte off-by-one (PR #338), L6 .align>=16 silent no-op (PR #339), L7 pass-1 backward-.org check missing (PR #340), L9 retired symbolic-encoder path removed (PR #371, q20 sweep), and L3/L4/L5/L10/L11 hardening guards (this PR): L3 symbol_abs_bit_ptr id<512 bound (symbols.asm), L4 reader STAGING_BUF exactly-full off-by-one — a 1024-byte record now fits (reader.asm), L5 zero-length LIT_DATA/INSN_RUN BC-underflow guard (main_loop.asm), L10 litpool u8 segment-counter wrap guard (litpool.asm), L11 load_in_file editor-offset b2>=&40 decode-wrap guard (main_loop.asm). Each guard only affects its documented edge case; release byte-match + boot self-tests unchanged. Open: L12 .set/.equ redefinition divergence — Go silently overwrites, Z80 hard-fails on duplicate; a real behaviour change with a two-pass tie-break question -> deferred to q49 (owner pete). Follow-up i109 (vestigial soft-reject hook). | OPEN | agent | 🔒 q49 | q49 |  |
| **i77** | **spectrum4 source succinctification — comments + pathological constant expressions** — Agent-driven, section-by-section, human-reviewed (Pete 2026-06-12): (a) comments rewritten aggressively succinct but still readable; (b) pathological constant expressions replaced with named equates (e.g. the longest instruction in the corpus: `movk w0, (65536 \| (((BORDER_COLOUR >> 16) & 65535) & 65535)), lsl #16`). Coordinate with upstream spectrum4 (content originates there; release.s re-flattens). Ties to editor-tui spec D1 (all comments <256B → u16 record length can revert to u8). Parked (editor era). | OPEN | agent | ⏳ i3 | i3 |  |
| **i81a** | **PNG-endian one-line upstream SimCoupé PR: the __aarch64__ little-endian-guard fix in Base/SimCoupe.h** — hunk 2 of tools/font-proof/simcoupe-local-capture.patch; confirmed NOT in upstream HEAD; ready to offer standalone; community submission is Pete's call. | OPEN | pete | 👤 pete |  |  |
| **i81c** | **Optionally offer the SDL dummy-driver headless renderer fallback upstream** — Lower priority than i81a; community submission is Pete's call. | OPEN | pete | 👤 pete |  |  |
| **i88c** | **i88 6b — hardware-gated integration: wire tls_reasm into tcp_conn, paging layout, bootable disk, real RST 8 on Trinity** — Wire tls_reasm_feed into tcp_conn CONN_SINK_FILTER; finalise paging layout; build a bootable disk; exercise over real RST 8 on Trinity hardware. Prerequisite: TC_RX must grow from 1056 to 16645 B (TLS max) before a real Certificate record fits. Hardware-gated (CLAUDE.md §5). | OPEN | agent | ⏳ i133 | i133 |  |
| **i90** | **%nobits/BSS section bodies dropped by -flatten — source-structure loss** — When source is flattened (-flatten, the release path), the entire body of every %nobits/BSS section (bss_kernel/bss_roms/…) is discarded: contributes zero bytes, so blank lines/comments inside BSS don't round-trip (release.s: 132 of 1472 blank lines, all in NOBITS sections). Pre-existing flatten property, not an i78 bug — i78 blank/comment preservation is exact for PROGBITS. Only matters once the SAM editor edits a flattened source with BSS content. Captured only — decide whether to preserve NOBITS editor content if/when the flattened-edit path needs it. | OPEN | agent | ⏳ i3 | i3 |  |
| **i100c** | **Picker UX — user-facing revision/file-selection interface for the firmware downloader** — User-facing revision/file-selection interface for the firmware downloader. Interface design is Pete's call (editor-era UX, no autonomous decision here). No specific qN gates it — bare Pete decision. | OPEN | pete | 👤 pete |  |  |
| **i101** | **End-to-end deliverable: fetch RPi firmware → assemble spectrum4 release on SAM → TFTP-serve a full Pi 400 netboot** — The integrated SAM IDE on ONE bootable disk — editor + assembler/disassembler + TFTP server — an interactive USER-DRIVEN program (q23 resolved 2026-06-20: NOT a boot→fetch→assemble→serve auto-sequence). User loads it, starts/edits a project, assembles release.bin/kernel8.img from release.tbn (M6 byte-match), and TFTP-serves so a Pi 400 netboots — features exercised on demand. The three org-&8000 layers coexist via paging (implementation detail), not sequential overlays. NOT SimCoupé-testable (no Trinity ENC/SD); real E2E is Pete's hardware gate. Arch: docs/specs/phase3-delivery-design.md §13. | OPEN | agent | ⏳ i3 | i3 |  |
| **i104** | **Per-session retro reports** — At wind-down each session writes a short blameless retrospective to ~/claude-retro/ on genuine process/workflow friction, written independently (not reading other sessions' reports first) so converging signal across sessions is real, not echoed. Rule: say what genuinely happened; 'nothing notable' is a valid report; each friction grounded in a specific incident. Reviewed by hand every few sessions / at milestone close -> recurring frictions become actionable changes (CLAUDE.md / memory / qN). Distinct from the diary (~/claude-diary/). Practice started 2026-06-15. | OPEN | agent | ready |  |  |
| **i106** | **SAM-as-terminal: SSH (or telnet) client to Pete's Mac over the physical LAN** — KEX-stub SSH the sweet spot: send constant X25519 basepoint as client ephemeral pubkey; use server's received pubkey as shared secret — zero EC math, instant connect, interoperates with stock OpenSSH sshd. Skip host-key verification (drops Ed25519+SHA-512). Per-byte: ChaCha20-Poly1305 ~few hundred B/s, usable for SAM MODE 3 64x24. Secure-mode toggle (real X25519, ~10-min) nearly free via existing i88 X25519. Telnet fallback: minimal SAM code but needs Homebrew inetutils on Mac. Reuses i88 crypto + i70 tcp_conn. | OPEN | agent | ready |  |  |
| **i109** | **Collapse the now-unarmed encode_logical_imm soft-reject hook** — PR #371 (q20/i73-L9 sweep) deleted encode_logical_imm_try, the only caller that ever armed encode_logical_imm_soft; with no armer, encode_logical_imm_reject always jp fails. The soft branch + encode_logical_imm_soft/encode_logical_imm_rejected flags are vestigial. Collapsing back to a plain jp fail + dropping the two equates is behaviourally identical (flag never set) -> release.bin byte-identical. LOW priority (touches v2-production code). The hook may be re-armed by the future i48c editor encoder — decide when i48c's encoder is designed. | OPEN | agent | ⏳ i48c | i48c |  |
| **i113b** | **photo-verify flagged data + fold verified facts into trinity-capabilities.md** — QA report flags specific data points (port addresses, EEPROM offsets, hex values, diagrams) that require photo confirmation before committing to the repo. Deliverable: run vision-verify on flagged items, then fold only confirmed facts into docs/notes/trinity-capabilities.md via the established off-repo pipeline. | OPEN | agent | ready |  |  |
| **i117a** | **Prior-art survey: existing Claude Code autonomous-orchestration plugins/frameworks vs i117** — Preliminary 2026-06-18 scan: metaswarm, ccswarm, ruflo, claude-plan-orchestrator, autonomous-dev, closedloop-ai, barkain, Composio AO + Claude Code Agent Teams; plus awesome-agent-orchestrators, awesome-cli-coding-agents. Most emphasise PARALLELISM. i117's model differs: single serial orchestrator + deterministic non-LLM monitor (i97) + prime-directive culture + blameless retro + iN/qN registry backbone. Deliverable: feature-by-feature comparison to confirm differentiation and whether gap justifies building i117. Research-first; cite each tool. Pete's call; do not auto-pick-up. | OPEN | agent | ready |  |  |
| **i126** | **One faithful SAM emulation layer used by every test — fix fragmented/inadequate emulation** — Root cause of repeated hardware-debug detours (i82 client_main; the 2026-06-23 yellow-screen): partial emulators, each modelling half the SAM. The flat Go netboot harness has Trinity seams (ENC/EEPROM/SD) + paging but NO screen/timing/sound/line-interrupts; SimCoupe is a faithful full SAM (real ROM, boots from address 0, screen, timing, sound, line-interrupt render) but has NO Trinity. So a routine that is a verbatim ROM copy can PASS the screenless Go harness and fail flat-yellow on hardware (the rainbow stripes are rendered by the live line-interrupt handlers, which the Go harness does not model). NORTH STAR (Pete 2026-06-23, feedback_comprehensive_emulation): ONE comprehensive emulation = fork SimCoupe (the most complete SAM) and PORT THE TRINITY MODELS IN (ENC28J60 + EEPROM + SD/B-DOS), so the single layer captures Trinity, network, storage, screen, timing, sound AND their interactions, boots from address 0 (i232), and is used by EVERY test. Then cross-subsystem surprises fail in emulation, not first on silicon, and the NETBOOT_HOSTTEST carve-outs (i231) are deleted. Stop the 'SimCoupe port is too much work' / 'merging the two emulators is overkill' shortcuts. Subsumes i124/i231/i232. | OPEN | agent | ready |  | i232 |
| **i143** | **bdos_picker: hardware MODE-2 blit for picker_render (follow-up from i119d)** — picker_render in src/netboot/bdos_picker.asm is a stub ret (all decision/show-name/confirm logic is host-verified in i119d). This item wires the actual SAM MODE-2 screen blit so the picker display is visible on real hardware. Depends on i119d being merged. | OPEN | agent | ⏳ i133 | i119d, i133 |  |
| **i147** | **Turn-end safety valve: make stop-after-merge structural, not discretionary** — The autonomous-loop context protection (touch task-done -> end turn -> monitor /clears for a fresh context) relies on the agent CHOOSING to stop after each merged PR — the easiest step to skip mid-flow. 2026-06-20: a session never stopped, accumulating ~10 merged PRs into one polluted context (judgment-picking + missing-edge blindness followed). Make end-turn-after-merge STRUCTURAL: e.g. the monitor detects 'new merge to main + no task-done while a turn is still running' and intervenes, and/or a gh-pr-merge wrapper auto-touches task-done. Design + own PR. | OPEN | agent | ready |  |  |
| **i152** | **Verify the A=drive-register contract for HRSAD/HWSAD raw-sector hooks on real Trinity** — bdos_read_sector (HRSAD, i119) and bdos_write_sector (HWSAD, i114c) both leave A = the sector value at the rst 8, but the B-DOS hook code reads A as the drive number (bdos15a.src.txt rwsad/xsad: LD A,(hk.a)). After HRECORD-select a Trinity record presents as drive 2. The koron-go harness models the selected record via HRECORD and ignores A, so it cannot catch a wrong-drive bug. Confirm on real Trinity whether these hooks need A explicitly set (e.g. A=2 or A=record) or whether HRECORD makes the record the active context regardless of A; fix both routines symmetrically if needed. | OPEN | agent | ⏳ i133 | i133 |  |
| **i159** | **Hyperlink item refs to in-file anchors in the registry .md tables** — In item-registry-open/closed.md + backlog.md, render id references (deps, dependents, parent, id-shaped refs) as in-file anchor links (e.g. [i122](#i122)) so a reader can jump to the referenced row within the same doc. Needs per-row anchors (GFM heading/HTML anchors in a table cell) + ref→anchor rewriting in gen.go. Pete request 2026-06-20. | OPEN | agent | ready |  |  |
| **i160** | **Tree-view preview at the bottom of the registry .md docs** — Append a visual tree view (umbrellas → children → bricks) to item-registry-open.md / item-registry-closed.md / backlog.md, below the full table, so the hierarchy is visible at a glance. Derive from the parent edges in gen.go. Pete request 2026-06-20 (the corrupted '#3'). | OPEN | agent | ready |  |  |
| **i161** | **Research CIIP (Z80 TCP/IP stack): locate source, assess license + portability** — From q25 (Pete: yes, investigate). Find CIIP — a pre-existing Z80 TCP/IP stack (TFTP/HTTP/DNS servers+clients; Simon Owen reportedly ported it to pyz80). Search ~/sam-archive + Pete's disks + online (GitHub etc.); do NOT scan Pete's photos (he checked — no source location, and it burns tokens). Assess license/portability and weigh 'adapt CIIP' vs continue i82/i83/i86. Report findings. | OPEN | agent | ready |  |  |
| **i162** | **Version-control ~/bin agent tools in a private ~/git/claude-tools repo** — From q33 (Pete: yes, private). Create ~/git/claude-tools, move the ~/bin agent tools in (claude-chat-export incl. the i155 fix, claude-diary helpers, claude-chat-export SessionEnd hook target, etc.), scrub anything private/secret, push to github.com/petemoore/claude-tools as a PRIVATE repo (Pete can make public later). Gives the tools backup + history. | OPEN | agent | ready |  |  |
| **i167** | **Determine the B-DOS save-buffer limit from first principles (read source; not hardware-only)** — q22 reframed (Pete 2026-06-20: 'only knowable on hardware' is a hand-wave — it's deterministic and we have the source). From first principles, read the B-DOS source (reference/bdos/) to find the max writable address / available save-buffer size deterministically, and replace the GUESSED FW_RECORD_CAP=4096 (src/netboot/http_main.asm, HTTP-streaming-sink path) with a source-derived value. Establish whether a hardware bench is truly required or (likely) the limit is computable from source. | OPEN | agent | ready |  |  |
| **i168** | **Future: import a full source file into the editor (fail-with-line-number on bad line)** — Future feature (q24, Pete 2026-06-20). The SAM editor accepts ONE LINE AT A TIME (parsed/validated on entry) — no whole-file import today. If/when import is added: on a bad line, fail and report the line number + the error; the user fixes the source and retries (Pete's preferred behaviour). Gated on the editor groundwork (depends i3) — a normal backlog item, not part of current editor specs. | OPEN | agent | ⏳ i3 | i3 |  |
| **i172** | **Audit i133's dependents: reclassify presence-gated ones as 'needs Pete' (or push i133 up)** — i133-dependents audit (Pete 2026-06-20): i133 (automate SAM testing via trinload) gates i118b, i135, i141, i143, i145, i152, i70, i88c, i93b — but many likely only need Pete PHYSICALLY PRESENT (a key-press / hardware action), not the full automation. Per the needs-Pete model (i169): go through each; for those that only need Pete present, mark owner:pete (needs-pete) + drop the i133 dep. Alternatively push i133 near the top so when it lands we verify it truly unblocks them. Depends on i169. | OPEN | agent | ready | i169a |  |
| **i178** | **Registry: raise the item-title length limit from 120 to ~160 chars (validate.go + mutators help + test)** — The 120-char title cap forced abbreviating SAMBOOT item titles. Bump the limit in tools/registry/validate.go (the check + message + the §147 comment), the mutators.go add-flag help text, and update validate_test.go's title-length assertion. Keep titles single-line. Pete invited this (2026-06-20). | OPEN | agent | ready |  |  |
| **i121c** | **WRQ auto-slot + write-out (write-to-free-only)** — Final block -> bdos_find_free_record (i119); none free -> reply TFTP ERROR(3,'no free record'); else select + bdos_fill_save_uifa (name from WRQ filename) + bdos_save_hook. q30 RESOLVED (write-to-free-only via disk-config auto-pick; prompt if config absent) so ungated. E2E mirrors TestClientE2EConfirm: assert HSAVE in the free record + right bytes; all-full -> ERROR(3) + zero HSAVEs. rst-8 HSAVE persist stays hardware-gated. | OPEN | agent | ready | i121b |  |
| **i182** | **Bootable fetch-and-boot disk + real-Trinity run of client_fetch_boot** — i122c delivered the host-verified PXE-style fetch-and-boot orchestration entry (client_fetch_boot: EEPROM identity -> TFTP-fetch a .mgt straight into a scratch record via the raw_record_sink -> validate -> ALHK-boot). To exercise it on real hardware, build a bootable SAM disk whose autorun invokes client_fetch_boot (a netboot-fetch-boot-disk make target mirroring netboot-client-disk), decide the autorun entry vs client_main, and run it on real SAM+Trinity: serve a stamped 819200-byte .mgt, confirm it streams into the scratch record (1600 HWSAD sectors), validates, and ALHK-boots it. Hardware-gated (the whole point is the real SD write + real boot, unverifiable in emulation); entangled with the auto-boot/control-return autonomy (i132/i135). The full-size valid stream is also not driven through the bit-banged ENC in the host harness, so the real wire run is the first full-scale fetch-and-boot. | OPEN | agent | ready | i122c |  |
| **i184** | **Harness prod-boot decode tests should serve a complete disk + StrictFileNotFound (emulation-first uniformity)** — Follow-up to i183. Several harness tests boot the PROD assembler with a minimal file set (align/org/fold guards, harness_test, compact_tbn) and rely on the legacy silent HLOAD no-op for the unconditional d15/sd13/zx013 boot loads. i183 made the faithful file-not-found longjmp OPT-IN (Config.StrictFileNotFound) to avoid breaking these. The emulation-first ideal (one layer, no carve-outs) is for every prod-boot test to serve a complete bootable disk and set StrictFileNotFound=true, so a future 'needed payload silently missing' bug fails loudly. Low priority, dev-tool test hygiene (not a CI gate). Refs: tools/z80-test-harness-go/harness.go Config.StrictFileNotFound, SCOPE.md i183 note. | OPEN | agent | ready | i183 |  |
| **i25b** | **SAM-side DOSER (&5BC0) file-I/O error handler (port COMET)** — Implement the assembler-side DOSER (&5BC0) error handler: set (&5BC0) to a handler before file ops; the handler does AND A; RET Z on success, else converts the SAMDOS file-I/O error into the existing diagnosed FAIL (jp fail with a load/save tag, reusing src/assembler.asm fail/fail_with_tag + src/print.asm). Port COMET's prepare/dier/sproom idiom (comet.asm:1265-1382). Verify in the i25a harness DOSER model + SimCoupe. Scope: alt D (minimal, chosen); alt A (per-error-number messages + stack recovery) future. Refs: src/loader.asm + src/main_loop.asm hook call sites. | OPEN | agent | ready | i25a |  |
| **i192** | **SAMBOOT dumper: headless auto-RET after all regions served (single dump-and-return, no Esc)** — Split from i188 (2026-06-21). i188 fixed the rom1 crash + the clean Esc-driven trinload return; the dumper still serves forever until Esc. For the FULLY UNATTENDED push->run->return->re-push loop (i133-era), the dumper must auto-RET to trinload once the host has pulled all regions (rom0/rom1/eep0..7), with no human Esc. Implement: track per-region transfer completion (the final short-block ACK in serve_serve_once) and RET from dm_serve_loop once all 10 regions have been served once (Esc stays as the fallback). Emulation-verify via the i181 paged harness + the trinload push/return round-trip. NOT a blocker for i87a-b2 (rom1 recapture) — that uses the Esc return, already proven by the eeprom/rom0 captures. | OPEN | agent | ready | i188 |  |
| **i121i** | **.mgt bootable server-disk vessel + BASIC loader that appends the same config block** — The OTHER shipping vessel (sibling of i121d's trinload vessel): a bootable .mgt that a BASIC loader boots, loading the WRQ server code + appending the i121h config block at the same memory location, so the runtime image matches the trinload vessel exactly. The disk carries the config file the loader reads (absence -> the future i121k SAM prompt). Same program source as the trinload vessel; only the packaging differs. Sibling deliverable to i121d. | OPEN | agent | ready |  |  |
| **i121j** | **SAM-side interactive strategy prompt when config is undefined (deferred)** — DEFERRED (Pete 2026-06-21: more complex to model, not needed now). Complementary to the host-launcher config: if the in-memory config block's strategy is undefined (the host launcher / disk config left it unset, e.g. user chose 'ask on the SAM'), the program prompts the user on the SAM keyboard for the placement strategy at launch. For now the host launcher always sets a concrete strategy (default highest-free), so this path is unused. Requires modelling a SAM keyboard prompt in emulation. | OPEN | agent | ready |  |  |
| **i194** | **Real-hardware disk push: stream all build/*.mgt to the SAM via the trinload serve vessel, into free Trinity records** — The i121 payoff on real hardware, autonomous: 'start trinload -> push the serve program (push-serve.py) -> tftp put each .mgt disk image (validated size==819200, streamed into a free record + claimed) -> tftp put tftp.done -> return to trinload', then read a stored record back (servable-raw) and byte-compare to confirm the store. Unblocked once the production CSD read lands (i145b, so the server finds free records) and the full experiment is emulation-proven (i145d). Agent-actionable: trinload runs on the SAM (192.168.2.75) over the LAN from the Pi; push via tools/trinload-push/push-serve.py + curl tftp://; the 19 build/*.mgt are valid + staged (/tmp/push-all-disks.sh batches them). NO Pete needed for the push+store+readback; booting a pushed record to watch it run is a downstream nicety (autonomous via i135 auto-boot, or a Pete-present spot-check) and NOT a blocker for this item. | OPEN | agent | ready | i145a, i145b-b1, i145c, i145d, i145e, i121d, i195, i141, i198 |  |
| **i207** | **Harness fidelity: load boot payloads from the build-disk image, not via separate hardcoded page-loads** — The Go test harness (tools/z80-test-harness-go) pre-loads off-axis boot payloads (test_cluster, test_mem, paged_call, enc_fix, ...) directly into physical pages in its test setup, bypassing the real disk->HLOAD path SimCoupe exercises. Consequence: a payload missing from the SimCoupe fixture disk still passes the harness (it pre-loads the page), so harness-green != SimCoupe-green. This gap let i69 (PR #577) pass the harness while all four test-variant SimCoupe jobs hung (rc=124) because tools/run-roundtrip.sh did not put the enc_fix payload on the fixture disk. Per CLAUDE.md s7 (one emulation layer capturing every integration point): make the harness derive its loaded payloads from the SAME build-disk image SimCoupe boots (parse the .mgt directory / mirror build-disk's file list), OR at minimum assert the build-disk manifest contains every payload the boot loader HLOADs, so a missing-from-disk payload fails the harness too. Owner agent. | OPEN | agent | ready |  |  |
| **i213** | **Trinity EEPROM capability signature + version (interfacing code auto-detects our patch, never assumes it)** — Trinity EEPROM/firmware version + identity stamp: when we customise the Trinity EEPROM (i135c — stripes restore, boot-a-record-on-startup), write a small magic signature + version byte at a stable offset, plus a read helper. JUSTIFICATION (Pete, 2026-06-23, refined): NOT for SD-capability negotiation (record access through the EEPROM B-DOS is transparent, so no capability check is needed). The real need: the B-DOS actually in control is NOT guaranteed to be the one we flashed — B-DOS can be loaded from FLOPPY (the traditional DOS-load path), which is the normal case for anyone WITHOUT our customised ROM chip (not universal). So our software can't assume the EEPROM's patched firmware is present; the stamp lets it DETECT which firmware/patch is actually running and handle a mismatch gracefully. SCOPE: just the stamp + read helper (not the heavyweight per-capability auto-detect tied to the WONTFIX i214). The write rides i135c; the stamp-format + read helper are owner:agent. Relatedly this is WHY i214 (fix BD_RECORDS in the EEPROM) was dropped — a floppy-loaded B-DOS bypasses any EEPROM fix, so our direct-SD record code (i141/i145, frozen-interface, version-independent) is the right layer, and it is already faithful-16-bit which is ample. | OPEN | agent | ready |  |  |
| **i215** | **IDE page-persistence (spill): lazy-spill cold DOC blocks to Trinity or floppy/tape when the pool is full** — Downstream of i2 (Pete, 2026-06-22, q36): the i2 baseline refuses-with-message when the page pool is exhausted. This OPTIONAL feature makes alloc_page lazy-SPILL a cold DOC block to a persistence backend instead of refusing (design §4.5/§5/§7): serialize the block to disk, free its page, satisfy the alloc; reload on demand. Backend is PLUGGABLE — Trinity storage if the machine has it, else a physical floppy/tape — so a space-limited user on ANY machine gains document headroom beyond the ~144KB/256KB ceiling. HARD CONSTRAINT: the assembler must run fully on a floppy/tape-only machine with NO Trinity, so the spill backend is optional and must degrade to refuse-with-message when absent. Depends on i2 (the pool allocator + the page_owner[] discipline) but NEVER blocks it. | OPEN | agent | ready | i2a |  |
| **i27b** | **i27b: Z80 port — Cortex-A53 835769 NOP insertion** — Faithful Z80 port (CLAUDE.md s6) of the i27a 835769 fix into the SAM-side assembler (src/). Detect a memory op immediately followed by a 64-bit multiply-accumulate (Ra!=XZR) and insert a NOP between them, off by default behind the SAM toggle. Mirror the Go authority's mask predicates + erratum_sequence RAW carve-out. NOP insertion shifts addresses, so it must be done in a way that PC-relative refs stay correct — detect in the record/IR stream (pass1) so the inserted NOP word is accounted for in PC assignment across both passes (the Go authority establishes the exact decision). SimCoupe-gated; add a boot self-test fixture. Go authority: errata.go (i27a). | OPEN | agent | 🔒 q54 | i27a, i234 |  |
| **i27c** | **i27c: Z80 port — Cortex-A53 843419 ADRP->ADR rewrite + SAM enable/disable toggle** — Faithful Z80 port (CLAUDE.md s6) of the i27a 843419 fix + the SAM-side enable/disable toggle UX (q37: explicit feature, off by default). Post-layout scan over the OUT buffer: for each ADRP at a final page-offset 0xff8/0xffc matching the sequence predicate (mem-op, optional 1 non-branch insn, then unsigned-imm ld/st whose base == ADRP Rd), rewrite ADRP->ADR in place (no address shift — the +/-1MB ADR range always fits our image). Mirror the Go authority masks. Wire the SAM toggle (directive or build option) covering BOTH 835769 (i27b) and 843419. SimCoupe-gated; boot self-test fixture. Go authority: errata.go (i27a). | OPEN | agent | 🔒 q54 | i27a, i234 |  |
| **i219** | **Acquire a stock (untarnished) SAM Coupé system ROM binary + disassembly** — Gap surfaced by i189 (the SAM software catalogue, docs/specs/sam-software-catalogue.md): the project has NO stock/untarnished SAM Coupe system ROM — no binary, no disassembly, no deep doc; it exists only conceptually (the ZX Spectrum ROM disasms under ~/git are a DIFFERENT machine, not a substitute). We have Colin's FORKED ROM captured (rom0/rom1/rom.bin) but not the stock one. Having the stock ROM would let us diff fork-vs-stock to isolate exactly what Colin changed for the EEPROM boot path, and ground the startup-stripes / boot behaviour. owner:pete — acquisition is Pete's call (Colin Piggot / Simon Owen are contacts, or a SimCoupe ROM image). Agent can disassemble/annotate once a binary is in hand. | OPEN | pete | 👤 pete |  |  |
| **i220** | **Comprehensive Go doc comments: package docs + every exported func/method/type** — Pete (2026-06-23): write accurate Go doc comments across ALL Go modules under tools/ so that, at a glance in godoc, the whole system's behaviour is visible. Cover package-level docs for every package + doc comments for every exported function/method/type (and key unexported ones where behaviour is non-obvious). SECONDARY BENEFIT Pete values: thorough godocs surface 'hacks' — a function whose honest doc has to describe a workaround (a test adapted to pass rather than the cause fixed) becomes visible, supporting the emulation-fidelity / no-silent-workarounds discipline. LOW PRIORITY (end of backlog; not urgent). Mechanical + large; a good incremental / autonomous-loop filler. Per-module: run go doc / a godoc server to review the result reads cleanly. Comments present-tense, describe what the code does now (no history) per the repo comment style. | OPEN | agent | ready |  |  |
| **i231** | **Systemically eliminate NETBOOT_HOSTTEST==0 emulation carve-outs + CI guard against reintroduction (emulation-first enforcement)** — Pete (2026-06-23), load-bearing + recurring: the 'if defined(NETBOOT_HOSTTEST)==0' pattern hides Z80 code from the emulation/host build so it ships to hardware UN-emulated — the exact CLAUDE.md §7 / feedback_emulation_first anti-pattern (the i82 client_main brick, the samboot_stripes stub). It keeps getting reintroduced (agents add new ones), so the fix must be SYSTEMIC, not per-stub. 34 sites across 13 src/netboot files (audit 2026-06-23). For each: determine what it gates + why the flat harness can't run it (ROM/RST 8/screen), then either (A) remove the gate (code runs safely in emulation as-is), (B) make the harness model what it needs, or (C) run that payload under SimCoupe (the ROM/screen-bearing CI gate). Plus a CI GUARD that FAILS the build if a NETBOOT_HOSTTEST==0 block is added beyond the shrinking allowlist (ratchet to zero), and crystal-clear CLAUDE.md §7 + memory wording so future agents never add one. First fix already landed: samboot_stripes (authoritative ROM port, no carve-out). Emulation-first: ANYTHING that runs on hardware is tested in emulation first, no exceptions. | OPEN | agent | ready |  |  |
| **i232** | **All Go-harness tests boot the SAM from address 0 (real ROM cold-init) for faithful machine state, not Call-with-seeded-state** — Pete (2026-06-23), foundational fidelity principle (part of i126): booting from address 0 with the real ROM IS what a SAM does — running a routine in isolation with hand-seeded registers/sysvars (e.g. a hand-seeded PALTAB) is not emulating the machine, it is testing a fragment in a made-up state. The Go library boots from reset in milliseconds (samboot_real_boot_test.go already does it: load rom.bin, run from PC=0, the ROM cold-init MNINIT->NEW2->streams populates PALTAB/STREAMS/FLAGS/etc.), so every harness test should boot from 0 first and then exercise the code in the REAL post-boot state. Deliverable: a reusable 'boot the SAM from reset, then run X' harness helper, adopted as the default across the koron-go tests (the netboot leaf tests currently Load+Call without booting). Big refactor; FUTURE — must not block the current bootblock RAM-test work. Captured per Pete's instruction to record + move on. | OPEN | agent | ⏳ i126 | i126 |  |
| **i89b** | **Capture a real Pi 3 netboot exchange + generate Pi 3 golden wire vectors** — Hardware-gated confirmation tail of i89. On Pete's Pi 3 + SAM with the hub+Wireshark rig, capture the DHCP DISCOVER/OFFER/ACK + first TFTP probe sequence and generate masked Pi 3 golden vectors (as the Pi 4 vectors were). Decisive open detail: whether the Pi 3 boot ROM is byte-strict about the option-43 'Raspberry Pi Boot' menu string (trailing spaces). Findings to confirm are in pi-netboot-capture-analysis.md section 4. | OPEN | pete | 👤 pete |  |  |
| **i120b** | **Port windowed TFTP to the Z80 client (i82) + server (i83/i95) and re-add windowsize to ClientOptionSet** — Z80 port of the windowed-TFTP logic whose Go authority is i120a. Add windowed ACK (ACK only last block of each window) + lost-block/out-of-window rewind to the SAM client and server, SimCoupe-verify, then re-add windowsize to ClientOptionSet (i118 had dropped it for lock-step correctness). Gated on i120a. | OPEN | agent | ready | i120a |  |
| **i123b** | **Perform the make->tup build migration (Pete-greenlit)** — Contingent on Pete's go-ahead per the i123a evaluation. Port the 167-target Makefile to per-dir Tupfiles + Tuprules.tup, reusing spectrum4's template. Order: (1) symbol-import cluster (enc_fix_payload->assembler->test_mem/test_cluster) first — it is the footgun; (2) netboot standalone binaries (parallel); (3) disk-pack + table-codegen edges. Keep SimCoupe/release-gate in ci.yml; byte-compare tup outputs vs make until parity; add the FUSE-privileged tup-under-docker CI launcher. owner:pete = the go/no-go decision is his; implementation is agent work once greenlit. | OPEN | pete | 👤 pete |  |  |
| **i240** | **monitor.sh: gate nudges on Pete-presence semaphore + announce arrival/departure in-session** — When Pete is present (the ~/.claude/autonomous-loop/pete-present semaphore exists), monitor.sh should NOT deliver autonomous-loop nudges/interruptions. Additionally: when the monitor DETECTS the semaphore was just created, stuff a friendly arrival line into the session (e.g. 'Hi Claude, I am back, Pete here.'); when it detects the file was just deleted, stuff a departure line (e.g. 'Pete has left - he deleted his presence flag, please continue working autonomously.'). Pete request 2026-06-24. Parked/future; do not work yet. | OPEN | agent | ready |  |  |
| **i243b** | **Audit + convert the OTHER trinload-pushed netboot di;halt sites to tr_terminate (http_main, client/server/serve, dumper, smoke)** — Per-file audit: distinguish trinload-pushed programs (-> tr_terminate, RET to trinload) from disk-booted ones (different exit). Leave SimCoupe -exitonhalt halts in src/assembler.asm + src/secd_probe.asm. Convert the pushed ones so failed experiments don't strand the SAM (Pete, 2026-06-24). | OPEN | agent | ready |  |  |
| **i245** | **Trinity SD model: enforce the one-byte read lag so a missing command flush (Ncc sync) FAILS in emulation (the i145g all-zeros bug class)** — The koron-go SD model (sdcard.go) frames a command by bit-pattern ((b&0xC0)==0x40), not clock count, and returns command responses with NO one-byte read lag — so a flush-less command (opcode sent immediately after &31 select, missing the leading &FF Ncc sync) frames and responds IDENTICALLY to a correct one. That is exactly why the i145g all-zeros CSD bug (missing leading flush in sd_cmd/sdc_cmd) passed every host test yet failed on real hardware. FIX: model the &DF one-byte read lag faithfully (doc trinity-sd-z80-interface.md §3: sd.in reads 'the byte clocked in by the PREVIOUS OUT'), so a command not preceded by the flush misaligns and yields no valid response / no &FE token — making the missing-flush class a build-time failure. Determine the exact mechanism (Ncc sync vs lag alignment) from the authority first; migrate existing SD tests (drivers now send the flush). Emulation-first: an emulator that cannot catch this class is the defect (feedback_port_diff_authority_first). Followup from i145g (PR #656). | OPEN | agent | ready |  |  |
