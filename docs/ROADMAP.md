# sam-aarch64 roadmap

<!-- HANDOVER-PROTOCOL-START -->
## START HERE — session handover protocol (read this first)

**This file is the canonical entry point and the agent→agent handover contract.** If you are an agent picking up this project: read this section, then the **"Current State & Next Actions"** block below, then the current milestone status doc, then the project memory index (auto-loaded at session start). That is the complete start-up read — everything else is reached via links from here.

### The contract (always inherited, always maintained)

1. **Tracking is part of "done."** A new idea, decision, or deferred item is not captured until it is an `iN` row in the item registry (or `qN` in the question registry) — **in the same change that introduces it**. Treat a missing tracking entry like a missing test: reviewers flag it. This is the rule that stops ideas being lost to context drift.
2. **One home per kind of thing** (this is how the docs stay un-sprawled):
   - **ROADMAP.md** (this file) — the index, the backlog, and the live "Current State" below.
   - **Item registry** (`docs/notes/item-registry.md`) — the project-wide, milestone-neutral `iN` id↔item map. Never archived. Its header documents the **shared registry discipline for both `iN` items and `qN` questions**.
   - **Question registry** (`docs/notes/question-registry.md`) — the project-wide, milestone-neutral `qN` open-questions-for-Pete map. Never archived; sibling of the item registry (same discipline). Every question for Pete goes here the moment it arises.
   - **Milestone status docs** (`docs/notes/m{N}-status.md`) — the per-milestone source of truth.
   - **Memory** (`~/.claude/projects/.../memory/`) — cross-session preferences, facts, feedback. *Not* work-tracking.
   - Superseded docs are **deleted** — git history is the archive; pin citations to a blob link if a living doc must cite one.
3. **At milestone close**, walk the item and question registries and ask of each item: still deferred, or does it now fold into the milestone in flight? **The `iN` item registry and the `qN` question registry are both milestone-neutral (`docs/notes/item-registry.md`, `docs/notes/question-registry.md`) and never archived:** at milestone close, every item belonging to that milestone must be marked ✅ done or ❌ wontfix in the registry, or migrated (re-pointed) to the active milestone's strands — no item may be left living only in a closing `m{N}-status` doc. Open questions for Pete live only in the question registry (`qN`) for the same reason. **The milestone status doc is deleted at close** (git history is the archive) after the registry walk completes.
4. **Delegate to subagents to preserve orchestration context.** The agent driving a session (the *orchestrator*) should push self-contained, well-scoped work out to subagents — codebase searches, mechanical edits, doc drafting, focused investigations, even whole low-risk PRs (implement → verify locally → commit → push → open PR) — via the `Agent` tool (and `Explore` / `Plan`), keeping only the conclusions in its own window. A subagent's full transcript is discarded; only its final summary returns, so the orchestrator's context lasts far longer. The orchestrator keeps the judgement calls itself: sequencing, monitoring CI, merge/launch decisions, and anything user-facing. The durable state lives in subagents-plus-these-docs, not in any one agent's window — so when context *does* fill, the handover (update "Current State", below) lets the next agent resume as orchestrator with nothing lost. Prefer launching independent subagents in parallel (one message, multiple `Agent` calls) when the work has no shared state.

### Session hygiene (the minimal ritual)

- **Closing a session:** **no magic words** — *any* wind-down phrasing ("close the session", "let's wrap up", "prepare for handover", "goodnight, what's next?") triggers this. The agent then updates the **"Current State & Next Actions"** block below *in place* — what landed, what's in flight (open PRs / branches / running agents), and the single immediate next action; confirms tracking is current (rule 1); and leaves the working copy on a clean, current `main`. Do **not** write a new dated handoff doc — update the standing block. **Get this update merged to `main` before handover** — a quick docs PR + CI, since branch protection blocks direct pushes. Never hand over with the live state stuck in an open PR, or the next session reads stale context.
- **Starting a session:** the standard start prompt is just **"Continue per docs/ROADMAP.md."** A SessionStart hook (`tools/session-handover.sh`) runs first: it surfaces this section and, **when you are on a clean `main`, auto-`fetch`es and fast-forwards to the latest** so the doc you read is current. If it instead **warns** (you are on a feature branch, have uncommitted changes, or `main` can't fast-forward), refresh deliberately before relying on the doc: stash/commit your work, then `git checkout main && git pull --ff-only`. Then read this section → Current State → resume; no bespoke per-session prompt is needed.

### Current State & Next Actions

*Updated in place each session — this is the live handover. Keep it ≤15 lines; history lives in the milestone status docs, the registries, and `git log`.*

- **Milestone:** **M9 active — the editor era** (`docs/notes/m9-status.md`). M0–M8 ✅ complete. A **Phase-3 design strand** (i80–i84) runs alongside, per Pete 2026-06-13.
- **★ Last landed (autonomous run, 2026-06-15):** the **i70 HTTP self-provisioning WIRE PATH is complete + host-verified end-to-end over the i80 emulation** — 5 bricks: TCP transport (`build_tcp_segment`), TCP connection (`tcp_conn`), HTTP/1.0 GET (`http_get`, #254), the fetch phase-machine **Go authority** `http/fetcher.go::Fetcher` (#256), and its **Z80 port** `netboot_http.asm` (#257, the capstone — ARP→handshake→piggybacked-ACK+GET→response→FIN, byte-for-byte vs `Fetcher`). **Plain-HTTP source VALIDATED** (mirror-check, design §7): `http://archive.raspberrypi.org/debian/` serves the *complete* Pi 3 + Pi 4/400 netboot firmware over **plain HTTP** (verified by unpacking a real `.deb` on :80) → **on-SAM TLS (i88) is now OPTIONAL, not needed**; the new sub-task is `.deb` (ar/xz/tar) extraction or a pre-extracted mirror = **q15**. Also: the **i97 monitor hardened** (#255 — the settled-Enter submit fix + a single-instance lock fixing the duplicate-`/clear`; `feedback_autonomous_loop` memory records the mid-turn-`/clear`-is-a-no-op rule). The **bootable HTTP-fetch disk + B-DOS write-out landed** (#259, brick 6 — `http_main` mirrors `netboot_client`'s EEPROM/HSAVE verbatim; the fetch loop host-verified, the HSAVE hardware-gated). **Remaining on i70 (unblocked, autonomous-eligible):** **i99** — *stream* the body to storage (the current fetch buffers it all in a 4 KB `CONN_DATA` / 16 KB `STAGING`; real firmware is multi-MB → must write incrementally to Trinity storage; `conn_accumulate` also needs a bound/flush). The real on-hardware fetch is Pete's test; **q15** decides the `.deb`-extraction strategy; the i99 design pass is **done**. **Morning 2026-06-15 (Pete engaged): q15 + q16 RESOLVED**, firmware-provisioning path decided = **(c)** fetch commit-pinned firmware from `http://cdn.githubraw.com/<owner>/<repo>/<sha>/<path>` (plain HTTP, commit-addressable — research-found) + a **SHA-256 verify** (the `sha256.asm` primitive **landed**, #269); persist by **spanning Trinity records** with bounded `HSAVE`s (q16); **i88 (TLS) parked at LOWEST priority**, scope-reduced. The **"stream + verify" increment LANDED** (#271 — `sha256_*` wired into the i99 streaming sink: `storage_sink_flush` hashes each window as it flushes, `conn_verify_init`/`conn_verify_final` set `CONN_HASH_MATCH` vs a pinned hash; host-verified the running digest == `crypto/sha256(body)`; all `NETBOOT_HOSTTEST`-gated so the bootable image stays byte-identical, independently re-verified). Next: the **i100 downloader UX** (see NEXT ACTION).
- **Earlier this M9/Phase-3 milestone (history in `git log` + `docs/notes/m9-status.md` + the iN/qN registries):** the full **DHCP+TFTP netboot server** + the **i80 SimCoupé Trinity-net emulation** (the wire I/O made host-verifiable) + the i91–i96 packet/loop bricks + the bootable **smoke / integrated-server / serve-files / TFTP-client** disks all landed + host-verified byte-for-byte over the emulation; i7 phase C done (only D=i74 remains, gated). The netboot **oracle** (`tools/netboot-oracle/`) is the byte-for-byte porting spec.
- **In flight:** nothing.
- **AWAITING PETE (his calls — none block the next agent; agents keep working other items):** (a) **q13** — the editor default rendering config (a taste call; agent recommendation on the table, A/B the four starter configs); (b) future hardware artifacts when convenient — the **patched-ROM dump** (i87) and a **Pi 3 capture** (i89), both non-blocking; (c) **i81 contributions** are community-facing so Pete submits — the aarch64 PNG-endian one-liner upstream, then a short #28 comment; (d) **q14** — live-test the **i97 autonomous-loop monitor** (`tools/autonomous-loop/`), now hardened (settled-Enter + single-instance lock; re-run unattended); **q15 ✅ RESOLVED (Pete 2026-06-15: option c)** — firmware source = `http://cdn.githubraw.com/<owner>/<repo>/<sha>/<path>` (plain HTTP, commit-SHA-addressable, found by research) + a **SAM-side SHA-256 verify**; **i88 (TLS) built only at the LOWEST priority** (scope-reduced to TLS-crypto-only, github.com-only, content already hash-pinned). **q16 ✅ RESOLVED (Pete 2026-06-15):** persist a multi-MB firmware as bounded files **spanning multiple Trinity records/disks** (~800 KB each), each a plain `HSAVE` (the verified hook), reassembled at serve time — sidesteps the broken append hooks. Pete also corrected the "64 KB file cap" (wrong — UIFA size is pages×16384, ~4 MB/file; the real limits are RAM + per-record capacity). All non-blocking.
- **Phase-3 implementation: AUTHORIZED + UNDERWAY** (memory `feedback_implementation_autonomy`, 2026-06-14) — spec-gate lifted; the Z80 netboot port proceeds per `docs/plans/phase3-netboot-implementation-plan.md`. **Verification line (the load-bearing distinction):** the netboot **protocol logic** — every packet *build/parse* routine — **IS host-verifiable** and merges green: the Z80 routine is pure arithmetic + memory writes, so running it under the koron-go/z80 harness and byte-comparing its output to the golden vector is a real, observable check (i92 proves the pattern). **As of i80's first brick (this session) the ENC28J60 wire I/O is host-verifiable too** — the Trinity board is emulated host-side (Go) and the real driver runs against it (frame bytes in/out asserted byte-exact). Still NOT host-verifiable: an **end-to-end Pi boot** + real-silicon TX/RX timing — gated on **real Trinity hardware** (the final integration gate; CLAUDE.md §5). Emulation-verified ≠ hardware-verified. The oracle (`tools/netboot-oracle/`) is the byte-for-byte porting spec.
- **NEXT ACTION (autonomous — the queue is NOT drained):** the i70 **wire path + the bootable HTTP-fetch disk are done + host-verified** (6 bricks, #254→#259; the plain-HTTP source is validated so i88/TLS is now optional). The next autonomous i70 item is **i99 — stream the fetched body to Trinity storage** (the current path buffers the whole response in a 4 KB `CONN_DATA` / 16 KB `STAGING`; a real multi-MB firmware blob won't fit, and `conn_accumulate` has no bound check). **i99 design pass is DONE (2026-06-15, see the i99 row):** the **RAM-side is autonomous-eligible now** — convert `conn_accumulate` to a bounded `FLUSH_WINDOW` → `storage_sink_flush` (deletes the overrun bug), skip the HTTP header before the first flush, **flush-before-ACK** for free back-pressure (the loop is single-threaded), mirror it in the Go `tcp.Conn`/`Fetcher` (route to a `Store` sink), host-verify by streaming a multi-window body to the observable sink. The **i99 streaming foundation landed** (#263 Go + #264 Z80 sink, host-verified) and **q16 is RESOLVED** (Pete 2026-06-15: multi-record spanning via bounded `HSAVE`s; the 64 KB file-cap premise corrected). **q15 + q16 are now RESOLVED (Pete 2026-06-15)**, so the firmware-provisioning path is fully decided and the build is unblocked:
  - **✅ DONE — SHA-256-on-Z80** (`src/netboot/sha256.asm`, #269): the verify primitive (init/update/final streaming, so it hashes as the firmware streams); host-verified byte-for-byte vs Go `crypto/sha256` + the NIST vectors (incl. the 1M-'a' + a 63-case chunked matrix). Also the one crypto block i88 reuses.
  - **✅ DONE — "stream + verify"** (#271): `sha256_*` wired into the i99 streaming sink — `storage_sink_flush` `sha256_update`s each window as it flushes (every body byte hashed once, in order); `conn_verify_init` resets the running hash at the start; `conn_verify_final` writes the digest into `CONN_HASH` and sets `CONN_HASH_MATCH` (1/0) by byte-comparing vs the caller-filled `CONN_PINNED_HASH`. Go authority `tcp.HashingSink` wraps any sink + runs `crypto/sha256`. Host-verified (`tcp_conn_verify_test.go`): a multi-window body's `CONN_HASH` == `crypto/sha256.Sum256(body)` + the flag is 1 for a correct pin / 0 for wrong pins. All `NETBOOT_HOSTTEST`-gated → the bootable `netboot_http_boot.bin` is byte-identical (ends `&FA52`, independently re-verified). **Not yet wired:** the bootable `http_main` doesn't yet *call* `conn_verify_init`/`final` around the fetch or skip the HTTP header before the first hashed flush — that on-hardware path comes with the downloader.
  - **✅ DONE — cdn.githubraw.com path builder** (the i100 downloader's first brick): `src/netboot/fw_source.asm` `fw_build_path` concatenates `/<owner>/<repo>/<sha>/<path>` (e.g. `/raspberrypi/firmware/<sha>/boot/start4.elf`) into `FW_PATH` from the `FW_OWNER`/`FW_REPO`/`FW_SHA`/`FW_REPOPATH` config. Host-verified vs the Go authority `http.GithubRawPath`.
  - **✅ DONE — the pinned manifest** (the downloader's second brick): `fw_source.asm` `FW_MANIFEST` — the Tupfile's 6 files at `FW_SHA`, each a `{ name ptr, in-repo-path ptr, pinned SHA-256 }` record, walked by `fw_manifest_entry` (BC=index → BC=record ptr). Go authority `http.RPiFirmware` (`http/manifest.go`); the pinned SHA-256s + sizes are verified to be the real file content at the commit (`manifest_test.go` re-hashes the local files when present; baked 32-B hashes, the multi-MB blobs stay out of the repo); the Z80 table is byte-compared to Go by `TestFWManifest`. Both bricks are standalone leaves → the bootable image stays byte-identical.
  - **✅ DONE — download plan / file selection (Go authority):** `http.Manifest.Plan(selection)` → the ordered `FetchSpec{Name, Path, Host, SHA256}` list the per-file fetch loop executes; `Manifest.SelectByName` resolves user-chosen names → indices. The picker's backend + the authority the Z80 multi-file loop will mirror (`http/manifest.go`).
  - **NEXT INCREMENT (the coupled, `&10000`-ceiling-sensitive brick — best with fresh context) — wire the downloader into the bootable fetch:** make `http_main` drive the manifest's per-file loop — for each selected file (`Manifest.Plan` is the authority), `fw_build_path` its cdn path + host=`cdn.githubraw.com`, fetch over the verifying streaming sink (`conn_verify_init`/`final` + the header-skip `bodySink` Z80 port; Go authority `http/fetcher.go::StreamTo`/`bodySink` already host-tested), check the file's pinned hash from `FW_MANIFEST`. This is the brick that brings `fw_source` + the streaming sink + the SHA-256 verify **into the bootable image** — mind the `&10000` ceiling (the #264 lesson: the streaming buffers + SHA-256 are sizeable; the bootable currently stubs them out behind `NETBOOT_HOSTTEST`, and the i99 stream should *replace* the 16 KB `STAGING`, not add to it). Then (c) the **revision + file-subset picker UX** drives the FW_* config + the selection. Reference pin: `~/git/spectrum4/src/spectrum4/firmware/Tupfile`.
  - then the **record-spanning persist** (q16 bounded-`HSAVE`-per-record) — the spanning-file naming/reassembly convention; the real RST-8 `HSAVE` is hardware-gated.
  - **i88 (TLS→GitHub) is the LOWEST-priority item** — agents build it ONLY when nothing else is left to do; scope reduced to TLS-1.3-crypto-only (github.com-only; content already SHA-256-pinned, so cert verification is skippable).
  The real RST-8 `HSAVE`-per-record + the end-to-end on-hardware fetch stay **Pete's test** (CLAUDE.md §5). **Separately:** i7 phase **D = i74** stays gated (do not start); i77 parked; q13 + i81 + hardware artifacts (i87/i89) + q14 (monitor live-test) wait on Pete (non-blocking); don't pivot to editor (M9/i78/q13 — Pete's interface thinking pending).
- Every strand keeps the **assembled-binary byte-identical** invariant (the `release-gate` 3-way byte-match); the i39 invariant is binary-identity + round-trip + `.tbn`-shrinks-or-holds, NOT `.tbn` byte-identity.
<!-- HANDOVER-PROTOCOL-END -->

Canonical index of milestones, design specs, and deferred work. **Update this any time a design doc is added, a milestone changes state, or deferred work gets folded into a milestone.**

## Vision

A SAM Coupé Z80 program that hosts a complete aarch64 development workflow — editor, assembler, disassembler, TFTP shipper — and produces byte-identical binaries to GNU `aarch64-none-elf-as` for the spectrum4 kernel. Daily-driver SAM-as-development-machine for Raspberry Pi 400 bare-metal work.

See `docs/specs/vision.md` for the long-form pitch and `docs/specs/phase1-assembler.md` for the Phase 1 (assembler) shape.

## Milestone status

| M | Title | Spec | Status doc | State |
|---|---|---|---|---|
| M0 | Toolchain bootstrap (pyz80 → SimCoupé → samfile → GNU `as` round-trip) | — | — | ✅ done (PR #1) |
| M1 | Binary tokenised source format (`.tbn`) + text2bin / bin2text | — | — | ✅ done (PR #6) |
| M2 | Encoder tables + Mac-side refenc; 20/20 M1 fixtures byte-match GNU | — | — | ✅ done (PR #7, #8); extended via #11, #14, #15, #17 |
| M3 | Z80 emitter: read `.tbn`, encode, HSAVE output (no symbol table; constant-only) | — | — | ✅ done (PRs #9, #12, #13, #16, #17, #19); 9/9 fixtures byte-match GNU end-to-end via SimCoupé |
| M4 | Symbol table, multi-pass, full expression evaluator on Z80 | — | — | ✅ done (PRs #21, #22, #23); 4/4 M4 fixtures byte-match GNU end-to-end via SimCoupé |
| M5 | Compound operands (`OpShiftedReg`, `OpExtendedReg`, `OpMem`, `OpSysName`, `OpLitPool`) + remaining directives (`.set`/`.equ`, `.balign`/`.align`, `.org`, `.skip`/`.space`, `.inst`, `.ltorg`) + `ror`-imm intercept | — | — | ✅ done (PRs #29–#34); 19/19 M5 fixtures byte-match GNU end-to-end via SimCoupé |
| M6 | Paged OUT + paged IN + compact `.tbn` format + built-in disassembler (real spectrum4 fixture round-tripping byte-identical via SAM) | `docs/specs/paged-out-design.md` + `docs/specs/paged-in-design.md` | — | ✅ done (PR #76) — paged OUT/IN, `paged_call`, multi-digit local labels, off-axis tables, release-stripped flatten, Go harness all landed (PRs #29-#73).  **Headline "spectrum4 release.bin byte-match on SAM" DONE 2026-05-29**: the full 88 KB release-stripped flows through SimCoupé and the SAM prod assembler HSAVEs a 21752-byte OUT byte-identical to GNU (8 encoder bug classes found+fixed, 358→0; PR #73).  **Closed by PR #76**: the `release-gate` CI gate (a hermetic 3-way byte-match — GNU == our Go toolchain == our Z80/SAM toolchain — over a vendored flattened release source, `tools/run-release-gate.sh`) + the `&C000` code-budget assertion now stand guard.  Compact `.tbn` + on-SAM disassembler + codegen tables move to M7. |
| M7 | Post-M6 consolidation: on-SAM disassembler (strand B) + compact `.tbn` (i1) + codegen tables + repo-cleanup/housekeeping + parity/robustness + Go-harness fidelity | — | — | ✅ done — headlines: on-SAM disassembler (PRs #93–#103), compact `.tbn` i1 (PRs #121–#124, −42.3%). Tail items i7/i17/i18 live in the item registry. |
| M8 | Next-gen compact `.tbn` **v2** — the instruction-overlay format (Format B): assembled-word + zeroed-bits + sparse expression overlay unifying literal/symbolic instructions; header label/offset table; one file with an evictable editor region; name front-coding. Foundation for the on-SAM editor. | `docs/specs/compact-tbn-nextgen-design.md` (design, §7 decisions) + `docs/specs/tbn-binary-format-reference.md` (v1 baseline) | — | ✅ done (closed 2026-06-12) — v2 overlay format shipped end-to-end: i39a/#131, i39b-1/#151 + i39b-2/#153, i40 + i51/#181, i48 host side/#141–#144. The B-DOS era opened at close: i71/#186 (reference vendored) + i72/#187 (`build-disk -dos` prep). Residuals migrated to M9 (i39c → i48c). |
| M9 | Editor era — Phase 2 foundations: codegen tables (i7), B-DOS boot-disk swap (i75), paged block-list edit model (i41), SAM-side text→overlay encoder (i48c, absorbs i39c), read-only viewer (i4), UI decisions (i6/i5/q1) | `docs/specs/editor-edit-model-design.md` + `docs/specs/comment-storage-design.md` + `docs/specs/codegen-tables-design.md` | `docs/notes/m9-status.md` | ⏳ active |
| (Phase 2) | On-SAM editor | `docs/specs/phase1-assembler.md` §editor + `docs/specs/editor-vision.md` (design pointers) + future Phase-2 spec | — | ⏳ started — M9 carries the foundations |
| (Phase 3) | SAM as aarch64 netboot server (Quazar Trinity) — DHCP+TFTP to any Pi, over a direct cable or a shared LAN | `docs/specs/phase3-delivery-design.md` (i84 — **design confirmed** Pete 2026-06-14) + `docs/plans/phase3-netboot-implementation-plan.md` + `docs/notes/netboot-trinity-testing.md` (on-hardware guide) | — | ⏳ implementation underway — **host oracle harness** (i91/#219), the **Z80 packet build/parse logic** (i92), the **i80 Trinity emulation + the three wire-I/O transfer state machines** (DHCP responder / TFTP server / TFTP client — host-verified byte-for-byte over the emulation), both **seams** (i82 originate front + i93 B-DOS store), the **bring-up smoke test** (i94, `make netboot-smoke-disk`), the **integrated netboot server** (i95 — `netboot_serve_once`, the bootable `make netboot-server-disk`, host-verified over the i80 emulation byte-for-byte vs `server.Server.OnFrame`), the **serve-files TFTP demo** (i96 — a plain-TFTP server, ARP+TFTP only, `make netboot-serve-disk`, testable with a stock `tftp`/`curl` client; bare-RRQ→DATA per RFC 2347 + optioned-RRQ→OACK, host-verified vs `serve.Responder.OnFrame`), and the **TFTP client boot disk** (i82 — `make netboot-client-disk`, fetches a `.mgt` from a TFTP server + HSAVEs it to Trinity storage; the ARP→RRQ→DATA/ACK wire side host-verified vs `client.Client`, the B-DOS HSAVE write-out hardware-gated) all landed. Design confirmed (i84, q11/q12): Model A; SAM=DHCP+TFTP server serving by-name; both topologies; on-SAM HTTP self-provisioning. **Remaining (autonomous, unblocked):** HTTP (i70) + HTTPS (i88). The protocol logic + ENC28J60 wire I/O are host-verified over the i80 emulation; an **end-to-end Pi boot + real-silicon timing + the RST-8 hook dispatch stay gated on real Trinity** (CLAUDE.md §5). Reference: `simonowen/trinload` |

Legend: ✅ done · ⏳ in progress · 📋 designed, not started

## Design notes not strictly inside a milestone

These are patterns or research findings that get applied *within* milestones rather than constituting their own milestone:

| Note | When to apply | Status |
|---|---|---|
| `docs/ARCHITECTURE.md` | **The first read**: the synthesized system overview (system shape, authority model, encoder tables, memory + paging, `.tbn` v2, build/test pipeline, dev inner loop). Each section links its deep spec. | ✅ living overview |
| `docs/specs/tbn-binary-format-reference.md` | The single authoritative reference for the complete `.tbn` binary encoding (header, records, operands, expr bytecode, directives, compaction levels). Read/cite this whenever touching the format; supersedes the retired M1 spec's tables. | ✅ living reference |
| `docs/specs/samdos-file-io.md` | Reference for the DOS file-I/O hook layer (SAMDOS 2 / B-DOS): the HLOAD trampoline pattern (READ), the HSAVE UIFA pattern (WRITE), hook register-clobbering facts, pre-built Z80 snippets. | ✅ living reference |
| `docs/notes/sam-stub-audit.md` | Already applied (PR #13 loader fix used findings). Keep as reference for any future DOS hook work. | ✅ applied; reference |
| `docs/notes/sam-paging.md` | Reference for any LMPR/HMPR work. | ✅ reference |

## How to extend this doc

When new deferred work or a design decision arises:
1. Add an `iN` row in `docs/notes/item-registry.md` (or `qN` in `docs/notes/question-registry.md` for open questions). That is the one home — do not add a checklist here.
2. Add the new design note to the appropriate table above (milestone, or "Design notes not strictly inside a milestone") and link it from `docs/specs/README.md` if it lives under `docs/specs/`.

The goal: no design discussion is ever lost to context drift. If it's worth writing down, it's worth a registry row.
