# sam-aarch64 roadmap

<!-- HANDOVER-PROTOCOL-START -->
## START HERE — session handover protocol (read this first)

**This file is the canonical entry point and the agent→agent handover contract.** If you are an agent picking up this project: read this section, then the **"Current State"** block below, then the current milestone status doc, then the project memory index (auto-loaded at session start). That is the complete start-up read — everything else is reached via links from here.

### The contract (always inherited, always maintained)

1. **Tracking is part of "done" — and it happens at REQUEST time, not PR time.** The moment a piece of work is well-defined *and* you have sign-off to do it, create its `iN` row in `registry/items.yaml` (or `qN` in `registry/questions.yaml`) **before you start implementing** — `build/registry add …` (the id is auto-allocated; there is no `next-id`, no `--id`) then `make registry`, committed **locally on your branch** (no separate PR/merge dance just to capture — it rides the work's PR or the wind-down sweep). Mark it **`IN_PROGRESS`** and **say so out loud**: "logged as iNNNN, marked IN_PROGRESS, now working on it." So intent survives a crash mid-work, and many parallel asks become many `IN_PROGRESS` rows — **a feature, not a violation: the registry is the session's completeness ledger** (query `build/registry in-progress` at wind-down — see hygiene). IN_PROGRESS is **transient and session-local — `main` never carries it**: before a PR **merges**, resolve every entry (DONE for finished work, `split` into a DONE part + a fresh OPEN part for partial, or back to OPEN if untouched), so what lands on `main` is *resolved* states (DONE/OPEN), never a wall of IN_PROGRESS. (A fleeting IN_PROGRESS commit may sit in a feature branch's history — a self-completing PR can't know its own number before the first push — but the merged result is always clean; that's the light, watertight line, no history-rewrite gymnastics.) A PR completes the IN_PROGRESS items it finished — and **one PR may complete several** (an item is one deliverable; a PR is one cohesive reviewable change). Treat a missing tracking entry like a missing test: reviewers flag it. **This is the agent's responsibility, never Pete's** — including for work you merely *raise* with him: asking "should we do X later / in a future session?" does **not** delegate tracking to Pete. Track X the moment you raise it (a future-session item is still an item); a question *to* Pete is itself a `qN`. Pete is not your tracker.
2. **One home per kind of thing** (this is how the docs stay un-sprawled):
   - **ROADMAP.md** (this file) — the index and the live "Current State" below.
   - **Item registry** — source: `registry/items.yaml`; generated views: `docs/notes/item-registry-open.md` + `item-registry-closed.md`. The project-wide, milestone-neutral `iN` id↔item map. A fresh agent reads the **open** view for outstanding work. The YAML is the source of truth — never hand-edit the generated `.md` (the `registry-sync` CI job fails if you do). Resolve an item with `build/registry set-status --id iNN --status DONE --pr N` then `make registry`, in the completing PR.
   - **Question registry** — source: `registry/questions.yaml`; generated view: `docs/notes/question-registry-open.md`. Questions are **transient**: every question for Pete goes into the YAML the moment it arises (`build/registry add --space questions …`); answering one means curating every dependent item (apply the decision), then deleting the question (`build/registry answer --id qNN`). There is no closed-questions view — the decision lives in the items + git history.
   - **Backlog** — `docs/notes/backlog.md` (generated from `registry/priority.yaml`). The priority-ordered list of open pullable items; `build/registry ready` returns the unblocked tip. This **supersedes** the prose "proposed implementation sequence" as the single source of work ordering.
   - **Milestone status docs** (`docs/notes/m{N}-status.md`) — the per-milestone source of truth.
   - **Memory** (`~/.claude/projects/.../memory/`) — cross-session preferences, facts, feedback. *Not* work-tracking.
   - Superseded docs are **deleted** — git history is the archive; pin citations to a blob link if a living doc must cite one.
3. **At milestone close**, walk the item registry and ask of each item: still deferred, or does it now fold into the milestone in flight? **The `iN` item registry and the `qN` question registry are both milestone-neutral and never archived:** at milestone close, every item belonging to that milestone must be marked `DONE`/`WONTFIX` (via `build/registry set-status …` then `make registry`) or migrated (re-pointed) to the active milestone's strands — no item may be left living only in a closing `m{N}-status` doc. Open questions for Pete live only in the question registry (`qN`) for the same reason. **The milestone status doc is deleted at close** (git history is the archive) after the registry walk completes.
4. **Delegate to subagents to preserve orchestration context.** The agent driving a session (the *orchestrator*) should push self-contained, well-scoped work out to subagents — codebase searches, mechanical edits, doc drafting, focused investigations, even whole low-risk PRs (implement → verify locally → commit → push → open PR) — via the `Agent` tool (and `Explore` / `Plan`), keeping only the conclusions in its own window. A subagent's full transcript is discarded; only its final summary returns, so the orchestrator's context lasts far longer. The orchestrator keeps the judgement calls itself: sequencing, monitoring CI, merge/launch decisions, and anything user-facing. The durable state lives in subagents-plus-these-docs, not in any one agent's window — so when context *does* fill, the handover (update "Current State", below) lets the next agent resume as orchestrator with nothing lost. Prefer launching independent subagents in parallel (one message, multiple `Agent` calls) when the work has no shared state.

### Session hygiene (the minimal ritual)

- **Continue vs wind down at a `/context` checkpoint:** the decision rule lives in **one** place — the `tools/autonomous-loop/monitor.sh` task-done nudge (`RESUME_NUDGE`), delivered at the decision point. Don't re-litigate it; follow the nudge. (It was i104's top friction — ~9 sessions re-litigated it — which is *why* the rule is encoded at the point of use, not restated here.)
- **Closing a session:** **no magic words** — *any* wind-down phrasing ("close the session", "let's wrap up", "prepare for handover", "goodnight, what's next?") triggers this. The agent then updates the **"Current State"** block below *in place* — what landed, what's in flight (open PRs / branches / running agents), key decisions/gotchas, and open Pete-questions. **It is state and context, NOT a work-ordering plan** — work selection lives solely in the registry (`build/registry ready`, the priority-queue tip; browse `docs/notes/backlog.md`). Never hand-write a "proposed next sequence" or "next tip = X" here: it duplicates the registry and inevitably drifts out of sync with it. The agent also confirms tracking is current (rule 1) and leaves the working copy on a clean, current `main`. Do **not** write a new dated handoff doc — update the standing block. **Commit this update directly to `main`** (it is doc-only — CLAUDE.md §4); don't leave the live state stuck in an open PR, or the next session reads stale context.
- **Before a PR merges — and at wind-down — reconcile every `IN_PROGRESS` item** (IN_PROGRESS is transient and never reaches `main`; rule 1). Run `build/registry in-progress`; for each row it lists, exactly one of: it is genuinely DONE (its PR merged → `set-status … --status DONE --pr N`); or you finished *part* of it → **`build/registry split`** the done part off as a DONE child and leave the rest a fresh OPEN child (capturing the true partial state); or it was untouched → set it back to `OPEN`. The list must end holding only what is *actually* still in flight. This is the completeness check that guarantees no signed-off ask was lost in the chat scroll (rule 1 / i154).
- **At wind-down, also write a per-session retro report** to `~/claude-retro/` (item i104): a short, blameless, *independent* note (don't read other sessions' reports first) on genuine process friction this session — *say what genuinely happened: no manufactured problems, no suppressed ones*; **"nothing notable" is a valid, good report**; ground each friction in a specific incident. Format + rationale: `~/claude-retro/README.md`.
- **Starting a session:** the standard start prompt is just **"Continue per docs/ROADMAP.md."** A SessionStart hook (`tools/session-handover.sh`) runs first: it surfaces this section and, **when you are on a clean `main`, auto-`fetch`es and fast-forwards to the latest** so the doc you read is current. If it instead **warns** (you are on a feature branch, have uncommitted changes, or `main` can't fast-forward), refresh deliberately before relying on the doc: stash/commit your work, then `git checkout main && git pull --ff-only`. Then read this section → Current State → resume; no bespoke per-session prompt is needed.

### Current State

*The live handover, updated in place each session: what's true now, what's in flight, key decisions/gotchas, and open Pete-questions. **It is state and context, NOT a work queue** — what to work next is `build/registry ready` (the authoritative priority-queue tip), never a list written here. Keep it tight; history lives in the milestone status docs, the registries, and `git log`.*

- **Milestone:** **M9 active — the editor era** (`docs/notes/m9-status.md`). M0–M8 ✅ complete. The **Phase-3 netboot** and **SAMBOOT** strands run alongside.
- **✅ RESOLVED (2026-06-29) — the Trinity SD-card WRITE HANG is FIXED and HARDWARE-CONFIRMED (Pete's #1).** Root cause (PR #748, i280b-b2t, §8af): the B-DOS raw sector hooks HWSAD(149)/HRSAD(160) device-select on `hk.a` = the caller's **main A** at the `rst 8` (the §8h/#730 "hk.a = alternate A'" finding was a `&01CB` reboot-artifact, refuted in the faithful Continue/WTKY2 rig). Our serve left main A = the **sector number**, so the first `.mgt` sector (1) selected the **floppy** (`&8662 cp 1`) → the un-timed FDC poll = the hang. Fix: present **A=2** (Trinity SD) before the `rst 8` in `bdos_seam.asm`. **Hardware shot (i283) confirms it:** markers `HWSAD_PRE → HWSAD_POST` repeat across blocks through `FINALIZE` — the per-block write completes on real silicon (it hung `HWSAD_PRE → silence` for the whole §8a–§8w investigation). The §8a FDC-poll model was right; §8c's "A=2 refuted" was a §8d-corrected upstream-handshake misattribution (the handshake was later fixed by b2d/#728, which is what made the write reachable). This closes **i280 / i280b / i280b-b2 / i280b-b2i / i283** (all DONE). The full §8a–§8ae blow-by-blow lives in `docs/notes/trinity-sd-z80-interface.md` §8 + git history. **NEXT: i194** — push a REAL 800K `.mgt` end-to-end (FINALIZE_VALID + final ACK to curl = a genuinely written record); the synthetic test payload only exercised the per-block write step (FINALIZE rejects a non-800K image by design). New harness primitive `Machine.ContinueFrom` (faithful injected-code driver) landed with #748. Earlier this session: #715–#732 (TAPO/i266, i280a, the b2b–b2h localization), #746/#747 (the faithful rig + the gold-write capture).
- **⚠️ FULL-DISK push (cj.mgt / i194 / i284) is BLOCKED on a SEPARATE hardware hang — q62 (2026-06-29, §8ag).** Pete's follow-on goal: write a real 800K `.mgt` (his CJ's Elephant remake `boot_m2b.mgt` → `cj.mgt`) to a free record and **boot it** (write-then-boot = full autonomy). Landed toward it: **#750 (i285)** — disk-record validation is now **size-only** (the `BDOS`@232 stamp gate was invalid: a `.mgt` needs no B-DOS installed on it; the stamp is B-DOS's catalog signature, still read by `bdos_inspect_record` for the overwrite-safety gate, not a boot/selection requirement — this had been analyzed+implemented 2026-06-21 but the code was lost on no branch, restored here); **#751** fixed the client-side `client_finalize` test #750 missed (netboot-z80 was briefly red on main). **The blocker:** the real-hardware `cj.mgt` push **hangs (`curl exit 28`)** for BOTH production and debug serve; the debug serve localizes it to the **claim's HRECORD select** (`CLAIM_SELECT_PRE`, no `CLAIM_SELECT_POST`) — BEFORE the per-block write. Same shape as §8af one hook earlier: our OWN raw CMD17 (`bd_list_read_hw`) self-heals + works, but the **B-DOS hooks** (HRECORD claim, HWSAD write) rely on SPI-mode persistence the serve's `drv_init` ENC `ereset` disturbs (the §8d/§8m shared-one-PIC class) → they hang at `&A7CC`. NOT an A=drive bug (HRECORD's contract is `A=0`). It is **flaky** (the §8af synthetic-payload shot's claim+write succeeded; the full 1600-block push hangs) and **not emulation-reproducible** (the SD model self-heals/clears BUSY; the full WRQ push + `serve_main` idle-loop are green in emulation). **q62 asks Pete the architecture direction** (own self-healing SD ops for the push vs B-DOS hooks vs the §8x scope path); **i194 + i284 depend on q62.** Detail: `docs/notes/trinity-sd-z80-interface.md` §8ag. **ACTIVE PATH (the session goal-hook directs continuing toward cj-install): q62 LEANS option (a)** — bypass the B-DOS hooks with our own self-healing ops. **Key de-risk:** `bd_list_write_hw` (sd_csd.asm) is already a self-healing CMD24-to-absolute-LBA write, so the record write is "set the LBA to the record's data block (`csd_base + 1600*(record-1) + i`) and reuse it", not a from-scratch reimplementation. **NEXT (fresh-context session — do NOT write this data-sensitive SD code at deep context): implement option (a) EMULATION-FIRST** (add `bd_record_write_sector` reusing `bd_list_write_hw`; rewire `raw_record_sink`/`wd_finalize` off HWSAD; drop the HRECORD select + the HRSAD validation read; keep `bdos_claim_record`); emulation-verify right-blocks/right-bytes/**free-record-only** (data safety). **The hardware deploy is PETE-GATED** — it writes a real `.mgt` to the shared SD card via a reimplemented path, so Pete signs off the direction + data safety (`trinity_storage_shared_resource`) before the first hardware push. Full executable plan in q62's body.
- **Systemic prevention captured this session:** **i273** — *the Z80/Colin SAM code is the authority for Trinity HW (EEPROM/SD/Ethernet); the Go model is DERIVED from it, never the authority* (the recurring rot: i270, i145g, i249/250/251). **i274** — a retrospective holding a **10-pattern catalog** of what keeps going wrong. **i271** net-debug channel (SAM reports state over UDP — the agent can't see the border under trinload), **i272** agent hardware-session autonomy (passwordless tcpdump/arping + allowlist, *owner:pete*), **i277** trinpush ergonomics, **i278** trinload reliability audit.
- **🔌 TAPO IS LIVE — the agent self-serves SAM hardware (i266 DONE, #718).** `~/bin/tapo.sh on|off` power-cycles the SAM; it boots into trinload **~80 s** after power-on (auto-boot record 3). **tapo.sh enforces a global 10 s minimum between any two power events** (a `flock` + `/var/tmp/tapo-last-power-event` timestamp, cross-process; real script `~/git/tapo-plug/tapo.sh`). **The SAM's physical back-switch must stay ON** (Pete left it on — a `tapo on` is a no-op if it's off). **Turn the SAM OFF when idle** (`tapo.sh off`) — it is off now. **Hardware shot recipe:** `tapo.sh off` → (≥10 s) `tapo.sh on` → wait ~80 s for trinload → `DEPLOY_CHECKED=1 python3 tools/trinload-push/trinpush-serve.py 192.168.2.75 --strategy highest --bin build/netboot_serve_boot_debug.bin --map build/netboot_serve_boot_debug.map` (poll until the discovery `!` reply — boot is slow) → `curl --max-time 25 -T file tftp://192.168.2.75/trinity-sam-disks/x.mgt` → read **UDP:9001** markers via a plain listener (no sudo; i271). A write-hang **wedges** the serve → power-cycle to recover. (`sudo tcpdump`/`arping` still need a password — i272's setcap is Pete's host action, but the marker listener removes that need.)
  - Follow-up items filed: **i268** (the SAM deploy-guard hook false-fires on plain `go test` commands that merely contain a protocol keyword like "tftp" — tighten the matcher), **i269** (remove the now-dead `SAMBOOT_BOOTBLOCK` conditional gating + stale comments left in the shared `eeprom.asm`/`bdos_seam.asm`/`samboot_config.asm` after i265 — composes with i231b because it touches the carve-out ledger).
  - **Gotchas for the next session:** (1) the old `samboot_inject_fullboot_test.go` private-ROM "HYPOTHESIS" `t.Error` that used to fail locally is **GONE** (deleted by i265) — the netboot z80 suite is clean locally now; still run it with `SKIP_PRIVATE_TESTS=true` to mirror CI for the *other* capture-gated tests. (2) `go.work.sum` is now **gitignored** (#705) — a broad `git add -A` will no longer sweep it onto `main`; don't re-add it.
  - **Deferred for the next (fresh-context) session:** (1) **q62 (Pete) gates the cj.mgt/i194/i284 disk push** — the full-disk push hangs in the B-DOS claim/write hooks (§8ag); needs Pete's architecture call (own self-healing SD ops vs B-DOS hooks vs the §8x scope path) before i194/i284 can proceed; (2) **implement i281** TAPO automation (monitor on-time tracking + escalating "still need it?" pings at 30/60/.../180 min + 3 h auto-off backstop + an agent keep-alive + idle-off guidance — the 10 s guard is already done in tapo.sh); (3) **owner-audit** hardware-gated items now that hardware is agent-self-serve (most are already owner:agent — workable now; i172 covers the i133-dependents audit); (4) **q60** raised — spill-backend storage design, gates i215c. Work selection remains `build/registry ready` (tip = i270a; i194/i284 are q62-gated).
- **SAMBOOT (the configurable-boot strand). 🎉 MILESTONE (2026-06-26): the SAM now auto-boots straight into trinload on power-on — unattended, no keyboard (i135c DONE, #686).** Power-on → opening screen → auto-boot SD record 3 (trinload) → reachable on the network at `192.168.2.75`. This is the configurable-boot crux: flash once, and the SAM comes up ready for remote work. **READ `docs/specs/samboot.md §3` + the `feedback_samboot_architecture_grounding` memory FIRST** — the architecture: a **forked physical SAM ROM chip** (unchangeable) bootstraps the **writable Trinity EEPROM bootblock**, which is *the thing we patch*. Bricking is impossible (never touch the ROM chip; SPACE-held bypasses the EEPROM boot; the EEPROM reflashes from a floppy-booted B-DOS with the captured `eeprom.bin` backup; Pete also has factory-ROM reflash). **Capture backed up + checksummed** (`~/sam-archive/samboot-capture` + a copy on `Petes-MacBook-Pro`; sha256s at `docs/notes/samboot-capture-sha256sums.txt`).
  - **The bootloader is a PRIVATE LOCAL FORK (q55):** it lives in the sibling repo `~/git/trinity-autoboot` (Colin Piggot's SR23 boot block, reconstructed byte-exact as `colin_base.asm` via `make verify`, + our opening-screen/auto-boot patches), **built locally, kept out of this repo and CI** (Colin's bytes are private). The in-repo splice-into-captured-bootblock approach is **ABANDONED** (i259/i260/i261/i230 WONTFIX) — the fork owns the source and inserts our code cleanly (no splice, no register-preservation hacks).
  - **The flasher** (`src/netboot/eeprom_flash_chunk1.asm`, this repo) writes the fork's `bootloader.bin` into EEPROM chunk 1 via the i225/i226 hardware-proven `write_chunk` path + read-back verify, reporting PASS/FAIL over UDP. The embedded bootloader data (`bootloader_chunk1_data.asm`) is gitignored + regenerated by `make gen-bootloader-data`; the test is `SKIP_PRIVATE_TESTS`-gated; the build is local-only. To re-flash an updated bootloader: rebuild it in the sibling repo, `make gen-bootloader-data`, `make netboot-eeprom-flash-chunk1`, then push via `tools/trinload-push` (DEPLOY_CHECKED=1) — confirm the SATR PASS on a `:9000` UDP listener, power-cycle.
  - **The two gaps to full autonomous SAM control:** **(1) nice boot-record selection — i264** (umbrella). **i264a DONE (#687, emulation):** the forked bootloader is now config-driven — hold ESC at power-on → manual control (opening screen → BASIC); else auto-boot the configured record, falling back to record 3 (trinload) when no config chunk exists (preserves today's behaviour). The ESC scan uses the stock ROM's `BRKTST` mechanism (row &F7 / STATPORT &F9 bit 5), emulation-verified. **i264b OPEN** = the record-picker UI (list records, select by number/name, persist the `SAMBOOT Config` chunk via the EEPROM write path; design fork: picker in-chunk vs B-DOS scratch page vs a dedicated setup record). **(2) writing disks to Trinity storage — i194** (push a `.mgt` → free Trinity record; **rank 1** per Pete). **TAPO is now installed (i266 DONE)** — the agent power-cycles the SAM itself (`tapo.sh`), so the i264a re-flash + i194 + all hardware flash/test are **agent-self-serve** (no longer Pete-present-gated). i194's blocker **i280 (the write fix) is now DONE + hardware-confirmed** (2026-06-29, see the RESOLVED bullet above) — i194/i284 now hit a SEPARATE hardware hang in the B-DOS claim/write hooks (§8ag) and are gated on q62 (Pete's architecture call).
- **Hardware-readiness: COMPLETE.** All six hardware fixes (leading `&FF` flush, drv_init-before-SD, enc_rx_reestablish, tr_terminate clean-exit, bounded SD busy-wait, Colin's 4-step deselect) are on serve_main/client_main, and the emulator now **fails** when #3/#5/#6 are missing (i249/i250/i251). The "no hardware until readiness lands" gate is satisfied → hardware (Test B, the disk-push experiments) can resume when Pete is at the SAM. SAM @ `192.168.2.75`. The **deploy-guard hook (i252)** denies any SAM-deploy command without `DEPLOY_CHECKED=1` + the checklist; power-cycle the SAM before any shot (it may be Test-B-crashed). **Standing directive:** emulation-prove every hardware-gated path (esp. destructive ones) and stage/verify the full capture path before Pete is at the SAM.
- **SAM-assembler-code strand is BUDGET-LOCKED behind q54** (Pete's section-C budget-relief architecture fork): i204b, i27b (errata 835769), and i27c (errata 843419 + toggle) all add inline section-C code that blows the ~20 B `&C000` headroom, and commoning is exhausted. **i234** (the relief) depends on q54. b8e encoder bricks remaining: i203/i204.
- **Editor / i48c:** lexer + parser front-end COMPLETE; only **B8** remains (b8a/b8b done; b8e encoder in progress; b8c/b8d). i41 edit-model: Go authority + Z80 Bricks 1/3/4a done; **i41d Brick 2** (real SAM paging via `OUT (251)`, host-verifiable in the `sampage` harness) + Brick 4b remain — needs a `split` when done. i119 free-record-detection: read list sectors directly + confirm-before-overwrite (`docs/specs/trinity-record-detection-design.md`).
- **Open questions awaiting Pete:** **q13** (editor default rendering, non-blocking), **q45** (the `emit_byte` hot-path fork → gates i24), **q46** (multi-source-file shape → gates i32), **q48** (`.section` handling), **q53** (tip-clutter disposition — now also gates **i104**/i117a, recurring-practice + do-not-pick items that clog the `ready` tip), **q54** (the budget-relief fork → gates i234/i204b/i27b/i27c), **q56** (publish trinboot open-source? → gates i177; q55 kept it private for now), **q57** (i106 SAM-as-terminal SSH/telnet — go/no-go + security posture: deliberately-weak KEX-stub vs real X25519 vs telnet → gates i106), **q58** (i126 comprehensive-emulation — fork strategy / start-now-vs-defer / keep-Go-harness → gates i126; grounded decision spec `docs/specs/comprehensive-emulation-design.md` decomposes the SimCoupé-fork + Trinity-port north-star into ordered increments).
- **Hardware tests are now AGENT-SELF-SERVE (TAPO live).** Items needing a real-Trinity shot — i194 disk push (gated on i280 write fix); i264 selector flash/test; i95/i101 DHCP + Pi-boot; HSAVE `RST 8` write-out (i119/i70/i93/i167); i88 6b; i100c picker UX — the agent runs them itself (power-cycle + push + read :9001 markers). Genuine **design** questions still need Pete (the qN list). **Emulation-verified ≠ hardware-verified** (CLAUDE.md §5/§7); emulation-prove a path before the hardware shot.
- **Environment + gotchas:** this host (Pi) has **no Docker**, but **SimCoupé IS installed natively** (`/usr/local/bin/simcoupe`, runs headless via `SDL_VIDEODRIVER=offscreen` — `tools/run-simcoupe.sh`) **and both aarch64 binutils toolchains are on PATH** (`aarch64-none-elf-*` + `aarch64-linux-gnu-*`). So SimCoupé- and binutils-gated work **runs locally here** — do NOT defer it as "CI-only / needs Docker" (the recurring mistake recorded in `feedback_simcoupe_runs_in_docker.md`; isolate `HOME` per that note to avoid clobbering Pete's `~/.simcoupe` cfg). The Go harnesses (netboot-oracle Z80 + the shared `tools/sampage`) also run locally. **i207 lesson:** the Go harness PRE-LOADS payload pages, so a missing-from-disk payload passes the harness but HANGS SimCoupé — **SimCoupé-verify any boot-payload/disk-wiring change** (locally or via CI). Every strand keeps the **assembled-binary byte-identical** invariant (the `release-gate` 3-way byte-match); the i39 invariant is binary-identity + round-trip + `.tbn`-shrinks-or-holds, NOT `.tbn` byte-identity.
- **Testing policy — no silent skips (i253 DONE, #680).** A test fails-or-runs; the only sanctioned skip is the explicit `SKIP_PRIVATE_TESTS` gate (Colin's non-redistributable captures), enforced by `make check-no-silent-skips` (a `staticcheck`-job step). Policy in CLAUDE.md "Testing policy"; the [[feedback_no_silent_skips]] memory. **Forward goal i254 (Pete-gated):** run ALL tests in CI — materialise the proprietary captures from encrypted GitHub secrets (`SKIP_PRIVATE_TESTS=true` the CI default; our repo overrides), and add the currently-CI-excluded dev-harness packages (`z80-test-harness-go`/`zx0-greedy`/`editor-prototype`) to CI (for the zx0 decode-bench, install the external `zx0` or switch the Makefile `zx0-blocks` target to our own Go compressor). Only Pete can create the secrets.
- **Autonomous-loop monitor:** while the `~/.claude/autonomous-loop/pete-present` marker exists, the monitor suppresses **only** the time-based hang nudge (i240/#676/#679) — the file-driven `task-done`/`wound-down`/`quiescent` transitions still fire, so Pete can wind a session down without marking himself away. `ready` also surfaces Pete-owned items first when the marker is set. The SAM-deploy guard now fires only on actual push **execution**, not mere mentions (i256/#681). The monitor now **structurally enforces stop-after-merge (i147/#688)**: it watches `main` for newly-landed PR merge commits and synthesizes `task-done` if a merge lands with no checkpoint pending (autonomous-mode only), so a forgotten `touch` can no longer let a session accumulate merges into one polluted context — takes effect when the monitor is next restarted.
<!-- HANDOVER-PROTOCOL-END -->

Canonical index of milestones, design specs, and deferred work. **Update this any time a design doc is added, a milestone changes state, or deferred work gets folded into a milestone.**

## Vision

A SAM Coupé Z80 program that hosts a complete aarch64 development workflow — editor, assembler, disassembler, TFTP shipper — and produces byte-identical binaries to GNU `aarch64-none-elf-as` for the spectrum4 kernel. Daily-driver SAM-as-development-machine for Raspberry Pi 400 bare-metal work.

See `docs/specs/vision.md` for the long-form pitch and `docs/specs/phase1-assembler.md` for the Phase 1 (assembler) shape.

## Milestone status

| M | Title | Spec | Status doc | State |
|---|---|---|---|---|
| M0 | Toolchain bootstrap (pyz80 → SimCoupé → samfile → GNU `as` round-trip) | — | — | ✅ done (PR #1) |
| M1 | Binary tokenised source format (`.tbn`) + text2bin / bin2text | — | — | ✅ done (PR #6) |
| M2 | Encoder tables + Mac-side refenc; 20/20 M1 fixtures byte-match GNU | — | — | ✅ done (PR #7, #8); extended via #11, #14, #15, #17 |
| M3 | Z80 emitter: read `.tbn`, encode, HSAVE output (no symbol table; constant-only) | — | — | ✅ done (PRs #9, #12, #13, #16, #17, #19); 9/9 fixtures byte-match GNU end-to-end via SimCoupé |
| M4 | Symbol table, multi-pass, full expression evaluator on Z80 | — | — | ✅ done (PRs #21, #22, #23); 4/4 M4 fixtures byte-match GNU end-to-end via SimCoupé |
| M5 | Compound operands (`OpShiftedReg`, `OpExtendedReg`, `OpMem`, `OpSysName`, `OpLitPool`) + remaining directives (`.set`/`.equ`, `.balign`/`.align`, `.org`, `.skip`/`.space`, `.inst`, `.ltorg`) + `ror`-imm intercept | — | — | ✅ done (PRs #29–#34); 19/19 M5 fixtures byte-match GNU end-to-end via SimCoupé |
| M6 | Paged OUT + paged IN + compact `.tbn` format + built-in disassembler (real spectrum4 fixture round-tripping byte-identical via SAM) | `docs/specs/paged-out-design.md` + `docs/specs/paged-in-design.md` | — | ✅ done (PR #76) — paged OUT/IN, `paged_call`, multi-digit local labels, off-axis tables, release-stripped flatten, Go harness all landed (PRs #29-#73).  **Headline "spectrum4 release.bin byte-match on SAM" DONE 2026-05-29**: the full 88 KB release-stripped flows through SimCoupé and the SAM prod assembler HSAVEs a 21752-byte OUT byte-identical to GNU (8 encoder bug classes found+fixed, 358→0; PR #73).  **Closed by PR #76**: the `release-gate` CI gate (a hermetic 3-way byte-match — GNU == our Go toolchain == our Z80/SAM toolchain — over a vendored flattened release source, `tools/run-release-gate.sh`) + the `&C000` code-budget assertion now stand guard.  Compact `.tbn` + on-SAM disassembler + codegen tables move to M7. |
| M7 | Post-M6 consolidation: on-SAM disassembler (strand B) + compact `.tbn` (i1) + codegen tables + repo-cleanup/housekeeping + parity/robustness + Go-harness fidelity | — | — | ✅ done — headlines: on-SAM disassembler (PRs #93–#103), compact `.tbn` i1 (PRs #121–#124, −42.3%). Tail items i7/i17/i18 live in the item registry. |
| M8 | Next-gen compact `.tbn` **v2** — the instruction-overlay format (Format B): assembled-word + zeroed-bits + sparse expression overlay unifying literal/symbolic instructions; header label/offset table; one file with an evictable editor region; name front-coding. Foundation for the on-SAM editor. | `docs/specs/compact-tbn-nextgen-design.md` (design, §7 decisions) + `docs/specs/tbn-binary-format-reference.md` (v1 baseline) | — | ✅ done (closed 2026-06-12) — v2 overlay format shipped end-to-end: i39a/#131, i39b-1/#151 + i39b-2/#153, i40 + i51/#181, i48 host side/#141–#144. The B-DOS era opened at close: i71/#186 (reference vendored) + i72/#187 (`build-disk -dos` prep). Residuals migrated to M9 (i39c → i48c). |
| M9 | Editor era — Phase 2 foundations: codegen tables (i7), B-DOS boot-disk swap (i75), paged block-list edit model (i41), SAM-side text→overlay encoder (i48c, absorbs i39c), read-only viewer (i4), UI decisions (i6/i5/q1) | `docs/specs/editor-edit-model-design.md` + `docs/specs/comment-storage-design.md` + `docs/specs/codegen-tables-design.md` | `docs/notes/m9-status.md` | ⏳ active |
| (Phase 2) | On-SAM editor | `docs/specs/phase1-assembler.md` §editor + `docs/specs/editor-vision.md` (design pointers) + future Phase-2 spec | — | ⏳ started — M9 carries the foundations |
| (Phase 3) | SAM as aarch64 netboot server (Quazar Trinity) — DHCP+TFTP to any Pi, over a direct cable or a shared LAN | `docs/specs/phase3-delivery-design.md` (i84 — **design confirmed** Pete 2026-06-14) + `docs/plans/phase3-netboot-implementation-plan.md` + `docs/notes/netboot-trinity-testing.md` (on-hardware guide) | — | ⏳ implementation underway — **host oracle harness** (i91/#219), the **Z80 packet build/parse logic** (i92), the **i80 Trinity emulation + the three wire-I/O transfer state machines** (DHCP responder / TFTP server / TFTP client — host-verified byte-for-byte over the emulation), both **seams** (i82 originate front + i93 B-DOS store), the **bring-up smoke test** (i94, `make netboot-smoke-disk`), the **integrated netboot server** (i95 — `netboot_serve_once`, the bootable `make netboot-server-disk`, host-verified over the i80 emulation byte-for-byte vs `server.Server.OnFrame`), the **serve-files TFTP demo** (i96 — a plain-TFTP server, ARP+TFTP only, `make netboot-serve-disk`, testable with a stock `tftp`/`curl` client; bare-RRQ→DATA per RFC 2347 + optioned-RRQ→OACK, host-verified vs `serve.Responder.OnFrame`), and the **TFTP client boot disk** (i82 — `make netboot-client-disk`, fetches a `.mgt` from a TFTP server + HSAVEs it to Trinity storage; the ARP→RRQ→DATA/ACK wire side host-verified vs `client.Client`, the B-DOS HSAVE write-out hardware-gated) all landed. Design confirmed (i84, q11/q12): Model A; SAM=DHCP+TFTP server serving by-name; both topologies; on-SAM HTTP self-provisioning. **Remaining (autonomous, unblocked):** HTTP (i70) + HTTPS (i88). The protocol logic + ENC28J60 wire I/O are host-verified over the i80 emulation; an **end-to-end Pi boot + real-silicon timing + the RST-8 hook dispatch stay gated on real Trinity** (CLAUDE.md §5). Reference: `simonowen/trinload` |

Legend: ✅ done · ⏳ in progress · 📋 designed, not started

## Design notes not strictly inside a milestone

These are patterns or research findings that get applied *within* milestones rather than constituting their own milestone:

| Note | When to apply | Status |
|---|---|---|
| `docs/ARCHITECTURE.md` | **The first read**: the synthesized system overview (system shape, authority model, encoder tables, memory + paging, `.tbn` v2, build/test pipeline, dev inner loop). Each section links its deep spec. | ✅ living overview |
| `docs/specs/tbn-binary-format-reference.md` | The single authoritative reference for the complete `.tbn` binary encoding (header, records, operands, expr bytecode, directives, compaction levels). Read/cite this whenever touching the format; supersedes the retired M1 spec's tables. | ✅ living reference |
| `docs/specs/samdos-file-io.md` | Reference for the DOS file-I/O hook layer (SAMDOS 2 / B-DOS): the HLOAD trampoline pattern (READ), the HSAVE UIFA pattern (WRITE), hook register-clobbering facts, pre-built Z80 snippets. | ✅ living reference |
| `docs/notes/sam-stub-audit.md` | Already applied (PR #13 loader fix used findings). Keep as reference for any future DOS hook work. | ✅ applied; reference |
| `docs/notes/sam-paging.md` | Reference for any LMPR/HMPR work. | ✅ reference |

## How to extend this doc

When new deferred work or a design decision arises:
1. Add an `iN` row via `build/registry add --title "…" --desc "…" --status OPEN --owner agent` (the id is auto-allocated; add `--space questions` for a `qN`; use `build/registry split --parent iNN --title "…" …` for a sub-item — its id is determined from the parent), then `make registry`. Commit the YAML and regenerated `.md` together. That is the one home — do not add a checklist here.
2. Add the new design note to the appropriate table above (milestone, or "Design notes not strictly inside a milestone") and link it from `docs/specs/README.md` if it lives under `docs/specs/`.

The goal: no design discussion is ever lost to context drift. If it's worth writing down, it's worth a registry row.
