# sam-aarch64 roadmap

<!-- HANDOVER-PROTOCOL-START -->
## START HERE — session handover protocol (read this first)

**This file is the canonical entry point and the agent→agent handover contract.** If you are an agent picking up this project: read this section, then the **"Current State & Next Actions"** block below, then the current milestone status doc, then the project memory index (auto-loaded at session start). That is the complete start-up read — everything else is reached via links from here.

### The contract (always inherited, always maintained)

1. **Tracking is part of "done" — and it happens at REQUEST time, not PR time.** The moment a piece of work is well-defined *and* you have sign-off to do it, create its `iN` row in `registry/items.yaml` (or `qN` in `registry/questions.yaml`) **before you start implementing** — `build/registry add …` (the id is auto-allocated; there is no `next-id`, no `--id`) then `make registry`, committed **locally on your branch** (no separate PR/merge dance just to capture — it rides the work's PR or the wind-down sweep). Mark it **`IN_PROGRESS`** and **say so out loud**: "logged as iNNNN, marked IN_PROGRESS, now working on it." So intent survives a crash mid-work, and many parallel asks become many `IN_PROGRESS` rows — **a feature, not a violation: the registry is the session's completeness ledger** (query `build/registry in-progress` at wind-down — see hygiene). IN_PROGRESS is **transient and session-local — `main` never carries it**: before a PR **merges**, resolve every entry (DONE for finished work, `split` into a DONE part + a fresh OPEN part for partial, or back to OPEN if untouched), so what lands on `main` is *resolved* states (DONE/OPEN), never a wall of IN_PROGRESS. (A fleeting IN_PROGRESS commit may sit in a feature branch's history — a self-completing PR can't know its own number before the first push — but the merged result is always clean; that's the light, watertight line, no history-rewrite gymnastics.) A PR completes the IN_PROGRESS items it finished — and **one PR may complete several** (an item is one deliverable; a PR is one cohesive reviewable change). Treat a missing tracking entry like a missing test: reviewers flag it. **This is the agent's responsibility, never Pete's** — including for work you merely *raise* with him: asking "should we do X later / in a future session?" does **not** delegate tracking to Pete. Track X the moment you raise it (a future-session item is still an item); a question *to* Pete is itself a `qN`. Pete is not your tracker.
2. **One home per kind of thing** (this is how the docs stay un-sprawled):
   - **ROADMAP.md** (this file) — the index and the live "Current State" below.
   - **Item registry** — source: `registry/items.yaml`; generated views: `docs/notes/item-registry-open.md` + `item-registry-closed.md`. The project-wide, milestone-neutral `iN` id↔item map. A fresh agent reads the **open** view for outstanding work. The YAML is the source of truth — never hand-edit the generated `.md` (the `registry-sync` CI job fails if you do). Resolve an item with `build/registry set-status --id iNN --status DONE --pr N` then `make registry`, in the completing PR.
   - **Question registry** — source: `registry/questions.yaml`; generated view: `docs/notes/question-registry-open.md`. Questions are **transient**: every question for Pete goes into the YAML the moment it arises (`build/registry add --space questions …`); answering one means curating every dependent item (apply the decision), then deleting the question (`build/registry answer --id qNN`). There is no closed-questions view — the decision lives in the items + git history.
   - **Backlog** — `docs/notes/backlog.md` (generated from `registry/priority.yaml`). The priority-ordered list of open pullable items; `build/registry ready` returns the unblocked tip. This **supersedes** the prose "proposed implementation sequence" as the single source of work ordering.
   - **Milestone status docs** (`docs/notes/m{N}-status.md`) — the per-milestone source of truth.
   - **Memory** (`~/.claude/projects/.../memory/`) — cross-session preferences, facts, feedback. *Not* work-tracking.
   - Superseded docs are **deleted** — git history is the archive; pin citations to a blob link if a living doc must cite one.
3. **At milestone close**, walk the item registry and ask of each item: still deferred, or does it now fold into the milestone in flight? **The `iN` item registry and the `qN` question registry are both milestone-neutral and never archived:** at milestone close, every item belonging to that milestone must be marked `DONE`/`WONTFIX` (via `build/registry set-status …` then `make registry`) or migrated (re-pointed) to the active milestone's strands — no item may be left living only in a closing `m{N}-status` doc. Open questions for Pete live only in the question registry (`qN`) for the same reason. **The milestone status doc is deleted at close** (git history is the archive) after the registry walk completes.
4. **Delegate to subagents to preserve orchestration context.** The agent driving a session (the *orchestrator*) should push self-contained, well-scoped work out to subagents — codebase searches, mechanical edits, doc drafting, focused investigations, even whole low-risk PRs (implement → verify locally → commit → push → open PR) — via the `Agent` tool (and `Explore` / `Plan`), keeping only the conclusions in its own window. A subagent's full transcript is discarded; only its final summary returns, so the orchestrator's context lasts far longer. The orchestrator keeps the judgement calls itself: sequencing, monitoring CI, merge/launch decisions, and anything user-facing. The durable state lives in subagents-plus-these-docs, not in any one agent's window — so when context *does* fill, the handover (update "Current State", below) lets the next agent resume as orchestrator with nothing lost. Prefer launching independent subagents in parallel (one message, multiple `Agent` calls) when the work has no shared state.

### Session hygiene (the minimal ritual)

- **Continue vs wind down at a `/context` checkpoint:** the decision rule lives in **one** place — the `tools/autonomous-loop/monitor.sh` task-done nudge (`RESUME_NUDGE`), delivered at the decision point. Don't re-litigate it; follow the nudge. (It was i104's top friction — ~9 sessions re-litigated it — which is *why* the rule is encoded at the point of use, not restated here.)
- **Closing a session:** **no magic words** — *any* wind-down phrasing ("close the session", "let's wrap up", "prepare for handover", "goodnight, what's next?") triggers this. The agent then updates the **"Current State & Next Actions"** block below *in place* — what landed, what's in flight (open PRs / branches / running agents), and the **proposed implementation sequence** (the ordered, reviewable-and-adjustable plan — re-rank it as priorities shift; it is *not* a single locked next-step); confirms tracking is current (rule 1); and leaves the working copy on a clean, current `main`. Do **not** write a new dated handoff doc — update the standing block. **Get this update merged to `main` before handover** — a quick docs PR + CI, since branch protection blocks direct pushes. Never hand over with the live state stuck in an open PR, or the next session reads stale context.
- **Before a PR merges — and at wind-down — reconcile every `IN_PROGRESS` item** (IN_PROGRESS is transient and never reaches `main`; rule 1). Run `build/registry in-progress`; for each row it lists, exactly one of: it is genuinely DONE (its PR merged → `set-status … --status DONE --pr N`); or you finished *part* of it → **`build/registry split`** the done part off as a DONE child and leave the rest a fresh OPEN child (capturing the true partial state); or it was untouched → set it back to `OPEN`. The list must end holding only what is *actually* still in flight. This is the completeness check that guarantees no signed-off ask was lost in the chat scroll (rule 1 / i154).
- **At wind-down, also write a per-session retro report** to `~/claude-retro/` (item i104): a short, blameless, *independent* note (don't read other sessions' reports first) on genuine process friction this session — *say what genuinely happened: no manufactured problems, no suppressed ones*; **"nothing notable" is a valid, good report**; ground each friction in a specific incident. Format + rationale: `~/claude-retro/README.md`.
- **Starting a session:** the standard start prompt is just **"Continue per docs/ROADMAP.md."** A SessionStart hook (`tools/session-handover.sh`) runs first: it surfaces this section and, **when you are on a clean `main`, auto-`fetch`es and fast-forwards to the latest** so the doc you read is current. If it instead **warns** (you are on a feature branch, have uncommitted changes, or `main` can't fast-forward), refresh deliberately before relying on the doc: stash/commit your work, then `git checkout main && git pull --ff-only`. Then read this section → Current State → resume; no bespoke per-session prompt is needed.

### Current State & Next Actions

*Updated in place each session — this is the live handover. Keep it ≤15 lines; history lives in the milestone status docs, the registries, and `git log`.*

- **Milestone:** **M9 active — the editor era** (`docs/notes/m9-status.md`). M0–M8 ✅ complete. A **Phase-3 netboot strand** runs alongside — now the **top priority** (below).
- **★★★ TOP PRIORITY — the SAMBOOT EEPROM-patch strand. FORK ANALYSIS NOW COMPLETE (2026-06-24, PR #624 awaits Pete's sign-off); the next phase (writing the patch) is GATED on that sign-off + Pete being present.** **READ `docs/specs/samboot-fork-analysis.md` FIRST** — it holds Pete's directives VERBATIM/uncut (the load-bearing discipline: *finish ALL fork research before any EEPROM code; derive, never guess; never flash unattended — only with Pete present*) + the methodology. The analysis (private write-ups in `~/sam-archive/samboot-capture/`: `colin-fork-complete-story.md` is the synthesis) is **twice adversarially reviewed → "safe to build the EEPROM patch from, no correctness blockers."** Key PROVEN results that REFRAME i229: (1) we now have a **real stock ROM** (SimCoupé's `samcoupe.rom` v3.1 → exact-v3.0 `rom_stock_v30.bin`; resolves i219 in practice); fork = stock + exactly **140 bytes in 6 patch regions** (probe/fetch/disk-timeout/banner→reader/F-key-remap/msg-ptr) + version byte. (2) **Colin's actual bootblock draws NO stripes** — the rainbow is a FEATURE WE ADD (not "restore"); the LongSteve repo added it, Colin's didn't. (3) The **exact return-to-BASIC is DERIVED from stock** (restore paging → `CALL CLSLOWER &06B5` → `LD (LINICOLS),&FF` → `NSPPC(&5C44)=&FF` → `TVDATA(&5BBE)=&10` → `JP ERRHAND2 &102F`; the stock boot itself sets those sysvars — don't guess them). (4) **Holding SPACE at power-on bypasses the EEPROM boot** (ROM-chip brick-recovery hatch — confirm on HW). (5) EEPROM B-DOS = byte-identical **1.5t beta 6** (1.5a + Atom→SD device-layer swap; fork already diffed in `docs/notes/bdos-trinity-fork-analysis.md`). **Chain: i229 (build the patched bootblock — only AFTER Pete signs off #624; mirror stock exactly, every instruction traced to the analysis) → i230 (Pete-gated hardware RAM test) → i135c (destructive flash; Pete present only). The old recipe in `docs/plans/i229-combined-bootblock.md` is SUPERSEDED by the analysis. DO NOT write EEPROM patch code or flash unattended.**
- **★★★★ 2026-06-25 MARATHON — FULLY LANDED. i145g CSD READ WORKS ON REAL HARDWARE; then a hardware-readiness systemic push after Test B crashed the SAM.** The CSD-read bug was a **missing leading `&FF` flush** before every SD command (authority: `trinity-sd-z80-interface.md` §5, Colin's `MMCSD` BASIC, his B-DOS disasm) — invisible to the emulator; ~50 hardware iterations burned guessing what the docs already answered (lesson: memory `feedback_port_diff_authority_first`). Card → 64 GB / 77,959 records.
  - **All marathon PRs MERGED:** #656 i145g, #657 i245, #658 i194, #659 i113b, #660 i167, #661 i178, #662 i161, #663 i192, #664 i243b, **#666 i244**, **#667** (hardware-readiness audit + emulation-gap items i246–i251), **#668 i252** (deploy-guard hook). The **i246 id-collision** is resolved — the deploy-guard hook was renumbered i246→**i252** (the hardware-readiness audit keeps i246 = bounded SD busy-wait). Registry hygiene done: completing-PRs backfilled across the train, i245 + i248 marked DONE.
  - **THE 6 HARDWARE FIXES** (matrix: `docs/notes/hardware-readiness-audit.md`): #1 flush (i145g✓), #2 drv_init-before-SD, #3 enc_rx_reestablish, #4 tr_terminate clean-exit, #5 bounded SD busy-wait, #6 Colin 4-step deselect. **csd_probe has all 6 (gold standard). #666 (i244) gave serve_main + client_main #2 + #3** (drv_init-first reorder + `enc_rx_reestablish` after every SD→ENC boundary + `ModelSDInitSettle` always-on so the class fails in emulation; closed i248). **Still MISSING on serve_main/client_main: #5 (i246), #6 (i247), #4.**
  - **⚠️⚠️ NO MORE HARDWARE until readiness lands (Pete's instruction). TOP PRIORITY = emulation-gap items i249/i250/i251** (priority ranks 1/2/3, now on `main`): make the emulator FAIL when #3/#5/#6 are missing (the i126 comprehensive-emulation north star). THEN the program fixes (#5 i246, #6 i247), verified by the emulator. THEN Test B. SAM is **Test-B-crashed — power-cycle before any hardware**; staged record image `scratchpad/boot_m2b_record.mgt`.
  - **DEPLOY-GUARD HOOK ACTIVE (i252, #668):** a PreToolUse hook (`.claude/settings.json` + `tools/deploy-guard/`) DENIES any SAM-deploy Bash command with the hardware checklist unless `DEPLOY_CHECKED=1` — now on `main`. Retro: `~/claude-retro/2026-06-25-i145g-csd-flush-read-the-authority.md`.
- **★★★ NEWEST — autonomous run (2026-06-24, Pete-away → returned mid-session for hardware). THREE PRs merged; `i114` umbrella DONE.** **#650 i114a + #651 i114b → umbrella i114 DONE — the netboot serve-manifest Go authority:** `tools/netboot-oracle/tftp/manifest.go` is a line-based text format mapping full Pi-facing TFTP names/paths → local B-DOS files or remote record locators (+ span/size/optional SHA-256); `*Manifest` is a drop-in `tftp.Store` (so the existing `Resolve` answers an RRQ straight off it) and `Entry.ServePlan` threads a remote blob through `bdos.SpanPlan` for the ordered read plan. `allocator.go` is the storage-allocation policy (first-free / fixed-list / highest-free, default highest-free) over a modelled `Card`, reusing already-claimed leftover space first and **warning rather than ever stealing** an unlisted/excluded record on overflow (the load-bearing §4 invariant, adversarially §3-verified). Host-verifiable Go-only; the real `RST 8` HRECORD/HSAVE per record stays the live-Trinity gate. **#652 i158** — the **gate column** now renders in `item-registry-open.md` (mirrors backlog's `computeGates`: ready / 👤 pete / 🛠 in-progress / 🔒 qN / ⏳ iN), open-only. **⚠️ ID-SPACE CONSTRAINT STILL HOLDS** — new NUMERIC item ids and new `qN` ids still **collide** with the unmerged i233–i236 / q-space on the two OPEN Pete-PRs (#624/#625); only **letter-id splits** are safe (i114a/i114b were pre-existing letter children — no new id needed). **Do NOT allocate new numeric/qN ids until #624/#625 merge.** **Pete returned for hardware** (`pete-present` marker set). **NEXT (hardware, Pete-present):** the SAMBOOT chain is gated on **#624 sign-off** (then i229 build → i230 RAM-test → i135c flash, Pete present only); the i145g CSD re-test staging (`scratchpad/hw_retest_csd.sh`) was session-local and is **gone — re-stage the full path before any hardware shot** (prepare-before-you-poke, per the #641 lesson).
- **★★★ NEWEST (prior) — autonomous run (2026-06-24, Pete-away). SIX PRs merged + i104 retro.** **#643 i89a** — netboot oracle extended to the **Pi 3 family** (`TestServerServesBothPiFamilies` proves one flat store serves Pi3+Pi4; Pi3 DHCP/TFTP differences documented from the RPi docs in `pi-netboot-capture-analysis.md` §4; **no fabricated wire vectors** → split **i89b** = real Pi3 capture, owner:pete). **#644 i120a** — **RFC 7440 windowed TFTP** in the Go oracle (the authority for the Z80 port; lock-step path byte-identical) → split **i120b** = Z80 port + re-add windowsize. **#645 i123a** — make→tup build-system **evaluation** (`docs/notes/build-system-tup-evaluation.md`: tup auto-tracks pyz80 includes + hard-errors on undeclared *generated* reads → structurally kills the off-axis-cluster stale-symbol footgun; recommend migrate) → split **i123b** = do the migration, **owner:pete**. **#646 i137 WONTFIX** — its premise (">&BFFF loads ROM1 → crash") is a stale paging model; at boot LMPR=&1F → section D is RAM (proven, `make secd-loadability`), so build-disk's &10000 ceiling is correct/complete. **#647 i142** — registry CLI now **refuses mutations on a stale `build/registry` binary** (kills the i138 silent-corruption class). **#648 i144** — HSAVE **write-back**: saved file bytes land in the record + round-trip via HRSAD (unblocks i119e content asserts). **⚠️ ID-SPACE CONSTRAINT (load-bearing):** new NUMERIC item ids and new `qN` ids currently **COLLIDE** with the unmerged i233–i236 / q-space on the two OPEN Pete-PRs (#624/#625) — so I could **not** gate **i113b** (needs Pete photos) / **i117a** ("Pete's call; do not auto-pick-up") with blocker items, nor raise a qN; they stay un-gated but are Pete-gated by their descriptions. **Letter-id splits (i89b/i120b/i123b) are safe** (derived from the parent, not the counter). **Do NOT allocate new numeric/qN ids until #624/#625 merge.** **NEXT tip = i147** (turn-end safety valve) — a DESIGN item touching the loop's own monitor: `gh pr merge` runs server-side (no local hook fires) and `screen -X stuff` can't interrupt a running turn, so "force turn-end from outside" is a genuine open design Q → wants fresh context + likely a Pete qN (allocate post-#624/#625). **#624/#625 STILL await Pete — do not agent-merge.**
- **★ LATEST — autonomous run (2026-06-24, Pete-away). Two PRs merged; the queue's design-gated tip-cluster recorded.** **#628** — i229 bootblock-splice **research** preserved as execution-ready docs (disassembled the local \`eeprom.bin\` chunk-1 bootblock; mapped the Go reset-chain harness) + **q50** raised on the design forks (i229 gated on it) — NOT a build (brick-adjacent boot code Pete reserved for his presence). **Durable finding (\`samboot-bootblock-analysis.md\` §8):** the Go reset boot does NOT run past \`CALL &805F\` (B-DOS init never returns), so the post-\`&805F\` spliced decision is unreachable in emulation. **#629** — registry hygiene: **q51** (i41e serialize-UX product choice, the design-doc §8.4 "→qN" that was never tracked), **q52** (i42 integrity decision) + i42 **re-scoped v1→v2** (its row referenced the superseded v1 header), **i73→q49** (L12 is the only residual + it's already q49-gated), and **i43–i47/i65→i41** (editor Phase-2 features need the edit-model). **⚠️ RECONCILE — overlap with the two OPEN Pete-PRs below:** #628 worked off the OLD i229 plan, so **q50 overlaps #624**'s fork analysis (flagged on #624 — reconcile q50 vs i233-i236, keep the §8 \`&805F\` finding); **q52 DUPLICATES #625**'s i42-integrity question + the format is now noted v2 (flagged on #625 — drop #625's dup question, depend i42 on q52, renumber #625's q to **q53**, adopt the v1→v2 re-scope; #625's \`tbn-header-integrity.md\` spec is the real deliverable). **PROCESS LESSON:** read THIS Current-State block (it lists in-flight PRs) BEFORE \`build/registry ready\` — \`ready\` reflects \`main\` only, so open PRs awaiting Pete don't show and you can duplicate their work. (i74 / i7 phase D was COMPLETED the next session — see the bullet below; the #628/#629 q50/q51/q52 reconcile notes here remain live because #624/#625 are still open.)
- **★★ LATEST — autonomous run (2026-06-24, continued; Pete-away). FOUR PRs merged.** **#631 i74a + #632 i74b → i7 PHASE D COMPLETE (i74 umbrella DONE):** the at/ic/barrier System-group option tables are now GENERATED from the Go authority — the `barrierOption`/`atName`/`icName` switches moved out of `tools/aarch64dec/sys.go` into data + accessors in `tools/sam-aarch64-format/sysregs.go` (the sole authority), a new `tables-gen -sysopts-inc` emitter projects them into the committed `src/disasm_sysopts.inc`, and `src/disasm.asm` `include`s it; `disasm.bin`/`disasm-test.bin` byte-identical, and `sysopts_z80sync_test.go` guards at/ic (which are ABSENT from the release corpus, so `TestDisasmOracle` can't see them). **The i7 hand-sync drift class is now eliminated for every table in the inventory.** **#633 i103 → autonomous-loop monitor `$QUIESCENT` hold:** a genuine backlog drain now `touch`es `$QUIESCENT` (not `$WOUND_DOWN`) and the monitor HOLDS — stops nudging, no `/clear`/restart — until the session transcript grows (Pete returns), then auto-expires; this kills the old drain→restart→re-drain waste loop. **#634 i233 → registry CLI derives umbrella status** at the `applyAndCommit` chokepoint (fixpoint, nested-safe), so completing an umbrella's last child no longer leaves it stale-OPEN; backfilled **11 stale-OPEN completed umbrellas → DONE** (incl. the nested i102→i102m cascade). **New q53** (Pete): how to disposition the long-deferred i55/i66 that keep clogging the `ready` tip. i204 (then the next tip) was worked the FOLLOWING session — see the newest bullet below (i204a DONE #636; i204b/i234 now Pete-gated). **The two Pete-PRs #624 (SAMBOOT fork analysis) + #625 (i42 `.tbn`-CRC spec) STILL await sign-off** — do not agent-merge.
- **★★★ NEWEST — Pete-present session (2026-06-24). Trinity emulator brought to documented-hardware PARITY (#641, i235 DONE), via a hardware-debug saga + a research-first/comprehensive-emulation directive.** Pete prioritised hardware testing (i145g CSD-probe). A rushed hardware attempt hung the SAM (3 restarts) — lessons: **prepare the FULL path before any hardware shot (verify the capture end-to-end first), ground every hypothesis in the captured docs not code-speculation, and the emulator-fidelity gap IS the bug** (it accepted a probe real hardware rejects). Per Pete's directive ("don't narrow to the failure — bring the emulator to parity with the hardware, exhaustively, no guessing, no skipping"), mined EVERY authority (OCR'd Trinity manual 135 behaviours, Colin B-DOS 1.5t SD driver, simonowen trinload/encdrv/eeprom) → committed fidelity inventory `docs/specs/trinity-emulation-fidelity.md` → **#641 encoded all of it**: the shared one-PIC microcontroller (global busy that gates/drops OUTs, ONE global auto-null mode, aliased `IN &DD/&DE/&DF` latch, mutually-exclusive MUX select) + ENCINT, `&38` 0/1/2, configurable WP, ENC filter, `&23` pulse, deselect-tail, etc. (ABSENT→0; full suite green; §3-reviewed). **KEY FINDING: even faithful, the hang does NOT reproduce** — the full `probe_main` serves the CSD end-to-end (also fixed: probe_main was hidden from emulation by a `NETBOOT_HOSTTEST==0` carve-out, the i231 anti-pattern). So the cause is below the documented digital contract (real-silicon timing the Trinity docs leave unspecified) OR the original capture was flawed. **NEXT (Pete-gated): the fully-staged one-shot hardware re-test (`scratchpad/hw_retest_csd.sh`, ARP-state diagnostic) distinguishes a real hang from a capture-issue; if a real hang, mine the ENC28J60 chip datasheet next.** Earlier this session (Pete-away): **5 PRs** — #636 i204a (Imm16 gap), #637/#638 (budget-lock gating of i204b/i27b/i27c behind q54), #639 i216 (SAMDOS→B-DOS audit), #640 i116 (harness build-assertion). ~20 actionable non-Pete items remain in `ready` (the premature-quiescence correction).
- **★★ AUTONOMOUS run (2026-06-24, Pete-away). ONE PR merged + queue unclogged → wound down at agent-quiescence (superseded by the session above).** **#636 i204a DONE** — closed the Imm16 SlotKind `0x08` gap (`udf`/mnem 89): `encode_imm16` 16-bit placement encoder + SlotKind dispatch before the fold path, Go-authority fixture (`udf #0x1234`→`0x00001234`), a boot self-test row, and an `enc_synth_sr` commoning pass; **prod binary byte-identical (release-gate green)**, 19/19 CI incl. SimCoupé matrix. **i204 SPLIT (partly-done trigger):** the faithful overlay_classify/literal_word/compact_inst port is **~1300 B** (not the ~150 LOC estimated) — it calls `encode_inst` (ENCTAB in section A) so it **cannot** go off-axis, and the test variant has only **~11 B** of `&C000` headroom. So i204 → umbrella: **i204a DONE**, **i204b OPEN** (routines WRITTEN + Go-authority-verified, staged in `scratchpad/i204b-*.asm`; Go test `overlay_classify_fixtures_test.go` landed) gated on new **i234** (section-C budget relief). i48c-b8e re-pointed to the **umbrella i204** (i87b silent-gap trap avoided). **i234 is Pete-steer-gated:** commoning is EXHAUSTED for ENCTAB-coupled code (i205/q43 already spent that lever), so the relief lever — (2) a 2nd BUILD_TESTS boot variant + CI job / (3) chunked payloads / (4) the i2/q36 page-pool model — is a genuine architecture fork → raised **q54** (owner:pete; precedent: i205 steered via q43), i234 depends on it. **Queue hygiene:** **i55/i66 gated on the existing q53** (both are resolved-question "revisit only if X" deferrals that were clogging the `ready` tip — the missing-edge bug). **WIND-DOWN rationale:** after gating, the `ready` tip is all parked/hardware (i77 editor-era, i89 Pi-3-hardware, i90 captured-only) — no cleanly agent-actionable item remains; what's left needs Pete (q54/q53/q45/q46 steers, i41e design) or hardware (i89/i194/i135c). **STRUCTURAL FINDING — the whole SAM-assembler-code strand is budget-locked behind q54.** i204b, **i27b** (835769, ~500-650 B), and **i27c** (843419 + toggle, shares the heavyweight mem-op classifier) all add production code that must be inline in section C (can't go off-axis) and all blow the ~11 B `&C000` headroom; commoning recovers only ~tens of B. So i27b/i27c are now **gated on i234** (the missing-edge fix — they previously read as `ready` while actually blocked). The only NON-budget-locked unblocked agent work left is **large-multi-session** (i220 Go-doc sweep, i126 the comprehensive-emulator merge) or **parked/hardware/Pete-design** (i77 editor-era, i89/i194 hardware, i41e/q45/q46/q13). **NEXT session: answer q54 → build i234 → unblock i204b + i27b + i27c → close b8e + land errata.**
- **★★ THIS SESSION (2026-06-23) — the AUTONOMOUS TEST LOOP is HARDWARE-PROVEN, + the boot-screen diagnosed + the comprehensive-emulation lesson enshrined.** Merged: **#618 i41d Brick 2** (edit-model blocks page onto the i2 pool via `OUT (251)`); **#619** (registry title-limit 120→200); **#620 i225** (report-over-network test framework + EEPROM write round-trip) → **i226** EEPROM WRITE path **PASS on real hardware** (first ever); **#621 i228** + **#622** fix (runtime emulation-vs-hardware terminator: `di;halt` in emulation, RET to trinload on hardware — the IN A,(n)-high-byte bug was HARDWARE-FOUND, harness tightened). **On Pete's SAM the loop WORKS: push → test → report over the wire → RET → push again, no power-cycle, no human between tests** (`tools/trinload-push/trinload-push.py <ip> <bin> 1 0x8000` + a UDP listener on :9000; SAM @ `192.168.2.75`). **#623 (checkpoint, IN FLIGHT)** lands the boot-screen recipe + `samboot_stripes` ROM port + the WIP demo + the registry/lessons. **LESSON ENSHRINED (`feedback_comprehensive_emulation` + i126 sharpened to the north star):** ONE comprehensive emulation = fork SimCoupé (most complete SAM: screen/timing/sound/line-interrupts, boots from 0) + port the Trinity models IN (ENC/EEPROM/SD), used by EVERY test — the yellow screen PASSED the screenless Go harness. **i232** records the boot-from-0 principle (future); **i231** the systemic `NETBOOT_HOSTTEST==0` carve-out elimination + CI guard (parked, Pete's call). **HARDWARE STATE: trinload is DOWN** (the yellow demo took it down — Pete reloads to resume).
- **★ TOP PRIORITY (Pete, 2026-06-18) — ship disk images to the SAM over the network, ending the SD-card shuffle.** Every hardware experiment today means pulling the SD card to the Mac and back (real card wear). Goal: post a disk image over the wire → it lands on Trinity storage → no more card swaps. Path = the netboot TFTP/HTTP stack we're building (client **pull** + server **WRQ push**). **Fallback + immediate de-risk:** trinload-as-bootstrap (**i129**, proven + reactive — boot a trinload record, it network-loads *our* code into ephemeral RAM and runs it: kills the SD shuffle for *dev iteration* now, and is the ready fallback transport if our own stack stalls). Pursue our own stack first; build the trinload `.mgt` in parallel as the go-to bootstrap. See the proposed sequence.
- **★★ CRITICAL STANDING GATE — the on-SAM EEPROM WRITE path has NEVER run in emulation (brick risk).** An emulation-readiness audit (2026-06-23) of all hardware-gated items found ONE critical gap: `write_chunk`/`write_enable` (`src/netboot/eeprom.asm:389-501`) has never executed in any emulator — `tools/netboot-oracle/z80/eeprom.go` models only the SPI **READ** path. So flashing the real Trinity EEPROM (**i135c**) would be that code's first-ever run = brick risk. Tracked as **i221** — **DONE (#614, this session):** `eeprom.go` now models the 25LC1024 page-WRITE (WREN/WRDI latch, page-wrap, 24-bit addr) faithfully, with white-box + driver-round-trip + backup/restore tests; the gate is satisfied. **i135c (the real flash) stays Pete-present/hardware-gated**, but the brick-risk is retired — `write_chunk` now runs in emulation. All other hardware-gated items are either emulation-proven or hardware-only-by-nature (real RST-8 HSAVE, real Pi netboot, MODE-2 pixel blits). **STANDING DIRECTIVE (Pete, 2026-06-23): emulation is a required gate before ANY hardware session, especially destructive paths — every hardware-gated task must be emulation-proven before Pete is at the SAM.**
- **★ Last landed (2026-06-23, THIS session — disk-push launcher, EEPROM-write emulation, `-layout`, pipeline doc).** **#613 i121d DONE** — the host PUSH LAUNCHER `tools/trinload-push/trinpush-serve.py` (`--strategy highest|lowest|explicit:N` patches the i121h `SERVE_CONFIG` block, then pushes via the TrinLoad `?`/`@`/`X` protocol); the serve **boot** binary doubles as the pushable block; wire protocol single-sourced into `trinpush.py`; host patch-test in CI. **This unblocks i194** (real disk push) — its i121d dep is now satisfied (still needs i141). **#614 i221 DONE** (EEPROM-write model, above). **#615 i56+i20 DONE** — `SpectrumFourLayout` extracted behind a `-layout <file.json>` flag (host flatten path no longer spectrum4-locked; q42 decision (a)); default path byte-identical (release-gate-verified). **#616 i223 DONE** — `docs/toolchain-pipeline-walkthrough.md`, a byte-level worked example tracing one `.s` through every representation (`.tbn`/binary/disasm/`-flatten`/comment-strip) + the round-trips, real bytes/commands/Z80-citations. **q48 (`.section` handling) was ANSWERED inline** — no parity gap; `.section` is a zero-byte no-op on both host and SAM, recoverable from the `.tbn` (not the binary); flatten is a one-way linker lowering that consumes it. **Prior session (2026-06-23 earlier — the EMULATOR MERGE + disk-push WRITE):** **#607 i190b → i190 umbrella DONE — the two-emulator merge (q47 → GO, the i208 partial-merge recommendation):** the SAM LMPR/HMPR pager + ROM model (`sampage`) was extracted out of `tools/netboot-oracle/z80/` into a NEW neutral shared module **`tools/sampage`**, and the assembler harness (`tools/z80-test-harness-go/harness.go`) now DELEGATES to it — the SAM CPU+memory+paging+ROM model lives in ONE place both harnesses import (devices ENC28J60/EEPROM/DOS-hooks stay where used). **The `feedback_mirror_emulator_fixes` rule is RETIRED** (no duplicated pager left to mirror). **#609 i198 DONE — real SD CMD24 list-sector WRITE (disk-push agent side):** `bd_list_write_hw` (CMD24) added in `src/netboot/sd_csd.asm` mirroring the i141 CMD17 read; `bdos_write_list_sector` gated behind `NETBOOT_REAL_LISTREAD`; serve/client boot images flipped to the real read+write list path; B3 tests drive the real CMD17+CMD24 read-modify-write via the SD model (SeedSector). Incidental SD-SPI **fidelity** fix in `sdcard.go` (R1 response now streamed before the write data phase, as a real card does — reviewed, genuine fidelity not a hack). **#606 i102s DONE:** sha256 message-schedule extend-loop now uses a B,C,D,E register accumulator — per-block compress 315,355 → 309,211 T-states (~1.95%), digests bit-identical. **#602/#603/#604/#605 — four assessment/reference docs:** emulation-library merge review (`docs/specs/emulation-library-merge-review.md`, i208), on-SAM macro assessment (`…/on-sam-macro-assessment.md`, i209), linker-script/layout assessment (`…/linker-script-layout-assessment.md`, i210), SAM software catalogue (`…/sam-software-catalogue.md`, i189, linked from ARCHITECTURE.md). **#608/#610** captured Pete's decisions + the audit findings in the registry.
- **★ BUDGET WALL — fixtures solved structurally (i69, #577); HANDLER code is now the pressure.** i69 moved the growing `encode_inst` fixture DATA OFF-AXIS (HLOAD'd page → one bulk-LDIR into section-D RAM at boot), so fixtures cost **zero** &C000 budget and the whole i201/i204 fixture strand is unblocked. BUT each brick's inline **dispatch + OPVAL_ARRAY populator** (the handler code) CANNOT go off-axis (it reads ENCTAB in section A). i201a took headroom 1050→556 B; i201b 556→**~299 B**. **i201c landed (#598) on the back of i218's commoning + an `encode_msr_pstate` loop refactor; i204 will likely need yet another HANDLER-commoning pass** (factor the shared "populate OPVAL entry from operand stream" logic across the populators) — NOT another fixture move (i69 already solved that). The i206 prod-code commoning families (`ml_div*`, `main_dir_org_*`, sysname) remain as additional candidates (`build/coverage-report.txt`). Lesson banked (memory + i207): the Go harness PRE-LOADS payload pages, bypassing the disk→HLOAD path — so harness-green can mask a missing-disk-payload; **SimCoupé-verify any boot-payload/disk-wiring change.**
- **★ B8 = the Z80 compact-serializer port (b8a/b8b DONE; b8e = encoder, brick 1 (i199) DONE).** The SAM assembler reads label/local *defs* from the `.tbn` **header tables** (`reader.asm:151`), not the encoder's inline IR records — so a Z80 port of the host pipeline (`Pass1`→`Compact`→`headerRows`/`WriteFile`) sits between encoder and assembler. Chain (each a faithful port, Go authority cited in its registry row): **b8a Pass1-over-IR ✅ → b8b Compact non-encoder core ✅ → b8e Compact ENCODER adapter (IN PROGRESS: i199 ✅, i201 ✅, remaining i203/i204) → b8c serialize (header tables + front-coded names + editor region) → b8d corpus round-trip (text→.tbn byte-matches host `CompactTBNBytes`)**. Constraints captured in the rows: the parser & assembler scratch tables **collide in `&C100–&EFFF`** (b8d/editor must relocate one stage's tables — relates to q36); a directive-sizing reconciliation note on b8b.
- **★ b8e (the encoder) is now DECOMPOSED — i199 ✅ done; the remaining bricks are session-sized and agent-actionable.** The b8e umbrella `i48c-b8e` depends on **i199 (DONE — form-table path + reusable boot self-test harness)**, **i203 (special forms, → i203a/b/c/d)**, **i201 (compound forms, → i201a/b/c)**, and **i204 (overlayClassify + literalWord)**. Each child is a faithful port (CLAUDE.md §6) with its Go authority cited in its registry description, and reuses i199's `encode_inst` dispatch + the `test_encode_inst` boot-self-test + Go-authority-fixtures harness pattern. **Note (scope, on i204):** the plain `Imm16` SlotKind (0x08, only `udf`) is not yet encoded — `encode_inst` cleanly fail-tags it; cover before closing b8e. **Note for i201a:** the compound encoders `encode_shifted_reg_word`/`encode_extended_reg_word` (`src/slots/`) consume a parsed `OPVAL_ARRAY` (not the raw stream) populated by `main_parse_shifted_reg`/`intercepts.asm` — i201a's new work is the operand-stream→OPVAL_ARRAY populator (eval amtExpr→imm6) + wiring, not the encoders themselves. **Sysreg (i203c)** largely reuses `src/sysname.asm::encode_mrs_word/encode_msr_word` + the paged sysreg tables.
- **★ i119 free-record-detection — mechanism CORRECTED 2026-06-19 (Pete).** Faithful path: **read the list sectors `1..base-1` directly** against the **frozen on-card layout** (parse 16-byte entries, free ⇔ masked first name byte == 0 — `frec3x`, `bdos15a.src.txt:946-948`), depending only on frozen interfaces + **zero** B-DOS internal addresses — no `nmi.sp` trap. Per-record `"BDOS"`-stamp@232 + disk-label@210 inspect (`bdos_inspect_record`) backs confirm-before-overwrite. UX = list / pick / auto-pick-free + confirm. Design `docs/specs/trinity-record-detection-design.md`; q27 RESOLVED.
- **★ SD-storage bootstrap — origin & remaining chain.** The hardware disk push was correctly declined (TFTP ERROR 3) because **`BD_RECORDS`=0 on real hardware** — nothing read the card record count from the SD CSD; every emulation test INJECTED it, so the full trinload→push→store experiment was never faithfully emulated (the i82-class lesson Pete caught). The emulation-first ladder closed that at the logic level, and **i145b-b2 + i145d (this session) shipped the CSD read + the full E2E**. **Remaining:** **i194** (the real-hardware disk push) — now gated only on **i121d** (the trinload `.mgt` vessel) + **i141** (raw card-absolute list-sector read); the rest of its i145 deps are DONE. The 19 `build/*.mgt` are valid + staged; `push-serve.py` + `/tmp/push-all-disks.sh` ready. q40 (non-blocking): widen `BD_RECORDS` past 16-bit for >51GB cards? — agent recommends faithful 16-bit (matches Colin; 51GB ample). Plan: `docs/plans/i145-sd-csd-read.md` (delete when i194 lands; i145 umbrella all-but-hardware DONE).
- **Recent prior sessions (context, all DONE):** **SAMBOOT CRACKED — i197 family (#588):** the "boot-entry contradiction" was an emulator EEPROM-addressing fidelity bug; `deviceLinearEEPROM` un-rotates the chunk-ordered i87a capture (`device D = (file F + &2000) mod &20000`), the real boot runs coherently, **injection site SETTLED** = the bootblock (chunk 1, device `&2000`), post-`&805F` hook (`samboot-bootblock-analysis.md` §8); **#589 i217** extended Trinity emulator fidelity (status `%1100`/idle `&C0`; IDENT `"TRI v1.1"`). **i27a (#600)** — host Go authority for BOTH Cortex-A53 errata (`tools/sam-aarch64/assemble/errata.go`, binutils-2.44 predicates ported verbatim; 835769 = data-dep NOP-insert, 843419 = link-time ADRP→ADR; flags affect only the binary, never the `.tbn`; default build byte-identical); **i27 is now an umbrella — i27a DONE, i27b (Z80 835769) + i27c (Z80 843419 + SAM toggle) OPEN.** **i23 (#595)** — IN `.tbn` buffer migrated off hardcoded pages 7..12 onto the i2 pool as a contiguous run (`pp_alloc_run`; SAMDOS HLOAD/HSAVE are contiguous-only); **KEY FINDING — the >96 KB IN lift is LATENT until i24 lifts OUT** (OUT's 32 KB cap binds first). **i218 (#597)** + **i201c (#598)** — commoning pass #3 + litpool dispatch, i201 umbrella CLOSED; test variant ends at **&BFEC (20 B headroom)**, prod byte-identical.
- **In flight (2026-06-24 session):** **#626 i73 hardening — MERGED** (L3/L4/L5/L10/L11 defensive guards in the Z80 assembler; §3-reviewed + 19/19 CI; L12 `.set`-redefinition deferred to a question — i73 stays OPEN). **Two PRs AWAIT PETE:** **#624** (the SAMBOOT fork analysis — research tooling + `docs/specs/samboot-fork-analysis.md` with Pete's verbatim directives + the i233/i234/i235/i236 items that gate i229; awaits sign-off) and **#625** (the i42 `.tbn`-header-CRC design SPEC — no code; awaits Pete's approval + the q49 compat decision). **⚠️ q49 ID COLLISION:** #626 (merged) used q49 for its L12 question AND #625 has q49 for i42 — when #625 merges, `registry-sync`/a git conflict surfaces it; renumber #625's i42 question to q50 then (noted on PR #625). i233-i236 + i42 are IN_PROGRESS/OPEN on their PR branches (not yet on `main`), so `build/registry ready` on `main` does NOT yet show i229 as gated by the analysis — **do NOT start i229 until #624 is merged + signed off** (Pete's explicit directive). **i24** (the OUT-ceiling sibling) is BLOCKED on **q45** only (its **i218** dep is satisfied; q45 = Pete's `emit_byte` hot-path design fork — uniform per-byte bracket (A, recommended) vs a two-mode static-≤32 KB / pool-beyond scheme (B)). **i204** (overlayClassify + literalWord, incl. the still-unencoded plain `Imm16`/`udf` SlotKind 0x08) is the remaining b8e encoder brick — unblocked, but the test variant now has only **20 B** headroom, so i204's inline dispatch will likely need another commoning pass (mirror i218; candidates `ml_div*`/`main_dir_org_*` per `build/coverage-report.txt`). Earlier open items unchanged: **i207** (harness pre-loads-payloads fidelity gap), **i213**, **i215**, **i216**, **i121d**.
- **★ NEXT — pull the TOP item from `build/registry ready` (run from the repo root; authoritative, == the `docs/notes/backlog.md` tip). Do NOT judgment-pick, skip the tip, or grep the markdown views to improvise an ordering.** If the tip turns out to be blocked by something *not yet tracked* as a `depends_on` edge, that is a **missing-edge bug** (no-`BLOCKED` model — all blocking is an edge): ensure the blocker is a tracked item (`build/registry add …`, `--owner pete` if hardware/Pete-gated), then `build/registry dep add --id <tip> --on <blocker>` — the CLI auto-repairs the priority order (topological repair) — and re-pull `ready` until you get a genuinely workable tip. (Discipline landed by **i146 (#472)**: the CLI topological-repair pass; the hardware-gate edges so i141/i118b/i93b/i95b/i135 depend on the **i133** gate and leave `ready`; the fix that makes a bare `build/registry` operate on the live registry — and **never** the bundled test fixtures; and this prompt/doc sweep across `monitor.sh`, the loop README, CLAUDE.md, and here.)
- **Editor / i48c state:** i48c lexer COMPLETE (`src/asmlex.asm`); parser front-end **COMPLETE** (`src/asmparse.asm`) — instructions, all operand kinds, every B5 special form (movl/ldr=/barriers/mrs-msr/dc-tlbi), comments/blank-runs, directives, **and label/local defs (i48c-b5, #555)**. **Only B8 remains, now decomposed into b8a→b8b→b8c→b8d** (the Z80 compact-serializer port + corpus round-trip — see "B8 scope DISCOVERED" above); i39c is gated on the B8 umbrella. i41 edit-model: Go authority complete (i41a/b/c); Z80 Bricks 1/3/4a done (#387/#389/#391/#392); i41d Brick 2 (real SAM paging via `OUT (251)`) + Brick 4b (assembler-coupled, deferred) remain. **Brick 2 reassessed this session: it IS host-verifiable in the `sampage` paged harness (the "SimCoupé-only" note was stale — corrected in design §8.1), and it is net-new SAM paging DESIGN (the Go authority is flat), with two unsettled points now captured in §8.1 — resident structures must live in LMPR low memory (section C/D move with HMPR), and split/merge cross-block copy must route through a resident scratch buffer. Needs a `split` (Brick 2 → DONE, Brick 4b → fresh OPEN) when done.** **q23/q24 RESOLVED (2026-06-20):** i101 reframed — single interactive IDE on one disk, NOT sequenced phases; i41e reframed — editor is line-at-a-time (no whole-doc serialize; whole-file import = future i168).
- **HARDWARE-GATED / Pete-design (do NOT attempt the hardware half):** the client/http HSAVE `RST 8` write-out on real Trinity (i119/i70/i93/i167; i127 now closed — confirmed + emulator-modelled); **i95/i101** DHCP+Pi-boot (needs a direct cable / isolated switch + provisioned firmware); the SAMBOOT **i135c** EEPROM flash (the destructive write + power-cycle — genuinely Pete's; the i197 RE family is DONE so the injection site is settled, but **i135c is now HARD-GATED on i221** — the EEPROM-WRITE path has never run in emulation, see the CRITICAL gate above — and must be emulation-proven before any flash); i88 6b's bootable half; the i100c picker UX. Emulation-verified ≠ hardware-verified (CLAUDE.md §5).
- **★ PROPOSED NEXT SEQUENCE (re-rankable):** **(1) i121d ✅ DONE (#613)** and **(2) i221 ✅ DONE (#614)** this session. **NEXT actionable (agent, fresh-session-sized):** **i41d Brick 2** — the editor paging port (host-verifiable in the `sampage` harness; design points captured in §8.1; needs a `split`), a good full-budget task; OR the `ready` tip at pickup. **(3)** **i194** (real disk push — now gated only on i141) + **i135c** (EEPROM flash) are Pete-present hardware steps — Pete signals when back at the SAM. **Host note:** this host (Pi) has **NO Docker/SimCoupé** — SimCoupé-gated work is CI-gated only; the Go harnesses (incl. the netboot-oracle Z80 emulator and the new shared `tools/sampage`) run locally and cover most paths. **CRITICAL (i207 lesson): the Go harness PRE-LOADS payload pages, so a missing-from-disk payload passes the harness but HANGS SimCoupé — SimCoupé-verify (via CI's matrix) any boot-payload/disk-wiring change.** **Other agent-actionable ready work** (each fresh-session-sized): the SAM-side errata ports **i27b** (835769) + **i27c** (843419 + toggle) — *budget-wall-entangled* (production detection code that can't go off-axis; needs a commoning pass first, mirror i218; Go authority `errata.go`, CLAUDE.md §6); **i204** (last b8e encoder brick, also needs a commoning pass for the 20 B wall — reuse i199's harness: dispatch in `encode_inst` before the form-table path per `pass2.go`, fixtures in BOTH `encoder_skeleton_fixtures_test.go` and `src/test_encode_inst_payload.asm`, inner loop `go test -run TestBootSelfTestsPass`). Gated/large ready tips: **i32** gated on **q46** (multi-source-file shape); **i33** gated on **i24**; **i41d** Brick 2 = real SAM paging via `OUT (251)`, host-verifiable in the `sampage` harness (design §8.1; net-new paging design, not a flat-Go port), bundles a deferred Brick 4b so needs a `split` first; **i41e** needs a Pete design pass; **i24** needs **q45**.
- **QUESTION BACKLOG — heavily cleared with Pete 2026-06-23.** Resolved this session: **q47→i190 emulator merge (GO, DONE #607)**; **q38→GO** (on-SAM macros worth it — the i209 report says it "slips in", zero assemble-time cost; **i31** now depends on the B8 umbrella `i48c-b8` prerequisite, not q38); **q42→decision (a)** = the cheap `-layout`/`SpectrumFourLayout` struct extraction, NOT a `.ld` parser (**i56** rescoped/retitled, **i20** folds in). Earlier: **q36→i2** = unified shared page pool (`docs/specs/ide-memory-model-design.md`); **q41→i190** un-postponed; **q37→i27** (errata opt-in); **q40→i213 kept + i214 WONTFIX**; **q35→i122d WONTFIX**. **NEW open question — q48 (owner:pete, do NOT change behaviour until he decides):** how should the SAM treat `.section`/named-section layout? Finding — on the SAM, `.section`/`.text`/`.data` are zero-byte directives that ROUND-TRIP faithfully (`render/pc.go:200`) but are placement-inert (flat single-`.org` target, no linker; the host `-flatten` path consumes sections), so from-scratch on-SAM projects are NOT locked to spectrum4's layout. **Still awaiting Pete:** **q13** (editor default rendering; non-blocking), **q46** (i32 multi-source-file SHAPE; gates i32), **q45** (the OUT `emit_byte` perf fork; gates i24), **q48** (above). **NEW backlog: i219** (owner:pete — acquire a stock/untarnished SAM system ROM; we only have Colin's forked capture; surfaced by the i189 catalogue), **i220** (low priority, end of backlog — comprehensive Go package + function doc comments across `tools/`, partly to surface adapted-test hacks). **needs-Pete-present model LIVE (i169a #495):** `ready` excludes `owner:pete` by default; `--pete-present` includes them. **Hardware:** trinload running @ `192.168.2.75` (Pete, 2026-06-23); FIRST-LIGHT smoke+serve PASS (2026-06-18). Lower-priority/open: **i89**, **i81a/i81c**, **i106**; i7 phase **D=i74** gated (do not start); i77 parked; i113 OCR UNVERIFIED.
- Every strand keeps the **assembled-binary byte-identical** invariant (the `release-gate` 3-way byte-match); the i39 invariant is binary-identity + round-trip + `.tbn`-shrinks-or-holds, NOT `.tbn` byte-identity.
<!-- HANDOVER-PROTOCOL-END -->

Canonical index of milestones, design specs, and deferred work. **Update this any time a design doc is added, a milestone changes state, or deferred work gets folded into a milestone.**

## Vision

A SAM Coupé Z80 program that hosts a complete aarch64 development workflow — editor, assembler, disassembler, TFTP shipper — and produces byte-identical binaries to GNU `aarch64-none-elf-as` for the spectrum4 kernel. Daily-driver SAM-as-development-machine for Raspberry Pi 400 bare-metal work.

See `docs/specs/vision.md` for the long-form pitch and `docs/specs/phase1-assembler.md` for the Phase 1 (assembler) shape.

## Milestone status

| M | Title | Spec | Status doc | State |
|---|---|---|---|---|
| M0 | Toolchain bootstrap (pyz80 → SimCoupé → samfile → GNU `as` round-trip) | — | — | ✅ done (PR #1) |
| M1 | Binary tokenised source format (`.tbn`) + text2bin / bin2text | — | — | ✅ done (PR #6) |
| M2 | Encoder tables + Mac-side refenc; 20/20 M1 fixtures byte-match GNU | — | — | ✅ done (PR #7, #8); extended via #11, #14, #15, #17 |
| M3 | Z80 emitter: read `.tbn`, encode, HSAVE output (no symbol table; constant-only) | — | — | ✅ done (PRs #9, #12, #13, #16, #17, #19); 9/9 fixtures byte-match GNU end-to-end via SimCoupé |
| M4 | Symbol table, multi-pass, full expression evaluator on Z80 | — | — | ✅ done (PRs #21, #22, #23); 4/4 M4 fixtures byte-match GNU end-to-end via SimCoupé |
| M5 | Compound operands (`OpShiftedReg`, `OpExtendedReg`, `OpMem`, `OpSysName`, `OpLitPool`) + remaining directives (`.set`/`.equ`, `.balign`/`.align`, `.org`, `.skip`/`.space`, `.inst`, `.ltorg`) + `ror`-imm intercept | — | — | ✅ done (PRs #29–#34); 19/19 M5 fixtures byte-match GNU end-to-end via SimCoupé |
| M6 | Paged OUT + paged IN + compact `.tbn` format + built-in disassembler (real spectrum4 fixture round-tripping byte-identical via SAM) | `docs/specs/paged-out-design.md` + `docs/specs/paged-in-design.md` | — | ✅ done (PR #76) — paged OUT/IN, `paged_call`, multi-digit local labels, off-axis tables, release-stripped flatten, Go harness all landed (PRs #29-#73).  **Headline "spectrum4 release.bin byte-match on SAM" DONE 2026-05-29**: the full 88 KB release-stripped flows through SimCoupé and the SAM prod assembler HSAVEs a 21752-byte OUT byte-identical to GNU (8 encoder bug classes found+fixed, 358→0; PR #73).  **Closed by PR #76**: the `release-gate` CI gate (a hermetic 3-way byte-match — GNU == our Go toolchain == our Z80/SAM toolchain — over a vendored flattened release source, `tools/run-release-gate.sh`) + the `&C000` code-budget assertion now stand guard.  Compact `.tbn` + on-SAM disassembler + codegen tables move to M7. |
| M7 | Post-M6 consolidation: on-SAM disassembler (strand B) + compact `.tbn` (i1) + codegen tables + repo-cleanup/housekeeping + parity/robustness + Go-harness fidelity | — | — | ✅ done — headlines: on-SAM disassembler (PRs #93–#103), compact `.tbn` i1 (PRs #121–#124, −42.3%). Tail items i7/i17/i18 live in the item registry. |
| M8 | Next-gen compact `.tbn` **v2** — the instruction-overlay format (Format B): assembled-word + zeroed-bits + sparse expression overlay unifying literal/symbolic instructions; header label/offset table; one file with an evictable editor region; name front-coding. Foundation for the on-SAM editor. | `docs/specs/compact-tbn-nextgen-design.md` (design, §7 decisions) + `docs/specs/tbn-binary-format-reference.md` (v1 baseline) | — | ✅ done (closed 2026-06-12) — v2 overlay format shipped end-to-end: i39a/#131, i39b-1/#151 + i39b-2/#153, i40 + i51/#181, i48 host side/#141–#144. The B-DOS era opened at close: i71/#186 (reference vendored) + i72/#187 (`build-disk -dos` prep). Residuals migrated to M9 (i39c → i48c). |
| M9 | Editor era — Phase 2 foundations: codegen tables (i7), B-DOS boot-disk swap (i75), paged block-list edit model (i41), SAM-side text→overlay encoder (i48c, absorbs i39c), read-only viewer (i4), UI decisions (i6/i5/q1) | `docs/specs/editor-edit-model-design.md` + `docs/specs/comment-storage-design.md` + `docs/specs/codegen-tables-design.md` | `docs/notes/m9-status.md` | ⏳ active |
| (Phase 2) | On-SAM editor | `docs/specs/phase1-assembler.md` §editor + `docs/specs/editor-vision.md` (design pointers) + future Phase-2 spec | — | ⏳ started — M9 carries the foundations |
| (Phase 3) | SAM as aarch64 netboot server (Quazar Trinity) — DHCP+TFTP to any Pi, over a direct cable or a shared LAN | `docs/specs/phase3-delivery-design.md` (i84 — **design confirmed** Pete 2026-06-14) + `docs/plans/phase3-netboot-implementation-plan.md` + `docs/notes/netboot-trinity-testing.md` (on-hardware guide) | — | ⏳ implementation underway — **host oracle harness** (i91/#219), the **Z80 packet build/parse logic** (i92), the **i80 Trinity emulation + the three wire-I/O transfer state machines** (DHCP responder / TFTP server / TFTP client — host-verified byte-for-byte over the emulation), both **seams** (i82 originate front + i93 B-DOS store), the **bring-up smoke test** (i94, `make netboot-smoke-disk`), the **integrated netboot server** (i95 — `netboot_serve_once`, the bootable `make netboot-server-disk`, host-verified over the i80 emulation byte-for-byte vs `server.Server.OnFrame`), the **serve-files TFTP demo** (i96 — a plain-TFTP server, ARP+TFTP only, `make netboot-serve-disk`, testable with a stock `tftp`/`curl` client; bare-RRQ→DATA per RFC 2347 + optioned-RRQ→OACK, host-verified vs `serve.Responder.OnFrame`), and the **TFTP client boot disk** (i82 — `make netboot-client-disk`, fetches a `.mgt` from a TFTP server + HSAVEs it to Trinity storage; the ARP→RRQ→DATA/ACK wire side host-verified vs `client.Client`, the B-DOS HSAVE write-out hardware-gated) all landed. Design confirmed (i84, q11/q12): Model A; SAM=DHCP+TFTP server serving by-name; both topologies; on-SAM HTTP self-provisioning. **Remaining (autonomous, unblocked):** HTTP (i70) + HTTPS (i88). The protocol logic + ENC28J60 wire I/O are host-verified over the i80 emulation; an **end-to-end Pi boot + real-silicon timing + the RST-8 hook dispatch stay gated on real Trinity** (CLAUDE.md §5). Reference: `simonowen/trinload` |

Legend: ✅ done · ⏳ in progress · 📋 designed, not started

## Design notes not strictly inside a milestone

These are patterns or research findings that get applied *within* milestones rather than constituting their own milestone:

| Note | When to apply | Status |
|---|---|---|
| `docs/ARCHITECTURE.md` | **The first read**: the synthesized system overview (system shape, authority model, encoder tables, memory + paging, `.tbn` v2, build/test pipeline, dev inner loop). Each section links its deep spec. | ✅ living overview |
| `docs/specs/tbn-binary-format-reference.md` | The single authoritative reference for the complete `.tbn` binary encoding (header, records, operands, expr bytecode, directives, compaction levels). Read/cite this whenever touching the format; supersedes the retired M1 spec's tables. | ✅ living reference |
| `docs/specs/samdos-file-io.md` | Reference for the DOS file-I/O hook layer (SAMDOS 2 / B-DOS): the HLOAD trampoline pattern (READ), the HSAVE UIFA pattern (WRITE), hook register-clobbering facts, pre-built Z80 snippets. | ✅ living reference |
| `docs/notes/sam-stub-audit.md` | Already applied (PR #13 loader fix used findings). Keep as reference for any future DOS hook work. | ✅ applied; reference |
| `docs/notes/sam-paging.md` | Reference for any LMPR/HMPR work. | ✅ reference |

## How to extend this doc

When new deferred work or a design decision arises:
1. Add an `iN` row via `build/registry add --title "…" --desc "…" --status OPEN --owner agent` (the id is auto-allocated; add `--space questions` for a `qN`; use `build/registry split --parent iNN --title "…" …` for a sub-item — its id is determined from the parent), then `make registry`. Commit the YAML and regenerated `.md` together. That is the one home — do not add a checklist here.
2. Add the new design note to the appropriate table above (milestone, or "Design notes not strictly inside a milestone") and link it from `docs/specs/README.md` if it lives under `docs/specs/`.

The goal: no design discussion is ever lost to context drift. If it's worth writing down, it's worth a registry row.
