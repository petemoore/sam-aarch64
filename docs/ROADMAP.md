# sam-aarch64 roadmap

<!-- HANDOVER-PROTOCOL-START -->
## START HERE — session handover protocol (read this first)

**This file is the canonical entry point and the agent→agent handover contract.** If you are an agent picking up this project: read this section, then the **"Current State & Next Actions"** block below, then the current milestone status doc, then the project memory index (auto-loaded at session start). That is the complete start-up read — everything else is reached via links from here.

### The contract (always inherited, always maintained)

1. **Tracking is part of "done."** A new idea, decision, or deferred item is not captured until it is an `iN` row in the item registry (or `qN` in the question registry) — **in the same change that introduces it**. Treat a missing tracking entry like a missing test: reviewers flag it. This is the rule that stops ideas being lost to context drift.
2. **One home per kind of thing** (this is how the docs stay un-sprawled):
   - **ROADMAP.md** (this file) — the index, the backlog, and the live "Current State" below.
   - **Item registry** (`docs/notes/item-registry.md`) — the project-wide, milestone-neutral `iN` id↔item map. Never archived. Its header documents the **shared registry discipline for both `iN` items and `qN` questions**.
   - **Question registry** (`docs/notes/question-registry.md`) — the project-wide, milestone-neutral `qN` open-questions-for-Pete map. Never archived; sibling of the item registry (same discipline). Every question for Pete goes here the moment it arises.
   - **Milestone status docs** (`docs/notes/m{N}-status.md`) — the per-milestone source of truth.
   - **Memory** (`~/.claude/projects/.../memory/`) — cross-session preferences, facts, feedback. *Not* work-tracking.
   - Superseded docs are **deleted** — git history is the archive; pin citations to a blob link if a living doc must cite one.
3. **At milestone close**, walk the item and question registries and ask of each item: still deferred, or does it now fold into the milestone in flight? **The `iN` item registry and the `qN` question registry are both milestone-neutral (`docs/notes/item-registry.md`, `docs/notes/question-registry.md`) and never archived:** at milestone close, every item belonging to that milestone must be marked ✅ done or ❌ wontfix in the registry, or migrated (re-pointed) to the active milestone's strands — no item may be left living only in a closing `m{N}-status` doc. Open questions for Pete live only in the question registry (`qN`) for the same reason. **The milestone status doc is deleted at close** (git history is the archive) after the registry walk completes.
4. **Delegate to subagents to preserve orchestration context.** The agent driving a session (the *orchestrator*) should push self-contained, well-scoped work out to subagents — codebase searches, mechanical edits, doc drafting, focused investigations, even whole low-risk PRs (implement → verify locally → commit → push → open PR) — via the `Agent` tool (and `Explore` / `Plan`), keeping only the conclusions in its own window. A subagent's full transcript is discarded; only its final summary returns, so the orchestrator's context lasts far longer. The orchestrator keeps the judgement calls itself: sequencing, monitoring CI, merge/launch decisions, and anything user-facing. The durable state lives in subagents-plus-these-docs, not in any one agent's window — so when context *does* fill, the handover (update "Current State", below) lets the next agent resume as orchestrator with nothing lost. Prefer launching independent subagents in parallel (one message, multiple `Agent` calls) when the work has no shared state.

### Session hygiene (the minimal ritual)

- **Closing a session:** **no magic words** — *any* wind-down phrasing ("close the session", "let's wrap up", "prepare for handover", "goodnight, what's next?") triggers this. The agent then updates the **"Current State & Next Actions"** block below *in place* — what landed, what's in flight (open PRs / branches / running agents), and the single immediate next action; confirms tracking is current (rule 1); and leaves the working copy on a clean, current `main`. Do **not** write a new dated handoff doc — update the standing block. **Get this update merged to `main` before handover** — a quick docs PR + CI, since branch protection blocks direct pushes. Never hand over with the live state stuck in an open PR, or the next session reads stale context.
- **Starting a session:** the standard start prompt is just **"Continue per docs/ROADMAP.md."** A SessionStart hook (`tools/session-handover.sh`) runs first: it surfaces this section and, **when you are on a clean `main`, auto-`fetch`es and fast-forwards to the latest** so the doc you read is current. If it instead **warns** (you are on a feature branch, have uncommitted changes, or `main` can't fast-forward), refresh deliberately before relying on the doc: stash/commit your work, then `git checkout main && git pull --ff-only`. Then read this section → Current State → resume; no bespoke per-session prompt is needed.

### Current State & Next Actions

*Updated in place each session — this is the live handover. Keep it ≤15 lines; history lives in the milestone status docs, the registries, and `git log`.*

- **Milestone:** **M9 active — the editor era** (`docs/notes/m9-status.md`). M0–M8 ✅ complete. A **Phase-3 design strand** (i80–i84) runs alongside, per Pete 2026-06-13.
- **Last landed (Phase-3 design strand, 2026-06-13→14):** i82 ✅ (#210 research note), i84 ✅ (#211 design), i81 ✅ triaged; **#213** — Pete resolved **q11/q12**, design **CONFIRMED** (Model A; SAM = DHCP+TFTP server serving by-name/model-agnostic; both topologies; on-SAM HTTP provisioning; patched-ROM bootstrap; new i86/i87; Colin dropped). **#214** — Pete's real Pi 400 captures (`~/tftp-logs`) → `docs/notes/pi-netboot-capture-analysis.md`, the **DHCP+TFTP server oracle** (exact option-43 `Raspberry Pi Boot` blob, OACK `tsize`/`blksize`, serve-by-name + ERROR(1)-tolerant probe; the served-file *set* varies — the mechanism is the invariant); new items **i88** (HTTPS-from-GitHub firmware fetch — stretch) + **i89** (Pi 3 capture, future). **This session:** the **netboot implementation plan** (`docs/plans/phase3-netboot-implementation-plan.md`) — i86/i83/i82 sequenced, grounded in the design + oracle + trinload reuse-map; key finding: trinload has **no fresh-frame origination** (all sends are reply-by-swap), so the i82 client builds the `build_udp_frame` primitive the i86 DHCP responder reuses; **plan only, gated on Pete's go-ahead** (no part is host-verifiable without i80/hardware — only the unit packet builder/parser vs the captures is). Also landed: the **i78 source-structure-preservation design** (`docs/specs/source-structure-preservation-design.md`) — a blank-run row kind in the editor-region sidecar (disambiguated from textless `//`) + the multi-line-comment grouping rule (one entry, body newlines); indentation deferred behind the i76 interface sign-off. Build host-first; assembler/binary untouched. And the **i76 editor rendering-rules spec** (`docs/specs/editor-rendering-rules-design.md`) — §3–§9 write the config lab's rendering mechanics (render ladder, wrap/shift, 4-pen roles + marking, cursor expansion, comment/truncation/chrome) into a portable contract; §10 **recommends** a default shipping config (tuned `compressed.config`) → Pete's sign-off as new **q13** (open, non-blocking; geometry/font stay gated on the i6 P3 memo).
- **Last landed (2026-06-14):** the **i92 Z80 netboot port** is underway, each brick host-verified byte-equal to the i91 Go authority (`tools/netboot-oracle/`) via the flat-memory koron-go/z80 harness (`tools/netboot-oracle/z80/`, a nested module; `make ci-netboot-z80`, the `netboot-z80` CI job). Landed: **`build_udp_frame`** (#221, the fresh-frame primitive trinload lacks, plan §5.1; port of `frame/frame.go::BuildUDPFrame` + the trinload `chksum_blk` IP-checksum idiom), **`build_dhcp_reply`** (#222, the i86 OFFER/ACK body: option template + the fixed 32-byte option-43 "Raspberry Pi Boot" blob + the echoed UUID; port of `dhcp/dhcp.go::BuildReply`), the **i83 TFTP server reply builders** `build_oack`/`build_data`/`build_error` (#223 — each also byte-exact to the captured OACK/DATA/ERROR; port of `tftp/tftp.go::BuildOACK`/`BuildDATA`/`BuildError`), the **i83 TFTP server request side** `parse_request`/`resolve` (#224 — RRQ parse + serve-by-name resolve: 404 serial-subdir, OACK a hit with the stored size, ERROR(1) every miss; verified against the captured RRQs; port of `tftp/tftp.go::ParseRequest` + `tftp/server.go::Resolve`), the **i82 TFTP client request side** `build_rrq`/`parse_oack` (#225 — RRQ builder byte-exact to the captured RRQs + the client option set, OACK parse learning the negotiated blksize / file tsize; port of `tftp/tftp.go::BuildRRQ` + `::ParseOACK`), and the **ARP-request builder** `build_arp_request` (#226 — the fresh Ethernet ARP request the client broadcasts to learn the server MAC, plan §5.1; byte-exact to the new Go authority `frame/frame.go::BuildARPRequest`). Foundation: **#219** i91 host oracle (pure-Go DHCP/TFTP builders+parsers + masked golden vectors from Pete's real Pi 400 capture, 17 tests). (Earlier: **#218** i78(a) blank-run + finding i90 — see `git log`.)
- **This session (i80 first brick — the wire-I/O unblock):** the **emulated Trinity Ethernet path** lands (`tools/netboot-oracle/z80/enc28j60.go`) — the Trinity microcontroller (`&DC` select/busy + `&DD` identity probe) + the ENC28J60 SPI chip (`&DE`: RCR/WCR/RBM/WBM/BFS/BFC, 4 banks, 8 KB RX/TX buffer, MAC/MII read-dummy), grounded in the ENC28J60 datasheet (DS39662E) + the driver's port usage. The **real vendored driver** `src/netboot/encdrv.asm` (simonowen/trinload, verbatim) runs against it under the koron-go/z80 harness and asserts byte-exact: `drv_init` succeeds (+ missing-board negative control), `drv_write` → frame on the virtual wire (incl. a captured RRQ), an injected RX → `drv_read`, and a write→loopback→read of a real DHCP frame. Wired into CI (`make ci-netboot-z80`, the `netboot-z80` job). Found + corrected (at the harness boundary) a Z80 spec bug in koron-go/z80 v0.10.2 (INI/IND-family pass B as the port, not C). This turns the ENC28J60 wire I/O from hardware-gated into host-verifiable; **emulation-verified ≠ hardware-verified** — an end-to-end Pi boot stays the real-Trinity gate.
- **Last landed (wire-I/O state machines, 2026-06-14):** the **i86 DHCP responder loop** — `src/netboot/dhcp_loop.asm`'s `dhcp_serve_once` state machine (`drv_read` → dispatch DISCOVER→OFFER / REQUEST→ACK → per-MAC lease from a fixed pool → `build_dhcp_reply` + `build_udp_frame` UDP 67→68 broadcast → `drv_write`) composes the host-verified packet primitives + the real `encdrv.asm` and runs over the **i80 emulated Trinity**, host-verified **byte-for-byte vs the new Go authority** `dhcp/responder.go::Responder.OnRequest` (the OFFER + the full DORA + non-DHCP-ignored + empty-wire controls). Fixed a multi-packet-RX gap in the emulation (chip RX write position follows the next-pointer chain, not ERXRDPT). Wired into `make ci-netboot-z80`. This is the **first wire-I/O state machine made host-verifiable end-to-end** — emulation-verified, not hardware-verified. Then the **i83 TFTP server transfer loop** — `src/netboot/tftp_server_loop.asm` (`tftp_handle_rrq` → OACK on a hit / ERROR(1) on a miss + serial-subdir-404; `tftp_first_data` + `tftp_handle_ack` → the streamed DATA/ACK loop, short-final-block ends it) composes the host-verified `tftp_build`/`tftp_parse`/`build_udp_frame` + `encdrv.asm`, runs over the i80 emulation, and is host-verified **byte-for-byte vs the new Go authority** `tftp/serverloop.go::ServerLoop` (the OACK, the full transfer incl. the exact-multiple zero-length terminator, ERROR-on-miss, serial-subdir-404). New Z80 `negotiate_blksize` + `write_dec_u16`/`parse_dec_u16` for the OACK decimals. The flat-store B-DOS name→record index is the remaining server piece (the test fills an in-memory store). Then the **i82 TFTP client transfer loop** — `src/netboot/tftp_client_loop.asm` (`tftp_recv_data` → learn+validate the server TID, accumulate the payload, ACK, short-final-block ends it, unknown-TID → ERROR(5) to the stray sender per RFC 1350 §4; `tftp_recv_timeout` → the Sorcerer's-Apprentice retransmit-the-last-ACK-only fix) composes the host-verified `build_udp_frame`/`tftp_client` + `encdrv.asm`, runs over the i80 emulation, host-verified **byte-for-byte vs the new Go authority** `tftp/clientloop.go::ClientLoop` (the ACK cadence, the two-block transfer with accumulated bytes, unknown-TID ERROR, SAS retransmit). All three netboot transfer state machines (DHCP responder, TFTP server, TFTP client) are now host-verified end-to-end over the emulation.
- **This session (i82 originate front — request-origination seam (a)):** the **i82 client ARP-for-server + RRQ-send front** lands (`src/netboot/tftp_client_front.asm`) — `tftp_send_arp` (broadcast `build_arp_request` for SERVER_IP → `drv_write`), `tftp_recv_arp` (`drv_read` → ARP reply (EtherType &0806, OPER=2) for SERVER_IP? learn the server MAC → BC=1; else BC=0, keep reading), `tftp_send_rrq` (`build_rrq` octet + the settled ClientOptionSet → wrap UDP our IP+TID → the learned server MAC+IP:69 → `drv_write`). Composes the host-verified `build_arp_request`/`build_rrq`/`build_udp_frame` + the real `encdrv.asm`, runs over the **i80 emulated Trinity**, and is host-verified **byte-for-byte vs the new Go authority** `tftp/clientfront.go::ClientFront` (+ `frame.ParseARPReply`): the ARP request + the RRQ frame on the virtual wire, plus the wrong-IP-reply-ignored control. New `make netboot-tftp-client-front` target + `tftp_client_front_test.go` wired into `make ci-netboot-z80`. This completes the i82 client's **request-origination seam** (the client now originates the whole ARP→RRQ→recv flow); the **B-DOS write-out** (received blocks → records) is the remaining client piece, folded into the B-DOS-store increment (b). **Emulation-verified ≠ hardware-verified.**
- **This session (i93 B-DOS storage seam — storage seam (b)):** the **B-DOS flat-store name→record seam** lands as new item **i93** (`src/netboot/bdos_seam.asm` + `tools/netboot-oracle/bdos/`) — the name→record glue shared by the i83 server (serve by name) + the i82 client (write by name). Three routines ported from the new Go authority `bdos/bdos.go` and **host-verified byte-for-byte** (`make ci-netboot-z80`): `bdos_name_to_uifa` (name → the 48-byte UIFA name block HGTHD/HSAVE read), `bdos_difa_to_size` (DIFA pages×16384 + length-mod-16K → the 32-bit file size the OACK `tsize` reports), `bdos_fill_save_uifa` (the inverse HSAVE UIFA for the client write-out). Plus `bdos.Store` — the flat name→size directory model the loops resolve against (its `Wire()` is the format the existing Z80 `resolve` already walks). The field conventions are ported verbatim from the assembler's own real hook call sites (`src/io.asm`/`src/loader.asm`/`src/main_loop.asm`) + `docs/specs/samdos-file-io.md`. **The honesty line (CLAUDE.md §5):** the field arithmetic IS host-verifiable (pure memory build/decode) and merges green; the actual **RST 8 hook dispatch** (HGTHD/HLOAD/HSAVE/HRECORD) is NOT — the harness has no ROM/SAMDOS bank/RST 8, so the hook bodies (behind `NETBOOT_HOSTTEST`, excluded from the host build) stay UNVERIFIED until real Trinity (i62 proved them on real SAMDOS-2 + B-DOS-AL; the end-to-end fetch is the final integration gate). New `make netboot-bdos-seam` + `bdos_seam_test.go` + `bdos/bdos_test.go` wired into the netboot CI jobs. **Emulation-verified ≠ hardware-verified.**
- **Last landed (i7 phase C2 — codegen mnemonic IDs, 2026-06-14):** the `MNEM_<NAME>` mnemonic-ID equates (`src/mnemonic_ids.inc`) are now **generated from the Go authority** (`mnemonics.go` `MnemonicTable`) by a new `-mnemonic-ids-inc` emitter (`tools/tables-gen/emit/mnemonicids.go`); `src/intercepts.asm` `include`s it and **every bare-numeric `cp <id>` dispatch site now names its mnemonic** — the top dispatcher (ror/csetm/bic/mov/tst/add/sub/ldp/stp/ldr/lsl/lsr/bfi/bfxil/ubfx/bfc/sbfx/tbz/tbnz/isb/dsb/dmb/mrs/msr/dc/tlbi), the `is_mem_mnemonic_id` 17-ID list, `is_shifted_reg_mnemonic`, and the barrier/lslsr/bitfield/tbz encoders. Conditional-branch names map `.`→`_` (`b.eq`→`MNEM_B_EQ`). **Byte-identical** `assembler{,-prod}.bin` (equates resolve at assembly time — the rename-only invariant verified by `cmp`); `make tables` regenerates all three `.inc` files and the broadened `make tables-sync-check` (in `sysreg-sync`) fails on drift; `TestMnemonicIDsZ80Sync` is the emitter-faithfulness guard. **Earlier in phase C:** C1 generated `src/tbn_constants.inc` (`OP_KIND_*`/`REC_KIND_*`/`DIR_*`, the `REC_KIND_*` 6-of-9 subset); phase B generated `src/sysreg_tables.inc`; phase A deleted ENCTAB_LEN. **All of i7 phase C is done.** The only remaining i7 phase is D = i74 (gated on the `aarch64dec/sys.go` code→data refactor — not started).
- **This session (i94 — the first bootable, runnable netboot program):** the **Trinity bring-up smoke test** lands (`src/netboot/smoke_test.asm`) — the prior netboot work (i91/i92/i80/i93) was *verified components*; this is the first netboot program Pete can **boot and run on his real Trinity**. It runs `drv_init` on the real board then loops answering **ARP requests for the SAM's IP** with an ARP reply — one observable network action proving the Ethernet path comes up and talks (the "hello, the wire works" before the full server). New `build_arp_reply.asm` (the inverse of `build_arp_request`; port of the new Go authority `frame.BuildARPReply`) + a `smoke/smoke.go::Responder` authority; the bootable program reads the SAM's MAC/IP from the Trinity EEPROM via the vendored `eeprom.asm` (Colin Piggot, simonowen/trinload, verbatim). Ships a **bootable SAM disk** (`make netboot-smoke-disk` → B-DOS boot + AUTO LOADs+CALLs it at &8000) via a new `build-disk -netboot` mode + the on-hardware guide `docs/notes/netboot-trinity-testing.md`. **`smoke_serve_once` is host-verified end-to-end over the i80 emulation** (the ARP reply on the virtual wire is byte-for-byte `smoke.Responder.OnFrame`, `smoke_test_test.go`, 5 cases; `make ci-netboot-z80`); the EEPROM read + real ENC28J60 TX/RX timing + the actual ARP round trip are **Pete's on-hardware test**. Emulation-verified ≠ hardware-verified.
- **In flight:** nothing.
- **AWAITING PETE (his calls — none block the next agent):** (a) **q13** — the editor default rendering config (a taste call; agent recommendation on the table, A/B the four starter configs); (b) future hardware artifacts when convenient — the **patched-ROM dump** (i87) and a **Pi 3 capture** (i89), both non-blocking; (c) **i81 contributions** are community-facing so Pete submits — the aarch64 PNG-endian one-liner upstream, then a short #28 comment.
- **Phase-3 implementation: AUTHORIZED + UNDERWAY** (memory `feedback_implementation_autonomy`, 2026-06-14) — spec-gate lifted; the Z80 netboot port proceeds per `docs/plans/phase3-netboot-implementation-plan.md`. **Verification line (the load-bearing distinction):** the netboot **protocol logic** — every packet *build/parse* routine — **IS host-verifiable** and merges green: the Z80 routine is pure arithmetic + memory writes, so running it under the koron-go/z80 harness and byte-comparing its output to the golden vector is a real, observable check (i92 proves the pattern). **As of i80's first brick (this session) the ENC28J60 wire I/O is host-verifiable too** — the Trinity board is emulated host-side (Go) and the real driver runs against it (frame bytes in/out asserted byte-exact). Still NOT host-verifiable: an **end-to-end Pi boot** + real-silicon TX/RX timing — gated on **real Trinity hardware** (the final integration gate; CLAUDE.md §5). Emulation-verified ≠ hardware-verified. The oracle (`tools/netboot-oracle/`) is the byte-for-byte porting spec.
- **NEXT ACTION (autonomous — the queue is NOT drained):** build the **bootable netboot programs** that compose the verified components into runnable disks Pete can boot on real Trinity. The increments, in order: **(2) the netboot server** — the integrated main loop (`drv_init` → loop { `drv_read` → dispatch ARP / DHCP responder (i86) / TFTP server (i83) → serve by name from the B-DOS store (i93) → `drv_write` }), a bootable disk so a Pi on Pete's LAN netboots from the SAM; **(3) the client boot disk** (i82, fetch + B-DOS write-out, for provisioning firmware from a TFTP source); then **(4) HTTP fetch** (i70 / uIP) and **(5) HTTPS-from-GitHub** (i88, the TLS-1.3-on-Z80 stretch). Each ships a bootable artifact + a "how to test on real Trinity" section in `docs/notes/netboot-trinity-testing.md`. The protocol logic + ENC28J60 wire I/O are host-verifiable over the i80 emulation (assert the session on the virtual wire); the **on-hardware run is Pete's test** (CLAUDE.md §5) — that does NOT block writing the programs. i94 (the smoke test) is increment 1, done. **Separately:** i7 phase C is fully done; the only remaining i7 phase is **D = i74**, gated on the `aarch64dec/sys.go` code→data refactor — **NOT to be started** without that prerequisite (genuinely Pete-gated). i77 parked; q13 + i81 + hardware artifacts (i87/i89) wait on Pete. Don't pivot to editor (M9/i78/q13 — Pete's interface thinking pending). Don't start i74.
- Every strand keeps the **assembled-binary byte-identical** invariant (the `release-gate` 3-way byte-match); the i39 invariant is binary-identity + round-trip + `.tbn`-shrinks-or-holds, NOT `.tbn` byte-identity.
<!-- HANDOVER-PROTOCOL-END -->

Canonical index of milestones, design specs, and deferred work. **Update this any time a design doc is added, a milestone changes state, or deferred work gets folded into a milestone.**

## Vision

A SAM Coupé Z80 program that hosts a complete aarch64 development workflow — editor, assembler, disassembler, TFTP shipper — and produces byte-identical binaries to GNU `aarch64-none-elf-as` for the spectrum4 kernel. Daily-driver SAM-as-development-machine for Raspberry Pi 400 bare-metal work.

See `docs/specs/vision.md` for the long-form pitch and `docs/specs/phase1-assembler.md` for the Phase 1 (assembler) shape.

## Milestone status

| M | Title | Spec | Status doc | State |
|---|---|---|---|---|
| M0 | Toolchain bootstrap (pyz80 → SimCoupé → samfile → GNU `as` round-trip) | — | — | ✅ done (PR #1) |
| M1 | Binary tokenised source format (`.tbn`) + text2bin / bin2text | — | — | ✅ done (PR #6) |
| M2 | Encoder tables + Mac-side refenc; 20/20 M1 fixtures byte-match GNU | — | — | ✅ done (PR #7, #8); extended via #11, #14, #15, #17 |
| M3 | Z80 emitter: read `.tbn`, encode, HSAVE output (no symbol table; constant-only) | — | — | ✅ done (PRs #9, #12, #13, #16, #17, #19); 9/9 fixtures byte-match GNU end-to-end via SimCoupé |
| M4 | Symbol table, multi-pass, full expression evaluator on Z80 | — | — | ✅ done (PRs #21, #22, #23); 4/4 M4 fixtures byte-match GNU end-to-end via SimCoupé |
| M5 | Compound operands (`OpShiftedReg`, `OpExtendedReg`, `OpMem`, `OpSysName`, `OpLitPool`) + remaining directives (`.set`/`.equ`, `.balign`/`.align`, `.org`, `.skip`/`.space`, `.inst`, `.ltorg`) + `ror`-imm intercept | — | — | ✅ done (PRs #29–#34); 19/19 M5 fixtures byte-match GNU end-to-end via SimCoupé |
| M6 | Paged OUT + paged IN + compact `.tbn` format + built-in disassembler (real spectrum4 fixture round-tripping byte-identical via SAM) | `docs/specs/paged-out-design.md` + `docs/specs/paged-in-design.md` | — | ✅ done (PR #76) — paged OUT/IN, `paged_call`, multi-digit local labels, off-axis tables, release-stripped flatten, Go harness all landed (PRs #29-#73).  **Headline "spectrum4 release.bin byte-match on SAM" DONE 2026-05-29**: the full 88 KB release-stripped flows through SimCoupé and the SAM prod assembler HSAVEs a 21752-byte OUT byte-identical to GNU (8 encoder bug classes found+fixed, 358→0; PR #73).  **Closed by PR #76**: the `release-gate` CI gate (a hermetic 3-way byte-match — GNU == our Go toolchain == our Z80/SAM toolchain — over a vendored flattened release source, `tools/run-release-gate.sh`) + the `&C000` code-budget assertion now stand guard.  Compact `.tbn` + on-SAM disassembler + codegen tables move to M7. |
| M7 | Post-M6 consolidation: on-SAM disassembler (strand B) + compact `.tbn` (i1) + codegen tables + repo-cleanup/housekeeping + parity/robustness + Go-harness fidelity | — | — | ✅ done — headlines: on-SAM disassembler (PRs #93–#103), compact `.tbn` i1 (PRs #121–#124, −42.3%). Tail items i7/i17/i18 live in the item registry. |
| M8 | Next-gen compact `.tbn` **v2** — the instruction-overlay format (Format B): assembled-word + zeroed-bits + sparse expression overlay unifying literal/symbolic instructions; header label/offset table; one file with an evictable editor region; name front-coding. Foundation for the on-SAM editor. | `docs/specs/compact-tbn-nextgen-design.md` (design, §7 decisions) + `docs/specs/tbn-binary-format-reference.md` (v1 baseline) | — | ✅ done (closed 2026-06-12) — v2 overlay format shipped end-to-end: i39a/#131, i39b-1/#151 + i39b-2/#153, i40 + i51/#181, i48 host side/#141–#144. The B-DOS era opened at close: i71/#186 (reference vendored) + i72/#187 (`build-disk -dos` prep). Residuals migrated to M9 (i39c → i48c). |
| M9 | Editor era — Phase 2 foundations: codegen tables (i7), B-DOS boot-disk swap (i75), paged block-list edit model (i41), SAM-side text→overlay encoder (i48c, absorbs i39c), read-only viewer (i4), UI decisions (i6/i5/q1) | `docs/specs/editor-edit-model-design.md` + `docs/specs/comment-storage-design.md` + `docs/specs/codegen-tables-design.md` | `docs/notes/m9-status.md` | ⏳ active |
| (Phase 2) | On-SAM editor | `docs/specs/phase1-assembler.md` §editor + `docs/specs/editor-vision.md` (design pointers) + future Phase-2 spec | — | ⏳ started — M9 carries the foundations |
| (Phase 3) | SAM as aarch64 netboot server (Quazar Trinity) — DHCP+TFTP to any Pi, over a direct cable or a shared LAN | `docs/specs/phase3-delivery-design.md` (i84 — **design confirmed** Pete 2026-06-14) + `docs/plans/phase3-netboot-implementation-plan.md` + `docs/notes/netboot-trinity-testing.md` (on-hardware guide) | — | ⏳ implementation underway — **host oracle harness** (i91/#219), the **Z80 packet build/parse logic** (i92), the **i80 Trinity emulation + the three wire-I/O transfer state machines** (DHCP responder / TFTP server / TFTP client — host-verified byte-for-byte over the emulation), both **seams** (i82 originate front + i93 B-DOS store), and the **first bootable program** (i94, the bring-up smoke test — `make netboot-smoke-disk`) all landed. Design confirmed (i84, q11/q12): Model A; SAM=DHCP+TFTP server serving by-name; both topologies; on-SAM HTTP self-provisioning. **Remaining (autonomous, unblocked):** compose the verified components into the **bootable netboot server** (the integrated main loop + B-DOS store dispatch) and **client** disks, then HTTP (i70) + HTTPS (i88). The protocol logic + ENC28J60 wire I/O are host-verified over the i80 emulation; an **end-to-end Pi boot + real-silicon timing + the RST-8 hook dispatch stay gated on real Trinity** (CLAUDE.md §5). Reference: `simonowen/trinload` |

Legend: ✅ done · ⏳ in progress · 📋 designed, not started

## Design notes not strictly inside a milestone

These are patterns or research findings that get applied *within* milestones rather than constituting their own milestone:

| Note | When to apply | Status |
|---|---|---|
| `docs/ARCHITECTURE.md` | **The first read**: the synthesized system overview (system shape, authority model, encoder tables, memory + paging, `.tbn` v2, build/test pipeline, dev inner loop). Each section links its deep spec. | ✅ living overview |
| `docs/specs/tbn-binary-format-reference.md` | The single authoritative reference for the complete `.tbn` binary encoding (header, records, operands, expr bytecode, directives, compaction levels). Read/cite this whenever touching the format; supersedes the retired M1 spec's tables. | ✅ living reference |
| `docs/specs/samdos-file-io.md` | Reference for the DOS file-I/O hook layer (SAMDOS 2 / B-DOS): the HLOAD trampoline pattern (READ), the HSAVE UIFA pattern (WRITE), hook register-clobbering facts, pre-built Z80 snippets. | ✅ living reference |
| `docs/notes/sam-stub-audit.md` | Already applied (PR #13 loader fix used findings). Keep as reference for any future DOS hook work. | ✅ applied; reference |
| `docs/notes/sam-paging.md` | Reference for any LMPR/HMPR work. | ✅ reference |

## How to extend this doc

When new deferred work or a design decision arises:
1. Add an `iN` row in `docs/notes/item-registry.md` (or `qN` in `docs/notes/question-registry.md` for open questions). That is the one home — do not add a checklist here.
2. Add the new design note to the appropriate table above (milestone, or "Design notes not strictly inside a milestone") and link it from `docs/specs/README.md` if it lives under `docs/specs/`.

The goal: no design discussion is ever lost to context drift. If it's worth writing down, it's worth a registry row.
