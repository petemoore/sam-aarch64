# `docs/plans/` — ephemeral implementation plans

Plans are execution artifacts: committed when execution starts, deleted by
the PR that completes the work (the completing PR's description links the
plan's final blob). An empty directory is the healthy state.
