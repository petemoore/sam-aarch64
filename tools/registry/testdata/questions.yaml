# Registry questions fixture — exercises the transient question model.
# Spec §"Questions — transient by design": questions have id, body, owner only.
# A question exists only while open; no answer field, no status enum.

# q1: gated by i2 above via depends_on; exercises inv 13 (delete-gate).
- id: q1
  body: "Should the registry CLI use gopkg.in/yaml.v3 or encoding/json for source files?"
  owner: pete
